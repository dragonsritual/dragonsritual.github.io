Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_getScopedTheme = require("./_chunks/getScopedTheme.cjs");
/** @internal */
function getContrastRatio(bg, fg) {
	let rgb1 = require_getScopedTheme.u(bg), rgb2 = require_getScopedTheme.u(fg), c1 = rgb_lrgb(rgb1), c2 = rgb_lrgb(rgb2), l1 = lrgb_luminance(c1), l2 = lrgb_luminance(c2);
	return (Math.max(l1, l2) + .05) / (Math.min(l1, l2) + .05);
}
function rgb_lrgb({ r, g, b }) {
	return [
		rgb_lrgb1(r / 255),
		rgb_lrgb1(g / 255),
		rgb_lrgb1(b / 255)
	];
}
function rgb_lrgb1(v) {
	return v <= .04045 ? v / 12.92 : ((v + .055) / 1.055) ** 2.4;
}
function lrgb_luminance([r, g, b]) {
	return .2126 * r + .7152 * g + .0722 * b;
}
/** @internal */
function parseTokenKey(str) {
	let segments = str.split("/"), segment0 = segments.shift() || "";
	if (require_getScopedTheme.y(segment0)) {
		let key = segments.join("/");
		if (require_getScopedTheme.v(key) || require_getScopedTheme.b(key)) return {
			type: "base",
			tone: segment0,
			key
		};
	}
	if (segment0 === "button") {
		let segment1 = segments.shift() || "";
		if (require_getScopedTheme.S(segment1)) {
			let segment2 = segments.shift() || "";
			if (require_getScopedTheme.I(segment2)) {
				let key = segments.join("/");
				if (require_getScopedTheme.x(key) || require_getScopedTheme.b(key)) return {
					type: "button",
					tone: segment1,
					mode: segment2,
					key
				};
			}
		}
	}
}
/** @internal */
function is_v0(themeProp) {
	return themeProp._version === 0;
}
exports.COLOR_CONFIG_AVATAR_COLORS = require_getScopedTheme.E, exports.COLOR_CONFIG_BLEND_KEYS = require_getScopedTheme.D, exports.COLOR_CONFIG_CARD_KEYS = require_getScopedTheme.O, exports.COLOR_CONFIG_CARD_TONES = require_getScopedTheme.k, exports.COLOR_CONFIG_INPUT_MODES = require_getScopedTheme.A, exports.COLOR_CONFIG_INPUT_STATES = require_getScopedTheme.j, exports.COLOR_CONFIG_STATES = require_getScopedTheme.M, exports.COLOR_CONFIG_STATE_KEYS = require_getScopedTheme.N, exports.COLOR_CONFIG_STATE_TONES = require_getScopedTheme.P, exports.THEME_COLOR_AVATAR_COLORS = require_getScopedTheme.z, exports.THEME_COLOR_BLEND_MODES = require_getScopedTheme.B, exports.THEME_COLOR_BUTTON_MODES = require_getScopedTheme.V, exports.THEME_COLOR_CARD_TONES = require_getScopedTheme.H, exports.THEME_COLOR_INPUT_MODES = require_getScopedTheme.U, exports.THEME_COLOR_INPUT_STATES = require_getScopedTheme.W, exports.THEME_COLOR_SCHEMES = require_getScopedTheme.G, exports.THEME_COLOR_STATES = require_getScopedTheme.K, exports.THEME_COLOR_STATE_TONES = require_getScopedTheme.q, exports.buildTheme = require_getScopedTheme.o, exports.createColorTheme = require_getScopedTheme.a, exports.getContrastRatio = getContrastRatio, exports.getScopedTheme = require_getScopedTheme.t, exports.getTheme_v2 = require_getScopedTheme.i, exports.hexToRgb = require_getScopedTheme.d, exports.hslToRgb = require_getScopedTheme.f, exports.isColorBlendModeValue = require_getScopedTheme.F, exports.isColorButtonMode = require_getScopedTheme.I, exports.isColorConfigBaseKey = require_getScopedTheme.v, exports.isColorConfigBaseTone = require_getScopedTheme.y, exports.isColorConfigBlendKey = require_getScopedTheme.b, exports.isColorConfigStateKey = require_getScopedTheme.x, exports.isColorConfigStateTone = require_getScopedTheme.S, exports.isColorHueKey = require_getScopedTheme.L, exports.isColorOpacityValue = require_getScopedTheme.C, exports.isColorTintKey = require_getScopedTheme.R, exports.isColorTokenValue = require_getScopedTheme.w, exports.isColorValue = require_getScopedTheme.T, exports.is_v0 = is_v0, exports.is_v2 = require_getScopedTheme.r, exports.mix = require_getScopedTheme.g, exports.multiply = require_getScopedTheme.c, exports.parseColor = require_getScopedTheme.u, exports.parseTokenKey = parseTokenKey, exports.parseTokenValue = require_getScopedTheme._, exports.rgbToHex = require_getScopedTheme.p, exports.rgbToHsl = require_getScopedTheme.m, exports.rgba = require_getScopedTheme.l, exports.rgbaToRGBA = require_getScopedTheme.h, exports.screen = require_getScopedTheme.s, exports.v0_v2 = require_getScopedTheme.n, exports.v2_v0 = require_getScopedTheme.J;

//# sourceMappingURL=theme.cjs.map