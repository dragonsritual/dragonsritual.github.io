import { $ as InterfaceOfType, A as ApplicationId, B as getDeployedConfigModuleRef, C as findDeployedConfigRef, D as parseInstalledApplication, E as InstalledApplication, F as brandApplicationId, G as brandCanvasId, H as Canvas, I as APP_TYPE_SLUG, J as AbstractApplicationType, K as parseCanvas, L as ActiveConfig, M as BrettWorkspace, N as ExternalApplication, O as ActiveDeployment, P as InternalApplication, Q as Interface, R as CONFIG_MODULE_EXPOSE, S as MediaLibraryApplication, T as InstallationId, U as CanvasApplication, V as RemoteModule, W as CanvasId, X as LocalApplication, Y as IconDescriptor, Z as DockGroup, _ as CoreApplication, a as Project, at as getInitials, b as LocalCoreApplicationManifest, c as brandProjectId, ct as OrganizationMember, d as InternalStudioApplication, et as InterfaceSchema, f as LocalStudioApplicationManifest, g as CoreApp, h as parseStudioApplication, i as StudioWorkspace, it as PanelComponent, j as BrettApplication, k as ApplicationBase, l as parseProject, lt as brandOrganizationId, m as Workspace, n as ApplicationList, nt as LocalInterfaceSchema, o as ProjectId, ot as Organization, p as Studio, q as AbstractApplication, r as StudioApplication, rt as Panel, s as ProjectMember, st as OrganizationId, t as Application, tt as LocalInterface, u as ExternalStudioApplication, ut as parseOrganization, v as ExternalCoreApplication, w as Installation, x as parseCoreApplication, y as InternalCoreApplication, z as LocalAppConfig } from "./_chunks-dts/application-list.js";
import { n as RemoteModuleRenderOptions, r as ResolvedConfig, t as Config } from "./_chunks-dts/config.js";
import { a as getSanityEnv, i as getSanityDomain, n as getApiHost, o as createCoreApplicationFromLocal, r as getApplicationDomain, s as createStudioApplicationFromLocal, t as isSanityClientError } from "./_chunks-dts/index.js";
/**
 * Log levels in order of verbosity (least to most)
 * - none: Silent
 * - error: Critical failures that prevent operation
 * - warn: Issues that may cause problems but don't stop execution
 * - info: High-level informational messages (default)
 * - debug: Detailed debugging information (maintainer level)
 * - trace: Very detailed tracing — sets `internal: true` on context
 * @public
 */
type LogLevel = "none" | "error" | "warn" | "info" | "debug";
/**
 * Namespaces organize logs by functional domain.
 * @internal
 */
type LogNamespace = string;
type LogContext = {
  [key: string]: unknown;
};
/**
 * @public
 */
interface Logger {
  error: (message: string, context?: LogContext) => void;
  warn: (message: string, context?: LogContext) => void;
  info: (message: string, context?: LogContext) => void;
  debug: (message: string, context?: LogContext) => void;
  child: (domain: string, context?: LogContext) => Logger;
}
interface LoggerOptions {
  namespace?: LogNamespace;
  context?: LogContext;
  logLevel?: LogLevel;
}
/**
 * Creates a leveled logger with an optional namespace prefix and bound
 * context. Calls below the configured `logLevel` are suppressed; `"none"`
 * silences the logger entirely.
 *
 * Use {@link Logger.child} to derive a sub-logger with an extended namespace
 * (e.g. `parent:domain`) that inherits the parent's level and merges its
 * bound context.
 *
 * @param options - Logger configuration.
 * @param options.namespace - Prepended to every message in `[brackets]`.
 * @param options.context - Bound context merged with per-call context.
 *   Per-call keys win on conflict.
 * @param options.logLevel - Maximum verbosity to emit. Default `"info"`.
 *
 * @example
 * ```ts
 * const log = createLogger({ namespace: "checkout", logLevel: "debug" });
 * log.info("placed", { orderId: "abc" });
 * // → [checkout] placed { orderId: "abc" }
 *
 * const auth = log.child("auth", { tenant: "acme" });
 * auth.warn("token expiring");
 * // → [checkout:auth] token expiring { tenant: "acme" }
 * ```
 *
 * @public
 */
declare function createLogger({ namespace, context: baseContext, logLevel }?: LoggerOptions): Logger;
/**
 * Shared workbench logger instance. Use this from both the workbench host
 * and its remotes so lifecycle and diagnostic logs appear under a single
 * namespace.
 *
 * @public
 */
declare const logger: Logger;
/**
 * Joins multiple path segments into a single path string.
 * Handles null, undefined, and URL objects gracefully.
 *
 * @public
 * @param paths - An array of path segments to join.
 * @returns A single joined path string.
 */
declare const joinUrlPaths: (...paths: Array<string | URL | null | undefined>) => string;
export { APP_TYPE_SLUG, AbstractApplication, AbstractApplicationType, ActiveConfig, ActiveDeployment, Application, ApplicationBase, ApplicationId, ApplicationList, BrettApplication, BrettWorkspace, CONFIG_MODULE_EXPOSE, Canvas, CanvasApplication, CanvasId, type Config, CoreApp, CoreApplication, type DockGroup, ExternalApplication, ExternalCoreApplication, ExternalStudioApplication, IconDescriptor, Installation, InstallationId, InstalledApplication, type Interface, type InterfaceOfType, InterfaceSchema, InternalApplication, InternalCoreApplication, InternalStudioApplication, LocalAppConfig, LocalApplication, LocalCoreApplicationManifest, type LocalInterface, LocalInterfaceSchema, LocalStudioApplicationManifest, LogLevel, LogNamespace, Logger, MediaLibraryApplication, Organization, OrganizationId, OrganizationMember, type Panel, type PanelComponent, Project, ProjectId, ProjectMember, RemoteModule, type RemoteModuleRenderOptions, type ResolvedConfig, Studio, StudioApplication, StudioWorkspace, Workspace, brandApplicationId, brandCanvasId, brandOrganizationId, brandProjectId, createCoreApplicationFromLocal, createLogger, createStudioApplicationFromLocal, findDeployedConfigRef, getApiHost, getApplicationDomain, getDeployedConfigModuleRef, getInitials, getSanityDomain, getSanityEnv, isSanityClientError, joinUrlPaths, logger, parseCanvas, parseCoreApplication, parseInstalledApplication, parseOrganization, parseProject, parseStudioApplication };