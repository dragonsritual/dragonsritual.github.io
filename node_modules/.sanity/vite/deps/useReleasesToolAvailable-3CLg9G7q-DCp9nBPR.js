import { t as require_compiler_runtime } from "./compiler-runtime-C3z9ieX8.js";
import { d as useWorkspace } from "./dist-a7ePbwyj.js";
import { t as SCHEDULES_TOOL_NAME } from "./plugin-B_ATFgKZ-N7SBus4g.js";
//#region node_modules/sanity/lib/useReleasesToolAvailable-3CLg9G7q.js
var import_compiler_runtime = require_compiler_runtime();
/**
* Determine whether the releases tool is available in the current workspace.
*
* This check is performed on the available tools, rather than the enabled status of the releases
* feature itself. This caters to scenarios in which releases are enabled for the workspace, but the
* release tool was later removed (e.g. using a workspace tool filter).
*
* @internal
*/
function useReleasesToolAvailable() {
	let $ = (0, import_compiler_runtime.c)(2), workspace = useWorkspace(), t0;
	return $[0] === workspace.tools ? t0 = $[1] : (t0 = workspace.tools.some(_temp), $[0] = workspace.tools, $[1] = t0), t0;
}
function _temp(t0) {
	let { name } = t0;
	return name === SCHEDULES_TOOL_NAME;
}
//#endregion
export { useReleasesToolAvailable as t };
