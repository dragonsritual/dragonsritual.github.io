import { a as __toESM, t as __commonJSMin } from "./rolldown-runtime-7_rZTKki.js";
import { t as require_react } from "./react.js";
import { t as require_react_dom } from "./react-dom.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { $ as PackageVersionInfoContext, A as DocumentLimitUpsellContext, B as FormFieldPresenceContext, Et as ScrollContext, F as EventsContext, Ft as StudioAnnouncementContext, H as FreeTrialContext, Ht as UserApplicationCacheContext, I as FeedbackContext, It as StudioErrorHandlerContext, J as LocaleContext, Jt as WorkspacesContext, K as IsLastPaneContext, Kt as VisibleWorkspacesContext, Q as NavbarContext, Ut as UserColorManagerContext, Xt as zIndexContextDefaults, Y as LoggedOutReasonContext, Yt as ZIndexContext, Zt as SANITY_VERSION, b as ConfigErrorContext, bt as ReviewChangesContext, d as ColorSchemeValueContext, g as CommentsIntentContext, q as LiveUserApplicationContext, t as ActiveWorkspaceMatcherContext, u as ColorSchemeSetValueContext } from "./_singletons-Bw6wdunb.js";
import { t as require_compiler_runtime } from "./compiler-runtime-C3z9ieX8.js";
import { $ as Text, B as Layer, Bt as COLOR_HUES, C as TextArea, Dt as ft, Et as St, F as Card, Ft as multiply, G as MenuItem, Gt as hues, H as Menu, Ht as blue, I as Flex, It as parseColor, J as PortalProvider, Jt as white, Kt as purple, L as Grid, Lt as rgbToHex, M as useToast, Mt as buildTheme, N as Box, Nt as createColorTheme, Ot as gt, P as Button, Pt as getTheme_v2, Rt as rgba$1, T as ToastProvider, Tt as Et, U as MenuDivider, Ut as gray, V as LayerProvider, Vt as black, Wt as green, X as Stack, Y as Spinner, Yt as yellow, _t as useRootTheme, a as Badge, c as Checkbox, ct as studioTheme, et as TextInput, ft as useGlobalKeyDown, g as Label, gt as usePrefersDark, h as Heading, l as Code, mt as useMediaIndex, nt as ThemeProvider$1, o as BoundaryElementProvider, ot as rem, q as Portal, qt as red, s as Breadcrumbs, u as Container, ut as useClickOutsideEvent, w as TextSkeleton, wt as require_react_is, x as Switch, z as Inline, zt as screen$1 } from "./dist-D-cKgnXq.js";
import { t as CloseIcon } from "./Close-B5YkwCNU.js";
import { t as useEffectEvent } from "./dist-hHjL7y3D.js";
import { t as ChevronRightIcon } from "./ChevronRight-BsLQz3JF.js";
import { t as ChevronDownIcon } from "./ChevronDown-Cg4Qb_Ns.js";
import { t as CheckmarkIcon } from "./Checkmark-58aI_E57.js";
import { i as pipe, n as Subject, r as Observable, t as BehaviorSubject } from "./BehaviorSubject-CtPYBbcz.js";
import { t as map } from "./map-DAZpcdQM.js";
import { $ as getReleaseIdFromReleaseDocumentId, $i as defer, $t as usePresenceStore, Ar as and, Bi as take, Br as applyPatch, Cr as forwardTo, Di as trimLeft, Er as assign, Et as memoizedPair, F as createRequestErrorChannel, Fr as stateIn, Gt as useComlinkStore, Hi as debounceTime, Ht as studioLocaleNamespace, Jr as diffInput, Jt as useDocumentPreviewStore, K as getApiErrorCode, Mi as switchMap, Mr as not, Ni as skip, Nr as or, O as createHookFromObservableFactory, Or as fromObservable, Pi as scan, Qi as fromEvent, Qt as useKeyValueStore, Ri as exhaustMap, Ti as toString, Tr as sendTo, Tt as memoizeKeyGen, U as feedbackLocaleNamespace, Ui as connect, Ut as supportsLocalStorage, Vr as incrementalPatcher, Wi as toArray, Xi as NEVER, Xt as useGrantsStore, Yr as wrap$1, Yt as useDocumentStore, Zi as merge$1, Zt as useHistoryStore, _i as QuickLRU, an as useTemplates, b as classifyConfigError, br as setup, c as ResourceCacheProvider, ct as isClientRequestError, di as createClient, dn as validateTemplates, dt as isNetworkError, ea as timeout, en as useProjectStore, gn as wrapValue, gt as isReleaseDocumentId, hn as variantsClient, i as RELEASES_STUDIO_CLIENT_OPTIONS, in as useStudioErrorHandler, it as getVariantTitle, ji as takeUntil, kr as fromPromise, li as ClientError, mt as isRecord$4, na as throwError, nt as getVariantId, or as startCase, p as VARIANTS_STUDIO_CLIENT_OPTIONS, q as getDocumentVariantType, qi as using, qt as useCurrentUser, rn as useSchema, rr as capitalize, s as RESOLVE_INITIAL_VALUE_TIMEOUT_MS, st as isBuilder, tn as useRenderingContextStore, ut as isInvalidSessionError, vi as fromString, vt as isTimeoutError, wi as startsWith, wr as sendParent, wt as memoize$1, xi as numEqualSegments, z as defaultLocale, zi as distinctUntilChanged, zt as resolveInitialValue } from "./datastores-BncX_uvZ-Cz4txWdG.js";
import { C as mergeMap, D as from, E as of, L as ReplaySubject, d as useWorkspace, g as share, h as shareReplay, i as useSyncObservable, l as useClient, m as startWith, o as DEFAULT_STUDIO_CLIENT_OPTIONS, p as tap, r as useObservablePromise, t as useObservable, u as useSource, v as catchError, y as timer } from "./dist-a7ePbwyj.js";
import { T as EMPTY, d as createBatchedStore, f as createSessionId, g as useTelemetry, h as TelemetryProvider, m as DeferredTelemetryProvider, p as defineEvent, y as combineLatest } from "./dist-CCMvtkH6.js";
import { n as firstValueFrom, t as filter } from "./filter-CVvmgpYw.js";
import { A as isHotkey, C as isStaging, D as useSearchState, S as hasSearchableTerms, T as useRenderingContext, a as RecentSearches, c as SearchProvider, d as SearchResults, f as SearchWrapper, i as GLOBAL_SEARCH_KEY_MODIFIER, k as require_server_browser, o as SearchHeader, p as StatusButton, r as Filters, s as SearchPopover, t as AppIdCacheProvider, v as getSanityImportMapUrl, w as useProjectOrganizationId, x as hasSanityPackageInImportMap } from "./SearchProvider-DZy7i4QW-DhSBnoMm.js";
import { $a as useChangeIndicatorsReportedValues, $i as isPublishDocumentVersionEvent, $s as isAgentBundleName, $t as MemberItemError, Ai as isCardinalityOneRelease, Bs as _extends, Ca as releasesLocaleNamespace, Cr as defaultResolveInlineBlockComponent, Dn as ReleaseTitle, Do as useGetI18nText, En as ReleaseAvatarIcon, Fi as isDraftPerspective, Fo as useLoadable, Ga as useActiveWorkspace, Go as useReleaseOperations, Ha as updatePublishedEvents, Hs as orderBy, Ht as LATEST, Io as useMiddlewareComponents, Is as getDevicePixelRatio, Ka as useAllReleases, Ki as isNativeEditableElement, Ko as useReleasesStore, Kr as getDocumentAtRevision, La as squashLiveEditEvents, Li as isEditDocumentVersionEvent, Ls as es2015_default, Mn as ScrollContainer, Na as sortEvents, Ni as isDeleteDocumentGroupEvent, Nr as focusRingStyle$1, P as ColorSchemeProvider, Pi as isDeleteDocumentVersionEvent, Qi as isPausedCardinalityOneRelease, Qr as getEffectState, Ra as supportsTouch, Ri as isEmptyFocusPath, Rs as _inheritsLoose, Sa as readVersionType, Sr as defaultResolveFieldComponent, Ta as remoteMutationToTransaction, Tn as ReleaseAvatar, Tr as defaultResolveItemComponent, Tt as FileInputButton, Ua as updateVersionEvents, Ur as getCreatableVariantTarget, V as DEFAULT_RELEASE_TYPE, Vi as isFileTargetElement, Vt as InsufficientPermissionsMessage, Wa as useActiveReleases, Wo as useReferringDocuments, Xr as getDocumentVersionInfoFromVersions, Xs as require_react_fast_compare, Yr as getDocumentTransactions, Ys as clsx, Zo as useRovingFocus, Zr as getEditEvents, ao as useColorSchemeValue, bo as useDocumentVersionsObservable, br as defaultResolveAnnotationComponent, c as ArrayOfObjectsItem, ci as getReleaseDefaults, cn as ObserveElement, cs as useVariantsStore, ea as isPublishedPerspective, ei as getInitialFetchEvents, eo as useChangeIndicatorsReporter, er as UserAvatar, fi as getTargetDocument, fs as useZIndex, ho as useDocumentPairPermissions, hr as createReleaseId, ia as isReleaseScheduledOrScheduling, io as useColorSchemeSetValue, j as CollapseOverflowMenu, ji as isCreateDocumentVersionEvent, jn as SanityDefaultPreview, jo as useInitialValueResolverContext, ko as useI18nText, kr as fileTarget, la as isString$1, li as getReleaseDocumentIdFromReleaseId, lr as addParentToEvents, ls as useVersionRelease, m as AssetLimitUpsellProvider, na as isReleaseDocument, nc as iif, oo as useCopyPaste, qi as isNotArchivedRelease, qt as Media$1, ra as isReleaseLimitError, ro as useColorSchemeOptions, ui as getReleaseTone, un as PREVIEW_SIZES, ut as EMPTY_ARRAY$12, vn as Preview, vs as addWeeks, w as ChangeIndicatorsTracker, wr as defaultResolveInputComponent, xr as defaultResolveBlockComponent, yi as hasSelection, yo as useDocumentVersions, zo as usePerspective, zs as _setPrototypeOf } from "./useBundleDocuments-DpiMoPsr-CYBYABmS.js";
import { $ as readUnclaimedProjectRecord, At as writeUnclaimedProjectRecord, B as getDocumentValuePermissions, C as createDefaultIcon, F as getAuthTokenStorageKey, Ft as isAfter, G as hasDeployStudioGrant, I as getBundleIdFromPerspective, L as getCollectedConfigWarnings, Mt as escapeRegExp, N as fetchCanDeployStudio, Nt as require_with_selector, Pt as useHotModuleReload, Q as prepareConfig, R as getCookieAuthStateKey, S as createBroadcastState, Y as isDefaultRouteTool, ct as useConditionalToast, d as RouterHistoryProvider, et as readUnclaimedProjectSnoozedAt, g as WorkspaceLoaderBoundary, h as UNAUTHENTICATED, jt as writeUnclaimedProjectSnoozedAt, n as AUTHENTICATED, o as ConfigResolutionError, p as SchemaError, r as AUTH_CLIENT_OPTIONS, rt as storePreferredLocale, st as useCanvasCompanionDocsStore, u as PerspectiveProvider, v as clearUnclaimedProjectRecord, y as clearUnclaimedProjectSnooze } from "./WorkspaceLoader-DDy7F0Cs-CWTgyxhb.js";
import { d as useScheduledDraftMenuActions, i as Z_OFFSET, p as isBefore } from "./useScheduledDraftMenuActions-Dj-ZeVgl-CY6LSyao.js";
import { a as MenuItem$1, c as TooltipDelayGroupProvider$1, i as MenuButton$1, n as ErrorBoundary$1, s as Tooltip$1, t as Button$1$1 } from "./MenuItem-BWdL61CI-CCm2gi9d.js";
import { o as I18nContext, r as useTranslation$1 } from "./useTranslation-Cqs6Itwv-DgR2QpHX.js";
import { i as Popover$1, n as Dialog$1, r as MenuGroup$1 } from "./ConfirmPopover-CD1rDyuk-BprYXu6g.js";
import { C as isVersionId, S as isPublishedId, _ as getDraftId, b as getVersionId, s as isDocumentGroupId, v as getPublishedId, y as getVersionFromId } from "./draftUtils-D3HJGQaW-CpQZ8djC.js";
import { t as WarningOutlineIcon } from "./WarningOutline-CT08yzBL.js";
import { F as isKeyedObject, T as isDeprecatedSchemaType } from "./lib-BsvRcu2m.js";
import { P as isObject } from "./_baseIsEqual-B6KAWdgP.js";
import { L as memoize, c as pick, n as v4, r as require_browser } from "./partition-BKS1U6Z3.js";
import { Lt as useActorRef, Rt as useSelector, Vt as LoadingBlock } from "./esm-th5zsqnu.js";
import "./useFieldActions-BB2jFp9D-B1W-k6C9.js";
import { n as intlCache, r as useListFormat, t as Translate } from "./Translate-B8ORyU6k-DCjWWnpI.js";
import { a as useLocale, i as useDateTimeFormat, n as TextWithTone, o as useRelativeTime, r as useCurrentLocale, t as ContextMenuButton } from "./ContextMenuButton-CruB3rT2-DM9Tu9Z0.js";
import "./useFeatureEnabled-CKhvJ68U-DEOrP5Aj.js";
import { n as interpolateTemplate, t as UpsellDescriptionSerializer } from "./UpsellDescriptionSerializer-0J_sJnIT-DdoN9jS8.js";
import { t as LaunchIcon } from "./Launch-B4CWnDTI.js";
import { c as useUpsellContext, o as getDialogPropsFromContext, t as UpsellDialog } from "./UpsellDialog-D5JRZZeK-BSzIM2kk.js";
import { t as CalendarIcon } from "./Calendar-C26bhemB.js";
import { t as CopyIcon } from "./Copy-BEyFFRRW.js";
import { t as DocumentsIcon } from "./Documents-D0NyRpYf.js";
import { t as UnknownIcon } from "./Unknown-CimHLv5A.js";
import { t as InfoOutlineIcon } from "./InfoOutline-BKEhd6gP.js";
import { d as useRouter, f as useRouterState, p as useStateLink, r as RouteScope, t as IntentLink, u as useIntentLink } from "./router-r2bwUxf-.js";
import { t as LockIcon } from "./Lock-Bma6OCpI.js";
import { t as ErrorOutlineIcon } from "./ErrorOutline-D70KcRwu.js";
import { t as SyncIcon } from "./Sync-B6lCdSC7.js";
import { t as SearchIcon } from "./Search-YIDBTdGw.js";
import { t as ArrowLeftIcon } from "./ArrowLeft-H3unXjsS.js";
import { n as AnimatePresence, t as motion } from "./react-BSkHV9L1.js";
import { t as ChevronUpIcon } from "./ChevronUp-Dj34NbDt.js";
import { t as AddIcon } from "./Add-CgmyEV0_.js";
import { t as TrashIcon } from "./Trash-BJjmY_oX.js";
import { t as ChevronLeftIcon } from "./ChevronLeft-C36lLYaR.js";
import { t as BoltIcon } from "./Bolt-DcfFJRCv.js";
import { t as ClockIcon } from "./Clock-B8zC_Idr.js";
import { t as EyeOpenIcon } from "./EyeOpen-D0ZRPtOR.js";
import { t as HelpCircleIcon } from "./HelpCircle-acwVeIz8.js";
import { t as UploadIcon } from "./Upload-BlqQRws7.js";
import { t as BinaryDocumentIcon } from "./BinaryDocument-6ZcJRkIu.js";
import { t as CommandList } from "./CommandList-B7noXKqO-BISj7arG.js";
import "./useComments-D_nSQUGP-cp0GsTx0.js";
import "./useAddonDataset-BvCgByG6-DQy0kOqy.js";
import "./useCommentsEnabled-BPc3788v-C2oeLp3E.js";
import { a as DiscardVersionDialog, b as useVersionOperations, r as CreatedRelease, y as useSetPerspective } from "./UnpublishVersionDialog-ChXRl8Dq-BQbw0-WG.js";
import "./plugin-BEgXaH7N-CY7ZK-_W.js";
import { n as useScheduledPublishingEnabled } from "./ScheduledPublishingEnabledProvider-BcodZUBC-BH8XmEqr.js";
import "./SingleDocReleaseUpsellProvider-BL5BLfi7-B8pDBl98.js";
import { a as useSingleDocRelease, i as useClearScheduledDraftPerspectiveOnDelete, t as RELEASES_SCHEDULED_DRAFTS_INTENT } from "./plugin-vYuG7KUJ-BmdVvpsc.js";
import "./scheduleItem-m3XCzHtb-DaHzFKcH.js";
import { t as ComposeSparklesIcon } from "./ComposeSparkles-B1HOe2BW.js";
import "./CommentsListItem-C1qIKAOP-DgbvpkQT.js";
import "./buildTextSelectionFromFragment-CdiHClqK-Cbl2HRRd.js";
import "./useCommentsTelemetry-DX0iow3p-DYnhvRuw.js";
import "./CommentsSelectedPathProvider-LsaJLnuf-BJgNYVJZ.js";
import "./useCommentsSelectedPath-D5HXM3NF-C75QN3wU.js";
import { a as useProjectSubscriptions, i as useEnvAwareSanityWebsiteUrl, r as getIsScheduledDateInPast } from "./useStudioUrl-D9fzi8zJ-ZllAWUeP.js";
import "./RelativeTime-eow0hl8e-6reU21Ft.js";
import { a as ReleaseForm, c as getIsReleaseInvalid, d as useGuardWithReleaseLimitUpsell, f as useReleaseFormStorage, n as CreateVariantIcon, o as RhombusIcon, p as useReleasePermissions, u as useCreateReleaseMetadata } from "./navigation.telemetry-7YuNWCYN-D9v4pwXE.js";
import { t as useReleasesToolAvailable } from "./useReleasesToolAvailable-3CLg9G7q-DCp9nBPR.js";
import { i as ToolLink, l as useFormatRelativeLocalePublishDate, n as CreateReleaseMenuItem, o as useAgentBundles, r as ReleasesNav, s as useAgentBundlesStore, u as useSetVariant } from "./ReleasesNav-zudHaNfG-BdBTIpu4.js";
import "./schedules-fJmox8fE-BHkbDXoh.js";
import "./AddonDatasetProvider-Cm5sEq31-CdPdB6Y7.js";
import { t as UpsellPanel } from "./UpsellPanel-dmz9-L2H-9H0FdbnE.js";
import "./useDocumentPreviewValues-DYKMH1oy-m6nVwvBr.js";
import { t as FeedbackIcon } from "./Feedback-CsGg1A3t.js";
import { t as FaceHappyIcon } from "./FaceHappy-BljSRyIf.js";
import { t as FaceIndifferentIcon } from "./FaceIndifferent-DRRA2aMh.js";
import { t as FaceSadIcon } from "./FaceSad-6USWFZt0.js";
import { t as UnlockIcon } from "./Unlock-CRf2U6DF.js";
import { t as MenuIcon } from "./Menu-SgGdRBc6.js";
import { t as DoubleChevronRightIcon } from "./DoubleChevronRight-oUYQsCSS.js";
import { t as LeaveIcon } from "./Leave-DNJO_wGX.js";
import { t as AddUserIcon } from "./AddUser-APDnF4jI.js";
import { t as CogIcon } from "./Cog-Bjzb08qC.js";
import { t as UsersIcon } from "./Users-BLmAudp7.js";
import { t as GithubIcon } from "./Github-E6Af17Oc.js";
import { t as RefreshIcon } from "./Refresh-DzDJClBo.js";
import "C:/DEV/PROJECTS/WEBSITE/DragonsRitual/dragonsritual.github.io/node_modules/sanity/lib/bundle.css";
import "C:/DEV/PROJECTS/WEBSITE/DragonsRitual/dragonsritual.github.io/node_modules/@sanity-labs/ui-poc/dist/styles.css";
//#region node_modules/react-i18next/dist/es/I18nextProvider.js
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
function I18nextProvider({ i18n, defaultNS, children }) {
	const value = (0, import_react.useMemo)(() => ({
		i18n,
		defaultNS
	}), [i18n, defaultNS]);
	return (0, import_react.createElement)(I18nContext.Provider, { value }, children);
}
//#endregion
//#region node_modules/@sanity/generate-help-url/dist/index.js
var import_jsx_runtime = require_jsx_runtime();
var import_compiler_runtime = require_compiler_runtime();
var import_react_is = /* @__PURE__ */ __toESM(require_react_is(), 1);
var import_react_fast_compare = /* @__PURE__ */ __toESM(require_react_fast_compare(), 1);
var BASE_URL = "https://www.sanity.io/docs/help/";
function generateHelpUrl(slug) {
	return `${BASE_URL}${slug}`;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/debug-build.js
var DEBUG_BUILD$3 = typeof __SENTRY_DEBUG__ === "undefined" || __SENTRY_DEBUG__;
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/worldwide.js
var GLOBAL_OBJ = globalThis;
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/version.js
var SDK_VERSION = "10.69.0";
//#endregion
//#region node_modules/@sentry/core/build/esm/carrier.js
function getMainCarrier() {
	getSentryCarrier(GLOBAL_OBJ);
	return GLOBAL_OBJ;
}
function getSentryCarrier(carrier) {
	const __SENTRY__ = carrier.__SENTRY__ = carrier.__SENTRY__ || {};
	__SENTRY__.version = __SENTRY__.version || "10.69.0";
	return __SENTRY__[SDK_VERSION] = __SENTRY__["10.69.0"] || {};
}
function getGlobalSingleton(name, creator, obj = GLOBAL_OBJ) {
	const __SENTRY__ = obj.__SENTRY__ = obj.__SENTRY__ || {};
	const carrier = __SENTRY__[SDK_VERSION] = __SENTRY__["10.69.0"] || {};
	return carrier[name] || (carrier[name] = creator());
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/debug-logger.js
var CONSOLE_LEVELS$1 = [
	"debug",
	"info",
	"warn",
	"error",
	"log",
	"assert",
	"trace"
];
var PREFIX$1 = "Sentry Logger ";
var originalConsoleMethods = {};
function consoleSandbox(callback) {
	if (!("console" in GLOBAL_OBJ)) return callback();
	const console = GLOBAL_OBJ.console;
	const wrappedFuncs = {};
	const wrappedLevels = Object.keys(originalConsoleMethods);
	wrappedLevels.forEach((level) => {
		const originalConsoleMethod = originalConsoleMethods[level];
		wrappedFuncs[level] = console[level];
		console[level] = originalConsoleMethod;
	});
	try {
		return callback();
	} finally {
		wrappedLevels.forEach((level) => {
			console[level] = wrappedFuncs[level];
		});
	}
}
function enable() {
	_getLoggerSettings().enabled = true;
}
function disable() {
	_getLoggerSettings().enabled = false;
}
function isEnabled() {
	return _getLoggerSettings().enabled;
}
function log(...args) {
	_maybeLog("log", ...args);
}
function warn(...args) {
	_maybeLog("warn", ...args);
}
function error(...args) {
	_maybeLog("error", ...args);
}
function _maybeLog(level, ...args) {
	if (!DEBUG_BUILD$3) return;
	if (isEnabled()) consoleSandbox(() => {
		GLOBAL_OBJ.console[level](`${PREFIX$1}[${level}]:`, ...args);
	});
}
function _getLoggerSettings() {
	if (!DEBUG_BUILD$3) return { enabled: false };
	return getGlobalSingleton("loggerSettings", () => ({ enabled: false }));
}
var debug$4 = {
	/** Enable logging. */
	enable,
	/** Disable logging. */
	disable,
	/** Check if logging is enabled. */
	isEnabled,
	/** Log a message. */
	log,
	/** Log a warning. */
	warn,
	/** Log an error. */
	error
};
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/stacktrace.js
var STACKTRACE_FRAME_LIMIT = 50;
var WEBPACK_ERROR_REGEXP = /\(error: (.*)\)/;
var STRIP_FRAME_REGEXP = /captureMessage|captureException/;
function createStackParser(...parsers) {
	const sortedParsers = parsers.sort((a, b) => a[0] - b[0]).map((p) => p[1]);
	return (stack, skipFirstLines = 0, framesToPop = 0) => {
		const frames = [];
		const lines = stack.split("\n");
		for (let i = skipFirstLines; i < lines.length; i++) {
			let line = lines[i];
			if (line.length > 1024) line = line.slice(0, 1024);
			const cleanedLine = WEBPACK_ERROR_REGEXP.test(line) ? line.replace(WEBPACK_ERROR_REGEXP, "$1") : line;
			if (cleanedLine.includes("Error: ")) continue;
			for (const parser of sortedParsers) {
				const frame = parser(cleanedLine);
				if (frame) {
					frames.push(frame);
					break;
				}
			}
			if (frames.length >= STACKTRACE_FRAME_LIMIT + framesToPop) break;
		}
		return stripSentryFramesAndReverse(frames.slice(framesToPop));
	};
}
function stackParserFromStackParserOptions(stackParser) {
	if (Array.isArray(stackParser)) return createStackParser(...stackParser);
	return stackParser;
}
function stripSentryFramesAndReverse(stack) {
	if (!stack.length) return [];
	const localStack = Array.from(stack);
	if (/sentryWrapped/.test(getLastStackFrame(localStack).function || "")) localStack.pop();
	localStack.reverse();
	if (STRIP_FRAME_REGEXP.test(getLastStackFrame(localStack).function || "")) {
		localStack.pop();
		if (STRIP_FRAME_REGEXP.test(getLastStackFrame(localStack).function || "")) localStack.pop();
	}
	return localStack.slice(0, STACKTRACE_FRAME_LIMIT).map((frame) => ({
		...frame,
		filename: frame.filename || getLastStackFrame(localStack).filename,
		function: frame.function || "?"
	}));
}
function getLastStackFrame(arr) {
	return arr[arr.length - 1] || {};
}
var defaultFunctionName = "<anonymous>";
function getFunctionName(fn) {
	try {
		if (!fn || typeof fn !== "function") return defaultFunctionName;
		return fn.name || defaultFunctionName;
	} catch {
		return defaultFunctionName;
	}
}
function getFramesFromEvent(event) {
	const exception = event.exception;
	if (exception) {
		const frames = [];
		try {
			exception.values.forEach((value) => {
				if (value.stacktrace.frames) frames.push(...value.stacktrace.frames);
			});
			return frames;
		} catch {
			return;
		}
	}
}
//#endregion
//#region node_modules/@sentry/core/build/esm/instrument/handlers.js
var handlers = {};
var instrumented = {};
function addHandler(type, handler) {
	handlers[type] = handlers[type] || [];
	handlers[type].push(handler);
	return () => {
		const typeHandlers = handlers[type];
		if (typeHandlers) {
			const index = typeHandlers.indexOf(handler);
			if (index !== -1) typeHandlers.splice(index, 1);
		}
	};
}
function maybeInstrument(type, instrumentFn) {
	if (!instrumented[type]) {
		instrumented[type] = true;
		try {
			instrumentFn();
		} catch (e) {
			DEBUG_BUILD$3 && debug$4.error(`Error while instrumenting ${type}`, e);
		}
	}
}
function triggerHandlers(type, data) {
	const typeHandlers = type && handlers[type];
	if (!typeHandlers) return;
	for (const handler of typeHandlers) try {
		handler(data);
	} catch (e) {
		DEBUG_BUILD$3 && debug$4.error(`Error while triggering instrumentation handler.
Type: ${type}
Name: ${getFunctionName(handler)}
Error:`, e);
	}
}
//#endregion
//#region node_modules/@sentry/core/build/esm/instrument/globalError.js
var _oldOnErrorHandler = null;
function addGlobalErrorInstrumentationHandler(handler) {
	const type = "error";
	addHandler(type, handler);
	maybeInstrument(type, instrumentError);
}
function instrumentError() {
	_oldOnErrorHandler = GLOBAL_OBJ.onerror;
	GLOBAL_OBJ.onerror = function(msg, url, line, column, error) {
		triggerHandlers("error", {
			column,
			error,
			line,
			msg,
			url
		});
		if (_oldOnErrorHandler) return _oldOnErrorHandler.apply(this, arguments);
		return false;
	};
	GLOBAL_OBJ.onerror.__SENTRY_INSTRUMENTED__ = true;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/instrument/globalUnhandledRejection.js
var _oldOnUnhandledRejectionHandler = null;
function addGlobalUnhandledRejectionInstrumentationHandler(handler) {
	const type = "unhandledrejection";
	addHandler(type, handler);
	maybeInstrument(type, instrumentUnhandledRejection);
}
function instrumentUnhandledRejection() {
	_oldOnUnhandledRejectionHandler = GLOBAL_OBJ.onunhandledrejection;
	GLOBAL_OBJ.onunhandledrejection = function(e) {
		triggerHandlers("unhandledrejection", e);
		if (_oldOnUnhandledRejectionHandler) return _oldOnUnhandledRejectionHandler.apply(this, arguments);
		return true;
	};
	GLOBAL_OBJ.onunhandledrejection.__SENTRY_INSTRUMENTED__ = true;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/is.js
var objectToString$1 = Object.prototype.toString;
function isError$1(wat) {
	switch (objectToString$1.call(wat)) {
		case "[object Error]":
		case "[object Exception]":
		case "[object DOMException]":
		case "[object WebAssembly.Exception]": return true;
		default: return isInstanceOf$1(wat, Error);
	}
}
function isBuiltin(wat, className) {
	return objectToString$1.call(wat) === `[object ${className}]`;
}
function isErrorEvent$1(wat) {
	return isBuiltin(wat, "ErrorEvent");
}
function isDOMError(wat) {
	return isBuiltin(wat, "DOMError");
}
function isDOMException(wat) {
	return isBuiltin(wat, "DOMException");
}
function isString(wat) {
	return isBuiltin(wat, "String");
}
function isParameterizedString(wat) {
	return typeof wat === "object" && wat !== null && "__sentry_template_string__" in wat && "__sentry_template_values__" in wat;
}
function isPrimitive(wat) {
	return wat === null || isParameterizedString(wat) || typeof wat !== "object" && typeof wat !== "function";
}
function isPlainObject(wat) {
	return isBuiltin(wat, "Object");
}
function isObjectLike(wat) {
	return typeof wat === "object" && wat !== null;
}
function isEvent(wat) {
	return typeof Event !== "undefined" && isInstanceOf$1(wat, Event);
}
function isRegExp(wat) {
	return isBuiltin(wat, "RegExp");
}
function isThenable(wat) {
	return Boolean(wat?.then && typeof wat.then === "function");
}
function isInstanceOf$1(wat, base) {
	try {
		return wat instanceof base;
	} catch {
		return false;
	}
}
function isRequest(request) {
	return typeof Request !== "undefined" && isInstanceOf$1(request, Request);
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/object.js
function fill(source, name, replacementFactory) {
	if (!(name in source)) return;
	const original = source[name];
	if (typeof original !== "function") return;
	const wrapped = replacementFactory(original);
	if (typeof wrapped === "function") markFunctionWrapped(wrapped, original);
	try {
		source[name] = wrapped;
	} catch {
		DEBUG_BUILD$3 && debug$4.log(`Failed to replace method "${name}" in object`, source);
	}
}
function addNonEnumerableProperty(obj, name, value) {
	try {
		Object.defineProperty(obj, name, {
			value,
			writable: true,
			configurable: true
		});
	} catch {
		DEBUG_BUILD$3 && debug$4.log(`Failed to add non-enumerable property "${String(name)}" to object`, obj);
	}
}
function markFunctionWrapped(wrapped, original) {
	try {
		wrapped.prototype = original.prototype = original.prototype || {};
		addNonEnumerableProperty(wrapped, "__sentry_original__", original);
	} catch {}
}
function getOriginalFunction(func) {
	return func.__sentry_original__;
}
function convertToPlainObject(value) {
	if (isError$1(value)) return {
		message: value.message,
		name: value.name,
		stack: value.stack,
		...getOwnProperties(value)
	};
	if (isEvent(value)) {
		const { type, target, currentTarget, detail } = value;
		return {
			type,
			target,
			currentTarget,
			...detail ? { detail } : {},
			...getOwnProperties(value)
		};
	}
	return value;
}
function getOwnProperties(obj) {
	if (isObjectLike(obj)) return Object.fromEntries(Object.entries(obj));
	return {};
}
function extractExceptionKeysForMessage(exception) {
	const keys = Object.keys(convertToPlainObject(exception));
	keys.sort();
	return !keys[0] ? "[object has no keys]" : keys.join(", ");
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/randomSafeContext.js
var RESOLVED_RUNNER;
function withRandomSafeContext(cb) {
	if (RESOLVED_RUNNER !== void 0) return RESOLVED_RUNNER ? RESOLVED_RUNNER(cb) : cb();
	const sym = /* @__PURE__ */ Symbol.for("__SENTRY_SAFE_RANDOM_ID_WRAPPER__");
	const globalWithSymbol = GLOBAL_OBJ;
	if (sym in globalWithSymbol && typeof globalWithSymbol[sym] === "function") {
		RESOLVED_RUNNER = globalWithSymbol[sym];
		return RESOLVED_RUNNER(cb);
	}
	RESOLVED_RUNNER = null;
	return cb();
}
function safeMathRandom() {
	return withRandomSafeContext(() => Math.random());
}
function safeDateNow() {
	return withRandomSafeContext(() => Date.now());
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/normalizationHints.js
var SENTRY_SKIP_NORMALIZATION = /* @__PURE__ */ Symbol.for("sentry.skipNormalization");
var SENTRY_OVERRIDE_NORMALIZATION_DEPTH = /* @__PURE__ */ Symbol.for("sentry.overrideNormalizationDepth");
function hasSkipNormalizationHint(value) {
	return Boolean(value[SENTRY_SKIP_NORMALIZATION]);
}
function getNormalizationDepthOverrideHint(value) {
	const v = value[SENTRY_OVERRIDE_NORMALIZATION_DEPTH];
	return typeof v === "number" ? v : void 0;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/normalize.js
var stringifier;
function setNormalizeStringifier(newStringifier) {
	stringifier = newStringifier;
}
function normalize(input, depth = 100, maxProperties = Infinity) {
	try {
		return visit("", input, depth, maxProperties);
	} catch (err) {
		return { ERROR: `**non-serializable** (${err})` };
	}
}
function normalizeToSize(object, depth = 3, maxSize = 102400) {
	const normalized = normalize(object, depth);
	if (jsonSize(normalized) > maxSize) return normalizeToSize(object, depth - 1, maxSize);
	return normalized;
}
function visit(key, value, depth = Infinity, maxProperties = Infinity, memo = memoBuilder()) {
	const [memoize, unmemoize] = memo;
	if (value == null || ["boolean", "string"].includes(typeof value) || typeof value === "number" && Number.isFinite(value)) return value;
	const stringified = stringifyValue(key, value);
	if (!stringified.startsWith("[object ")) return stringified;
	if (hasSkipNormalizationHint(value)) return value;
	const overrideDepth = getNormalizationDepthOverrideHint(value);
	const remainingDepth = overrideDepth !== void 0 ? overrideDepth : depth;
	if (remainingDepth === 0) return stringified.replace("object ", "");
	if (memoize(value)) return "[Circular ~]";
	const valueWithToJSON = value;
	if (valueWithToJSON && typeof valueWithToJSON.toJSON === "function") try {
		return visit("", valueWithToJSON.toJSON(), remainingDepth - 1, maxProperties, memo);
	} catch {}
	const normalized = Array.isArray(value) ? [] : {};
	let numAdded = 0;
	const visitable = convertToPlainObject(value);
	for (const visitKey in visitable) {
		if (!Object.prototype.hasOwnProperty.call(visitable, visitKey)) continue;
		if (numAdded >= maxProperties) {
			normalized[visitKey] = "[MaxProperties ~]";
			break;
		}
		const visitValue = visitable[visitKey];
		normalized[visitKey] = visit(visitKey, visitValue, remainingDepth - 1, maxProperties, memo);
		numAdded++;
	}
	unmemoize(value);
	return normalized;
}
function stringifyValue(key, value) {
	try {
		if (stringifier) {
			const stringified = stringifier(value);
			if (stringified) return stringified;
		}
		if (typeof global !== "undefined" && value === global) return "[Global]";
		if (typeof value === "number" && !Number.isFinite(value)) return `[${value}]`;
		if (typeof value === "function") return `[Function: ${getFunctionName(value)}]`;
		if (typeof value === "symbol") return `[${String(value)}]`;
		if (typeof value === "bigint") return `[BigInt: ${String(value)}]`;
		return `[object ${getConstructorName$1(value)}]`;
	} catch (err) {
		return `**non-serializable** (${err})`;
	}
}
function getConstructorName$1(value) {
	const prototype = Object.getPrototypeOf(value);
	return prototype?.constructor ? prototype.constructor.name : "null prototype";
}
function utf8Length(value) {
	return ~-encodeURI(value).split(/%..|./).length;
}
function jsonSize(value) {
	return utf8Length(JSON.stringify(value));
}
function memoBuilder() {
	const inner = /* @__PURE__ */ new WeakSet();
	function memoize(obj) {
		if (inner.has(obj)) return true;
		inner.add(obj);
		return false;
	}
	function unmemoize(obj) {
		inner.delete(obj);
	}
	return [memoize, unmemoize];
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/string.js
function truncate(str, max = 0) {
	if (typeof str !== "string" || max === 0) return str;
	return str.length <= max ? str : `${str.slice(0, max)}...`;
}
function safeJoin(input, delimiter) {
	if (!Array.isArray(input)) return "";
	const output = [];
	for (let i = 0; i < input.length; i++) {
		const value = input[i];
		if (isPrimitive(value)) output.push(String(value));
		else if (value instanceof Error) output.push(value.message ? `${value.name}: ${value.message}` : value.name);
		else output.push(stringifyValue(void 0, value));
	}
	return output.join(delimiter);
}
function isMatchingPattern(value, pattern, requireExactStringMatch = false) {
	if (!isString(value)) return false;
	if (isRegExp(pattern)) return pattern.test(value);
	if (isString(pattern)) return requireExactStringMatch ? value === pattern : value.includes(pattern);
	if (typeof pattern === "function") return pattern(value);
	return false;
}
function stringMatchesSomePattern(testString, patterns = [], requireExactStringMatch = false) {
	for (const pattern of patterns) if (isMatchingPattern(testString, pattern, requireExactStringMatch)) return true;
	return false;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/misc.js
function getCrypto() {
	const gbl = GLOBAL_OBJ;
	return gbl.crypto || gbl.msCrypto;
}
var emptyUuid;
function getRandomByte() {
	return safeMathRandom() * 16;
}
function uuid4(crypto = getCrypto()) {
	try {
		if (crypto?.randomUUID) return withRandomSafeContext(() => crypto.randomUUID()).replace(/-/g, "");
	} catch {}
	if (!emptyUuid) emptyUuid = "10000000100040008000100000000000";
	return emptyUuid.replace(/[018]/g, (c) => (c ^ (getRandomByte() & 15) >> c / 4).toString(16));
}
function getFirstException(event) {
	return event.exception?.values?.[0];
}
function getEventDescription(event) {
	const { message, event_id: eventId } = event;
	if (message) return message;
	const firstException = getFirstException(event);
	if (firstException) {
		if (firstException.type && firstException.value) return `${firstException.type}: ${firstException.value}`;
		return firstException.type || firstException.value || eventId || "<unknown>";
	}
	return eventId || "<unknown>";
}
function addExceptionTypeValue(event, value, type) {
	const exception = event.exception = event.exception || {};
	const values = exception.values = exception.values || [];
	const firstException = values[0] = values[0] || {};
	if (!firstException.value) firstException.value = value || "";
	if (!firstException.type) firstException.type = type || "Error";
}
function addExceptionMechanism(event, newMechanism) {
	const firstException = getFirstException(event);
	if (!firstException) return;
	const defaultMechanism = {
		type: "generic",
		handled: true
	};
	const currentMechanism = firstException.mechanism;
	firstException.mechanism = {
		...defaultMechanism,
		...currentMechanism,
		...newMechanism
	};
	if (newMechanism && "data" in newMechanism) {
		const mergedData = {
			...currentMechanism?.data,
			...newMechanism.data
		};
		firstException.mechanism.data = mergedData;
	}
}
function checkOrSetAlreadyCaught(exception) {
	if (isAlreadyCaptured(exception)) return true;
	try {
		addNonEnumerableProperty(exception, "__sentry_captured__", true);
	} catch {}
	return false;
}
function isAlreadyCaptured(exception) {
	try {
		return exception.__sentry_captured__;
	} catch {}
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/time.js
var ONE_SECOND_IN_MS = 1e3;
function dateTimestampInSeconds() {
	return safeDateNow() / ONE_SECOND_IN_MS;
}
function createUnixTimestampInSecondsFunc() {
	const { performance } = GLOBAL_OBJ;
	if (!performance?.now || !performance.timeOrigin) return dateTimestampInSeconds;
	const timeOrigin = performance.timeOrigin;
	return () => {
		return (timeOrigin + withRandomSafeContext(() => performance.now())) / ONE_SECOND_IN_MS;
	};
}
var _cachedTimestampInSeconds;
function timestampInSeconds() {
	return (_cachedTimestampInSeconds ?? (_cachedTimestampInSeconds = createUnixTimestampInSecondsFunc()))();
}
//#endregion
//#region node_modules/@sentry/core/build/esm/session.js
function makeSession(context) {
	const startingTime = timestampInSeconds();
	const session = {
		sid: uuid4(),
		init: true,
		timestamp: startingTime,
		started: startingTime,
		duration: 0,
		status: "ok",
		errors: 0,
		ignoreDuration: false,
		toJSON: () => sessionToJSON(session)
	};
	if (context) updateSession(session, context);
	return session;
}
function updateSession(session, context = {}) {
	if (context.user) {
		if (!session.ipAddress && context.user.ip_address) session.ipAddress = context.user.ip_address;
		if (!session.did && !context.did) session.did = context.user.id || context.user.email || context.user.username;
	}
	session.timestamp = context.timestamp || timestampInSeconds();
	if (context.abnormal_mechanism) session.abnormal_mechanism = context.abnormal_mechanism;
	if (context.ignoreDuration) session.ignoreDuration = context.ignoreDuration;
	if (context.sid) session.sid = context.sid.length === 32 ? context.sid : uuid4();
	if (context.init !== void 0) session.init = context.init;
	if (!session.did && context.did) session.did = `${context.did}`;
	if (typeof context.started === "number") session.started = context.started;
	if (session.ignoreDuration) session.duration = void 0;
	else if (typeof context.duration === "number") session.duration = context.duration;
	else {
		const duration = session.timestamp - session.started;
		session.duration = duration >= 0 ? duration : 0;
	}
	if (context.release) session.release = context.release;
	if (context.environment) session.environment = context.environment;
	if (!session.ipAddress && context.ipAddress) session.ipAddress = context.ipAddress;
	if (!session.userAgent && context.userAgent) session.userAgent = context.userAgent;
	if (typeof context.errors === "number") session.errors = context.errors;
	if (context.status) session.status = context.status;
}
function closeSession(session, status) {
	let context = {};
	if (status) context = { status };
	else if (session.status === "ok") context = { status: "exited" };
	updateSession(session, context);
}
function sessionToJSON(session) {
	return {
		sid: `${session.sid}`,
		init: session.init,
		started: (/* @__PURE__ */ new Date(session.started * 1e3)).toISOString(),
		timestamp: (/* @__PURE__ */ new Date(session.timestamp * 1e3)).toISOString(),
		status: session.status,
		errors: session.errors,
		did: typeof session.did === "number" || typeof session.did === "string" ? `${session.did}` : void 0,
		duration: session.duration,
		abnormal_mechanism: session.abnormal_mechanism,
		attrs: {
			release: session.release,
			environment: session.environment,
			ip_address: session.ipAddress,
			user_agent: session.userAgent
		}
	};
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/merge.js
function merge(initialObj, mergeObj, levels = 2) {
	if (!mergeObj || typeof mergeObj !== "object" || levels <= 0) return mergeObj;
	if (initialObj && Object.keys(mergeObj).length === 0) return initialObj;
	const output = { ...initialObj };
	for (const key in mergeObj) if (Object.prototype.hasOwnProperty.call(mergeObj, key)) output[key] = merge(output[key], mergeObj[key], levels - 1);
	return output;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/propagationContext.js
function generateTraceId() {
	return uuid4();
}
function generateSpanId() {
	return uuid4().substring(16);
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/weakRef.js
function makeWeakRef(value) {
	try {
		const WeakRefImpl = GLOBAL_OBJ.WeakRef;
		if (typeof WeakRefImpl === "function") return new WeakRefImpl(value);
	} catch {}
	return value;
}
function derefWeakRef(ref) {
	if (!ref) return;
	if (typeof ref === "object" && "deref" in ref && typeof ref.deref === "function") try {
		return ref.deref();
	} catch {
		return;
	}
	return ref;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/spanOnScope.js
var SCOPE_SPAN_FIELD = "_sentrySpan";
function _setSpanForScope(scope, span) {
	if (span) addNonEnumerableProperty(scope, SCOPE_SPAN_FIELD, makeWeakRef(span));
	else delete scope[SCOPE_SPAN_FIELD];
}
function _getSpanForScope(scope) {
	return derefWeakRef(scope[SCOPE_SPAN_FIELD]);
}
//#endregion
//#region node_modules/@sentry/core/build/esm/scope.js
var DEFAULT_MAX_BREADCRUMBS = 100;
var Scope = class Scope {
	constructor() {
		this._notifyingListeners = false;
		this._scopeListeners = [];
		this._eventProcessors = [];
		this._breadcrumbs = [];
		this._attachments = [];
		this._user = {};
		this._tags = {};
		this._attributes = {};
		this._extra = {};
		this._contexts = {};
		this._sdkProcessingMetadata = {};
		this._propagationContext = {
			traceId: generateTraceId(),
			sampleRand: safeMathRandom()
		};
	}
	/**
	* Clone all data from this scope into a new scope.
	*/
	clone() {
		const newScope = new Scope();
		newScope._breadcrumbs = [...this._breadcrumbs];
		newScope._tags = { ...this._tags };
		newScope._attributes = { ...this._attributes };
		newScope._extra = { ...this._extra };
		newScope._contexts = { ...this._contexts };
		if (this._contexts.flags) newScope._contexts.flags = { values: [...this._contexts.flags.values] };
		newScope._user = this._user;
		newScope._level = this._level;
		newScope._session = this._session;
		newScope._transactionName = this._transactionName;
		newScope._fingerprint = this._fingerprint;
		newScope._eventProcessors = [...this._eventProcessors];
		newScope._attachments = [...this._attachments];
		newScope._sdkProcessingMetadata = { ...this._sdkProcessingMetadata };
		newScope._propagationContext = { ...this._propagationContext };
		newScope._client = this._client;
		newScope._lastEventId = this._lastEventId;
		newScope._conversationId = this._conversationId;
		_setSpanForScope(newScope, _getSpanForScope(this));
		return newScope;
	}
	/**
	* Update the client assigned to this scope.
	* Note that not every scope will have a client assigned - isolation scopes & the global scope will generally not have a client,
	* as well as manually created scopes.
	*/
	setClient(client) {
		this._client = client;
	}
	/**
	* Set the ID of the last captured error event.
	* This is generally only captured on the isolation scope.
	*/
	setLastEventId(lastEventId) {
		this._lastEventId = lastEventId;
	}
	/**
	* Get the client assigned to this scope.
	*/
	getClient() {
		return this._client;
	}
	/**
	* Get the ID of the last captured error event.
	* This is generally only available on the isolation scope.
	*/
	lastEventId() {
		return this._lastEventId;
	}
	/**
	* @inheritDoc
	*/
	addScopeListener(callback) {
		this._scopeListeners.push(callback);
	}
	/**
	* Add an event processor that will be called before an event is sent.
	*/
	addEventProcessor(callback) {
		this._eventProcessors.push(callback);
		return this;
	}
	/**
	* Set the user for this scope.
	* Set to `null` to unset the user.
	*/
	setUser(user) {
		this._user = user || {
			email: void 0,
			id: void 0,
			ip_address: void 0,
			username: void 0
		};
		if (this._session) updateSession(this._session, { user });
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Get the user from this scope.
	*/
	getUser() {
		return this._user;
	}
	/**
	* Set the conversation ID for this scope.
	* Set to `null` to unset the conversation ID.
	*/
	setConversationId(conversationId) {
		this._conversationId = conversationId || void 0;
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Set an object that will be merged into existing tags on the scope,
	* and will be sent as tags data with the event.
	*/
	setTags(tags) {
		this._tags = {
			...this._tags,
			...tags
		};
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Set a single tag that will be sent as tags data with the event.
	*/
	setTag(key, value) {
		return this.setTags({ [key]: value });
	}
	/**
	* Sets attributes onto the scope.
	*
	* These attributes are applied to logs, metrics and streamed spans.
	*
	* Supported attribute value types are `string`, `number`, `boolean`, `string[]`, `number[]` and `boolean[]`.
	*
	* @param newAttributes - The attributes to set on the scope, as key-value pairs.
	*
	* @example
	* ```typescript
	* scope.setAttributes({
	*   is_admin: true,
	*   payment_selection: 'credit_card',
	*   render_duration: 150,
	* });
	* ```
	*/
	setAttributes(newAttributes) {
		this._attributes = {
			...this._attributes,
			...newAttributes
		};
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Sets an attribute onto the scope.
	*
	* These attributes are applied to logs, metrics and streamed spans.
	*
	* Supported attribute value types are `string`, `number`, `boolean`, `string[]`, `number[]` and `boolean[]`.
	*
	* @param key - The attribute key.
	* @param value - The attribute value.
	*
	* @example
	* ```typescript
	* scope.setAttribute('is_admin', true);
	* scope.setAttribute('render_duration', 150);
	* ```
	*/
	setAttribute(key, value) {
		return this.setAttributes({ [key]: value });
	}
	/**
	* Removes the attribute with the given key from the scope.
	*
	* @param key - The attribute key.
	*
	* @example
	* ```typescript
	* scope.removeAttribute('is_admin');
	* ```
	*/
	removeAttribute(key) {
		if (key in this._attributes) {
			delete this._attributes[key];
			this._notifyScopeListeners();
		}
		return this;
	}
	/**
	* Set an object that will be merged into existing extra on the scope,
	* and will be sent as extra data with the event.
	*/
	setExtras(extras) {
		this._extra = {
			...this._extra,
			...extras
		};
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Set a single key:value extra entry that will be sent as extra data with the event.
	*/
	setExtra(key, extra) {
		this._extra = {
			...this._extra,
			[key]: extra
		};
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Sets the fingerprint on the scope to send with the events.
	* @param {string[]} fingerprint Fingerprint to group events in Sentry.
	*/
	setFingerprint(fingerprint) {
		this._fingerprint = fingerprint;
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Sets the level on the scope for future events.
	*/
	setLevel(level) {
		this._level = level;
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Sets the transaction name on the scope so that the name of e.g. taken server route or
	* the page location is attached to future events.
	*
	* IMPORTANT: Calling this function does NOT change the name of the currently active
	* root span. If you want to change the name of the active root span, use
	* `Sentry.updateSpanName(rootSpan, 'new name')` instead.
	*
	* By default, the SDK updates the scope's transaction name automatically on sensible
	* occasions, such as a page navigation or when handling a new request on the server.
	*/
	setTransactionName(name) {
		this._transactionName = name;
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Sets context data with the given name.
	* Data passed as context will be normalized. You can also pass `null` to unset the context.
	* Note that context data will not be merged - calling `setContext` will overwrite an existing context with the same key.
	*/
	setContext(key, context) {
		if (context === null) delete this._contexts[key];
		else this._contexts[key] = context;
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Set the session for the scope.
	*/
	setSession(session) {
		if (!session) delete this._session;
		else this._session = session;
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Get the session from the scope.
	*/
	getSession() {
		return this._session;
	}
	/**
	* Updates the scope with provided data. Can work in three variations:
	* - plain object containing updatable attributes
	* - Scope instance that'll extract the attributes from
	* - callback function that'll receive the current scope as an argument and allow for modifications
	*/
	update(captureContext) {
		if (!captureContext) return this;
		const scopeToMerge = typeof captureContext === "function" ? captureContext(this) : captureContext;
		const { tags, attributes, extra, user, contexts, level, fingerprint = [], propagationContext, conversationId } = (scopeToMerge instanceof Scope ? scopeToMerge.getScopeData() : isPlainObject(scopeToMerge) ? captureContext : void 0) || {};
		this._tags = {
			...this._tags,
			...tags
		};
		this._attributes = {
			...this._attributes,
			...attributes
		};
		this._extra = {
			...this._extra,
			...extra
		};
		this._contexts = {
			...this._contexts,
			...contexts
		};
		if (user && Object.keys(user).length) this._user = user;
		if (level) this._level = level;
		if (fingerprint.length) this._fingerprint = fingerprint;
		if (propagationContext) this._propagationContext = propagationContext;
		if (conversationId) this._conversationId = conversationId;
		return this;
	}
	/**
	* Clears the current scope and resets its properties.
	* Note: The client will not be cleared.
	*/
	clear() {
		this._breadcrumbs = [];
		this._tags = {};
		this._attributes = {};
		this._extra = {};
		this._user = {};
		this._contexts = {};
		this._level = void 0;
		this._transactionName = void 0;
		this._fingerprint = void 0;
		this._session = void 0;
		this._conversationId = void 0;
		_setSpanForScope(this, void 0);
		this._attachments = [];
		this.setPropagationContext({
			traceId: generateTraceId(),
			sampleRand: safeMathRandom()
		});
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Adds a breadcrumb to the scope.
	* By default, the last 100 breadcrumbs are kept.
	*/
	addBreadcrumb(breadcrumb, maxBreadcrumbs) {
		const maxCrumbs = typeof maxBreadcrumbs === "number" ? maxBreadcrumbs : DEFAULT_MAX_BREADCRUMBS;
		if (maxCrumbs <= 0) return this;
		const mergedBreadcrumb = {
			timestamp: dateTimestampInSeconds(),
			...breadcrumb,
			message: breadcrumb.message ? truncate(breadcrumb.message, 2048) : breadcrumb.message
		};
		this._breadcrumbs.push(mergedBreadcrumb);
		if (this._breadcrumbs.length > maxCrumbs) {
			this._breadcrumbs = this._breadcrumbs.slice(-maxCrumbs);
			this._client?.recordDroppedEvent("buffer_overflow", "log_item");
		}
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Get the last breadcrumb of the scope.
	*/
	getLastBreadcrumb() {
		return this._breadcrumbs[this._breadcrumbs.length - 1];
	}
	/**
	* Clear all breadcrumbs from the scope.
	*/
	clearBreadcrumbs() {
		this._breadcrumbs = [];
		this._notifyScopeListeners();
		return this;
	}
	/**
	* Add an attachment to the scope.
	*/
	addAttachment(attachment) {
		this._attachments.push(attachment);
		return this;
	}
	/**
	* Clear all attachments from the scope.
	*/
	clearAttachments() {
		this._attachments = [];
		return this;
	}
	/**
	* Get the data of this scope, which should be applied to an event during processing.
	*/
	getScopeData() {
		return {
			breadcrumbs: this._breadcrumbs,
			attachments: this._attachments,
			contexts: this._contexts,
			tags: this._tags,
			attributes: this._attributes,
			extra: this._extra,
			user: this._user,
			level: this._level,
			fingerprint: this._fingerprint || [],
			eventProcessors: this._eventProcessors,
			propagationContext: this._propagationContext,
			sdkProcessingMetadata: this._sdkProcessingMetadata,
			transactionName: this._transactionName,
			span: _getSpanForScope(this),
			conversationId: this._conversationId
		};
	}
	/**
	* Add data which will be accessible during event processing but won't get sent to Sentry.
	*/
	setSDKProcessingMetadata(newData) {
		this._sdkProcessingMetadata = merge(this._sdkProcessingMetadata, newData, 2);
		return this;
	}
	/**
	* Add propagation context to the scope, used for distributed tracing
	*/
	setPropagationContext(context) {
		this._propagationContext = context;
		return this;
	}
	/**
	* Get propagation context from the scope, used for distributed tracing
	*/
	getPropagationContext() {
		return this._propagationContext;
	}
	/**
	* Capture an exception for this scope.
	*
	* @returns {string} The id of the captured Sentry event.
	*/
	captureException(exception, hint) {
		const eventId = hint?.event_id || uuid4();
		if (!this._client) {
			DEBUG_BUILD$3 && debug$4.warn("No client configured on scope - will not capture exception!");
			return eventId;
		}
		const syntheticException = /* @__PURE__ */ new Error("Sentry syntheticException");
		this._client.captureException(exception, {
			originalException: exception,
			syntheticException,
			...hint,
			event_id: eventId
		}, this);
		return eventId;
	}
	/**
	* Capture a message for this scope.
	*
	* @returns {string} The id of the captured message.
	*/
	captureMessage(message, level, hint) {
		const eventId = hint?.event_id || uuid4();
		if (!this._client) {
			DEBUG_BUILD$3 && debug$4.warn("No client configured on scope - will not capture message!");
			return eventId;
		}
		const syntheticException = hint?.syntheticException ?? new Error(message);
		this._client.captureMessage(message, level, {
			originalException: message,
			syntheticException,
			...hint,
			event_id: eventId
		}, this);
		return eventId;
	}
	/**
	* Capture a Sentry event for this scope.
	*
	* @returns {string} The id of the captured event.
	*/
	captureEvent(event, hint) {
		const eventId = event.event_id || hint?.event_id || uuid4();
		if (!this._client) {
			DEBUG_BUILD$3 && debug$4.warn("No client configured on scope - will not capture event!");
			return eventId;
		}
		this._client.captureEvent(event, {
			...hint,
			event_id: eventId
		}, this);
		return eventId;
	}
	/**
	* This will be called on every set call.
	*/
	_notifyScopeListeners() {
		if (!this._notifyingListeners) {
			this._notifyingListeners = true;
			this._scopeListeners.forEach((callback) => {
				callback(this);
			});
			this._notifyingListeners = false;
		}
	}
};
//#endregion
//#region node_modules/@sentry/core/build/esm/defaultScopes.js
function getDefaultCurrentScope() {
	return getGlobalSingleton("defaultCurrentScope", () => new Scope());
}
function getDefaultIsolationScope() {
	return getGlobalSingleton("defaultIsolationScope", () => new Scope());
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/chain-and-copy-promiselike.js
var isActualPromise = (p) => p instanceof Promise && !p[kChainedCopy];
var kChainedCopy = /* @__PURE__ */ Symbol("chained PromiseLike");
var chainAndCopyPromiseLike = (original, onSuccess, onError) => {
	const chained = original.then((value) => {
		onSuccess(value);
		return value;
	}, (err) => {
		onError(err);
		throw err;
	});
	return isActualPromise(chained) && isActualPromise(original) ? chained : copyProps(original, chained);
};
var copyProps = (original, chained) => {
	if (!chained) return original;
	let mutated = false;
	for (const key in original) {
		if (key in chained) continue;
		mutated = true;
		const value = original[key];
		if (typeof value === "function") Object.defineProperty(chained, key, {
			value: (...args) => value.apply(original, args),
			enumerable: true,
			configurable: true,
			writable: true
		});
		else chained[key] = value;
	}
	if (mutated) Object.assign(chained, { [kChainedCopy]: true });
	return chained;
};
//#endregion
//#region node_modules/@sentry/core/build/esm/asyncContext/stackStrategy.js
var AsyncContextStack = class {
	constructor(scope, isolationScope) {
		let assignedScope;
		if (!scope) assignedScope = new Scope();
		else assignedScope = scope;
		let assignedIsolationScope;
		if (!isolationScope) assignedIsolationScope = new Scope();
		else assignedIsolationScope = isolationScope;
		this._stack = [{ scope: assignedScope }];
		this._isolationScope = assignedIsolationScope;
	}
	/**
	* Fork a scope for the stack.
	*/
	withScope(callback) {
		const scope = this._pushScope();
		let maybePromiseResult;
		try {
			maybePromiseResult = callback(scope);
		} catch (e) {
			this._popScope();
			throw e;
		}
		if (isThenable(maybePromiseResult)) return chainAndCopyPromiseLike(maybePromiseResult, () => this._popScope(), () => this._popScope());
		this._popScope();
		return maybePromiseResult;
	}
	/**
	* Get the client of the stack.
	*/
	getClient() {
		return this.getStackTop().client;
	}
	/**
	* Returns the scope of the top stack.
	*/
	getScope() {
		return this.getStackTop().scope;
	}
	/**
	* Get the isolation scope for the stack.
	*/
	getIsolationScope() {
		return this._isolationScope;
	}
	/**
	* Returns the topmost scope layer in the order domain > local > process.
	*/
	getStackTop() {
		return this._stack[this._stack.length - 1];
	}
	/**
	* Push a scope to the stack.
	*/
	_pushScope() {
		const scope = this.getScope().clone();
		this._stack.push({
			client: this.getClient(),
			scope
		});
		return scope;
	}
	/**
	* Pop a scope from the stack.
	*/
	_popScope() {
		if (this._stack.length <= 1) return false;
		return !!this._stack.pop();
	}
};
function getAsyncContextStack() {
	const sentry = getSentryCarrier(getMainCarrier());
	return sentry.stack = sentry.stack || new AsyncContextStack(getDefaultCurrentScope(), getDefaultIsolationScope());
}
function withScope$1(callback) {
	return getAsyncContextStack().withScope(callback);
}
function withSetScope(scope, callback) {
	const stack = getAsyncContextStack();
	return stack.withScope(() => {
		stack.getStackTop().scope = scope;
		return callback(scope);
	});
}
function withIsolationScope(callback) {
	return getAsyncContextStack().withScope(() => {
		return callback(getAsyncContextStack().getIsolationScope());
	});
}
function getStackAsyncContextStrategy() {
	return {
		withIsolationScope,
		withScope: withScope$1,
		withSetScope,
		withSetIsolationScope: (_isolationScope, callback) => {
			return withIsolationScope(callback);
		},
		getCurrentScope: () => getAsyncContextStack().getScope(),
		getIsolationScope: () => getAsyncContextStack().getIsolationScope()
	};
}
//#endregion
//#region node_modules/@sentry/core/build/esm/asyncContext/index.js
function getAsyncContextStrategy(carrier) {
	const sentry = getSentryCarrier(carrier);
	if (sentry.acs) return sentry.acs;
	return getStackAsyncContextStrategy();
}
//#endregion
//#region node_modules/@sentry/core/build/esm/attributes.js
function isAttributeObject(maybeObj) {
	return typeof maybeObj === "object" && maybeObj != null && !Array.isArray(maybeObj) && Object.keys(maybeObj).includes("value");
}
function attributeValueToTypedAttributeValue(rawValue, useFallback) {
	const { value, unit } = isAttributeObject(rawValue) ? rawValue : {
		value: rawValue,
		unit: void 0
	};
	const attributeValue = getTypedAttributeValue(value);
	const checkedUnit = unit && typeof unit === "string" ? { unit } : {};
	if (attributeValue) return {
		...attributeValue,
		...checkedUnit
	};
	if (!useFallback || useFallback === "skip-undefined" && value === void 0) return;
	let stringValue = "";
	try {
		stringValue = JSON.stringify(value) ?? "";
	} catch {}
	return {
		value: stringValue,
		type: "string",
		...checkedUnit
	};
}
function serializeAttributes(attributes, fallback = false) {
	const serializedAttributes = {};
	for (const [key, value] of Object.entries(attributes ?? {})) {
		const typedValue = attributeValueToTypedAttributeValue(value, fallback);
		if (typedValue) serializedAttributes[key] = typedValue;
	}
	return serializedAttributes;
}
function getTypedAttributeValue(value) {
	if (Array.isArray(value)) return {
		value,
		type: "array"
	};
	const primitiveType = typeof value === "string" ? "string" : typeof value === "boolean" ? "boolean" : typeof value === "number" && !Number.isNaN(value) ? Number.isInteger(value) ? "integer" : "double" : null;
	if (primitiveType) return {
		value,
		type: primitiveType
	};
}
//#endregion
//#region node_modules/@sentry/core/build/esm/currentScopes.js
var _externalPropagationContextProvider;
function getExternalPropagationContext() {
	return _externalPropagationContextProvider?.();
}
function getCurrentScope() {
	return getAsyncContextStrategy(getMainCarrier()).getCurrentScope();
}
function getIsolationScope() {
	return getAsyncContextStrategy(getMainCarrier()).getIsolationScope();
}
function getGlobalScope$1() {
	return getGlobalSingleton("globalScope", () => new Scope());
}
function withScope(...rest) {
	const acs = getAsyncContextStrategy(getMainCarrier());
	if (rest.length === 2) {
		const [scope, callback] = rest;
		if (!scope) return acs.withScope(callback);
		return acs.withSetScope(scope, callback);
	}
	return acs.withScope(rest[0]);
}
function getClient() {
	return getCurrentScope().getClient();
}
function getTraceContextFromScope(scope) {
	const externalContext = getExternalPropagationContext();
	if (externalContext) return {
		trace_id: externalContext.traceId,
		span_id: externalContext.spanId
	};
	const { traceId, parentSpanId, propagationSpanId } = scope.getPropagationContext();
	const traceContext = {
		trace_id: traceId,
		span_id: propagationSpanId || generateSpanId()
	};
	if (parentSpanId) traceContext.parent_span_id = parentSpanId;
	return traceContext;
}
var SEMANTIC_ATTRIBUTE_SENTRY_OP = "sentry.op";
var SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN = "sentry.origin";
var SEMANTIC_ATTRIBUTE_PROFILE_ID = "sentry.profile_id";
var SEMANTIC_ATTRIBUTE_EXCLUSIVE_TIME = "sentry.exclusive_time";
var GEN_AI_CONVERSATION_ID_ATTRIBUTE = "gen_ai.conversation.id";
//#endregion
//#region node_modules/@sentry/core/build/esm/tracing/utils.js
var SCOPE_ON_START_SPAN_FIELD = "_sentryScope";
var ISOLATION_SCOPE_ON_START_SPAN_FIELD = "_sentryIsolationScope";
function getCapturedScopesOnSpan(span) {
	const spanWithScopes = span;
	return {
		scope: spanWithScopes[SCOPE_ON_START_SPAN_FIELD],
		isolationScope: derefWeakRef(spanWithScopes[ISOLATION_SCOPE_ON_START_SPAN_FIELD])
	};
}
function baggageHeaderToDynamicSamplingContext(baggageHeader) {
	const baggageObject = parseBaggageHeader(baggageHeader);
	if (!baggageObject) return;
	const dynamicSamplingContext = Object.entries(baggageObject).reduce((acc, [key, value]) => {
		if (key.startsWith("sentry-")) {
			const nonPrefixedKey = key.slice(7);
			acc[nonPrefixedKey] = value;
		}
		return acc;
	}, {});
	if (Object.keys(dynamicSamplingContext).length > 0) return dynamicSamplingContext;
	else return;
}
function parseBaggageHeader(baggageHeader) {
	if (!baggageHeader || !isString(baggageHeader) && !Array.isArray(baggageHeader)) return;
	if (Array.isArray(baggageHeader)) return baggageHeader.reduce((acc, curr) => {
		const currBaggageObject = baggageHeaderToObject(curr);
		Object.entries(currBaggageObject).forEach(([key, value]) => {
			acc[key] = value;
		});
		return acc;
	}, {});
	return baggageHeaderToObject(baggageHeader);
}
function baggageHeaderToObject(baggageHeader) {
	return baggageHeader.split(",").map((baggageEntry) => {
		const eqIdx = baggageEntry.indexOf("=");
		if (eqIdx === -1) return [];
		return [baggageEntry.slice(0, eqIdx), baggageEntry.slice(eqIdx + 1)].map((keyOrValue) => {
			try {
				return decodeURIComponent(keyOrValue.trim());
			} catch {
				return;
			}
		});
	}).reduce((acc, [key, value]) => {
		if (key && value) acc[key] = value;
		return acc;
	}, {});
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/dsn.js
var ORG_ID_REGEX = /^o(\d+)\./;
var DSN_REGEX = /^(?:(\w+):)\/\/(?:(\w+)(?::(\w+)?)?@)((?:\[[:.%\w]+\]|[\w.-]+))(?::(\d+))?\/(.+)/;
function isValidProtocol(protocol) {
	return protocol === "http" || protocol === "https";
}
function dsnToString(dsn, withPassword = false) {
	const { host, path, pass, port, projectId, protocol, publicKey } = dsn;
	return `${protocol}://${publicKey}${withPassword && pass ? `:${pass}` : ""}@${host}${port ? `:${port}` : ""}/${path ? `${path}/` : path}${projectId}`;
}
function dsnFromString(str) {
	const match = DSN_REGEX.exec(str);
	if (!match) {
		consoleSandbox(() => {
			console.error(`Invalid Sentry Dsn: ${str}`);
		});
		return;
	}
	const [protocol, publicKey, pass = "", host = "", port = "", lastPath = ""] = match.slice(1);
	let path = "";
	let projectId = lastPath;
	const split = projectId.split("/");
	if (split.length > 1) {
		path = split.slice(0, -1).join("/");
		projectId = split.pop();
	}
	if (projectId) {
		const projectMatch = projectId.match(/^\d+/);
		if (projectMatch) projectId = projectMatch[0];
	}
	return dsnFromComponents({
		host,
		pass,
		path,
		projectId,
		port,
		protocol,
		publicKey
	});
}
function dsnFromComponents(components) {
	return {
		protocol: components.protocol,
		publicKey: components.publicKey || "",
		pass: components.pass || "",
		host: components.host,
		port: components.port || "",
		path: components.path || "",
		projectId: components.projectId
	};
}
function validateDsn(dsn) {
	if (!DEBUG_BUILD$3) return true;
	const { port, projectId, protocol } = dsn;
	if ([
		"protocol",
		"publicKey",
		"host",
		"projectId"
	].find((component) => {
		if (!dsn[component]) {
			debug$4.error(`Invalid Sentry Dsn: ${component} missing`);
			return true;
		}
		return false;
	})) return false;
	if (!projectId.match(/^\d+$/)) {
		debug$4.error(`Invalid Sentry Dsn: Invalid projectId ${projectId}`);
		return false;
	}
	if (!isValidProtocol(protocol)) {
		debug$4.error(`Invalid Sentry Dsn: Invalid protocol ${protocol}`);
		return false;
	}
	if (port && isNaN(parseInt(port, 10))) {
		debug$4.error(`Invalid Sentry Dsn: Invalid port ${port}`);
		return false;
	}
	return true;
}
function extractOrgIdFromDsnHost(host) {
	return host.match(ORG_ID_REGEX)?.[1];
}
function extractOrgIdFromClient(client) {
	const options = client.getOptions();
	const { host } = client.getDsn() || {};
	let org_id;
	if (options.orgId) org_id = String(options.orgId);
	else if (host) org_id = extractOrgIdFromDsnHost(host);
	return org_id;
}
function makeDsn(from) {
	const components = typeof from === "string" ? dsnFromString(from) : dsnFromComponents(from);
	if (!components || !validateDsn(components)) return;
	return components;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/parseSampleRate.js
function parseSampleRate(sampleRate) {
	if (typeof sampleRate === "boolean") return Number(sampleRate);
	const rate = typeof sampleRate === "string" ? parseFloat(sampleRate) : sampleRate;
	if (typeof rate !== "number" || isNaN(rate) || rate < 0 || rate > 1) return;
	return rate;
}
var hasShownSpanDropWarning = false;
function spanToTraceContext(span) {
	const { spanId, traceId: trace_id, isRemote } = span.spanContext();
	const parent_span_id = isRemote ? spanId : spanToJSON(span).parent_span_id;
	const scope = getCapturedScopesOnSpan(span).scope;
	return {
		parent_span_id,
		span_id: isRemote ? scope?.getPropagationContext().propagationSpanId || generateSpanId() : spanId,
		trace_id
	};
}
function convertSpanLinksForEnvelope(links) {
	if (links && links.length > 0) return links.map(({ context: { spanId, traceId, traceFlags, ...restContext }, attributes }) => ({
		span_id: spanId,
		trace_id: traceId,
		sampled: traceFlags === 1,
		attributes,
		...restContext
	}));
	else return;
}
function spanTimeInputToSeconds(input) {
	if (typeof input === "number") return ensureTimestampInSeconds(input);
	if (Array.isArray(input)) return input[0] + input[1] / 1e9;
	if (input instanceof Date) return ensureTimestampInSeconds(input.getTime());
	return timestampInSeconds();
}
function ensureTimestampInSeconds(timestamp) {
	return timestamp > 9999999999 ? timestamp / 1e3 : timestamp;
}
function spanToJSON(span) {
	if (spanIsSentrySpan(span)) return span.getSpanJSON();
	const { spanId: span_id, traceId: trace_id } = span.spanContext();
	if (spanIsOpenTelemetrySdkTraceBaseSpan(span)) {
		const { attributes, startTime, name, endTime, status, links } = span;
		return {
			span_id,
			trace_id,
			data: attributes,
			description: name,
			parent_span_id: getOtelParentSpanId(span),
			start_timestamp: spanTimeInputToSeconds(startTime),
			timestamp: spanTimeInputToSeconds(endTime) || void 0,
			status: getStatusMessage(status),
			op: attributes[SEMANTIC_ATTRIBUTE_SENTRY_OP],
			origin: attributes[SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN],
			links: convertSpanLinksForEnvelope(links)
		};
	}
	return {
		span_id,
		trace_id,
		start_timestamp: 0,
		data: {}
	};
}
function getOtelParentSpanId(span) {
	return "parentSpanId" in span ? span.parentSpanId : "parentSpanContext" in span ? span.parentSpanContext?.spanId : void 0;
}
function streamedSpanJsonToSerializedSpan(spanJson) {
	return {
		...spanJson,
		attributes: serializeAttributes(spanJson.attributes),
		links: spanJson.links?.map((link) => ({
			...link,
			attributes: serializeAttributes(link.attributes)
		}))
	};
}
function spanIsOpenTelemetrySdkTraceBaseSpan(span) {
	const castSpan = span;
	return !!castSpan.attributes && !!castSpan.startTime && !!castSpan.name && !!castSpan.endTime && !!castSpan.status;
}
function spanIsSentrySpan(span) {
	return typeof span.getSpanJSON === "function";
}
function spanIsSampled(span) {
	const { traceFlags } = span.spanContext();
	return traceFlags === 1;
}
function getStatusMessage(status) {
	if (!status || status.code === 0) return;
	if (status.code === 1) return "ok";
	return status.message || "internal_error";
}
var ROOT_SPAN_FIELD = "_sentryRootSpan";
var getRootSpan = INTERNAL_getSegmentSpan;
function INTERNAL_getSegmentSpan(span) {
	return span[ROOT_SPAN_FIELD] || span;
}
function showSpanDropWarning() {
	if (!hasShownSpanDropWarning) {
		consoleSandbox(() => {
			console.warn("[Sentry] Returning null from `beforeSendSpan` is disallowed. To drop certain spans, configure the respective integrations directly or use `ignoreSpans`.");
		});
		hasShownSpanDropWarning = true;
	}
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/hasSpansEnabled.js
function hasSpansEnabled(maybeOptions) {
	if (typeof __SENTRY_TRACING__ === "boolean" && !__SENTRY_TRACING__) return false;
	const options = maybeOptions || getClient()?.getOptions();
	return !!options && (options.tracesSampleRate != null || !!options.tracesSampler);
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/should-ignore-span.js
function logIgnoredSpan(droppedSpan) {
	debug$4.log(`Ignoring span ${droppedSpan.op} - ${droppedSpan.description} because it matches \`ignoreSpans\`.`);
}
function shouldIgnoreSpan(span, ignoreSpans) {
	if (!ignoreSpans?.length) return false;
	for (const pattern of ignoreSpans) {
		if (isStringOrRegExp(pattern)) {
			if (span.description && isMatchingPattern(span.description, pattern)) {
				DEBUG_BUILD$3 && logIgnoredSpan(span);
				return true;
			}
			continue;
		}
		const hasAttributes = !!pattern.attributes && Object.keys(pattern.attributes).length > 0;
		if (!pattern.name && !pattern.op && !hasAttributes) continue;
		const nameMatches = pattern.name ? span.description && isMatchingPattern(span.description, pattern.name) : true;
		const opMatches = pattern.op ? span.op && isMatchingPattern(span.op, pattern.op) : true;
		const attrsMatch = pattern.attributes ? Object.entries(pattern.attributes).every(([key, valuePattern]) => _matchesAttributeValue(span.attributes?.[key], valuePattern)) : true;
		if (nameMatches && opMatches && attrsMatch) {
			DEBUG_BUILD$3 && logIgnoredSpan(span);
			return true;
		}
	}
	return false;
}
function _matchesAttributeValue(actual, pat) {
	if (typeof actual === "string" && (typeof pat === "string" || pat instanceof RegExp)) return isMatchingPattern(actual, pat);
	if (Array.isArray(actual) && Array.isArray(pat)) return actual.length === pat.length && actual.every((v, i) => v === pat[i]);
	return actual === pat;
}
function reparentChildSpans(spans, dropSpan) {
	const droppedSpanParentId = dropSpan.parent_span_id;
	const droppedSpanId = dropSpan.span_id;
	if (!droppedSpanParentId) return;
	for (const span of spans) if (span.parent_span_id === droppedSpanId) span.parent_span_id = droppedSpanParentId;
}
function isStringOrRegExp(value) {
	return typeof value === "string" || value instanceof RegExp;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/tracing/sentryNonRecordingSpan.js
var NON_RECORDING_SPAN_FIELD = /* @__PURE__ */ Symbol.for("sentry.nonRecordingSpan");
function spanIsNonRecordingSpan(span) {
	return !!span && span[NON_RECORDING_SPAN_FIELD] === true;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/constants.js
var DEFAULT_ENVIRONMENT = "production";
//#endregion
//#region node_modules/@sentry/core/build/esm/tracing/dynamicSamplingContext.js
var FROZEN_DSC_FIELD = "_frozenDsc";
function getDynamicSamplingContextFromClient(trace_id, client) {
	const options = client.getOptions();
	const { publicKey: public_key } = client.getDsn() || {};
	const dsc = {
		environment: options.environment || "production",
		release: options.release,
		public_key,
		trace_id,
		org_id: extractOrgIdFromClient(client)
	};
	client.emit("createDsc", dsc);
	return dsc;
}
function getDynamicSamplingContextFromScope(client, scope) {
	const propagationContext = scope.getPropagationContext();
	return propagationContext.dsc || getDynamicSamplingContextFromClient(propagationContext.traceId, client);
}
function getDynamicSamplingContextFromSpan(span) {
	const client = getClient();
	if (!client) return {};
	const rootSpan = getRootSpan(span);
	const rootSpanJson = spanToJSON(rootSpan);
	const rootSpanAttributes = rootSpanJson.data;
	const traceState = rootSpan.spanContext().traceState;
	const rootSpanSampleRate = traceState?.get("sentry.sample_rate") ?? rootSpanAttributes["sentry.sample_rate"] ?? rootSpanAttributes["sentry.previous_trace_sample_rate"];
	function applyLocalSampleRateToDsc(dsc2) {
		if (typeof rootSpanSampleRate === "number" || typeof rootSpanSampleRate === "string") dsc2.sample_rate = `${rootSpanSampleRate}`;
		return dsc2;
	}
	const frozenDsc = rootSpan[FROZEN_DSC_FIELD];
	if (frozenDsc) return applyLocalSampleRateToDsc(frozenDsc);
	const isNonRecordingRoot = spanIsNonRecordingSpan(rootSpan);
	const isIgnoredRoot = isNonRecordingRoot && rootSpan.dropReason === "ignored";
	if (isNonRecordingRoot && (!hasSpansEnabled(client.getOptions()) || isIgnoredRoot)) {
		const capturedScope = getCapturedScopesOnSpan(rootSpan).scope;
		if (capturedScope) {
			const dsc2 = { ...getDynamicSamplingContextFromScope(client, capturedScope) };
			if (isIgnoredRoot) dsc2.sampled = "false";
			return applyLocalSampleRateToDsc(dsc2);
		}
	}
	const traceStateDsc = traceState?.get("sentry.dsc");
	const dscOnTraceState = traceStateDsc && baggageHeaderToDynamicSamplingContext(traceStateDsc);
	if (dscOnTraceState) return applyLocalSampleRateToDsc(dscOnTraceState);
	const dsc = getDynamicSamplingContextFromClient(span.spanContext().traceId, client);
	const source = rootSpanAttributes["sentry.source"] ?? rootSpanAttributes["sentry.segment.name.source"];
	const name = rootSpanJson.description;
	if (source !== "url" && name) dsc.transaction = name;
	if (hasSpansEnabled()) {
		dsc.sampled = String(spanIsSampled(rootSpan));
		dsc.sample_rand = traceState?.get("sentry.sample_rand") ?? getCapturedScopesOnSpan(rootSpan).scope?.getPropagationContext().sampleRand.toString();
	}
	applyLocalSampleRateToDsc(dsc);
	client.emit("createDsc", dsc, rootSpan);
	return dsc;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/tracing/spans/beforeSendSpan.js
function isStreamedBeforeSendSpanCallback(callback) {
	return !!callback && typeof callback === "function" && "_streamed" in callback && !!callback._streamed;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/envelope.js
function createEnvelope(headers, items = []) {
	return [headers, items];
}
function addItemToEnvelope(envelope, newItem) {
	const [headers, items] = envelope;
	return [headers, [...items, newItem]];
}
function forEachEnvelopeItem(envelope, callback) {
	const envelopeItems = envelope[1];
	for (const envelopeItem of envelopeItems) {
		const envelopeItemType = envelopeItem[0].type;
		if (callback(envelopeItem, envelopeItemType)) return true;
	}
	return false;
}
function envelopeContainsItemType(envelope, types) {
	return forEachEnvelopeItem(envelope, (_, type) => types.includes(type));
}
function encodeUTF8(input) {
	const carrier = getSentryCarrier(GLOBAL_OBJ);
	return carrier.encodePolyfill ? carrier.encodePolyfill(input) : new TextEncoder().encode(input);
}
function serializeEnvelope(envelope) {
	const [envHeaders, items] = envelope;
	let parts = JSON.stringify(envHeaders);
	function append(next) {
		if (typeof parts === "string") parts = typeof next === "string" ? parts + next : [encodeUTF8(parts), next];
		else parts.push(typeof next === "string" ? encodeUTF8(next) : next);
	}
	for (const item of items) {
		const [itemHeaders, payload] = item;
		append(`
${JSON.stringify(itemHeaders)}
`);
		if (typeof payload === "string" || payload instanceof Uint8Array) append(payload);
		else {
			let stringifiedPayload;
			try {
				stringifiedPayload = JSON.stringify(payload);
			} catch {
				stringifiedPayload = JSON.stringify(normalize(payload));
			}
			append(stringifiedPayload);
		}
	}
	return typeof parts === "string" ? parts : concatBuffers(parts);
}
function concatBuffers(buffers) {
	const totalLength = buffers.reduce((acc, buf) => acc + buf.length, 0);
	const merged = new Uint8Array(totalLength);
	let offset = 0;
	for (const buffer of buffers) {
		merged.set(buffer, offset);
		offset += buffer.length;
	}
	return merged;
}
function createAttachmentEnvelopeItem(attachment) {
	const buffer = typeof attachment.data === "string" ? encodeUTF8(attachment.data) : attachment.data;
	return [{
		type: "attachment",
		length: buffer.length,
		filename: attachment.filename,
		content_type: attachment.contentType,
		attachment_type: attachment.attachmentType
	}, buffer];
}
var DATA_CATEGORY_OVERRIDES = {
	sessions: "session",
	event: "error",
	client_report: "internal",
	user_report: "default",
	profile_chunk: "profile",
	replay_event: "replay",
	replay_recording: "replay",
	check_in: "monitor",
	raw_security: "security",
	log: "log_item",
	trace_metric: "metric"
};
function _isOverriddenType(type) {
	return type in DATA_CATEGORY_OVERRIDES;
}
function envelopeItemTypeToDataCategory(type) {
	return _isOverriddenType(type) ? DATA_CATEGORY_OVERRIDES[type] : type;
}
function getSdkMetadataForEnvelopeHeader(metadataOrEvent) {
	if (!metadataOrEvent?.sdk) return;
	const { name, version } = metadataOrEvent.sdk;
	return {
		name,
		version
	};
}
function createEventEnvelopeHeaders(event, sdkInfo, tunnel, dsn) {
	const dynamicSamplingContext = event.sdkProcessingMetadata?.dynamicSamplingContext;
	return {
		event_id: event.event_id,
		sent_at: new Date(safeDateNow()).toISOString(),
		...sdkInfo && { sdk: sdkInfo },
		...!!tunnel && dsn && { dsn: dsnToString(dsn) },
		...dynamicSamplingContext && { trace: dynamicSamplingContext }
	};
}
//#endregion
//#region node_modules/@sentry/core/build/esm/envelope.js
function _enhanceEventWithSdkInfo(event, newSdkInfo) {
	if (!newSdkInfo) return event;
	const eventSdkInfo = event.sdk || {};
	event.sdk = {
		...eventSdkInfo,
		name: eventSdkInfo.name || newSdkInfo.name,
		version: eventSdkInfo.version || newSdkInfo.version,
		integrations: [...event.sdk?.integrations || [], ...newSdkInfo.integrations || []],
		packages: [...event.sdk?.packages || [], ...newSdkInfo.packages || []],
		settings: event.sdk?.settings || newSdkInfo.settings ? {
			...event.sdk?.settings,
			...newSdkInfo.settings
		} : void 0
	};
	return event;
}
function createSessionEnvelope(session, dsn, metadata, tunnel) {
	const sdkInfo = getSdkMetadataForEnvelopeHeader(metadata);
	return createEnvelope({
		sent_at: new Date(safeDateNow()).toISOString(),
		...sdkInfo && { sdk: sdkInfo },
		...!!tunnel && dsn && { dsn: dsnToString(dsn) }
	}, ["aggregates" in session ? [{ type: "sessions" }, session] : [{ type: "session" }, session.toJSON()]]);
}
function createEventEnvelope(event, dsn, metadata, tunnel) {
	const sdkInfo = getSdkMetadataForEnvelopeHeader(metadata);
	const eventType = event.type && event.type !== "replay_event" ? event.type : "event";
	_enhanceEventWithSdkInfo(event, metadata?.sdk);
	const envelopeHeaders = createEventEnvelopeHeaders(event, sdkInfo, tunnel, dsn);
	delete event.sdkProcessingMetadata;
	return createEnvelope(envelopeHeaders, [[{ type: eventType }, event]]);
}
//#endregion
//#region node_modules/@sentry/core/build/esm/tracing/spans/hasSpanStreamingEnabled.js
function hasSpanStreamingEnabled(client) {
	return client.getOptions().traceLifecycle === "stream";
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/scopeData.js
function applyScopeDataToEvent(event, data) {
	const { fingerprint, span, breadcrumbs, sdkProcessingMetadata } = data;
	applyDataToEvent(event, data);
	if (span) applySpanToEvent(event, span);
	applyFingerprintToEvent(event, fingerprint);
	applyBreadcrumbsToEvent(event, breadcrumbs);
	applySdkMetadataToEvent(event, sdkProcessingMetadata);
}
function mergeScopeData(data, mergeData) {
	const { extra, tags, attributes, user, contexts, level, sdkProcessingMetadata, breadcrumbs, fingerprint, eventProcessors, attachments, propagationContext, transactionName, span } = mergeData;
	mergeAndOverwriteScopeData(data, "extra", extra);
	mergeAndOverwriteScopeData(data, "tags", tags);
	mergeAndOverwriteScopeData(data, "attributes", attributes);
	mergeAndOverwriteScopeData(data, "user", user);
	mergeAndOverwriteScopeData(data, "contexts", contexts);
	data.sdkProcessingMetadata = merge(data.sdkProcessingMetadata, sdkProcessingMetadata, 2);
	if (level) data.level = level;
	if (transactionName) data.transactionName = transactionName;
	if (span) data.span = span;
	if (breadcrumbs.length) data.breadcrumbs = [...data.breadcrumbs, ...breadcrumbs];
	if (fingerprint.length) data.fingerprint = [...data.fingerprint, ...fingerprint];
	if (eventProcessors.length) data.eventProcessors = [...data.eventProcessors, ...eventProcessors];
	if (attachments.length) data.attachments = [...data.attachments, ...attachments];
	data.propagationContext = {
		...data.propagationContext,
		...propagationContext
	};
}
function mergeAndOverwriteScopeData(data, prop, mergeVal) {
	data[prop] = merge(data[prop], mergeVal, 1);
}
function getCombinedScopeData(isolationScope, currentScope) {
	const scopeData = getGlobalScope$1().getScopeData();
	isolationScope && mergeScopeData(scopeData, isolationScope.getScopeData());
	currentScope && mergeScopeData(scopeData, currentScope.getScopeData());
	return scopeData;
}
function applyDataToEvent(event, data) {
	const { extra, tags, user, contexts, level, transactionName } = data;
	if (Object.keys(extra).length) event.extra = {
		...extra,
		...event.extra
	};
	if (Object.keys(tags).length) event.tags = {
		...tags,
		...event.tags
	};
	if (Object.keys(user).length) event.user = {
		...user,
		...event.user
	};
	if (Object.keys(contexts).length) event.contexts = {
		...contexts,
		...event.contexts
	};
	if (level) event.level = level;
	if (transactionName && event.type !== "transaction") event.transaction = transactionName;
}
function applyBreadcrumbsToEvent(event, breadcrumbs) {
	const mergedBreadcrumbs = [...event.breadcrumbs || [], ...breadcrumbs];
	event.breadcrumbs = mergedBreadcrumbs.length ? mergedBreadcrumbs : void 0;
}
function applySdkMetadataToEvent(event, sdkProcessingMetadata) {
	event.sdkProcessingMetadata = {
		...event.sdkProcessingMetadata,
		...sdkProcessingMetadata
	};
}
function applySpanToEvent(event, span) {
	event.contexts = {
		trace: spanToTraceContext(span),
		...event.contexts
	};
	event.sdkProcessingMetadata = {
		dynamicSamplingContext: getDynamicSamplingContextFromSpan(span),
		...event.sdkProcessingMetadata
	};
	const transactionName = spanToJSON(getRootSpan(span)).description;
	if (transactionName && !event.transaction && event.type === "transaction") event.transaction = transactionName;
}
function applyFingerprintToEvent(event, fingerprint) {
	event.fingerprint = event.fingerprint ? Array.isArray(event.fingerprint) ? event.fingerprint : [event.fingerprint] : [];
	if (fingerprint) event.fingerprint = event.fingerprint.concat(fingerprint);
	if (!event.fingerprint.length) delete event.fingerprint;
}
//#endregion
//#region node_modules/@sentry/conventions/dist/attributes.mjs
var Yu = "url.full";
//#endregion
//#region node_modules/@sentry/core/build/esm/tracing/spans/captureSpan.js
function safeSetSpanJSONAttributes(spanJSON, newAttributes) {
	const originalAttributes = spanJSON.attributes ?? (spanJSON.attributes = {});
	Object.entries(newAttributes).forEach(([key, value]) => {
		if (value != null && !(key in originalAttributes)) originalAttributes[key] = value;
	});
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/syncpromise.js
var STATE_PENDING = 0;
var STATE_RESOLVED = 1;
var STATE_REJECTED = 2;
function resolvedSyncPromise(value) {
	return new SyncPromise((resolve) => {
		resolve(value);
	});
}
function rejectedSyncPromise(reason) {
	return new SyncPromise((_, reject) => {
		reject(reason);
	});
}
var SyncPromise = class SyncPromise {
	constructor(executor) {
		this._state = STATE_PENDING;
		this._handlers = [];
		this._runExecutor(executor);
	}
	/** @inheritdoc */
	then(onfulfilled, onrejected) {
		return new SyncPromise((resolve, reject) => {
			this._handlers.push([
				false,
				(result) => {
					if (!onfulfilled) resolve(result);
					else try {
						resolve(onfulfilled(result));
					} catch (e) {
						reject(e);
					}
				},
				(reason) => {
					if (!onrejected) reject(reason);
					else try {
						resolve(onrejected(reason));
					} catch (e) {
						reject(e);
					}
				}
			]);
			this._executeHandlers();
		});
	}
	/** @inheritdoc */
	catch(onrejected) {
		return this.then((val) => val, onrejected);
	}
	/** @inheritdoc */
	finally(onfinally) {
		return new SyncPromise((resolve, reject) => {
			let val;
			let isRejected;
			return this.then((value) => {
				isRejected = false;
				val = value;
				if (onfinally) onfinally();
			}, (reason) => {
				isRejected = true;
				val = reason;
				if (onfinally) onfinally();
			}).then(() => {
				if (isRejected) {
					reject(val);
					return;
				}
				resolve(val);
			});
		});
	}
	/** Excute the resolve/reject handlers. */
	_executeHandlers() {
		if (this._state === STATE_PENDING) return;
		const cachedHandlers = this._handlers.slice();
		this._handlers = [];
		cachedHandlers.forEach((handler) => {
			if (handler[0]) return;
			if (this._state === STATE_RESOLVED) handler[1](this._value);
			if (this._state === STATE_REJECTED) handler[2](this._value);
			handler[0] = true;
		});
	}
	/** Run the executor for the SyncPromise. */
	_runExecutor(executor) {
		const setResult = (state, value) => {
			if (this._state !== STATE_PENDING) return;
			if (isThenable(value)) {
				value.then(resolve, reject);
				return;
			}
			this._state = state;
			this._value = value;
			this._executeHandlers();
		};
		const resolve = (value) => {
			setResult(STATE_RESOLVED, value);
		};
		const reject = (reason) => {
			setResult(STATE_REJECTED, reason);
		};
		try {
			executor(resolve, reject);
		} catch (e) {
			reject(e);
		}
	}
};
//#endregion
//#region node_modules/@sentry/core/build/esm/eventProcessors.js
function notifyEventProcessors(processors, event, hint, index = 0) {
	try {
		const result = _notifyEventProcessors(event, hint, processors, index);
		return isThenable(result) ? result : resolvedSyncPromise(result);
	} catch (error) {
		return rejectedSyncPromise(error);
	}
}
function _notifyEventProcessors(event, hint, processors, index) {
	const processor = processors[index];
	if (!event || !processor) return event;
	const result = processor({ ...event }, hint);
	DEBUG_BUILD$3 && result === null && debug$4.log(`Event processor "${processor.id || "?"}" dropped event`);
	if (isThenable(result)) return result.then((final) => _notifyEventProcessors(final, hint, processors, index + 1));
	return _notifyEventProcessors(result, hint, processors, index + 1);
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/debug-ids.js
var parsedStackResults;
var lastSentryKeysCount;
var lastNativeKeysCount;
var cachedFilenameDebugIds;
function getFilenameToDebugIdMap(stackParser) {
	const sentryDebugIdMap = GLOBAL_OBJ._sentryDebugIds;
	const nativeDebugIdMap = GLOBAL_OBJ._debugIds;
	if (!sentryDebugIdMap && !nativeDebugIdMap) return {};
	const sentryDebugIdKeys = sentryDebugIdMap ? Object.keys(sentryDebugIdMap) : [];
	const nativeDebugIdKeys = nativeDebugIdMap ? Object.keys(nativeDebugIdMap) : [];
	if (cachedFilenameDebugIds && sentryDebugIdKeys.length === lastSentryKeysCount && nativeDebugIdKeys.length === lastNativeKeysCount) return cachedFilenameDebugIds;
	lastSentryKeysCount = sentryDebugIdKeys.length;
	lastNativeKeysCount = nativeDebugIdKeys.length;
	cachedFilenameDebugIds = {};
	if (!parsedStackResults) parsedStackResults = {};
	const processDebugIds = (debugIdKeys, debugIdMap) => {
		for (const key of debugIdKeys) {
			const debugId = debugIdMap[key];
			const result = parsedStackResults?.[key];
			if (result && cachedFilenameDebugIds && debugId) {
				cachedFilenameDebugIds[result[0]] = debugId;
				if (parsedStackResults) parsedStackResults[key] = [result[0], debugId];
			} else if (debugId) {
				const parsedStack = stackParser(key);
				for (let i = parsedStack.length - 1; i >= 0; i--) {
					const filename = parsedStack[i]?.filename;
					if (filename && cachedFilenameDebugIds && parsedStackResults) {
						cachedFilenameDebugIds[filename] = debugId;
						parsedStackResults[key] = [filename, debugId];
						break;
					}
				}
			}
		}
	};
	if (sentryDebugIdMap) processDebugIds(sentryDebugIdKeys, sentryDebugIdMap);
	if (nativeDebugIdMap) processDebugIds(nativeDebugIdKeys, nativeDebugIdMap);
	return cachedFilenameDebugIds;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/prepareEvent.js
function prepareEvent(options, event, hint, scope, client, isolationScope) {
	const { normalizeDepth = 3, normalizeMaxBreadth = 1e3 } = options;
	const prepared = {
		...event,
		event_id: event.event_id || hint.event_id || uuid4(),
		timestamp: event.timestamp || dateTimestampInSeconds()
	};
	const integrations = hint.integrations || options.integrations.map((i) => i.name);
	applyClientOptions(prepared, options);
	applyIntegrationsMetadata(prepared, integrations);
	if (client) client.emit("applyFrameMetadata", event);
	if (event.type === void 0) applyDebugIds(prepared, options.stackParser);
	const finalScope = getFinalScope(scope, hint.captureContext);
	if (hint.mechanism) addExceptionMechanism(prepared, hint.mechanism);
	const clientEventProcessors = client ? client.getEventProcessors() : [];
	const data = getCombinedScopeData(isolationScope, finalScope);
	const attachments = [...hint.attachments || [], ...data.attachments];
	if (attachments.length) hint.attachments = attachments;
	applyScopeDataToEvent(prepared, data);
	const eventProcessors = [...clientEventProcessors, ...data.eventProcessors];
	return (hint.data && hint.data.__sentry__ === true ? resolvedSyncPromise(prepared) : notifyEventProcessors(eventProcessors, prepared, hint)).then((evt) => {
		if (evt) applyDebugMeta(evt);
		if (typeof normalizeDepth === "number" && normalizeDepth > 0) return normalizeEvent(evt, normalizeDepth, normalizeMaxBreadth);
		return evt;
	});
}
function applyClientOptions(event, options) {
	const { environment, release, dist, maxValueLength } = options;
	event.environment = event.environment || environment || "production";
	if (!event.release && release) event.release = release;
	if (!event.dist && dist) event.dist = dist;
	const request = event.request;
	if (request?.url && maxValueLength) request.url = truncate(request.url, maxValueLength);
	if (maxValueLength) event.exception?.values?.forEach((exception) => {
		if (exception.value) exception.value = truncate(exception.value, maxValueLength);
	});
}
function applyDebugIds(event, stackParser) {
	const filenameDebugIdMap = getFilenameToDebugIdMap(stackParser);
	event.exception?.values?.forEach((exception) => {
		exception.stacktrace?.frames?.forEach((frame) => {
			if (frame.filename) frame.debug_id = filenameDebugIdMap[frame.filename];
		});
	});
}
function applyDebugMeta(event) {
	const filenameDebugIdMap = {};
	event.exception?.values?.forEach((exception) => {
		exception.stacktrace?.frames?.forEach((frame) => {
			if (frame.debug_id) {
				if (frame.abs_path) filenameDebugIdMap[frame.abs_path] = frame.debug_id;
				else if (frame.filename) filenameDebugIdMap[frame.filename] = frame.debug_id;
				delete frame.debug_id;
			}
		});
	});
	if (Object.keys(filenameDebugIdMap).length === 0) return;
	event.debug_meta = event.debug_meta || {};
	event.debug_meta.images = event.debug_meta.images || [];
	const images = event.debug_meta.images;
	Object.entries(filenameDebugIdMap).forEach(([filename, debug_id]) => {
		images.push({
			type: "sourcemap",
			code_file: filename,
			debug_id
		});
	});
}
function applyIntegrationsMetadata(event, integrationNames) {
	if (integrationNames.length > 0) {
		event.sdk = event.sdk || {};
		event.sdk.integrations = [...event.sdk.integrations || [], ...integrationNames];
	}
}
function normalizeEvent(event, depth, maxBreadth) {
	if (!event) return null;
	const normalized = {
		...event,
		...event.breadcrumbs && { breadcrumbs: event.breadcrumbs.map((b) => ({
			...b,
			...b.data && { data: normalize(b.data, depth, maxBreadth) }
		})) },
		...event.user && { user: normalize(event.user, depth, maxBreadth) },
		...event.contexts && { contexts: normalize(event.contexts, depth, maxBreadth) },
		...event.extra && { extra: normalize(event.extra, depth, maxBreadth) }
	};
	if (event.contexts?.trace && normalized.contexts) {
		normalized.contexts.trace = event.contexts.trace;
		if (event.contexts.trace.data) normalized.contexts.trace.data = normalize(event.contexts.trace.data, depth, maxBreadth);
	}
	if (event.spans) normalized.spans = event.spans.map((span) => {
		return {
			...span,
			...span.data && { data: normalize(span.data, depth, maxBreadth) }
		};
	});
	if (event.contexts?.flags && normalized.contexts) normalized.contexts.flags = normalize(event.contexts.flags, 3, maxBreadth);
	return normalized;
}
function getFinalScope(scope, captureContext) {
	if (!captureContext) return scope;
	const finalScope = scope ? scope.clone() : new Scope();
	finalScope.update(captureContext);
	return finalScope;
}
function parseEventHintOrCaptureContext(hint) {
	if (!hint) return;
	if (hintIsScopeOrFunction(hint)) return { captureContext: hint };
	if (hintIsScopeContext(hint)) return { captureContext: hint };
	return hint;
}
function hintIsScopeOrFunction(hint) {
	return hint instanceof Scope || typeof hint === "function";
}
var captureContextKeys = [
	"user",
	"level",
	"extra",
	"contexts",
	"tags",
	"fingerprint",
	"propagationContext"
];
function hintIsScopeContext(hint) {
	return Object.keys(hint).some((key) => captureContextKeys.includes(key));
}
//#endregion
//#region node_modules/@sentry/core/build/esm/exports.js
function captureException(exception, hint) {
	return getCurrentScope().captureException(exception, parseEventHintOrCaptureContext(hint));
}
function captureEvent(event, hint) {
	return getCurrentScope().captureEvent(event, hint);
}
function setContext(name, context) {
	getIsolationScope().setContext(name, context);
}
function isInitialized() {
	return !!getClient();
}
function startSession(context) {
	const isolationScope = getIsolationScope();
	const { user } = getCombinedScopeData(isolationScope, getCurrentScope());
	const { userAgent } = GLOBAL_OBJ.navigator || {};
	const session = makeSession({
		user,
		...userAgent && { userAgent },
		...context
	});
	const currentSession = isolationScope.getSession();
	if (currentSession?.status === "ok") updateSession(currentSession, { status: "exited" });
	endSession();
	isolationScope.setSession(session);
	return session;
}
function endSession() {
	const isolationScope = getIsolationScope();
	const session = getCurrentScope().getSession() || isolationScope.getSession();
	if (session) closeSession(session);
	_sendSessionUpdate();
	isolationScope.setSession();
}
function _sendSessionUpdate() {
	const isolationScope = getIsolationScope();
	const client = getClient();
	const session = isolationScope.getSession();
	if (session && client) client.captureSession(session);
}
function captureSession(end = false) {
	if (end) {
		endSession();
		return;
	}
	_sendSessionUpdate();
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/timer.js
function safeUnref(timer) {
	if (typeof timer === "object" && typeof timer.unref === "function") timer.unref();
	return timer;
}
function getBaseApiEndpoint(dsn) {
	const protocol = dsn.protocol ? `${dsn.protocol}:` : "";
	const port = dsn.port ? `:${dsn.port}` : "";
	return `${protocol}//${dsn.host}${port}${dsn.path ? `/${dsn.path}` : ""}/api/`;
}
function _getIngestEndpoint(dsn) {
	return `${getBaseApiEndpoint(dsn)}${dsn.projectId}/envelope/`;
}
function _encodedAuth(dsn, sdkInfo) {
	const params = { sentry_version: "7" };
	if (dsn.publicKey) params.sentry_key = dsn.publicKey;
	if (sdkInfo) params.sentry_client = `${sdkInfo.name}/${sdkInfo.version}`;
	return new URLSearchParams(params).toString();
}
function getEnvelopeEndpointWithUrlEncodedAuth(dsn, tunnel, sdkInfo) {
	return tunnel ? tunnel : `${_getIngestEndpoint(dsn)}?${_encodedAuth(dsn, sdkInfo)}`;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/integration.js
var installedIntegrations = [];
function filterDuplicates(integrations) {
	const integrationsByName = {};
	integrations.forEach((currentInstance) => {
		const { name } = currentInstance;
		const existingInstance = integrationsByName[name];
		if (existingInstance && !existingInstance.isDefaultInstance && currentInstance.isDefaultInstance) return;
		integrationsByName[name] = currentInstance;
	});
	return Object.values(integrationsByName);
}
function getIntegrationsToSetup(options) {
	const defaultIntegrations = options.defaultIntegrations || [];
	const userIntegrations = options.integrations;
	defaultIntegrations.forEach((integration) => {
		integration.isDefaultInstance = true;
	});
	let integrations;
	if (Array.isArray(userIntegrations)) integrations = [...defaultIntegrations, ...userIntegrations];
	else if (typeof userIntegrations === "function") {
		const resolvedUserIntegrations = userIntegrations(defaultIntegrations);
		integrations = Array.isArray(resolvedUserIntegrations) ? resolvedUserIntegrations : [resolvedUserIntegrations];
	} else integrations = defaultIntegrations;
	return filterDuplicates(integrations);
}
function setupIntegrations(client, integrations) {
	const integrationIndex = {};
	integrations.forEach((integration) => {
		if (integration?.beforeSetup) integration.beforeSetup(client);
	});
	integrations.forEach((integration) => {
		if (integration) setupIntegration(client, integration, integrationIndex);
	});
	return integrationIndex;
}
function afterSetupIntegrations(client, integrations) {
	for (const integration of integrations) if (integration?.afterAllSetup) integration.afterAllSetup(client);
}
function setupIntegration(client, integration, integrationIndex) {
	if (integrationIndex[integration.name]) {
		DEBUG_BUILD$3 && debug$4.log(`Integration skipped because it was already installed: ${integration.name}`);
		return;
	}
	integrationIndex[integration.name] = integration;
	if (!installedIntegrations.includes(integration.name) && typeof integration.setupOnce === "function") {
		integration.setupOnce();
		installedIntegrations.push(integration.name);
	}
	if (integration.setup && typeof integration.setup === "function") integration.setup(client);
	if (typeof integration.preprocessEvent === "function") {
		const callback = integration.preprocessEvent.bind(integration);
		client.on("preprocessEvent", (event, hint) => callback(event, hint, client));
	}
	if (typeof integration.processEvent === "function") {
		const callback = integration.processEvent.bind(integration);
		const processor = Object.assign((event, hint) => callback(event, hint, client), { id: integration.name });
		client.addEventProcessor(processor);
	}
	["processSpan", "processSegmentSpan"].forEach((hook) => {
		const callback = integration[hook];
		if (typeof callback === "function") client.on(hook, (span) => callback.call(integration, span, client));
	});
	DEBUG_BUILD$3 && debug$4.log(`Integration installed: ${integration.name}`);
}
function defineIntegration(fn) {
	return fn;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/env.js
function isBrowserBundle() {
	return typeof __SENTRY_BROWSER_BUNDLE__ !== "undefined" && !!__SENTRY_BROWSER_BUNDLE__;
}
function getSDKSource() {
	/*! __SENTRY_SDK_SOURCE__ */
	return "npm";
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/node.js
function isNodeEnv() {
	return !isBrowserBundle() && Object.prototype.toString.call(typeof process !== "undefined" ? process : 0) === "[object process]";
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/isBrowser.js
function isBrowser$1() {
	return typeof window !== "undefined" && (!isNodeEnv() || isElectronNodeRenderer());
}
function isElectronNodeRenderer() {
	return GLOBAL_OBJ.process?.type === "renderer";
}
//#endregion
//#region node_modules/@sentry/core/build/esm/logs/envelope.js
function createLogContainerEnvelopeItem(items, inferUserData) {
	const inferSetting = inferUserData ? "auto" : "never";
	return [{
		type: "log",
		item_count: items.length,
		content_type: "application/vnd.sentry.items.log+json"
	}, {
		version: 2,
		...isBrowser$1() && { ingest_settings: {
			infer_ip: inferSetting,
			infer_user_agent: inferSetting
		} },
		items
	}];
}
function createLogEnvelope(logs, metadata, tunnel, dsn, inferUserData) {
	const headers = {};
	if (metadata?.sdk) headers.sdk = {
		name: metadata.sdk.name,
		version: metadata.sdk.version
	};
	if (!!tunnel && !!dsn) headers.dsn = dsnToString(dsn);
	return createEnvelope(headers, [createLogContainerEnvelopeItem(logs, inferUserData)]);
}
//#endregion
//#region node_modules/@sentry/core/build/esm/logs/internal.js
function _INTERNAL_flushLogsBuffer(client, maybeLogBuffer) {
	const logBuffer = maybeLogBuffer ?? _INTERNAL_getLogBuffer(client) ?? [];
	if (logBuffer.length === 0) return;
	const clientOptions = client.getOptions();
	const envelope = createLogEnvelope(logBuffer, clientOptions._metadata, clientOptions.tunnel, client.getDsn(), client.getDataCollectionOptions().userInfo);
	_getBufferMap$1().set(client, []);
	client.emit("flushLogs");
	client.sendEnvelope(envelope);
}
function _INTERNAL_getLogBuffer(client) {
	return _getBufferMap$1().get(client);
}
function _getBufferMap$1() {
	return getGlobalSingleton("clientToLogBufferMap", () => /* @__PURE__ */ new WeakMap());
}
//#endregion
//#region node_modules/@sentry/core/build/esm/metrics/envelope.js
function createMetricContainerEnvelopeItem(items, inferUserData) {
	const inferSetting = inferUserData ? "auto" : "never";
	return [{
		type: "trace_metric",
		item_count: items.length,
		content_type: "application/vnd.sentry.items.trace-metric+json"
	}, {
		version: 2,
		...isBrowser$1() && { ingest_settings: {
			infer_ip: inferSetting,
			infer_user_agent: inferSetting
		} },
		items
	}];
}
function createMetricEnvelope(metrics, metadata, tunnel, dsn, inferUserData) {
	const headers = {};
	if (metadata?.sdk) headers.sdk = {
		name: metadata.sdk.name,
		version: metadata.sdk.version
	};
	if (!!tunnel && !!dsn) headers.dsn = dsnToString(dsn);
	return createEnvelope(headers, [createMetricContainerEnvelopeItem(metrics, inferUserData)]);
}
//#endregion
//#region node_modules/@sentry/core/build/esm/metrics/internal.js
function _INTERNAL_flushMetricsBuffer(client, maybeMetricBuffer) {
	const metricBuffer = maybeMetricBuffer ?? _INTERNAL_getMetricBuffer(client) ?? [];
	if (metricBuffer.length === 0) return;
	const clientOptions = client.getOptions();
	const envelope = createMetricEnvelope(metricBuffer, clientOptions._metadata, clientOptions.tunnel, client.getDsn(), client.getDataCollectionOptions().userInfo);
	_getBufferMap().set(client, []);
	client.emit("flushMetrics");
	client.sendEnvelope(envelope);
}
function _INTERNAL_getMetricBuffer(client) {
	return _getBufferMap().get(client);
}
function _getBufferMap() {
	return getGlobalSingleton("clientToMetricBufferMap", () => /* @__PURE__ */ new WeakMap());
}
//#endregion
//#region node_modules/@sentry/core/build/esm/tracing/spans/spanJsonToStreamedSpan.js
function spanJsonToSerializedStreamedSpan(span) {
	return streamedSpanJsonToSerializedSpan({
		trace_id: span.trace_id,
		span_id: span.span_id,
		parent_span_id: span.parent_span_id,
		name: span.description || "",
		start_timestamp: span.start_timestamp,
		end_timestamp: span.timestamp || span.start_timestamp,
		status: !span.status || span.status === "ok" || span.status === "cancelled" ? "ok" : "error",
		is_segment: false,
		attributes: { ...span.data },
		links: span.links
	});
}
//#endregion
//#region node_modules/@sentry/core/build/esm/tracing/spans/extractGenAiSpans.js
function extractGenAiSpansFromEvent(event, client) {
	if (event.type !== "transaction" || !event.spans?.length || !event.sdkProcessingMetadata?.hasGenAiSpans || client.getOptions().streamGenAiSpans === false || hasSpanStreamingEnabled(client)) return;
	const genAiSpans = [];
	const remainingSpans = [];
	for (const span of event.spans) if (span.op?.startsWith("gen_ai.")) genAiSpans.push(spanJsonToSerializedStreamedSpan(span));
	else remainingSpans.push(span);
	if (genAiSpans.length === 0) return;
	event.spans = remainingSpans;
	const inferSetting = client.getDataCollectionOptions().userInfo ? "auto" : "never";
	return [{
		type: "span",
		item_count: genAiSpans.length,
		content_type: "application/vnd.sentry.items.span.v2+json"
	}, {
		version: 2,
		...isBrowser$1() && { ingest_settings: {
			infer_ip: inferSetting,
			infer_user_agent: inferSetting
		} },
		items: genAiSpans
	}];
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/promisebuffer.js
var SENTRY_BUFFER_FULL_ERROR = /* @__PURE__ */ Symbol.for("SentryBufferFullError");
function makePromiseBuffer(limit = 100) {
	const buffer = /* @__PURE__ */ new Set();
	function isReady() {
		return buffer.size < limit;
	}
	function remove(task) {
		buffer.delete(task);
	}
	function add(taskProducer) {
		if (!isReady()) return rejectedSyncPromise(SENTRY_BUFFER_FULL_ERROR);
		const task = taskProducer();
		buffer.add(task);
		task.then(() => remove(task), () => remove(task));
		return task;
	}
	function drain(timeout) {
		if (!buffer.size) return resolvedSyncPromise(true);
		const drainPromise = Promise.allSettled(Array.from(buffer)).then(() => true);
		if (!timeout) return drainPromise;
		const promises = [drainPromise, new Promise((resolve) => safeUnref(setTimeout(() => resolve(false), timeout)))];
		return Promise.race(promises);
	}
	return {
		get $() {
			return Array.from(buffer);
		},
		add,
		drain
	};
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/ratelimit.js
var DEFAULT_RETRY_AFTER = 6e4;
function parseRetryAfterHeader(header, now = safeDateNow()) {
	const headerDelay = parseInt(`${header}`, 10);
	if (!isNaN(headerDelay)) return headerDelay * 1e3;
	const headerDate = Date.parse(`${header}`);
	if (!isNaN(headerDate)) return headerDate - now;
	return DEFAULT_RETRY_AFTER;
}
function disabledUntil(limits, dataCategory) {
	return limits[dataCategory] || limits.all || 0;
}
function isRateLimited(limits, dataCategory, now = safeDateNow()) {
	return disabledUntil(limits, dataCategory) > now;
}
function updateRateLimits(limits, { statusCode, headers }, now = safeDateNow()) {
	const updatedRateLimits = { ...limits };
	const rateLimitHeader = headers?.["x-sentry-rate-limits"];
	const retryAfterHeader = headers?.["retry-after"];
	if (rateLimitHeader) for (const limit of rateLimitHeader.trim().split(",")) {
		const [retryAfter, categories, , , namespaces] = limit.split(":", 5);
		const headerDelay = parseInt(retryAfter, 10);
		const delay = (!isNaN(headerDelay) ? headerDelay : 60) * 1e3;
		if (!categories) updatedRateLimits.all = now + delay;
		else for (const category of categories.split(";")) if (category === "metric_bucket") {
			if (!namespaces || namespaces.split(";").includes("custom")) updatedRateLimits[category] = now + delay;
		} else updatedRateLimits[category] = now + delay;
	}
	else if (retryAfterHeader) updatedRateLimits.all = now + parseRetryAfterHeader(retryAfterHeader, now);
	else if (statusCode === 429) updatedRateLimits.all = now + 6e4;
	return updatedRateLimits;
}
function createTransport(options, makeRequest, buffer = makePromiseBuffer(options.bufferSize || 64)) {
	let rateLimits = {};
	const flush = (timeout) => buffer.drain(timeout);
	function send(envelope) {
		const filteredEnvelopeItems = [];
		forEachEnvelopeItem(envelope, (item, type) => {
			const dataCategory = envelopeItemTypeToDataCategory(type);
			if (isRateLimited(rateLimits, dataCategory)) options.recordDroppedEvent("ratelimit_backoff", dataCategory);
			else filteredEnvelopeItems.push(item);
		});
		if (filteredEnvelopeItems.length === 0) return Promise.resolve({});
		const filteredEnvelope = createEnvelope(envelope[0], filteredEnvelopeItems);
		const recordEnvelopeLoss = (reason) => {
			if (envelopeContainsItemType(filteredEnvelope, ["client_report"])) {
				DEBUG_BUILD$3 && debug$4.warn(`Dropping client report. Will not send outcomes (reason: ${reason}).`);
				return;
			}
			forEachEnvelopeItem(filteredEnvelope, (item, type) => {
				options.recordDroppedEvent(reason, envelopeItemTypeToDataCategory(type));
			});
		};
		const requestTask = () => makeRequest({ body: serializeEnvelope(filteredEnvelope) }).then((response) => {
			if (response.statusCode === 413) {
				DEBUG_BUILD$3 && debug$4.error("Sentry responded with status code 413. Envelope was discarded due to exceeding size limits.");
				recordEnvelopeLoss("send_error");
				return response;
			}
			if (DEBUG_BUILD$3 && response.statusCode !== void 0 && (response.statusCode < 200 || response.statusCode >= 300)) debug$4.warn(`Sentry responded with status code ${response.statusCode} to sent event.`);
			rateLimits = updateRateLimits(rateLimits, response);
			return response;
		}, (error) => {
			recordEnvelopeLoss("network_error");
			DEBUG_BUILD$3 && debug$4.error("Encountered error running transport request:", error);
			throw error;
		});
		return buffer.add(requestTask).then((result) => result, (error) => {
			if (error === SENTRY_BUFFER_FULL_ERROR) {
				DEBUG_BUILD$3 && debug$4.error("Skipped sending event because buffer is full.");
				recordEnvelopeLoss("queue_overflow");
				return Promise.resolve({});
			} else throw error;
		});
	}
	return {
		send,
		flush
	};
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/clientreport.js
function createClientReportEnvelope(discarded_events, dsn, timestamp) {
	const clientReportItem = [{ type: "client_report" }, {
		timestamp: timestamp || dateTimestampInSeconds(),
		discarded_events
	}];
	return createEnvelope(dsn ? { dsn } : {}, [clientReportItem]);
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/eventUtils.js
function getPossibleEventMessages(event) {
	const possibleMessages = [];
	if (event.message) possibleMessages.push(event.message);
	try {
		const lastException = event.exception.values[event.exception.values.length - 1];
		if (lastException?.value) {
			possibleMessages.push(lastException.value);
			if (lastException.type) possibleMessages.push(`${lastException.type}: ${lastException.value}`);
		}
	} catch {}
	return possibleMessages;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/transactionEvent.js
function convertTransactionEventToSpanJson(event) {
	const { trace_id, parent_span_id, span_id, status, origin, data, op } = event.contexts?.trace ?? {};
	return {
		data: data ?? {},
		description: event.transaction,
		op,
		parent_span_id,
		span_id: span_id ?? "",
		start_timestamp: event.start_timestamp ?? 0,
		status,
		timestamp: event.timestamp,
		trace_id: trace_id ?? "",
		origin,
		profile_id: data?.[SEMANTIC_ATTRIBUTE_PROFILE_ID],
		exclusive_time: data?.[SEMANTIC_ATTRIBUTE_EXCLUSIVE_TIME],
		measurements: event.measurements,
		is_segment: true
	};
}
function convertSpanJsonToTransactionEvent(span) {
	return {
		type: "transaction",
		timestamp: span.timestamp,
		start_timestamp: span.start_timestamp,
		transaction: span.description,
		contexts: { trace: {
			trace_id: span.trace_id,
			span_id: span.span_id,
			parent_span_id: span.parent_span_id,
			op: span.op,
			status: span.status,
			origin: span.origin,
			data: {
				...span.data,
				...span.profile_id && { ["sentry.profile_id"]: span.profile_id },
				...span.exclusive_time && { ["sentry.exclusive_time"]: span.exclusive_time }
			}
		} },
		measurements: span.measurements
	};
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/data-collection/filtering-snippets.js
var PII_HEADER_SNIPPETS = [
	"forwarded",
	"-ip",
	"remote-",
	"via",
	"-user"
];
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/data-collection/defaultPiiToCollectionOptions.js
function defaultPiiToCollectionOptions(sendDefaultPii) {
	return sendDefaultPii === true ? {
		userInfo: true,
		cookies: true,
		httpHeaders: {
			request: true,
			response: true
		},
		httpBodies: [
			"incomingRequest",
			"outgoingRequest",
			"incomingResponse",
			"outgoingResponse"
		],
		urlQueryParams: true,
		graphQL: {
			document: true,
			variables: true
		},
		genAI: {
			inputs: true,
			outputs: true
		},
		databaseQueryData: true,
		stackFrameVariables: true,
		frameContextLines: 7
	} : {
		userInfo: false,
		cookies: { deny: PII_HEADER_SNIPPETS },
		httpHeaders: {
			request: { deny: PII_HEADER_SNIPPETS },
			response: { deny: PII_HEADER_SNIPPETS }
		},
		httpBodies: [],
		urlQueryParams: { deny: PII_HEADER_SNIPPETS },
		graphQL: {
			document: true,
			variables: true
		},
		genAI: {
			inputs: false,
			outputs: false
		},
		databaseQueryData: false,
		stackFrameVariables: true,
		frameContextLines: 7
	};
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/data-collection/resolveDataCollectionOptions.js
var DEFAULTS = {
	userInfo: true,
	cookies: true,
	httpHeaders: {
		request: true,
		response: true
	},
	httpBodies: [
		"incomingRequest",
		"outgoingRequest",
		"incomingResponse",
		"outgoingResponse"
	],
	urlQueryParams: true,
	graphQL: {
		document: true,
		variables: true
	},
	genAI: {
		inputs: true,
		outputs: true
	},
	databaseQueryData: true,
	stackFrameVariables: true,
	frameContextLines: 5
};
function resolveDataCollectionOptions(options) {
	const base = options.dataCollection != null ? DEFAULTS : defaultPiiToCollectionOptions(options.sendDefaultPii);
	const dc = options.dataCollection ?? {};
	return {
		userInfo: dc.userInfo ?? base.userInfo,
		cookies: dc.cookies ?? base.cookies,
		httpHeaders: {
			request: dc.httpHeaders?.request ?? base.httpHeaders.request,
			response: dc.httpHeaders?.response ?? base.httpHeaders.response
		},
		httpBodies: dc.httpBodies ?? base.httpBodies,
		urlQueryParams: dc.urlQueryParams ?? dc.queryParams ?? base.urlQueryParams,
		graphQL: {
			document: dc.graphQL?.document ?? base.graphQL.document,
			variables: dc.graphQL?.variables ?? base.graphQL.variables
		},
		genAI: {
			inputs: dc.genAI?.inputs ?? base.genAI.inputs,
			outputs: dc.genAI?.outputs ?? base.genAI.outputs
		},
		databaseQueryData: dc.databaseQueryData ?? base.databaseQueryData,
		stackFrameVariables: dc.stackFrameVariables ?? base.stackFrameVariables,
		frameContextLines: dc.frameContextLines ?? base.frameContextLines
	};
}
//#endregion
//#region node_modules/@sentry/core/build/esm/client.js
var ALREADY_SEEN_ERROR = "Not capturing exception because it's already been captured.";
var MISSING_RELEASE_FOR_SESSION_ERROR = "Discarded session because of missing or non-string release";
var INTERNAL_ERROR_SYMBOL = /* @__PURE__ */ Symbol.for("SentryInternalError");
var DO_NOT_SEND_EVENT_SYMBOL = /* @__PURE__ */ Symbol.for("SentryDoNotSendEventError");
var DEFAULT_FLUSH_INTERVAL = 5e3;
function _makeInternalError(message) {
	return {
		message,
		[INTERNAL_ERROR_SYMBOL]: true
	};
}
function _makeDoNotSendEventError(message) {
	return {
		message,
		[DO_NOT_SEND_EVENT_SYMBOL]: true
	};
}
function _isInternalError(error) {
	return isObjectLike(error) && INTERNAL_ERROR_SYMBOL in error;
}
function _isDoNotSendEventError(error) {
	return isObjectLike(error) && DO_NOT_SEND_EVENT_SYMBOL in error;
}
function setupWeightBasedFlushing(client, afterCaptureHook, flushHook, estimateSizeFn, flushFn) {
	let weight = 0;
	let flushTimeout;
	let isTimerActive = false;
	client.on(flushHook, () => {
		weight = 0;
		clearTimeout(flushTimeout);
		isTimerActive = false;
	});
	client.on(afterCaptureHook, (item) => {
		weight += estimateSizeFn(item);
		if (weight >= 8e5) flushFn(client);
		else if (!isTimerActive) {
			const flushInterval = client.getOptions()._flushInterval ?? DEFAULT_FLUSH_INTERVAL;
			if (flushInterval > 0) {
				isTimerActive = true;
				flushTimeout = safeUnref(setTimeout(() => {
					flushFn(client);
				}, flushInterval));
			}
		}
	});
	client.on("flush", () => {
		flushFn(client);
	});
}
var Client = class {
	/**
	* Initializes this client instance.
	*
	* @param options Options for the client.
	*/
	constructor(options) {
		this._options = options;
		this._integrations = {};
		this._numProcessing = 0;
		this._outcomes = {};
		this._hooks = {};
		this._eventProcessors = [];
		this._promiseBuffer = makePromiseBuffer(options.transportOptions?.bufferSize ?? 64);
		this._dataCollection = resolveDataCollectionOptions(options);
		if (options.dsn) this._dsn = makeDsn(options.dsn);
		else DEBUG_BUILD$3 && debug$4.warn("No DSN provided, client will not send events.");
		if (this._dsn) {
			const url = getEnvelopeEndpointWithUrlEncodedAuth(this._dsn, options.tunnel, options._metadata ? options._metadata.sdk : void 0);
			this._transport = options.transport({
				tunnel: this._options.tunnel,
				recordDroppedEvent: this.recordDroppedEvent.bind(this),
				...options.transportOptions,
				url
			});
		}
		this._options.enableLogs = this._options.enableLogs ?? this._options._experiments?.enableLogs;
		if (this._options.enableLogs) setupWeightBasedFlushing(this, "afterCaptureLog", "flushLogs", estimateLogSizeInBytes, _INTERNAL_flushLogsBuffer);
		if (this._options.enableMetrics ?? this._options._experiments?.enableMetrics ?? true) setupWeightBasedFlushing(this, "afterCaptureMetric", "flushMetrics", estimateMetricSizeInBytes, _INTERNAL_flushMetricsBuffer);
	}
	/**
	* Captures an exception event and sends it to Sentry.
	*
	* Unlike `captureException` exported from every SDK, this method requires that you pass it the current scope.
	*/
	captureException(exception, hint, scope) {
		const eventId = uuid4();
		if (checkOrSetAlreadyCaught(exception)) {
			DEBUG_BUILD$3 && debug$4.log(ALREADY_SEEN_ERROR);
			return eventId;
		}
		const hintWithEventId = {
			event_id: eventId,
			...hint
		};
		this._process(() => this.eventFromException(exception, hintWithEventId).then((event) => this._captureEvent(event, hintWithEventId, scope)).then((res) => res), "error");
		return hintWithEventId.event_id;
	}
	/**
	* Captures a message event and sends it to Sentry.
	*
	* Unlike `captureMessage` exported from every SDK, this method requires that you pass it the current scope.
	*/
	captureMessage(message, level, hint, currentScope) {
		const hintWithEventId = {
			event_id: uuid4(),
			...hint
		};
		const eventMessage = isParameterizedString(message) ? message : String(message);
		const isMessage = isPrimitive(message);
		const promisedEvent = isMessage ? this.eventFromMessage(eventMessage, level, hintWithEventId) : this.eventFromException(message, hintWithEventId);
		this._process(() => promisedEvent.then((event) => this._captureEvent(event, hintWithEventId, currentScope)), isMessage ? "unknown" : "error");
		return hintWithEventId.event_id;
	}
	/**
	* Captures a manually created event and sends it to Sentry.
	*
	* Unlike `captureEvent` exported from every SDK, this method requires that you pass it the current scope.
	*/
	captureEvent(event, hint, currentScope) {
		const eventId = uuid4();
		if (hint?.originalException && checkOrSetAlreadyCaught(hint.originalException)) {
			DEBUG_BUILD$3 && debug$4.log(ALREADY_SEEN_ERROR);
			return eventId;
		}
		const hintWithEventId = {
			event_id: eventId,
			...hint
		};
		const sdkProcessingMetadata = event.sdkProcessingMetadata || {};
		const capturedSpanScope = sdkProcessingMetadata.capturedSpanScope;
		const capturedSpanIsolationScope = sdkProcessingMetadata.capturedSpanIsolationScope;
		const dataCategory = getDataCategoryByType(event.type);
		this._process(() => this._captureEvent(event, hintWithEventId, capturedSpanScope || currentScope, capturedSpanIsolationScope), dataCategory);
		return hintWithEventId.event_id;
	}
	/**
	* Captures a session.
	*/
	captureSession(session) {
		this.sendSession(session);
		updateSession(session, { init: false });
	}
	/**
	* Get the current Dsn.
	*/
	getDsn() {
		return this._dsn;
	}
	/**
	* Get the current options.
	*/
	getOptions() {
		return this._options;
	}
	/**
	* Get the resolved data collection configuration.
	*/
	getDataCollectionOptions() {
		return this._dataCollection;
	}
	/**
	* Get the SDK metadata.
	* @see SdkMetadata
	*/
	getSdkMetadata() {
		return this._options._metadata;
	}
	/**
	* Returns the transport that is used by the client.
	* Please note that the transport gets lazy initialized so it will only be there once the first event has been sent.
	*/
	getTransport() {
		return this._transport;
	}
	/**
	* Wait for all events to be sent or the timeout to expire, whichever comes first.
	*
	* @param timeout Maximum time in ms the client should wait for events to be flushed. Omitting this parameter will
	*   cause the client to wait until all events are sent before resolving the promise.
	* @returns A promise that will resolve with `true` if all events are sent before the timeout, or `false` if there are
	* still events in the queue when the timeout is reached.
	*/
	async flush(timeout) {
		const transport = this._transport;
		this.emit("flush");
		if (!transport) return true;
		const clientFinished = await this._isClientDoneProcessing(timeout);
		const transportFlushed = await transport.flush(timeout);
		return clientFinished && transportFlushed;
	}
	/**
	* Flush the event queue and set the client to `enabled = false`. See {@link Client.flush}.
	*
	* @param {number} timeout Maximum time in ms the client should wait before shutting down. Omitting this parameter will cause
	*   the client to wait until all events are sent before disabling itself.
	* @returns {Promise<boolean>} A promise which resolves to `true` if the flush completes successfully before the timeout, or `false` if
	* it doesn't.
	*/
	async close(timeout) {
		const result = await this.flush(timeout);
		this.getOptions().enabled = false;
		this.emit("close");
		return result;
	}
	/**
	* Get all installed event processors.
	*/
	getEventProcessors() {
		return this._eventProcessors;
	}
	/**
	* Adds an event processor that applies to any event processed by this client.
	*/
	addEventProcessor(eventProcessor) {
		this._eventProcessors.push(eventProcessor);
	}
	/**
	* Initialize this client.
	* Call this after the client was set on a scope.
	*/
	init() {
		if (this._isEnabled() || this._options.integrations.some(({ name }) => name.startsWith("Spotlight"))) this._setupIntegrations();
	}
	/**
	* Gets an installed integration by its name.
	*
	* @returns {Integration|undefined} The installed integration or `undefined` if no integration with that `name` was installed.
	*/
	getIntegrationByName(integrationName) {
		return this._integrations[integrationName];
	}
	/**
	* Returns the names of all installed integrations.
	*/
	getIntegrationNames() {
		return Object.keys(this._integrations);
	}
	/**
	* Add an integration to the client.
	* This can be used to e.g. lazy load integrations.
	* In most cases, this should not be necessary,
	* and you're better off just passing the integrations via `integrations: []` at initialization time.
	* However, if you find the need to conditionally load & add an integration, you can use `addIntegration` to do so.
	*/
	addIntegration(integration) {
		const isAlreadyInstalled = this._integrations[integration.name];
		if (!isAlreadyInstalled && integration.beforeSetup) integration.beforeSetup(this);
		setupIntegration(this, integration, this._integrations);
		if (!isAlreadyInstalled) afterSetupIntegrations(this, [integration]);
	}
	/**
	* Send a fully prepared event to Sentry.
	*/
	sendEvent(event, hint = {}) {
		this.emit("beforeSendEvent", event, hint);
		const genAiSpanItem = extractGenAiSpansFromEvent(event, this);
		let env = createEventEnvelope(event, this._dsn, this._options._metadata, this._options.tunnel);
		for (const attachment of hint.attachments || []) env = addItemToEnvelope(env, createAttachmentEnvelopeItem(attachment));
		if (genAiSpanItem) env = addItemToEnvelope(env, genAiSpanItem);
		this.sendEnvelope(env).then((sendResponse) => this.emit("afterSendEvent", event, sendResponse));
	}
	/**
	* Send a session or session aggregrates to Sentry.
	*/
	sendSession(session) {
		const { release: clientReleaseOption, environment: clientEnvironmentOption = DEFAULT_ENVIRONMENT } = this._options;
		if ("aggregates" in session) {
			const sessionAttrs = session.attrs || {};
			if (!sessionAttrs.release && !clientReleaseOption) {
				DEBUG_BUILD$3 && debug$4.warn(MISSING_RELEASE_FOR_SESSION_ERROR);
				return;
			}
			sessionAttrs.release = sessionAttrs.release || clientReleaseOption;
			sessionAttrs.environment = sessionAttrs.environment || clientEnvironmentOption;
			session.attrs = sessionAttrs;
		} else {
			if (!session.release && !clientReleaseOption) {
				DEBUG_BUILD$3 && debug$4.warn(MISSING_RELEASE_FOR_SESSION_ERROR);
				return;
			}
			session.release = session.release || clientReleaseOption;
			session.environment = session.environment || clientEnvironmentOption;
		}
		this.emit("beforeSendSession", session);
		const env = createSessionEnvelope(session, this._dsn, this._options._metadata, this._options.tunnel);
		this.sendEnvelope(env);
	}
	/**
	* Record on the client that an event got dropped (ie, an event that will not be sent to Sentry).
	*/
	recordDroppedEvent(reason, category, count = 1) {
		if (this._options.sendClientReports) {
			const key = `${reason}:${category}`;
			DEBUG_BUILD$3 && debug$4.log(`Recording outcome: "${key}"${count > 1 ? ` (${count} times)` : ""}`);
			this._outcomes[key] = (this._outcomes[key] || 0) + count;
		}
	}
	/**
	* Register a hook on this client.
	*/
	on(hook, callback) {
		const hookCallbacks = this._hooks[hook] = this._hooks[hook] || /* @__PURE__ */ new Set();
		const uniqueCallback = (...args) => callback(...args);
		hookCallbacks.add(uniqueCallback);
		return () => {
			hookCallbacks.delete(uniqueCallback);
		};
	}
	/**
	* Emit a hook that was previously registered via `on()`.
	*/
	emit(hook, ...rest) {
		const callbacks = this._hooks[hook];
		if (callbacks) callbacks.forEach((callback) => callback(...rest));
	}
	/**
	* Send an envelope to Sentry.
	*/
	async sendEnvelope(envelope) {
		this.emit("beforeEnvelope", envelope);
		if (this._isEnabled() && this._transport) try {
			return await this._transport.send(envelope);
		} catch (reason) {
			DEBUG_BUILD$3 && debug$4.error("Error while sending envelope:", reason);
			return {};
		}
		DEBUG_BUILD$3 && debug$4.error("Transport disabled");
		return {};
	}
	/**
	* Register a cleanup function to be called when the client is disposed.
	* This is useful for integrations that need to clean up global state.
	*
	* NOTE: This is a no-op in the base `Client` class. Subclasses like `ServerRuntimeClient`
	* override this method to actually register and execute cleanup callbacks.
	*/
	registerCleanup(callback) {}
	/**
	* Disposes of the client and releases all resources.
	*
	* Subclasses should override this method to clean up their own resources, including invoking
	* any callbacks registered via {@link Client.registerCleanup}. The base implementation is a
	* no-op and does NOT execute registered cleanup callbacks.
	*
	* After calling dispose(), the client should not be used anymore.
	*/
	dispose() {}
	/** Setup integrations for this client. */
	_setupIntegrations() {
		const { integrations } = this._options;
		this._integrations = setupIntegrations(this, integrations);
		afterSetupIntegrations(this, integrations);
	}
	/** Updates existing session based on the provided event */
	_updateSessionFromEvent(session, event) {
		let crashed = event.level === "fatal";
		let errored = false;
		const exceptions = event.exception?.values;
		if (exceptions) {
			errored = true;
			crashed = false;
			for (const ex of exceptions) if (ex.mechanism?.handled === false) {
				crashed = true;
				break;
			}
		}
		const sessionNonTerminal = session.status === "ok";
		if (sessionNonTerminal && session.errors === 0 || sessionNonTerminal && crashed) {
			updateSession(session, {
				...crashed && { status: "crashed" },
				errors: session.errors || Number(errored || crashed)
			});
			this.captureSession(session);
		}
	}
	/**
	* Determine if the client is finished processing. Returns a promise because it will wait `timeout` ms before saying
	* "no" (resolving to `false`) in order to give the client a chance to potentially finish first.
	*
	* @param timeout The time, in ms, after which to resolve to `false` if the client is still busy. Passing `0` (or not
	* passing anything) will make the promise wait as long as it takes for processing to finish before resolving to
	* `true`.
	* @returns A promise which will resolve to `true` if processing is already done or finishes before the timeout, and
	* `false` otherwise
	*/
	async _isClientDoneProcessing(timeout) {
		let ticked = 0;
		while (!timeout || ticked < timeout) {
			await new Promise((resolve) => setTimeout(resolve, 1));
			if (!this._numProcessing) return true;
			ticked++;
		}
		return false;
	}
	/** Determines whether this SDK is enabled and a transport is present. */
	_isEnabled() {
		return this.getOptions().enabled !== false && this._transport !== void 0;
	}
	/**
	* Adds common information to events.
	*
	* The information includes release and environment from `options`,
	* breadcrumbs and context (extra, tags and user) from the scope.
	*
	* Information that is already present in the event is never overwritten. For
	* nested objects, such as the context, keys are merged.
	*
	* @param event The original event.
	* @param hint May contain additional information about the original exception.
	* @param currentScope A scope containing event metadata.
	* @returns A new event with more information.
	*/
	_prepareEvent(event, hint, currentScope, isolationScope) {
		const options = this.getOptions();
		const integrations = this.getIntegrationNames();
		if (!hint.integrations && integrations.length) hint.integrations = integrations;
		this.emit("preprocessEvent", event, hint);
		if (!event.type) isolationScope.setLastEventId(event.event_id || hint.event_id);
		return prepareEvent(options, event, hint, currentScope, this, isolationScope).then((evt) => {
			if (evt === null) return evt;
			this.emit("postprocessEvent", evt, hint);
			evt.contexts = {
				trace: {
					...evt.contexts?.trace,
					...getTraceContextFromScope(currentScope)
				},
				...evt.contexts
			};
			evt.sdkProcessingMetadata = {
				dynamicSamplingContext: getDynamicSamplingContextFromScope(this, currentScope),
				...evt.sdkProcessingMetadata
			};
			return evt;
		});
	}
	/**
	* Processes the event and logs an error in case of rejection
	* @param event
	* @param hint
	* @param scope
	*/
	_captureEvent(event, hint = {}, currentScope = getCurrentScope(), isolationScope = getIsolationScope()) {
		if (DEBUG_BUILD$3 && isErrorEvent(event)) debug$4.log(`Captured error event \`${getPossibleEventMessages(event)[0] || "<unknown>"}\``);
		return this._processEvent(event, hint, currentScope, isolationScope).then((finalEvent) => {
			return finalEvent.event_id;
		}, (reason) => {
			if (DEBUG_BUILD$3) {
				if (_isDoNotSendEventError(reason)) debug$4.log(reason.message);
				else if (_isInternalError(reason)) debug$4.warn(reason.message);
				else debug$4.warn(reason);
			}
		});
	}
	/**
	* Processes an event (either error or message) and sends it to Sentry.
	*
	* This also adds breadcrumbs and context information to the event. However,
	* platform specific meta data (such as the User's IP address) must be added
	* by the SDK implementor.
	*
	*
	* @param event The event to send to Sentry.
	* @param hint May contain additional information about the original exception.
	* @param currentScope A scope containing event metadata.
	* @returns A SyncPromise that resolves with the event or rejects in case event was/will not be send.
	*/
	_processEvent(event, hint, currentScope, isolationScope) {
		const options = this.getOptions();
		const { sampleRate } = options;
		const isTransaction = isTransactionEvent(event);
		const isError = isErrorEvent(event);
		const beforeSendLabel = `before send for type \`${event.type || "error"}\``;
		const parsedSampleRate = typeof sampleRate === "undefined" ? void 0 : parseSampleRate(sampleRate);
		if (isError && typeof parsedSampleRate === "number" && safeMathRandom() > parsedSampleRate) {
			this.recordDroppedEvent("sample_rate", "error");
			return rejectedSyncPromise(_makeDoNotSendEventError(`Discarding event because it's not included in the random sample (sampling rate = ${sampleRate})`));
		}
		const dataCategory = getDataCategoryByType(event.type);
		return this._prepareEvent(event, hint, currentScope, isolationScope).then((prepared) => {
			if (prepared === null) {
				this.recordDroppedEvent("event_processor", dataCategory);
				throw _makeDoNotSendEventError("An event processor returned `null`, will not send event.");
			}
			if (hint.data?.__sentry__ === true) return prepared;
			return _validateBeforeSendResult(processBeforeSend(this, options, prepared, hint), beforeSendLabel);
		}).then((processedEvent) => {
			if (processedEvent === null) {
				this.recordDroppedEvent("before_send", dataCategory);
				if (isTransaction) {
					const spanCount = 1 + (event.spans || []).length;
					this.recordDroppedEvent("before_send", "span", spanCount);
				}
				throw _makeDoNotSendEventError(`${beforeSendLabel} returned \`null\`, will not send event.`);
			}
			const session = currentScope.getSession() || isolationScope.getSession();
			if (isError && session) this._updateSessionFromEvent(session, processedEvent);
			if (isTransaction) {
				const droppedSpanCount = (processedEvent.sdkProcessingMetadata?.spanCountBeforeProcessing || 0) - (processedEvent.spans ? processedEvent.spans.length : 0);
				if (droppedSpanCount > 0) this.recordDroppedEvent("before_send", "span", droppedSpanCount);
			}
			const transactionInfo = processedEvent.transaction_info;
			if (isTransaction && transactionInfo && processedEvent.transaction !== event.transaction) {
				const source = "custom";
				processedEvent.transaction_info = {
					...transactionInfo,
					source
				};
			}
			this.sendEvent(processedEvent, hint);
			return processedEvent;
		}).then(null, (reason) => {
			if (_isDoNotSendEventError(reason) || _isInternalError(reason)) throw reason;
			this.captureException(reason, {
				mechanism: {
					handled: false,
					type: "internal"
				},
				data: { __sentry__: true },
				originalException: reason
			});
			throw _makeInternalError(`Event processing pipeline threw an error, original event will not be sent. Details have been sent as a new event.
Reason: ${reason}`);
		});
	}
	/**
	* Occupies the client with processing and event
	*/
	_process(taskProducer, dataCategory) {
		this._numProcessing++;
		this._promiseBuffer.add(taskProducer).then((value) => {
			this._numProcessing--;
			return value;
		}, (reason) => {
			this._numProcessing--;
			if (reason === SENTRY_BUFFER_FULL_ERROR) this.recordDroppedEvent("queue_overflow", dataCategory);
			return reason;
		});
	}
	/**
	* Clears outcomes on this client and returns them.
	*/
	_clearOutcomes() {
		const outcomes = this._outcomes;
		this._outcomes = {};
		return Object.entries(outcomes).map(([key, quantity]) => {
			const [reason, category] = key.split(":");
			return {
				reason,
				category,
				quantity
			};
		});
	}
	/**
	* Sends client reports as an envelope.
	*/
	_flushOutcomes() {
		DEBUG_BUILD$3 && debug$4.log("Flushing outcomes...");
		const outcomes = this._clearOutcomes();
		if (outcomes.length === 0) {
			DEBUG_BUILD$3 && debug$4.log("No outcomes to send");
			return;
		}
		if (!this._dsn) {
			DEBUG_BUILD$3 && debug$4.log("No dsn provided, will not send outcomes");
			return;
		}
		DEBUG_BUILD$3 && debug$4.log("Sending outcomes:", outcomes);
		const envelope = createClientReportEnvelope(outcomes, this._options.tunnel && dsnToString(this._dsn));
		this.sendEnvelope(envelope);
	}
};
function getDataCategoryByType(type) {
	return type === "replay_event" ? "replay" : type || "error";
}
function _validateBeforeSendResult(beforeSendResult, beforeSendLabel) {
	const invalidValueError = `${beforeSendLabel} must return \`null\` or a valid event.`;
	if (isThenable(beforeSendResult)) return beforeSendResult.then((event) => {
		if (!isPlainObject(event) && event !== null) throw _makeInternalError(invalidValueError);
		return event;
	}, (e) => {
		throw _makeInternalError(`${beforeSendLabel} rejected with ${e}`);
	});
	else if (!isPlainObject(beforeSendResult) && beforeSendResult !== null) throw _makeInternalError(invalidValueError);
	return beforeSendResult;
}
function processBeforeSend(client, options, event, hint) {
	const { beforeSend, beforeSendTransaction, ignoreSpans } = options;
	const beforeSendSpan = !isStreamedBeforeSendSpanCallback(options.beforeSendSpan) && options.beforeSendSpan;
	let processedEvent = event;
	if (isErrorEvent(processedEvent) && beforeSend) return beforeSend(processedEvent, hint);
	if (isTransactionEvent(processedEvent)) {
		if (beforeSendSpan || ignoreSpans) {
			const rootSpanJson = convertTransactionEventToSpanJson(processedEvent);
			if (ignoreSpans?.length && shouldIgnoreSpan({
				description: rootSpanJson.description,
				op: rootSpanJson.op,
				attributes: rootSpanJson.data
			}, ignoreSpans)) return null;
			if (beforeSendSpan) {
				const processedRootSpanJson = beforeSendSpan(rootSpanJson);
				if (!processedRootSpanJson) showSpanDropWarning();
				else processedEvent = merge(event, convertSpanJsonToTransactionEvent(processedRootSpanJson));
			}
			if (processedEvent.spans) {
				const processedSpans = [];
				const initialSpans = processedEvent.spans;
				for (const span of initialSpans) {
					if (ignoreSpans?.length && shouldIgnoreSpan({
						description: span.description,
						op: span.op,
						attributes: span.data
					}, ignoreSpans)) {
						reparentChildSpans(initialSpans, span);
						continue;
					}
					if (beforeSendSpan) {
						const processedSpan = beforeSendSpan(span);
						if (!processedSpan) {
							showSpanDropWarning();
							processedSpans.push(span);
						} else processedSpans.push(processedSpan);
					} else processedSpans.push(span);
				}
				const droppedSpans = processedEvent.spans.length - processedSpans.length;
				if (droppedSpans) client.recordDroppedEvent("before_send", "span", droppedSpans);
				processedEvent.spans = processedSpans;
			}
		}
		if (beforeSendTransaction) {
			if (processedEvent.spans) {
				const spanCountBefore = processedEvent.spans.length;
				processedEvent.sdkProcessingMetadata = {
					...event.sdkProcessingMetadata,
					spanCountBeforeProcessing: spanCountBefore
				};
			}
			return beforeSendTransaction(processedEvent, hint);
		}
	}
	return processedEvent;
}
function isErrorEvent(event) {
	return event.type === void 0;
}
function isTransactionEvent(event) {
	return event.type === "transaction";
}
function estimateMetricSizeInBytes(metric) {
	let weight = 0;
	if (metric.name) weight += metric.name.length * 2;
	weight += 8;
	return weight + estimateAttributesSizeInBytes(metric.attributes);
}
function estimateLogSizeInBytes(log) {
	let weight = 0;
	if (log.message) weight += log.message.length * 2;
	return weight + estimateAttributesSizeInBytes(log.attributes);
}
function estimateAttributesSizeInBytes(attributes) {
	if (!attributes) return 0;
	let weight = 0;
	Object.values(attributes).forEach((value) => {
		if (Array.isArray(value)) weight += value.length * estimatePrimitiveSizeInBytes(value[0]);
		else if (isPrimitive(value)) weight += estimatePrimitiveSizeInBytes(value);
		else weight += 100;
	});
	return weight;
}
function estimatePrimitiveSizeInBytes(value) {
	if (typeof value === "string") return value.length * 2;
	else if (typeof value === "number") return 8;
	else if (typeof value === "boolean") return 4;
	return 0;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/sdk.js
function initAndBind(clientClass, options) {
	if (options.debug === true) {
		if (DEBUG_BUILD$3) debug$4.enable();
		else consoleSandbox(() => {
			console.warn("[Sentry] Cannot initialize SDK with `debug` option using a non-debug bundle.");
		});
	}
	getCurrentScope().update(options.initialScope);
	const client = new clientClass(options);
	setCurrentClient(client);
	client.init();
	return client;
}
function setCurrentClient(client) {
	getCurrentScope().setClient(client);
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/url.js
function parseUrl(url) {
	if (!url) return {};
	const match = url.match(/^(([^:/?#]+):)?(\/\/([^/?#]*))?([^?#]*)(\?([^#]*))?(#(.*))?$/);
	if (!match) return {};
	const query = match[6] || "";
	const fragment = match[8] || "";
	return {
		host: match[4],
		path: match[5],
		protocol: match[2],
		search: query,
		hash: fragment,
		relative: match[5] + query + fragment
	};
}
function stripDataUrlContent(url, includeDataPrefix = true) {
	if (url.startsWith("data:")) {
		const match = url.match(/^data:([^;,]+)/);
		const mimeType = match ? match[1] : "text/plain";
		const isBase64 = url.includes(";base64,");
		const dataStart = url.indexOf(",");
		let dataPrefix = "";
		if (includeDataPrefix && dataStart !== -1) {
			const data = url.slice(dataStart + 1);
			dataPrefix = data.length > 10 ? `${data.slice(0, 10)}... [truncated]` : data;
		}
		return `data:${mimeType}${isBase64 ? ",base64" : ""}${dataPrefix ? `,${dataPrefix}` : ""}`;
	}
	return url;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/ipAddress.js
function addAutoIpAddressToSession(session) {
	if ("aggregates" in session) {
		if (session.attrs?.["ip_address"] === void 0) session.attrs = {
			...session.attrs,
			ip_address: "{{auto}}"
		};
	} else if (session.ipAddress === void 0) session.ipAddress = "{{auto}}";
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/sdkMetadata.js
function applySdkMetadata(options, name, names = [name], source = "npm") {
	const sdk = (options._metadata = options._metadata || {}).sdk = options._metadata.sdk || {};
	if (!sdk.name) {
		sdk.name = `sentry.javascript.${name}`;
		sdk.packages = names.map((name2) => ({
			name: `${source}:@sentry/${name2}`,
			version: SDK_VERSION
		}));
		sdk.version = SDK_VERSION;
	}
}
//#endregion
//#region node_modules/@sentry/core/build/esm/breadcrumbs.js
var DEFAULT_BREADCRUMBS = 100;
function addBreadcrumb(breadcrumb, hint) {
	const client = getClient();
	const isolationScope = getIsolationScope();
	if (!client) return;
	const { beforeBreadcrumb = null, maxBreadcrumbs = DEFAULT_BREADCRUMBS } = client.getOptions();
	if (maxBreadcrumbs <= 0) return;
	const mergedBreadcrumb = {
		timestamp: dateTimestampInSeconds(),
		...breadcrumb
	};
	const finalBreadcrumb = beforeBreadcrumb ? consoleSandbox(() => beforeBreadcrumb(mergedBreadcrumb, hint)) : mergedBreadcrumb;
	if (finalBreadcrumb === null) return;
	if (client.emit) client.emit("beforeAddBreadcrumb", finalBreadcrumb, hint);
	isolationScope.addBreadcrumb(finalBreadcrumb, maxBreadcrumbs);
}
//#endregion
//#region node_modules/@sentry/core/build/esm/integrations/functiontostring.js
var INTEGRATION_NAME$10 = "FunctionToString";
var SETUP_CLIENTS = /* @__PURE__ */ new WeakMap();
var _functionToStringIntegration = (() => {
	return {
		name: INTEGRATION_NAME$10,
		setupOnce() {
			const originalFunctionToString = Function.prototype.toString;
			try {
				Function.prototype.toString = function(...args) {
					const originalFunction = getOriginalFunction(this);
					let unwrappedFunction;
					try {
						if (SETUP_CLIENTS.has(getClient()) && originalFunction !== void 0) unwrappedFunction = originalFunction;
					} catch {}
					return originalFunctionToString.apply(unwrappedFunction ?? this, args);
				};
			} catch {}
		},
		setup(client) {
			SETUP_CLIENTS.set(client, true);
		}
	};
});
var functionToStringIntegration = defineIntegration(_functionToStringIntegration);
//#endregion
//#region node_modules/@sentry/core/build/esm/integrations/eventFilters.js
var DEFAULT_IGNORE_ERRORS = [
	/^Script error\.?$/,
	/^Javascript error: Script error\.? on line 0$/,
	/^ResizeObserver loop completed with undelivered notifications.$/,
	/^Cannot redefine property: googletag$/,
	/^Can't find variable: gmo$/,
	/^undefined is not an object \(evaluating 'a\.[A-Z]'\)$/,
	/can't redefine non-configurable property "solana"/,
	/vv\(\)\.getRestrictions is not a function/,
	/Can't find variable: _AutofillCallbackHandler/,
	/Object Not Found Matching Id:\d+, MethodName:simulateEvent/,
	/^Java exception was raised during method invocation$/
];
var INTEGRATION_NAME$9 = "EventFilters";
var eventFiltersIntegration = defineIntegration((options = {}) => {
	let mergedOptions;
	return {
		name: INTEGRATION_NAME$9,
		setup(client) {
			mergedOptions = _mergeOptions(options, client.getOptions());
		},
		processEvent(event, _hint, client) {
			if (!mergedOptions) mergedOptions = _mergeOptions(options, client.getOptions());
			return _shouldDropEvent$1(event, mergedOptions) ? null : event;
		}
	};
});
var inboundFiltersIntegration = defineIntegration(((options = {}) => {
	return {
		...eventFiltersIntegration(options),
		name: "InboundFilters"
	};
}));
function _mergeOptions(internalOptions = {}, clientOptions = {}) {
	return {
		allowUrls: [...internalOptions.allowUrls || [], ...clientOptions.allowUrls || []],
		denyUrls: [...internalOptions.denyUrls || [], ...clientOptions.denyUrls || []],
		ignoreErrors: [
			...internalOptions.ignoreErrors || [],
			...clientOptions.ignoreErrors || [],
			...internalOptions.disableErrorDefaults ? [] : DEFAULT_IGNORE_ERRORS
		],
		ignoreTransactions: [...internalOptions.ignoreTransactions || [], ...clientOptions.ignoreTransactions || []]
	};
}
function _shouldDropEvent$1(event, options) {
	if (!event.type) {
		if (_isIgnoredError(event, options.ignoreErrors)) {
			DEBUG_BUILD$3 && debug$4.warn(`Event dropped due to being matched by \`ignoreErrors\` option.
Event: ${getEventDescription(event)}`);
			return true;
		}
		if (_isUselessError(event)) {
			DEBUG_BUILD$3 && debug$4.warn(`Event dropped due to not having an error message, error type or stacktrace.
Event: ${getEventDescription(event)}`);
			return true;
		}
		if (_isDeniedUrl(event, options.denyUrls)) {
			DEBUG_BUILD$3 && debug$4.warn(`Event dropped due to being matched by \`denyUrls\` option.
Event: ${getEventDescription(event)}.
Url: ${_getEventFilterUrl(event)}`);
			return true;
		}
		if (!_isAllowedUrl(event, options.allowUrls)) {
			DEBUG_BUILD$3 && debug$4.warn(`Event dropped due to not being matched by \`allowUrls\` option.
Event: ${getEventDescription(event)}.
Url: ${_getEventFilterUrl(event)}`);
			return true;
		}
	} else if (event.type === "transaction") {
		if (_isIgnoredTransaction(event, options.ignoreTransactions)) {
			DEBUG_BUILD$3 && debug$4.warn(`Event dropped due to being matched by \`ignoreTransactions\` option.
Event: ${getEventDescription(event)}`);
			return true;
		}
	}
	return false;
}
function _isIgnoredError(event, ignoreErrors) {
	if (!ignoreErrors?.length) return false;
	return getPossibleEventMessages(event).some((message) => stringMatchesSomePattern(message, ignoreErrors));
}
function _isIgnoredTransaction(event, ignoreTransactions) {
	if (!ignoreTransactions?.length) return false;
	const name = event.transaction;
	return name ? stringMatchesSomePattern(name, ignoreTransactions) : false;
}
function _isDeniedUrl(event, denyUrls) {
	if (!denyUrls?.length) return false;
	const url = _getEventFilterUrl(event);
	return !url ? false : stringMatchesSomePattern(url, denyUrls);
}
function _isAllowedUrl(event, allowUrls) {
	if (!allowUrls?.length) return true;
	const url = _getEventFilterUrl(event);
	return !url ? true : stringMatchesSomePattern(url, allowUrls);
}
function _getLastValidUrl(frames = []) {
	for (let i = frames.length - 1; i >= 0; i--) {
		const frame = frames[i];
		if (frame && frame.filename !== "<anonymous>" && frame.filename !== "[native code]") return frame.filename || null;
	}
	return null;
}
function _getEventFilterUrl(event) {
	try {
		const frames = [...event.exception?.values ?? []].reverse().find((value) => value.mechanism?.parent_id === void 0 && value.stacktrace?.frames?.length)?.stacktrace?.frames;
		return frames ? _getLastValidUrl(frames) : null;
	} catch {
		DEBUG_BUILD$3 && debug$4.error(`Cannot extract url for event ${getEventDescription(event)}`);
		return null;
	}
}
function _isUselessError(event) {
	if (!event.exception?.values?.length) return false;
	return !event.message && !event.exception.values.some((value) => value.stacktrace || value.type && value.type !== "Error" || value.value);
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/aggregate-errors.js
function applyAggregateErrorsToEvent(exceptionFromErrorImplementation, parser, key, limit, event, hint) {
	if (!event.exception?.values || !hint || !isInstanceOf$1(hint.originalException, Error)) return;
	const originalException = event.exception.values.length > 0 ? event.exception.values[event.exception.values.length - 1] : void 0;
	if (originalException) event.exception.values = aggregateExceptionsFromError(exceptionFromErrorImplementation, parser, limit, hint.originalException, key, event.exception.values, originalException, 0);
}
function aggregateExceptionsFromError(exceptionFromErrorImplementation, parser, limit, error, key, prevExceptions, exception, exceptionId) {
	if (prevExceptions.length >= limit + 1) return prevExceptions;
	let newExceptions = [...prevExceptions];
	if (isInstanceOf$1(error[key], Error)) {
		applyExceptionGroupFieldsForParentException(exception, exceptionId, error);
		const newException = exceptionFromErrorImplementation(parser, error[key]);
		const newExceptionId = newExceptions.length;
		applyExceptionGroupFieldsForChildException(newException, key, newExceptionId, exceptionId);
		newExceptions = aggregateExceptionsFromError(exceptionFromErrorImplementation, parser, limit, error[key], key, [newException, ...newExceptions], newException, newExceptionId);
	}
	if (isExceptionGroup(error)) error.errors.forEach((childError, i) => {
		if (isInstanceOf$1(childError, Error)) {
			applyExceptionGroupFieldsForParentException(exception, exceptionId, error);
			const newException = exceptionFromErrorImplementation(parser, childError);
			const newExceptionId = newExceptions.length;
			applyExceptionGroupFieldsForChildException(newException, `errors[${i}]`, newExceptionId, exceptionId);
			newExceptions = aggregateExceptionsFromError(exceptionFromErrorImplementation, parser, limit, childError, key, [newException, ...newExceptions], newException, newExceptionId);
		}
	});
	return newExceptions;
}
function isExceptionGroup(error) {
	return Array.isArray(error.errors);
}
function applyExceptionGroupFieldsForParentException(exception, exceptionId, error) {
	exception.mechanism = {
		handled: true,
		type: "auto.core.linked_errors",
		...isExceptionGroup(error) && { is_exception_group: true },
		...exception.mechanism,
		exception_id: exceptionId
	};
}
function applyExceptionGroupFieldsForChildException(exception, source, exceptionId, parentId) {
	exception.mechanism = {
		handled: true,
		...exception.mechanism,
		type: "chained",
		source,
		exception_id: exceptionId,
		parent_id: parentId
	};
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/eventbuilder.js
function hasSentryFetchUrlHost(error) {
	return isError$1(error) && "__sentry_fetch_url_host__" in error && typeof error.__sentry_fetch_url_host__ === "string";
}
function _enhanceErrorWithSentryInfo(error) {
	if (hasSentryFetchUrlHost(error)) return `${error.message} (${error.__sentry_fetch_url_host__})`;
	return error.message;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/instrument/console.js
var _filter = /* @__PURE__ */ new Set([]);
function addConsoleInstrumentationHandler(handler) {
	const type = "console";
	const removeHandler = addHandler(type, handler);
	maybeInstrument(type, instrumentConsole);
	return removeHandler;
}
var instrumentedLevels = /* @__PURE__ */ new Set();
function instrumentConsole() {
	if (!("console" in GLOBAL_OBJ)) return;
	CONSOLE_LEVELS$1.forEach(function(level) {
		if (instrumentedLevels.has(level) || !(level in GLOBAL_OBJ.console)) return;
		instrumentedLevels.add(level);
		fill(GLOBAL_OBJ.console, level, function(originalConsoleMethod) {
			originalConsoleMethods[level] = originalConsoleMethod;
			return function(...args) {
				const firstArg = args[0];
				const log = originalConsoleMethods[level];
				const isFiltered = _filter.size && typeof firstArg === "string" && stringMatchesSomePattern(firstArg, _filter);
				if (!isFiltered) triggerHandlers("console", {
					args,
					level
				});
				if (!isFiltered || DEBUG_BUILD$3 && debug$4.isEnabled()) log?.apply(GLOBAL_OBJ.console, args);
			};
		});
	});
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/severity.js
function severityLevelFromString(level) {
	return level === "warn" ? "warning" : [
		"fatal",
		"error",
		"warning",
		"log",
		"info",
		"debug"
	].includes(level) ? level : "log";
}
//#endregion
//#region node_modules/@sentry/core/build/esm/integrations/dedupe.js
var INTEGRATION_NAME$8 = "Dedupe";
var _dedupeIntegration = (() => {
	let previousEvent;
	return {
		name: INTEGRATION_NAME$8,
		processEvent(currentEvent) {
			if (currentEvent.type) return currentEvent;
			try {
				if (_shouldDropEvent(currentEvent, previousEvent)) {
					DEBUG_BUILD$3 && debug$4.warn("Event dropped due to being a duplicate of previously captured event.");
					return null;
				}
			} catch {}
			return previousEvent = currentEvent;
		}
	};
});
var dedupeIntegration = defineIntegration(_dedupeIntegration);
function _shouldDropEvent(currentEvent, previousEvent) {
	if (!previousEvent) return false;
	if (_isSameMessageEvent(currentEvent, previousEvent)) return true;
	if (_isSameExceptionEvent(currentEvent, previousEvent)) return true;
	return false;
}
function _isSameMessageEvent(currentEvent, previousEvent) {
	const currentMessage = currentEvent.message;
	const previousMessage = previousEvent.message;
	if (!currentMessage && !previousMessage) return false;
	if (currentMessage && !previousMessage || !currentMessage && previousMessage) return false;
	if (currentMessage !== previousMessage) return false;
	if (!_isSameFingerprint(currentEvent, previousEvent)) return false;
	if (!_isSameStacktrace(currentEvent, previousEvent)) return false;
	return true;
}
function _isSameExceptionEvent(currentEvent, previousEvent) {
	const previousException = _getExceptionFromEvent(previousEvent);
	const currentException = _getExceptionFromEvent(currentEvent);
	if (!previousException || !currentException) return false;
	if (previousException.type !== currentException.type || previousException.value !== currentException.value) return false;
	if (!_isSameFingerprint(currentEvent, previousEvent)) return false;
	if (!_isSameStacktrace(currentEvent, previousEvent)) return false;
	return true;
}
function _isSameStacktrace(currentEvent, previousEvent) {
	let currentFrames = getFramesFromEvent(currentEvent);
	let previousFrames = getFramesFromEvent(previousEvent);
	if (!currentFrames && !previousFrames) return true;
	if (currentFrames && !previousFrames || !currentFrames && previousFrames) return false;
	currentFrames = currentFrames;
	previousFrames = previousFrames;
	if (previousFrames.length !== currentFrames.length) return false;
	for (let i = 0; i < previousFrames.length; i++) {
		const frameA = previousFrames[i];
		const frameB = currentFrames[i];
		if (frameA.filename !== frameB.filename || frameA.lineno !== frameB.lineno || frameA.colno !== frameB.colno || frameA.function !== frameB.function) return false;
	}
	return true;
}
function _isSameFingerprint(currentEvent, previousEvent) {
	let currentFingerprint = currentEvent.fingerprint;
	let previousFingerprint = previousEvent.fingerprint;
	if (!currentFingerprint && !previousFingerprint) return true;
	if (currentFingerprint && !previousFingerprint || !currentFingerprint && previousFingerprint) return false;
	currentFingerprint = currentFingerprint;
	previousFingerprint = previousFingerprint;
	try {
		return !!(currentFingerprint.join("") === previousFingerprint.join(""));
	} catch {
		return false;
	}
}
function _getExceptionFromEvent(event) {
	return event.exception?.values?.[0];
}
//#endregion
//#region node_modules/@sentry/core/build/esm/integrations/conversationId.js
var INTEGRATION_NAME$7 = "ConversationId";
var _conversationIdIntegration = (() => {
	return {
		name: INTEGRATION_NAME$7,
		setup(client) {
			client.on("spanStart", (span) => {
				const scopeData = getCurrentScope().getScopeData();
				const isolationScopeData = getIsolationScope().getScopeData();
				const conversationId = scopeData.conversationId || isolationScopeData.conversationId;
				if (conversationId) {
					const { op, data: attributes, description: name } = spanToJSON(span);
					if (!op?.startsWith("gen_ai.") && !attributes["ai.operationId"] && !name?.startsWith("ai.")) return;
					span.setAttribute(GEN_AI_CONVERSATION_ID_ATTRIBUTE, conversationId);
				}
			});
		}
	};
});
var conversationIdIntegration = defineIntegration(_conversationIdIntegration);
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/breadcrumb-log-level.js
function getBreadcrumbLogLevelFromHttpStatusCode(statusCode) {
	if (statusCode === void 0) return;
	else if (statusCode >= 400 && statusCode < 500) return "warning";
	else if (statusCode >= 500) return "error";
	else return;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/supports.js
var WINDOW$4 = GLOBAL_OBJ;
function supportsHistory() {
	return "history" in WINDOW$4 && !!WINDOW$4.history;
}
function _isFetchSupported() {
	if (!("fetch" in WINDOW$4)) return false;
	try {
		new Headers();
		new Request("data:,");
		new Response();
		return true;
	} catch {
		return false;
	}
}
function isNativeFunction(func) {
	return func && /^function\s+\w+\(\)\s+\{\s+\[native code\]\s+\}$/.test(func.toString());
}
function supportsNativeFetch() {
	if (typeof EdgeRuntime === "string") return true;
	if (!_isFetchSupported()) return false;
	if (isNativeFunction(WINDOW$4.fetch)) return true;
	let result = false;
	const doc = WINDOW$4.document;
	if (doc && typeof doc.createElement === "function") try {
		const sandbox = doc.createElement("iframe");
		sandbox.hidden = true;
		doc.head.appendChild(sandbox);
		if (sandbox.contentWindow?.fetch) result = isNativeFunction(sandbox.contentWindow.fetch);
		doc.head.removeChild(sandbox);
	} catch (err) {
		DEBUG_BUILD$3 && debug$4.warn("Could not create sandbox iframe for pure fetch check, bailing to window.fetch: ", err);
	}
	return result;
}
//#endregion
//#region node_modules/@sentry/core/build/esm/instrument/fetch.js
function addFetchInstrumentationHandler(handler, skipNativeFetchCheck) {
	const type = "fetch";
	const removeHandler = addHandler(type, handler);
	maybeInstrument(type, () => instrumentFetch(void 0, skipNativeFetchCheck));
	return removeHandler;
}
function instrumentFetch(onFetchResolved, skipNativeFetchCheck = false) {
	if (skipNativeFetchCheck && !supportsNativeFetch()) return;
	fill(GLOBAL_OBJ, "fetch", function(originalFetch) {
		return function(...args) {
			const virtualError = /* @__PURE__ */ new Error();
			const { method, url } = parseFetchArgs(args);
			const handlerData = {
				args,
				fetchData: {
					method,
					url
				},
				startTimestamp: timestampInSeconds() * 1e3,
				virtualError,
				headers: getHeadersFromFetchArgs(args)
			};
			if (!onFetchResolved) triggerHandlers("fetch", { ...handlerData });
			return originalFetch.apply(GLOBAL_OBJ, args).then(async (response) => {
				if (onFetchResolved) onFetchResolved(response);
				else triggerHandlers("fetch", {
					...handlerData,
					endTimestamp: timestampInSeconds() * 1e3,
					response
				});
				return response;
			}, (error) => {
				triggerHandlers("fetch", {
					...handlerData,
					endTimestamp: timestampInSeconds() * 1e3,
					error
				});
				if (isError$1(error) && error.stack === void 0) {
					error.stack = virtualError.stack;
					addNonEnumerableProperty(error, "framesToPop", 1);
				}
				const enhanceOption = getClient()?.getOptions().enhanceFetchErrorMessages ?? "always";
				if (enhanceOption !== false && error instanceof TypeError && (error.message === "Failed to fetch" || error.message === "Load failed" || error.message === "NetworkError when attempting to fetch resource.")) try {
					const hostname = new URL(handlerData.fetchData.url).host;
					if (enhanceOption === "always") error.message = `${error.message} (${hostname})`;
					else addNonEnumerableProperty(error, "__sentry_fetch_url_host__", hostname);
				} catch {}
				throw error;
			});
		};
	});
}
function hasProp(obj, prop) {
	return isObjectLike(obj) && !!obj[prop];
}
function getUrlFromResource(resource) {
	if (typeof resource === "string") return resource;
	if (!resource) return "";
	if (hasProp(resource, "url")) return resource.url;
	if (resource.toString) return resource.toString();
	return "";
}
function parseFetchArgs(fetchArgs) {
	if (fetchArgs.length === 0) return {
		method: "GET",
		url: ""
	};
	if (fetchArgs.length === 2) {
		const [resource, options] = fetchArgs;
		return {
			url: getUrlFromResource(resource),
			method: hasProp(options, "method") ? String(options.method).toUpperCase() : isRequest(resource) && hasProp(resource, "method") ? String(resource.method).toUpperCase() : "GET"
		};
	}
	const arg = fetchArgs[0];
	return {
		url: getUrlFromResource(arg),
		method: hasProp(arg, "method") ? String(arg.method).toUpperCase() : "GET"
	};
}
function getHeadersFromFetchArgs(fetchArgs) {
	const [requestArgument, optionsArgument] = fetchArgs;
	try {
		if (typeof optionsArgument === "object" && optionsArgument !== null && "headers" in optionsArgument && optionsArgument.headers) return new Headers(optionsArgument.headers);
		if (isRequest(requestArgument)) return new Headers(requestArgument.headers);
	} catch {}
}
//#endregion
//#region node_modules/@sentry/core/build/esm/utils/browser.js
var WINDOW$3 = GLOBAL_OBJ;
function getLocationHref() {
	try {
		return WINDOW$3.document.location.href;
	} catch {
		return "";
	}
}
function getComponentName(elem, maxTraverseHeight = 5) {
	if (!WINDOW$3.HTMLElement) return null;
	let currentElem = elem;
	for (let i = 0; i < maxTraverseHeight; i++) {
		if (!currentElem) return null;
		if (currentElem instanceof HTMLElement) {
			if (currentElem.dataset["sentryComponent"]) return currentElem.dataset["sentryComponent"];
			if (currentElem.dataset["sentryElement"]) return currentElem.dataset["sentryElement"];
		}
		currentElem = currentElem.parentNode;
	}
	return null;
}
//#endregion
//#region node_modules/@sentry/feedback/build/npm/esm/index.js
var import_browser = /* @__PURE__ */ __toESM(require_browser(), 1);
var WINDOW$2 = GLOBAL_OBJ;
WINDOW$2.document;
WINDOW$2.navigator;
var n$3;
var l$1;
var u$1;
var i$1$1;
var o$1;
var r$1$1;
var f$1;
var c$1$1 = {};
var s$1 = [];
var a$1 = /acit|ex(?:s|g|n|p|$)|rph|grid|ows|mnc|ntw|ine[ch]|zoo|^ord|itera/i;
var h$1 = Array.isArray;
function v$1(n, l) {
	for (var u in l) n[u] = l[u];
	return n;
}
function p$1(n) {
	var l = n.parentNode;
	l && l.removeChild(n);
}
function d$1(n, t, i, o, r) {
	var f = {
		type: n,
		props: t,
		key: i,
		ref: o,
		__k: null,
		__: null,
		__b: 0,
		__e: null,
		__d: void 0,
		__c: null,
		constructor: void 0,
		__v: null == r ? ++u$1 : r,
		__i: -1,
		__u: 0
	};
	return null == r && null != l$1.vnode && l$1.vnode(f), f;
}
function g$1(n) {
	return n.children;
}
function b$1(n, l) {
	this.props = n, this.context = l;
}
function m$1(n, l) {
	if (null == l) return n.__ ? m$1(n.__, n.__i + 1) : null;
	for (var u; l < n.__k.length; l++) if (null != (u = n.__k[l]) && null != u.__e) return u.__e;
	return "function" == typeof n.type ? m$1(n) : null;
}
function w$1(n, u, t) {
	var i, o = n.__v, r = o.__e, f = n.__P;
	if (f) return (i = v$1({}, o)).__v = o.__v + 1, l$1.vnode && l$1.vnode(i), M$1(f, i, o, n.__n, void 0 !== f.ownerSVGElement, 32 & o.__u ? [r] : null, u, null == r ? m$1(o) : r, !!(32 & o.__u), t), i.__.__k[i.__i] = i, i.__d = void 0, i.__e != r && k$1(i), i;
}
function k$1(n) {
	var l, u;
	if (null != (n = n.__) && null != n.__c) {
		for (n.__e = n.__c.base = null, l = 0; l < n.__k.length; l++) if (null != (u = n.__k[l]) && null != u.__e) {
			n.__e = n.__c.base = u.__e;
			break;
		}
		return k$1(n);
	}
}
function x$1(n) {
	(!n.__d && (n.__d = true) && i$1$1.push(n) && !C$1.__r++ || o$1 !== l$1.debounceRendering) && ((o$1 = l$1.debounceRendering) || r$1$1)(C$1);
}
function C$1() {
	var n, u, t, o = [], r = [];
	for (i$1$1.sort(f$1); n = i$1$1.shift();) n.__d && (t = i$1$1.length, u = w$1(n, o, r) || u, 0 === t || i$1$1.length > t ? (j$1(o, u, r), r.length = o.length = 0, u = void 0, i$1$1.sort(f$1)) : u && l$1.__c && l$1.__c(u, s$1));
	u && j$1(o, u, r), C$1.__r = 0;
}
function P$1(n, l, u, t, i, o, r, f, e, a, h) {
	var v, p, y, d, _, g = t && t.__k || s$1, b = l.length;
	for (u.__d = e, S$1(u, l, g), e = u.__d, v = 0; v < b; v++) null != (y = u.__k[v]) && "boolean" != typeof y && "function" != typeof y && (p = -1 === y.__i ? c$1$1 : g[y.__i] || c$1$1, y.__i = v, M$1(n, y, p, i, o, r, f, e, a, h), d = y.__e, y.ref && p.ref != y.ref && (p.ref && N$1(p.ref, null, y), h.push(y.ref, y.__c || d, y)), null == _ && null != d && (_ = d), 65536 & y.__u || p.__k === y.__k ? e = $$1(y, e, n) : "function" == typeof y.type && void 0 !== y.__d ? e = y.__d : d && (e = d.nextSibling), y.__d = void 0, y.__u &= -196609);
	u.__d = e, u.__e = _;
}
function S$1(n, l, u) {
	var t, i, o, r, f, e = l.length, c = u.length, s = c, a = 0;
	for (n.__k = [], t = 0; t < e; t++) null != (i = n.__k[t] = null == (i = l[t]) || "boolean" == typeof i || "function" == typeof i ? null : "string" == typeof i || "number" == typeof i || "bigint" == typeof i || i.constructor == String ? d$1(null, i, null, null, i) : h$1(i) ? d$1(g$1, { children: i }, null, null, null) : void 0 === i.constructor && i.__b > 0 ? d$1(i.type, i.props, i.key, i.ref ? i.ref : null, i.__v) : i) ? (i.__ = n, i.__b = n.__b + 1, f = I$1(i, u, r = t + a, s), i.__i = f, o = null, -1 !== f && (s--, (o = u[f]) && (o.__u |= 131072)), null == o || null === o.__v ? (-1 == f && a--, "function" != typeof i.type && (i.__u |= 65536)) : f !== r && (f === r + 1 ? a++ : f > r ? s > e - r ? a += f - r : a-- : a = f < r && f == r - 1 ? f - r : 0, f !== t + a && (i.__u |= 65536))) : (o = u[t]) && null == o.key && o.__e && (o.__e == n.__d && (n.__d = m$1(o)), O$1(o, o, false), u[t] = null, s--);
	if (s) for (t = 0; t < c; t++) null != (o = u[t]) && 0 == (131072 & o.__u) && (o.__e == n.__d && (n.__d = m$1(o)), O$1(o, o));
}
function $$1(n, l, u) {
	var t, i;
	if ("function" == typeof n.type) {
		for (t = n.__k, i = 0; t && i < t.length; i++) t[i] && (t[i].__ = n, l = $$1(t[i], l, u));
		return l;
	}
	n.__e != l && (u.insertBefore(n.__e, l || null), l = n.__e);
	do
		l = l && l.nextSibling;
	while (null != l && 8 === l.nodeType);
	return l;
}
function I$1(n, l, u, t) {
	var i = n.key, o = n.type, r = u - 1, f = u + 1, e = l[u];
	if (null === e || e && i == e.key && o === e.type) return u;
	if (t > (null != e && 0 == (131072 & e.__u) ? 1 : 0)) for (; r >= 0 || f < l.length;) {
		if (r >= 0) {
			if ((e = l[r]) && 0 == (131072 & e.__u) && i == e.key && o === e.type) return r;
			r--;
		}
		if (f < l.length) {
			if ((e = l[f]) && 0 == (131072 & e.__u) && i == e.key && o === e.type) return f;
			f++;
		}
	}
	return -1;
}
function T$1(n, l, u) {
	"-" === l[0] ? n.setProperty(l, null == u ? "" : u) : n[l] = null == u ? "" : "number" != typeof u || a$1.test(l) ? u : u + "px";
}
function A$1(n, l, u, t, i) {
	var o;
	n: if ("style" === l) if ("string" == typeof u) n.style.cssText = u;
	else {
		if ("string" == typeof t && (n.style.cssText = t = ""), t) for (l in t) u && l in u || T$1(n.style, l, "");
		if (u) for (l in u) t && u[l] === t[l] || T$1(n.style, l, u[l]);
	}
	else if ("o" === l[0] && "n" === l[1]) o = l !== (l = l.replace(/(PointerCapture)$|Capture$/i, "$1")), l = l.toLowerCase() in n ? l.toLowerCase().slice(2) : l.slice(2), n.l || (n.l = {}), n.l[l + o] = u, u ? t ? u.u = t.u : (u.u = Date.now(), n.addEventListener(l, o ? L$1 : D$1, o)) : n.removeEventListener(l, o ? L$1 : D$1, o);
	else {
		if (i) l = l.replace(/xlink(H|:h)/, "h").replace(/sName$/, "s");
		else if ("width" !== l && "height" !== l && "href" !== l && "list" !== l && "form" !== l && "tabIndex" !== l && "download" !== l && "rowSpan" !== l && "colSpan" !== l && "role" !== l && l in n) try {
			n[l] = null == u ? "" : u;
			break n;
		} catch (n) {}
		"function" == typeof u || (null == u || false === u && "-" !== l[4] ? n.removeAttribute(l) : n.setAttribute(l, u));
	}
}
function D$1(n) {
	if (this.l) {
		var u = this.l[n.type + false];
		if (n.t) {
			if (n.t <= u.u) return;
		} else n.t = Date.now();
		return u(l$1.event ? l$1.event(n) : n);
	}
}
function L$1(n) {
	if (this.l) return this.l[n.type + true](l$1.event ? l$1.event(n) : n);
}
function M$1(n, u, t, i, o, r, f, e, c, s) {
	var a, p, y, d, _, m, w, k, x, C, S, $, H, I, T, A = u.type;
	if (void 0 !== u.constructor) return null;
	128 & t.__u && (c = !!(32 & t.__u), r = [e = u.__e = t.__e]), (a = l$1.__b) && a(u);
	n: if ("function" == typeof A) try {
		if (k = u.props, x = (a = A.contextType) && i[a.__c], C = a ? x ? x.props.value : a.__ : i, t.__c ? w = (p = u.__c = t.__c).__ = p.__E : ("prototype" in A && A.prototype.render ? u.__c = p = new A(k, C) : (u.__c = p = new b$1(k, C), p.constructor = A, p.render = q$1), x && x.sub(p), p.props = k, p.state || (p.state = {}), p.context = C, p.__n = i, y = p.__d = !0, p.__h = [], p._sb = []), p.__s ?? (p.__s = p.state), null != A.getDerivedStateFromProps && (p.__s == p.state && (p.__s = v$1({}, p.__s)), v$1(p.__s, A.getDerivedStateFromProps(k, p.__s))), d = p.props, _ = p.state, p.__v = u, y) null == A.getDerivedStateFromProps && null != p.componentWillMount && p.componentWillMount(), null != p.componentDidMount && p.__h.push(p.componentDidMount);
		else {
			if (null == A.getDerivedStateFromProps && k !== d && null != p.componentWillReceiveProps && p.componentWillReceiveProps(k, C), !p.__e && (null != p.shouldComponentUpdate && !1 === p.shouldComponentUpdate(k, p.__s, C) || u.__v === t.__v)) {
				for (u.__v !== t.__v && (p.props = k, p.state = p.__s, p.__d = !1), u.__e = t.__e, u.__k = t.__k, u.__k.forEach(function(n) {
					n && (n.__ = u);
				}), S = 0; S < p._sb.length; S++) p.__h.push(p._sb[S]);
				p._sb = [], p.__h.length && f.push(p);
				break n;
			}
			null != p.componentWillUpdate && p.componentWillUpdate(k, p.__s, C), null != p.componentDidUpdate && p.__h.push(function() {
				p.componentDidUpdate(d, _, m);
			});
		}
		if (p.context = C, p.props = k, p.__P = n, p.__e = !1, $ = l$1.__r, H = 0, "prototype" in A && A.prototype.render) {
			for (p.state = p.__s, p.__d = !1, $ && $(u), a = p.render(p.props, p.state, p.context), I = 0; I < p._sb.length; I++) p.__h.push(p._sb[I]);
			p._sb = [];
		} else do
			p.__d = !1, $ && $(u), a = p.render(p.props, p.state, p.context), p.state = p.__s;
		while (p.__d && ++H < 25);
		p.state = p.__s, null != p.getChildContext && (i = v$1(v$1({}, i), p.getChildContext())), y || null == p.getSnapshotBeforeUpdate || (m = p.getSnapshotBeforeUpdate(d, _)), P$1(n, h$1(T = null != a && a.type === g$1 && null == a.key ? a.props.children : a) ? T : [T], u, t, i, o, r, f, e, c, s), p.base = u.__e, u.__u &= -161, p.__h.length && f.push(p), w && (p.__E = p.__ = null);
	} catch (n) {
		u.__v = null, c || null != r ? (u.__e = e, u.__u |= c ? 160 : 32, r[r.indexOf(e)] = null) : (u.__e = t.__e, u.__k = t.__k), l$1.__e(n, u, t);
	}
	else null == r && u.__v === t.__v ? (u.__k = t.__k, u.__e = t.__e) : u.__e = z$1(t.__e, u, t, i, o, r, f, c, s);
	(a = l$1.diffed) && a(u);
}
function j$1(n, u, t) {
	for (var i = 0; i < t.length; i++) N$1(t[i], t[++i], t[++i]);
	l$1.__c && l$1.__c(u, n), n.some(function(u) {
		try {
			n = u.__h, u.__h = [], n.some(function(n) {
				n.call(u);
			});
		} catch (n) {
			l$1.__e(n, u.__v);
		}
	});
}
function z$1(l, u, t, i, o, r, f, e, s) {
	var a, v, y, d, _, g, b, w = t.props, k = u.props, x = u.type;
	if ("svg" === x && (o = true), null != r) {
		for (a = 0; a < r.length; a++) if ((_ = r[a]) && "setAttribute" in _ == !!x && (x ? _.localName === x : 3 === _.nodeType)) {
			l = _, r[a] = null;
			break;
		}
	}
	if (null == l) {
		if (null === x) return document.createTextNode(k);
		l = o ? document.createElementNS("http://www.w3.org/2000/svg", x) : document.createElement(x, k.is && k), r = null, e = false;
	}
	if (null === x) w === k || e && l.data === k || (l.data = k);
	else {
		if (r = r && n$3.call(l.childNodes), w = t.props || c$1$1, !e && null != r) for (w = {}, a = 0; a < l.attributes.length; a++) w[(_ = l.attributes[a]).name] = _.value;
		for (a in w) _ = w[a], "children" == a || ("dangerouslySetInnerHTML" == a ? y = _ : "key" === a || a in k || A$1(l, a, null, _, o));
		for (a in k) _ = k[a], "children" == a ? d = _ : "dangerouslySetInnerHTML" == a ? v = _ : "value" == a ? g = _ : "checked" == a ? b = _ : "key" === a || e && "function" != typeof _ || w[a] === _ || A$1(l, a, _, w[a], o);
		if (v) e || y && (v.__html === y.__html || v.__html === l.innerHTML) || (l.innerHTML = v.__html), u.__k = [];
		else if (y && (l.innerHTML = ""), P$1(l, h$1(d) ? d : [d], u, t, i, o && "foreignObject" !== x, r, f, r ? r[0] : t.__k && m$1(t, 0), e, s), null != r) for (a = r.length; a--;) null != r[a] && p$1(r[a]);
		e || (a = "value", void 0 !== g && (g !== l[a] || "progress" === x && !g || "option" === x && g !== w[a]) && A$1(l, a, g, w[a], false), a = "checked", void 0 !== b && b !== l[a] && A$1(l, a, b, w[a], false));
	}
	return l;
}
function N$1(n, u, t) {
	try {
		"function" == typeof n ? n(u) : n.current = u;
	} catch (n) {
		l$1.__e(n, t);
	}
}
function O$1(n, u, t) {
	var i, o;
	if (l$1.unmount && l$1.unmount(n), (i = n.ref) && (i.current && i.current !== n.__e || N$1(i, null, u)), null != (i = n.__c)) {
		if (i.componentWillUnmount) try {
			i.componentWillUnmount();
		} catch (n) {
			l$1.__e(n, u);
		}
		i.base = i.__P = null, n.__c = void 0;
	}
	if (i = n.__k) for (o = 0; o < i.length; o++) i[o] && O$1(i[o], u, t || "function" != typeof n.type);
	t || null == n.__e || p$1(n.__e), n.__ = n.__e = n.__d = void 0;
}
function q$1(n, l, u) {
	return this.constructor(n, u);
}
n$3 = s$1.slice, l$1 = { __e: function(n, l, u, t) {
	for (var i, o, r; l = l.__;) if ((i = l.__c) && !i.__) try {
		if ((o = i.constructor) && null != o.getDerivedStateFromError && (i.setState(o.getDerivedStateFromError(n)), r = i.__d), null != i.componentDidCatch && (i.componentDidCatch(n, t || {}), r = i.__d), r) return i.__E = i;
	} catch (l) {
		n = l;
	}
	throw n;
} }, u$1 = 0, b$1.prototype.setState = function(n, l) {
	var u = null != this.__s && this.__s !== this.state ? this.__s : this.__s = v$1({}, this.state);
	"function" == typeof n && (n = n(v$1({}, u), this.props)), n && v$1(u, n), null != n && this.__v && (l && this._sb.push(l), x$1(this));
}, b$1.prototype.forceUpdate = function(n) {
	this.__v && (this.__e = true, n && this.__h.push(n), x$1(this));
}, b$1.prototype.render = g$1, i$1$1 = [], r$1$1 = "function" == typeof Promise ? Promise.prototype.then.bind(Promise.resolve()) : setTimeout, f$1 = function(n, l) {
	return n.__v.__b - l.__v.__b;
}, C$1.__r = 0;
var r$2;
var u$2;
var i$3;
var f$2 = [];
var c$2 = [];
var e$1 = l$1;
var a$2 = e$1.__b;
var v$2 = e$1.__r;
var l$2 = e$1.diffed;
var m$2 = e$1.__c;
var s$2 = e$1.unmount;
var d$2 = e$1.__;
function j$2() {
	for (var n; n = f$2.shift();) if (n.__P && n.__H) try {
		n.__H.__h.forEach(z$2), n.__H.__h.forEach(B$1), n.__H.__h = [];
	} catch (t) {
		n.__H.__h = [], e$1.__e(t, n.__v);
	}
}
e$1.__b = function(n) {
	r$2 = null, a$2 && a$2(n);
}, e$1.__ = function(n, t) {
	t.__k && t.__k.__m && (n.__m = t.__k.__m), d$2 && d$2(n, t);
}, e$1.__r = function(n) {
	v$2 && v$2(n);
	var i = (r$2 = n.__c).__H;
	i && (u$2 === r$2 ? (i.__h = [], r$2.__h = [], i.__.forEach(function(n) {
		n.__N && (n.__ = n.__N), n.__V = c$2, n.__N = n.i = void 0;
	})) : (i.__h.forEach(z$2), i.__h.forEach(B$1), i.__h = [])), u$2 = r$2;
}, e$1.diffed = function(n) {
	l$2 && l$2(n);
	var t = n.__c;
	t && t.__H && (t.__H.__h.length && (1 !== f$2.push(t) && i$3 === e$1.requestAnimationFrame || ((i$3 = e$1.requestAnimationFrame) || w$2)(j$2)), t.__H.__.forEach(function(n) {
		n.i && (n.__H = n.i), n.__V !== c$2 && (n.__ = n.__V), n.i = void 0, n.__V = c$2;
	})), u$2 = r$2 = null;
}, e$1.__c = function(n, t) {
	t.some(function(n) {
		try {
			n.__h.forEach(z$2), n.__h = n.__h.filter(function(n) {
				return !n.__ || B$1(n);
			});
		} catch (r) {
			t.some(function(n) {
				n.__h && (n.__h = []);
			}), t = [], e$1.__e(r, n.__v);
		}
	}), m$2 && m$2(n, t);
}, e$1.unmount = function(n) {
	s$2 && s$2(n);
	var t, r = n.__c;
	r && r.__H && (r.__H.__.forEach(function(n) {
		try {
			z$2(n);
		} catch (n) {
			t = n;
		}
	}), r.__H = void 0, t && e$1.__e(t, r.__v));
};
var k$2 = "function" == typeof requestAnimationFrame;
function w$2(n) {
	var t, r = function() {
		clearTimeout(u), k$2 && cancelAnimationFrame(t), setTimeout(n);
	}, u = setTimeout(r, 100);
	k$2 && (t = requestAnimationFrame(r));
}
function z$2(n) {
	var t = r$2, u = n.__c;
	"function" == typeof u && (n.__c = void 0, u()), r$2 = t;
}
function B$1(n) {
	var t = r$2;
	n.__c = n.__(), r$2 = t;
}
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/helpers.js
var WINDOW$1 = GLOBAL_OBJ;
var ignoreOnError = 0;
function shouldIgnoreOnError() {
	return ignoreOnError > 0;
}
function ignoreNextOnError() {
	ignoreOnError++;
	setTimeout(() => {
		ignoreOnError--;
	});
}
function wrap(fn, options = {}) {
	function isFunction(fn2) {
		return typeof fn2 === "function";
	}
	if (!isFunction(fn)) return fn;
	try {
		if (Object.prototype.hasOwnProperty.call(fn, "__sentry_wrapped__")) {
			const wrapper = fn.__sentry_wrapped__;
			if (typeof wrapper === "function") return wrapper;
			else return fn;
		}
		if (getOriginalFunction(fn)) return fn;
	} catch {
		return fn;
	}
	const sentryWrapped = function(...args) {
		GLOBAL_OBJ._sentryWrappedDepth = (GLOBAL_OBJ._sentryWrappedDepth || 0) + 1;
		try {
			const wrappedArguments = args.map((arg) => wrap(arg, options));
			return fn.apply(this, wrappedArguments);
		} catch (ex) {
			ignoreNextOnError();
			withScope((scope) => {
				scope.addEventProcessor((event) => {
					if (options.mechanism) {
						addExceptionTypeValue(event, void 0, void 0);
						addExceptionMechanism(event, options.mechanism);
					}
					event.extra = {
						...event.extra,
						arguments: args
					};
					return event;
				});
				captureException(ex);
			});
			throw ex;
		} finally {
			GLOBAL_OBJ._sentryWrappedDepth = (GLOBAL_OBJ._sentryWrappedDepth || 0) - 1;
		}
	};
	try {
		for (const property in fn) if (Object.prototype.hasOwnProperty.call(fn, property)) sentryWrapped[property] = fn[property];
	} catch {}
	markFunctionWrapped(sentryWrapped, fn);
	addNonEnumerableProperty(fn, "__sentry_wrapped__", sentryWrapped);
	try {
		if (Object.getOwnPropertyDescriptor(sentryWrapped, "name").configurable) Object.defineProperty(sentryWrapped, "name", { get() {
			return fn.name;
		} });
	} catch {}
	return sentryWrapped;
}
function getHttpRequestData() {
	const url = getLocationHref();
	const { referrer } = WINDOW$1.document || {};
	const { userAgent } = WINDOW$1.navigator || {};
	return {
		url,
		headers: {
			...referrer && { Referer: referrer },
			...userAgent && { "User-Agent": userAgent }
		}
	};
}
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/eventbuilder.js
function exceptionFromError(stackParser, ex) {
	const frames = parseStackFrames(stackParser, ex);
	const exception = {
		type: extractType(ex),
		value: extractMessage(ex)
	};
	if (frames.length) exception.stacktrace = { frames };
	if (exception.type === void 0 && exception.value === "") exception.value = "Unrecoverable error caught";
	return exception;
}
function eventFromPlainObject(stackParser, exception, syntheticException, isUnhandledRejection) {
	const normalizeDepth = getClient()?.getOptions().normalizeDepth;
	const errorFromProp = getErrorPropertyFromObject(exception);
	const extra = { __serialized__: normalizeToSize(exception, normalizeDepth) };
	if (errorFromProp) return {
		exception: { values: [exceptionFromError(stackParser, errorFromProp)] },
		extra
	};
	const event = {
		exception: { values: [{
			type: isEvent(exception) ? exception.constructor.name : isUnhandledRejection ? "UnhandledRejection" : "Error",
			value: getNonErrorObjectExceptionValue(exception, { isUnhandledRejection })
		}] },
		extra
	};
	if (syntheticException) {
		const frames = parseStackFrames(stackParser, syntheticException);
		if (frames.length) event.exception.values[0].stacktrace = { frames };
	}
	return event;
}
function eventFromError(stackParser, ex) {
	return { exception: { values: [exceptionFromError(stackParser, ex)] } };
}
function parseStackFrames(stackParser, ex) {
	const stacktrace = ex.stacktrace || ex.stack || "";
	const skipLines = getSkipFirstStackStringLines(ex);
	const framesToPop = getPopFirstTopFrames(ex);
	try {
		return stackParser(stacktrace, skipLines, framesToPop);
	} catch {}
	return [];
}
var reactMinifiedRegexp = /Minified React error #\d+;/i;
function getSkipFirstStackStringLines(ex) {
	if (ex && reactMinifiedRegexp.test(ex.message)) return 1;
	return 0;
}
function getPopFirstTopFrames(ex) {
	if (typeof ex.framesToPop === "number") return ex.framesToPop;
	return 0;
}
function isWebAssemblyException(exception) {
	if (typeof WebAssembly !== "undefined" && typeof WebAssembly.Exception !== "undefined") return exception instanceof WebAssembly.Exception;
	else return false;
}
function extractType(ex) {
	const name = ex?.name;
	if (!name && isWebAssemblyException(ex)) return ex.message && Array.isArray(ex.message) && ex.message.length == 2 ? ex.message[0] : "WebAssembly.Exception";
	return name;
}
function extractMessage(ex) {
	const message = ex?.message;
	if (isWebAssemblyException(ex)) {
		if (Array.isArray(ex.message) && ex.message.length == 2) return ex.message[1];
		return "wasm exception";
	}
	if (!message) return "No error message";
	if (message.error && typeof message.error.message === "string") return _enhanceErrorWithSentryInfo(message.error);
	return _enhanceErrorWithSentryInfo(ex);
}
function eventFromException(stackParser, exception, hint, attachStacktrace) {
	const event = eventFromUnknownInput(stackParser, exception, hint?.syntheticException || void 0, attachStacktrace);
	addExceptionMechanism(event);
	event.level = "error";
	if (hint?.event_id) event.event_id = hint.event_id;
	return resolvedSyncPromise(event);
}
function eventFromMessage(stackParser, message, level = "info", hint, attachStacktrace) {
	const event = eventFromString(stackParser, message, hint?.syntheticException || void 0, attachStacktrace);
	event.level = level;
	if (hint?.event_id) event.event_id = hint.event_id;
	return resolvedSyncPromise(event);
}
function eventFromUnknownInput(stackParser, exception, syntheticException, attachStacktrace, isUnhandledRejection) {
	let event;
	if (isErrorEvent$1(exception) && exception.error) return eventFromError(stackParser, exception.error);
	if (isDOMError(exception) || isDOMException(exception)) {
		const domException = exception;
		if ("stack" in exception) {
			event = eventFromError(stackParser, exception);
			const firstException = event.exception?.values?.[0];
			if (attachStacktrace && syntheticException && firstException && !firstException.stacktrace) {
				const frames = parseStackFrames(stackParser, syntheticException);
				if (frames.length) {
					firstException.stacktrace = { frames };
					addExceptionMechanism(event, { synthetic: true });
				}
			}
		} else {
			const name = domException.name || (isDOMError(domException) ? "DOMError" : "DOMException");
			const message = domException.message ? `${name}: ${domException.message}` : name;
			event = eventFromString(stackParser, message, syntheticException, attachStacktrace);
			addExceptionTypeValue(event, message);
		}
		if ("code" in domException) event.tags = {
			...event.tags,
			"DOMException.code": `${domException.code}`
		};
		return event;
	}
	if (isError$1(exception)) return eventFromError(stackParser, exception);
	if (isPlainObject(exception) || isEvent(exception)) {
		event = eventFromPlainObject(stackParser, exception, syntheticException, isUnhandledRejection);
		addExceptionMechanism(event, { synthetic: true });
		return event;
	}
	event = eventFromString(stackParser, exception, syntheticException, attachStacktrace);
	addExceptionTypeValue(event, `${exception}`, void 0);
	addExceptionMechanism(event, { synthetic: true });
	return event;
}
function eventFromString(stackParser, message, syntheticException, attachStacktrace) {
	const event = {};
	if (attachStacktrace && syntheticException) {
		const frames = parseStackFrames(stackParser, syntheticException);
		if (frames.length) event.exception = { values: [{
			value: message,
			stacktrace: { frames }
		}] };
		addExceptionMechanism(event, { synthetic: true });
	}
	if (isParameterizedString(message)) {
		const { __sentry_template_string__, __sentry_template_values__ } = message;
		event.logentry = {
			message: __sentry_template_string__,
			params: __sentry_template_values__
		};
		return event;
	}
	event.message = message;
	return event;
}
function getNonErrorObjectExceptionValue(exception, { isUnhandledRejection }) {
	const keys = extractExceptionKeysForMessage(exception);
	const captureType = isUnhandledRejection ? "promise rejection" : "exception";
	if (isErrorEvent$1(exception)) return `Event \`ErrorEvent\` captured as ${captureType} with message \`${exception.message}\``;
	if (isEvent(exception)) return `Event \`${getObjectClassName(exception)}\` (type=${exception.type}) captured as ${captureType}`;
	return `Object captured as ${captureType} with keys: ${keys}`;
}
function getObjectClassName(obj) {
	try {
		const prototype = Object.getPrototypeOf(obj);
		return prototype ? prototype.constructor.name : void 0;
	} catch {}
}
function getErrorPropertyFromObject(obj) {
	return Object.values(obj).find((v) => v instanceof Error);
}
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/client.js
var BrowserClient = class extends Client {
	/**
	* Creates a new Browser SDK instance.
	*
	* @param options Configuration options for this SDK.
	*/
	constructor(options) {
		const opts = applyDefaultOptions(options);
		applySdkMetadata(opts, "browser", ["browser"], WINDOW$1.SENTRY_SDK_SOURCE || getSDKSource());
		super(opts);
		const { userInfo } = this.getDataCollectionOptions();
		if (opts._metadata?.sdk) opts._metadata.sdk.settings = {
			infer_ip: userInfo ? "auto" : "never",
			...opts._metadata.sdk.settings
		};
		const { sendClientReports } = this._options;
		if (WINDOW$1.document) WINDOW$1.document.addEventListener("visibilitychange", () => {
			if (WINDOW$1.document.visibilityState === "hidden") {
				if (sendClientReports) this._flushOutcomes();
				queueMicrotask(() => {
					this.flush();
				});
			}
		});
		if (userInfo) this.on("beforeSendSession", addAutoIpAddressToSession);
	}
	/**
	* @inheritDoc
	*/
	eventFromException(exception, hint) {
		return eventFromException(this._options.stackParser, exception, hint, this._options.attachStacktrace);
	}
	/**
	* @inheritDoc
	*/
	eventFromMessage(message, level = "info", hint) {
		return eventFromMessage(this._options.stackParser, message, level, hint, this._options.attachStacktrace);
	}
	/**
	* @inheritDoc
	*/
	_prepareEvent(event, hint, currentScope, isolationScope) {
		event.platform = event.platform || "javascript";
		return super._prepareEvent(event, hint, currentScope, isolationScope);
	}
};
function applyDefaultOptions(optionsArg) {
	return {
		release: typeof __SENTRY_RELEASE__ === "string" ? __SENTRY_RELEASE__ : WINDOW$1.SENTRY_RELEASE?.id,
		sendClientReports: true,
		parentSpanIsAlwaysRootSpan: true,
		...optionsArg
	};
}
//#endregion
//#region node_modules/@sentry/browser-utils/build/esm/debug-build.js
var DEBUG_BUILD$2 = typeof __SENTRY_DEBUG__ === "undefined" || __SENTRY_DEBUG__;
//#endregion
//#region node_modules/@sentry/browser-utils/build/esm/types.js
var WINDOW = GLOBAL_OBJ;
//#endregion
//#region node_modules/@sentry/browser-utils/build/esm/metrics/web-vitals/lib/globalListeners.js
function addPageListener(type, listener, options) {
	if (WINDOW.document) WINDOW.addEventListener(type, listener, options);
}
function removePageListener(type, listener, options) {
	if (WINDOW.document) WINDOW.removeEventListener(type, listener, options);
}
//#endregion
//#region node_modules/@sentry/browser-utils/build/esm/metrics/web-vitals/lib/runOnce.js
var runOnce = (cb) => {
	let called = false;
	return () => {
		if (!called) {
			cb();
			called = true;
		}
	};
};
//#endregion
//#region node_modules/@sentry/browser-utils/build/esm/metrics/web-vitals/lib/whenIdleOrHidden.js
var whenIdleOrHidden = (cb) => {
	const rIC = WINDOW.requestIdleCallback || WINDOW.setTimeout;
	if (WINDOW.document?.visibilityState === "hidden") cb();
	else {
		cb = runOnce(cb);
		addPageListener("visibilitychange", cb, {
			once: true,
			capture: true
		});
		addPageListener("pagehide", cb, {
			once: true,
			capture: true
		});
		rIC(() => {
			cb();
			removePageListener("visibilitychange", cb, { capture: true });
			removePageListener("pagehide", cb, { capture: true });
		});
	}
};
//#endregion
//#region node_modules/@sentry/browser-utils/build/esm/htmlTreeAsString.js
var DEFAULT_MAX_STRING_LENGTH = 80;
var accessors = {};
try {
	if (typeof Node !== "undefined") accessors.parentNode = Object.getOwnPropertyDescriptor(Node.prototype, "parentNode").get;
	if (typeof Element !== "undefined") {
		accessors.tagName = Object.getOwnPropertyDescriptor(Element.prototype, "tagName").get;
		accessors.id = Object.getOwnPropertyDescriptor(Element.prototype, "id").get;
		accessors.className = Object.getOwnPropertyDescriptor(Element.prototype, "className").get;
		accessors.getAttribute = Element.prototype.getAttribute;
	}
	if (typeof HTMLElement !== "undefined") accessors.dataset = Object.getOwnPropertyDescriptor(HTMLElement.prototype, "dataset").get;
} catch {}
function _safeRead(el, prop, arg) {
	const fn = accessors[prop];
	if (fn) try {
		return fn.call(el, arg);
	} catch {}
	const val = el[prop];
	return typeof val === "function" ? val.call(el, arg) : val;
}
function htmlTreeAsString(elem, options = {}) {
	if (!elem) return "<unknown>";
	try {
		let currentElem = elem;
		const MAX_TRAVERSE_HEIGHT = 5;
		const out = [];
		let height = 0;
		let len = 0;
		const separator = " > ";
		const sepLength = 3;
		let nextStr;
		const keyAttrs = Array.isArray(options) ? options : options.keyAttrs;
		const maxStringLength = !Array.isArray(options) && options.maxStringLength || DEFAULT_MAX_STRING_LENGTH;
		while (currentElem && height++ < MAX_TRAVERSE_HEIGHT) {
			nextStr = _htmlElementAsString(currentElem, keyAttrs);
			if (nextStr === "html" || height > 1 && len + out.length * sepLength + nextStr.length >= maxStringLength) break;
			out.push(nextStr);
			len += nextStr.length;
			currentElem = _safeRead(currentElem, "parentNode");
		}
		return out.reverse().join(separator);
	} catch {
		return "<unknown>";
	}
}
function _htmlElementAsString(el, keyAttrs) {
	const out = [];
	const tagName = _safeRead(el, "tagName");
	if (!tagName) return "";
	if (typeof HTMLElement !== "undefined") {
		if (el instanceof HTMLElement) {
			const dataset = _safeRead(el, "dataset");
			if (dataset) {
				if (dataset["sentryComponent"]) return dataset["sentryComponent"];
				if (dataset["sentryElement"]) return dataset["sentryElement"];
			}
		}
	}
	out.push(tagName.toLowerCase());
	const keyAttrPairs = keyAttrs?.length ? keyAttrs.filter((keyAttr) => _safeRead(el, "getAttribute", keyAttr)).map((keyAttr) => [keyAttr, _safeRead(el, "getAttribute", keyAttr)]) : null;
	if (keyAttrPairs?.length) keyAttrPairs.forEach((keyAttrPair) => {
		out.push(`[${keyAttrPair[0]}="${keyAttrPair[1]}"]`);
	});
	else {
		const id = _safeRead(el, "id");
		if (id) out.push(`#${id}`);
		const className = _safeRead(el, "className");
		if (className && isString(className)) {
			const classes = className.split(/\s+/);
			for (const c of classes) out.push(`.${c}`);
		}
	}
	for (const k of [
		"aria-label",
		"type",
		"name",
		"title",
		"alt"
	]) {
		const attr = _safeRead(el, "getAttribute", k);
		if (attr) out.push(`[${k}="${attr}"]`);
	}
	return out.join("");
}
//#endregion
//#region node_modules/@sentry/browser-utils/build/esm/instrument/dom.js
var DEBOUNCE_DURATION = 1e3;
var debounceTimerID;
var lastCapturedEventType;
var lastCapturedEventTargetId;
function addClickKeypressInstrumentationHandler(handler) {
	const type = "dom";
	addHandler(type, handler);
	maybeInstrument(type, instrumentDOM);
}
function instrumentDOM() {
	if (!WINDOW.document) return;
	const triggerDOMHandler = triggerHandlers.bind(null, "dom");
	const globalDOMEventHandler = makeDOMEventHandler(triggerDOMHandler, true);
	WINDOW.document.addEventListener("click", globalDOMEventHandler, false);
	WINDOW.document.addEventListener("keypress", globalDOMEventHandler, false);
	["EventTarget", "Node"].forEach((target) => {
		const proto = WINDOW[target]?.prototype;
		if (!proto?.hasOwnProperty?.("addEventListener")) return;
		fill(proto, "addEventListener", function(originalAddEventListener) {
			return function(type, listener, options) {
				if (type === "click" || type == "keypress") try {
					const handlers = this.__sentry_instrumentation_handlers__ = this.__sentry_instrumentation_handlers__ || {};
					const handlerForType = handlers[type] = handlers[type] || { refCount: 0 };
					if (!handlerForType.handler) {
						const handler = makeDOMEventHandler(triggerDOMHandler);
						handlerForType.handler = handler;
						originalAddEventListener.call(this, type, handler, options);
					}
					handlerForType.refCount++;
				} catch {}
				return originalAddEventListener.call(this, type, listener, options);
			};
		});
		fill(proto, "removeEventListener", function(originalRemoveEventListener) {
			return function(type, listener, options) {
				if (type === "click" || type == "keypress") try {
					const handlers = this.__sentry_instrumentation_handlers__ || {};
					const handlerForType = handlers[type];
					if (handlerForType) {
						handlerForType.refCount--;
						if (handlerForType.refCount <= 0) {
							originalRemoveEventListener.call(this, type, handlerForType.handler, options);
							handlerForType.handler = void 0;
							delete handlers[type];
						}
						if (Object.keys(handlers).length === 0) delete this.__sentry_instrumentation_handlers__;
					}
				} catch {}
				return originalRemoveEventListener.call(this, type, listener, options);
			};
		});
	});
}
function isSimilarToLastCapturedEvent(event) {
	if (event.type !== lastCapturedEventType) return false;
	try {
		if (!event.target || event.target._sentryId !== lastCapturedEventTargetId) return false;
	} catch {}
	return true;
}
function shouldSkipDOMEvent(eventType, target) {
	if (eventType !== "keypress") return false;
	if (!target?.tagName) return true;
	if (target.tagName === "INPUT" || target.tagName === "TEXTAREA" || target.isContentEditable) return false;
	return true;
}
function makeDOMEventHandler(handler, globalListener = false) {
	return (event) => {
		if (!event || event["_sentryCaptured"]) return;
		const target = getEventTarget$1(event);
		if (shouldSkipDOMEvent(event.type, target)) return;
		addNonEnumerableProperty(event, "_sentryCaptured", true);
		if (target && !target._sentryId) addNonEnumerableProperty(target, "_sentryId", uuid4());
		const name = event.type === "keypress" ? "input" : event.type;
		if (!isSimilarToLastCapturedEvent(event)) {
			handler({
				event,
				name,
				global: globalListener
			});
			lastCapturedEventType = event.type;
			lastCapturedEventTargetId = target ? target._sentryId : void 0;
		}
		clearTimeout(debounceTimerID);
		debounceTimerID = WINDOW.setTimeout(() => {
			lastCapturedEventTargetId = void 0;
			lastCapturedEventType = void 0;
		}, DEBOUNCE_DURATION);
	};
}
function getEventTarget$1(event) {
	try {
		return event.target;
	} catch {
		return null;
	}
}
//#endregion
//#region node_modules/@sentry/browser-utils/build/esm/instrument/history.js
var lastHref;
function addHistoryInstrumentationHandler(handler) {
	const type = "history";
	addHandler(type, handler);
	maybeInstrument(type, instrumentHistory);
}
function instrumentHistory() {
	WINDOW.addEventListener("popstate", () => {
		const to = WINDOW.location.href;
		const from = lastHref;
		lastHref = to;
		if (from === to) return;
		triggerHandlers("history", {
			from,
			to
		});
	});
	if (!supportsHistory()) return;
	function historyReplacementFunction(originalHistoryFunction) {
		return function(...args) {
			const url = args.length > 2 ? args[2] : void 0;
			if (url) {
				const from = lastHref;
				const to = getAbsoluteUrl(String(url));
				lastHref = to;
				if (from === to) return originalHistoryFunction.apply(this, args);
				triggerHandlers("history", {
					from,
					to
				});
			}
			return originalHistoryFunction.apply(this, args);
		};
	}
	fill(WINDOW.history, "pushState", historyReplacementFunction);
	fill(WINDOW.history, "replaceState", historyReplacementFunction);
}
function getAbsoluteUrl(urlOrPath) {
	try {
		return new URL(urlOrPath, WINDOW.location.origin).toString();
	} catch {
		return urlOrPath;
	}
}
//#endregion
//#region node_modules/@sentry/browser-utils/build/esm/getNativeImplementation.js
var cachedImplementations$3 = {};
function getNativeImplementation(name) {
	const cached = cachedImplementations$3[name];
	if (cached) return cached;
	let impl = WINDOW[name];
	if (isNativeFunction(impl)) return cachedImplementations$3[name] = impl.bind(WINDOW);
	const document = WINDOW.document;
	if (document && typeof document.createElement === "function") try {
		const sandbox = document.createElement("iframe");
		sandbox.hidden = true;
		document.head.appendChild(sandbox);
		const contentWindow = sandbox.contentWindow;
		if (contentWindow?.[name]) impl = contentWindow[name];
		document.head.removeChild(sandbox);
	} catch (e) {
		DEBUG_BUILD$2 && debug$4.warn(`Could not create sandbox iframe for ${name} check, bailing to window.${name}: `, e);
	}
	if (!impl) return impl;
	return cachedImplementations$3[name] = impl.bind(WINDOW);
}
function clearCachedImplementation(name) {
	cachedImplementations$3[name] = void 0;
}
//#endregion
//#region node_modules/@sentry/browser-utils/build/esm/instrument/xhr.js
var SENTRY_XHR_DATA_KEY = "__sentry_xhr_v3__";
function addXhrInstrumentationHandler(handler) {
	const type = "xhr";
	addHandler(type, handler);
	maybeInstrument(type, instrumentXHR);
}
function instrumentXHR() {
	if (!WINDOW.XMLHttpRequest) return;
	const xhrproto = XMLHttpRequest.prototype;
	xhrproto.open = new Proxy(xhrproto.open, { apply(originalOpen, xhrOpenThisArg, xhrOpenArgArray) {
		const virtualError = /* @__PURE__ */ new Error();
		const startTimestamp = timestampInSeconds() * 1e3;
		const method = isString(xhrOpenArgArray[0]) ? xhrOpenArgArray[0].toUpperCase() : void 0;
		const url = parseXhrUrlArg(xhrOpenArgArray[1]);
		if (!method || !url) return originalOpen.apply(xhrOpenThisArg, xhrOpenArgArray);
		xhrOpenThisArg[SENTRY_XHR_DATA_KEY] = {
			method,
			url,
			request_headers: {}
		};
		if (method === "POST" && url.match(/sentry_key/)) xhrOpenThisArg.__sentry_own_request__ = true;
		const onreadystatechangeHandler = () => {
			const xhrInfo = xhrOpenThisArg[SENTRY_XHR_DATA_KEY];
			if (!xhrInfo) return;
			if (xhrOpenThisArg.readyState === 4) {
				try {
					xhrInfo.status_code = xhrOpenThisArg.status;
				} catch {}
				triggerHandlers("xhr", {
					endTimestamp: timestampInSeconds() * 1e3,
					startTimestamp,
					xhr: xhrOpenThisArg,
					virtualError
				});
				xhrOpenThisArg.removeEventListener("readystatechange", onreadystatechangeHandler);
			}
		};
		if ("onreadystatechange" in xhrOpenThisArg && typeof xhrOpenThisArg.onreadystatechange === "function") xhrOpenThisArg.onreadystatechange = new Proxy(xhrOpenThisArg.onreadystatechange, { apply(originalOnreadystatechange, onreadystatechangeThisArg, onreadystatechangeArgArray) {
			onreadystatechangeHandler();
			return originalOnreadystatechange.apply(onreadystatechangeThisArg, onreadystatechangeArgArray);
		} });
		else xhrOpenThisArg.addEventListener("readystatechange", onreadystatechangeHandler);
		xhrOpenThisArg.setRequestHeader = new Proxy(xhrOpenThisArg.setRequestHeader, { apply(originalSetRequestHeader, setRequestHeaderThisArg, setRequestHeaderArgArray) {
			const [header, value] = setRequestHeaderArgArray;
			const xhrInfo = setRequestHeaderThisArg[SENTRY_XHR_DATA_KEY];
			if (xhrInfo && isString(header) && isString(value)) xhrInfo.request_headers[header.toLowerCase()] = value;
			return originalSetRequestHeader.apply(setRequestHeaderThisArg, setRequestHeaderArgArray);
		} });
		return originalOpen.apply(xhrOpenThisArg, xhrOpenArgArray);
	} });
	xhrproto.send = new Proxy(xhrproto.send, { apply(originalSend, sendThisArg, sendArgArray) {
		const sentryXhrData = sendThisArg[SENTRY_XHR_DATA_KEY];
		if (!sentryXhrData) return originalSend.apply(sendThisArg, sendArgArray);
		if (sendArgArray[0] !== void 0) sentryXhrData.body = sendArgArray[0];
		triggerHandlers("xhr", {
			startTimestamp: timestampInSeconds() * 1e3,
			xhr: sendThisArg
		});
		return originalSend.apply(sendThisArg, sendArgArray);
	} });
}
function parseXhrUrlArg(url) {
	if (isString(url)) return url;
	try {
		return url.toString();
	} catch {}
}
//#endregion
//#region node_modules/@sentry/browser-utils/build/esm/is.js
function isElement(wat) {
	if (typeof Element === "undefined") return false;
	try {
		return wat instanceof Element;
	} catch {
		return false;
	}
}
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/transports/fetch.js
var DEFAULT_BROWSER_TRANSPORT_BUFFER_SIZE = 40;
function makeFetchTransport(options, nativeFetch = getNativeImplementation("fetch")) {
	let pendingBodySize = 0;
	let pendingCount = 0;
	async function makeRequest(request) {
		const requestSize = request.body.length;
		pendingBodySize += requestSize;
		pendingCount++;
		const requestOptions = {
			body: request.body,
			method: "POST",
			referrerPolicy: "strict-origin",
			headers: options.headers,
			keepalive: pendingBodySize <= 6e4 && pendingCount < 15,
			...options.fetchOptions
		};
		try {
			const response = await nativeFetch(options.url, requestOptions);
			return {
				statusCode: response.status,
				headers: {
					"x-sentry-rate-limits": response.headers.get("X-Sentry-Rate-Limits"),
					"retry-after": response.headers.get("Retry-After")
				}
			};
		} catch (e) {
			clearCachedImplementation("fetch");
			throw e;
		} finally {
			pendingBodySize -= requestSize;
			pendingCount--;
		}
	}
	return createTransport(options, makeRequest, makePromiseBuffer(options.bufferSize || DEFAULT_BROWSER_TRANSPORT_BUFFER_SIZE));
}
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/debug-build.js
var DEBUG_BUILD$1 = typeof __SENTRY_DEBUG__ === "undefined" || __SENTRY_DEBUG__;
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/stack-parsers.js
var CHROME_PRIORITY = 30;
var GECKO_PRIORITY = 50;
function createFrame(filename, func, lineno, colno) {
	const frame = {
		filename,
		function: func === "<anonymous>" ? "?" : func,
		in_app: true
	};
	if (lineno !== void 0) frame.lineno = lineno;
	if (colno !== void 0) frame.colno = colno;
	return frame;
}
var chromeRegexNoFnName = /^\s*at (\S+?)(?::(\d+))(?::(\d+))\s*$/i;
var chromeRegex = /^\s*at (?:(.+?\)(?: \[.+\])?|.*?) ?\((?:address at )?)?(?:async )?((?:<anonymous>|[-a-z]+:|.*bundle|\/)?.*?)(?::(\d+))?(?::(\d+))?\)?\s*$/i;
var chromeEvalRegex = /\((\S*)(?::(\d+))(?::(\d+))\)/;
var chromeDataUriRegex = /at (.+?) ?\(data:(.+?),/;
var chromeStackParserFn = (line) => {
	const dataUriMatch = line.match(chromeDataUriRegex);
	if (dataUriMatch) return {
		filename: `<data:${dataUriMatch[2]}>`,
		function: dataUriMatch[1]
	};
	const noFnParts = chromeRegexNoFnName.exec(line);
	if (noFnParts) {
		const [, filename, line2, col] = noFnParts;
		return createFrame(filename, "?", +line2, +col);
	}
	const parts = chromeRegex.exec(line);
	if (parts) {
		if (parts[2]?.indexOf("eval") === 0) {
			const subMatch = chromeEvalRegex.exec(parts[2]);
			if (subMatch) {
				parts[2] = subMatch[1];
				parts[3] = subMatch[2];
				parts[4] = subMatch[3];
			}
		}
		const [func, filename] = extractSafariExtensionDetails(parts[1] || "?", parts[2]);
		return createFrame(filename, func, parts[3] ? +parts[3] : void 0, parts[4] ? +parts[4] : void 0);
	}
};
var chromeStackLineParser = [CHROME_PRIORITY, chromeStackParserFn];
var geckoREgex = /^\s*(.*?)(?:\((.*?)\))?(?:^|@)?((?:[-a-z]+)?:\/.*?|\[native code\]|[^@]*(?:bundle|\d+\.js)|\/[\w\-. /=]+)(?::(\d+))?(?::(\d+))?\s*$/i;
var geckoEvalRegex = /(\S+) line (\d+)(?: > eval line \d+)* > eval/i;
var gecko = (line) => {
	const parts = geckoREgex.exec(line);
	if (parts) {
		if (parts[3] && parts[3].indexOf(" > eval") > -1) {
			const subMatch = geckoEvalRegex.exec(parts[3]);
			if (subMatch) {
				parts[1] = parts[1] || "eval";
				parts[3] = subMatch[1];
				parts[4] = subMatch[2];
				parts[5] = "";
			}
		}
		let filename = parts[3];
		let func = parts[1] || "?";
		[func, filename] = extractSafariExtensionDetails(func, filename);
		return createFrame(filename, func, parts[4] ? +parts[4] : void 0, parts[5] ? +parts[5] : void 0);
	}
};
var defaultStackParser = createStackParser(...[chromeStackLineParser, [GECKO_PRIORITY, gecko]]);
var extractSafariExtensionDetails = (func, filename) => {
	const isSafariExtension = func.indexOf("safari-extension") !== -1;
	const isSafariWebExtension = func.indexOf("safari-web-extension") !== -1;
	return isSafariExtension || isSafariWebExtension ? [func.indexOf("@") !== -1 ? func.split("@")[0] : "?", isSafariExtension ? `safari-extension:${filename}` : `safari-web-extension:${filename}`] : [func, filename];
};
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/integrations/breadcrumbs.js
var MAX_ALLOWED_STRING_LENGTH = 1024;
var INTEGRATION_NAME$6 = "Breadcrumbs";
var _breadcrumbsIntegration = ((options = {}) => {
	const _options = {
		console: true,
		dom: true,
		fetch: true,
		history: true,
		sentry: true,
		xhr: true,
		...options
	};
	return {
		name: INTEGRATION_NAME$6,
		setup(client) {
			if (_options.console) addConsoleInstrumentationHandler(_getConsoleBreadcrumbHandler(client));
			if (_options.dom) addClickKeypressInstrumentationHandler(_getDomBreadcrumbHandler(client, _options.dom));
			if (_options.xhr) addXhrInstrumentationHandler(_getXhrBreadcrumbHandler(client));
			if (_options.fetch) addFetchInstrumentationHandler(_getFetchBreadcrumbHandler(client));
			if (_options.history) addHistoryInstrumentationHandler(_getHistoryBreadcrumbHandler(client));
			if (_options.sentry) client.on("beforeSendEvent", _getSentryBreadcrumbHandler(client));
		}
	};
});
var breadcrumbsIntegration = defineIntegration(_breadcrumbsIntegration);
function _getSentryBreadcrumbHandler(client) {
	return function addSentryBreadcrumb(event) {
		if (getClient() !== client) return;
		addBreadcrumb({
			category: `sentry.${event.type === "transaction" ? "transaction" : "event"}`,
			event_id: event.event_id,
			level: event.level,
			message: getEventDescription(event)
		}, { event });
	};
}
function _getDomBreadcrumbHandler(client, dom) {
	return function _innerDomBreadcrumb(handlerData) {
		if (getClient() !== client) return;
		let target;
		let componentName;
		let keyAttrs = typeof dom === "object" ? dom.serializeAttribute : void 0;
		let maxStringLength = typeof dom === "object" && typeof dom.maxStringLength === "number" ? dom.maxStringLength : void 0;
		if (maxStringLength && maxStringLength > MAX_ALLOWED_STRING_LENGTH) {
			DEBUG_BUILD$1 && debug$4.warn(`\`dom.maxStringLength\` cannot exceed ${MAX_ALLOWED_STRING_LENGTH}, but a value of ${maxStringLength} was configured. Sentry will use ${MAX_ALLOWED_STRING_LENGTH} instead.`);
			maxStringLength = MAX_ALLOWED_STRING_LENGTH;
		}
		if (typeof keyAttrs === "string") keyAttrs = [keyAttrs];
		try {
			const event = handlerData.event;
			const element = _isEvent(event) ? event.target : event;
			target = htmlTreeAsString(element, {
				keyAttrs,
				maxStringLength
			});
			componentName = getComponentName(element);
		} catch {
			target = "<unknown>";
		}
		if (target.length === 0) return;
		const breadcrumb = {
			category: `ui.${handlerData.name}`,
			message: target
		};
		if (componentName) breadcrumb.data = { "ui.component_name": componentName };
		addBreadcrumb(breadcrumb, {
			event: handlerData.event,
			name: handlerData.name,
			global: handlerData.global
		});
	};
}
function _getConsoleBreadcrumbHandler(client) {
	return function _consoleBreadcrumb(handlerData) {
		if (getClient() !== client) return;
		const breadcrumb = {
			category: "console",
			data: {
				arguments: handlerData.args,
				logger: "console"
			},
			level: severityLevelFromString(handlerData.level),
			message: safeJoin(handlerData.args, " ")
		};
		if (handlerData.level === "assert") {
			if (handlerData.args[0] === false) {
				breadcrumb.message = `Assertion failed: ${safeJoin(handlerData.args.slice(1), " ") || "console.assert"}`;
				breadcrumb.data.arguments = handlerData.args.slice(1);
			} else return;
		}
		addBreadcrumb(breadcrumb, {
			input: handlerData.args,
			level: handlerData.level
		});
	};
}
function _getXhrBreadcrumbHandler(client) {
	return function _xhrBreadcrumb(handlerData) {
		if (getClient() !== client) return;
		const { startTimestamp, endTimestamp } = handlerData;
		const sentryXhrData = handlerData.xhr[SENTRY_XHR_DATA_KEY];
		if (!startTimestamp || !endTimestamp || !sentryXhrData) return;
		const { method, url, status_code, body } = sentryXhrData;
		const data = {
			method,
			url,
			status_code
		};
		const hint = {
			xhr: handlerData.xhr,
			input: body,
			startTimestamp,
			endTimestamp
		};
		const breadcrumb = {
			category: "xhr",
			data,
			type: "http",
			level: getBreadcrumbLogLevelFromHttpStatusCode(status_code)
		};
		client.emit("beforeOutgoingRequestBreadcrumb", breadcrumb, hint);
		addBreadcrumb(breadcrumb, hint);
	};
}
function _getFetchBreadcrumbHandler(client) {
	return function _fetchBreadcrumb(handlerData) {
		if (getClient() !== client) return;
		const { startTimestamp, endTimestamp } = handlerData;
		if (!endTimestamp) return;
		if (handlerData.fetchData.url.match(/sentry_key/) && handlerData.fetchData.method === "POST") return;
		if (handlerData.error) {
			const hint = {
				data: handlerData.error,
				input: handlerData.args,
				startTimestamp,
				endTimestamp
			};
			const breadcrumb = {
				category: "fetch",
				data: handlerData.fetchData,
				level: "error",
				type: "http"
			};
			client.emit("beforeOutgoingRequestBreadcrumb", breadcrumb, hint);
			addBreadcrumb(breadcrumb, hint);
		} else {
			const response = handlerData.response;
			const data = {
				...handlerData.fetchData,
				status_code: response?.status
			};
			const hint = {
				input: handlerData.args,
				response,
				startTimestamp,
				endTimestamp
			};
			const breadcrumb = {
				category: "fetch",
				data,
				type: "http",
				level: getBreadcrumbLogLevelFromHttpStatusCode(data.status_code)
			};
			client.emit("beforeOutgoingRequestBreadcrumb", breadcrumb, hint);
			addBreadcrumb(breadcrumb, hint);
		}
	};
}
function _getHistoryBreadcrumbHandler(client) {
	return function _historyBreadcrumb(handlerData) {
		if (getClient() !== client) return;
		let from = handlerData.from;
		let to = handlerData.to;
		const parsedLoc = parseUrl(WINDOW$1.location.href);
		let parsedFrom = from ? parseUrl(from) : void 0;
		const parsedTo = parseUrl(to);
		if (!parsedFrom?.path) parsedFrom = parsedLoc;
		if (parsedLoc.protocol === parsedTo.protocol && parsedLoc.host === parsedTo.host) to = parsedTo.relative;
		if (parsedLoc.protocol === parsedFrom.protocol && parsedLoc.host === parsedFrom.host) from = parsedFrom.relative;
		addBreadcrumb({
			category: "navigation",
			data: {
				from,
				to
			}
		});
	};
}
function _isEvent(event) {
	return !!event && !!event.target;
}
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/integrations/browserapierrors.js
var DEFAULT_EVENT_TARGET = "EventTarget,Window,Node,ApplicationCache,AudioTrackList,BroadcastChannel,ChannelMergerNode,CryptoOperation,EventSource,FileReader,HTMLUnknownElement,IDBDatabase,IDBRequest,IDBTransaction,KeyOperation,MediaController,MessagePort,ModalWindow,Notification,SVGElementInstance,Screen,SharedWorker,TextTrack,TextTrackCue,TextTrackList,WebSocket,WebSocketWorker,Worker,XMLHttpRequest,XMLHttpRequestEventTarget,XMLHttpRequestUpload".split(",");
var INTEGRATION_NAME$5 = "BrowserApiErrors";
var _browserApiErrorsIntegration = ((options = {}) => {
	const _options = {
		XMLHttpRequest: true,
		eventTarget: true,
		requestAnimationFrame: true,
		setInterval: true,
		setTimeout: true,
		unregisterOriginalCallbacks: false,
		...options
	};
	return {
		name: INTEGRATION_NAME$5,
		setupOnce() {
			if (_options.setTimeout) fill(WINDOW$1, "setTimeout", _wrapTimeFunction);
			if (_options.setInterval) fill(WINDOW$1, "setInterval", _wrapTimeFunction);
			if (_options.requestAnimationFrame) fill(WINDOW$1, "requestAnimationFrame", _wrapRAF);
			if (_options.XMLHttpRequest && "XMLHttpRequest" in WINDOW$1) fill(XMLHttpRequest.prototype, "send", _wrapXHR);
			const eventTargetOption = _options.eventTarget;
			if (eventTargetOption) (Array.isArray(eventTargetOption) ? eventTargetOption : DEFAULT_EVENT_TARGET).forEach((target) => _wrapEventTarget(target, _options));
		}
	};
});
var browserApiErrorsIntegration = defineIntegration(_browserApiErrorsIntegration);
function _wrapTimeFunction(original) {
	return function(...args) {
		const originalCallback = args[0];
		args[0] = wrap(originalCallback, { mechanism: {
			handled: false,
			type: `auto.browser.browserapierrors.${getFunctionName(original)}`
		} });
		return original.apply(this, args);
	};
}
function _wrapRAF(original) {
	return function(callback) {
		return original.apply(this, [wrap(callback, { mechanism: {
			data: { handler: getFunctionName(original) },
			handled: false,
			type: "auto.browser.browserapierrors.requestAnimationFrame"
		} })]);
	};
}
function _wrapXHR(originalSend) {
	return function(...args) {
		const xhr = this;
		[
			"onload",
			"onerror",
			"onprogress",
			"onreadystatechange"
		].forEach((prop) => {
			if (prop in xhr && typeof xhr[prop] === "function") fill(xhr, prop, function(original) {
				const wrapOptions = { mechanism: {
					data: { handler: getFunctionName(original) },
					handled: false,
					type: `auto.browser.browserapierrors.xhr.${prop}`
				} };
				const originalFunction = getOriginalFunction(original);
				if (originalFunction) wrapOptions.mechanism.data.handler = getFunctionName(originalFunction);
				return wrap(original, wrapOptions);
			});
		});
		return originalSend.apply(this, args);
	};
}
function _wrapEventTarget(target, integrationOptions) {
	const proto = WINDOW$1[target]?.prototype;
	if (!proto?.hasOwnProperty?.("addEventListener")) return;
	fill(proto, "addEventListener", function(original) {
		return function(eventName, fn, options) {
			try {
				if (isEventListenerObject(fn)) fn.handleEvent = wrap(fn.handleEvent, { mechanism: {
					data: {
						handler: getFunctionName(fn),
						target
					},
					handled: false,
					type: "auto.browser.browserapierrors.handleEvent"
				} });
			} catch {}
			if (integrationOptions.unregisterOriginalCallbacks) unregisterOriginalCallback(this, eventName, fn);
			return original.apply(this, [
				eventName,
				wrap(fn, { mechanism: {
					data: {
						handler: getFunctionName(fn),
						target
					},
					handled: false,
					type: "auto.browser.browserapierrors.addEventListener"
				} }),
				options
			]);
		};
	});
	fill(proto, "removeEventListener", function(originalRemoveEventListener) {
		return function(eventName, fn, options) {
			try {
				if (Object.prototype.hasOwnProperty.call(fn, "__sentry_wrapped__")) {
					const originalEventHandler = fn.__sentry_wrapped__;
					if (originalEventHandler) originalRemoveEventListener.call(this, eventName, originalEventHandler, options);
				}
			} catch {}
			return originalRemoveEventListener.call(this, eventName, fn, options);
		};
	});
}
function isEventListenerObject(obj) {
	return typeof obj.handleEvent === "function";
}
function unregisterOriginalCallback(target, eventName, fn) {
	if (target && typeof target === "object" && "removeEventListener" in target && typeof target.removeEventListener === "function") target.removeEventListener(eventName, fn);
}
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/integrations/browsersession.js
var browserSessionIntegration = defineIntegration((options = {}) => {
	const lifecycle = options.lifecycle ?? "route";
	return {
		name: "BrowserSession",
		setupOnce() {
			if (typeof WINDOW$1.document === "undefined") {
				DEBUG_BUILD$1 && debug$4.warn("Using the `browserSessionIntegration` in non-browser environments is not supported.");
				return;
			}
			startSession({ ignoreDuration: true });
			let initialSessionSent = false;
			whenIdleOrHidden(() => {
				if (!initialSessionSent) {
					captureSession();
					initialSessionSent = true;
				}
			});
			const isolationScope = getIsolationScope();
			let previousUser = isolationScope.getUser();
			isolationScope.addScopeListener((scope) => {
				const maybeNewUser = scope.getUser();
				if (previousUser?.id !== maybeNewUser?.id || previousUser?.ip_address !== maybeNewUser?.ip_address) {
					previousUser = maybeNewUser;
					if (initialSessionSent) captureSession();
				}
			});
			if (lifecycle === "route") addHistoryInstrumentationHandler(({ from, to }) => {
				if (from !== to) {
					startSession({ ignoreDuration: true });
					captureSession();
					initialSessionSent = true;
				}
			});
		}
	};
});
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/integrations/culturecontext.js
var INTEGRATION_NAME$4 = "CultureContext";
var _cultureContextIntegration = (() => {
	return {
		name: INTEGRATION_NAME$4,
		preprocessEvent(event) {
			const culture = getCultureContext();
			if (culture) event.contexts = {
				...event.contexts,
				culture: {
					...culture,
					...event.contexts?.culture
				}
			};
		},
		processSegmentSpan(span) {
			const culture = getCultureContext();
			if (culture) safeSetSpanJSONAttributes(span, {
				"culture.locale": culture.locale,
				"culture.timezone": culture.timezone,
				"culture.calendar": culture.calendar
			});
		}
	};
});
var cultureContextIntegration = defineIntegration(_cultureContextIntegration);
function getCultureContext() {
	try {
		const intl = WINDOW$1.Intl;
		if (!intl) return;
		const options = intl.DateTimeFormat().resolvedOptions();
		return {
			locale: options.locale,
			timezone: options.timeZone,
			calendar: options.calendar
		};
	} catch {
		return;
	}
}
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/integrations/globalhandlers.js
var INTEGRATION_NAME$3 = "GlobalHandlers";
var _globalHandlersIntegration = ((options = {}) => {
	const _options = {
		onerror: true,
		onunhandledrejection: true,
		...options
	};
	return {
		name: INTEGRATION_NAME$3,
		setupOnce() {
			Error.stackTraceLimit = 50;
		},
		setup(client) {
			if (_options.onerror) {
				_installGlobalOnErrorHandler(client);
				globalHandlerLog("onerror");
			}
			if (_options.onunhandledrejection) {
				_installGlobalOnUnhandledRejectionHandler(client);
				globalHandlerLog("onunhandledrejection");
			}
		}
	};
});
var globalHandlersIntegration = defineIntegration(_globalHandlersIntegration);
function _installGlobalOnErrorHandler(client) {
	addGlobalErrorInstrumentationHandler((data) => {
		const { stackParser, attachStacktrace } = getOptions();
		if (getClient() !== client || shouldIgnoreOnError()) return;
		const { msg, url, line, column, error } = data;
		const event = _enhanceEventWithInitialFrame(eventFromUnknownInput(stackParser, error || msg, void 0, attachStacktrace, false), url, line, column);
		event.level = "error";
		captureEvent(event, {
			originalException: error,
			mechanism: {
				handled: false,
				type: "auto.browser.global_handlers.onerror"
			}
		});
	});
}
function _installGlobalOnUnhandledRejectionHandler(client) {
	addGlobalUnhandledRejectionInstrumentationHandler((e) => {
		const { stackParser, attachStacktrace } = getOptions();
		if (getClient() !== client || shouldIgnoreOnError()) return;
		const error = _getUnhandledRejectionError(e);
		const event = isPrimitive(error) ? _eventFromRejectionWithPrimitive(error) : eventFromUnknownInput(stackParser, error, void 0, attachStacktrace, true);
		event.level = "error";
		captureEvent(event, {
			originalException: error,
			mechanism: {
				handled: false,
				type: "auto.browser.global_handlers.onunhandledrejection"
			}
		});
	});
}
function _getUnhandledRejectionError(error) {
	if (isPrimitive(error)) return error;
	try {
		if ("reason" in error) return error.reason;
		if ("detail" in error && "reason" in error.detail) return error.detail.reason;
	} catch {}
	return error;
}
function _eventFromRejectionWithPrimitive(reason) {
	return { exception: { values: [{
		type: "UnhandledRejection",
		value: `Non-Error promise rejection captured with value: ${String(reason)}`
	}] } };
}
function _enhanceEventWithInitialFrame(event, url, lineno, colno) {
	const e = event.exception = event.exception || {};
	const ev = e.values = e.values || [];
	const ev0 = ev[0] = ev[0] || {};
	const ev0s = ev0.stacktrace = ev0.stacktrace || {};
	const ev0sf = ev0s.frames = ev0s.frames || [];
	if (ev0sf.length === 0) ev0sf.push({
		colno,
		lineno,
		filename: getFilenameFromUrl(url) ?? getLocationHref(),
		function: "?",
		in_app: true
	});
	return event;
}
function globalHandlerLog(type) {
	DEBUG_BUILD$1 && debug$4.log(`Global Handler attached: ${type}`);
}
function getOptions() {
	return getClient()?.getOptions() || {
		stackParser: () => [],
		attachStacktrace: false
	};
}
function getFilenameFromUrl(url) {
	if (!isString(url) || url.length === 0) return;
	if (url.startsWith("data:")) return `<${stripDataUrlContent(url, false)}>`;
	return url;
}
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/integrations/httpcontext.js
var httpContextIntegration = defineIntegration(() => {
	return {
		name: "HttpContext",
		preprocessEvent(event) {
			if (!WINDOW$1.navigator && !WINDOW$1.location && !WINDOW$1.document) return;
			const reqData = getHttpRequestData();
			const headers = {
				...reqData.headers,
				...event.request?.headers
			};
			event.request = {
				...reqData,
				...event.request,
				headers
			};
		},
		processSegmentSpan(span) {
			const spanOp = span.attributes?.[SEMANTIC_ATTRIBUTE_SENTRY_OP];
			if (!WINDOW$1.navigator && !WINDOW$1.location && !WINDOW$1.document) return;
			const reqData = getHttpRequestData();
			safeSetSpanJSONAttributes(span, {
				[Yu]: spanOp !== "http.client" ? reqData.url : void 0,
				"http.request.header.user_agent": reqData.headers["User-Agent"],
				"http.request.header.referer": reqData.headers["Referer"]
			});
		}
	};
});
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/integrations/linkederrors.js
var DEFAULT_KEY = "cause";
var DEFAULT_LIMIT = 5;
var INTEGRATION_NAME$2 = "LinkedErrors";
var _linkedErrorsIntegration = ((options = {}) => {
	const limit = options.limit || DEFAULT_LIMIT;
	const key = options.key || DEFAULT_KEY;
	return {
		name: INTEGRATION_NAME$2,
		preprocessEvent(event, hint, client) {
			applyAggregateErrorsToEvent(exceptionFromError, client.getOptions().stackParser, key, limit, event, hint);
		}
	};
});
var linkedErrorsIntegration = defineIntegration(_linkedErrorsIntegration);
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/integrations/spotlight.js
var INTEGRATION_NAME$1 = "SpotlightBrowser";
var SPOTLIGHT_IGNORE_SPANS = [{
	op: "ui.interaction.click",
	name: "#sentry-spotlight"
}];
var _spotlightIntegration = ((options = {}) => {
	const sidecarUrl = options.sidecarUrl || "http://localhost:8969/stream";
	return {
		name: INTEGRATION_NAME$1,
		setup: () => {
			DEBUG_BUILD$1 && debug$4.log("Using Sidecar URL", sidecarUrl);
		},
		beforeSetup(client) {
			const opts = client.getOptions();
			opts.ignoreSpans = [...opts.ignoreSpans || [], ...SPOTLIGHT_IGNORE_SPANS];
		},
		afterAllSetup: (client) => {
			setupSidecarForwarding(client, sidecarUrl);
		}
	};
});
function setupSidecarForwarding(client, sidecarUrl) {
	const makeFetch = getNativeImplementation("fetch");
	let failCount = 0;
	client.on("beforeEnvelope", (envelope) => {
		if (failCount > 3) {
			debug$4.warn("[Spotlight] Disabled Sentry -> Spotlight integration due to too many failed requests:", failCount);
			return;
		}
		makeFetch(sidecarUrl, {
			method: "POST",
			body: serializeEnvelope(envelope),
			headers: { "Content-Type": "application/x-sentry-envelope" },
			mode: "cors"
		}).then((res) => {
			if (res.status >= 200 && res.status < 400) failCount = 0;
		}, (err) => {
			failCount++;
			debug$4.error("Sentry SDK can't connect to Sidecar is it running? See: https://spotlightjs.com/sidecar/npx/", err);
		});
	});
}
var spotlightBrowserIntegration = defineIntegration(_spotlightIntegration);
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/normalizeStringifyValue.js
var HTML_ELEMENT_CONSTRUCTOR_NAME_REGEX = /^HTML(\w*)Element$/;
function normalizeStringifyValue$1(value) {
	if (typeof window !== "undefined" && value === window) return "[Window]";
	if (typeof document !== "undefined" && value === document) return "[Document]";
	if (isElement(value)) {
		const objName = getConstructorName(value);
		if (HTML_ELEMENT_CONSTRUCTOR_NAME_REGEX.test(objName)) return `[HTMLElement: ${htmlTreeAsString(value)}]`;
	}
}
function getConstructorName(value) {
	const prototype = Object.getPrototypeOf(value);
	return prototype?.constructor ? prototype.constructor.name : "null prototype";
}
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/utils/detectBrowserExtension.js
function checkAndWarnIfIsEmbeddedBrowserExtension() {
	if (_isEmbeddedBrowserExtension()) {
		if (DEBUG_BUILD$1) consoleSandbox(() => {
			console.error("[Sentry] You cannot use Sentry.init() in a browser extension, see: https://docs.sentry.io/platforms/javascript/best-practices/browser-extensions/");
		});
		return true;
	}
	return false;
}
function _isEmbeddedBrowserExtension() {
	if (typeof WINDOW$1.window === "undefined") return false;
	const _window = WINDOW$1;
	if (_window.nw) return false;
	if (!(_window["chrome"] || _window["browser"])?.runtime?.id) return false;
	const href = getLocationHref();
	return !(WINDOW$1 === WINDOW$1.top && /^(?:chrome-extension|moz-extension|ms-browser-extension|safari-web-extension):\/\//.test(href));
}
//#endregion
//#region node_modules/@sentry/browser/build/npm/esm/dev/sdk.js
function getDefaultIntegrations(_options) {
	return [
		inboundFiltersIntegration(),
		functionToStringIntegration(),
		conversationIdIntegration(),
		browserApiErrorsIntegration(),
		breadcrumbsIntegration(),
		globalHandlersIntegration(),
		linkedErrorsIntegration(),
		dedupeIntegration(),
		httpContextIntegration(),
		cultureContextIntegration(),
		browserSessionIntegration()
	];
}
function init$1(options = {}) {
	const shouldDisableBecauseIsBrowserExtenstion = !options.skipBrowserExtensionCheck && checkAndWarnIfIsEmbeddedBrowserExtension();
	let defaultIntegrations = options.defaultIntegrations == null ? getDefaultIntegrations() : options.defaultIntegrations;
	/*! rollup-include-development-only */
	if (options.spotlight) {
		if (!defaultIntegrations) defaultIntegrations = [];
		const args = typeof options.spotlight === "string" ? { sidecarUrl: options.spotlight } : void 0;
		defaultIntegrations.push(spotlightBrowserIntegration(args));
	}
	/*! rollup-include-development-only-end */
	const clientOptions = {
		...options,
		enabled: shouldDisableBecauseIsBrowserExtenstion ? false : options.enabled,
		stackParser: stackParserFromStackParserOptions(options.stackParser || defaultStackParser),
		integrations: getIntegrationsToSetup({
			integrations: options.integrations,
			defaultIntegrations
		}),
		transport: options.transport || makeFetchTransport
	};
	setNormalizeStringifier(normalizeStringifyValue$1);
	return initAndBind(BrowserClient, clientOptions);
}
//#endregion
//#region node_modules/@sentry/replay/build/npm/esm/index.js
var __defProp$1 = Object.defineProperty;
var __defNormalProp$1 = (obj, key, value) => key in obj ? __defProp$1(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __publicField$1 = (obj, key, value) => __defNormalProp$1(obj, typeof key !== "symbol" ? key + "" : key, value);
var NodeType$1 = /* @__PURE__ */ ((NodeType2) => {
	NodeType2[NodeType2["Document"] = 0] = "Document";
	NodeType2[NodeType2["DocumentType"] = 1] = "DocumentType";
	NodeType2[NodeType2["Element"] = 2] = "Element";
	NodeType2[NodeType2["Text"] = 3] = "Text";
	NodeType2[NodeType2["CDATA"] = 4] = "CDATA";
	NodeType2[NodeType2["Comment"] = 5] = "Comment";
	return NodeType2;
})(NodeType$1 || {});
function isElement$1(n) {
	return n.nodeType === n.ELEMENT_NODE;
}
function isShadowRoot(n) {
	const host = n?.host;
	return Boolean(host?.shadowRoot === n);
}
function isNativeShadowDom(shadowRoot) {
	return Object.prototype.toString.call(shadowRoot) === "[object ShadowRoot]";
}
function fixBrowserCompatibilityIssuesInCSS(cssText) {
	if (cssText.includes(" background-clip: text;") && !cssText.includes(" -webkit-background-clip: text;")) cssText = cssText.replace(/\sbackground-clip:\s*text;/g, " -webkit-background-clip: text; background-clip: text;");
	return cssText;
}
function escapeImportStatement(rule) {
	const { cssText } = rule;
	if (cssText.split("\"").length < 3) return cssText;
	const statement = ["@import", `url(${JSON.stringify(rule.href)})`];
	if (rule.layerName === "") statement.push(`layer`);
	else if (rule.layerName) statement.push(`layer(${rule.layerName})`);
	if (rule.supportsText) statement.push(`supports(${rule.supportsText})`);
	if (rule.media.length) statement.push(rule.media.mediaText);
	return statement.join(" ") + ";";
}
function stringifyStylesheet(s) {
	try {
		const rules = s.rules || s.cssRules;
		return rules ? fixBrowserCompatibilityIssuesInCSS(Array.from(rules, stringifyRule).join("")) : null;
	} catch (error) {
		return null;
	}
}
function fixAllCssProperty(rule) {
	let styles = "";
	for (let i2 = 0; i2 < rule.style.length; i2++) {
		const styleDeclaration = rule.style;
		const attribute = styleDeclaration[i2];
		const isImportant = styleDeclaration.getPropertyPriority(attribute);
		styles += `${attribute}:${styleDeclaration.getPropertyValue(attribute)}${isImportant ? ` !important` : ""};`;
	}
	return `${rule.selectorText} { ${styles} }`;
}
function stringifyRule(rule) {
	let importStringified;
	if (isCSSImportRule(rule)) try {
		importStringified = stringifyStylesheet(rule.styleSheet) || escapeImportStatement(rule);
	} catch (error) {}
	else if (isCSSStyleRule(rule)) {
		let cssText = rule.cssText;
		const needsSafariColonFix = rule.selectorText.includes(":");
		const needsAllFix = typeof rule.style["all"] === "string" && rule.style["all"];
		if (needsAllFix) cssText = fixAllCssProperty(rule);
		if (needsSafariColonFix) cssText = fixSafariColons(cssText);
		if (needsSafariColonFix || needsAllFix) return cssText;
	}
	return importStringified || rule.cssText;
}
function fixSafariColons(cssStringified) {
	return cssStringified.replace(/(\[(?:[\w-]+)[^\\])(:(?:[\w-]+)\])/gm, "$1\\$2");
}
function isCSSImportRule(rule) {
	return "styleSheet" in rule;
}
function isCSSStyleRule(rule) {
	return "selectorText" in rule;
}
var Mirror$1 = class {
	constructor() {
		__publicField$1(this, "idNodeMap", /* @__PURE__ */ new Map());
		__publicField$1(this, "nodeMetaMap", /* @__PURE__ */ new WeakMap());
	}
	getId(n) {
		if (!n) return -1;
		return this.getMeta(n)?.id ?? -1;
	}
	getNode(id) {
		return this.idNodeMap.get(id) || null;
	}
	getIds() {
		return Array.from(this.idNodeMap.keys());
	}
	getMeta(n) {
		return this.nodeMetaMap.get(n) || null;
	}
	removeNodeFromMap(n) {
		const id = this.getId(n);
		this.idNodeMap.delete(id);
		if (n.childNodes) n.childNodes.forEach((childNode) => this.removeNodeFromMap(childNode));
	}
	has(id) {
		return this.idNodeMap.has(id);
	}
	hasNode(node) {
		return this.nodeMetaMap.has(node);
	}
	add(n, meta) {
		const id = meta.id;
		this.idNodeMap.set(id, n);
		this.nodeMetaMap.set(n, meta);
	}
	replace(id, n) {
		const oldNode = this.getNode(id);
		if (oldNode) {
			const meta = this.nodeMetaMap.get(oldNode);
			if (meta) this.nodeMetaMap.set(n, meta);
		}
		this.idNodeMap.set(id, n);
	}
	reset() {
		this.idNodeMap = /* @__PURE__ */ new Map();
		this.nodeMetaMap = /* @__PURE__ */ new WeakMap();
	}
};
function createMirror$1() {
	return new Mirror$1();
}
function shouldMaskInput({ maskInputOptions, tagName, type }) {
	if (tagName === "OPTION") tagName = "SELECT";
	return Boolean(maskInputOptions[tagName.toLowerCase()] || type && maskInputOptions[type] || type === "password" || tagName === "INPUT" && !type && maskInputOptions["text"]);
}
function maskInputValue({ isMasked, element, value, maskInputFn }) {
	let text = value || "";
	if (!isMasked) return text;
	if (maskInputFn) text = maskInputFn(text, element);
	return "*".repeat(text.length);
}
function toLowerCase(str) {
	return str.toLowerCase();
}
function toUpperCase(str) {
	return str.toUpperCase();
}
var ORIGINAL_ATTRIBUTE_NAME = "__rrweb_original__";
function is2DCanvasBlank(canvas) {
	const ctx = canvas.getContext("2d");
	if (!ctx) return true;
	const chunkSize = 50;
	for (let x = 0; x < canvas.width; x += chunkSize) for (let y = 0; y < canvas.height; y += chunkSize) {
		const getImageData = ctx.getImageData;
		const originalGetImageData = ORIGINAL_ATTRIBUTE_NAME in getImageData ? getImageData[ORIGINAL_ATTRIBUTE_NAME] : getImageData;
		if (new Uint32Array(originalGetImageData.call(ctx, x, y, Math.min(chunkSize, canvas.width - x), Math.min(chunkSize, canvas.height - y)).data.buffer).some((pixel) => pixel !== 0)) return false;
	}
	return true;
}
function getInputType(element) {
	const type = element.type;
	return element.hasAttribute("data-rr-is-password") ? "password" : type ? toLowerCase(type) : null;
}
function getInputValue(el, tagName, type) {
	if (tagName === "INPUT" && (type === "radio" || type === "checkbox")) return el.getAttribute("value") || "";
	return el.value;
}
function extractFileExtension(path, baseURL) {
	let url;
	try {
		url = new URL(path, baseURL ?? window.location.href);
	} catch (err) {
		return null;
	}
	return url.pathname.match(/\.([0-9a-z]+)(?:$)/i)?.[1] ?? null;
}
var cachedImplementations$1 = {};
function getImplementation$1(name) {
	const cached = cachedImplementations$1[name];
	if (cached) return cached;
	const document2 = window.document;
	let impl = window[name];
	if (document2 && typeof document2.createElement === "function") try {
		const sandbox = document2.createElement("iframe");
		sandbox.hidden = true;
		document2.head.appendChild(sandbox);
		const contentWindow = sandbox.contentWindow;
		if (contentWindow && contentWindow[name]) impl = contentWindow[name];
		document2.head.removeChild(sandbox);
	} catch (e) {}
	return cachedImplementations$1[name] = impl.bind(window);
}
function setTimeout$1$1(...rest) {
	return getImplementation$1("setTimeout")(...rest);
}
function clearTimeout$1(...rest) {
	return getImplementation$1("clearTimeout")(...rest);
}
function getIFrameContentDocument$1(iframe) {
	try {
		return iframe.contentDocument;
	} catch {}
}
function getIFrameContentWindow$1(iframe) {
	try {
		return iframe.contentWindow;
	} catch {}
}
var _id = 1;
var tagNameRegex = /* @__PURE__ */ new RegExp("[^a-z0-9-_:]");
var IGNORED_NODE = -2;
function genId() {
	return _id++;
}
function getValidTagName(element) {
	if (element instanceof HTMLFormElement) return "form";
	const processedTagName = toLowerCase(element.tagName);
	if (tagNameRegex.test(processedTagName)) return "div";
	return processedTagName;
}
function extractOrigin(url) {
	let origin = "";
	if (url.indexOf("//") > -1) origin = url.split("/").slice(0, 3).join("/");
	else origin = url.split("/")[0];
	origin = origin.split("?")[0];
	return origin;
}
var canvasService;
var canvasCtx;
var URL_IN_CSS_REF = /url\((?:(')([^']*)'|(")(.*?)"|([^)]*))\)/gm;
var URL_PROTOCOL_MATCH = /^(?:[a-z+]+:)?\/\//i;
var URL_WWW_MATCH = /^www\..*/i;
var DATA_URI = /^(data:)([^,]*),(.*)/i;
function filterCSSPropertiesFromInlineStyle(cssText, ignoredProperties) {
	if (!cssText || ignoredProperties.size === 0) return cssText;
	try {
		const properties = cssText.split(";");
		const filteredProperties = [];
		for (let property of properties) {
			property = property.trim();
			if (!property) continue;
			const colonIndex = property.indexOf(":");
			if (colonIndex === -1) {
				filteredProperties.push(property);
				continue;
			}
			const propertyName = property.slice(0, colonIndex).trim();
			if (!ignoredProperties.has(propertyName)) filteredProperties.push(property);
		}
		return filteredProperties.join("; ") + (filteredProperties.length > 0 && cssText.endsWith(";") ? ";" : "");
	} catch (error) {
		console.warn("Error filtering CSS properties:", error);
		return cssText;
	}
}
function absoluteToStylesheet(cssText, href) {
	return (cssText || "").replace(URL_IN_CSS_REF, (origin, quote1, path1, quote2, path2, path3) => {
		const filePath = path1 || path2 || path3;
		const maybeQuote = quote1 || quote2 || "";
		if (!filePath) return origin;
		if (URL_PROTOCOL_MATCH.test(filePath) || URL_WWW_MATCH.test(filePath)) return `url(${maybeQuote}${filePath}${maybeQuote})`;
		if (DATA_URI.test(filePath)) return `url(${maybeQuote}${filePath}${maybeQuote})`;
		if (filePath[0] === "/") return `url(${maybeQuote}${extractOrigin(href) + filePath}${maybeQuote})`;
		const stack = href.split("/");
		const parts = filePath.split("/");
		stack.pop();
		for (const part of parts) if (part === ".") continue;
		else if (part === "..") stack.pop();
		else stack.push(part);
		return `url(${maybeQuote}${stack.join("/")}${maybeQuote})`;
	});
}
var SRCSET_NOT_SPACES = /^[^ \t\n\r\u000c]+/;
var SRCSET_COMMAS_OR_SPACES = /^[, \t\n\r\u000c]+/;
function getAbsoluteSrcsetString(doc, attributeValue) {
	if (attributeValue.trim() === "") return attributeValue;
	let pos = 0;
	function collectCharacters(regEx) {
		let chars2;
		const match = regEx.exec(attributeValue.substring(pos));
		if (match) {
			chars2 = match[0];
			pos += chars2.length;
			return chars2;
		}
		return "";
	}
	const output = [];
	while (true) {
		collectCharacters(SRCSET_COMMAS_OR_SPACES);
		if (pos >= attributeValue.length) break;
		let url = collectCharacters(SRCSET_NOT_SPACES);
		if (url.slice(-1) === ",") {
			url = absoluteToDoc(doc, url.substring(0, url.length - 1));
			output.push(url);
		} else {
			let descriptorsStr = "";
			url = absoluteToDoc(doc, url);
			let inParens = false;
			while (true) {
				const c = attributeValue.charAt(pos);
				if (c === "") {
					output.push((url + descriptorsStr).trim());
					break;
				} else if (!inParens) {
					if (c === ",") {
						pos += 1;
						output.push((url + descriptorsStr).trim());
						break;
					} else if (c === "(") inParens = true;
				} else if (c === ")") inParens = false;
				descriptorsStr += c;
				pos += 1;
			}
		}
	}
	return output.join(", ");
}
var cachedDocument = /* @__PURE__ */ new WeakMap();
function absoluteToDoc(doc, attributeValue) {
	if (!attributeValue || attributeValue.trim() === "") return attributeValue;
	return getHref(doc, attributeValue);
}
function isSVGElement(el) {
	return Boolean(el.tagName === "svg" || el.ownerSVGElement);
}
function getHref(doc, customHref) {
	let a = cachedDocument.get(doc);
	if (!a) {
		a = doc.createElement("a");
		cachedDocument.set(doc, a);
	}
	if (!customHref) customHref = "";
	else if (customHref.startsWith("blob:") || customHref.startsWith("data:")) return customHref;
	a.setAttribute("href", customHref);
	return a.href;
}
function transformAttribute(doc, tagName, name, value, element, maskAttributeFn, ignoreCSSAttributes) {
	if (!value) return value;
	if (name === "src" || name === "href" && !(tagName === "use" && value[0] === "#")) return absoluteToDoc(doc, value);
	else if (name === "xlink:href" && value[0] !== "#") return absoluteToDoc(doc, value);
	else if (name === "background" && (tagName === "table" || tagName === "td" || tagName === "th")) return absoluteToDoc(doc, value);
	else if (name === "srcset") return getAbsoluteSrcsetString(doc, value);
	else if (name === "style") {
		let processedStyle = absoluteToStylesheet(value, getHref(doc));
		if (ignoreCSSAttributes && ignoreCSSAttributes.size > 0) processedStyle = filterCSSPropertiesFromInlineStyle(processedStyle, ignoreCSSAttributes);
		return processedStyle;
	} else if (tagName === "object" && name === "data") return absoluteToDoc(doc, value);
	if (typeof maskAttributeFn === "function") return maskAttributeFn(name, value, element);
	return value;
}
function ignoreAttribute(tagName, name, _value) {
	return (tagName === "video" || tagName === "audio") && name === "autoplay";
}
function _isBlockedElement(element, blockClass, blockSelector, unblockSelector) {
	try {
		if (unblockSelector && element.matches(unblockSelector)) return false;
		if (typeof blockClass === "string") {
			if (element.classList.contains(blockClass)) return true;
		} else for (let eIndex = element.classList.length; eIndex--;) {
			const className = element.classList[eIndex];
			if (blockClass.test(className)) return true;
		}
		if (blockSelector) return element.matches(blockSelector);
	} catch (e) {}
	return false;
}
function elementClassMatchesRegex$1(el, regex) {
	for (let eIndex = el.classList.length; eIndex--;) {
		const className = el.classList[eIndex];
		if (regex.test(className)) return true;
	}
	return false;
}
function distanceToMatch$1(node, matchPredicate, limit = Infinity, distance = 0) {
	if (!node) return -1;
	if (node.nodeType !== node.ELEMENT_NODE) return -1;
	if (distance > limit) return -1;
	if (matchPredicate(node)) return distance;
	return distanceToMatch$1(node.parentNode, matchPredicate, limit, distance + 1);
}
function createMatchPredicate$1(className, selector) {
	return (node) => {
		const el = node;
		if (el === null) return false;
		try {
			if (className) {
				if (typeof className === "string") {
					if (el.matches(`.${className}`)) return true;
				} else if (elementClassMatchesRegex$1(el, className)) return true;
			}
			if (selector && el.matches(selector)) return true;
			return false;
		} catch {
			return false;
		}
	};
}
function needMaskingText(node, maskTextClass, maskTextSelector, unmaskTextClass, unmaskTextSelector, maskAllText) {
	try {
		const el = node.nodeType === node.ELEMENT_NODE ? node : node.parentElement;
		if (el === null) return false;
		if (el.tagName === "INPUT") {
			const autocomplete = el.getAttribute("autocomplete");
			if ([
				"current-password",
				"new-password",
				"cc-number",
				"cc-exp",
				"cc-exp-month",
				"cc-exp-year",
				"cc-csc"
			].includes(autocomplete)) return true;
		}
		let maskDistance = -1;
		let unmaskDistance = -1;
		if (maskAllText) {
			unmaskDistance = distanceToMatch$1(el, createMatchPredicate$1(unmaskTextClass, unmaskTextSelector));
			if (unmaskDistance < 0) return true;
			maskDistance = distanceToMatch$1(el, createMatchPredicate$1(maskTextClass, maskTextSelector), unmaskDistance >= 0 ? unmaskDistance : Infinity);
		} else {
			maskDistance = distanceToMatch$1(el, createMatchPredicate$1(maskTextClass, maskTextSelector));
			if (maskDistance < 0) return false;
			unmaskDistance = distanceToMatch$1(el, createMatchPredicate$1(unmaskTextClass, unmaskTextSelector), maskDistance >= 0 ? maskDistance : Infinity);
		}
		return maskDistance >= 0 ? unmaskDistance >= 0 ? maskDistance <= unmaskDistance : true : unmaskDistance >= 0 ? false : !!maskAllText;
	} catch (e) {}
	return !!maskAllText;
}
function onceIframeLoaded(iframeEl, listener, iframeLoadTimeout) {
	const win = getIFrameContentWindow$1(iframeEl);
	if (!win) return;
	let fired = false;
	let readyState;
	try {
		readyState = win.document.readyState;
	} catch (error) {
		return;
	}
	if (readyState !== "complete") {
		const timer = setTimeout$1$1(() => {
			if (!fired) {
				listener();
				fired = true;
			}
		}, iframeLoadTimeout);
		iframeEl.addEventListener("load", () => {
			clearTimeout$1(timer);
			fired = true;
			listener();
		});
		return;
	}
	const blankUrl = "about:blank";
	if (win.location.href !== blankUrl || iframeEl.src === blankUrl || iframeEl.src === "") {
		setTimeout$1$1(listener, 0);
		return iframeEl.addEventListener("load", listener);
	}
	iframeEl.addEventListener("load", listener);
}
function onceStylesheetLoaded(link, listener, styleSheetLoadTimeout) {
	let fired = false;
	let styleSheetLoaded;
	try {
		styleSheetLoaded = link.sheet;
	} catch (error) {
		styleSheetLoaded = null;
	}
	if (styleSheetLoaded) return;
	const timer = setTimeout$1$1(() => {
		if (!fired) {
			listener();
			fired = true;
		}
	}, styleSheetLoadTimeout);
	link.addEventListener("load", () => {
		clearTimeout$1(timer);
		fired = true;
		listener();
	});
}
function serializeNode(n, options) {
	const { doc, mirror, blockClass, blockSelector, unblockSelector, maskAllText, maskAttributeFn, maskTextClass, unmaskTextClass, maskTextSelector, unmaskTextSelector, inlineStylesheet, maskInputOptions = {}, maskTextFn, maskInputFn, dataURLOptions = {}, inlineImages, recordCanvas, keepIframeSrcFn, newlyAddedElement = false, ignoreCSSAttributes } = options;
	const rootId = getRootId(doc, mirror);
	switch (n.nodeType) {
		case n.DOCUMENT_NODE: if (n.compatMode !== "CSS1Compat") return {
			type: NodeType$1.Document,
			childNodes: [],
			compatMode: n.compatMode
		};
		else return {
			type: NodeType$1.Document,
			childNodes: []
		};
		case n.DOCUMENT_TYPE_NODE: return {
			type: NodeType$1.DocumentType,
			name: n.name,
			publicId: n.publicId,
			systemId: n.systemId,
			rootId
		};
		case n.ELEMENT_NODE: return serializeElementNode(n, {
			doc,
			blockClass,
			blockSelector,
			unblockSelector,
			inlineStylesheet,
			maskAttributeFn,
			maskInputOptions,
			maskInputFn,
			dataURLOptions,
			inlineImages,
			recordCanvas,
			keepIframeSrcFn,
			newlyAddedElement,
			rootId,
			maskTextClass,
			unmaskTextClass,
			maskTextSelector,
			unmaskTextSelector,
			ignoreCSSAttributes
		});
		case n.TEXT_NODE: return serializeTextNode(n, {
			doc,
			maskAllText,
			maskTextClass,
			unmaskTextClass,
			maskTextSelector,
			unmaskTextSelector,
			maskTextFn,
			maskInputOptions,
			maskInputFn,
			rootId
		});
		case n.CDATA_SECTION_NODE: return {
			type: NodeType$1.CDATA,
			textContent: "",
			rootId
		};
		case n.COMMENT_NODE: return {
			type: NodeType$1.Comment,
			textContent: n.textContent || "",
			rootId
		};
		default: return false;
	}
}
function getRootId(doc, mirror) {
	if (!mirror.hasNode(doc)) return void 0;
	const docId = mirror.getId(doc);
	return docId === 1 ? void 0 : docId;
}
function serializeTextNode(n, options) {
	const { maskAllText, maskTextClass, unmaskTextClass, maskTextSelector, unmaskTextSelector, maskTextFn, maskInputOptions, maskInputFn, rootId } = options;
	const parentTagName = n.parentNode && n.parentNode.tagName;
	let textContent = n.textContent;
	const isStyle = parentTagName === "STYLE" ? true : void 0;
	const isScript = parentTagName === "SCRIPT" ? true : void 0;
	const isTextarea = parentTagName === "TEXTAREA" ? true : void 0;
	if (isStyle && textContent) {
		try {
			if (n.nextSibling || n.previousSibling) {} else if (n.parentNode.sheet?.cssRules) textContent = stringifyStylesheet(n.parentNode.sheet);
		} catch (err) {
			console.warn(`Cannot get CSS styles from text's parentNode. Error: ${err}`, n);
		}
		textContent = absoluteToStylesheet(textContent, getHref(options.doc));
	}
	if (isScript) textContent = "SCRIPT_PLACEHOLDER";
	const forceMask = needMaskingText(n, maskTextClass, maskTextSelector, unmaskTextClass, unmaskTextSelector, maskAllText);
	if (!isStyle && !isScript && !isTextarea && textContent && forceMask) textContent = maskTextFn ? maskTextFn(textContent, n.parentElement) : textContent.replace(/[\S]/g, "*");
	if (isTextarea && textContent && (maskInputOptions.textarea || forceMask)) textContent = maskInputFn ? maskInputFn(textContent, n.parentNode) : textContent.replace(/[\S]/g, "*");
	if (parentTagName === "OPTION" && textContent) textContent = maskInputValue({
		isMasked: needMaskingText(n, maskTextClass, maskTextSelector, unmaskTextClass, unmaskTextSelector, shouldMaskInput({
			type: null,
			tagName: parentTagName,
			maskInputOptions
		})),
		element: n,
		value: textContent,
		maskInputFn
	});
	return {
		type: NodeType$1.Text,
		textContent: textContent || "",
		isStyle,
		rootId
	};
}
function serializeElementNode(n, options) {
	const { doc, blockClass, blockSelector, unblockSelector, inlineStylesheet, maskInputOptions = {}, maskAttributeFn, maskInputFn, dataURLOptions = {}, inlineImages, recordCanvas, keepIframeSrcFn, newlyAddedElement = false, rootId, maskTextClass, unmaskTextClass, maskTextSelector, unmaskTextSelector, ignoreCSSAttributes } = options;
	const needBlock = _isBlockedElement(n, blockClass, blockSelector, unblockSelector);
	const tagName = getValidTagName(n);
	let attributes2 = {};
	const len = n.attributes.length;
	for (let i2 = 0; i2 < len; i2++) {
		const attr = n.attributes[i2];
		if (attr.name && !ignoreAttribute(tagName, attr.name, attr.value)) attributes2[attr.name] = transformAttribute(doc, tagName, toLowerCase(attr.name), attr.value, n, maskAttributeFn, ignoreCSSAttributes);
	}
	if (tagName === "link" && inlineStylesheet) {
		const stylesheet = Array.from(doc.styleSheets).find((s) => {
			return s.href === n.href;
		});
		let cssText = null;
		if (stylesheet) cssText = stringifyStylesheet(stylesheet);
		if (cssText) {
			attributes2.rel = null;
			attributes2.href = null;
			attributes2.crossorigin = null;
			attributes2._cssText = absoluteToStylesheet(cssText, stylesheet.href);
		}
	}
	if (tagName === "style" && n.sheet && !(n.innerText || n.textContent || "").trim().length) {
		const cssText = stringifyStylesheet(n.sheet);
		if (cssText) attributes2._cssText = absoluteToStylesheet(cssText, getHref(doc));
	}
	if (tagName === "input" || tagName === "textarea" || tagName === "select" || tagName === "option") {
		const el = n;
		const type = getInputType(el);
		const value = getInputValue(el, toUpperCase(tagName), type);
		const checked = el.checked;
		if (type !== "submit" && type !== "button" && value) {
			const forceMask = needMaskingText(el, maskTextClass, maskTextSelector, unmaskTextClass, unmaskTextSelector, shouldMaskInput({
				type,
				tagName: toUpperCase(tagName),
				maskInputOptions
			}));
			attributes2.value = maskInputValue({
				isMasked: forceMask,
				element: el,
				value,
				maskInputFn
			});
		}
		if (checked) attributes2.checked = checked;
	}
	if (tagName === "option") {
		if (n.selected && !maskInputOptions["select"]) attributes2.selected = true;
		else delete attributes2.selected;
	}
	if (tagName === "canvas" && recordCanvas) {
		if (n.__context === "2d") {
			if (!is2DCanvasBlank(n)) attributes2.rr_dataURL = n.toDataURL(dataURLOptions.type, dataURLOptions.quality);
		} else if (!("__context" in n)) {
			const canvasDataURL = n.toDataURL(dataURLOptions.type, dataURLOptions.quality);
			const blankCanvas = doc.createElement("canvas");
			blankCanvas.width = n.width;
			blankCanvas.height = n.height;
			if (canvasDataURL !== blankCanvas.toDataURL(dataURLOptions.type, dataURLOptions.quality)) attributes2.rr_dataURL = canvasDataURL;
		}
	}
	if (tagName === "img" && inlineImages) {
		if (!canvasService) {
			canvasService = doc.createElement("canvas");
			canvasCtx = canvasService.getContext("2d");
		}
		const image = n;
		const imageSrc = image.currentSrc || image.getAttribute("src") || "<unknown-src>";
		const priorCrossOrigin = image.crossOrigin;
		const recordInlineImage = () => {
			image.removeEventListener("load", recordInlineImage);
			try {
				canvasService.width = image.naturalWidth;
				canvasService.height = image.naturalHeight;
				canvasCtx.drawImage(image, 0, 0);
				attributes2.rr_dataURL = canvasService.toDataURL(dataURLOptions.type, dataURLOptions.quality);
			} catch (err) {
				if (image.crossOrigin !== "anonymous") {
					image.crossOrigin = "anonymous";
					if (image.complete && image.naturalWidth !== 0) recordInlineImage();
					else image.addEventListener("load", recordInlineImage);
					return;
				} else console.warn(`Cannot inline img src=${imageSrc}! Error: ${err}`);
			}
			if (image.crossOrigin === "anonymous") priorCrossOrigin ? attributes2.crossOrigin = priorCrossOrigin : image.removeAttribute("crossorigin");
		};
		if (image.complete && image.naturalWidth !== 0) recordInlineImage();
		else image.addEventListener("load", recordInlineImage);
	}
	if (tagName === "audio" || tagName === "video") {
		attributes2.rr_mediaState = n.paused ? "paused" : "played";
		attributes2.rr_mediaCurrentTime = n.currentTime;
	}
	if (!newlyAddedElement) {
		if (n.scrollLeft) attributes2.rr_scrollLeft = n.scrollLeft;
		if (n.scrollTop) attributes2.rr_scrollTop = n.scrollTop;
	}
	if (needBlock) {
		const { width, height } = n.getBoundingClientRect();
		attributes2 = {
			class: attributes2.class,
			rr_width: `${width}px`,
			rr_height: `${height}px`
		};
	}
	if (tagName === "iframe" && !keepIframeSrcFn(attributes2.src)) {
		if (!needBlock && !getIFrameContentDocument$1(n)) attributes2.rr_src = attributes2.src;
		delete attributes2.src;
	}
	let isCustomElement;
	try {
		if (customElements.get(tagName)) isCustomElement = true;
	} catch (e) {}
	return {
		type: NodeType$1.Element,
		tagName,
		attributes: attributes2,
		childNodes: [],
		isSVG: isSVGElement(n) || void 0,
		needBlock,
		rootId,
		isCustom: isCustomElement
	};
}
function lowerIfExists(maybeAttr) {
	if (maybeAttr === void 0 || maybeAttr === null) return "";
	else return maybeAttr.toLowerCase();
}
function slimDOMExcluded(sn, slimDOMOptions) {
	if (slimDOMOptions.comment && sn.type === NodeType$1.Comment) return true;
	else if (sn.type === NodeType$1.Element) {
		if (slimDOMOptions.script && (sn.tagName === "script" || sn.tagName === "link" && (sn.attributes.rel === "preload" || sn.attributes.rel === "modulepreload") || sn.tagName === "link" && sn.attributes.rel === "prefetch" && typeof sn.attributes.href === "string" && extractFileExtension(sn.attributes.href) === "js")) return true;
		else if (slimDOMOptions.headFavicon && (sn.tagName === "link" && sn.attributes.rel === "shortcut icon" || sn.tagName === "meta" && (lowerIfExists(sn.attributes.name).match(/^msapplication-tile(image|color)$/) || lowerIfExists(sn.attributes.name) === "application-name" || lowerIfExists(sn.attributes.rel) === "icon" || lowerIfExists(sn.attributes.rel) === "apple-touch-icon" || lowerIfExists(sn.attributes.rel) === "shortcut icon"))) return true;
		else if (sn.tagName === "meta") {
			if (slimDOMOptions.headMetaDescKeywords && lowerIfExists(sn.attributes.name).match(/^description|keywords$/)) return true;
			else if (slimDOMOptions.headMetaSocial && (lowerIfExists(sn.attributes.property).match(/^(og|twitter|fb):/) || lowerIfExists(sn.attributes.name).match(/^(og|twitter):/) || lowerIfExists(sn.attributes.name) === "pinterest")) return true;
			else if (slimDOMOptions.headMetaRobots && (lowerIfExists(sn.attributes.name) === "robots" || lowerIfExists(sn.attributes.name) === "googlebot" || lowerIfExists(sn.attributes.name) === "bingbot")) return true;
			else if (slimDOMOptions.headMetaHttpEquiv && sn.attributes["http-equiv"] !== void 0) return true;
			else if (slimDOMOptions.headMetaAuthorship && (lowerIfExists(sn.attributes.name) === "author" || lowerIfExists(sn.attributes.name) === "generator" || lowerIfExists(sn.attributes.name) === "framework" || lowerIfExists(sn.attributes.name) === "publisher" || lowerIfExists(sn.attributes.name) === "progid" || lowerIfExists(sn.attributes.property).match(/^article:/) || lowerIfExists(sn.attributes.property).match(/^product:/))) return true;
			else if (slimDOMOptions.headMetaVerification && (lowerIfExists(sn.attributes.name) === "google-site-verification" || lowerIfExists(sn.attributes.name) === "yandex-verification" || lowerIfExists(sn.attributes.name) === "csrf-token" || lowerIfExists(sn.attributes.name) === "p:domain_verify" || lowerIfExists(sn.attributes.name) === "verify-v1" || lowerIfExists(sn.attributes.name) === "verification" || lowerIfExists(sn.attributes.name) === "shopify-checkout-api-token")) return true;
		}
	}
	return false;
}
function serializeNodeWithId(n, options) {
	const { doc, mirror, blockClass, blockSelector, unblockSelector, maskAllText, maskTextClass, unmaskTextClass, maskTextSelector, unmaskTextSelector, skipChild = false, inlineStylesheet = true, maskInputOptions = {}, maskAttributeFn, maskTextFn, maskInputFn, slimDOMOptions, dataURLOptions = {}, inlineImages = false, recordCanvas = false, onSerialize, onIframeLoad, iframeLoadTimeout = 5e3, onBlockedImageLoad, onStylesheetLoad, stylesheetLoadTimeout = 5e3, keepIframeSrcFn = () => false, newlyAddedElement = false, ignoreCSSAttributes } = options;
	let { preserveWhiteSpace = true } = options;
	const _serializedNode = serializeNode(n, {
		doc,
		mirror,
		blockClass,
		blockSelector,
		maskAllText,
		unblockSelector,
		maskTextClass,
		unmaskTextClass,
		maskTextSelector,
		unmaskTextSelector,
		inlineStylesheet,
		maskInputOptions,
		maskAttributeFn,
		maskTextFn,
		maskInputFn,
		dataURLOptions,
		inlineImages,
		recordCanvas,
		keepIframeSrcFn,
		newlyAddedElement,
		ignoreCSSAttributes
	});
	if (!_serializedNode) {
		console.warn(n, "not serialized");
		return null;
	}
	let id;
	if (mirror.hasNode(n)) id = mirror.getId(n);
	else if (slimDOMExcluded(_serializedNode, slimDOMOptions) || !preserveWhiteSpace && _serializedNode.type === NodeType$1.Text && !_serializedNode.isStyle && !_serializedNode.textContent.trim().length) id = IGNORED_NODE;
	else id = genId();
	const serializedNode2 = Object.assign(_serializedNode, { id });
	mirror.add(n, serializedNode2);
	if (id === IGNORED_NODE) return null;
	if (onSerialize) onSerialize(n);
	let recordChild = !skipChild;
	if (serializedNode2.type === NodeType$1.Element) {
		recordChild = recordChild && !serializedNode2.needBlock;
		const shadowRoot = n.shadowRoot;
		if (shadowRoot && isNativeShadowDom(shadowRoot)) serializedNode2.isShadowHost = true;
	}
	if ((serializedNode2.type === NodeType$1.Document || serializedNode2.type === NodeType$1.Element) && recordChild) {
		if (slimDOMOptions.headWhitespace && serializedNode2.type === NodeType$1.Element && serializedNode2.tagName === "head") preserveWhiteSpace = false;
		const bypassOptions = {
			doc,
			mirror,
			blockClass,
			blockSelector,
			maskAllText,
			unblockSelector,
			maskTextClass,
			unmaskTextClass,
			maskTextSelector,
			unmaskTextSelector,
			skipChild,
			inlineStylesheet,
			maskInputOptions,
			maskAttributeFn,
			maskTextFn,
			maskInputFn,
			slimDOMOptions,
			dataURLOptions,
			inlineImages,
			recordCanvas,
			preserveWhiteSpace,
			onSerialize,
			onIframeLoad,
			iframeLoadTimeout,
			onBlockedImageLoad,
			onStylesheetLoad,
			stylesheetLoadTimeout,
			keepIframeSrcFn,
			ignoreCSSAttributes
		};
		const childNodes = n.childNodes ? Array.from(n.childNodes) : [];
		for (const childN of childNodes) {
			const serializedChildNode = serializeNodeWithId(childN, bypassOptions);
			if (serializedChildNode) serializedNode2.childNodes.push(serializedChildNode);
		}
		if (isElement$1(n) && n.shadowRoot) for (const childN of Array.from(n.shadowRoot.childNodes)) {
			const serializedChildNode = serializeNodeWithId(childN, bypassOptions);
			if (serializedChildNode) {
				isNativeShadowDom(n.shadowRoot) && (serializedChildNode.isShadow = true);
				serializedNode2.childNodes.push(serializedChildNode);
			}
		}
	}
	if (n.parentNode && isShadowRoot(n.parentNode) && isNativeShadowDom(n.parentNode)) serializedNode2.isShadow = true;
	if (serializedNode2.type === NodeType$1.Element && serializedNode2.tagName === "iframe" && !serializedNode2.needBlock) onceIframeLoaded(n, () => {
		const iframeDoc = getIFrameContentDocument$1(n);
		if (iframeDoc && onIframeLoad) {
			const serializedIframeNode = serializeNodeWithId(iframeDoc, {
				doc: iframeDoc,
				mirror,
				blockClass,
				blockSelector,
				unblockSelector,
				maskAllText,
				maskTextClass,
				unmaskTextClass,
				maskTextSelector,
				unmaskTextSelector,
				skipChild: false,
				inlineStylesheet,
				maskInputOptions,
				maskAttributeFn,
				maskTextFn,
				maskInputFn,
				slimDOMOptions,
				dataURLOptions,
				inlineImages,
				recordCanvas,
				preserveWhiteSpace,
				onSerialize,
				onIframeLoad,
				iframeLoadTimeout,
				onStylesheetLoad,
				stylesheetLoadTimeout,
				keepIframeSrcFn,
				ignoreCSSAttributes
			});
			if (serializedIframeNode) onIframeLoad(n, serializedIframeNode);
		}
	}, iframeLoadTimeout);
	if (serializedNode2.type === NodeType$1.Element && serializedNode2.tagName === "img" && !n.complete && serializedNode2.needBlock) {
		const image = n;
		const updateImageDimensions = () => {
			if (image.isConnected && !image.complete && onBlockedImageLoad) try {
				const rect = image.getBoundingClientRect();
				if (rect.width > 0 && rect.height > 0) onBlockedImageLoad(image, serializedNode2, rect);
			} catch (error) {}
			image.removeEventListener("load", updateImageDimensions);
		};
		if (image.isConnected) image.addEventListener("load", updateImageDimensions);
	}
	if (serializedNode2.type === NodeType$1.Element && serializedNode2.tagName === "link" && typeof serializedNode2.attributes.rel === "string" && (serializedNode2.attributes.rel === "stylesheet" || serializedNode2.attributes.rel === "preload" && typeof serializedNode2.attributes.href === "string" && extractFileExtension(serializedNode2.attributes.href) === "css")) onceStylesheetLoaded(n, () => {
		if (onStylesheetLoad) {
			const serializedLinkNode = serializeNodeWithId(n, {
				doc,
				mirror,
				blockClass,
				blockSelector,
				unblockSelector,
				maskAllText,
				maskTextClass,
				unmaskTextClass,
				maskTextSelector,
				unmaskTextSelector,
				skipChild: false,
				inlineStylesheet,
				maskInputOptions,
				maskAttributeFn,
				maskTextFn,
				maskInputFn,
				slimDOMOptions,
				dataURLOptions,
				inlineImages,
				recordCanvas,
				preserveWhiteSpace,
				onSerialize,
				onIframeLoad,
				iframeLoadTimeout,
				onStylesheetLoad,
				stylesheetLoadTimeout,
				keepIframeSrcFn,
				ignoreCSSAttributes
			});
			if (serializedLinkNode) onStylesheetLoad(n, serializedLinkNode);
		}
	}, stylesheetLoadTimeout);
	if (serializedNode2.type === NodeType$1.Element) delete serializedNode2.needBlock;
	return serializedNode2;
}
function snapshot(n, options) {
	const { mirror = new Mirror$1(), blockClass = "rr-block", blockSelector = null, unblockSelector = null, maskAllText = false, maskTextClass = "rr-mask", unmaskTextClass = null, maskTextSelector = null, unmaskTextSelector = null, inlineStylesheet = true, inlineImages = false, recordCanvas = false, maskAllInputs = false, maskAttributeFn, maskTextFn, maskInputFn, slimDOM = false, dataURLOptions, preserveWhiteSpace, onSerialize, onIframeLoad, iframeLoadTimeout, onBlockedImageLoad, onStylesheetLoad, stylesheetLoadTimeout, keepIframeSrcFn = () => false, ignoreCSSAttributes = /* @__PURE__ */ new Set([]) } = options || {};
	return serializeNodeWithId(n, {
		doc: n,
		mirror,
		blockClass,
		blockSelector,
		unblockSelector,
		maskAllText,
		maskTextClass,
		unmaskTextClass,
		maskTextSelector,
		unmaskTextSelector,
		skipChild: false,
		inlineStylesheet,
		maskInputOptions: maskAllInputs === true ? {
			color: true,
			date: true,
			"datetime-local": true,
			email: true,
			month: true,
			number: true,
			range: true,
			search: true,
			tel: true,
			text: true,
			time: true,
			url: true,
			week: true,
			textarea: true,
			select: true
		} : maskAllInputs === false ? {} : maskAllInputs,
		maskAttributeFn,
		maskTextFn,
		maskInputFn,
		slimDOMOptions: slimDOM === true || slimDOM === "all" ? {
			script: true,
			comment: true,
			headFavicon: true,
			headWhitespace: true,
			headMetaDescKeywords: slimDOM === "all",
			headMetaSocial: true,
			headMetaRobots: true,
			headMetaHttpEquiv: true,
			headMetaAuthorship: true,
			headMetaVerification: true
		} : slimDOM === false ? {} : slimDOM,
		dataURLOptions,
		inlineImages,
		recordCanvas,
		preserveWhiteSpace,
		onSerialize,
		onIframeLoad,
		iframeLoadTimeout,
		onBlockedImageLoad,
		onStylesheetLoad,
		stylesheetLoadTimeout,
		keepIframeSrcFn,
		newlyAddedElement: false,
		ignoreCSSAttributes
	});
}
function on(type, fn, target = document) {
	const options = {
		capture: true,
		passive: true
	};
	target.addEventListener(type, fn, options);
	return () => target.removeEventListener(type, fn, options);
}
var DEPARTED_MIRROR_ACCESS_WARNING$1 = "Please stop import mirror directly. Instead of that,\r\nnow you can use replayer.getMirror() to access the mirror instance of a replayer,\r\nor you can use record.mirror to access the mirror instance during recording.";
var _mirror$1 = {
	map: {},
	getId() {
		console.error(DEPARTED_MIRROR_ACCESS_WARNING$1);
		return -1;
	},
	getNode() {
		console.error(DEPARTED_MIRROR_ACCESS_WARNING$1);
		return null;
	},
	removeNodeFromMap() {
		console.error(DEPARTED_MIRROR_ACCESS_WARNING$1);
	},
	has() {
		console.error(DEPARTED_MIRROR_ACCESS_WARNING$1);
		return false;
	},
	reset() {
		console.error(DEPARTED_MIRROR_ACCESS_WARNING$1);
	}
};
if (typeof window !== "undefined" && window.Proxy && window.Reflect) _mirror$1 = new Proxy(_mirror$1, { get(target, prop, receiver) {
	if (prop === "map") console.error(DEPARTED_MIRROR_ACCESS_WARNING$1);
	return Reflect.get(target, prop, receiver);
} });
function throttle$1(func, wait, options = {}) {
	let timeout = null;
	let previous = 0;
	return function(...args) {
		const now = Date.now();
		if (!previous && options.leading === false) previous = now;
		const remaining = wait - (now - previous);
		const context = this;
		if (remaining <= 0 || remaining > wait) {
			if (timeout) {
				clearTimeout$2(timeout);
				timeout = null;
			}
			previous = now;
			func.apply(context, args);
		} else if (!timeout && options.trailing !== false) timeout = setTimeout$2(() => {
			previous = options.leading === false ? 0 : Date.now();
			timeout = null;
			func.apply(context, args);
		}, remaining);
	};
}
function hookSetter$1(target, key, d, isRevoked, win = window) {
	const original = win.Object.getOwnPropertyDescriptor(target, key);
	win.Object.defineProperty(target, key, isRevoked ? d : { set(value) {
		setTimeout$2(() => {
			d.set.call(this, value);
		}, 0);
		if (original && original.set) original.set.call(this, value);
	} });
	return () => hookSetter$1(target, key, original || {}, true);
}
function patch$1(source, name, replacement) {
	try {
		if (!(name in source)) return () => {};
		const original = source[name];
		const wrapped = replacement(original);
		if (typeof wrapped === "function") {
			wrapped.prototype = wrapped.prototype || {};
			Object.defineProperties(wrapped, { __rrweb_original__: {
				enumerable: false,
				value: original
			} });
		}
		source[name] = wrapped;
		return () => {
			source[name] = original;
		};
	} catch {
		return () => {};
	}
}
var nowTimestamp = Date.now;
if (!/* @__PURE__ */ /[1-9][0-9]{12}/.test(Date.now().toString())) nowTimestamp = () => (/* @__PURE__ */ new Date()).getTime();
function getWindowScroll(win) {
	const doc = win.document;
	return {
		left: doc.scrollingElement ? doc.scrollingElement.scrollLeft : win.pageXOffset !== void 0 ? win.pageXOffset : doc?.documentElement.scrollLeft || doc?.body?.parentElement?.scrollLeft || doc?.body?.scrollLeft || 0,
		top: doc.scrollingElement ? doc.scrollingElement.scrollTop : win.pageYOffset !== void 0 ? win.pageYOffset : doc?.documentElement.scrollTop || doc?.body?.parentElement?.scrollTop || doc?.body?.scrollTop || 0
	};
}
function getWindowHeight() {
	return window.innerHeight || document.documentElement && document.documentElement.clientHeight || document.body && document.body.clientHeight;
}
function getWindowWidth() {
	return window.innerWidth || document.documentElement && document.documentElement.clientWidth || document.body && document.body.clientWidth;
}
function closestElementOfNode$1(node) {
	if (!node) return null;
	try {
		return node.nodeType === node.ELEMENT_NODE ? node : node.parentElement;
	} catch (error) {
		return null;
	}
}
function isBlocked$1(node, blockClass, blockSelector, unblockSelector, checkAncestors) {
	if (!node) return false;
	const el = closestElementOfNode$1(node);
	if (!el) return false;
	const blockedPredicate = createMatchPredicate$1(blockClass, blockSelector);
	if (!checkAncestors) {
		const isUnblocked = unblockSelector && el.matches(unblockSelector);
		return blockedPredicate(el) && !isUnblocked;
	}
	const blockDistance = distanceToMatch$1(el, blockedPredicate);
	let unblockDistance = -1;
	if (blockDistance < 0) return false;
	if (unblockSelector) unblockDistance = distanceToMatch$1(el, createMatchPredicate$1(null, unblockSelector));
	if (blockDistance > -1 && unblockDistance < 0) return true;
	return blockDistance < unblockDistance;
}
function isSerialized(n, mirror) {
	return mirror.getId(n) !== -1;
}
function isIgnored(n, mirror) {
	return mirror.getId(n) === IGNORED_NODE;
}
function isAncestorRemoved(target, mirror) {
	if (isShadowRoot(target)) return false;
	const id = mirror.getId(target);
	if (!mirror.has(id)) return true;
	if (target.parentNode && target.parentNode.nodeType === target.DOCUMENT_NODE) return false;
	if (!target.parentNode) return true;
	return isAncestorRemoved(target.parentNode, mirror);
}
function legacy_isTouchEvent(event) {
	return Boolean(event.changedTouches);
}
function polyfill(win = window) {
	if ("NodeList" in win && !win.NodeList.prototype.forEach) win.NodeList.prototype.forEach = Array.prototype.forEach;
	if ("DOMTokenList" in win && !win.DOMTokenList.prototype.forEach) win.DOMTokenList.prototype.forEach = Array.prototype.forEach;
	if (!Node.prototype.contains) Node.prototype.contains = (...args) => {
		let node = args[0];
		if (!(0 in args)) throw new TypeError("1 argument is required");
		do
			if (this === node) return true;
		while (node = node && node.parentNode);
		return false;
	};
}
function isSerializedIframe(n, mirror) {
	return Boolean(n.nodeName === "IFRAME" && mirror.getMeta(n));
}
function isSerializedStylesheet(n, mirror) {
	return Boolean(n.nodeName === "LINK" && n.nodeType === n.ELEMENT_NODE && n.getAttribute && n.getAttribute("rel") === "stylesheet" && mirror.getMeta(n));
}
function hasShadowRoot(n) {
	return Boolean(n?.shadowRoot);
}
var StyleSheetMirror = class {
	constructor() {
		this.id = 1;
		this.styleIDMap = /* @__PURE__ */ new WeakMap();
		this.idStyleMap = /* @__PURE__ */ new Map();
	}
	getId(stylesheet) {
		return this.styleIDMap.get(stylesheet) ?? -1;
	}
	has(stylesheet) {
		return this.styleIDMap.has(stylesheet);
	}
	/**
	* @returns If the stylesheet is in the mirror, returns the id of the stylesheet. If not, return the new assigned id.
	*/
	add(stylesheet, id) {
		if (this.has(stylesheet)) return this.getId(stylesheet);
		let newId;
		if (id === void 0) newId = this.id++;
		else newId = id;
		this.styleIDMap.set(stylesheet, newId);
		this.idStyleMap.set(newId, stylesheet);
		return newId;
	}
	getStyle(id) {
		return this.idStyleMap.get(id) || null;
	}
	reset() {
		this.styleIDMap = /* @__PURE__ */ new WeakMap();
		this.idStyleMap = /* @__PURE__ */ new Map();
		this.id = 1;
	}
	generateId() {
		return this.id++;
	}
};
function getShadowHost(n) {
	let shadowHost = null;
	if (n.getRootNode?.()?.nodeType === Node.DOCUMENT_FRAGMENT_NODE && n.getRootNode().host) shadowHost = n.getRootNode().host;
	return shadowHost;
}
function getRootShadowHost(n) {
	let rootShadowHost = n;
	let shadowHost;
	while (shadowHost = getShadowHost(rootShadowHost)) rootShadowHost = shadowHost;
	return rootShadowHost;
}
function shadowHostInDom(n) {
	const doc = n.ownerDocument;
	if (!doc) return false;
	const shadowHost = getRootShadowHost(n);
	return doc.contains(shadowHost);
}
function inDom(n) {
	const doc = n.ownerDocument;
	if (!doc) return false;
	return doc.contains(n) || shadowHostInDom(n);
}
var cachedImplementations$2 = {};
function getImplementation$2(name) {
	const cached = cachedImplementations$2[name];
	if (cached) return cached;
	const document2 = window.document;
	let impl = window[name];
	if (document2 && typeof document2.createElement === "function") try {
		const sandbox = document2.createElement("iframe");
		sandbox.hidden = true;
		document2.head.appendChild(sandbox);
		const contentWindow = sandbox.contentWindow;
		if (contentWindow && contentWindow[name]) impl = contentWindow[name];
		document2.head.removeChild(sandbox);
	} catch (e) {}
	return cachedImplementations$2[name] = impl.bind(window);
}
function onRequestAnimationFrame$1(...rest) {
	return getImplementation$2("requestAnimationFrame")(...rest);
}
function setTimeout$2(...rest) {
	return getImplementation$2("setTimeout")(...rest);
}
function clearTimeout$2(...rest) {
	return getImplementation$2("clearTimeout")(...rest);
}
var EventType = /* @__PURE__ */ ((EventType2) => {
	EventType2[EventType2["DomContentLoaded"] = 0] = "DomContentLoaded";
	EventType2[EventType2["Load"] = 1] = "Load";
	EventType2[EventType2["FullSnapshot"] = 2] = "FullSnapshot";
	EventType2[EventType2["IncrementalSnapshot"] = 3] = "IncrementalSnapshot";
	EventType2[EventType2["Meta"] = 4] = "Meta";
	EventType2[EventType2["Custom"] = 5] = "Custom";
	EventType2[EventType2["Plugin"] = 6] = "Plugin";
	return EventType2;
})(EventType || {});
var IncrementalSource = /* @__PURE__ */ ((IncrementalSource2) => {
	IncrementalSource2[IncrementalSource2["Mutation"] = 0] = "Mutation";
	IncrementalSource2[IncrementalSource2["MouseMove"] = 1] = "MouseMove";
	IncrementalSource2[IncrementalSource2["MouseInteraction"] = 2] = "MouseInteraction";
	IncrementalSource2[IncrementalSource2["Scroll"] = 3] = "Scroll";
	IncrementalSource2[IncrementalSource2["ViewportResize"] = 4] = "ViewportResize";
	IncrementalSource2[IncrementalSource2["Input"] = 5] = "Input";
	IncrementalSource2[IncrementalSource2["TouchMove"] = 6] = "TouchMove";
	IncrementalSource2[IncrementalSource2["MediaInteraction"] = 7] = "MediaInteraction";
	IncrementalSource2[IncrementalSource2["StyleSheetRule"] = 8] = "StyleSheetRule";
	IncrementalSource2[IncrementalSource2["CanvasMutation"] = 9] = "CanvasMutation";
	IncrementalSource2[IncrementalSource2["Font"] = 10] = "Font";
	IncrementalSource2[IncrementalSource2["Log"] = 11] = "Log";
	IncrementalSource2[IncrementalSource2["Drag"] = 12] = "Drag";
	IncrementalSource2[IncrementalSource2["StyleDeclaration"] = 13] = "StyleDeclaration";
	IncrementalSource2[IncrementalSource2["Selection"] = 14] = "Selection";
	IncrementalSource2[IncrementalSource2["AdoptedStyleSheet"] = 15] = "AdoptedStyleSheet";
	IncrementalSource2[IncrementalSource2["CustomElement"] = 16] = "CustomElement";
	return IncrementalSource2;
})(IncrementalSource || {});
var MouseInteractions = /* @__PURE__ */ ((MouseInteractions2) => {
	MouseInteractions2[MouseInteractions2["MouseUp"] = 0] = "MouseUp";
	MouseInteractions2[MouseInteractions2["MouseDown"] = 1] = "MouseDown";
	MouseInteractions2[MouseInteractions2["Click"] = 2] = "Click";
	MouseInteractions2[MouseInteractions2["ContextMenu"] = 3] = "ContextMenu";
	MouseInteractions2[MouseInteractions2["DblClick"] = 4] = "DblClick";
	MouseInteractions2[MouseInteractions2["Focus"] = 5] = "Focus";
	MouseInteractions2[MouseInteractions2["Blur"] = 6] = "Blur";
	MouseInteractions2[MouseInteractions2["TouchStart"] = 7] = "TouchStart";
	MouseInteractions2[MouseInteractions2["TouchMove_Departed"] = 8] = "TouchMove_Departed";
	MouseInteractions2[MouseInteractions2["TouchEnd"] = 9] = "TouchEnd";
	MouseInteractions2[MouseInteractions2["TouchCancel"] = 10] = "TouchCancel";
	return MouseInteractions2;
})(MouseInteractions || {});
var PointerTypes = /* @__PURE__ */ ((PointerTypes2) => {
	PointerTypes2[PointerTypes2["Mouse"] = 0] = "Mouse";
	PointerTypes2[PointerTypes2["Pen"] = 1] = "Pen";
	PointerTypes2[PointerTypes2["Touch"] = 2] = "Touch";
	return PointerTypes2;
})(PointerTypes || {});
var MediaInteractions = /* @__PURE__ */ ((MediaInteractions2) => {
	MediaInteractions2[MediaInteractions2["Play"] = 0] = "Play";
	MediaInteractions2[MediaInteractions2["Pause"] = 1] = "Pause";
	MediaInteractions2[MediaInteractions2["Seeked"] = 2] = "Seeked";
	MediaInteractions2[MediaInteractions2["VolumeChange"] = 3] = "VolumeChange";
	MediaInteractions2[MediaInteractions2["RateChange"] = 4] = "RateChange";
	return MediaInteractions2;
})(MediaInteractions || {});
var errorHandler$1;
function registerErrorHandler$1(handler) {
	errorHandler$1 = handler;
}
function unregisterErrorHandler() {
	errorHandler$1 = void 0;
}
var callbackWrapper$1 = (cb) => {
	if (!errorHandler$1) return cb;
	const rrwebWrapped = ((...rest) => {
		try {
			return cb(...rest);
		} catch (error) {
			if (errorHandler$1 && errorHandler$1(error) === true) return () => {};
			throw error;
		}
	});
	return rrwebWrapped;
};
var chars$1 = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
var lookup$1 = typeof Uint8Array === "undefined" ? [] : /* @__PURE__ */ new Uint8Array(256);
for (var i$2 = 0; i$2 < chars$1.length; i$2++) lookup$1[chars$1.charCodeAt(i$2)] = i$2;
var CanvasManagerNoop = class {
	reset() {}
	freeze() {}
	unfreeze() {}
	lock() {}
	unlock() {}
	snapshot() {}
	addWindow() {}
	addShadowRoot() {}
	resetShadowRoots() {}
};
function getIFrameContentDocument(iframe) {
	try {
		return iframe.contentDocument;
	} catch (e2) {}
}
function getIFrameContentWindow(iframe) {
	try {
		return iframe.contentWindow;
	} catch (e2) {}
}
var StyleDeclarationParser = class {
	constructor(doc) {
		this.doc = doc;
		this.unattachedDoc = null;
	}
	parse(styleText) {
		return this.parseWithConstructableStylesheet(styleText) || this.parseWithDetachedElement(styleText);
	}
	parseWithConstructableStylesheet(styleText) {
		if (typeof CSSStyleSheet === "undefined" || typeof CSSStyleSheet.prototype.replaceSync !== "function") return null;
		try {
			const sheet = new CSSStyleSheet();
			sheet.replaceSync(`x { ${styleText} }`);
			const rule = sheet.cssRules[0];
			if (!rule || rule.type !== CSSRule.STYLE_RULE) return null;
			return rule.style;
		} catch {
			return null;
		}
	}
	parseWithDetachedElement(styleText) {
		const old = this.getUnattachedDoc().createElement("span");
		old.setAttribute("style", styleText);
		return old.style;
	}
	getUnattachedDoc() {
		if (!this.unattachedDoc) try {
			this.unattachedDoc = document.implementation.createHTMLDocument();
		} catch {
			this.unattachedDoc = this.doc;
		}
		return this.unattachedDoc;
	}
};
function isNodeInLinkedList(n2) {
	return "__ln" in n2;
}
var DoubleLinkedList = class {
	constructor() {
		this.length = 0;
		this.head = null;
		this.tail = null;
	}
	get(position) {
		if (position >= this.length) throw new Error("Position outside of list range");
		let current = this.head;
		for (let index = 0; index < position; index++) current = current?.next || null;
		return current;
	}
	addNode(n2) {
		const node = {
			value: n2,
			previous: null,
			next: null
		};
		n2.__ln = node;
		if (n2.previousSibling && isNodeInLinkedList(n2.previousSibling)) {
			const current = n2.previousSibling.__ln.next;
			node.next = current;
			node.previous = n2.previousSibling.__ln;
			n2.previousSibling.__ln.next = node;
			if (current) current.previous = node;
		} else if (n2.nextSibling && isNodeInLinkedList(n2.nextSibling) && n2.nextSibling.__ln.previous) {
			const current = n2.nextSibling.__ln.previous;
			node.previous = current;
			node.next = n2.nextSibling.__ln;
			n2.nextSibling.__ln.previous = node;
			if (current) current.next = node;
		} else {
			if (this.head) this.head.previous = node;
			node.next = this.head;
			this.head = node;
		}
		if (node.next === null) this.tail = node;
		this.length++;
	}
	removeNode(n2) {
		const current = n2.__ln;
		if (!this.head) return;
		if (!current.previous) {
			this.head = current.next;
			if (this.head) this.head.previous = null;
			else this.tail = null;
		} else {
			current.previous.next = current.next;
			if (current.next) current.next.previous = current.previous;
			else this.tail = current.previous;
		}
		if (n2.__ln) delete n2.__ln;
		this.length--;
	}
};
var moveKey = (id, parentId) => `${id}@${parentId}`;
var MutationBuffer = class {
	constructor() {
		this.frozen = false;
		this.locked = false;
		this.texts = [];
		this.attributes = [];
		this.attributeMap = /* @__PURE__ */ new WeakMap();
		this.removes = [];
		this.mapRemoves = [];
		this.movedMap = {};
		this.addedSet = /* @__PURE__ */ new Set();
		this.movedSet = /* @__PURE__ */ new Set();
		this.droppedSet = /* @__PURE__ */ new Set();
		this.processMutations = (mutations) => {
			mutations.forEach(this.processMutation);
			this.emit();
		};
		this.emit = () => {
			if (this.frozen || this.locked) return;
			const adds = [];
			const addedIds = /* @__PURE__ */ new Set();
			const addList = new DoubleLinkedList();
			const getNextId = (n2) => {
				let ns = n2;
				let nextId = IGNORED_NODE;
				while (nextId === IGNORED_NODE) {
					ns = ns && ns.nextSibling;
					nextId = ns && this.mirror.getId(ns);
				}
				return nextId;
			};
			const pushAdd = (n2) => {
				if (!n2.parentNode || !inDom(n2)) return;
				const parentId = isShadowRoot(n2.parentNode) ? this.mirror.getId(getShadowHost(n2)) : this.mirror.getId(n2.parentNode);
				const nextId = getNextId(n2);
				if (parentId === -1 || nextId === -1) return addList.addNode(n2);
				const sn = serializeNodeWithId(n2, {
					doc: this.doc,
					mirror: this.mirror,
					blockClass: this.blockClass,
					blockSelector: this.blockSelector,
					maskAllText: this.maskAllText,
					unblockSelector: this.unblockSelector,
					maskTextClass: this.maskTextClass,
					unmaskTextClass: this.unmaskTextClass,
					maskTextSelector: this.maskTextSelector,
					unmaskTextSelector: this.unmaskTextSelector,
					skipChild: true,
					newlyAddedElement: true,
					inlineStylesheet: this.inlineStylesheet,
					maskInputOptions: this.maskInputOptions,
					maskAttributeFn: this.maskAttributeFn,
					maskTextFn: this.maskTextFn,
					maskInputFn: this.maskInputFn,
					slimDOMOptions: this.slimDOMOptions,
					dataURLOptions: this.dataURLOptions,
					recordCanvas: this.recordCanvas,
					inlineImages: this.inlineImages,
					onSerialize: (currentN) => {
						if (isSerializedIframe(currentN, this.mirror) && !isBlocked$1(currentN, this.blockClass, this.blockSelector, this.unblockSelector, false)) this.iframeManager.addIframe(currentN);
						if (isSerializedStylesheet(currentN, this.mirror)) this.stylesheetManager.trackLinkElement(currentN);
						if (hasShadowRoot(n2)) this.shadowDomManager.addShadowRoot(n2.shadowRoot, this.doc);
					},
					onIframeLoad: (iframe, childSn) => {
						if (isBlocked$1(iframe, this.blockClass, this.blockSelector, this.unblockSelector, false)) return;
						this.iframeManager.attachIframe(iframe, childSn);
						const contentWindow = getIFrameContentWindow(iframe);
						if (contentWindow) this.canvasManager.addWindow(contentWindow);
						this.shadowDomManager.observeAttachShadow(iframe);
					},
					onStylesheetLoad: (link, childSn) => {
						this.stylesheetManager.attachLinkElement(link, childSn);
					},
					onBlockedImageLoad: (_imageEl, serializedNode, { width, height }) => {
						this.mutationCb({
							adds: [],
							removes: [],
							texts: [],
							attributes: [{
								id: serializedNode.id,
								attributes: { style: {
									width: `${width}px`,
									height: `${height}px`
								} }
							}]
						});
					},
					ignoreCSSAttributes: this.ignoreCSSAttributes
				});
				if (sn) {
					adds.push({
						parentId,
						nextId,
						node: sn
					});
					addedIds.add(sn.id);
				}
			};
			while (this.mapRemoves.length) this.mirror.removeNodeFromMap(this.mapRemoves.shift());
			for (const n2 of this.movedSet) {
				if (isParentRemoved(this.removes, n2, this.mirror) && !this.movedSet.has(n2.parentNode)) continue;
				pushAdd(n2);
			}
			for (const n2 of this.addedSet) if (!isAncestorInSet(this.droppedSet, n2) && !isParentRemoved(this.removes, n2, this.mirror)) pushAdd(n2);
			else if (isAncestorInSet(this.movedSet, n2)) pushAdd(n2);
			else this.droppedSet.add(n2);
			let candidate = null;
			while (addList.length) {
				let node = null;
				if (candidate) {
					const parentId = this.mirror.getId(candidate.value.parentNode);
					const nextId = getNextId(candidate.value);
					if (parentId !== -1 && nextId !== -1) node = candidate;
				}
				if (!node) {
					let tailNode = addList.tail;
					while (tailNode) {
						const _node = tailNode;
						tailNode = tailNode.previous;
						if (_node) {
							const parentId = this.mirror.getId(_node.value.parentNode);
							if (getNextId(_node.value) === -1) continue;
							else if (parentId !== -1) {
								node = _node;
								break;
							} else {
								const unhandledNode = _node.value;
								if (unhandledNode.parentNode && unhandledNode.parentNode.nodeType === Node.DOCUMENT_FRAGMENT_NODE) {
									const shadowHost = unhandledNode.parentNode.host;
									if (this.mirror.getId(shadowHost) !== -1) {
										node = _node;
										break;
									}
								}
							}
						}
					}
				}
				if (!node) {
					while (addList.head) addList.removeNode(addList.head.value);
					break;
				}
				candidate = node.previous;
				addList.removeNode(node.value);
				pushAdd(node.value);
			}
			const payload = {
				texts: this.texts.map((text) => ({
					id: this.mirror.getId(text.node),
					value: text.value
				})).filter((text) => !addedIds.has(text.id)).filter((text) => this.mirror.has(text.id)),
				attributes: this.attributes.map((attribute) => {
					const { attributes } = attribute;
					if (typeof attributes.style === "string") {
						const diffAsStr = JSON.stringify(attribute.styleDiff);
						const unchangedAsStr = JSON.stringify(attribute._unchangedStyles);
						if (diffAsStr.length < attributes.style.length) {
							if ((diffAsStr + unchangedAsStr).split("var(").length === attributes.style.split("var(").length) attributes.style = attribute.styleDiff;
						}
					}
					return {
						id: this.mirror.getId(attribute.node),
						attributes
					};
				}).filter((attribute) => !addedIds.has(attribute.id)).filter((attribute) => this.mirror.has(attribute.id)),
				removes: this.removes,
				adds
			};
			if (!payload.texts.length && !payload.attributes.length && !payload.removes.length && !payload.adds.length) return;
			this.texts = [];
			this.attributes = [];
			this.attributeMap = /* @__PURE__ */ new WeakMap();
			this.removes = [];
			this.addedSet = /* @__PURE__ */ new Set();
			this.movedSet = /* @__PURE__ */ new Set();
			this.droppedSet = /* @__PURE__ */ new Set();
			this.movedMap = {};
			this.mutationCb(payload);
		};
		this.processMutation = (m) => {
			if (isIgnored(m.target, this.mirror)) return;
			switch (m.type) {
				case "characterData": {
					const value = m.target.textContent;
					if (!isBlocked$1(m.target, this.blockClass, this.blockSelector, this.unblockSelector, false) && value !== m.oldValue) this.texts.push({
						value: needMaskingText(m.target, this.maskTextClass, this.maskTextSelector, this.unmaskTextClass, this.unmaskTextSelector, this.maskAllText) && value ? this.maskTextFn ? this.maskTextFn(value, closestElementOfNode$1(m.target)) : value.replace(/[\S]/g, "*") : value,
						node: m.target
					});
					break;
				}
				case "attributes": {
					const target = m.target;
					let attributeName = m.attributeName;
					let value = m.target.getAttribute(attributeName);
					if (attributeName === "value") {
						const type = getInputType(target);
						const tagName = target.tagName;
						value = getInputValue(target, tagName, type);
						const isInputMasked = shouldMaskInput({
							maskInputOptions: this.maskInputOptions,
							tagName,
							type
						});
						value = maskInputValue({
							isMasked: needMaskingText(m.target, this.maskTextClass, this.maskTextSelector, this.unmaskTextClass, this.unmaskTextSelector, isInputMasked),
							element: target,
							value,
							maskInputFn: this.maskInputFn
						});
					}
					if (isBlocked$1(m.target, this.blockClass, this.blockSelector, this.unblockSelector, false) || value === m.oldValue) return;
					let item = this.attributeMap.get(m.target);
					if (target.tagName === "IFRAME" && attributeName === "src" && !this.keepIframeSrcFn(value)) {
						if (!getIFrameContentDocument(target)) attributeName = "rr_src";
						else return;
					}
					if (!item) {
						item = {
							node: m.target,
							attributes: {},
							styleDiff: {},
							_unchangedStyles: {}
						};
						this.attributes.push(item);
						this.attributeMap.set(m.target, item);
					}
					if (attributeName === "type" && target.tagName === "INPUT" && (m.oldValue || "").toLowerCase() === "password") target.setAttribute("data-rr-is-password", "true");
					if (!ignoreAttribute(target.tagName, attributeName)) {
						item.attributes[attributeName] = transformAttribute(this.doc, toLowerCase(target.tagName), toLowerCase(attributeName), value, target, this.maskAttributeFn);
						if (attributeName === "style") {
							const oldStyle = m.oldValue ? this.styleDeclarationParser.parse(m.oldValue) : null;
							for (const pname of Array.from(target.style)) {
								const newValue = target.style.getPropertyValue(pname);
								const newPriority = target.style.getPropertyPriority(pname);
								if (newValue !== (oldStyle?.getPropertyValue(pname) || "") || newPriority !== (oldStyle?.getPropertyPriority(pname) || "")) {
									if (newPriority === "") item.styleDiff[pname] = newValue;
									else item.styleDiff[pname] = [newValue, newPriority];
								} else item._unchangedStyles[pname] = [newValue, newPriority];
							}
							if (oldStyle) {
								for (const pname of Array.from(oldStyle)) if (target.style.getPropertyValue(pname) === "") item.styleDiff[pname] = false;
							}
						}
					}
					break;
				}
				case "childList":
					if (isBlocked$1(m.target, this.blockClass, this.blockSelector, this.unblockSelector, true)) return;
					m.addedNodes.forEach((n2) => this.genAdds(n2, m.target));
					m.removedNodes.forEach((n2) => {
						const nodeId = this.mirror.getId(n2);
						const parentId = isShadowRoot(m.target) ? this.mirror.getId(m.target.host) : this.mirror.getId(m.target);
						if (isBlocked$1(m.target, this.blockClass, this.blockSelector, this.unblockSelector, false) || isIgnored(n2, this.mirror) || !isSerialized(n2, this.mirror)) return;
						if (this.addedSet.has(n2)) {
							deepDelete(this.addedSet, n2);
							this.droppedSet.add(n2);
						} else if (this.addedSet.has(m.target) && nodeId === -1);
						else if (isAncestorRemoved(m.target, this.mirror));
						else if (this.movedSet.has(n2) && this.movedMap[moveKey(nodeId, parentId)]) deepDelete(this.movedSet, n2);
						else this.removes.push({
							parentId,
							id: nodeId,
							isShadow: isShadowRoot(m.target) && isNativeShadowDom(m.target) ? true : void 0
						});
						this.mapRemoves.push(n2);
					});
			}
		};
		this.genAdds = (n2, target) => {
			if (this.processedNodeManager.inOtherBuffer(n2, this)) return;
			if (this.addedSet.has(n2) || this.movedSet.has(n2)) return;
			if (this.mirror.hasNode(n2)) {
				if (isIgnored(n2, this.mirror)) return;
				this.movedSet.add(n2);
				let targetId = null;
				if (target && this.mirror.hasNode(target)) targetId = this.mirror.getId(target);
				if (targetId && targetId !== -1) this.movedMap[moveKey(this.mirror.getId(n2), targetId)] = true;
			} else {
				this.addedSet.add(n2);
				this.droppedSet.delete(n2);
			}
			if (!isBlocked$1(n2, this.blockClass, this.blockSelector, this.unblockSelector, false)) {
				if (n2.childNodes) n2.childNodes.forEach((childN) => this.genAdds(childN));
				if (hasShadowRoot(n2)) n2.shadowRoot.childNodes.forEach((childN) => {
					this.processedNodeManager.add(childN, this);
					this.genAdds(childN, n2);
				});
			}
		};
	}
	init(options) {
		[
			"mutationCb",
			"blockClass",
			"blockSelector",
			"unblockSelector",
			"maskAllText",
			"maskTextClass",
			"unmaskTextClass",
			"maskTextSelector",
			"unmaskTextSelector",
			"inlineStylesheet",
			"maskInputOptions",
			"maskAttributeFn",
			"maskTextFn",
			"maskInputFn",
			"keepIframeSrcFn",
			"recordCanvas",
			"inlineImages",
			"slimDOMOptions",
			"dataURLOptions",
			"doc",
			"mirror",
			"iframeManager",
			"stylesheetManager",
			"shadowDomManager",
			"canvasManager",
			"processedNodeManager",
			"ignoreCSSAttributes"
		].forEach((key) => {
			this[key] = options[key];
		});
		this.styleDeclarationParser = new StyleDeclarationParser(this.doc);
	}
	freeze() {
		this.frozen = true;
		this.canvasManager.freeze();
	}
	unfreeze() {
		this.frozen = false;
		this.canvasManager.unfreeze();
		this.emit();
	}
	isFrozen() {
		return this.frozen;
	}
	lock() {
		this.locked = true;
		this.canvasManager.lock();
	}
	unlock() {
		this.locked = false;
		this.canvasManager.unlock();
		this.emit();
	}
	reset() {
		this.shadowDomManager.reset();
		this.canvasManager.reset();
	}
};
function deepDelete(addsSet, n2) {
	addsSet.delete(n2);
	n2.childNodes?.forEach((childN) => deepDelete(addsSet, childN));
}
function isParentRemoved(removes, n2, mirror2) {
	if (removes.length === 0) return false;
	return _isParentRemoved(removes, n2, mirror2);
}
function _isParentRemoved(removes, n2, mirror2) {
	let node = n2.parentNode;
	while (node) {
		const parentId = mirror2.getId(node);
		if (removes.some((r2) => r2.id === parentId)) return true;
		node = node.parentNode;
	}
	return false;
}
function isAncestorInSet(set, n2) {
	if (set.size === 0) return false;
	return _isAncestorInSet(set, n2);
}
function _isAncestorInSet(set, n2) {
	const { parentNode } = n2;
	if (!parentNode) return false;
	if (set.has(parentNode)) return true;
	return _isAncestorInSet(set, parentNode);
}
var mutationBuffers = [];
function getEventTarget(event) {
	try {
		if ("composedPath" in event) {
			const path = event.composedPath();
			if (path.length) return path[0];
		} else if ("path" in event && event.path.length) return event.path[0];
	} catch {}
	return event && event.target;
}
function initMutationObserver(options, rootEl) {
	const mutationBuffer = new MutationBuffer();
	mutationBuffers.push(mutationBuffer);
	mutationBuffer.init(options);
	let mutationObserverCtor = window.MutationObserver || window.__rrMutationObserver;
	const angularZoneSymbol = window?.Zone?.__symbol__?.("MutationObserver");
	if (angularZoneSymbol && window[angularZoneSymbol]) mutationObserverCtor = window[angularZoneSymbol];
	const observer = new mutationObserverCtor(callbackWrapper$1((mutations) => {
		if (options.onMutation && options.onMutation(mutations) === false) return;
		mutationBuffer.processMutations.bind(mutationBuffer)(mutations);
	}));
	observer.observe(rootEl, {
		attributes: true,
		attributeOldValue: true,
		characterData: true,
		characterDataOldValue: true,
		childList: true,
		subtree: true
	});
	return observer;
}
function initMoveObserver({ mousemoveCb, sampling, doc, mirror: mirror2 }) {
	if (sampling.mousemove === false) return () => {};
	const threshold = typeof sampling.mousemove === "number" ? sampling.mousemove : 50;
	const callbackThreshold = typeof sampling.mousemoveCallback === "number" ? sampling.mousemoveCallback : 500;
	let positions = [];
	let timeBaseline;
	const wrappedCb = throttle$1(callbackWrapper$1((source) => {
		const totalOffset = Date.now() - timeBaseline;
		mousemoveCb(positions.map((p) => {
			p.timeOffset -= totalOffset;
			return p;
		}), source);
		positions = [];
		timeBaseline = null;
	}), callbackThreshold);
	const updatePosition = callbackWrapper$1(throttle$1(callbackWrapper$1((evt) => {
		const target = getEventTarget(evt);
		const { clientX, clientY } = legacy_isTouchEvent(evt) ? evt.changedTouches[0] : evt;
		if (!timeBaseline) timeBaseline = nowTimestamp();
		positions.push({
			x: clientX,
			y: clientY,
			id: mirror2.getId(target),
			timeOffset: nowTimestamp() - timeBaseline
		});
		wrappedCb(typeof DragEvent !== "undefined" && evt instanceof DragEvent ? IncrementalSource.Drag : evt instanceof MouseEvent ? IncrementalSource.MouseMove : IncrementalSource.TouchMove);
	}), threshold, { trailing: false }));
	const handlers = [
		on("mousemove", updatePosition, doc),
		on("touchmove", updatePosition, doc),
		on("drag", updatePosition, doc)
	];
	return callbackWrapper$1(() => {
		handlers.forEach((h) => h());
	});
}
function initMouseInteractionObserver({ mouseInteractionCb, doc, mirror: mirror2, blockClass, blockSelector, unblockSelector, sampling }) {
	if (sampling.mouseInteraction === false) return () => {};
	const disableMap = sampling.mouseInteraction === true || sampling.mouseInteraction === void 0 ? {} : sampling.mouseInteraction;
	const handlers = [];
	let currentPointerType = null;
	const getHandler = (eventKey) => {
		return (event) => {
			const target = getEventTarget(event);
			if (isBlocked$1(target, blockClass, blockSelector, unblockSelector, true)) return;
			let pointerType = null;
			let thisEventKey = eventKey;
			if ("pointerType" in event) {
				switch (event.pointerType) {
					case "mouse":
						pointerType = PointerTypes.Mouse;
						break;
					case "touch":
						pointerType = PointerTypes.Touch;
						break;
					case "pen": pointerType = PointerTypes.Pen;
				}
				if (pointerType === PointerTypes.Touch) {
					if (MouseInteractions[eventKey] === MouseInteractions.MouseDown) thisEventKey = "TouchStart";
					else if (MouseInteractions[eventKey] === MouseInteractions.MouseUp) thisEventKey = "TouchEnd";
				} else if (pointerType === PointerTypes.Pen);
			} else if (legacy_isTouchEvent(event)) pointerType = PointerTypes.Touch;
			if (pointerType !== null) {
				currentPointerType = pointerType;
				if (thisEventKey.startsWith("Touch") && pointerType === PointerTypes.Touch || thisEventKey.startsWith("Mouse") && pointerType === PointerTypes.Mouse) pointerType = null;
			} else if (MouseInteractions[eventKey] === MouseInteractions.Click) {
				pointerType = currentPointerType;
				currentPointerType = null;
			}
			const e2 = legacy_isTouchEvent(event) ? event.changedTouches[0] : event;
			if (!e2) return;
			const id = mirror2.getId(target);
			const { clientX, clientY } = e2;
			callbackWrapper$1(mouseInteractionCb)({
				type: MouseInteractions[thisEventKey],
				id,
				x: clientX,
				y: clientY,
				...pointerType !== null && { pointerType }
			});
		};
	};
	Object.keys(MouseInteractions).filter((key) => Number.isNaN(Number(key)) && !key.endsWith("_Departed") && disableMap[key] !== false).forEach((eventKey) => {
		let eventName = toLowerCase(eventKey);
		const handler = getHandler(eventKey);
		if (window.PointerEvent) switch (MouseInteractions[eventKey]) {
			case MouseInteractions.MouseDown:
			case MouseInteractions.MouseUp:
				eventName = eventName.replace("mouse", "pointer");
				break;
			case MouseInteractions.TouchStart:
			case MouseInteractions.TouchEnd: return;
		}
		handlers.push(on(eventName, handler, doc));
	});
	return callbackWrapper$1(() => {
		handlers.forEach((h) => h());
	});
}
function initScrollObserver({ scrollCb, doc, mirror: mirror2, blockClass, blockSelector, unblockSelector, sampling }) {
	return on("scroll", callbackWrapper$1(throttle$1(callbackWrapper$1((evt) => {
		const target = getEventTarget(evt);
		if (!target || isBlocked$1(target, blockClass, blockSelector, unblockSelector, true)) return;
		const id = mirror2.getId(target);
		if (target === doc && doc.defaultView) {
			const scrollLeftTop = getWindowScroll(doc.defaultView);
			scrollCb({
				id,
				x: scrollLeftTop.left,
				y: scrollLeftTop.top
			});
		} else scrollCb({
			id,
			x: target.scrollLeft,
			y: target.scrollTop
		});
	}), sampling.scroll || 100)), doc);
}
function initViewportResizeObserver({ viewportResizeCb }, { win }) {
	let lastH = -1;
	let lastW = -1;
	return on("resize", callbackWrapper$1(throttle$1(callbackWrapper$1(() => {
		const height = getWindowHeight();
		const width = getWindowWidth();
		if (lastH !== height || lastW !== width) {
			viewportResizeCb({
				width: Number(width),
				height: Number(height)
			});
			lastH = height;
			lastW = width;
		}
	}), 200)), win);
}
var INPUT_TAGS = [
	"INPUT",
	"TEXTAREA",
	"SELECT"
];
var lastInputValueMap = /* @__PURE__ */ new WeakMap();
function initInputObserver({ inputCb, doc, mirror: mirror2, blockClass, blockSelector, unblockSelector, ignoreClass, ignoreSelector, maskInputOptions, maskInputFn, sampling, userTriggeredOnInput, maskTextClass, unmaskTextClass, maskTextSelector, unmaskTextSelector }) {
	function eventHandler(event) {
		let target = getEventTarget(event);
		const userTriggered = event.isTrusted;
		const tagName = target && toUpperCase(target.tagName);
		if (tagName === "OPTION") target = target.parentElement;
		if (!target || !tagName || INPUT_TAGS.indexOf(tagName) < 0 || isBlocked$1(target, blockClass, blockSelector, unblockSelector, true)) return;
		const el = target;
		if (el.classList.contains(ignoreClass) || ignoreSelector && el.matches(ignoreSelector)) return;
		const type = getInputType(target);
		let text = getInputValue(el, tagName, type);
		let isChecked = false;
		const isInputMasked = shouldMaskInput({
			maskInputOptions,
			tagName,
			type
		});
		const forceMask = needMaskingText(target, maskTextClass, maskTextSelector, unmaskTextClass, unmaskTextSelector, isInputMasked);
		if (type === "radio" || type === "checkbox") isChecked = target.checked;
		text = maskInputValue({
			isMasked: forceMask,
			element: target,
			value: text,
			maskInputFn
		});
		cbWithDedup(target, userTriggeredOnInput ? {
			text,
			isChecked,
			userTriggered
		} : {
			text,
			isChecked
		});
		const name = target.name;
		if (type === "radio" && name && isChecked) doc.querySelectorAll(`input[type="radio"][name="${name}"]`).forEach((el2) => {
			if (el2 !== target) {
				const text2 = maskInputValue({
					isMasked: forceMask,
					element: el2,
					value: getInputValue(el2, tagName, type),
					maskInputFn
				});
				cbWithDedup(el2, userTriggeredOnInput ? {
					text: text2,
					isChecked: !isChecked,
					userTriggered: false
				} : {
					text: text2,
					isChecked: !isChecked
				});
			}
		});
	}
	function cbWithDedup(target, v2) {
		const lastInputValue = lastInputValueMap.get(target);
		if (!lastInputValue || lastInputValue.text !== v2.text || lastInputValue.isChecked !== v2.isChecked) {
			lastInputValueMap.set(target, v2);
			const id = mirror2.getId(target);
			callbackWrapper$1(inputCb)({
				...v2,
				id
			});
		}
	}
	const handlers = (sampling.input === "last" ? ["change"] : ["input", "change"]).map((eventName) => on(eventName, callbackWrapper$1(eventHandler), doc));
	const currentWindow = doc.defaultView;
	if (!currentWindow) return () => {
		handlers.forEach((h) => h());
	};
	const propertyDescriptor = currentWindow.Object.getOwnPropertyDescriptor(currentWindow.HTMLInputElement.prototype, "value");
	const hookProperties = [
		[currentWindow.HTMLInputElement.prototype, "value"],
		[currentWindow.HTMLInputElement.prototype, "checked"],
		[currentWindow.HTMLSelectElement.prototype, "value"],
		[currentWindow.HTMLTextAreaElement.prototype, "value"],
		[currentWindow.HTMLSelectElement.prototype, "selectedIndex"],
		[currentWindow.HTMLOptionElement.prototype, "selected"]
	];
	if (propertyDescriptor && propertyDescriptor.set) handlers.push(...hookProperties.map((p) => hookSetter$1(p[0], p[1], { set() {
		callbackWrapper$1(eventHandler)({
			target: this,
			isTrusted: false
		});
	} }, false, currentWindow)));
	return callbackWrapper$1(() => {
		handlers.forEach((h) => h());
	});
}
function getNestedCSSRulePositions(rule) {
	const positions = [];
	function recurse(childRule, pos) {
		if (hasNestedCSSRule("CSSGroupingRule") && childRule.parentRule instanceof CSSGroupingRule || hasNestedCSSRule("CSSMediaRule") && childRule.parentRule instanceof CSSMediaRule || hasNestedCSSRule("CSSSupportsRule") && childRule.parentRule instanceof CSSSupportsRule || hasNestedCSSRule("CSSConditionRule") && childRule.parentRule instanceof CSSConditionRule) {
			const index = Array.from(childRule.parentRule.cssRules).indexOf(childRule);
			pos.unshift(index);
		} else if (childRule.parentStyleSheet) {
			const index = Array.from(childRule.parentStyleSheet.cssRules).indexOf(childRule);
			pos.unshift(index);
		}
		return pos;
	}
	return recurse(rule, positions);
}
function getIdAndStyleId(sheet, mirror2, styleMirror) {
	let id, styleId;
	if (!sheet) return {};
	if (sheet.ownerNode) id = mirror2.getId(sheet.ownerNode);
	else styleId = styleMirror.getId(sheet);
	return {
		styleId,
		id
	};
}
function initStyleSheetObserver({ styleSheetRuleCb, mirror: mirror2, stylesheetManager }, { win }) {
	if (!win.CSSStyleSheet || !win.CSSStyleSheet.prototype) return () => {};
	const insertRule = win.CSSStyleSheet.prototype.insertRule;
	win.CSSStyleSheet.prototype.insertRule = new Proxy(insertRule, { apply: callbackWrapper$1((target, thisArg, argumentsList) => {
		const [rule, index] = argumentsList;
		const { id, styleId } = getIdAndStyleId(thisArg, mirror2, stylesheetManager.styleMirror);
		if (id && id !== -1 || styleId && styleId !== -1) styleSheetRuleCb({
			id,
			styleId,
			adds: [{
				rule,
				index
			}]
		});
		return target.apply(thisArg, argumentsList);
	}) });
	const deleteRule = win.CSSStyleSheet.prototype.deleteRule;
	win.CSSStyleSheet.prototype.deleteRule = new Proxy(deleteRule, { apply: callbackWrapper$1((target, thisArg, argumentsList) => {
		const [index] = argumentsList;
		const { id, styleId } = getIdAndStyleId(thisArg, mirror2, stylesheetManager.styleMirror);
		if (id && id !== -1 || styleId && styleId !== -1) styleSheetRuleCb({
			id,
			styleId,
			removes: [{ index }]
		});
		return target.apply(thisArg, argumentsList);
	}) });
	let replace;
	if (win.CSSStyleSheet.prototype.replace) {
		replace = win.CSSStyleSheet.prototype.replace;
		win.CSSStyleSheet.prototype.replace = new Proxy(replace, { apply: callbackWrapper$1((target, thisArg, argumentsList) => {
			const [text] = argumentsList;
			const { id, styleId } = getIdAndStyleId(thisArg, mirror2, stylesheetManager.styleMirror);
			if (id && id !== -1 || styleId && styleId !== -1) styleSheetRuleCb({
				id,
				styleId,
				replace: text
			});
			return target.apply(thisArg, argumentsList);
		}) });
	}
	let replaceSync;
	if (win.CSSStyleSheet.prototype.replaceSync) {
		replaceSync = win.CSSStyleSheet.prototype.replaceSync;
		win.CSSStyleSheet.prototype.replaceSync = new Proxy(replaceSync, { apply: callbackWrapper$1((target, thisArg, argumentsList) => {
			const [text] = argumentsList;
			const { id, styleId } = getIdAndStyleId(thisArg, mirror2, stylesheetManager.styleMirror);
			if (id && id !== -1 || styleId && styleId !== -1) styleSheetRuleCb({
				id,
				styleId,
				replaceSync: text
			});
			return target.apply(thisArg, argumentsList);
		}) });
	}
	const supportedNestedCSSRuleTypes = {};
	if (canMonkeyPatchNestedCSSRule("CSSGroupingRule")) supportedNestedCSSRuleTypes.CSSGroupingRule = win.CSSGroupingRule;
	else {
		if (canMonkeyPatchNestedCSSRule("CSSMediaRule")) supportedNestedCSSRuleTypes.CSSMediaRule = win.CSSMediaRule;
		if (canMonkeyPatchNestedCSSRule("CSSConditionRule")) supportedNestedCSSRuleTypes.CSSConditionRule = win.CSSConditionRule;
		if (canMonkeyPatchNestedCSSRule("CSSSupportsRule")) supportedNestedCSSRuleTypes.CSSSupportsRule = win.CSSSupportsRule;
	}
	const unmodifiedFunctions = {};
	Object.entries(supportedNestedCSSRuleTypes).forEach(([typeKey, type]) => {
		unmodifiedFunctions[typeKey] = {
			insertRule: type.prototype.insertRule,
			deleteRule: type.prototype.deleteRule
		};
		type.prototype.insertRule = new Proxy(unmodifiedFunctions[typeKey].insertRule, { apply: callbackWrapper$1((target, thisArg, argumentsList) => {
			const [rule, index] = argumentsList;
			const { id, styleId } = getIdAndStyleId(thisArg.parentStyleSheet, mirror2, stylesheetManager.styleMirror);
			if (id && id !== -1 || styleId && styleId !== -1) styleSheetRuleCb({
				id,
				styleId,
				adds: [{
					rule,
					index: [...getNestedCSSRulePositions(thisArg), index || 0]
				}]
			});
			return target.apply(thisArg, argumentsList);
		}) });
		type.prototype.deleteRule = new Proxy(unmodifiedFunctions[typeKey].deleteRule, { apply: callbackWrapper$1((target, thisArg, argumentsList) => {
			const [index] = argumentsList;
			const { id, styleId } = getIdAndStyleId(thisArg.parentStyleSheet, mirror2, stylesheetManager.styleMirror);
			if (id && id !== -1 || styleId && styleId !== -1) styleSheetRuleCb({
				id,
				styleId,
				removes: [{ index: [...getNestedCSSRulePositions(thisArg), index] }]
			});
			return target.apply(thisArg, argumentsList);
		}) });
	});
	return callbackWrapper$1(() => {
		win.CSSStyleSheet.prototype.insertRule = insertRule;
		win.CSSStyleSheet.prototype.deleteRule = deleteRule;
		replace && (win.CSSStyleSheet.prototype.replace = replace);
		replaceSync && (win.CSSStyleSheet.prototype.replaceSync = replaceSync);
		Object.entries(supportedNestedCSSRuleTypes).forEach(([typeKey, type]) => {
			type.prototype.insertRule = unmodifiedFunctions[typeKey].insertRule;
			type.prototype.deleteRule = unmodifiedFunctions[typeKey].deleteRule;
		});
	});
}
function initAdoptedStyleSheetObserver({ mirror: mirror2, stylesheetManager }, host) {
	let hostId = null;
	if (host.nodeName === "#document") hostId = mirror2.getId(host);
	else hostId = mirror2.getId(host.host);
	const patchTarget = host.nodeName === "#document" ? host.defaultView?.Document : host.ownerDocument?.defaultView?.ShadowRoot;
	const originalPropertyDescriptor = patchTarget?.prototype ? Object.getOwnPropertyDescriptor(patchTarget?.prototype, "adoptedStyleSheets") : void 0;
	if (hostId === null || hostId === -1 || !patchTarget || !originalPropertyDescriptor) return () => {};
	Object.defineProperty(host, "adoptedStyleSheets", {
		configurable: originalPropertyDescriptor.configurable,
		enumerable: originalPropertyDescriptor.enumerable,
		get() {
			return originalPropertyDescriptor.get?.call(this);
		},
		set(sheets) {
			const result = originalPropertyDescriptor.set?.call(this, sheets);
			if (hostId !== null && hostId !== -1) try {
				stylesheetManager.adoptStyleSheets(sheets, hostId);
			} catch (e2) {}
			return result;
		}
	});
	return callbackWrapper$1(() => {
		Object.defineProperty(host, "adoptedStyleSheets", {
			configurable: originalPropertyDescriptor.configurable,
			enumerable: originalPropertyDescriptor.enumerable,
			get: originalPropertyDescriptor.get,
			set: originalPropertyDescriptor.set
		});
	});
}
function initStyleDeclarationObserver({ styleDeclarationCb, mirror: mirror2, ignoreCSSAttributes, stylesheetManager }, { win }) {
	const setProperty = win.CSSStyleDeclaration.prototype.setProperty;
	win.CSSStyleDeclaration.prototype.setProperty = new Proxy(setProperty, { apply: callbackWrapper$1((target, thisArg, argumentsList) => {
		const [property, value, priority] = argumentsList;
		if (ignoreCSSAttributes.has(property)) return setProperty.apply(thisArg, [
			property,
			value,
			priority
		]);
		const { id, styleId } = getIdAndStyleId(thisArg.parentRule?.parentStyleSheet, mirror2, stylesheetManager.styleMirror);
		if (id && id !== -1 || styleId && styleId !== -1) styleDeclarationCb({
			id,
			styleId,
			set: {
				property,
				value,
				priority
			},
			index: getNestedCSSRulePositions(thisArg.parentRule)
		});
		return target.apply(thisArg, argumentsList);
	}) });
	const removeProperty = win.CSSStyleDeclaration.prototype.removeProperty;
	win.CSSStyleDeclaration.prototype.removeProperty = new Proxy(removeProperty, { apply: callbackWrapper$1((target, thisArg, argumentsList) => {
		const [property] = argumentsList;
		if (ignoreCSSAttributes.has(property)) return removeProperty.apply(thisArg, [property]);
		const { id, styleId } = getIdAndStyleId(thisArg.parentRule?.parentStyleSheet, mirror2, stylesheetManager.styleMirror);
		if (id && id !== -1 || styleId && styleId !== -1) styleDeclarationCb({
			id,
			styleId,
			remove: { property },
			index: getNestedCSSRulePositions(thisArg.parentRule)
		});
		return target.apply(thisArg, argumentsList);
	}) });
	return callbackWrapper$1(() => {
		win.CSSStyleDeclaration.prototype.setProperty = setProperty;
		win.CSSStyleDeclaration.prototype.removeProperty = removeProperty;
	});
}
function initMediaInteractionObserver({ mediaInteractionCb, blockClass, blockSelector, unblockSelector, mirror: mirror2, sampling, doc }) {
	const handler = callbackWrapper$1((type) => throttle$1(callbackWrapper$1((event) => {
		const target = getEventTarget(event);
		if (!target || isBlocked$1(target, blockClass, blockSelector, unblockSelector, true)) return;
		const { currentTime, volume, muted, playbackRate } = target;
		mediaInteractionCb({
			type,
			id: mirror2.getId(target),
			currentTime,
			volume,
			muted,
			playbackRate
		});
	}), sampling.media || 500));
	const handlers = [
		on("play", handler(MediaInteractions.Play), doc),
		on("pause", handler(MediaInteractions.Pause), doc),
		on("seeked", handler(MediaInteractions.Seeked), doc),
		on("volumechange", handler(MediaInteractions.VolumeChange), doc),
		on("ratechange", handler(MediaInteractions.RateChange), doc)
	];
	return callbackWrapper$1(() => {
		handlers.forEach((h) => h());
	});
}
function initFontObserver({ fontCb, doc }) {
	const win = doc.defaultView;
	if (!win) return () => {};
	const handlers = [];
	const fontMap = /* @__PURE__ */ new WeakMap();
	const originalFontFace = win.FontFace;
	win.FontFace = function FontFace2(family, source, descriptors) {
		const fontFace = new originalFontFace(family, source, descriptors);
		fontMap.set(fontFace, {
			family,
			buffer: typeof source !== "string",
			descriptors,
			fontSource: typeof source === "string" ? source : JSON.stringify(Array.from(new Uint8Array(source)))
		});
		return fontFace;
	};
	const restoreHandler = patch$1(doc.fonts, "add", function(original) {
		return function(fontFace) {
			setTimeout$2(callbackWrapper$1(() => {
				const p = fontMap.get(fontFace);
				if (p) {
					fontCb(p);
					fontMap.delete(fontFace);
				}
			}), 0);
			return original.apply(this, [fontFace]);
		};
	});
	handlers.push(() => {
		win.FontFace = originalFontFace;
	});
	handlers.push(restoreHandler);
	return callbackWrapper$1(() => {
		handlers.forEach((h) => h());
	});
}
function initSelectionObserver(param) {
	const { doc, mirror: mirror2, blockClass, blockSelector, unblockSelector, selectionCb } = param;
	let collapsed = true;
	const updateSelection = callbackWrapper$1(() => {
		const selection = doc.getSelection();
		if (!selection || collapsed && selection?.isCollapsed) return;
		collapsed = selection.isCollapsed || false;
		const ranges = [];
		const count = selection.rangeCount || 0;
		for (let i2 = 0; i2 < count; i2++) {
			const { startContainer, startOffset, endContainer, endOffset } = selection.getRangeAt(i2);
			if (isBlocked$1(startContainer, blockClass, blockSelector, unblockSelector, true) || isBlocked$1(endContainer, blockClass, blockSelector, unblockSelector, true)) continue;
			ranges.push({
				start: mirror2.getId(startContainer),
				startOffset,
				end: mirror2.getId(endContainer),
				endOffset
			});
		}
		selectionCb({ ranges });
	});
	updateSelection();
	return on("selectionchange", updateSelection);
}
function initCustomElementObserver({ doc, customElementCb }) {
	const win = doc.defaultView;
	if (!win || !win.customElements) return () => {};
	return patch$1(win.customElements, "define", function(original) {
		return function(name, constructor, options) {
			try {
				customElementCb({ define: { name } });
			} catch (e2) {}
			return original.apply(this, [
				name,
				constructor,
				options
			]);
		};
	});
}
function initObservers(o2, _hooks = {}) {
	const currentWindow = o2.doc.defaultView;
	if (!currentWindow) return () => {};
	let mutationObserver;
	if (o2.recordDOM) mutationObserver = initMutationObserver(o2, o2.doc);
	const mousemoveHandler = initMoveObserver(o2);
	const mouseInteractionHandler = initMouseInteractionObserver(o2);
	const scrollHandler = initScrollObserver(o2);
	const viewportResizeHandler = initViewportResizeObserver(o2, { win: currentWindow });
	const inputHandler = initInputObserver(o2);
	const mediaInteractionHandler = initMediaInteractionObserver(o2);
	let styleSheetObserver = () => {};
	let adoptedStyleSheetObserver = () => {};
	let styleDeclarationObserver = () => {};
	let fontObserver = () => {};
	if (o2.recordDOM) {
		styleSheetObserver = initStyleSheetObserver(o2, { win: currentWindow });
		adoptedStyleSheetObserver = initAdoptedStyleSheetObserver(o2, o2.doc);
		styleDeclarationObserver = initStyleDeclarationObserver(o2, { win: currentWindow });
		if (o2.collectFonts) fontObserver = initFontObserver(o2);
	}
	const selectionObserver = initSelectionObserver(o2);
	const customElementObserver = initCustomElementObserver(o2);
	const pluginHandlers = [];
	for (const plugin of o2.plugins) pluginHandlers.push(plugin.observer(plugin.callback, currentWindow, plugin.options));
	return callbackWrapper$1(() => {
		mutationBuffers.forEach((b) => b.reset());
		mutationObserver?.disconnect();
		mousemoveHandler();
		mouseInteractionHandler();
		scrollHandler();
		viewportResizeHandler();
		inputHandler();
		mediaInteractionHandler();
		styleSheetObserver();
		adoptedStyleSheetObserver();
		styleDeclarationObserver();
		fontObserver();
		selectionObserver();
		customElementObserver();
		pluginHandlers.forEach((h) => h());
	});
}
function hasNestedCSSRule(prop) {
	return typeof window[prop] !== "undefined";
}
function canMonkeyPatchNestedCSSRule(prop) {
	return Boolean(typeof window[prop] !== "undefined" && window[prop].prototype && "insertRule" in window[prop].prototype && "deleteRule" in window[prop].prototype);
}
var CrossOriginIframeMirror = class {
	constructor(generateIdFn) {
		this.generateIdFn = generateIdFn;
		this.iframeIdToRemoteIdMap = /* @__PURE__ */ new WeakMap();
		this.iframeRemoteIdToIdMap = /* @__PURE__ */ new WeakMap();
	}
	getId(iframe, remoteId, idToRemoteMap, remoteToIdMap) {
		const idToRemoteIdMap = idToRemoteMap || this.getIdToRemoteIdMap(iframe);
		const remoteIdToIdMap = remoteToIdMap || this.getRemoteIdToIdMap(iframe);
		let id = idToRemoteIdMap.get(remoteId);
		if (!id) {
			id = this.generateIdFn();
			idToRemoteIdMap.set(remoteId, id);
			remoteIdToIdMap.set(id, remoteId);
		}
		return id;
	}
	getIds(iframe, remoteId) {
		const idToRemoteIdMap = this.getIdToRemoteIdMap(iframe);
		const remoteIdToIdMap = this.getRemoteIdToIdMap(iframe);
		return remoteId.map((id) => this.getId(iframe, id, idToRemoteIdMap, remoteIdToIdMap));
	}
	getRemoteId(iframe, id, map) {
		const remoteIdToIdMap = map || this.getRemoteIdToIdMap(iframe);
		if (typeof id !== "number") return id;
		const remoteId = remoteIdToIdMap.get(id);
		if (!remoteId) return -1;
		return remoteId;
	}
	getRemoteIds(iframe, ids) {
		const remoteIdToIdMap = this.getRemoteIdToIdMap(iframe);
		return ids.map((id) => this.getRemoteId(iframe, id, remoteIdToIdMap));
	}
	reset(iframe) {
		if (!iframe) {
			this.iframeIdToRemoteIdMap = /* @__PURE__ */ new WeakMap();
			this.iframeRemoteIdToIdMap = /* @__PURE__ */ new WeakMap();
			return;
		}
		this.iframeIdToRemoteIdMap.delete(iframe);
		this.iframeRemoteIdToIdMap.delete(iframe);
	}
	getIdToRemoteIdMap(iframe) {
		let idToRemoteIdMap = this.iframeIdToRemoteIdMap.get(iframe);
		if (!idToRemoteIdMap) {
			idToRemoteIdMap = /* @__PURE__ */ new Map();
			this.iframeIdToRemoteIdMap.set(iframe, idToRemoteIdMap);
		}
		return idToRemoteIdMap;
	}
	getRemoteIdToIdMap(iframe) {
		let remoteIdToIdMap = this.iframeRemoteIdToIdMap.get(iframe);
		if (!remoteIdToIdMap) {
			remoteIdToIdMap = /* @__PURE__ */ new Map();
			this.iframeRemoteIdToIdMap.set(iframe, remoteIdToIdMap);
		}
		return remoteIdToIdMap;
	}
};
var IframeManagerNoop = class {
	constructor() {
		this.crossOriginIframeMirror = new CrossOriginIframeMirror(genId);
		this.crossOriginIframeRootIdMap = /* @__PURE__ */ new WeakMap();
	}
	addIframe() {}
	addLoadListener() {}
	attachIframe() {}
};
var IframeManager = class {
	constructor(options) {
		this.iframes = /* @__PURE__ */ new WeakMap();
		this.crossOriginIframeMap = /* @__PURE__ */ new WeakMap();
		this.crossOriginIframeMirror = new CrossOriginIframeMirror(genId);
		this.crossOriginIframeRootIdMap = /* @__PURE__ */ new WeakMap();
		this.mutationCb = options.mutationCb;
		this.wrappedEmit = options.wrappedEmit;
		this.stylesheetManager = options.stylesheetManager;
		this.recordCrossOriginIframes = options.recordCrossOriginIframes;
		this.crossOriginIframeStyleMirror = new CrossOriginIframeMirror(this.stylesheetManager.styleMirror.generateId.bind(this.stylesheetManager.styleMirror));
		this.mirror = options.mirror;
		if (this.recordCrossOriginIframes) window.addEventListener("message", this.handleMessage.bind(this));
	}
	addIframe(iframeEl) {
		this.iframes.set(iframeEl, true);
		const contentWindow = getIFrameContentWindow(iframeEl);
		if (contentWindow) this.crossOriginIframeMap.set(contentWindow, iframeEl);
	}
	addLoadListener(cb) {
		this.loadListener = cb;
	}
	attachIframe(iframeEl, childSn) {
		this.mutationCb({
			adds: [{
				parentId: this.mirror.getId(iframeEl),
				nextId: null,
				node: childSn
			}],
			removes: [],
			texts: [],
			attributes: [],
			isAttachIframe: true
		});
		if (this.recordCrossOriginIframes) getIFrameContentWindow(iframeEl)?.addEventListener("message", this.handleMessage.bind(this));
		this.loadListener?.(iframeEl);
		const iframeDoc = getIFrameContentDocument(iframeEl);
		if (iframeDoc && iframeDoc.adoptedStyleSheets && iframeDoc.adoptedStyleSheets.length > 0) this.stylesheetManager.adoptStyleSheets(iframeDoc.adoptedStyleSheets, this.mirror.getId(iframeDoc));
	}
	handleMessage(message) {
		const crossOriginMessageEvent = message;
		if (crossOriginMessageEvent.data.type !== "rrweb" || crossOriginMessageEvent.origin !== crossOriginMessageEvent.data.origin) return;
		if (!message.source) return;
		const iframeEl = this.crossOriginIframeMap.get(message.source);
		if (!iframeEl) return;
		const transformedEvent = this.transformCrossOriginEvent(iframeEl, crossOriginMessageEvent.data.event);
		if (transformedEvent) this.wrappedEmit(transformedEvent, crossOriginMessageEvent.data.isCheckout);
	}
	transformCrossOriginEvent(iframeEl, e2) {
		switch (e2.type) {
			case EventType.FullSnapshot: {
				this.crossOriginIframeMirror.reset(iframeEl);
				this.crossOriginIframeStyleMirror.reset(iframeEl);
				this.replaceIdOnNode(e2.data.node, iframeEl);
				const rootId = e2.data.node.id;
				this.crossOriginIframeRootIdMap.set(iframeEl, rootId);
				this.patchRootIdOnNode(e2.data.node, rootId);
				return {
					timestamp: e2.timestamp,
					type: EventType.IncrementalSnapshot,
					data: {
						source: IncrementalSource.Mutation,
						adds: [{
							parentId: this.mirror.getId(iframeEl),
							nextId: null,
							node: e2.data.node
						}],
						removes: [],
						texts: [],
						attributes: [],
						isAttachIframe: true
					}
				};
			}
			case EventType.Meta:
			case EventType.Load:
			case EventType.DomContentLoaded: return false;
			case EventType.Plugin: return e2;
			case EventType.Custom:
				this.replaceIds(e2.data.payload, iframeEl, [
					"id",
					"parentId",
					"previousId",
					"nextId"
				]);
				return e2;
			case EventType.IncrementalSnapshot: switch (e2.data.source) {
				case IncrementalSource.Mutation:
					e2.data.adds.forEach((n2) => {
						this.replaceIds(n2, iframeEl, [
							"parentId",
							"nextId",
							"previousId"
						]);
						this.replaceIdOnNode(n2.node, iframeEl);
						const rootId = this.crossOriginIframeRootIdMap.get(iframeEl);
						rootId && this.patchRootIdOnNode(n2.node, rootId);
					});
					e2.data.removes.forEach((n2) => {
						this.replaceIds(n2, iframeEl, ["parentId", "id"]);
					});
					e2.data.attributes.forEach((n2) => {
						this.replaceIds(n2, iframeEl, ["id"]);
					});
					e2.data.texts.forEach((n2) => {
						this.replaceIds(n2, iframeEl, ["id"]);
					});
					return e2;
				case IncrementalSource.Drag:
				case IncrementalSource.TouchMove:
				case IncrementalSource.MouseMove:
					e2.data.positions.forEach((p) => {
						this.replaceIds(p, iframeEl, ["id"]);
					});
					return e2;
				case IncrementalSource.ViewportResize: return false;
				case IncrementalSource.MediaInteraction:
				case IncrementalSource.MouseInteraction:
				case IncrementalSource.Scroll:
				case IncrementalSource.CanvasMutation:
				case IncrementalSource.Input:
					this.replaceIds(e2.data, iframeEl, ["id"]);
					return e2;
				case IncrementalSource.StyleSheetRule:
				case IncrementalSource.StyleDeclaration:
					this.replaceIds(e2.data, iframeEl, ["id"]);
					this.replaceStyleIds(e2.data, iframeEl, ["styleId"]);
					return e2;
				case IncrementalSource.Font: return e2;
				case IncrementalSource.Selection:
					e2.data.ranges.forEach((range) => {
						this.replaceIds(range, iframeEl, ["start", "end"]);
					});
					return e2;
				case IncrementalSource.AdoptedStyleSheet:
					this.replaceIds(e2.data, iframeEl, ["id"]);
					this.replaceStyleIds(e2.data, iframeEl, ["styleIds"]);
					e2.data.styles?.forEach((style) => {
						this.replaceStyleIds(style, iframeEl, ["styleId"]);
					});
					return e2;
			}
		}
		return false;
	}
	replace(iframeMirror, obj, iframeEl, keys) {
		for (const key of keys) {
			if (!Array.isArray(obj[key]) && typeof obj[key] !== "number") continue;
			if (Array.isArray(obj[key])) obj[key] = iframeMirror.getIds(iframeEl, obj[key]);
			else obj[key] = iframeMirror.getId(iframeEl, obj[key]);
		}
		return obj;
	}
	replaceIds(obj, iframeEl, keys) {
		return this.replace(this.crossOriginIframeMirror, obj, iframeEl, keys);
	}
	replaceStyleIds(obj, iframeEl, keys) {
		return this.replace(this.crossOriginIframeStyleMirror, obj, iframeEl, keys);
	}
	replaceIdOnNode(node, iframeEl) {
		this.replaceIds(node, iframeEl, ["id", "rootId"]);
		if ("childNodes" in node) node.childNodes.forEach((child) => {
			this.replaceIdOnNode(child, iframeEl);
		});
	}
	patchRootIdOnNode(node, rootId) {
		if (node.type !== NodeType$1.Document && !node.rootId) node.rootId = rootId;
		if ("childNodes" in node) node.childNodes.forEach((child) => {
			this.patchRootIdOnNode(child, rootId);
		});
	}
};
var ShadowDomManagerNoop = class {
	init() {}
	addShadowRoot() {}
	observeAttachShadow() {}
	reset() {}
};
var ShadowDomManager = class {
	constructor(options) {
		this.shadowDoms = /* @__PURE__ */ new WeakSet();
		this.restoreHandlers = [];
		this.mutationCb = options.mutationCb;
		this.scrollCb = options.scrollCb;
		this.bypassOptions = options.bypassOptions;
		this.mirror = options.mirror;
		this.init();
	}
	init() {
		this.reset();
		this.patchAttachShadow(Element, document);
	}
	addShadowRoot(shadowRoot, doc) {
		if (!isNativeShadowDom(shadowRoot)) return;
		if (this.shadowDoms.has(shadowRoot)) return;
		this.shadowDoms.add(shadowRoot);
		this.bypassOptions.canvasManager.addShadowRoot(shadowRoot);
		const observer = initMutationObserver({
			...this.bypassOptions,
			doc,
			mutationCb: this.mutationCb,
			mirror: this.mirror,
			shadowDomManager: this
		}, shadowRoot);
		this.restoreHandlers.push(() => observer.disconnect());
		this.restoreHandlers.push(initScrollObserver({
			...this.bypassOptions,
			scrollCb: this.scrollCb,
			doc: shadowRoot,
			mirror: this.mirror
		}));
		setTimeout$2(() => {
			if (shadowRoot.adoptedStyleSheets && shadowRoot.adoptedStyleSheets.length > 0) this.bypassOptions.stylesheetManager.adoptStyleSheets(shadowRoot.adoptedStyleSheets, this.mirror.getId(shadowRoot.host));
			this.restoreHandlers.push(initAdoptedStyleSheetObserver({
				mirror: this.mirror,
				stylesheetManager: this.bypassOptions.stylesheetManager
			}, shadowRoot));
		}, 0);
	}
	/**
	* Monkey patch 'attachShadow' of an IFrameElement to observe newly added shadow doms.
	*/
	observeAttachShadow(iframeElement) {
		const iframeDoc = getIFrameContentDocument(iframeElement);
		const iframeWindow = getIFrameContentWindow(iframeElement);
		if (!iframeDoc || !iframeWindow) return;
		this.patchAttachShadow(iframeWindow.Element, iframeDoc);
	}
	/**
	* Patch 'attachShadow' to observe newly added shadow doms.
	*/
	patchAttachShadow(element, doc) {
		const manager = this;
		this.restoreHandlers.push(patch$1(element.prototype, "attachShadow", function(original) {
			return function(option) {
				const shadowRoot = original.call(this, option);
				if (this.shadowRoot && inDom(this)) manager.addShadowRoot(this.shadowRoot, doc);
				return shadowRoot;
			};
		}));
	}
	reset() {
		this.restoreHandlers.forEach((handler) => {
			try {
				handler();
			} catch (e2) {}
		});
		this.restoreHandlers = [];
		this.shadowDoms = /* @__PURE__ */ new WeakSet();
		this.bypassOptions.canvasManager.resetShadowRoots();
	}
};
var StylesheetManager = class {
	constructor(options) {
		this.trackedLinkElements = /* @__PURE__ */ new WeakSet();
		this.styleMirror = new StyleSheetMirror();
		this.mutationCb = options.mutationCb;
		this.adoptedStyleSheetCb = options.adoptedStyleSheetCb;
	}
	attachLinkElement(linkEl, childSn) {
		if ("_cssText" in childSn.attributes) this.mutationCb({
			adds: [],
			removes: [],
			texts: [],
			attributes: [{
				id: childSn.id,
				attributes: childSn.attributes
			}]
		});
		this.trackLinkElement(linkEl);
	}
	trackLinkElement(linkEl) {
		if (this.trackedLinkElements.has(linkEl)) return;
		this.trackedLinkElements.add(linkEl);
		this.trackStylesheetInLinkElement(linkEl);
	}
	adoptStyleSheets(sheets, hostId) {
		if (sheets.length === 0) return;
		const adoptedStyleSheetData = {
			id: hostId,
			styleIds: []
		};
		const styles = [];
		for (const sheet of sheets) {
			let styleId;
			if (!this.styleMirror.has(sheet)) {
				styleId = this.styleMirror.add(sheet);
				styles.push({
					styleId,
					rules: Array.from(sheet.rules || CSSRule, (r2, index) => ({
						rule: stringifyRule(r2),
						index
					}))
				});
			} else styleId = this.styleMirror.getId(sheet);
			adoptedStyleSheetData.styleIds.push(styleId);
		}
		if (styles.length > 0) adoptedStyleSheetData.styles = styles;
		this.adoptedStyleSheetCb(adoptedStyleSheetData);
	}
	reset() {
		this.styleMirror.reset();
		this.trackedLinkElements = /* @__PURE__ */ new WeakSet();
	}
	trackStylesheetInLinkElement(_linkEl) {}
};
var ProcessedNodeManager = class {
	constructor() {
		this.nodeMap = /* @__PURE__ */ new WeakMap();
		this.active = false;
	}
	inOtherBuffer(node, thisBuffer) {
		const buffers = this.nodeMap.get(node);
		return buffers && Array.from(buffers).some((buffer) => buffer !== thisBuffer);
	}
	add(node, buffer) {
		if (!this.active) {
			this.active = true;
			onRequestAnimationFrame$1(() => {
				this.nodeMap = /* @__PURE__ */ new WeakMap();
				this.active = false;
			});
		}
		this.nodeMap.set(node, (this.nodeMap.get(node) || /* @__PURE__ */ new Set()).add(buffer));
	}
	destroy() {}
};
var wrappedEmit;
var _takeFullSnapshot;
try {
	if (Array.from([1], (x) => x * 2)[0] !== 2) {
		const cleanFrame = document.createElement("iframe");
		document.body.appendChild(cleanFrame);
		Array.from = cleanFrame.contentWindow?.Array.from || Array.from;
		document.body.removeChild(cleanFrame);
	}
} catch (err) {
	console.debug("Unable to override Array.from", err);
}
var mirror = createMirror$1();
function record(options = {}) {
	const { emit, checkoutEveryNms, checkoutEveryNth, blockClass = "rr-block", blockSelector = null, unblockSelector = null, ignoreClass = "rr-ignore", ignoreSelector = null, maskAllText = false, maskTextClass = "rr-mask", unmaskTextClass = null, maskTextSelector = null, unmaskTextSelector = null, inlineStylesheet = true, maskAllInputs, maskInputOptions: _maskInputOptions, slimDOMOptions: _slimDOMOptions, maskAttributeFn, maskInputFn, maskTextFn, maxCanvasSize = null, packFn, sampling = {}, dataURLOptions = {}, mousemoveWait, recordDOM = true, recordCanvas = false, recordCrossOriginIframes = false, recordAfter = options.recordAfter === "DOMContentLoaded" ? options.recordAfter : "load", userTriggeredOnInput = false, collectFonts = false, inlineImages = false, plugins, keepIframeSrcFn = () => false, ignoreCSSAttributes = /* @__PURE__ */ new Set([]), errorHandler, onMutation, getCanvasManager } = options;
	registerErrorHandler$1(errorHandler);
	const inEmittingFrame = recordCrossOriginIframes ? window.parent === window : true;
	let passEmitsToParent = false;
	if (!inEmittingFrame) try {
		if (window.parent.document) passEmitsToParent = false;
	} catch (e2) {
		passEmitsToParent = true;
	}
	if (inEmittingFrame && !emit) throw new Error("emit function is required");
	if (!inEmittingFrame && !passEmitsToParent) return () => {};
	if (mousemoveWait !== void 0 && sampling.mousemove === void 0) sampling.mousemove = mousemoveWait;
	mirror.reset();
	const maskInputOptions = maskAllInputs === true ? {
		color: true,
		date: true,
		"datetime-local": true,
		email: true,
		month: true,
		number: true,
		range: true,
		search: true,
		tel: true,
		text: true,
		time: true,
		url: true,
		week: true,
		textarea: true,
		select: true,
		radio: true,
		checkbox: true
	} : _maskInputOptions !== void 0 ? _maskInputOptions : {};
	const slimDOMOptions = _slimDOMOptions === true || _slimDOMOptions === "all" ? {
		script: true,
		comment: true,
		headFavicon: true,
		headWhitespace: true,
		headMetaSocial: true,
		headMetaRobots: true,
		headMetaHttpEquiv: true,
		headMetaVerification: true,
		headMetaAuthorship: _slimDOMOptions === "all",
		headMetaDescKeywords: _slimDOMOptions === "all"
	} : _slimDOMOptions ? _slimDOMOptions : {};
	polyfill();
	let lastFullSnapshotEvent;
	let incrementalSnapshotCount = 0;
	const eventProcessor = (e2) => {
		for (const plugin of plugins || []) if (plugin.eventProcessor) e2 = plugin.eventProcessor(e2);
		if (packFn && !passEmitsToParent) e2 = packFn(e2);
		return e2;
	};
	wrappedEmit = (r2, isCheckout) => {
		const e2 = r2;
		e2.timestamp = nowTimestamp();
		if (mutationBuffers[0]?.isFrozen() && e2.type !== EventType.FullSnapshot && !(e2.type === EventType.IncrementalSnapshot && e2.data.source === IncrementalSource.Mutation)) mutationBuffers.forEach((buf) => buf.unfreeze());
		if (inEmittingFrame) emit?.(eventProcessor(e2), isCheckout);
		else if (passEmitsToParent) {
			const message = {
				type: "rrweb",
				event: eventProcessor(e2),
				origin: window.location.origin,
				isCheckout
			};
			window.parent.postMessage(message, "*");
		}
		if (e2.type === EventType.FullSnapshot) {
			lastFullSnapshotEvent = e2;
			incrementalSnapshotCount = 0;
		} else if (e2.type === EventType.IncrementalSnapshot) {
			if (e2.data.source === IncrementalSource.Mutation && e2.data.isAttachIframe) return;
			incrementalSnapshotCount++;
			const exceedCount = checkoutEveryNth && incrementalSnapshotCount >= checkoutEveryNth;
			const exceedTime = checkoutEveryNms && lastFullSnapshotEvent && e2.timestamp - lastFullSnapshotEvent.timestamp > checkoutEveryNms;
			if (exceedCount || exceedTime) takeFullSnapshot2(true);
		}
	};
	const wrappedMutationEmit = (m) => {
		wrappedEmit({
			type: EventType.IncrementalSnapshot,
			data: {
				source: IncrementalSource.Mutation,
				...m
			}
		});
	};
	const wrappedScrollEmit = (p) => wrappedEmit({
		type: EventType.IncrementalSnapshot,
		data: {
			source: IncrementalSource.Scroll,
			...p
		}
	});
	const wrappedCanvasMutationEmit = (p) => wrappedEmit({
		type: EventType.IncrementalSnapshot,
		data: {
			source: IncrementalSource.CanvasMutation,
			...p
		}
	});
	const wrappedAdoptedStyleSheetEmit = (a2) => wrappedEmit({
		type: EventType.IncrementalSnapshot,
		data: {
			source: IncrementalSource.AdoptedStyleSheet,
			...a2
		}
	});
	const stylesheetManager = new StylesheetManager({
		mutationCb: wrappedMutationEmit,
		adoptedStyleSheetCb: wrappedAdoptedStyleSheetEmit
	});
	const iframeManager = typeof __RRWEB_EXCLUDE_IFRAME__ === "boolean" && __RRWEB_EXCLUDE_IFRAME__ ? new IframeManagerNoop() : new IframeManager({
		mirror,
		mutationCb: wrappedMutationEmit,
		stylesheetManager,
		recordCrossOriginIframes,
		wrappedEmit
	});
	for (const plugin of plugins || []) if (plugin.getMirror) plugin.getMirror({
		nodeMirror: mirror,
		crossOriginIframeMirror: iframeManager.crossOriginIframeMirror,
		crossOriginIframeStyleMirror: iframeManager.crossOriginIframeStyleMirror
	});
	const processedNodeManager = new ProcessedNodeManager();
	const canvasManager = _getCanvasManager(getCanvasManager, {
		mirror,
		win: window,
		mutationCb: (p) => wrappedEmit({
			type: EventType.IncrementalSnapshot,
			data: {
				source: IncrementalSource.CanvasMutation,
				...p
			}
		}),
		recordCanvas,
		blockClass,
		blockSelector,
		unblockSelector,
		maxCanvasSize,
		sampling: sampling["canvas"],
		dataURLOptions,
		errorHandler
	});
	const shadowDomManager = typeof __RRWEB_EXCLUDE_SHADOW_DOM__ === "boolean" && __RRWEB_EXCLUDE_SHADOW_DOM__ ? new ShadowDomManagerNoop() : new ShadowDomManager({
		mutationCb: wrappedMutationEmit,
		scrollCb: wrappedScrollEmit,
		bypassOptions: {
			onMutation,
			blockClass,
			blockSelector,
			unblockSelector,
			maskAllText,
			maskTextClass,
			unmaskTextClass,
			maskTextSelector,
			unmaskTextSelector,
			inlineStylesheet,
			maskInputOptions,
			dataURLOptions,
			maskAttributeFn,
			maskTextFn,
			maskInputFn,
			recordCanvas,
			inlineImages,
			sampling,
			slimDOMOptions,
			iframeManager,
			stylesheetManager,
			canvasManager,
			keepIframeSrcFn,
			processedNodeManager,
			ignoreCSSAttributes
		},
		mirror
	});
	const takeFullSnapshot2 = (isCheckout = false) => {
		if (!recordDOM) return;
		wrappedEmit({
			type: EventType.Meta,
			data: {
				href: window.location.href,
				width: getWindowWidth(),
				height: getWindowHeight()
			}
		}, isCheckout);
		stylesheetManager.reset();
		shadowDomManager.init();
		mutationBuffers.forEach((buf) => buf.lock());
		const node = snapshot(document, {
			mirror,
			blockClass,
			blockSelector,
			unblockSelector,
			maskAllText,
			maskTextClass,
			unmaskTextClass,
			maskTextSelector,
			unmaskTextSelector,
			inlineStylesheet,
			maskAllInputs: maskInputOptions,
			maskAttributeFn,
			maskInputFn,
			maskTextFn,
			slimDOM: slimDOMOptions,
			dataURLOptions,
			recordCanvas,
			inlineImages,
			onSerialize: (n2) => {
				if (isSerializedIframe(n2, mirror)) iframeManager.addIframe(n2);
				if (isSerializedStylesheet(n2, mirror)) stylesheetManager.trackLinkElement(n2);
				if (hasShadowRoot(n2)) shadowDomManager.addShadowRoot(n2.shadowRoot, document);
			},
			onIframeLoad: (iframe, childSn) => {
				iframeManager.attachIframe(iframe, childSn);
				const contentWindow = getIFrameContentWindow(iframe);
				if (contentWindow) canvasManager.addWindow(contentWindow);
				shadowDomManager.observeAttachShadow(iframe);
			},
			onStylesheetLoad: (linkEl, childSn) => {
				stylesheetManager.attachLinkElement(linkEl, childSn);
			},
			onBlockedImageLoad: (_imageEl, serializedNode, { width, height }) => {
				wrappedMutationEmit({
					adds: [],
					removes: [],
					texts: [],
					attributes: [{
						id: serializedNode.id,
						attributes: { style: {
							width: `${width}px`,
							height: `${height}px`
						} }
					}]
				});
			},
			keepIframeSrcFn,
			ignoreCSSAttributes
		});
		if (!node) return console.warn("Failed to snapshot the document");
		wrappedEmit({
			type: EventType.FullSnapshot,
			data: {
				node,
				initialOffset: getWindowScroll(window)
			}
		});
		mutationBuffers.forEach((buf) => buf.unlock());
		if (document.adoptedStyleSheets && document.adoptedStyleSheets.length > 0) stylesheetManager.adoptStyleSheets(document.adoptedStyleSheets, mirror.getId(document));
	};
	_takeFullSnapshot = takeFullSnapshot2;
	try {
		const handlers = [];
		const observe = (doc) => {
			return callbackWrapper$1(initObservers)({
				onMutation,
				mutationCb: wrappedMutationEmit,
				mousemoveCb: (positions, source) => wrappedEmit({
					type: EventType.IncrementalSnapshot,
					data: {
						source,
						positions
					}
				}),
				mouseInteractionCb: (d) => wrappedEmit({
					type: EventType.IncrementalSnapshot,
					data: {
						source: IncrementalSource.MouseInteraction,
						...d
					}
				}),
				scrollCb: wrappedScrollEmit,
				viewportResizeCb: (d) => wrappedEmit({
					type: EventType.IncrementalSnapshot,
					data: {
						source: IncrementalSource.ViewportResize,
						...d
					}
				}),
				inputCb: (v2) => wrappedEmit({
					type: EventType.IncrementalSnapshot,
					data: {
						source: IncrementalSource.Input,
						...v2
					}
				}),
				mediaInteractionCb: (p) => wrappedEmit({
					type: EventType.IncrementalSnapshot,
					data: {
						source: IncrementalSource.MediaInteraction,
						...p
					}
				}),
				styleSheetRuleCb: (r2) => wrappedEmit({
					type: EventType.IncrementalSnapshot,
					data: {
						source: IncrementalSource.StyleSheetRule,
						...r2
					}
				}),
				styleDeclarationCb: (r2) => wrappedEmit({
					type: EventType.IncrementalSnapshot,
					data: {
						source: IncrementalSource.StyleDeclaration,
						...r2
					}
				}),
				canvasMutationCb: wrappedCanvasMutationEmit,
				fontCb: (p) => wrappedEmit({
					type: EventType.IncrementalSnapshot,
					data: {
						source: IncrementalSource.Font,
						...p
					}
				}),
				selectionCb: (p) => {
					wrappedEmit({
						type: EventType.IncrementalSnapshot,
						data: {
							source: IncrementalSource.Selection,
							...p
						}
					});
				},
				customElementCb: (c2) => {
					wrappedEmit({
						type: EventType.IncrementalSnapshot,
						data: {
							source: IncrementalSource.CustomElement,
							...c2
						}
					});
				},
				blockClass,
				ignoreClass,
				ignoreSelector,
				maskAllText,
				maskTextClass,
				unmaskTextClass,
				maskTextSelector,
				unmaskTextSelector,
				maskInputOptions,
				inlineStylesheet,
				sampling,
				recordDOM,
				recordCanvas,
				inlineImages,
				userTriggeredOnInput,
				collectFonts,
				doc,
				maskAttributeFn,
				maskInputFn,
				maskTextFn,
				keepIframeSrcFn,
				blockSelector,
				unblockSelector,
				slimDOMOptions,
				dataURLOptions,
				mirror,
				iframeManager,
				stylesheetManager,
				shadowDomManager,
				processedNodeManager,
				canvasManager,
				ignoreCSSAttributes,
				plugins: plugins?.filter((p) => p.observer)?.map((p) => ({
					observer: p.observer,
					options: p.options,
					callback: (payload) => wrappedEmit({
						type: EventType.Plugin,
						data: {
							plugin: p.name,
							payload
						}
					})
				})) || []
			}, {});
		};
		iframeManager.addLoadListener((iframeEl) => {
			try {
				handlers.push(observe(iframeEl.contentDocument));
			} catch (error) {
				console.warn(error);
			}
		});
		const init = () => {
			takeFullSnapshot2();
			handlers.push(observe(document));
		};
		if (document.readyState === "interactive" || document.readyState === "complete") init();
		else {
			handlers.push(on("DOMContentLoaded", () => {
				wrappedEmit({
					type: EventType.DomContentLoaded,
					data: {}
				});
				if (recordAfter === "DOMContentLoaded") init();
			}));
			handlers.push(on("load", () => {
				wrappedEmit({
					type: EventType.Load,
					data: {}
				});
				if (recordAfter === "load") init();
			}, window));
		}
		return () => {
			handlers.forEach((h) => h());
			processedNodeManager.destroy();
			_takeFullSnapshot = void 0;
			unregisterErrorHandler();
		};
	} catch (error) {
		console.warn(error);
	}
}
function takeFullSnapshot(isCheckout) {
	if (!_takeFullSnapshot) throw new Error("please take full snapshot after start recording");
	_takeFullSnapshot(isCheckout);
}
record.mirror = mirror;
record.takeFullSnapshot = takeFullSnapshot;
function _getCanvasManager(getCanvasManagerFn, options) {
	try {
		return getCanvasManagerFn ? getCanvasManagerFn(options) : new CanvasManagerNoop();
	} catch {
		console.warn("Unable to initialize CanvasManager");
		return new CanvasManagerNoop();
	}
}
var n$2;
(function(t2) {
	t2[t2.NotStarted = 0] = "NotStarted", t2[t2.Running = 1] = "Running", t2[t2.Stopped = 2] = "Stopped";
})(n$2 || (n$2 = {}));
IncrementalSource.Mutation, IncrementalSource.StyleSheetRule, IncrementalSource.StyleDeclaration, IncrementalSource.AdoptedStyleSheet, IncrementalSource.CanvasMutation, IncrementalSource.Selection, IncrementalSource.MediaInteraction;
var DEBUG_BUILD = typeof __SENTRY_DEBUG__ === "undefined" || __SENTRY_DEBUG__;
var CONSOLE_LEVELS = [
	"log",
	"warn",
	"error"
];
var PREFIX = "[Replay] ";
function _addBreadcrumb(message, level = "info") {
	addBreadcrumb({
		category: "console",
		data: { logger: "replay" },
		level,
		message: `${PREFIX}${message}`
	}, { level });
}
function makeReplayDebugLogger() {
	let _capture = false;
	let _trace = false;
	const _debug = {
		exception: () => void 0,
		infoTick: () => void 0,
		setConfig: (opts) => {
			_capture = !!opts.captureExceptions;
			_trace = !!opts.traceInternals;
		}
	};
	if (DEBUG_BUILD) {
		CONSOLE_LEVELS.forEach((name) => {
			_debug[name] = (...args) => {
				debug$4[name](PREFIX, ...args);
				if (_trace) _addBreadcrumb(args.join(""), severityLevelFromString(name));
			};
		});
		_debug.exception = (error, ...message) => {
			if (message.length && _debug.error) _debug.error(...message);
			debug$4.error(PREFIX, error);
			if (_capture) captureException(error, { mechanism: {
				handled: true,
				type: "auto.function.replay.debug"
			} });
			else if (_trace) _addBreadcrumb(error, "error");
		};
		_debug.infoTick = (...args) => {
			debug$4.log(PREFIX, ...args);
			if (_trace) setTimeout(() => _addBreadcrumb(args[0]), 0);
		};
	} else CONSOLE_LEVELS.forEach((name) => {
		_debug[name] = () => void 0;
	});
	return _debug;
}
makeReplayDebugLogger();
GLOBAL_OBJ.navigator;
//#endregion
//#region node_modules/@sentry/replay-canvas/build/npm/esm/index.js
var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var Mirror = class {
	constructor() {
		__publicField(this, "idNodeMap", /* @__PURE__ */ new Map());
		__publicField(this, "nodeMetaMap", /* @__PURE__ */ new WeakMap());
	}
	getId(n) {
		if (!n) return -1;
		return this.getMeta(n)?.id ?? -1;
	}
	getNode(id) {
		return this.idNodeMap.get(id) || null;
	}
	getIds() {
		return Array.from(this.idNodeMap.keys());
	}
	getMeta(n) {
		return this.nodeMetaMap.get(n) || null;
	}
	removeNodeFromMap(n) {
		const id = this.getId(n);
		this.idNodeMap.delete(id);
		if (n.childNodes) n.childNodes.forEach((childNode) => this.removeNodeFromMap(childNode));
	}
	has(id) {
		return this.idNodeMap.has(id);
	}
	hasNode(node) {
		return this.nodeMetaMap.has(node);
	}
	add(n, meta) {
		const id = meta.id;
		this.idNodeMap.set(id, n);
		this.nodeMetaMap.set(n, meta);
	}
	replace(id, n) {
		const oldNode = this.getNode(id);
		if (oldNode) {
			const meta = this.nodeMetaMap.get(oldNode);
			if (meta) this.nodeMetaMap.set(n, meta);
		}
		this.idNodeMap.set(id, n);
	}
	reset() {
		this.idNodeMap = /* @__PURE__ */ new Map();
		this.nodeMetaMap = /* @__PURE__ */ new WeakMap();
	}
};
function createMirror() {
	return new Mirror();
}
var DEPARTED_MIRROR_ACCESS_WARNING = "Please stop import mirror directly. Instead of that,\r\nnow you can use replayer.getMirror() to access the mirror instance of a replayer,\r\nor you can use record.mirror to access the mirror instance during recording.";
var _mirror = {
	map: {},
	getId() {
		console.error(DEPARTED_MIRROR_ACCESS_WARNING);
		return -1;
	},
	getNode() {
		console.error(DEPARTED_MIRROR_ACCESS_WARNING);
		return null;
	},
	removeNodeFromMap() {
		console.error(DEPARTED_MIRROR_ACCESS_WARNING);
	},
	has() {
		console.error(DEPARTED_MIRROR_ACCESS_WARNING);
		return false;
	},
	reset() {
		console.error(DEPARTED_MIRROR_ACCESS_WARNING);
	}
};
if (typeof window !== "undefined" && window.Proxy && window.Reflect) _mirror = new Proxy(_mirror, { get(target, prop, receiver) {
	if (prop === "map") console.error(DEPARTED_MIRROR_ACCESS_WARNING);
	return Reflect.get(target, prop, receiver);
} });
if (!/* @__PURE__ */ /[1-9][0-9]{12}/.test(Date.now().toString()));
var CanvasContext = /* @__PURE__ */ ((CanvasContext2) => {
	CanvasContext2[CanvasContext2["2D"] = 0] = "2D";
	CanvasContext2[CanvasContext2["WebGL"] = 1] = "WebGL";
	CanvasContext2[CanvasContext2["WebGL2"] = 2] = "WebGL2";
	return CanvasContext2;
})(CanvasContext || {});
var chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
var lookup = typeof Uint8Array === "undefined" ? [] : /* @__PURE__ */ new Uint8Array(256);
for (var i$1 = 0; i$1 < chars.length; i$1++) lookup[chars.charCodeAt(i$1)] = i$1;
try {
	if (Array.from([1], (x) => x * 2)[0] !== 2) {
		const cleanFrame = document.createElement("iframe");
		document.body.appendChild(cleanFrame);
		Array.from = cleanFrame.contentWindow?.Array.from || Array.from;
		document.body.removeChild(cleanFrame);
	}
} catch (err) {
	console.debug("Unable to override Array.from", err);
}
createMirror();
var n$1;
(function(t2) {
	t2[t2.NotStarted = 0] = "NotStarted", t2[t2.Running = 1] = "Running", t2[t2.Stopped = 2] = "Stopped";
})(n$1 || (n$1 = {}));
//#endregion
//#region node_modules/@sentry/react/build/esm/isSyntheticEvent.js
function isSyntheticEvent(wat) {
	return isPlainObject(wat) && "nativeEvent" in wat && "preventDefault" in wat && "stopPropagation" in wat;
}
//#endregion
//#region node_modules/@sentry/react/build/esm/sdk.js
function init(options) {
	const opts = { ...options };
	applySdkMetadata(opts, "react");
	setContext("react", { version: import_react.version });
	const client = init$1(opts);
	setNormalizeStringifier(normalizeStringifyValue);
	return client;
}
function normalizeStringifyValue(value) {
	if (isSyntheticEvent(value)) return "[SyntheticEvent]";
	return normalizeStringifyValue$1(value);
}
//#endregion
//#region node_modules/history/index.js
var import_with_selector = require_with_selector();
/**
* Actions represent the type of change to a location value.
*
* @see https://github.com/remix-run/history/tree/main/docs/api-reference.md#action
*/
var Action;
(function(Action) {
	/**
	* A POP indicates a change to an arbitrary index in the history stack, such
	* as a back or forward navigation. It does not describe the direction of the
	* navigation, only that the current index changed.
	*
	* Note: This is the default action for newly created history objects.
	*/
	Action["Pop"] = "POP";
	/**
	* A PUSH indicates a new entry being added to the history stack, such as when
	* a link is clicked and a new page loads. When this happens, all subsequent
	* entries in the stack are lost.
	*/
	Action["Push"] = "PUSH";
	/**
	* A REPLACE indicates the entry at the current index in the history stack
	* being replaced by a new one.
	*/
	Action["Replace"] = "REPLACE";
})(Action || (Action = {}));
var readOnly = function(obj) {
	return Object.freeze(obj);
};
function warning(cond, message) {
	if (!cond) {
		if (typeof console !== "undefined") console.warn(message);
		try {
			throw new Error(message);
		} catch (e) {}
	}
}
var BeforeUnloadEventType = "beforeunload";
var PopStateEventType = "popstate";
/**
* Browser history stores the location in regular URLs. This is the standard for
* most web apps, but it requires some configuration on the server to ensure you
* serve the same app at multiple URLs.
*
* @see https://github.com/remix-run/history/tree/main/docs/api-reference.md#createbrowserhistory
*/
function createBrowserHistory(options) {
	if (options === void 0) options = {};
	var _options$window = options.window, window = _options$window === void 0 ? document.defaultView : _options$window;
	var globalHistory = window.history;
	function getIndexAndLocation() {
		var _window$location = window.location, pathname = _window$location.pathname, search = _window$location.search, hash = _window$location.hash;
		var state = globalHistory.state || {};
		return [state.idx, readOnly({
			pathname,
			search,
			hash,
			state: state.usr || null,
			key: state.key || "default"
		})];
	}
	var blockedPopTx = null;
	function handlePop() {
		if (blockedPopTx) {
			blockers.call(blockedPopTx);
			blockedPopTx = null;
		} else {
			var nextAction = Action.Pop;
			var _getIndexAndLocation = getIndexAndLocation(), nextIndex = _getIndexAndLocation[0], nextLocation = _getIndexAndLocation[1];
			if (blockers.length) {
				if (nextIndex != null) {
					var delta = index - nextIndex;
					if (delta) {
						blockedPopTx = {
							action: nextAction,
							location: nextLocation,
							retry: function retry() {
								go(delta * -1);
							}
						};
						go(delta);
					}
				} else warning(false, "You are trying to block a POP navigation to a location that was not created by the history library. The block will fail silently in production, but in general you should do all navigation with the history library (instead of using window.history.pushState directly) to avoid this situation.");
			} else applyTx(nextAction);
		}
	}
	window.addEventListener(PopStateEventType, handlePop);
	var action = Action.Pop;
	var _getIndexAndLocation2 = getIndexAndLocation(), index = _getIndexAndLocation2[0], location = _getIndexAndLocation2[1];
	var listeners = createEvents();
	var blockers = createEvents();
	if (index == null) {
		index = 0;
		globalHistory.replaceState(_extends({}, globalHistory.state, { idx: index }), "");
	}
	function createHref(to) {
		return typeof to === "string" ? to : createPath(to);
	}
	function getNextLocation(to, state) {
		if (state === void 0) state = null;
		return readOnly(_extends({
			pathname: location.pathname,
			hash: "",
			search: ""
		}, typeof to === "string" ? parsePath(to) : to, {
			state,
			key: createKey()
		}));
	}
	function getHistoryStateAndUrl(nextLocation, index) {
		return [{
			usr: nextLocation.state,
			key: nextLocation.key,
			idx: index
		}, createHref(nextLocation)];
	}
	function allowTx(action, location, retry) {
		return !blockers.length || (blockers.call({
			action,
			location,
			retry
		}), false);
	}
	function applyTx(nextAction) {
		action = nextAction;
		var _getIndexAndLocation3 = getIndexAndLocation();
		index = _getIndexAndLocation3[0];
		location = _getIndexAndLocation3[1];
		listeners.call({
			action,
			location
		});
	}
	function push(to, state) {
		var nextAction = Action.Push;
		var nextLocation = getNextLocation(to, state);
		function retry() {
			push(to, state);
		}
		if (allowTx(nextAction, nextLocation, retry)) {
			var _getHistoryStateAndUr = getHistoryStateAndUrl(nextLocation, index + 1), historyState = _getHistoryStateAndUr[0], url = _getHistoryStateAndUr[1];
			try {
				globalHistory.pushState(historyState, "", url);
			} catch (error) {
				window.location.assign(url);
			}
			applyTx(nextAction);
		}
	}
	function replace(to, state) {
		var nextAction = Action.Replace;
		var nextLocation = getNextLocation(to, state);
		function retry() {
			replace(to, state);
		}
		if (allowTx(nextAction, nextLocation, retry)) {
			var _getHistoryStateAndUr2 = getHistoryStateAndUrl(nextLocation, index), historyState = _getHistoryStateAndUr2[0], url = _getHistoryStateAndUr2[1];
			globalHistory.replaceState(historyState, "", url);
			applyTx(nextAction);
		}
	}
	function go(delta) {
		globalHistory.go(delta);
	}
	return {
		get action() {
			return action;
		},
		get location() {
			return location;
		},
		createHref,
		push,
		replace,
		go,
		back: function back() {
			go(-1);
		},
		forward: function forward() {
			go(1);
		},
		listen: function listen(listener) {
			return listeners.push(listener);
		},
		block: function block(blocker) {
			var unblock = blockers.push(blocker);
			if (blockers.length === 1) window.addEventListener(BeforeUnloadEventType, promptBeforeUnload);
			return function() {
				unblock();
				if (!blockers.length) window.removeEventListener(BeforeUnloadEventType, promptBeforeUnload);
			};
		}
	};
}
/**
* Memory history stores the current location in memory. It is designed for use
* in stateful non-browser environments like tests and React Native.
*
* @see https://github.com/remix-run/history/tree/main/docs/api-reference.md#creatememoryhistory
*/
function createMemoryHistory(options) {
	if (options === void 0) options = {};
	var _options3 = options, _options3$initialEntr = _options3.initialEntries, initialEntries = _options3$initialEntr === void 0 ? ["/"] : _options3$initialEntr, initialIndex = _options3.initialIndex;
	var entries = initialEntries.map(function(entry) {
		var location = readOnly(_extends({
			pathname: "/",
			search: "",
			hash: "",
			state: null,
			key: createKey()
		}, typeof entry === "string" ? parsePath(entry) : entry));
		warning(location.pathname.charAt(0) === "/", "Relative pathnames are not supported in createMemoryHistory({ initialEntries }) (invalid entry: " + JSON.stringify(entry) + ")");
		return location;
	});
	var index = clamp(initialIndex == null ? entries.length - 1 : initialIndex, 0, entries.length - 1);
	var action = Action.Pop;
	var location = entries[index];
	var listeners = createEvents();
	var blockers = createEvents();
	function createHref(to) {
		return typeof to === "string" ? to : createPath(to);
	}
	function getNextLocation(to, state) {
		if (state === void 0) state = null;
		return readOnly(_extends({
			pathname: location.pathname,
			search: "",
			hash: ""
		}, typeof to === "string" ? parsePath(to) : to, {
			state,
			key: createKey()
		}));
	}
	function allowTx(action, location, retry) {
		return !blockers.length || (blockers.call({
			action,
			location,
			retry
		}), false);
	}
	function applyTx(nextAction, nextLocation) {
		action = nextAction;
		location = nextLocation;
		listeners.call({
			action,
			location
		});
	}
	function push(to, state) {
		var nextAction = Action.Push;
		var nextLocation = getNextLocation(to, state);
		function retry() {
			push(to, state);
		}
		warning(location.pathname.charAt(0) === "/", "Relative pathnames are not supported in memory history.push(" + JSON.stringify(to) + ")");
		if (allowTx(nextAction, nextLocation, retry)) {
			index += 1;
			entries.splice(index, entries.length, nextLocation);
			applyTx(nextAction, nextLocation);
		}
	}
	function replace(to, state) {
		var nextAction = Action.Replace;
		var nextLocation = getNextLocation(to, state);
		function retry() {
			replace(to, state);
		}
		warning(location.pathname.charAt(0) === "/", "Relative pathnames are not supported in memory history.replace(" + JSON.stringify(to) + ")");
		if (allowTx(nextAction, nextLocation, retry)) {
			entries[index] = nextLocation;
			applyTx(nextAction, nextLocation);
		}
	}
	function go(delta) {
		var nextIndex = clamp(index + delta, 0, entries.length - 1);
		var nextAction = Action.Pop;
		var nextLocation = entries[nextIndex];
		function retry() {
			go(delta);
		}
		if (allowTx(nextAction, nextLocation, retry)) {
			index = nextIndex;
			applyTx(nextAction, nextLocation);
		}
	}
	return {
		get index() {
			return index;
		},
		get action() {
			return action;
		},
		get location() {
			return location;
		},
		createHref,
		push,
		replace,
		go,
		back: function back() {
			go(-1);
		},
		forward: function forward() {
			go(1);
		},
		listen: function listen(listener) {
			return listeners.push(listener);
		},
		block: function block(blocker) {
			return blockers.push(blocker);
		}
	};
}
function clamp(n, lowerBound, upperBound) {
	return Math.min(Math.max(n, lowerBound), upperBound);
}
function promptBeforeUnload(event) {
	event.preventDefault();
	event.returnValue = "";
}
function createEvents() {
	var handlers = [];
	return {
		get length() {
			return handlers.length;
		},
		push: function push(fn) {
			handlers.push(fn);
			return function() {
				handlers = handlers.filter(function(handler) {
					return handler !== fn;
				});
			};
		},
		call: function call(arg) {
			handlers.forEach(function(fn) {
				return fn && fn(arg);
			});
		}
	};
}
function createKey() {
	return Math.random().toString(36).substr(2, 8);
}
/**
* Creates a string URL path from the given pathname, search, and hash components.
*
* @see https://github.com/remix-run/history/tree/main/docs/api-reference.md#createpath
*/
function createPath(_ref) {
	var _ref$pathname = _ref.pathname, pathname = _ref$pathname === void 0 ? "/" : _ref$pathname, _ref$search = _ref.search, search = _ref$search === void 0 ? "" : _ref$search, _ref$hash = _ref.hash, hash = _ref$hash === void 0 ? "" : _ref$hash;
	if (search && search !== "?") pathname += search.charAt(0) === "?" ? search : "?" + search;
	if (hash && hash !== "#") pathname += hash.charAt(0) === "#" ? hash : "#" + hash;
	return pathname;
}
/**
* Parses a string URL path into its separate pathname, search, and hash components.
*
* @see https://github.com/remix-run/history/tree/main/docs/api-reference.md#parsepath
*/
function parsePath(path) {
	var parsedPath = {};
	if (path) {
		var hashIndex = path.indexOf("#");
		if (hashIndex >= 0) {
			parsedPath.hash = path.substr(hashIndex);
			path = path.substr(0, hashIndex);
		}
		var searchIndex = path.indexOf("?");
		if (searchIndex >= 0) {
			parsedPath.search = path.substr(searchIndex);
			path = path.substr(0, searchIndex);
		}
		if (path) parsedPath.pathname = path;
	}
	return parsedPath;
}
//#endregion
//#region node_modules/semver/internal/constants.js
var require_constants = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = {
		MAX_LENGTH: 256,
		MAX_SAFE_COMPONENT_LENGTH: 16,
		MAX_SAFE_BUILD_LENGTH: 250,
		MAX_SAFE_INTEGER: Number.MAX_SAFE_INTEGER || 
		/* istanbul ignore next */ 9007199254740991,
		RELEASE_TYPES: [
			"major",
			"premajor",
			"minor",
			"preminor",
			"patch",
			"prepatch",
			"prerelease"
		],
		SEMVER_SPEC_VERSION: "2.0.0",
		FLAG_INCLUDE_PRERELEASE: 1,
		FLAG_LOOSE: 2
	};
}));
//#endregion
//#region node_modules/semver/internal/debug.js
var require_debug = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = typeof process === "object" && process.env && process.env.NODE_DEBUG && /\bsemver\b/i.test(process.env.NODE_DEBUG) ? (...args) => console.error("SEMVER", ...args) : () => {};
}));
//#endregion
//#region node_modules/semver/internal/re.js
var require_re = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { MAX_SAFE_COMPONENT_LENGTH, MAX_SAFE_BUILD_LENGTH, MAX_LENGTH } = require_constants();
	var debug = require_debug();
	exports = module.exports = {};
	var re = exports.re = [];
	var safeRe = exports.safeRe = [];
	var src = exports.src = [];
	var safeSrc = exports.safeSrc = [];
	var t = exports.t = {};
	var R = 0;
	var LETTERDASHNUMBER = "[a-zA-Z0-9-]";
	var safeRegexReplacements = [
		["\\s", 1],
		["\\d", MAX_LENGTH],
		[LETTERDASHNUMBER, MAX_SAFE_BUILD_LENGTH]
	];
	var makeSafeRegex = (value) => {
		for (const [token, max] of safeRegexReplacements) value = value.split(`${token}*`).join(`${token}{0,${max}}`).split(`${token}+`).join(`${token}{1,${max}}`);
		return value;
	};
	var createToken = (name, value, isGlobal) => {
		const safe = makeSafeRegex(value);
		const index = R++;
		debug(name, index, value);
		t[name] = index;
		src[index] = value;
		safeSrc[index] = safe;
		re[index] = new RegExp(value, isGlobal ? "g" : void 0);
		safeRe[index] = new RegExp(safe, isGlobal ? "g" : void 0);
	};
	createToken("NUMERICIDENTIFIER", "0|[1-9]\\d*");
	createToken("NUMERICIDENTIFIERLOOSE", "\\d+");
	createToken("NONNUMERICIDENTIFIER", `\\d*[a-zA-Z-]${LETTERDASHNUMBER}*`);
	createToken("MAINVERSION", `(${src[t.NUMERICIDENTIFIER]})\\.(${src[t.NUMERICIDENTIFIER]})\\.(${src[t.NUMERICIDENTIFIER]})`);
	createToken("MAINVERSIONLOOSE", `(${src[t.NUMERICIDENTIFIERLOOSE]})\\.(${src[t.NUMERICIDENTIFIERLOOSE]})\\.(${src[t.NUMERICIDENTIFIERLOOSE]})`);
	createToken("PRERELEASEIDENTIFIER", `(?:${src[t.NONNUMERICIDENTIFIER]}|${src[t.NUMERICIDENTIFIER]})`);
	createToken("PRERELEASEIDENTIFIERLOOSE", `(?:${src[t.NONNUMERICIDENTIFIER]}|${src[t.NUMERICIDENTIFIERLOOSE]})`);
	createToken("PRERELEASE", `(?:-(${src[t.PRERELEASEIDENTIFIER]}(?:\\.${src[t.PRERELEASEIDENTIFIER]})*))`);
	createToken("PRERELEASELOOSE", `(?:-?(${src[t.PRERELEASEIDENTIFIERLOOSE]}(?:\\.${src[t.PRERELEASEIDENTIFIERLOOSE]})*))`);
	createToken("BUILDIDENTIFIER", `${LETTERDASHNUMBER}+`);
	createToken("BUILD", `(?:\\+(${src[t.BUILDIDENTIFIER]}(?:\\.${src[t.BUILDIDENTIFIER]})*))`);
	createToken("FULLPLAIN", `v?${src[t.MAINVERSION]}${src[t.PRERELEASE]}?${src[t.BUILD]}?`);
	createToken("FULL", `^${src[t.FULLPLAIN]}$`);
	createToken("LOOSEPLAIN", `[v=\\s]*${src[t.MAINVERSIONLOOSE]}${src[t.PRERELEASELOOSE]}?${src[t.BUILD]}?`);
	createToken("LOOSE", `^${src[t.LOOSEPLAIN]}$`);
	createToken("GTLT", "((?:<|>)?=?)");
	createToken("XRANGEIDENTIFIERLOOSE", `${src[t.NUMERICIDENTIFIERLOOSE]}|x|X|\\*`);
	createToken("XRANGEIDENTIFIER", `${src[t.NUMERICIDENTIFIER]}|x|X|\\*`);
	createToken("XRANGEPLAIN", `[v=\\s]*(${src[t.XRANGEIDENTIFIER]})(?:\\.(${src[t.XRANGEIDENTIFIER]})(?:\\.(${src[t.XRANGEIDENTIFIER]})(?:${src[t.PRERELEASE]})?${src[t.BUILD]}?)?)?`);
	createToken("XRANGEPLAINLOOSE", `[v=\\s]*(${src[t.XRANGEIDENTIFIERLOOSE]})(?:\\.(${src[t.XRANGEIDENTIFIERLOOSE]})(?:\\.(${src[t.XRANGEIDENTIFIERLOOSE]})(?:${src[t.PRERELEASELOOSE]})?${src[t.BUILD]}?)?)?`);
	createToken("XRANGE", `^${src[t.GTLT]}\\s*${src[t.XRANGEPLAIN]}$`);
	createToken("XRANGELOOSE", `^${src[t.GTLT]}\\s*${src[t.XRANGEPLAINLOOSE]}$`);
	createToken("COERCEPLAIN", `(^|[^\\d])(\\d{1,${MAX_SAFE_COMPONENT_LENGTH}})(?:\\.(\\d{1,${MAX_SAFE_COMPONENT_LENGTH}}))?(?:\\.(\\d{1,${MAX_SAFE_COMPONENT_LENGTH}}))?`);
	createToken("COERCE", `${src[t.COERCEPLAIN]}(?:$|[^\\d])`);
	createToken("COERCEFULL", src[t.COERCEPLAIN] + `(?:${src[t.PRERELEASE]})?(?:${src[t.BUILD]})?(?:$|[^\\d])`);
	createToken("COERCERTL", src[t.COERCE], true);
	createToken("COERCERTLFULL", src[t.COERCEFULL], true);
	createToken("LONETILDE", "(?:~>?)");
	createToken("TILDETRIM", `(\\s*)${src[t.LONETILDE]}\\s+`, true);
	exports.tildeTrimReplace = "$1~";
	createToken("TILDE", `^${src[t.LONETILDE]}${src[t.XRANGEPLAIN]}$`);
	createToken("TILDELOOSE", `^${src[t.LONETILDE]}${src[t.XRANGEPLAINLOOSE]}$`);
	createToken("LONECARET", "(?:\\^)");
	createToken("CARETTRIM", `(\\s*)${src[t.LONECARET]}\\s+`, true);
	exports.caretTrimReplace = "$1^";
	createToken("CARET", `^${src[t.LONECARET]}${src[t.XRANGEPLAIN]}$`);
	createToken("CARETLOOSE", `^${src[t.LONECARET]}${src[t.XRANGEPLAINLOOSE]}$`);
	createToken("COMPARATORLOOSE", `^${src[t.GTLT]}\\s*(${src[t.LOOSEPLAIN]})$|^$`);
	createToken("COMPARATOR", `^${src[t.GTLT]}\\s*(${src[t.FULLPLAIN]})$|^$`);
	createToken("COMPARATORTRIM", `(\\s*)${src[t.GTLT]}\\s*(${src[t.LOOSEPLAIN]}|${src[t.XRANGEPLAIN]})`, true);
	exports.comparatorTrimReplace = "$1$2$3";
	createToken("HYPHENRANGE", `^\\s*(${src[t.XRANGEPLAIN]})\\s+-\\s+(${src[t.XRANGEPLAIN]})\\s*$`);
	createToken("HYPHENRANGELOOSE", `^\\s*(${src[t.XRANGEPLAINLOOSE]})\\s+-\\s+(${src[t.XRANGEPLAINLOOSE]})\\s*$`);
	createToken("STAR", "(<|>)?=?\\s*\\*");
	createToken("GTE0", "^\\s*>=\\s*0\\.0\\.0\\s*$");
	createToken("GTE0PRE", "^\\s*>=\\s*0\\.0\\.0-0\\s*$");
}));
//#endregion
//#region node_modules/semver/internal/parse-options.js
var require_parse_options = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var looseOption = Object.freeze({ loose: true });
	var emptyOpts = Object.freeze({});
	var parseOptions = (options) => {
		if (!options) return emptyOpts;
		if (typeof options !== "object") return looseOption;
		return options;
	};
	module.exports = parseOptions;
}));
//#endregion
//#region node_modules/semver/internal/identifiers.js
var require_identifiers = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var numeric = /^[0-9]+$/;
	var compareIdentifiers = (a, b) => {
		if (typeof a === "number" && typeof b === "number") return a === b ? 0 : a < b ? -1 : 1;
		const anum = numeric.test(a);
		const bnum = numeric.test(b);
		if (anum && bnum) {
			a = +a;
			b = +b;
		}
		return a === b ? 0 : anum && !bnum ? -1 : bnum && !anum ? 1 : a < b ? -1 : 1;
	};
	var rcompareIdentifiers = (a, b) => compareIdentifiers(b, a);
	module.exports = {
		compareIdentifiers,
		rcompareIdentifiers
	};
}));
//#endregion
//#region node_modules/semver/classes/semver.js
var require_semver$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var debug = require_debug();
	var { MAX_LENGTH, MAX_SAFE_INTEGER } = require_constants();
	var { safeRe: re, t } = require_re();
	var parseOptions = require_parse_options();
	var { compareIdentifiers } = require_identifiers();
	var isPrereleaseIdentifier = (prerelease, identifier) => {
		const identifiers = identifier.split(".");
		if (identifiers.length > prerelease.length) return false;
		for (let i = 0; i < identifiers.length; i++) if (compareIdentifiers(prerelease[i], identifiers[i]) !== 0) return false;
		return true;
	};
	module.exports = class SemVer {
		constructor(version, options) {
			options = parseOptions(options);
			if (version instanceof SemVer) {
				if (version.loose === !!options.loose && version.includePrerelease === !!options.includePrerelease) return version;
				else version = version.version;
			} else if (typeof version !== "string") throw new TypeError(`Invalid version. Must be a string. Got type "${typeof version}".`);
			if (version.length > MAX_LENGTH) throw new TypeError(`version is longer than ${MAX_LENGTH} characters`);
			debug("SemVer", version, options);
			this.options = options;
			this.loose = !!options.loose;
			this.includePrerelease = !!options.includePrerelease;
			const m = version.trim().match(options.loose ? re[t.LOOSE] : re[t.FULL]);
			if (!m) throw new TypeError(`Invalid Version: ${version}`);
			this.raw = version;
			this.major = +m[1];
			this.minor = +m[2];
			this.patch = +m[3];
			if (this.major > MAX_SAFE_INTEGER || this.major < 0) throw new TypeError("Invalid major version");
			if (this.minor > MAX_SAFE_INTEGER || this.minor < 0) throw new TypeError("Invalid minor version");
			if (this.patch > MAX_SAFE_INTEGER || this.patch < 0) throw new TypeError("Invalid patch version");
			if (!m[4]) this.prerelease = [];
			else this.prerelease = m[4].split(".").map((id) => {
				if (/^[0-9]+$/.test(id)) {
					const num = +id;
					if (num >= 0 && num < MAX_SAFE_INTEGER) return num;
				}
				return id;
			});
			this.build = m[5] ? m[5].split(".") : [];
			this.format();
		}
		format() {
			this.version = `${this.major}.${this.minor}.${this.patch}`;
			if (this.prerelease.length) this.version += `-${this.prerelease.join(".")}`;
			return this.version;
		}
		toString() {
			return this.version;
		}
		compare(other) {
			debug("SemVer.compare", this.version, this.options, other);
			if (!(other instanceof SemVer)) {
				if (typeof other === "string" && other === this.version) return 0;
				other = new SemVer(other, this.options);
			}
			if (other.version === this.version) return 0;
			return this.compareMain(other) || this.comparePre(other);
		}
		compareMain(other) {
			if (!(other instanceof SemVer)) other = new SemVer(other, this.options);
			if (this.major < other.major) return -1;
			if (this.major > other.major) return 1;
			if (this.minor < other.minor) return -1;
			if (this.minor > other.minor) return 1;
			if (this.patch < other.patch) return -1;
			if (this.patch > other.patch) return 1;
			return 0;
		}
		comparePre(other) {
			if (!(other instanceof SemVer)) other = new SemVer(other, this.options);
			if (this.prerelease.length && !other.prerelease.length) return -1;
			else if (!this.prerelease.length && other.prerelease.length) return 1;
			else if (!this.prerelease.length && !other.prerelease.length) return 0;
			let i = 0;
			do {
				const a = this.prerelease[i];
				const b = other.prerelease[i];
				debug("prerelease compare", i, a, b);
				if (a === void 0 && b === void 0) return 0;
				else if (b === void 0) return 1;
				else if (a === void 0) return -1;
				else if (a === b) continue;
				else return compareIdentifiers(a, b);
			} while (++i);
		}
		compareBuild(other) {
			if (!(other instanceof SemVer)) other = new SemVer(other, this.options);
			let i = 0;
			do {
				const a = this.build[i];
				const b = other.build[i];
				debug("build compare", i, a, b);
				if (a === void 0 && b === void 0) return 0;
				else if (b === void 0) return 1;
				else if (a === void 0) return -1;
				else if (a === b) continue;
				else return compareIdentifiers(a, b);
			} while (++i);
		}
		inc(release, identifier, identifierBase) {
			if (release.startsWith("pre")) {
				if (!identifier && identifierBase === false) throw new Error("invalid increment argument: identifier is empty");
				if (identifier) {
					const match = `-${identifier}`.match(this.options.loose ? re[t.PRERELEASELOOSE] : re[t.PRERELEASE]);
					if (!match || match[1] !== identifier) throw new Error(`invalid identifier: ${identifier}`);
				}
			}
			switch (release) {
				case "premajor":
					this.prerelease.length = 0;
					this.patch = 0;
					this.minor = 0;
					this.major++;
					this.inc("pre", identifier, identifierBase);
					break;
				case "preminor":
					this.prerelease.length = 0;
					this.patch = 0;
					this.minor++;
					this.inc("pre", identifier, identifierBase);
					break;
				case "prepatch":
					this.prerelease.length = 0;
					this.inc("patch", identifier, identifierBase);
					this.inc("pre", identifier, identifierBase);
					break;
				case "prerelease":
					if (this.prerelease.length === 0) this.inc("patch", identifier, identifierBase);
					this.inc("pre", identifier, identifierBase);
					break;
				case "release":
					if (this.prerelease.length === 0) throw new Error(`version ${this.raw} is not a prerelease`);
					this.prerelease.length = 0;
					break;
				case "major":
					if (this.minor !== 0 || this.patch !== 0 || this.prerelease.length === 0) this.major++;
					this.minor = 0;
					this.patch = 0;
					this.prerelease = [];
					break;
				case "minor":
					if (this.patch !== 0 || this.prerelease.length === 0) this.minor++;
					this.patch = 0;
					this.prerelease = [];
					break;
				case "patch":
					if (this.prerelease.length === 0) this.patch++;
					this.prerelease = [];
					break;
				case "pre": {
					const base = Number(identifierBase) ? 1 : 0;
					if (this.prerelease.length === 0) this.prerelease = [base];
					else {
						let i = this.prerelease.length;
						while (--i >= 0) if (typeof this.prerelease[i] === "number") {
							this.prerelease[i]++;
							i = -2;
						}
						if (i === -1) {
							if (identifier === this.prerelease.join(".") && identifierBase === false) throw new Error("invalid increment argument: identifier already exists");
							this.prerelease.push(base);
						}
					}
					if (identifier) {
						let prerelease = [identifier, base];
						if (identifierBase === false) prerelease = [identifier];
						if (isPrereleaseIdentifier(this.prerelease, identifier)) {
							const prereleaseBase = this.prerelease[identifier.split(".").length];
							if (isNaN(prereleaseBase)) this.prerelease = prerelease;
						} else this.prerelease = prerelease;
					}
					break;
				}
				default: throw new Error(`invalid increment argument: ${release}`);
			}
			this.raw = this.format();
			if (this.build.length) this.raw += `+${this.build.join(".")}`;
			return this;
		}
	};
}));
//#endregion
//#region node_modules/semver/functions/parse.js
var require_parse = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SemVer = require_semver$1();
	var parse = (version, options, throwErrors = false) => {
		if (version instanceof SemVer) return version;
		try {
			return new SemVer(version, options);
		} catch (er) {
			if (!throwErrors) return null;
			throw er;
		}
	};
	module.exports = parse;
}));
//#endregion
//#region node_modules/semver/functions/valid.js
var require_valid$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parse = require_parse();
	var valid = (version, options) => {
		const v = parse(version, options);
		return v ? v.version : null;
	};
	module.exports = valid;
}));
//#endregion
//#region node_modules/semver/functions/clean.js
var require_clean = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parse = require_parse();
	var clean = (version, options) => {
		const s = parse(version.trim().replace(/^[=v]+/, ""), options);
		return s ? s.version : null;
	};
	module.exports = clean;
}));
//#endregion
//#region node_modules/semver/functions/inc.js
var require_inc = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SemVer = require_semver$1();
	var inc = (version, release, options, identifier, identifierBase) => {
		if (typeof options === "string") {
			identifierBase = identifier;
			identifier = options;
			options = void 0;
		}
		try {
			return new SemVer(version instanceof SemVer ? version.version : version, options).inc(release, identifier, identifierBase).version;
		} catch (er) {
			return null;
		}
	};
	module.exports = inc;
}));
//#endregion
//#region node_modules/semver/functions/diff.js
var require_diff = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parse = require_parse();
	var diff = (version1, version2) => {
		const v1 = parse(version1, null, true);
		const v2 = parse(version2, null, true);
		const comparison = v1.compare(v2);
		if (comparison === 0) return null;
		const v1Higher = comparison > 0;
		const highVersion = v1Higher ? v1 : v2;
		const lowVersion = v1Higher ? v2 : v1;
		const highHasPre = !!highVersion.prerelease.length;
		if (!!lowVersion.prerelease.length && !highHasPre) {
			if (!lowVersion.patch && !lowVersion.minor) return "major";
			if (lowVersion.compareMain(highVersion) === 0) {
				if (lowVersion.minor && !lowVersion.patch) return "minor";
				return "patch";
			}
		}
		const prefix = highHasPre ? "pre" : "";
		if (v1.major !== v2.major) return prefix + "major";
		if (v1.minor !== v2.minor) return prefix + "minor";
		if (v1.patch !== v2.patch) return prefix + "patch";
		return "prerelease";
	};
	module.exports = diff;
}));
//#endregion
//#region node_modules/semver/functions/major.js
var require_major = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SemVer = require_semver$1();
	var major = (a, loose) => new SemVer(a, loose).major;
	module.exports = major;
}));
//#endregion
//#region node_modules/semver/functions/minor.js
var require_minor = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SemVer = require_semver$1();
	var minor = (a, loose) => new SemVer(a, loose).minor;
	module.exports = minor;
}));
//#endregion
//#region node_modules/semver/functions/patch.js
var require_patch = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SemVer = require_semver$1();
	var patch = (a, loose) => new SemVer(a, loose).patch;
	module.exports = patch;
}));
//#endregion
//#region node_modules/semver/functions/prerelease.js
var require_prerelease = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parse = require_parse();
	var prerelease = (version, options) => {
		const parsed = parse(version, options);
		return parsed && parsed.prerelease.length ? parsed.prerelease : null;
	};
	module.exports = prerelease;
}));
//#endregion
//#region node_modules/semver/functions/compare.js
var require_compare = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SemVer = require_semver$1();
	var compare = (a, b, loose) => new SemVer(a, loose).compare(new SemVer(b, loose));
	module.exports = compare;
}));
//#endregion
//#region node_modules/semver/functions/rcompare.js
var require_rcompare = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var compare = require_compare();
	var rcompare = (a, b, loose) => compare(b, a, loose);
	module.exports = rcompare;
}));
//#endregion
//#region node_modules/semver/functions/compare-loose.js
var require_compare_loose = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var compare = require_compare();
	var compareLoose = (a, b) => compare(a, b, true);
	module.exports = compareLoose;
}));
//#endregion
//#region node_modules/semver/functions/compare-build.js
var require_compare_build = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SemVer = require_semver$1();
	var compareBuild = (a, b, loose) => {
		const versionA = new SemVer(a, loose);
		const versionB = new SemVer(b, loose);
		return versionA.compare(versionB) || versionA.compareBuild(versionB);
	};
	module.exports = compareBuild;
}));
//#endregion
//#region node_modules/semver/functions/sort.js
var require_sort = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var compareBuild = require_compare_build();
	var sort = (list, loose) => list.sort((a, b) => compareBuild(a, b, loose));
	module.exports = sort;
}));
//#endregion
//#region node_modules/semver/functions/rsort.js
var require_rsort = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var compareBuild = require_compare_build();
	var rsort = (list, loose) => list.sort((a, b) => compareBuild(b, a, loose));
	module.exports = rsort;
}));
//#endregion
//#region node_modules/semver/functions/gt.js
var require_gt = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var compare = require_compare();
	var gt = (a, b, loose) => compare(a, b, loose) > 0;
	module.exports = gt;
}));
//#endregion
//#region node_modules/semver/functions/lt.js
var require_lt = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var compare = require_compare();
	var lt = (a, b, loose) => compare(a, b, loose) < 0;
	module.exports = lt;
}));
//#endregion
//#region node_modules/semver/functions/eq.js
var require_eq = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var compare = require_compare();
	var eq = (a, b, loose) => compare(a, b, loose) === 0;
	module.exports = eq;
}));
//#endregion
//#region node_modules/semver/functions/neq.js
var require_neq = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var compare = require_compare();
	var neq = (a, b, loose) => compare(a, b, loose) !== 0;
	module.exports = neq;
}));
//#endregion
//#region node_modules/semver/functions/gte.js
var require_gte = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var compare = require_compare();
	var gte = (a, b, loose) => compare(a, b, loose) >= 0;
	module.exports = gte;
}));
//#endregion
//#region node_modules/semver/functions/lte.js
var require_lte = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var compare = require_compare();
	var lte = (a, b, loose) => compare(a, b, loose) <= 0;
	module.exports = lte;
}));
//#endregion
//#region node_modules/semver/functions/cmp.js
var require_cmp = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var eq = require_eq();
	var neq = require_neq();
	var gt = require_gt();
	var gte = require_gte();
	var lt = require_lt();
	var lte = require_lte();
	var cmp = (a, op, b, loose) => {
		switch (op) {
			case "===":
				if (typeof a === "object") a = a.version;
				if (typeof b === "object") b = b.version;
				return a === b;
			case "!==":
				if (typeof a === "object") a = a.version;
				if (typeof b === "object") b = b.version;
				return a !== b;
			case "":
			case "=":
			case "==": return eq(a, b, loose);
			case "!=": return neq(a, b, loose);
			case ">": return gt(a, b, loose);
			case ">=": return gte(a, b, loose);
			case "<": return lt(a, b, loose);
			case "<=": return lte(a, b, loose);
			default: throw new TypeError(`Invalid operator: ${op}`);
		}
	};
	module.exports = cmp;
}));
//#endregion
//#region node_modules/semver/functions/coerce.js
var require_coerce = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SemVer = require_semver$1();
	var parse = require_parse();
	var { safeRe: re, t } = require_re();
	var coerce = (version, options) => {
		if (version instanceof SemVer) return version;
		if (typeof version === "number") version = String(version);
		if (typeof version !== "string") return null;
		options = options || {};
		let match = null;
		if (!options.rtl) match = version.match(options.includePrerelease ? re[t.COERCEFULL] : re[t.COERCE]);
		else {
			const coerceRtlRegex = options.includePrerelease ? re[t.COERCERTLFULL] : re[t.COERCERTL];
			let next;
			while ((next = coerceRtlRegex.exec(version)) && (!match || match.index + match[0].length !== version.length)) {
				if (!match || next.index + next[0].length !== match.index + match[0].length) match = next;
				coerceRtlRegex.lastIndex = next.index + next[1].length + next[2].length;
			}
			coerceRtlRegex.lastIndex = -1;
		}
		if (match === null) return null;
		const major = match[2];
		return parse(`${major}.${match[3] || "0"}.${match[4] || "0"}${options.includePrerelease && match[5] ? `-${match[5]}` : ""}${options.includePrerelease && match[6] ? `+${match[6]}` : ""}`, options);
	};
	module.exports = coerce;
}));
//#endregion
//#region node_modules/semver/functions/truncate.js
var require_truncate = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var parse = require_parse();
	var constants = require_constants();
	var SemVer = require_semver$1();
	var truncate = (version, truncation, options) => {
		if (!constants.RELEASE_TYPES.includes(truncation)) return null;
		const clonedVersion = cloneInputVersion(version, options);
		return clonedVersion && doTruncation(clonedVersion, truncation);
	};
	var cloneInputVersion = (version, options) => {
		return parse(version instanceof SemVer ? version.version : version, options);
	};
	var doTruncation = (version, truncation) => {
		if (isPrerelease(truncation)) return version.version;
		version.prerelease = [];
		switch (truncation) {
			case "major":
				version.minor = 0;
				version.patch = 0;
				break;
			case "minor": version.patch = 0;
		}
		return version.format();
	};
	var isPrerelease = (type) => {
		return type.startsWith("pre");
	};
	module.exports = truncate;
}));
//#endregion
//#region node_modules/semver/internal/lrucache.js
var require_lrucache = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var LRUCache = class {
		constructor() {
			this.max = 1e3;
			this.map = /* @__PURE__ */ new Map();
		}
		get(key) {
			const value = this.map.get(key);
			if (value === void 0) return;
			else {
				this.map.delete(key);
				this.map.set(key, value);
				return value;
			}
		}
		delete(key) {
			return this.map.delete(key);
		}
		set(key, value) {
			if (!this.delete(key) && value !== void 0) {
				if (this.map.size >= this.max) {
					const firstKey = this.map.keys().next().value;
					this.delete(firstKey);
				}
				this.map.set(key, value);
			}
			return this;
		}
	};
	module.exports = LRUCache;
}));
//#endregion
//#region node_modules/semver/classes/range.js
var require_range = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SPACE_CHARACTERS = /\s+/g;
	module.exports = class Range {
		constructor(range, options) {
			options = parseOptions(options);
			if (range instanceof Range) {
				if (range.loose === !!options.loose && range.includePrerelease === !!options.includePrerelease) return range;
				else return new Range(range.raw, options);
			}
			if (range instanceof Comparator) {
				this.raw = range.value;
				this.set = [[range]];
				this.formatted = void 0;
				return this;
			}
			this.options = options;
			this.loose = !!options.loose;
			this.includePrerelease = !!options.includePrerelease;
			this.raw = range.trim().replace(SPACE_CHARACTERS, " ");
			this.set = this.raw.split("||").map((r) => this.parseRange(r.trim())).filter((c) => c.length);
			if (!this.set.length) throw new TypeError(`Invalid SemVer Range: ${this.raw}`);
			if (this.set.length > 1) {
				const first = this.set[0];
				this.set = this.set.filter((c) => !isNullSet(c[0]));
				if (this.set.length === 0) this.set = [first];
				else if (this.set.length > 1) {
					for (const c of this.set) if (c.length === 1 && isAny(c[0])) {
						this.set = [c];
						break;
					}
				}
			}
			this.formatted = void 0;
		}
		get range() {
			if (this.formatted === void 0) {
				this.formatted = "";
				for (let i = 0; i < this.set.length; i++) {
					if (i > 0) this.formatted += "||";
					const comps = this.set[i];
					for (let k = 0; k < comps.length; k++) {
						if (k > 0) this.formatted += " ";
						this.formatted += comps[k].toString().trim();
					}
				}
			}
			return this.formatted;
		}
		format() {
			return this.range;
		}
		toString() {
			return this.range;
		}
		parseRange(range) {
			range = range.replace(BUILDSTRIPRE, "");
			const memoKey = ((this.options.includePrerelease && FLAG_INCLUDE_PRERELEASE) | (this.options.loose && FLAG_LOOSE)) + ":" + range;
			const cached = cache.get(memoKey);
			if (cached) return cached;
			const loose = this.options.loose;
			const hr = loose ? re[t.HYPHENRANGELOOSE] : re[t.HYPHENRANGE];
			range = range.replace(hr, hyphenReplace(this.options.includePrerelease));
			debug("hyphen replace", range);
			range = range.replace(re[t.COMPARATORTRIM], comparatorTrimReplace);
			debug("comparator trim", range);
			range = range.replace(re[t.TILDETRIM], tildeTrimReplace);
			debug("tilde trim", range);
			range = range.replace(re[t.CARETTRIM], caretTrimReplace);
			debug("caret trim", range);
			let rangeList = range.split(" ").map((comp) => parseComparator(comp, this.options)).join(" ").split(/\s+/).map((comp) => replaceGTE0(comp, this.options));
			if (loose) rangeList = rangeList.filter((comp) => {
				debug("loose invalid filter", comp, this.options);
				return !!comp.match(re[t.COMPARATORLOOSE]);
			});
			debug("range list", rangeList);
			const rangeMap = /* @__PURE__ */ new Map();
			const comparators = rangeList.map((comp) => new Comparator(comp, this.options));
			for (const comp of comparators) {
				if (isNullSet(comp)) return [comp];
				rangeMap.set(comp.value, comp);
			}
			if (rangeMap.size > 1 && rangeMap.has("")) rangeMap.delete("");
			const result = [...rangeMap.values()];
			cache.set(memoKey, result);
			return result;
		}
		intersects(range, options) {
			if (!(range instanceof Range)) throw new TypeError("a Range is required");
			return this.set.some((thisComparators) => {
				return isSatisfiable(thisComparators, options) && range.set.some((rangeComparators) => {
					return isSatisfiable(rangeComparators, options) && thisComparators.every((thisComparator) => {
						return rangeComparators.every((rangeComparator) => {
							return thisComparator.intersects(rangeComparator, options);
						});
					});
				});
			});
		}
		test(version) {
			if (!version) return false;
			if (typeof version === "string") try {
				version = new SemVer(version, this.options);
			} catch (er) {
				return false;
			}
			for (let i = 0; i < this.set.length; i++) if (testSet(this.set[i], version, this.options)) return true;
			return false;
		}
	};
	var cache = new (require_lrucache())();
	var parseOptions = require_parse_options();
	var Comparator = require_comparator();
	var debug = require_debug();
	var SemVer = require_semver$1();
	var { safeRe: re, src, t, comparatorTrimReplace, tildeTrimReplace, caretTrimReplace } = require_re();
	var { FLAG_INCLUDE_PRERELEASE, FLAG_LOOSE } = require_constants();
	var BUILDSTRIPRE = new RegExp(src[t.BUILD], "g");
	var isNullSet = (c) => c.value === "<0.0.0-0";
	var isAny = (c) => c.value === "";
	var isSatisfiable = (comparators, options) => {
		let result = true;
		const remainingComparators = comparators.slice();
		let testComparator = remainingComparators.pop();
		while (result && remainingComparators.length) {
			result = remainingComparators.every((otherComparator) => {
				return testComparator.intersects(otherComparator, options);
			});
			testComparator = remainingComparators.pop();
		}
		return result;
	};
	var parseComparator = (comp, options) => {
		comp = comp.replace(re[t.BUILD], "");
		debug("comp", comp, options);
		comp = replaceCarets(comp, options);
		debug("caret", comp);
		comp = replaceTildes(comp, options);
		debug("tildes", comp);
		comp = replaceXRanges(comp, options);
		debug("xrange", comp);
		comp = replaceStars(comp, options);
		debug("stars", comp);
		return comp;
	};
	var isX = (id) => !id || id.toLowerCase() === "x" || id === "*";
	var invalidXRangeOrder = (M, m, p) => isX(M) && !isX(m) || isX(m) && p && !isX(p);
	var replaceTildes = (comp, options) => {
		return comp.trim().split(/\s+/).map((c) => replaceTilde(c, options)).join(" ");
	};
	var replaceTilde = (comp, options) => {
		const r = options.loose ? re[t.TILDELOOSE] : re[t.TILDE];
		const z = options.includePrerelease ? "-0" : "";
		return comp.replace(r, (_, M, m, p, pr) => {
			debug("tilde", comp, _, M, m, p, pr);
			let ret;
			if (isX(M)) ret = "";
			else if (isX(m)) ret = `>=${M}.0.0${z} <${+M + 1}.0.0-0`;
			else if (isX(p)) ret = `>=${M}.${m}.0${z} <${M}.${+m + 1}.0-0`;
			else if (pr) {
				debug("replaceTilde pr", pr);
				ret = `>=${M}.${m}.${p}-${pr} <${M}.${+m + 1}.0-0`;
			} else ret = `>=${M}.${m}.${p} <${M}.${+m + 1}.0-0`;
			debug("tilde return", ret);
			return ret;
		});
	};
	var replaceCarets = (comp, options) => {
		return comp.trim().split(/\s+/).map((c) => replaceCaret(c, options)).join(" ");
	};
	var replaceCaret = (comp, options) => {
		debug("caret", comp, options);
		const r = options.loose ? re[t.CARETLOOSE] : re[t.CARET];
		const z = options.includePrerelease ? "-0" : "";
		return comp.replace(r, (_, M, m, p, pr) => {
			debug("caret", comp, _, M, m, p, pr);
			let ret;
			if (isX(M)) ret = "";
			else if (isX(m)) ret = `>=${M}.0.0${z} <${+M + 1}.0.0-0`;
			else if (isX(p)) {
				if (M === "0") ret = `>=${M}.${m}.0${z} <${M}.${+m + 1}.0-0`;
				else ret = `>=${M}.${m}.0${z} <${+M + 1}.0.0-0`;
			} else if (pr) {
				debug("replaceCaret pr", pr);
				if (M === "0") {
					if (m === "0") ret = `>=${M}.${m}.${p}-${pr} <${M}.${m}.${+p + 1}-0`;
					else ret = `>=${M}.${m}.${p}-${pr} <${M}.${+m + 1}.0-0`;
				} else ret = `>=${M}.${m}.${p}-${pr} <${+M + 1}.0.0-0`;
			} else {
				debug("no pr");
				if (M === "0") {
					if (m === "0") ret = `>=${M}.${m}.${p} <${M}.${m}.${+p + 1}-0`;
					else ret = `>=${M}.${m}.${p} <${M}.${+m + 1}.0-0`;
				} else ret = `>=${M}.${m}.${p} <${+M + 1}.0.0-0`;
			}
			debug("caret return", ret);
			return ret;
		});
	};
	var replaceXRanges = (comp, options) => {
		debug("replaceXRanges", comp, options);
		return comp.split(/\s+/).map((c) => replaceXRange(c, options)).join(" ");
	};
	var replaceXRange = (comp, options) => {
		comp = comp.trim();
		const r = options.loose ? re[t.XRANGELOOSE] : re[t.XRANGE];
		return comp.replace(r, (ret, gtlt, M, m, p, pr) => {
			debug("xRange", comp, ret, gtlt, M, m, p, pr);
			if (invalidXRangeOrder(M, m, p)) return comp;
			const xM = isX(M);
			const xm = xM || isX(m);
			const xp = xm || isX(p);
			const anyX = xp;
			if (gtlt === "=" && anyX) gtlt = "";
			pr = options.includePrerelease ? "-0" : "";
			if (xM) {
				if (gtlt === ">" || gtlt === "<") ret = "<0.0.0-0";
				else ret = "*";
			} else if (gtlt && anyX) {
				if (xm) m = 0;
				p = 0;
				if (gtlt === ">") {
					gtlt = ">=";
					if (xm) {
						M = +M + 1;
						m = 0;
						p = 0;
					} else {
						m = +m + 1;
						p = 0;
					}
				} else if (gtlt === "<=") {
					gtlt = "<";
					if (xm) M = +M + 1;
					else m = +m + 1;
				}
				if (gtlt === "<") pr = "-0";
				ret = `${gtlt + M}.${m}.${p}${pr}`;
			} else if (xm) ret = `>=${M}.0.0${pr} <${+M + 1}.0.0-0`;
			else if (xp) ret = `>=${M}.${m}.0${pr} <${M}.${+m + 1}.0-0`;
			debug("xRange return", ret);
			return ret;
		});
	};
	var replaceStars = (comp, options) => {
		debug("replaceStars", comp, options);
		return comp.trim().replace(re[t.STAR], "");
	};
	var replaceGTE0 = (comp, options) => {
		debug("replaceGTE0", comp, options);
		return comp.trim().replace(re[options.includePrerelease ? t.GTE0PRE : t.GTE0], "");
	};
	var hyphenReplace = (incPr) => ($0, from, fM, fm, fp, fpr, fb, to, tM, tm, tp, tpr) => {
		if (isX(fM)) from = "";
		else if (isX(fm)) from = `>=${fM}.0.0${incPr ? "-0" : ""}`;
		else if (isX(fp)) from = `>=${fM}.${fm}.0${incPr ? "-0" : ""}`;
		else if (fpr) from = `>=${from}`;
		else from = `>=${from}${incPr ? "-0" : ""}`;
		if (isX(tM)) to = "";
		else if (isX(tm)) to = `<${+tM + 1}.0.0-0`;
		else if (isX(tp)) to = `<${tM}.${+tm + 1}.0-0`;
		else if (tpr) to = `<=${tM}.${tm}.${tp}-${tpr}`;
		else if (incPr) to = `<${tM}.${tm}.${+tp + 1}-0`;
		else to = `<=${to}`;
		return `${from} ${to}`.trim();
	};
	var testSet = (set, version, options) => {
		for (let i = 0; i < set.length; i++) if (!set[i].test(version)) return false;
		if (version.prerelease.length && !options.includePrerelease) {
			for (let i = 0; i < set.length; i++) {
				debug(set[i].semver);
				if (set[i].semver === Comparator.ANY) continue;
				if (set[i].semver.prerelease.length > 0) {
					const allowed = set[i].semver;
					if (allowed.major === version.major && allowed.minor === version.minor && allowed.patch === version.patch) return true;
				}
			}
			return false;
		}
		return true;
	};
}));
//#endregion
//#region node_modules/semver/classes/comparator.js
var require_comparator = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var ANY = Symbol("SemVer ANY");
	module.exports = class Comparator {
		static get ANY() {
			return ANY;
		}
		constructor(comp, options) {
			options = parseOptions(options);
			if (comp instanceof Comparator) {
				if (comp.loose === !!options.loose) return comp;
				else comp = comp.value;
			}
			comp = comp.trim().split(/\s+/).join(" ");
			debug("comparator", comp, options);
			this.options = options;
			this.loose = !!options.loose;
			this.parse(comp);
			if (this.semver === ANY) this.value = "";
			else this.value = this.operator + this.semver.version;
			debug("comp", this);
		}
		parse(comp) {
			const r = this.options.loose ? re[t.COMPARATORLOOSE] : re[t.COMPARATOR];
			const m = comp.match(r);
			if (!m) throw new TypeError(`Invalid comparator: ${comp}`);
			this.operator = m[1] !== void 0 ? m[1] : "";
			if (this.operator === "=") this.operator = "";
			if (!m[2]) this.semver = ANY;
			else this.semver = new SemVer(m[2], this.options.loose);
		}
		toString() {
			return this.value;
		}
		test(version) {
			debug("Comparator.test", version, this.options.loose);
			if (this.semver === ANY || version === ANY) return true;
			if (typeof version === "string") try {
				version = new SemVer(version, this.options);
			} catch (er) {
				return false;
			}
			return cmp(version, this.operator, this.semver, this.options);
		}
		intersects(comp, options) {
			if (!(comp instanceof Comparator)) throw new TypeError("a Comparator is required");
			if (this.operator === "") {
				if (this.value === "") return true;
				return new Range(comp.value, options).test(this.value);
			} else if (comp.operator === "") {
				if (comp.value === "") return true;
				return new Range(this.value, options).test(comp.semver);
			}
			options = parseOptions(options);
			if (options.includePrerelease && (this.value === "<0.0.0-0" || comp.value === "<0.0.0-0")) return false;
			if (!options.includePrerelease && (this.value.startsWith("<0.0.0") || comp.value.startsWith("<0.0.0"))) return false;
			if (this.operator.startsWith(">") && comp.operator.startsWith(">")) return true;
			if (this.operator.startsWith("<") && comp.operator.startsWith("<")) return true;
			if (this.semver.version === comp.semver.version && this.operator.includes("=") && comp.operator.includes("=")) return true;
			if (cmp(this.semver, "<", comp.semver, options) && this.operator.startsWith(">") && comp.operator.startsWith("<")) return true;
			if (cmp(this.semver, ">", comp.semver, options) && this.operator.startsWith("<") && comp.operator.startsWith(">")) return true;
			return false;
		}
	};
	var parseOptions = require_parse_options();
	var { safeRe: re, t } = require_re();
	var cmp = require_cmp();
	var debug = require_debug();
	var SemVer = require_semver$1();
	var Range = require_range();
}));
//#endregion
//#region node_modules/semver/functions/satisfies.js
var require_satisfies = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Range = require_range();
	var satisfies = (version, range, options) => {
		try {
			range = new Range(range, options);
		} catch (er) {
			return false;
		}
		return range.test(version);
	};
	module.exports = satisfies;
}));
//#endregion
//#region node_modules/semver/ranges/to-comparators.js
var require_to_comparators = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Range = require_range();
	var toComparators = (range, options) => new Range(range, options).set.map((comp) => comp.map((c) => c.value).join(" ").trim().split(" "));
	module.exports = toComparators;
}));
//#endregion
//#region node_modules/semver/ranges/max-satisfying.js
var require_max_satisfying = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SemVer = require_semver$1();
	var Range = require_range();
	var maxSatisfying = (versions, range, options) => {
		let max = null;
		let maxSV = null;
		let rangeObj = null;
		try {
			rangeObj = new Range(range, options);
		} catch (er) {
			return null;
		}
		versions.forEach((v) => {
			if (rangeObj.test(v)) {
				if (!max || maxSV.compare(v) === -1) {
					max = v;
					maxSV = new SemVer(max, options);
				}
			}
		});
		return max;
	};
	module.exports = maxSatisfying;
}));
//#endregion
//#region node_modules/semver/ranges/min-satisfying.js
var require_min_satisfying = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SemVer = require_semver$1();
	var Range = require_range();
	var minSatisfying = (versions, range, options) => {
		let min = null;
		let minSV = null;
		let rangeObj = null;
		try {
			rangeObj = new Range(range, options);
		} catch (er) {
			return null;
		}
		versions.forEach((v) => {
			if (rangeObj.test(v)) {
				if (!min || minSV.compare(v) === 1) {
					min = v;
					minSV = new SemVer(min, options);
				}
			}
		});
		return min;
	};
	module.exports = minSatisfying;
}));
//#endregion
//#region node_modules/semver/ranges/min-version.js
var require_min_version = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SemVer = require_semver$1();
	var Range = require_range();
	var gt = require_gt();
	var minVersion = (range, loose) => {
		range = new Range(range, loose);
		let minver = new SemVer("0.0.0");
		if (range.test(minver)) return minver;
		minver = new SemVer("0.0.0-0");
		if (range.test(minver)) return minver;
		minver = null;
		for (let i = 0; i < range.set.length; ++i) {
			const comparators = range.set[i];
			let setMin = null;
			comparators.forEach((comparator) => {
				const compver = new SemVer(comparator.semver.version);
				switch (comparator.operator) {
					case ">":
						if (compver.prerelease.length === 0) compver.patch++;
						else compver.prerelease.push(0);
						compver.raw = compver.format();
					case "":
					case ">=":
						if (!setMin || gt(compver, setMin)) setMin = compver;
						break;
					case "<":
					case "<=": break;
					/* istanbul ignore next */
					default: throw new Error(`Unexpected operation: ${comparator.operator}`);
				}
			});
			if (setMin && (!minver || gt(minver, setMin))) minver = setMin;
		}
		if (minver && range.test(minver)) return minver;
		return null;
	};
	module.exports = minVersion;
}));
//#endregion
//#region node_modules/semver/ranges/valid.js
var require_valid = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Range = require_range();
	var validRange = (range, options) => {
		try {
			return new Range(range, options).range || "*";
		} catch (er) {
			return null;
		}
	};
	module.exports = validRange;
}));
//#endregion
//#region node_modules/semver/ranges/outside.js
var require_outside = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SemVer = require_semver$1();
	var Comparator = require_comparator();
	var { ANY } = Comparator;
	var Range = require_range();
	var satisfies = require_satisfies();
	var gt = require_gt();
	var lt = require_lt();
	var lte = require_lte();
	var gte = require_gte();
	var outside = (version, range, hilo, options) => {
		version = new SemVer(version, options);
		range = new Range(range, options);
		let gtfn, ltefn, ltfn, comp, ecomp;
		switch (hilo) {
			case ">":
				gtfn = gt;
				ltefn = lte;
				ltfn = lt;
				comp = ">";
				ecomp = ">=";
				break;
			case "<":
				gtfn = lt;
				ltefn = gte;
				ltfn = gt;
				comp = "<";
				ecomp = "<=";
				break;
			default: throw new TypeError("Must provide a hilo val of \"<\" or \">\"");
		}
		if (satisfies(version, range, options)) return false;
		for (let i = 0; i < range.set.length; ++i) {
			const comparators = range.set[i];
			let high = null;
			let low = null;
			comparators.forEach((comparator) => {
				if (comparator.semver === ANY) comparator = new Comparator(">=0.0.0");
				high = high || comparator;
				low = low || comparator;
				if (gtfn(comparator.semver, high.semver, options)) high = comparator;
				else if (ltfn(comparator.semver, low.semver, options)) low = comparator;
			});
			if (high.operator === comp || high.operator === ecomp) return false;
			if ((!low.operator || low.operator === comp) && ltefn(version, low.semver)) return false;
			else if (low.operator === ecomp && ltfn(version, low.semver)) return false;
		}
		return true;
	};
	module.exports = outside;
}));
//#endregion
//#region node_modules/semver/ranges/gtr.js
var require_gtr = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var outside = require_outside();
	var gtr = (version, range, options) => outside(version, range, ">", options);
	module.exports = gtr;
}));
//#endregion
//#region node_modules/semver/ranges/ltr.js
var require_ltr = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var outside = require_outside();
	var ltr = (version, range, options) => outside(version, range, "<", options);
	module.exports = ltr;
}));
//#endregion
//#region node_modules/semver/ranges/intersects.js
var require_intersects = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Range = require_range();
	var intersects = (r1, r2, options) => {
		r1 = new Range(r1, options);
		r2 = new Range(r2, options);
		return r1.intersects(r2, options);
	};
	module.exports = intersects;
}));
//#endregion
//#region node_modules/semver/ranges/simplify.js
var require_simplify = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var satisfies = require_satisfies();
	var compare = require_compare();
	module.exports = (versions, range, options) => {
		const set = [];
		let first = null;
		let prev = null;
		const v = versions.sort((a, b) => compare(a, b, options));
		for (const version of v) if (satisfies(version, range, options)) {
			prev = version;
			if (!first) first = version;
		} else {
			if (prev) set.push([first, prev]);
			prev = null;
			first = null;
		}
		if (first) set.push([first, null]);
		const ranges = [];
		for (const [min, max] of set) if (min === max) ranges.push(min);
		else if (!max && min === v[0]) ranges.push("*");
		else if (!max) ranges.push(`>=${min}`);
		else if (min === v[0]) ranges.push(`<=${max}`);
		else ranges.push(`${min} - ${max}`);
		const simplified = ranges.join(" || ");
		const original = typeof range.raw === "string" ? range.raw : String(range);
		return simplified.length < original.length ? simplified : range;
	};
}));
//#endregion
//#region node_modules/semver/ranges/subset.js
var require_subset = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Range = require_range();
	var Comparator = require_comparator();
	var { ANY } = Comparator;
	var satisfies = require_satisfies();
	var compare = require_compare();
	var subset = (sub, dom, options = {}) => {
		if (sub === dom) return true;
		sub = new Range(sub, options);
		dom = new Range(dom, options);
		let sawNonNull = false;
		OUTER: for (const simpleSub of sub.set) {
			for (const simpleDom of dom.set) {
				const isSub = simpleSubset(simpleSub, simpleDom, options);
				sawNonNull = sawNonNull || isSub !== null;
				if (isSub) continue OUTER;
			}
			if (sawNonNull) return false;
		}
		return true;
	};
	var minimumVersionWithPreRelease = [new Comparator(">=0.0.0-0")];
	var minimumVersion = [new Comparator(">=0.0.0")];
	var simpleSubset = (sub, dom, options) => {
		if (sub === dom) return true;
		if (sub.length === 1 && sub[0].semver === ANY) {
			if (dom.length === 1 && dom[0].semver === ANY) return true;
			else if (options.includePrerelease) sub = minimumVersionWithPreRelease;
			else sub = minimumVersion;
		}
		if (dom.length === 1 && dom[0].semver === ANY) {
			if (options.includePrerelease) return true;
			else dom = minimumVersion;
		}
		const eqSet = /* @__PURE__ */ new Set();
		let gt, lt;
		for (const c of sub) if (c.operator === ">" || c.operator === ">=") gt = higherGT(gt, c, options);
		else if (c.operator === "<" || c.operator === "<=") lt = lowerLT(lt, c, options);
		else eqSet.add(c.semver);
		if (eqSet.size > 1) return null;
		let gtltComp;
		if (gt && lt) {
			gtltComp = compare(gt.semver, lt.semver, options);
			if (gtltComp > 0) return null;
			else if (gtltComp === 0 && (gt.operator !== ">=" || lt.operator !== "<=")) return null;
		}
		for (const eq of eqSet) {
			if (gt && !satisfies(eq, String(gt), options)) return null;
			if (lt && !satisfies(eq, String(lt), options)) return null;
			for (const c of dom) if (!satisfies(eq, String(c), options)) return false;
			return true;
		}
		let higher, lower;
		let hasDomLT, hasDomGT;
		let needDomLTPre = lt && !options.includePrerelease && lt.semver.prerelease.length ? lt.semver : false;
		let needDomGTPre = gt && !options.includePrerelease && gt.semver.prerelease.length ? gt.semver : false;
		if (needDomLTPre && needDomLTPre.prerelease.length === 1 && lt.operator === "<" && needDomLTPre.prerelease[0] === 0) needDomLTPre = false;
		for (const c of dom) {
			hasDomGT = hasDomGT || c.operator === ">" || c.operator === ">=";
			hasDomLT = hasDomLT || c.operator === "<" || c.operator === "<=";
			if (gt) {
				if (needDomGTPre) {
					if (c.semver.prerelease && c.semver.prerelease.length && c.semver.major === needDomGTPre.major && c.semver.minor === needDomGTPre.minor && c.semver.patch === needDomGTPre.patch) needDomGTPre = false;
				}
				if (c.operator === ">" || c.operator === ">=") {
					higher = higherGT(gt, c, options);
					if (higher === c && higher !== gt) return false;
				} else if (gt.operator === ">=" && !c.test(gt.semver)) return false;
			}
			if (lt) {
				if (needDomLTPre) {
					if (c.semver.prerelease && c.semver.prerelease.length && c.semver.major === needDomLTPre.major && c.semver.minor === needDomLTPre.minor && c.semver.patch === needDomLTPre.patch) needDomLTPre = false;
				}
				if (c.operator === "<" || c.operator === "<=") {
					lower = lowerLT(lt, c, options);
					if (lower === c && lower !== lt) return false;
				} else if (lt.operator === "<=" && !c.test(lt.semver)) return false;
			}
			if (!c.operator && (lt || gt) && gtltComp !== 0) return false;
		}
		if (gt && hasDomLT && !lt && gtltComp !== 0) return false;
		if (lt && hasDomGT && !gt && gtltComp !== 0) return false;
		if (needDomGTPre || needDomLTPre) return false;
		return true;
	};
	var higherGT = (a, b, options) => {
		if (!a) return b;
		const comp = compare(a.semver, b.semver, options);
		return comp > 0 ? a : comp < 0 ? b : b.operator === ">" && a.operator === ">=" ? b : a;
	};
	var lowerLT = (a, b, options) => {
		if (!a) return b;
		const comp = compare(a.semver, b.semver, options);
		return comp < 0 ? a : comp > 0 ? b : b.operator === "<" && a.operator === "<=" ? b : a;
	};
	module.exports = subset;
}));
//#endregion
//#region node_modules/semver/index.js
var require_semver = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var internalRe = require_re();
	var constants = require_constants();
	var SemVer = require_semver$1();
	var identifiers = require_identifiers();
	module.exports = {
		parse: require_parse(),
		valid: require_valid$1(),
		clean: require_clean(),
		inc: require_inc(),
		diff: require_diff(),
		major: require_major(),
		minor: require_minor(),
		patch: require_patch(),
		prerelease: require_prerelease(),
		compare: require_compare(),
		rcompare: require_rcompare(),
		compareLoose: require_compare_loose(),
		compareBuild: require_compare_build(),
		sort: require_sort(),
		rsort: require_rsort(),
		gt: require_gt(),
		lt: require_lt(),
		eq: require_eq(),
		neq: require_neq(),
		gte: require_gte(),
		lte: require_lte(),
		cmp: require_cmp(),
		coerce: require_coerce(),
		truncate: require_truncate(),
		Comparator: require_comparator(),
		Range: require_range(),
		satisfies: require_satisfies(),
		toComparators: require_to_comparators(),
		maxSatisfying: require_max_satisfying(),
		minSatisfying: require_min_satisfying(),
		minVersion: require_min_version(),
		validRange: require_valid(),
		outside: require_outside(),
		gtr: require_gtr(),
		ltr: require_ltr(),
		intersects: require_intersects(),
		simplifyRange: require_simplify(),
		subset: require_subset(),
		SemVer,
		re: internalRe.re,
		src: internalRe.src,
		tokens: internalRe.t,
		SEMVER_SPEC_VERSION: constants.SEMVER_SPEC_VERSION,
		RELEASE_TYPES: constants.RELEASE_TYPES,
		compareIdentifiers: identifiers.compareIdentifiers,
		rcompareIdentifiers: identifiers.rcompareIdentifiers
	};
}));
//#endregion
//#region node_modules/@sanity/logos/dist/index.js
var import_server_browser = require_server_browser();
var import_semver = /* @__PURE__ */ __toESM(require_semver(), 1);
var NEW_SANITY_BLACK = "#0D0E12";
var SanityLogo = /* @__PURE__ */ (0, import_react.forwardRef)(function SanityLogo(props, ref) {
	let { dark, ...svgProps } = props, fill = dark ? white.hex : NEW_SANITY_BLACK;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
		"data-sanity-icon": "sanity-logo",
		height: "1em",
		viewBox: "0 0 435 154",
		fill: "none",
		xmlns: "http://www.w3.org/2000/svg",
		...svgProps,
		ref,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				d: "M44.883 52.4463C33.4181 43.7972 21.8121 37.1021 21.8121 27.3381C21.8121 21.482 26.9827 15.4851 34.1181 15.4851C46.983 15.4851 54.5302 28.8754 61.942 45.6103H66.5538V7.11182H34.3946C7.82952 7.11182 0 23.1543 0 35.7055C0 52.1647 13.5649 61.9287 28.9416 73.365C39.7064 81.3158 48.7948 88.7092 48.7948 97.2175C48.7948 106.424 42.5006 111.723 34.5299 111.723C26.0004 111.723 13.4178 100.562 4.60595 77.4137H0V120.096H36.777C59.4302 120.096 70.7539 102.24 71.1716 87.876C71.7304 71.1411 57.3302 61.7937 44.883 52.4463Z",
				fill
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				d: "M145.002 106.841V61.7878C145.002 42.2598 132.837 37.1021 115.219 37.1021H83.8952L84.0364 68.9054H89.2071C94.6601 55.3743 102.49 45.4754 110.878 45.4754C118.149 45.4754 121.366 51.7539 121.366 58.3082V64.4459C109.619 71.4227 78.301 77.2788 78.301 98.4791C78.301 110.754 86.8306 120.935 99.2778 120.935C110.043 120.935 117.454 113.958 121.231 105.732C121.931 112.568 126.402 120.096 136.614 120.096H152.696V113.958C147.525 113.958 145.008 110.473 145.008 106.847L145.002 106.841ZM120.949 99.8639C118.713 103.074 115.213 106.7 111.019 106.7C105.148 106.7 101.372 102.234 101.372 93.3096C101.372 83.129 115.072 76.5746 120.949 71.6926V99.8697V99.8639Z",
				fill
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				d: "M270.145 28.4529C279.092 28.4529 284.686 22.1744 284.686 14.0885C284.686 6.00274 279.092 0 270.145 0C261.198 0 255.462 5.99688 255.462 14.0885C255.462 22.1802 261.474 28.4529 270.145 28.4529Z",
				fill
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				d: "M409.135 37.102V43.2397C417.947 43.2397 420.459 47.84 416.129 60.2563L405.641 88.5683L392.217 54.1186C389.141 47.4234 390.817 43.2397 397.252 43.2397V37.102H330.557V9.20654H324.263C322.446 18.6889 312.516 37.102 295.598 37.102V43.2397H306.504V100.568C306.504 110.191 309.722 121.07 328.457 121.07H357.54V89.5482H352.37C350.269 96.9417 345.94 111.723 336.569 111.723C331.399 111.723 330.557 106.002 330.557 101.401V45.8861H357.264C360.481 45.8861 363.834 46.3027 365.511 50.2106L393.894 120.231C388.441 132.647 379.07 134.595 362.293 129.437V153.988C366.629 153.988 377.811 154.129 379.77 153.572C390.817 150.362 398.364 128.745 401.441 120.378L425.488 55.521C428.147 48.4092 430.382 43.2456 434.994 43.2456V37.1079H409.129L409.135 37.102Z",
				fill
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				d: "M237.703 106.841V59.2764C237.703 44.3546 230.574 35.4238 216.45 35.4238C204.009 35.4238 196.803 44.0319 191.279 52.0414V37.0961H159.396V43.2338C164.849 43.2338 167.226 46.5785 167.226 50.4864V106.835C167.226 110.602 164.432 113.947 159.396 113.947V120.084H199.109V113.947C194.073 113.947 191.279 110.602 191.279 106.835V59.646C194.503 55.3743 198.214 50.7681 204.42 50.7681C210.291 50.7681 213.65 55.5093 213.65 61.2303V106.841C213.65 110.608 210.856 113.952 205.82 113.952V120.09H245.533V113.952C240.497 113.952 237.703 110.608 237.703 106.841Z",
				fill
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				d: "M284.127 106.841V37.1021H252.245V43.2398C257.698 43.2398 260.074 46.5844 260.074 50.4923V106.841C260.074 110.608 257.28 113.952 252.245 113.952V120.09H291.957V113.952C286.922 113.952 284.127 110.608 284.127 106.841Z",
				fill
			})
		]
	});
});
var SANITY_MONOGRAM_COLOR = {
	light: {
		bg: white.hex,
		fg: NEW_SANITY_BLACK
	},
	dark: {
		bg: NEW_SANITY_BLACK,
		fg: white.hex
	},
	default: {
		bg: "#FF5500",
		fg: NEW_SANITY_BLACK
	}
};
var SanityMonogram = /* @__PURE__ */ (0, import_react.forwardRef)(function SanityMonogram(props, ref) {
	let { scheme = "default", color, ...restProps } = props, bg = (color == null ? void 0 : color.bg1) || SANITY_MONOGRAM_COLOR[scheme].bg, fg = (color == null ? void 0 : color.fg) || SANITY_MONOGRAM_COLOR[scheme].fg;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
		"data-sanity-icon": "sanity-monogram",
		height: "1em",
		viewBox: "0 0 192 192",
		fill: "none",
		xmlns: "http://www.w3.org/2000/svg",
		...restProps,
		ref,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", {
			width: "192",
			height: "192",
			fill: bg
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "M160.077 112.697L154.865 103.629L129.659 118.981L157.655 83.3368L161.888 80.8533L160.841 79.2802L162.764 76.8232L153.929 69.4699L149.886 74.6225L68.2657 122.375L98.4429 86.0855L154.651 55.2759L149.311 44.953L118.696 61.7277L133.771 43.6096L125.134 36L91.2055 76.7966L57.5083 95.2771L83.307 61.1709L99.4731 52.757L94.3391 42.3192L47.2403 66.8361L60.0839 49.8405L51.1123 42.6551L24 78.5378L24.4207 78.8736L29.486 89.1877L59.543 73.5354L32.1474 109.745L36.6375 113.342L39.3075 118.504L70.9528 101.154L36.1052 143.065L44.742 150.674L46.4762 148.588L130.543 99.2454L102.632 134.792L103.088 135.172L103.045 135.199L108.831 145.265L145.954 122.649L131.659 145.716L141.24 152L164 115.278L160.077 112.697Z",
			fill: fg
		})]
	});
});
//#endregion
//#region node_modules/scheduler/cjs/scheduler.development.js
/**
* @license React
* scheduler.development.js
*
* Copyright (c) Meta Platforms, Inc. and affiliates.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_scheduler_development = /* @__PURE__ */ __commonJSMin(((exports) => {
	(function() {
		function performWorkUntilDeadline() {
			needsPaint = !1;
			if (isMessageLoopRunning) {
				var currentTime = exports.unstable_now();
				startTime = currentTime;
				var hasMoreWork = !0;
				try {
					a: {
						isHostCallbackScheduled = !1;
						isHostTimeoutScheduled && (isHostTimeoutScheduled = !1, localClearTimeout(taskTimeoutID), taskTimeoutID = -1);
						isPerformingWork = !0;
						var previousPriorityLevel = currentPriorityLevel;
						try {
							b: {
								advanceTimers(currentTime);
								for (currentTask = peek(taskQueue); null !== currentTask && !(currentTask.expirationTime > currentTime && shouldYieldToHost());) {
									var callback = currentTask.callback;
									if ("function" === typeof callback) {
										currentTask.callback = null;
										currentPriorityLevel = currentTask.priorityLevel;
										var continuationCallback = callback(currentTask.expirationTime <= currentTime);
										currentTime = exports.unstable_now();
										if ("function" === typeof continuationCallback) {
											currentTask.callback = continuationCallback;
											advanceTimers(currentTime);
											hasMoreWork = !0;
											break b;
										}
										currentTask === peek(taskQueue) && pop(taskQueue);
										advanceTimers(currentTime);
									} else pop(taskQueue);
									currentTask = peek(taskQueue);
								}
								if (null !== currentTask) hasMoreWork = !0;
								else {
									var firstTimer = peek(timerQueue);
									null !== firstTimer && requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
									hasMoreWork = !1;
								}
							}
							break a;
						} finally {
							currentTask = null, currentPriorityLevel = previousPriorityLevel, isPerformingWork = !1;
						}
					}
				} finally {
					hasMoreWork ? schedulePerformWorkUntilDeadline() : isMessageLoopRunning = !1;
				}
			}
		}
		function push(heap, node) {
			var index = heap.length;
			heap.push(node);
			a: for (; 0 < index;) {
				var parentIndex = index - 1 >>> 1, parent = heap[parentIndex];
				if (0 < compare(parent, node)) heap[parentIndex] = node, heap[index] = parent, index = parentIndex;
				else break a;
			}
		}
		function peek(heap) {
			return 0 === heap.length ? null : heap[0];
		}
		function pop(heap) {
			if (0 === heap.length) return null;
			var first = heap[0], last = heap.pop();
			if (last !== first) {
				heap[0] = last;
				a: for (var index = 0, length = heap.length, halfLength = length >>> 1; index < halfLength;) {
					var leftIndex = 2 * (index + 1) - 1, left = heap[leftIndex], rightIndex = leftIndex + 1, right = heap[rightIndex];
					if (0 > compare(left, last)) rightIndex < length && 0 > compare(right, left) ? (heap[index] = right, heap[rightIndex] = last, index = rightIndex) : (heap[index] = left, heap[leftIndex] = last, index = leftIndex);
					else if (rightIndex < length && 0 > compare(right, last)) heap[index] = right, heap[rightIndex] = last, index = rightIndex;
					else break a;
				}
			}
			return first;
		}
		function compare(a, b) {
			var diff = a.sortIndex - b.sortIndex;
			return 0 !== diff ? diff : a.id - b.id;
		}
		function advanceTimers(currentTime) {
			for (var timer = peek(timerQueue); null !== timer;) {
				if (null === timer.callback) pop(timerQueue);
				else if (timer.startTime <= currentTime) pop(timerQueue), timer.sortIndex = timer.expirationTime, push(taskQueue, timer);
				else break;
				timer = peek(timerQueue);
			}
		}
		function handleTimeout(currentTime) {
			isHostTimeoutScheduled = !1;
			advanceTimers(currentTime);
			if (!isHostCallbackScheduled) if (null !== peek(taskQueue)) isHostCallbackScheduled = !0, isMessageLoopRunning || (isMessageLoopRunning = !0, schedulePerformWorkUntilDeadline());
			else {
				var firstTimer = peek(timerQueue);
				null !== firstTimer && requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
			}
		}
		function shouldYieldToHost() {
			return needsPaint ? !0 : exports.unstable_now() - startTime < frameInterval ? !1 : !0;
		}
		function requestHostTimeout(callback, ms) {
			taskTimeoutID = localSetTimeout(function() {
				callback(exports.unstable_now());
			}, ms);
		}
		"undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
		exports.unstable_now = void 0;
		if ("object" === typeof performance && "function" === typeof performance.now) {
			var localPerformance = performance;
			exports.unstable_now = function() {
				return localPerformance.now();
			};
		} else {
			var localDate = Date, initialTime = localDate.now();
			exports.unstable_now = function() {
				return localDate.now() - initialTime;
			};
		}
		var taskQueue = [], timerQueue = [], taskIdCounter = 1, currentTask = null, currentPriorityLevel = 3, isPerformingWork = !1, isHostCallbackScheduled = !1, isHostTimeoutScheduled = !1, needsPaint = !1, localSetTimeout = "function" === typeof setTimeout ? setTimeout : null, localClearTimeout = "function" === typeof clearTimeout ? clearTimeout : null, localSetImmediate = "undefined" !== typeof setImmediate ? setImmediate : null, isMessageLoopRunning = !1, taskTimeoutID = -1, frameInterval = 5, startTime = -1;
		if ("function" === typeof localSetImmediate) var schedulePerformWorkUntilDeadline = function() {
			localSetImmediate(performWorkUntilDeadline);
		};
		else if ("undefined" !== typeof MessageChannel) {
			var channel = new MessageChannel(), port = channel.port2;
			channel.port1.onmessage = performWorkUntilDeadline;
			schedulePerformWorkUntilDeadline = function() {
				port.postMessage(null);
			};
		} else schedulePerformWorkUntilDeadline = function() {
			localSetTimeout(performWorkUntilDeadline, 0);
		};
		exports.unstable_IdlePriority = 5;
		exports.unstable_ImmediatePriority = 1;
		exports.unstable_LowPriority = 4;
		exports.unstable_NormalPriority = 3;
		exports.unstable_Profiling = null;
		exports.unstable_UserBlockingPriority = 2;
		exports.unstable_cancelCallback = function(task) {
			task.callback = null;
		};
		exports.unstable_forceFrameRate = function(fps) {
			0 > fps || 125 < fps ? console.error("forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported") : frameInterval = 0 < fps ? Math.floor(1e3 / fps) : 5;
		};
		exports.unstable_getCurrentPriorityLevel = function() {
			return currentPriorityLevel;
		};
		exports.unstable_next = function(eventHandler) {
			switch (currentPriorityLevel) {
				case 1:
				case 2:
				case 3:
					var priorityLevel = 3;
					break;
				default: priorityLevel = currentPriorityLevel;
			}
			var previousPriorityLevel = currentPriorityLevel;
			currentPriorityLevel = priorityLevel;
			try {
				return eventHandler();
			} finally {
				currentPriorityLevel = previousPriorityLevel;
			}
		};
		exports.unstable_requestPaint = function() {
			needsPaint = !0;
		};
		exports.unstable_runWithPriority = function(priorityLevel, eventHandler) {
			switch (priorityLevel) {
				case 1:
				case 2:
				case 3:
				case 4:
				case 5: break;
				default: priorityLevel = 3;
			}
			var previousPriorityLevel = currentPriorityLevel;
			currentPriorityLevel = priorityLevel;
			try {
				return eventHandler();
			} finally {
				currentPriorityLevel = previousPriorityLevel;
			}
		};
		exports.unstable_scheduleCallback = function(priorityLevel, callback, options) {
			var currentTime = exports.unstable_now();
			"object" === typeof options && null !== options ? (options = options.delay, options = "number" === typeof options && 0 < options ? currentTime + options : currentTime) : options = currentTime;
			switch (priorityLevel) {
				case 1:
					var timeout = -1;
					break;
				case 2:
					timeout = 250;
					break;
				case 5:
					timeout = 1073741823;
					break;
				case 4:
					timeout = 1e4;
					break;
				default: timeout = 5e3;
			}
			timeout = options + timeout;
			priorityLevel = {
				id: taskIdCounter++,
				callback,
				priorityLevel,
				startTime: options,
				expirationTime: timeout,
				sortIndex: -1
			};
			options > currentTime ? (priorityLevel.sortIndex = options, push(timerQueue, priorityLevel), null === peek(taskQueue) && priorityLevel === peek(timerQueue) && (isHostTimeoutScheduled ? (localClearTimeout(taskTimeoutID), taskTimeoutID = -1) : isHostTimeoutScheduled = !0, requestHostTimeout(handleTimeout, options - currentTime))) : (priorityLevel.sortIndex = timeout, push(taskQueue, priorityLevel), isHostCallbackScheduled || isPerformingWork || (isHostCallbackScheduled = !0, isMessageLoopRunning || (isMessageLoopRunning = !0, schedulePerformWorkUntilDeadline())));
			return priorityLevel;
		};
		exports.unstable_shouldYield = shouldYieldToHost;
		exports.unstable_wrapCallback = function(callback) {
			var parentPriorityLevel = currentPriorityLevel;
			return function() {
				var previousPriorityLevel = currentPriorityLevel;
				currentPriorityLevel = parentPriorityLevel;
				try {
					return callback.apply(this, arguments);
				} finally {
					currentPriorityLevel = previousPriorityLevel;
				}
			};
		};
		"undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
	})();
}));
//#endregion
//#region node_modules/scheduler/index.js
var require_scheduler = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_scheduler_development();
}));
//#endregion
//#region node_modules/react-dom/cjs/react-dom-client.development.js
/**
* @license React
* react-dom-client.development.js
*
* Copyright (c) Meta Platforms, Inc. and affiliates.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_react_dom_client_development = /* @__PURE__ */ __commonJSMin(((exports) => {
	(function() {
		function findHook(fiber, id) {
			for (fiber = fiber.memoizedState; null !== fiber && 0 < id;) fiber = fiber.next, id--;
			return fiber;
		}
		function copyWithSetImpl(obj, path, index, value) {
			if (index >= path.length) return value;
			var key = path[index], updated = isArrayImpl(obj) ? obj.slice() : assign({}, obj);
			updated[key] = copyWithSetImpl(obj[key], path, index + 1, value);
			return updated;
		}
		function copyWithRename(obj, oldPath, newPath) {
			if (oldPath.length !== newPath.length) console.warn("copyWithRename() expects paths of the same length");
			else {
				for (var i = 0; i < newPath.length - 1; i++) if (oldPath[i] !== newPath[i]) {
					console.warn("copyWithRename() expects paths to be the same except for the deepest key");
					return;
				}
				return copyWithRenameImpl(obj, oldPath, newPath, 0);
			}
		}
		function copyWithRenameImpl(obj, oldPath, newPath, index) {
			var oldKey = oldPath[index], updated = isArrayImpl(obj) ? obj.slice() : assign({}, obj);
			index + 1 === oldPath.length ? (updated[newPath[index]] = updated[oldKey], isArrayImpl(updated) ? updated.splice(oldKey, 1) : delete updated[oldKey]) : updated[oldKey] = copyWithRenameImpl(obj[oldKey], oldPath, newPath, index + 1);
			return updated;
		}
		function copyWithDeleteImpl(obj, path, index) {
			var key = path[index], updated = isArrayImpl(obj) ? obj.slice() : assign({}, obj);
			if (index + 1 === path.length) return isArrayImpl(updated) ? updated.splice(key, 1) : delete updated[key], updated;
			updated[key] = copyWithDeleteImpl(obj[key], path, index + 1);
			return updated;
		}
		function shouldSuspendImpl() {
			return !1;
		}
		function shouldErrorImpl() {
			return null;
		}
		function warnInvalidHookAccess() {
			console.error("Do not call Hooks inside useEffect(...), useMemo(...), or other built-in Hooks. You can only call Hooks at the top level of your React function. For more information, see https://react.dev/link/rules-of-hooks");
		}
		function warnInvalidContextAccess() {
			console.error("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo().");
		}
		function noop() {}
		function warnForMissingKey() {}
		function setToSortedString(set) {
			var array = [];
			set.forEach(function(value) {
				array.push(value);
			});
			return array.sort().join(", ");
		}
		function createFiber(tag, pendingProps, key, mode) {
			return new FiberNode(tag, pendingProps, key, mode);
		}
		function scheduleRoot(root, element) {
			root.context === emptyContextObject && (updateContainerImpl(root.current, 2, element, root, null, null), flushSyncWork$1());
		}
		function scheduleRefresh(root, update) {
			if (null !== resolveFamily) {
				var staleFamilies = update.staleFamilies;
				update = update.updatedFamilies;
				flushPendingEffects();
				scheduleFibersWithFamiliesRecursively(root.current, update, staleFamilies);
				flushSyncWork$1();
			}
		}
		function setRefreshHandler(handler) {
			resolveFamily = handler;
		}
		function isValidContainer(node) {
			return !(!node || 1 !== node.nodeType && 9 !== node.nodeType && 11 !== node.nodeType);
		}
		function getNearestMountedFiber(fiber) {
			var node = fiber, nearestMounted = fiber;
			if (fiber.alternate) for (; node.return;) node = node.return;
			else {
				fiber = node;
				do
					node = fiber, 0 !== (node.flags & 4098) && (nearestMounted = node.return), fiber = node.return;
				while (fiber);
			}
			return 3 === node.tag ? nearestMounted : null;
		}
		function getSuspenseInstanceFromFiber(fiber) {
			if (13 === fiber.tag) {
				var suspenseState = fiber.memoizedState;
				null === suspenseState && (fiber = fiber.alternate, null !== fiber && (suspenseState = fiber.memoizedState));
				if (null !== suspenseState) return suspenseState.dehydrated;
			}
			return null;
		}
		function getActivityInstanceFromFiber(fiber) {
			if (31 === fiber.tag) {
				var activityState = fiber.memoizedState;
				null === activityState && (fiber = fiber.alternate, null !== fiber && (activityState = fiber.memoizedState));
				if (null !== activityState) return activityState.dehydrated;
			}
			return null;
		}
		function assertIsMounted(fiber) {
			if (getNearestMountedFiber(fiber) !== fiber) throw Error("Unable to find node on an unmounted component.");
		}
		function findCurrentFiberUsingSlowPath(fiber) {
			var alternate = fiber.alternate;
			if (!alternate) {
				alternate = getNearestMountedFiber(fiber);
				if (null === alternate) throw Error("Unable to find node on an unmounted component.");
				return alternate !== fiber ? null : fiber;
			}
			for (var a = fiber, b = alternate;;) {
				var parentA = a.return;
				if (null === parentA) break;
				var parentB = parentA.alternate;
				if (null === parentB) {
					b = parentA.return;
					if (null !== b) {
						a = b;
						continue;
					}
					break;
				}
				if (parentA.child === parentB.child) {
					for (parentB = parentA.child; parentB;) {
						if (parentB === a) return assertIsMounted(parentA), fiber;
						if (parentB === b) return assertIsMounted(parentA), alternate;
						parentB = parentB.sibling;
					}
					throw Error("Unable to find node on an unmounted component.");
				}
				if (a.return !== b.return) a = parentA, b = parentB;
				else {
					for (var didFindChild = !1, _child = parentA.child; _child;) {
						if (_child === a) {
							didFindChild = !0;
							a = parentA;
							b = parentB;
							break;
						}
						if (_child === b) {
							didFindChild = !0;
							b = parentA;
							a = parentB;
							break;
						}
						_child = _child.sibling;
					}
					if (!didFindChild) {
						for (_child = parentB.child; _child;) {
							if (_child === a) {
								didFindChild = !0;
								a = parentB;
								b = parentA;
								break;
							}
							if (_child === b) {
								didFindChild = !0;
								b = parentB;
								a = parentA;
								break;
							}
							_child = _child.sibling;
						}
						if (!didFindChild) throw Error("Child was not found in either parent set. This indicates a bug in React related to the return pointer. Please file an issue.");
					}
				}
				if (a.alternate !== b) throw Error("Return fibers should always be each others' alternates. This error is likely caused by a bug in React. Please file an issue.");
			}
			if (3 !== a.tag) throw Error("Unable to find node on an unmounted component.");
			return a.stateNode.current === a ? fiber : alternate;
		}
		function findCurrentHostFiberImpl(node) {
			var tag = node.tag;
			if (5 === tag || 26 === tag || 27 === tag || 6 === tag) return node;
			for (node = node.child; null !== node;) {
				tag = findCurrentHostFiberImpl(node);
				if (null !== tag) return tag;
				node = node.sibling;
			}
			return null;
		}
		function getIteratorFn(maybeIterable) {
			if (null === maybeIterable || "object" !== typeof maybeIterable) return null;
			maybeIterable = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable["@@iterator"];
			return "function" === typeof maybeIterable ? maybeIterable : null;
		}
		function getComponentNameFromType(type) {
			if (null == type) return null;
			if ("function" === typeof type) return type.$$typeof === REACT_CLIENT_REFERENCE ? null : type.displayName || type.name || null;
			if ("string" === typeof type) return type;
			switch (type) {
				case REACT_FRAGMENT_TYPE: return "Fragment";
				case REACT_PROFILER_TYPE: return "Profiler";
				case REACT_STRICT_MODE_TYPE: return "StrictMode";
				case REACT_SUSPENSE_TYPE: return "Suspense";
				case REACT_SUSPENSE_LIST_TYPE: return "SuspenseList";
				case REACT_ACTIVITY_TYPE: return "Activity";
			}
			if ("object" === typeof type) switch ("number" === typeof type.tag && console.error("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), type.$$typeof) {
				case REACT_PORTAL_TYPE: return "Portal";
				case REACT_CONTEXT_TYPE: return type.displayName || "Context";
				case REACT_CONSUMER_TYPE: return (type._context.displayName || "Context") + ".Consumer";
				case REACT_FORWARD_REF_TYPE:
					var innerType = type.render;
					type = type.displayName;
					type || (type = innerType.displayName || innerType.name || "", type = "" !== type ? "ForwardRef(" + type + ")" : "ForwardRef");
					return type;
				case REACT_MEMO_TYPE: return innerType = type.displayName || null, null !== innerType ? innerType : getComponentNameFromType(type.type) || "Memo";
				case REACT_LAZY_TYPE:
					innerType = type._payload;
					type = type._init;
					try {
						return getComponentNameFromType(type(innerType));
					} catch (x) {}
			}
			return null;
		}
		function getComponentNameFromOwner(owner) {
			return "number" === typeof owner.tag ? getComponentNameFromFiber(owner) : "string" === typeof owner.name ? owner.name : null;
		}
		function getComponentNameFromFiber(fiber) {
			var type = fiber.type;
			switch (fiber.tag) {
				case 31: return "Activity";
				case 24: return "Cache";
				case 9: return (type._context.displayName || "Context") + ".Consumer";
				case 10: return type.displayName || "Context";
				case 18: return "DehydratedFragment";
				case 11: return fiber = type.render, fiber = fiber.displayName || fiber.name || "", type.displayName || ("" !== fiber ? "ForwardRef(" + fiber + ")" : "ForwardRef");
				case 7: return "Fragment";
				case 26:
				case 27:
				case 5: return type;
				case 4: return "Portal";
				case 3: return "Root";
				case 6: return "Text";
				case 16: return getComponentNameFromType(type);
				case 8: return type === REACT_STRICT_MODE_TYPE ? "StrictMode" : "Mode";
				case 22: return "Offscreen";
				case 12: return "Profiler";
				case 21: return "Scope";
				case 13: return "Suspense";
				case 19: return "SuspenseList";
				case 25: return "TracingMarker";
				case 1:
				case 0:
				case 14:
				case 15:
					if ("function" === typeof type) return type.displayName || type.name || null;
					if ("string" === typeof type) return type;
					break;
				case 29:
					type = fiber._debugInfo;
					if (null != type) {
						for (var i = type.length - 1; 0 <= i; i--) if ("string" === typeof type[i].name) return type[i].name;
					}
					if (null !== fiber.return) return getComponentNameFromFiber(fiber.return);
			}
			return null;
		}
		function createCursor(defaultValue) {
			return { current: defaultValue };
		}
		function pop(cursor, fiber) {
			0 > index$jscomp$0 ? console.error("Unexpected pop.") : (fiber !== fiberStack[index$jscomp$0] && console.error("Unexpected Fiber popped."), cursor.current = valueStack[index$jscomp$0], valueStack[index$jscomp$0] = null, fiberStack[index$jscomp$0] = null, index$jscomp$0--);
		}
		function push(cursor, value, fiber) {
			index$jscomp$0++;
			valueStack[index$jscomp$0] = cursor.current;
			fiberStack[index$jscomp$0] = fiber;
			cursor.current = value;
		}
		function requiredContext(c) {
			null === c && console.error("Expected host context to exist. This error is likely caused by a bug in React. Please file an issue.");
			return c;
		}
		function pushHostContainer(fiber, nextRootInstance) {
			push(rootInstanceStackCursor, nextRootInstance, fiber);
			push(contextFiberStackCursor, fiber, fiber);
			push(contextStackCursor, null, fiber);
			var nextRootContext = nextRootInstance.nodeType;
			switch (nextRootContext) {
				case 9:
				case 11:
					nextRootContext = 9 === nextRootContext ? "#document" : "#fragment";
					nextRootInstance = (nextRootInstance = nextRootInstance.documentElement) ? (nextRootInstance = nextRootInstance.namespaceURI) ? getOwnHostContext(nextRootInstance) : HostContextNamespaceNone : HostContextNamespaceNone;
					break;
				default: if (nextRootContext = nextRootInstance.tagName, nextRootInstance = nextRootInstance.namespaceURI) nextRootInstance = getOwnHostContext(nextRootInstance), nextRootInstance = getChildHostContextProd(nextRootInstance, nextRootContext);
				else switch (nextRootContext) {
					case "svg":
						nextRootInstance = HostContextNamespaceSvg;
						break;
					case "math":
						nextRootInstance = HostContextNamespaceMath;
						break;
					default: nextRootInstance = HostContextNamespaceNone;
				}
			}
			nextRootContext = nextRootContext.toLowerCase();
			nextRootContext = updatedAncestorInfoDev(null, nextRootContext);
			nextRootContext = {
				context: nextRootInstance,
				ancestorInfo: nextRootContext
			};
			pop(contextStackCursor, fiber);
			push(contextStackCursor, nextRootContext, fiber);
		}
		function popHostContainer(fiber) {
			pop(contextStackCursor, fiber);
			pop(contextFiberStackCursor, fiber);
			pop(rootInstanceStackCursor, fiber);
		}
		function getHostContext() {
			return requiredContext(contextStackCursor.current);
		}
		function pushHostContext(fiber) {
			null !== fiber.memoizedState && push(hostTransitionProviderCursor, fiber, fiber);
			var context = requiredContext(contextStackCursor.current);
			var type = fiber.type;
			var nextContext = getChildHostContextProd(context.context, type);
			type = updatedAncestorInfoDev(context.ancestorInfo, type);
			nextContext = {
				context: nextContext,
				ancestorInfo: type
			};
			context !== nextContext && (push(contextFiberStackCursor, fiber, fiber), push(contextStackCursor, nextContext, fiber));
		}
		function popHostContext(fiber) {
			contextFiberStackCursor.current === fiber && (pop(contextStackCursor, fiber), pop(contextFiberStackCursor, fiber));
			hostTransitionProviderCursor.current === fiber && (pop(hostTransitionProviderCursor, fiber), HostTransitionContext._currentValue = NotPendingTransition);
		}
		function disabledLog() {}
		function disableLogs() {
			if (0 === disabledDepth) {
				prevLog = console.log;
				prevInfo = console.info;
				prevWarn = console.warn;
				prevError = console.error;
				prevGroup = console.group;
				prevGroupCollapsed = console.groupCollapsed;
				prevGroupEnd = console.groupEnd;
				var props = {
					configurable: !0,
					enumerable: !0,
					value: disabledLog,
					writable: !0
				};
				Object.defineProperties(console, {
					info: props,
					log: props,
					warn: props,
					error: props,
					group: props,
					groupCollapsed: props,
					groupEnd: props
				});
			}
			disabledDepth++;
		}
		function reenableLogs() {
			disabledDepth--;
			if (0 === disabledDepth) {
				var props = {
					configurable: !0,
					enumerable: !0,
					writable: !0
				};
				Object.defineProperties(console, {
					log: assign({}, props, { value: prevLog }),
					info: assign({}, props, { value: prevInfo }),
					warn: assign({}, props, { value: prevWarn }),
					error: assign({}, props, { value: prevError }),
					group: assign({}, props, { value: prevGroup }),
					groupCollapsed: assign({}, props, { value: prevGroupCollapsed }),
					groupEnd: assign({}, props, { value: prevGroupEnd })
				});
			}
			0 > disabledDepth && console.error("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
		}
		function formatOwnerStack(error) {
			var prevPrepareStackTrace = Error.prepareStackTrace;
			Error.prepareStackTrace = void 0;
			error = error.stack;
			Error.prepareStackTrace = prevPrepareStackTrace;
			error.startsWith("Error: react-stack-top-frame\n") && (error = error.slice(29));
			prevPrepareStackTrace = error.indexOf("\n");
			-1 !== prevPrepareStackTrace && (error = error.slice(prevPrepareStackTrace + 1));
			prevPrepareStackTrace = error.indexOf("react_stack_bottom_frame");
			-1 !== prevPrepareStackTrace && (prevPrepareStackTrace = error.lastIndexOf("\n", prevPrepareStackTrace));
			if (-1 !== prevPrepareStackTrace) error = error.slice(0, prevPrepareStackTrace);
			else return "";
			return error;
		}
		function describeBuiltInComponentFrame(name) {
			if (void 0 === prefix) try {
				throw Error();
			} catch (x) {
				var match = x.stack.trim().match(/\n( *(at )?)/);
				prefix = match && match[1] || "";
				suffix = -1 < x.stack.indexOf("\n    at") ? " (<anonymous>)" : -1 < x.stack.indexOf("@") ? "@unknown:0:0" : "";
			}
			return "\n" + prefix + name + suffix;
		}
		function describeNativeComponentFrame(fn, construct) {
			if (!fn || reentry) return "";
			var frame = componentFrameCache.get(fn);
			if (void 0 !== frame) return frame;
			reentry = !0;
			frame = Error.prepareStackTrace;
			Error.prepareStackTrace = void 0;
			var previousDispatcher = null;
			previousDispatcher = ReactSharedInternals.H;
			ReactSharedInternals.H = null;
			disableLogs();
			try {
				var RunInRootFrame = { DetermineComponentFrameRoot: function() {
					try {
						if (construct) {
							var Fake = function() {
								throw Error();
							};
							Object.defineProperty(Fake.prototype, "props", { set: function() {
								throw Error();
							} });
							if ("object" === typeof Reflect && Reflect.construct) {
								try {
									Reflect.construct(Fake, []);
								} catch (x) {
									var control = x;
								}
								Reflect.construct(fn, [], Fake);
							} else {
								try {
									Fake.call();
								} catch (x$0) {
									control = x$0;
								}
								fn.call(Fake.prototype);
							}
						} else {
							try {
								throw Error();
							} catch (x$1) {
								control = x$1;
							}
							(Fake = fn()) && "function" === typeof Fake.catch && Fake.catch(function() {});
						}
					} catch (sample) {
						if (sample && control && "string" === typeof sample.stack) return [sample.stack, control.stack];
					}
					return [null, null];
				} };
				RunInRootFrame.DetermineComponentFrameRoot.displayName = "DetermineComponentFrameRoot";
				var namePropDescriptor = Object.getOwnPropertyDescriptor(RunInRootFrame.DetermineComponentFrameRoot, "name");
				namePropDescriptor && namePropDescriptor.configurable && Object.defineProperty(RunInRootFrame.DetermineComponentFrameRoot, "name", { value: "DetermineComponentFrameRoot" });
				var _RunInRootFrame$Deter = RunInRootFrame.DetermineComponentFrameRoot(), sampleStack = _RunInRootFrame$Deter[0], controlStack = _RunInRootFrame$Deter[1];
				if (sampleStack && controlStack) {
					var sampleLines = sampleStack.split("\n"), controlLines = controlStack.split("\n");
					for (_RunInRootFrame$Deter = namePropDescriptor = 0; namePropDescriptor < sampleLines.length && !sampleLines[namePropDescriptor].includes("DetermineComponentFrameRoot");) namePropDescriptor++;
					for (; _RunInRootFrame$Deter < controlLines.length && !controlLines[_RunInRootFrame$Deter].includes("DetermineComponentFrameRoot");) _RunInRootFrame$Deter++;
					if (namePropDescriptor === sampleLines.length || _RunInRootFrame$Deter === controlLines.length) for (namePropDescriptor = sampleLines.length - 1, _RunInRootFrame$Deter = controlLines.length - 1; 1 <= namePropDescriptor && 0 <= _RunInRootFrame$Deter && sampleLines[namePropDescriptor] !== controlLines[_RunInRootFrame$Deter];) _RunInRootFrame$Deter--;
					for (; 1 <= namePropDescriptor && 0 <= _RunInRootFrame$Deter; namePropDescriptor--, _RunInRootFrame$Deter--) if (sampleLines[namePropDescriptor] !== controlLines[_RunInRootFrame$Deter]) {
						if (1 !== namePropDescriptor || 1 !== _RunInRootFrame$Deter) do
							if (namePropDescriptor--, _RunInRootFrame$Deter--, 0 > _RunInRootFrame$Deter || sampleLines[namePropDescriptor] !== controlLines[_RunInRootFrame$Deter]) {
								var _frame = "\n" + sampleLines[namePropDescriptor].replace(" at new ", " at ");
								fn.displayName && _frame.includes("<anonymous>") && (_frame = _frame.replace("<anonymous>", fn.displayName));
								"function" === typeof fn && componentFrameCache.set(fn, _frame);
								return _frame;
							}
						while (1 <= namePropDescriptor && 0 <= _RunInRootFrame$Deter);
						break;
					}
				}
			} finally {
				reentry = !1, ReactSharedInternals.H = previousDispatcher, reenableLogs(), Error.prepareStackTrace = frame;
			}
			sampleLines = (sampleLines = fn ? fn.displayName || fn.name : "") ? describeBuiltInComponentFrame(sampleLines) : "";
			"function" === typeof fn && componentFrameCache.set(fn, sampleLines);
			return sampleLines;
		}
		function describeFiber(fiber, childFiber) {
			switch (fiber.tag) {
				case 26:
				case 27:
				case 5: return describeBuiltInComponentFrame(fiber.type);
				case 16: return describeBuiltInComponentFrame("Lazy");
				case 13: return fiber.child !== childFiber && null !== childFiber ? describeBuiltInComponentFrame("Suspense Fallback") : describeBuiltInComponentFrame("Suspense");
				case 19: return describeBuiltInComponentFrame("SuspenseList");
				case 0:
				case 15: return describeNativeComponentFrame(fiber.type, !1);
				case 11: return describeNativeComponentFrame(fiber.type.render, !1);
				case 1: return describeNativeComponentFrame(fiber.type, !0);
				case 31: return describeBuiltInComponentFrame("Activity");
				default: return "";
			}
		}
		function getStackByFiberInDevAndProd(workInProgress) {
			try {
				var info = "", previous = null;
				do {
					info += describeFiber(workInProgress, previous);
					var debugInfo = workInProgress._debugInfo;
					if (debugInfo) for (var i = debugInfo.length - 1; 0 <= i; i--) {
						var entry = debugInfo[i];
						if ("string" === typeof entry.name) {
							var JSCompiler_temp_const = info;
							a: {
								var name = entry.name, env = entry.env, location = entry.debugLocation;
								if (null != location) {
									var childStack = formatOwnerStack(location), idx = childStack.lastIndexOf("\n"), lastLine = -1 === idx ? childStack : childStack.slice(idx + 1);
									if (-1 !== lastLine.indexOf(name)) {
										var JSCompiler_inline_result = "\n" + lastLine;
										break a;
									}
								}
								JSCompiler_inline_result = describeBuiltInComponentFrame(name + (env ? " [" + env + "]" : ""));
							}
							info = JSCompiler_temp_const + JSCompiler_inline_result;
						}
					}
					previous = workInProgress;
					workInProgress = workInProgress.return;
				} while (workInProgress);
				return info;
			} catch (x) {
				return "\nError generating stack: " + x.message + "\n" + x.stack;
			}
		}
		function describeFunctionComponentFrameWithoutLineNumber(fn) {
			return (fn = fn ? fn.displayName || fn.name : "") ? describeBuiltInComponentFrame(fn) : "";
		}
		function getCurrentFiberOwnerNameInDevOrNull() {
			if (null === current) return null;
			var owner = current._debugOwner;
			return null != owner ? getComponentNameFromOwner(owner) : null;
		}
		function getCurrentFiberStackInDev() {
			if (null === current) return "";
			var workInProgress = current;
			try {
				var info = "";
				6 === workInProgress.tag && (workInProgress = workInProgress.return);
				switch (workInProgress.tag) {
					case 26:
					case 27:
					case 5:
						info += describeBuiltInComponentFrame(workInProgress.type);
						break;
					case 13:
						info += describeBuiltInComponentFrame("Suspense");
						break;
					case 19:
						info += describeBuiltInComponentFrame("SuspenseList");
						break;
					case 31:
						info += describeBuiltInComponentFrame("Activity");
						break;
					case 30:
					case 0:
					case 15:
					case 1:
						workInProgress._debugOwner || "" !== info || (info += describeFunctionComponentFrameWithoutLineNumber(workInProgress.type));
						break;
					case 11: workInProgress._debugOwner || "" !== info || (info += describeFunctionComponentFrameWithoutLineNumber(workInProgress.type.render));
				}
				for (; workInProgress;) if ("number" === typeof workInProgress.tag) {
					var fiber = workInProgress;
					workInProgress = fiber._debugOwner;
					var debugStack = fiber._debugStack;
					if (workInProgress && debugStack) {
						var formattedStack = formatOwnerStack(debugStack);
						"" !== formattedStack && (info += "\n" + formattedStack);
					}
				} else if (null != workInProgress.debugStack) {
					var ownerStack = workInProgress.debugStack;
					(workInProgress = workInProgress.owner) && ownerStack && (info += "\n" + formatOwnerStack(ownerStack));
				} else break;
				var JSCompiler_inline_result = info;
			} catch (x) {
				JSCompiler_inline_result = "\nError generating stack: " + x.message + "\n" + x.stack;
			}
			return JSCompiler_inline_result;
		}
		function runWithFiberInDEV(fiber, callback, arg0, arg1, arg2, arg3, arg4) {
			var previousFiber = current;
			setCurrentFiber(fiber);
			try {
				return null !== fiber && fiber._debugTask ? fiber._debugTask.run(callback.bind(null, arg0, arg1, arg2, arg3, arg4)) : callback(arg0, arg1, arg2, arg3, arg4);
			} finally {
				setCurrentFiber(previousFiber);
			}
		}
		function setCurrentFiber(fiber) {
			ReactSharedInternals.getCurrentStack = null === fiber ? null : getCurrentFiberStackInDev;
			isRendering = !1;
			current = fiber;
		}
		function typeName(value) {
			return "function" === typeof Symbol && Symbol.toStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
		}
		function willCoercionThrow(value) {
			try {
				return testStringCoercion(value), !1;
			} catch (e) {
				return !0;
			}
		}
		function testStringCoercion(value) {
			return "" + value;
		}
		function checkAttributeStringCoercion(value, attributeName) {
			if (willCoercionThrow(value)) return console.error("The provided `%s` attribute is an unsupported type %s. This value must be coerced to a string before using it here.", attributeName, typeName(value)), testStringCoercion(value);
		}
		function checkCSSPropertyStringCoercion(value, propName) {
			if (willCoercionThrow(value)) return console.error("The provided `%s` CSS property is an unsupported type %s. This value must be coerced to a string before using it here.", propName, typeName(value)), testStringCoercion(value);
		}
		function checkFormFieldValueStringCoercion(value) {
			if (willCoercionThrow(value)) return console.error("Form field values (value, checked, defaultValue, or defaultChecked props) must be strings, not %s. This value must be coerced to a string before using it here.", typeName(value)), testStringCoercion(value);
		}
		function injectInternals(internals) {
			if ("undefined" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__) return !1;
			var hook = __REACT_DEVTOOLS_GLOBAL_HOOK__;
			if (hook.isDisabled) return !0;
			if (!hook.supportsFiber) return console.error("The installed version of React DevTools is too old and will not work with the current version of React. Please update React DevTools. https://react.dev/link/react-devtools"), !0;
			try {
				rendererID = hook.inject(internals), injectedHook = hook;
			} catch (err) {
				console.error("React instrumentation encountered an error: %o.", err);
			}
			return hook.checkDCE ? !0 : !1;
		}
		function setIsStrictModeForDevtools(newIsStrictMode) {
			"function" === typeof log$1 && unstable_setDisableYieldValue(newIsStrictMode);
			if (injectedHook && "function" === typeof injectedHook.setStrictMode) try {
				injectedHook.setStrictMode(rendererID, newIsStrictMode);
			} catch (err) {
				hasLoggedError || (hasLoggedError = !0, console.error("React instrumentation encountered an error: %o", err));
			}
		}
		function clz32Fallback(x) {
			x >>>= 0;
			return 0 === x ? 32 : 31 - (log(x) / LN2 | 0) | 0;
		}
		function getHighestPriorityLanes(lanes) {
			var pendingSyncLanes = lanes & 42;
			if (0 !== pendingSyncLanes) return pendingSyncLanes;
			switch (lanes & -lanes) {
				case 1: return 1;
				case 2: return 2;
				case 4: return 4;
				case 8: return 8;
				case 16: return 16;
				case 32: return 32;
				case 64: return 64;
				case 128: return 128;
				case 256:
				case 512:
				case 1024:
				case 2048:
				case 4096:
				case 8192:
				case 16384:
				case 32768:
				case 65536:
				case 131072: return lanes & 261888;
				case 262144:
				case 524288:
				case 1048576:
				case 2097152: return lanes & 3932160;
				case 4194304:
				case 8388608:
				case 16777216:
				case 33554432: return lanes & 62914560;
				case 67108864: return 67108864;
				case 134217728: return 134217728;
				case 268435456: return 268435456;
				case 536870912: return 536870912;
				case 1073741824: return 0;
				default: return console.error("Should have found matching lanes. This is a bug in React."), lanes;
			}
		}
		function getNextLanes(root, wipLanes, rootHasPendingCommit) {
			var pendingLanes = root.pendingLanes;
			if (0 === pendingLanes) return 0;
			var nextLanes = 0, suspendedLanes = root.suspendedLanes, pingedLanes = root.pingedLanes;
			root = root.warmLanes;
			var nonIdlePendingLanes = pendingLanes & 134217727;
			0 !== nonIdlePendingLanes ? (pendingLanes = nonIdlePendingLanes & ~suspendedLanes, 0 !== pendingLanes ? nextLanes = getHighestPriorityLanes(pendingLanes) : (pingedLanes &= nonIdlePendingLanes, 0 !== pingedLanes ? nextLanes = getHighestPriorityLanes(pingedLanes) : rootHasPendingCommit || (rootHasPendingCommit = nonIdlePendingLanes & ~root, 0 !== rootHasPendingCommit && (nextLanes = getHighestPriorityLanes(rootHasPendingCommit))))) : (nonIdlePendingLanes = pendingLanes & ~suspendedLanes, 0 !== nonIdlePendingLanes ? nextLanes = getHighestPriorityLanes(nonIdlePendingLanes) : 0 !== pingedLanes ? nextLanes = getHighestPriorityLanes(pingedLanes) : rootHasPendingCommit || (rootHasPendingCommit = pendingLanes & ~root, 0 !== rootHasPendingCommit && (nextLanes = getHighestPriorityLanes(rootHasPendingCommit))));
			return 0 === nextLanes ? 0 : 0 !== wipLanes && wipLanes !== nextLanes && 0 === (wipLanes & suspendedLanes) && (suspendedLanes = nextLanes & -nextLanes, rootHasPendingCommit = wipLanes & -wipLanes, suspendedLanes >= rootHasPendingCommit || 32 === suspendedLanes && 0 !== (rootHasPendingCommit & 4194048)) ? wipLanes : nextLanes;
		}
		function checkIfRootIsPrerendering(root, renderLanes) {
			return 0 === (root.pendingLanes & ~(root.suspendedLanes & ~root.pingedLanes) & renderLanes);
		}
		function computeExpirationTime(lane, currentTime) {
			switch (lane) {
				case 1:
				case 2:
				case 4:
				case 8:
				case 64: return currentTime + 250;
				case 16:
				case 32:
				case 128:
				case 256:
				case 512:
				case 1024:
				case 2048:
				case 4096:
				case 8192:
				case 16384:
				case 32768:
				case 65536:
				case 131072:
				case 262144:
				case 524288:
				case 1048576:
				case 2097152: return currentTime + 5e3;
				case 4194304:
				case 8388608:
				case 16777216:
				case 33554432: return -1;
				case 67108864:
				case 134217728:
				case 268435456:
				case 536870912:
				case 1073741824: return -1;
				default: return console.error("Should have found matching lanes. This is a bug in React."), -1;
			}
		}
		function claimNextRetryLane() {
			var lane = nextRetryLane;
			nextRetryLane <<= 1;
			0 === (nextRetryLane & 62914560) && (nextRetryLane = 4194304);
			return lane;
		}
		function createLaneMap(initial) {
			for (var laneMap = [], i = 0; 31 > i; i++) laneMap.push(initial);
			return laneMap;
		}
		function markRootUpdated$1(root, updateLane) {
			root.pendingLanes |= updateLane;
			268435456 !== updateLane && (root.suspendedLanes = 0, root.pingedLanes = 0, root.warmLanes = 0);
		}
		function markRootFinished(root, finishedLanes, remainingLanes, spawnedLane, updatedLanes, suspendedRetryLanes) {
			var previouslyPendingLanes = root.pendingLanes;
			root.pendingLanes = remainingLanes;
			root.suspendedLanes = 0;
			root.pingedLanes = 0;
			root.warmLanes = 0;
			root.expiredLanes &= remainingLanes;
			root.entangledLanes &= remainingLanes;
			root.errorRecoveryDisabledLanes &= remainingLanes;
			root.shellSuspendCounter = 0;
			var entanglements = root.entanglements, expirationTimes = root.expirationTimes, hiddenUpdates = root.hiddenUpdates;
			for (remainingLanes = previouslyPendingLanes & ~remainingLanes; 0 < remainingLanes;) {
				var index = 31 - clz32(remainingLanes), lane = 1 << index;
				entanglements[index] = 0;
				expirationTimes[index] = -1;
				var hiddenUpdatesForLane = hiddenUpdates[index];
				if (null !== hiddenUpdatesForLane) for (hiddenUpdates[index] = null, index = 0; index < hiddenUpdatesForLane.length; index++) {
					var update = hiddenUpdatesForLane[index];
					null !== update && (update.lane &= -536870913);
				}
				remainingLanes &= ~lane;
			}
			0 !== spawnedLane && markSpawnedDeferredLane(root, spawnedLane, 0);
			0 !== suspendedRetryLanes && 0 === updatedLanes && 0 !== root.tag && (root.suspendedLanes |= suspendedRetryLanes & ~(previouslyPendingLanes & ~finishedLanes));
		}
		function markSpawnedDeferredLane(root, spawnedLane, entangledLanes) {
			root.pendingLanes |= spawnedLane;
			root.suspendedLanes &= ~spawnedLane;
			var spawnedLaneIndex = 31 - clz32(spawnedLane);
			root.entangledLanes |= spawnedLane;
			root.entanglements[spawnedLaneIndex] = root.entanglements[spawnedLaneIndex] | 1073741824 | entangledLanes & 261930;
		}
		function markRootEntangled(root, entangledLanes) {
			var rootEntangledLanes = root.entangledLanes |= entangledLanes;
			for (root = root.entanglements; rootEntangledLanes;) {
				var index = 31 - clz32(rootEntangledLanes), lane = 1 << index;
				lane & entangledLanes | root[index] & entangledLanes && (root[index] |= entangledLanes);
				rootEntangledLanes &= ~lane;
			}
		}
		function getBumpedLaneForHydration(root, renderLanes) {
			var renderLane = renderLanes & -renderLanes;
			renderLane = 0 !== (renderLane & 42) ? 1 : getBumpedLaneForHydrationByLane(renderLane);
			return 0 !== (renderLane & (root.suspendedLanes | renderLanes)) ? 0 : renderLane;
		}
		function getBumpedLaneForHydrationByLane(lane) {
			switch (lane) {
				case 2:
					lane = 1;
					break;
				case 8:
					lane = 4;
					break;
				case 32:
					lane = 16;
					break;
				case 256:
				case 512:
				case 1024:
				case 2048:
				case 4096:
				case 8192:
				case 16384:
				case 32768:
				case 65536:
				case 131072:
				case 262144:
				case 524288:
				case 1048576:
				case 2097152:
				case 4194304:
				case 8388608:
				case 16777216:
				case 33554432:
					lane = 128;
					break;
				case 268435456:
					lane = 134217728;
					break;
				default: lane = 0;
			}
			return lane;
		}
		function addFiberToLanesMap(root, fiber, lanes) {
			if (isDevToolsPresent) for (root = root.pendingUpdatersLaneMap; 0 < lanes;) {
				var index = 31 - clz32(lanes), lane = 1 << index;
				root[index].add(fiber);
				lanes &= ~lane;
			}
		}
		function movePendingFibersToMemoized(root, lanes) {
			if (isDevToolsPresent) for (var pendingUpdatersLaneMap = root.pendingUpdatersLaneMap, memoizedUpdaters = root.memoizedUpdaters; 0 < lanes;) {
				var index = 31 - clz32(lanes);
				root = 1 << index;
				index = pendingUpdatersLaneMap[index];
				0 < index.size && (index.forEach(function(fiber) {
					var alternate = fiber.alternate;
					null !== alternate && memoizedUpdaters.has(alternate) || memoizedUpdaters.add(fiber);
				}), index.clear());
				lanes &= ~root;
			}
		}
		function lanesToEventPriority(lanes) {
			lanes &= -lanes;
			return 0 !== DiscreteEventPriority && DiscreteEventPriority < lanes ? 0 !== ContinuousEventPriority && ContinuousEventPriority < lanes ? 0 !== (lanes & 134217727) ? DefaultEventPriority : IdleEventPriority : ContinuousEventPriority : DiscreteEventPriority;
		}
		function resolveUpdatePriority() {
			var updatePriority = ReactDOMSharedInternals.p;
			if (0 !== updatePriority) return updatePriority;
			updatePriority = window.event;
			return void 0 === updatePriority ? DefaultEventPriority : getEventPriority(updatePriority.type);
		}
		function runWithPriority(priority, fn) {
			var previousPriority = ReactDOMSharedInternals.p;
			try {
				return ReactDOMSharedInternals.p = priority, fn();
			} finally {
				ReactDOMSharedInternals.p = previousPriority;
			}
		}
		function detachDeletedInstance(node) {
			delete node[internalInstanceKey];
			delete node[internalPropsKey];
			delete node[internalEventHandlersKey];
			delete node[internalEventHandlerListenersKey];
			delete node[internalEventHandlesSetKey];
		}
		function getClosestInstanceFromNode(targetNode) {
			var targetInst = targetNode[internalInstanceKey];
			if (targetInst) return targetInst;
			for (var parentNode = targetNode.parentNode; parentNode;) {
				if (targetInst = parentNode[internalContainerInstanceKey] || parentNode[internalInstanceKey]) {
					parentNode = targetInst.alternate;
					if (null !== targetInst.child || null !== parentNode && null !== parentNode.child) for (targetNode = getParentHydrationBoundary(targetNode); null !== targetNode;) {
						if (parentNode = targetNode[internalInstanceKey]) return parentNode;
						targetNode = getParentHydrationBoundary(targetNode);
					}
					return targetInst;
				}
				targetNode = parentNode;
				parentNode = targetNode.parentNode;
			}
			return null;
		}
		function getInstanceFromNode(node) {
			if (node = node[internalInstanceKey] || node[internalContainerInstanceKey]) {
				var tag = node.tag;
				if (5 === tag || 6 === tag || 13 === tag || 31 === tag || 26 === tag || 27 === tag || 3 === tag) return node;
			}
			return null;
		}
		function getNodeFromInstance(inst) {
			var tag = inst.tag;
			if (5 === tag || 26 === tag || 27 === tag || 6 === tag) return inst.stateNode;
			throw Error("getNodeFromInstance: Invalid argument.");
		}
		function getResourcesFromRoot(root) {
			var resources = root[internalRootNodeResourcesKey];
			resources || (resources = root[internalRootNodeResourcesKey] = {
				hoistableStyles: /* @__PURE__ */ new Map(),
				hoistableScripts: /* @__PURE__ */ new Map()
			});
			return resources;
		}
		function markNodeAsHoistable(node) {
			node[internalHoistableMarker] = !0;
		}
		function registerTwoPhaseEvent(registrationName, dependencies) {
			registerDirectEvent(registrationName, dependencies);
			registerDirectEvent(registrationName + "Capture", dependencies);
		}
		function registerDirectEvent(registrationName, dependencies) {
			registrationNameDependencies[registrationName] && console.error("EventRegistry: More than one plugin attempted to publish the same registration name, `%s`.", registrationName);
			registrationNameDependencies[registrationName] = dependencies;
			var lowerCasedName = registrationName.toLowerCase();
			possibleRegistrationNames[lowerCasedName] = registrationName;
			"onDoubleClick" === registrationName && (possibleRegistrationNames.ondblclick = registrationName);
			for (registrationName = 0; registrationName < dependencies.length; registrationName++) allNativeEvents.add(dependencies[registrationName]);
		}
		function checkControlledValueProps(tagName, props) {
			hasReadOnlyValue[props.type] || props.onChange || props.onInput || props.readOnly || props.disabled || null == props.value || ("select" === tagName ? console.error("You provided a `value` prop to a form field without an `onChange` handler. This will render a read-only field. If the field should be mutable use `defaultValue`. Otherwise, set `onChange`.") : console.error("You provided a `value` prop to a form field without an `onChange` handler. This will render a read-only field. If the field should be mutable use `defaultValue`. Otherwise, set either `onChange` or `readOnly`."));
			props.onChange || props.readOnly || props.disabled || null == props.checked || console.error("You provided a `checked` prop to a form field without an `onChange` handler. This will render a read-only field. If the field should be mutable use `defaultChecked`. Otherwise, set either `onChange` or `readOnly`.");
		}
		function isAttributeNameSafe(attributeName) {
			if (hasOwnProperty.call(validatedAttributeNameCache, attributeName)) return !0;
			if (hasOwnProperty.call(illegalAttributeNameCache, attributeName)) return !1;
			if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName)) return validatedAttributeNameCache[attributeName] = !0;
			illegalAttributeNameCache[attributeName] = !0;
			console.error("Invalid attribute name: `%s`", attributeName);
			return !1;
		}
		function getValueForAttributeOnCustomComponent(node, name, expected) {
			if (isAttributeNameSafe(name)) {
				if (!node.hasAttribute(name)) {
					switch (typeof expected) {
						case "symbol":
						case "object": return expected;
						case "function": return expected;
						case "boolean": if (!1 === expected) return expected;
					}
					return void 0 === expected ? void 0 : null;
				}
				node = node.getAttribute(name);
				if ("" === node && !0 === expected) return !0;
				checkAttributeStringCoercion(expected, name);
				return node === "" + expected ? expected : node;
			}
		}
		function setValueForAttribute(node, name, value) {
			if (isAttributeNameSafe(name)) if (null === value) node.removeAttribute(name);
			else {
				switch (typeof value) {
					case "undefined":
					case "function":
					case "symbol":
						node.removeAttribute(name);
						return;
					case "boolean":
						var prefix = name.toLowerCase().slice(0, 5);
						if ("data-" !== prefix && "aria-" !== prefix) {
							node.removeAttribute(name);
							return;
						}
				}
				checkAttributeStringCoercion(value, name);
				node.setAttribute(name, "" + value);
			}
		}
		function setValueForKnownAttribute(node, name, value) {
			if (null === value) node.removeAttribute(name);
			else {
				switch (typeof value) {
					case "undefined":
					case "function":
					case "symbol":
					case "boolean":
						node.removeAttribute(name);
						return;
				}
				checkAttributeStringCoercion(value, name);
				node.setAttribute(name, "" + value);
			}
		}
		function setValueForNamespacedAttribute(node, namespace, name, value) {
			if (null === value) node.removeAttribute(name);
			else {
				switch (typeof value) {
					case "undefined":
					case "function":
					case "symbol":
					case "boolean":
						node.removeAttribute(name);
						return;
				}
				checkAttributeStringCoercion(value, name);
				node.setAttributeNS(namespace, name, "" + value);
			}
		}
		function getToStringValue(value) {
			switch (typeof value) {
				case "bigint":
				case "boolean":
				case "number":
				case "string":
				case "undefined": return value;
				case "object": return checkFormFieldValueStringCoercion(value), value;
				default: return "";
			}
		}
		function isCheckable(elem) {
			var type = elem.type;
			return (elem = elem.nodeName) && "input" === elem.toLowerCase() && ("checkbox" === type || "radio" === type);
		}
		function trackValueOnNode(node, valueField, currentValue) {
			var descriptor = Object.getOwnPropertyDescriptor(node.constructor.prototype, valueField);
			if (!node.hasOwnProperty(valueField) && "undefined" !== typeof descriptor && "function" === typeof descriptor.get && "function" === typeof descriptor.set) {
				var get = descriptor.get, set = descriptor.set;
				Object.defineProperty(node, valueField, {
					configurable: !0,
					get: function() {
						return get.call(this);
					},
					set: function(value) {
						checkFormFieldValueStringCoercion(value);
						currentValue = "" + value;
						set.call(this, value);
					}
				});
				Object.defineProperty(node, valueField, { enumerable: descriptor.enumerable });
				return {
					getValue: function() {
						return currentValue;
					},
					setValue: function(value) {
						checkFormFieldValueStringCoercion(value);
						currentValue = "" + value;
					},
					stopTracking: function() {
						node._valueTracker = null;
						delete node[valueField];
					}
				};
			}
		}
		function track(node) {
			if (!node._valueTracker) {
				var valueField = isCheckable(node) ? "checked" : "value";
				node._valueTracker = trackValueOnNode(node, valueField, "" + node[valueField]);
			}
		}
		function updateValueIfChanged(node) {
			if (!node) return !1;
			var tracker = node._valueTracker;
			if (!tracker) return !0;
			var lastValue = tracker.getValue();
			var value = "";
			node && (value = isCheckable(node) ? node.checked ? "true" : "false" : node.value);
			node = value;
			return node !== lastValue ? (tracker.setValue(node), !0) : !1;
		}
		function getActiveElement(doc) {
			doc = doc || ("undefined" !== typeof document ? document : void 0);
			if ("undefined" === typeof doc) return null;
			try {
				return doc.activeElement || doc.body;
			} catch (e) {
				return doc.body;
			}
		}
		function escapeSelectorAttributeValueInsideDoubleQuotes(value) {
			return value.replace(escapeSelectorAttributeValueInsideDoubleQuotesRegex, function(ch) {
				return "\\" + ch.charCodeAt(0).toString(16) + " ";
			});
		}
		function validateInputProps(element, props) {
			void 0 === props.checked || void 0 === props.defaultChecked || didWarnCheckedDefaultChecked || (console.error("%s contains an input of type %s with both checked and defaultChecked props. Input elements must be either controlled or uncontrolled (specify either the checked prop, or the defaultChecked prop, but not both). Decide between using a controlled or uncontrolled input element and remove one of these props. More info: https://react.dev/link/controlled-components", getCurrentFiberOwnerNameInDevOrNull() || "A component", props.type), didWarnCheckedDefaultChecked = !0);
			void 0 === props.value || void 0 === props.defaultValue || didWarnValueDefaultValue$1 || (console.error("%s contains an input of type %s with both value and defaultValue props. Input elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled input element and remove one of these props. More info: https://react.dev/link/controlled-components", getCurrentFiberOwnerNameInDevOrNull() || "A component", props.type), didWarnValueDefaultValue$1 = !0);
		}
		function updateInput(element, value, defaultValue, lastDefaultValue, checked, defaultChecked, type, name) {
			element.name = "";
			null != type && "function" !== typeof type && "symbol" !== typeof type && "boolean" !== typeof type ? (checkAttributeStringCoercion(type, "type"), element.type = type) : element.removeAttribute("type");
			if (null != value) if ("number" === type) {
				if (0 === value && "" === element.value || element.value != value) element.value = "" + getToStringValue(value);
			} else element.value !== "" + getToStringValue(value) && (element.value = "" + getToStringValue(value));
			else "submit" !== type && "reset" !== type || element.removeAttribute("value");
			null != value ? setDefaultValue(element, type, getToStringValue(value)) : null != defaultValue ? setDefaultValue(element, type, getToStringValue(defaultValue)) : null != lastDefaultValue && element.removeAttribute("value");
			null == checked && null != defaultChecked && (element.defaultChecked = !!defaultChecked);
			null != checked && (element.checked = checked && "function" !== typeof checked && "symbol" !== typeof checked);
			null != name && "function" !== typeof name && "symbol" !== typeof name && "boolean" !== typeof name ? (checkAttributeStringCoercion(name, "name"), element.name = "" + getToStringValue(name)) : element.removeAttribute("name");
		}
		function initInput(element, value, defaultValue, checked, defaultChecked, type, name, isHydrating) {
			null != type && "function" !== typeof type && "symbol" !== typeof type && "boolean" !== typeof type && (checkAttributeStringCoercion(type, "type"), element.type = type);
			if (null != value || null != defaultValue) {
				if (!("submit" !== type && "reset" !== type || void 0 !== value && null !== value)) {
					track(element);
					return;
				}
				defaultValue = null != defaultValue ? "" + getToStringValue(defaultValue) : "";
				value = null != value ? "" + getToStringValue(value) : defaultValue;
				isHydrating || value === element.value || (element.value = value);
				element.defaultValue = value;
			}
			checked = null != checked ? checked : defaultChecked;
			checked = "function" !== typeof checked && "symbol" !== typeof checked && !!checked;
			element.checked = isHydrating ? element.checked : !!checked;
			element.defaultChecked = !!checked;
			null != name && "function" !== typeof name && "symbol" !== typeof name && "boolean" !== typeof name && (checkAttributeStringCoercion(name, "name"), element.name = name);
			track(element);
		}
		function setDefaultValue(node, type, value) {
			"number" === type && getActiveElement(node.ownerDocument) === node || node.defaultValue === "" + value || (node.defaultValue = "" + value);
		}
		function validateOptionProps(element, props) {
			props.value ?? ("object" === typeof props.children && null !== props.children ? React.Children.forEach(props.children, function(child) {
				null == child || "string" === typeof child || "number" === typeof child || "bigint" === typeof child || didWarnInvalidChild || (didWarnInvalidChild = !0, console.error("Cannot infer the option value of complex children. Pass a `value` prop or use a plain string as children to <option>."));
			}) : null == props.dangerouslySetInnerHTML || didWarnInvalidInnerHTML || (didWarnInvalidInnerHTML = !0, console.error("Pass a `value` prop if you set dangerouslyInnerHTML so React knows which value should be selected.")));
			null == props.selected || didWarnSelectedSetOnOption || (console.error("Use the `defaultValue` or `value` props on <select> instead of setting `selected` on <option>."), didWarnSelectedSetOnOption = !0);
		}
		function getDeclarationErrorAddendum() {
			var ownerName = getCurrentFiberOwnerNameInDevOrNull();
			return ownerName ? "\n\nCheck the render method of `" + ownerName + "`." : "";
		}
		function updateOptions(node, multiple, propValue, setDefaultSelected) {
			node = node.options;
			if (multiple) {
				multiple = {};
				for (var i = 0; i < propValue.length; i++) multiple["$" + propValue[i]] = !0;
				for (propValue = 0; propValue < node.length; propValue++) i = multiple.hasOwnProperty("$" + node[propValue].value), node[propValue].selected !== i && (node[propValue].selected = i), i && setDefaultSelected && (node[propValue].defaultSelected = !0);
			} else {
				propValue = "" + getToStringValue(propValue);
				multiple = null;
				for (i = 0; i < node.length; i++) {
					if (node[i].value === propValue) {
						node[i].selected = !0;
						setDefaultSelected && (node[i].defaultSelected = !0);
						return;
					}
					null !== multiple || node[i].disabled || (multiple = node[i]);
				}
				null !== multiple && (multiple.selected = !0);
			}
		}
		function validateSelectProps(element, props) {
			for (element = 0; element < valuePropNames.length; element++) {
				var propName = valuePropNames[element];
				if (null != props[propName]) {
					var propNameIsArray = isArrayImpl(props[propName]);
					props.multiple && !propNameIsArray ? console.error("The `%s` prop supplied to <select> must be an array if `multiple` is true.%s", propName, getDeclarationErrorAddendum()) : !props.multiple && propNameIsArray && console.error("The `%s` prop supplied to <select> must be a scalar value if `multiple` is false.%s", propName, getDeclarationErrorAddendum());
				}
			}
			void 0 === props.value || void 0 === props.defaultValue || didWarnValueDefaultValue || (console.error("Select elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled select element and remove one of these props. More info: https://react.dev/link/controlled-components"), didWarnValueDefaultValue = !0);
		}
		function validateTextareaProps(element, props) {
			void 0 === props.value || void 0 === props.defaultValue || didWarnValDefaultVal || (console.error("%s contains a textarea with both value and defaultValue props. Textarea elements must be either controlled or uncontrolled (specify either the value prop, or the defaultValue prop, but not both). Decide between using a controlled or uncontrolled textarea and remove one of these props. More info: https://react.dev/link/controlled-components", getCurrentFiberOwnerNameInDevOrNull() || "A component"), didWarnValDefaultVal = !0);
			null != props.children && null == props.value && console.error("Use the `defaultValue` or `value` props instead of setting children on <textarea>.");
		}
		function updateTextarea(element, value, defaultValue) {
			if (null != value && (value = "" + getToStringValue(value), value !== element.value && (element.value = value), null == defaultValue)) {
				element.defaultValue !== value && (element.defaultValue = value);
				return;
			}
			element.defaultValue = null != defaultValue ? "" + getToStringValue(defaultValue) : "";
		}
		function initTextarea(element, value, defaultValue, children) {
			if (null == value) {
				if (null != children) {
					if (null != defaultValue) throw Error("If you supply `defaultValue` on a <textarea>, do not pass children.");
					if (isArrayImpl(children)) {
						if (1 < children.length) throw Error("<textarea> can only have at most one child.");
						children = children[0];
					}
					defaultValue = children;
				}
				defaultValue ??= "";
				value = defaultValue;
			}
			defaultValue = getToStringValue(value);
			element.defaultValue = defaultValue;
			children = element.textContent;
			children === defaultValue && "" !== children && null !== children && (element.value = children);
			track(element);
		}
		function findNotableNode(node, indent) {
			return void 0 === node.serverProps && 0 === node.serverTail.length && 1 === node.children.length && 3 < node.distanceFromLeaf && node.distanceFromLeaf > 15 - indent ? findNotableNode(node.children[0], indent) : node;
		}
		function indentation(indent) {
			return "  " + "  ".repeat(indent);
		}
		function added(indent) {
			return "+ " + "  ".repeat(indent);
		}
		function removed(indent) {
			return "- " + "  ".repeat(indent);
		}
		function describeFiberType(fiber) {
			switch (fiber.tag) {
				case 26:
				case 27:
				case 5: return fiber.type;
				case 16: return "Lazy";
				case 31: return "Activity";
				case 13: return "Suspense";
				case 19: return "SuspenseList";
				case 0:
				case 15: return fiber = fiber.type, fiber.displayName || fiber.name || null;
				case 11: return fiber = fiber.type.render, fiber.displayName || fiber.name || null;
				case 1: return fiber = fiber.type, fiber.displayName || fiber.name || null;
				default: return null;
			}
		}
		function describeTextNode(content, maxLength) {
			return needsEscaping.test(content) ? (content = JSON.stringify(content), content.length > maxLength - 2 ? 8 > maxLength ? "{\"...\"}" : "{" + content.slice(0, maxLength - 7) + "...\"}" : "{" + content + "}") : content.length > maxLength ? 5 > maxLength ? "{\"...\"}" : content.slice(0, maxLength - 3) + "..." : content;
		}
		function describeTextDiff(clientText, serverProps, indent) {
			var maxLength = 120 - 2 * indent;
			if (null === serverProps) return added(indent) + describeTextNode(clientText, maxLength) + "\n";
			if ("string" === typeof serverProps) {
				for (var firstDiff = 0; firstDiff < serverProps.length && firstDiff < clientText.length && serverProps.charCodeAt(firstDiff) === clientText.charCodeAt(firstDiff); firstDiff++);
				firstDiff > maxLength - 8 && 10 < firstDiff && (clientText = "..." + clientText.slice(firstDiff - 8), serverProps = "..." + serverProps.slice(firstDiff - 8));
				return added(indent) + describeTextNode(clientText, maxLength) + "\n" + removed(indent) + describeTextNode(serverProps, maxLength) + "\n";
			}
			return indentation(indent) + describeTextNode(clientText, maxLength) + "\n";
		}
		function objectName(object) {
			return Object.prototype.toString.call(object).replace(/^\[object (.*)\]$/, function(m, p0) {
				return p0;
			});
		}
		function describeValue(value, maxLength) {
			switch (typeof value) {
				case "string": return value = JSON.stringify(value), value.length > maxLength ? 5 > maxLength ? "\"...\"" : value.slice(0, maxLength - 4) + "...\"" : value;
				case "object":
					if (null === value) return "null";
					if (isArrayImpl(value)) return "[...]";
					if (value.$$typeof === REACT_ELEMENT_TYPE) return (maxLength = getComponentNameFromType(value.type)) ? "<" + maxLength + ">" : "<...>";
					var name = objectName(value);
					if ("Object" === name) {
						name = "";
						maxLength -= 2;
						for (var propName in value) if (value.hasOwnProperty(propName)) {
							var jsonPropName = JSON.stringify(propName);
							jsonPropName !== "\"" + propName + "\"" && (propName = jsonPropName);
							maxLength -= propName.length - 2;
							jsonPropName = describeValue(value[propName], 15 > maxLength ? maxLength : 15);
							maxLength -= jsonPropName.length;
							if (0 > maxLength) {
								name += "" === name ? "..." : ", ...";
								break;
							}
							name += ("" === name ? "" : ",") + propName + ":" + jsonPropName;
						}
						return "{" + name + "}";
					}
					return name;
				case "function": return (maxLength = value.displayName || value.name) ? "function " + maxLength : "function";
				default: return String(value);
			}
		}
		function describePropValue(value, maxLength) {
			return "string" !== typeof value || needsEscaping.test(value) ? "{" + describeValue(value, maxLength - 2) + "}" : value.length > maxLength - 2 ? 5 > maxLength ? "\"...\"" : "\"" + value.slice(0, maxLength - 5) + "...\"" : "\"" + value + "\"";
		}
		function describeExpandedElement(type, props, rowPrefix) {
			var remainingRowLength = 120 - rowPrefix.length - type.length, properties = [], propName;
			for (propName in props) if (props.hasOwnProperty(propName) && "children" !== propName) {
				var propValue = describePropValue(props[propName], 120 - rowPrefix.length - propName.length - 1);
				remainingRowLength -= propName.length + propValue.length + 2;
				properties.push(propName + "=" + propValue);
			}
			return 0 === properties.length ? rowPrefix + "<" + type + ">\n" : 0 < remainingRowLength ? rowPrefix + "<" + type + " " + properties.join(" ") + ">\n" : rowPrefix + "<" + type + "\n" + rowPrefix + "  " + properties.join("\n" + rowPrefix + "  ") + "\n" + rowPrefix + ">\n";
		}
		function describePropertiesDiff(clientObject, serverObject, indent) {
			var properties = "", remainingServerProperties = assign({}, serverObject), propName;
			for (propName in clientObject) if (clientObject.hasOwnProperty(propName)) {
				delete remainingServerProperties[propName];
				var maxLength = 120 - 2 * indent - propName.length - 2, clientPropValue = describeValue(clientObject[propName], maxLength);
				serverObject.hasOwnProperty(propName) ? (maxLength = describeValue(serverObject[propName], maxLength), properties += added(indent) + propName + ": " + clientPropValue + "\n", properties += removed(indent) + propName + ": " + maxLength + "\n") : properties += added(indent) + propName + ": " + clientPropValue + "\n";
			}
			for (var _propName in remainingServerProperties) remainingServerProperties.hasOwnProperty(_propName) && (clientObject = describeValue(remainingServerProperties[_propName], 120 - 2 * indent - _propName.length - 2), properties += removed(indent) + _propName + ": " + clientObject + "\n");
			return properties;
		}
		function describeElementDiff(type, clientProps, serverProps, indent) {
			var content = "", serverPropNames = /* @__PURE__ */ new Map();
			for (propName$jscomp$0 in serverProps) serverProps.hasOwnProperty(propName$jscomp$0) && serverPropNames.set(propName$jscomp$0.toLowerCase(), propName$jscomp$0);
			if (1 === serverPropNames.size && serverPropNames.has("children")) content += describeExpandedElement(type, clientProps, indentation(indent));
			else {
				for (var _propName2 in clientProps) if (clientProps.hasOwnProperty(_propName2) && "children" !== _propName2) {
					var maxLength$jscomp$0 = 120 - 2 * (indent + 1) - _propName2.length - 1, serverPropName = serverPropNames.get(_propName2.toLowerCase());
					if (void 0 !== serverPropName) {
						serverPropNames.delete(_propName2.toLowerCase());
						var propName$jscomp$0 = clientProps[_propName2];
						serverPropName = serverProps[serverPropName];
						var clientPropValue = describePropValue(propName$jscomp$0, maxLength$jscomp$0);
						maxLength$jscomp$0 = describePropValue(serverPropName, maxLength$jscomp$0);
						"object" === typeof propName$jscomp$0 && null !== propName$jscomp$0 && "object" === typeof serverPropName && null !== serverPropName && "Object" === objectName(propName$jscomp$0) && "Object" === objectName(serverPropName) && (2 < Object.keys(propName$jscomp$0).length || 2 < Object.keys(serverPropName).length || -1 < clientPropValue.indexOf("...") || -1 < maxLength$jscomp$0.indexOf("...")) ? content += indentation(indent + 1) + _propName2 + "={{\n" + describePropertiesDiff(propName$jscomp$0, serverPropName, indent + 2) + indentation(indent + 1) + "}}\n" : (content += added(indent + 1) + _propName2 + "=" + clientPropValue + "\n", content += removed(indent + 1) + _propName2 + "=" + maxLength$jscomp$0 + "\n");
					} else content += indentation(indent + 1) + _propName2 + "=" + describePropValue(clientProps[_propName2], maxLength$jscomp$0) + "\n";
				}
				serverPropNames.forEach(function(propName) {
					if ("children" !== propName) {
						var maxLength = 120 - 2 * (indent + 1) - propName.length - 1;
						content += removed(indent + 1) + propName + "=" + describePropValue(serverProps[propName], maxLength) + "\n";
					}
				});
				content = "" === content ? indentation(indent) + "<" + type + ">\n" : indentation(indent) + "<" + type + "\n" + content + indentation(indent) + ">\n";
			}
			type = serverProps.children;
			clientProps = clientProps.children;
			if ("string" === typeof type || "number" === typeof type || "bigint" === typeof type) {
				serverPropNames = "";
				if ("string" === typeof clientProps || "number" === typeof clientProps || "bigint" === typeof clientProps) serverPropNames = "" + clientProps;
				content += describeTextDiff(serverPropNames, "" + type, indent + 1);
			} else if ("string" === typeof clientProps || "number" === typeof clientProps || "bigint" === typeof clientProps) content = null == type ? content + describeTextDiff("" + clientProps, null, indent + 1) : content + describeTextDiff("" + clientProps, void 0, indent + 1);
			return content;
		}
		function describeSiblingFiber(fiber, indent) {
			var type = describeFiberType(fiber);
			if (null === type) {
				type = "";
				for (fiber = fiber.child; fiber;) type += describeSiblingFiber(fiber, indent), fiber = fiber.sibling;
				return type;
			}
			return indentation(indent) + "<" + type + ">\n";
		}
		function describeNode(node, indent) {
			var skipToNode = findNotableNode(node, indent);
			if (skipToNode !== node && (1 !== node.children.length || node.children[0] !== skipToNode)) return indentation(indent) + "...\n" + describeNode(skipToNode, indent + 1);
			skipToNode = "";
			var debugInfo = node.fiber._debugInfo;
			if (debugInfo) for (var i = 0; i < debugInfo.length; i++) {
				var serverComponentName = debugInfo[i].name;
				"string" === typeof serverComponentName && (skipToNode += indentation(indent) + "<" + serverComponentName + ">\n", indent++);
			}
			debugInfo = "";
			i = node.fiber.pendingProps;
			if (6 === node.fiber.tag) debugInfo = describeTextDiff(i, node.serverProps, indent), indent++;
			else if (serverComponentName = describeFiberType(node.fiber), null !== serverComponentName) if (void 0 === node.serverProps) {
				debugInfo = indent;
				var maxLength = 120 - 2 * debugInfo - serverComponentName.length - 2, content = "";
				for (propName in i) if (i.hasOwnProperty(propName) && "children" !== propName) {
					var propValue = describePropValue(i[propName], 15);
					maxLength -= propName.length + propValue.length + 2;
					if (0 > maxLength) {
						content += " ...";
						break;
					}
					content += " " + propName + "=" + propValue;
				}
				debugInfo = indentation(debugInfo) + "<" + serverComponentName + content + ">\n";
				indent++;
			} else null === node.serverProps ? (debugInfo = describeExpandedElement(serverComponentName, i, added(indent)), indent++) : "string" === typeof node.serverProps ? console.error("Should not have matched a non HostText fiber to a Text node. This is a bug in React.") : (debugInfo = describeElementDiff(serverComponentName, i, node.serverProps, indent), indent++);
			var propName = "";
			i = node.fiber.child;
			for (serverComponentName = 0; i && serverComponentName < node.children.length;) maxLength = node.children[serverComponentName], maxLength.fiber === i ? (propName += describeNode(maxLength, indent), serverComponentName++) : propName += describeSiblingFiber(i, indent), i = i.sibling;
			i && 0 < node.children.length && (propName += indentation(indent) + "...\n");
			i = node.serverTail;
			null === node.serverProps && indent--;
			for (node = 0; node < i.length; node++) serverComponentName = i[node], propName = "string" === typeof serverComponentName ? propName + (removed(indent) + describeTextNode(serverComponentName, 120 - 2 * indent) + "\n") : propName + describeExpandedElement(serverComponentName.type, serverComponentName.props, removed(indent));
			return skipToNode + debugInfo + propName;
		}
		function describeDiff(rootNode) {
			try {
				return "\n\n" + describeNode(rootNode, 0);
			} catch (x) {
				return "";
			}
		}
		function describeAncestors(ancestor, child, props) {
			for (var fiber = child, node = null, distanceFromLeaf = 0; fiber;) fiber === ancestor && (distanceFromLeaf = 0), node = {
				fiber,
				children: null !== node ? [node] : [],
				serverProps: fiber === child ? props : fiber === ancestor ? null : void 0,
				serverTail: [],
				distanceFromLeaf
			}, distanceFromLeaf++, fiber = fiber.return;
			return null !== node ? describeDiff(node).replaceAll(/^[+-]/gm, ">") : "";
		}
		function updatedAncestorInfoDev(oldInfo, tag) {
			var ancestorInfo = assign({}, oldInfo || emptyAncestorInfoDev), info = { tag };
			-1 !== inScopeTags.indexOf(tag) && (ancestorInfo.aTagInScope = null, ancestorInfo.buttonTagInScope = null, ancestorInfo.nobrTagInScope = null);
			-1 !== buttonScopeTags.indexOf(tag) && (ancestorInfo.pTagInButtonScope = null);
			-1 !== specialTags.indexOf(tag) && "address" !== tag && "div" !== tag && "p" !== tag && (ancestorInfo.listItemTagAutoclosing = null, ancestorInfo.dlItemTagAutoclosing = null);
			ancestorInfo.current = info;
			"form" === tag && (ancestorInfo.formTag = info);
			"a" === tag && (ancestorInfo.aTagInScope = info);
			"button" === tag && (ancestorInfo.buttonTagInScope = info);
			"nobr" === tag && (ancestorInfo.nobrTagInScope = info);
			"p" === tag && (ancestorInfo.pTagInButtonScope = info);
			"li" === tag && (ancestorInfo.listItemTagAutoclosing = info);
			if ("dd" === tag || "dt" === tag) ancestorInfo.dlItemTagAutoclosing = info;
			"#document" === tag || "html" === tag ? ancestorInfo.containerTagInScope = null : ancestorInfo.containerTagInScope || (ancestorInfo.containerTagInScope = info);
			null !== oldInfo || "#document" !== tag && "html" !== tag && "body" !== tag ? !0 === ancestorInfo.implicitRootScope && (ancestorInfo.implicitRootScope = !1) : ancestorInfo.implicitRootScope = !0;
			return ancestorInfo;
		}
		function isTagValidWithParent(tag, parentTag, implicitRootScope) {
			switch (parentTag) {
				case "select": return "hr" === tag || "option" === tag || "optgroup" === tag || "script" === tag || "template" === tag || "#text" === tag;
				case "optgroup": return "option" === tag || "#text" === tag;
				case "option": return "#text" === tag;
				case "tr": return "th" === tag || "td" === tag || "style" === tag || "script" === tag || "template" === tag;
				case "tbody":
				case "thead":
				case "tfoot": return "tr" === tag || "style" === tag || "script" === tag || "template" === tag;
				case "colgroup": return "col" === tag || "template" === tag;
				case "table": return "caption" === tag || "colgroup" === tag || "tbody" === tag || "tfoot" === tag || "thead" === tag || "style" === tag || "script" === tag || "template" === tag;
				case "head": return "base" === tag || "basefont" === tag || "bgsound" === tag || "link" === tag || "meta" === tag || "title" === tag || "noscript" === tag || "noframes" === tag || "style" === tag || "script" === tag || "template" === tag;
				case "html":
					if (implicitRootScope) break;
					return "head" === tag || "body" === tag || "frameset" === tag;
				case "frameset": return "frame" === tag;
				case "#document": if (!implicitRootScope) return "html" === tag;
			}
			switch (tag) {
				case "h1":
				case "h2":
				case "h3":
				case "h4":
				case "h5":
				case "h6": return "h1" !== parentTag && "h2" !== parentTag && "h3" !== parentTag && "h4" !== parentTag && "h5" !== parentTag && "h6" !== parentTag;
				case "rp":
				case "rt": return -1 === impliedEndTags.indexOf(parentTag);
				case "caption":
				case "col":
				case "colgroup":
				case "frameset":
				case "frame":
				case "tbody":
				case "td":
				case "tfoot":
				case "th":
				case "thead":
				case "tr": return null == parentTag;
				case "head": return implicitRootScope || null === parentTag;
				case "html": return implicitRootScope && "#document" === parentTag || null === parentTag;
				case "body": return implicitRootScope && ("#document" === parentTag || "html" === parentTag) || null === parentTag;
			}
			return !0;
		}
		function findInvalidAncestorForTag(tag, ancestorInfo) {
			switch (tag) {
				case "address":
				case "article":
				case "aside":
				case "blockquote":
				case "center":
				case "details":
				case "dialog":
				case "dir":
				case "div":
				case "dl":
				case "fieldset":
				case "figcaption":
				case "figure":
				case "footer":
				case "header":
				case "hgroup":
				case "main":
				case "menu":
				case "nav":
				case "ol":
				case "p":
				case "section":
				case "summary":
				case "ul":
				case "pre":
				case "listing":
				case "table":
				case "hr":
				case "xmp":
				case "h1":
				case "h2":
				case "h3":
				case "h4":
				case "h5":
				case "h6": return ancestorInfo.pTagInButtonScope;
				case "form": return ancestorInfo.formTag || ancestorInfo.pTagInButtonScope;
				case "li": return ancestorInfo.listItemTagAutoclosing;
				case "dd":
				case "dt": return ancestorInfo.dlItemTagAutoclosing;
				case "button": return ancestorInfo.buttonTagInScope;
				case "a": return ancestorInfo.aTagInScope;
				case "nobr": return ancestorInfo.nobrTagInScope;
			}
			return null;
		}
		function findAncestor(parent, tagName) {
			for (; parent;) {
				switch (parent.tag) {
					case 5:
					case 26:
					case 27: if (parent.type === tagName) return parent;
				}
				parent = parent.return;
			}
			return null;
		}
		function validateDOMNesting(childTag, ancestorInfo) {
			ancestorInfo = ancestorInfo || emptyAncestorInfoDev;
			var parentInfo = ancestorInfo.current;
			ancestorInfo = (parentInfo = isTagValidWithParent(childTag, parentInfo && parentInfo.tag, ancestorInfo.implicitRootScope) ? null : parentInfo) ? null : findInvalidAncestorForTag(childTag, ancestorInfo);
			ancestorInfo = parentInfo || ancestorInfo;
			if (!ancestorInfo) return !0;
			var ancestorTag = ancestorInfo.tag;
			ancestorInfo = String(!!parentInfo) + "|" + childTag + "|" + ancestorTag;
			if (didWarn[ancestorInfo]) return !1;
			didWarn[ancestorInfo] = !0;
			var ancestor = (ancestorInfo = current) ? findAncestor(ancestorInfo.return, ancestorTag) : null, ancestorDescription = null !== ancestorInfo && null !== ancestor ? describeAncestors(ancestor, ancestorInfo, null) : "", tagDisplayName = "<" + childTag + ">";
			parentInfo ? (parentInfo = "", "table" === ancestorTag && "tr" === childTag && (parentInfo += " Add a <tbody>, <thead> or <tfoot> to your code to match the DOM tree generated by the browser."), console.error("In HTML, %s cannot be a child of <%s>.%s\nThis will cause a hydration error.%s", tagDisplayName, ancestorTag, parentInfo, ancestorDescription)) : console.error("In HTML, %s cannot be a descendant of <%s>.\nThis will cause a hydration error.%s", tagDisplayName, ancestorTag, ancestorDescription);
			ancestorInfo && (childTag = ancestorInfo.return, null === ancestor || null === childTag || ancestor === childTag && childTag._debugOwner === ancestorInfo._debugOwner || runWithFiberInDEV(ancestor, function() {
				console.error("<%s> cannot contain a nested %s.\nSee this log for the ancestor stack trace.", ancestorTag, tagDisplayName);
			}));
			return !1;
		}
		function validateTextNesting(childText, parentTag, implicitRootScope) {
			if (implicitRootScope || isTagValidWithParent("#text", parentTag, !1)) return !0;
			implicitRootScope = "#text|" + parentTag;
			if (didWarn[implicitRootScope]) return !1;
			didWarn[implicitRootScope] = !0;
			var ancestor = (implicitRootScope = current) ? findAncestor(implicitRootScope, parentTag) : null;
			implicitRootScope = null !== implicitRootScope && null !== ancestor ? describeAncestors(ancestor, implicitRootScope, 6 !== implicitRootScope.tag ? { children: null } : null) : "";
			/\S/.test(childText) ? console.error("In HTML, text nodes cannot be a child of <%s>.\nThis will cause a hydration error.%s", parentTag, implicitRootScope) : console.error("In HTML, whitespace text nodes cannot be a child of <%s>. Make sure you don't have any extra whitespace between tags on each line of your source code.\nThis will cause a hydration error.%s", parentTag, implicitRootScope);
			return !1;
		}
		function setTextContent(node, text) {
			if (text) {
				var firstChild = node.firstChild;
				if (firstChild && firstChild === node.lastChild && 3 === firstChild.nodeType) {
					firstChild.nodeValue = text;
					return;
				}
			}
			node.textContent = text;
		}
		function camelize(string) {
			return string.replace(hyphenPattern, function(_, character) {
				return character.toUpperCase();
			});
		}
		function setValueForStyle(style, styleName, value) {
			var isCustomProperty = 0 === styleName.indexOf("--");
			isCustomProperty || (-1 < styleName.indexOf("-") ? warnedStyleNames.hasOwnProperty(styleName) && warnedStyleNames[styleName] || (warnedStyleNames[styleName] = !0, console.error("Unsupported style property %s. Did you mean %s?", styleName, camelize(styleName.replace(msPattern, "ms-")))) : badVendoredStyleNamePattern.test(styleName) ? warnedStyleNames.hasOwnProperty(styleName) && warnedStyleNames[styleName] || (warnedStyleNames[styleName] = !0, console.error("Unsupported vendor-prefixed style property %s. Did you mean %s?", styleName, styleName.charAt(0).toUpperCase() + styleName.slice(1))) : !badStyleValueWithSemicolonPattern.test(value) || warnedStyleValues.hasOwnProperty(value) && warnedStyleValues[value] || (warnedStyleValues[value] = !0, console.error("Style property values shouldn't contain a semicolon. Try \"%s: %s\" instead.", styleName, value.replace(badStyleValueWithSemicolonPattern, ""))), "number" === typeof value && (isNaN(value) ? warnedForNaNValue || (warnedForNaNValue = !0, console.error("`NaN` is an invalid value for the `%s` css style property.", styleName)) : isFinite(value) || warnedForInfinityValue || (warnedForInfinityValue = !0, console.error("`Infinity` is an invalid value for the `%s` css style property.", styleName))));
			null == value || "boolean" === typeof value || "" === value ? isCustomProperty ? style.setProperty(styleName, "") : "float" === styleName ? style.cssFloat = "" : style[styleName] = "" : isCustomProperty ? style.setProperty(styleName, value) : "number" !== typeof value || 0 === value || unitlessNumbers.has(styleName) ? "float" === styleName ? style.cssFloat = value : (checkCSSPropertyStringCoercion(value, styleName), style[styleName] = ("" + value).trim()) : style[styleName] = value + "px";
		}
		function setValueForStyles(node, styles, prevStyles) {
			if (null != styles && "object" !== typeof styles) throw Error("The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX.");
			styles && Object.freeze(styles);
			node = node.style;
			if (null != prevStyles) {
				if (styles) {
					var expandedUpdates = {};
					if (prevStyles) {
						for (var key in prevStyles) if (prevStyles.hasOwnProperty(key) && !styles.hasOwnProperty(key)) for (var longhands = shorthandToLonghand[key] || [key], i = 0; i < longhands.length; i++) expandedUpdates[longhands[i]] = key;
					}
					for (var _key in styles) if (styles.hasOwnProperty(_key) && (!prevStyles || prevStyles[_key] !== styles[_key])) for (key = shorthandToLonghand[_key] || [_key], longhands = 0; longhands < key.length; longhands++) expandedUpdates[key[longhands]] = _key;
					_key = {};
					for (var key$jscomp$0 in styles) for (key = shorthandToLonghand[key$jscomp$0] || [key$jscomp$0], longhands = 0; longhands < key.length; longhands++) _key[key[longhands]] = key$jscomp$0;
					key$jscomp$0 = {};
					for (var _key2 in expandedUpdates) if (key = expandedUpdates[_key2], (longhands = _key[_key2]) && key !== longhands && (i = key + "," + longhands, !key$jscomp$0[i])) {
						key$jscomp$0[i] = !0;
						i = console;
						var value = styles[key];
						i.error.call(i, "%s a style property during rerender (%s) when a conflicting property is set (%s) can lead to styling bugs. To avoid this, don't mix shorthand and non-shorthand properties for the same value; instead, replace the shorthand with separate values.", null == value || "boolean" === typeof value || "" === value ? "Removing" : "Updating", key, longhands);
					}
				}
				for (var styleName in prevStyles) !prevStyles.hasOwnProperty(styleName) || null != styles && styles.hasOwnProperty(styleName) || (0 === styleName.indexOf("--") ? node.setProperty(styleName, "") : "float" === styleName ? node.cssFloat = "" : node[styleName] = "");
				for (var _styleName in styles) _key2 = styles[_styleName], styles.hasOwnProperty(_styleName) && prevStyles[_styleName] !== _key2 && setValueForStyle(node, _styleName, _key2);
			} else for (expandedUpdates in styles) styles.hasOwnProperty(expandedUpdates) && setValueForStyle(node, expandedUpdates, styles[expandedUpdates]);
		}
		function isCustomElement(tagName) {
			if (-1 === tagName.indexOf("-")) return !1;
			switch (tagName) {
				case "annotation-xml":
				case "color-profile":
				case "font-face":
				case "font-face-src":
				case "font-face-uri":
				case "font-face-format":
				case "font-face-name":
				case "missing-glyph": return !1;
				default: return !0;
			}
		}
		function getAttributeAlias(name) {
			return aliases.get(name) || name;
		}
		function validateProperty$1(tagName, name) {
			if (hasOwnProperty.call(warnedProperties$1, name) && warnedProperties$1[name]) return !0;
			if (rARIACamel$1.test(name)) {
				tagName = "aria-" + name.slice(4).toLowerCase();
				tagName = ariaProperties.hasOwnProperty(tagName) ? tagName : null;
				if (null == tagName) return console.error("Invalid ARIA attribute `%s`. ARIA attributes follow the pattern aria-* and must be lowercase.", name), warnedProperties$1[name] = !0;
				if (name !== tagName) return console.error("Invalid ARIA attribute `%s`. Did you mean `%s`?", name, tagName), warnedProperties$1[name] = !0;
			}
			if (rARIA$1.test(name)) {
				tagName = name.toLowerCase();
				tagName = ariaProperties.hasOwnProperty(tagName) ? tagName : null;
				if (null == tagName) return warnedProperties$1[name] = !0, !1;
				name !== tagName && (console.error("Unknown ARIA attribute `%s`. Did you mean `%s`?", name, tagName), warnedProperties$1[name] = !0);
			}
			return !0;
		}
		function validateProperties$2(type, props) {
			var invalidProps = [], key;
			for (key in props) validateProperty$1(type, key) || invalidProps.push(key);
			props = invalidProps.map(function(prop) {
				return "`" + prop + "`";
			}).join(", ");
			1 === invalidProps.length ? console.error("Invalid aria prop %s on <%s> tag. For details, see https://react.dev/link/invalid-aria-props", props, type) : 1 < invalidProps.length && console.error("Invalid aria props %s on <%s> tag. For details, see https://react.dev/link/invalid-aria-props", props, type);
		}
		function validateProperty(tagName, name, value, eventRegistry) {
			if (hasOwnProperty.call(warnedProperties, name) && warnedProperties[name]) return !0;
			var lowerCasedName = name.toLowerCase();
			if ("onfocusin" === lowerCasedName || "onfocusout" === lowerCasedName) return console.error("React uses onFocus and onBlur instead of onFocusIn and onFocusOut. All React events are normalized to bubble, so onFocusIn and onFocusOut are not needed/supported by React."), warnedProperties[name] = !0;
			if ("function" === typeof value && ("form" === tagName && "action" === name || "input" === tagName && "formAction" === name || "button" === tagName && "formAction" === name)) return !0;
			if (null != eventRegistry) {
				tagName = eventRegistry.possibleRegistrationNames;
				if (eventRegistry.registrationNameDependencies.hasOwnProperty(name)) return !0;
				eventRegistry = tagName.hasOwnProperty(lowerCasedName) ? tagName[lowerCasedName] : null;
				if (null != eventRegistry) return console.error("Invalid event handler property `%s`. Did you mean `%s`?", name, eventRegistry), warnedProperties[name] = !0;
				if (EVENT_NAME_REGEX.test(name)) return console.error("Unknown event handler property `%s`. It will be ignored.", name), warnedProperties[name] = !0;
			} else if (EVENT_NAME_REGEX.test(name)) return INVALID_EVENT_NAME_REGEX.test(name) && console.error("Invalid event handler property `%s`. React events use the camelCase naming convention, for example `onClick`.", name), warnedProperties[name] = !0;
			if (rARIA.test(name) || rARIACamel.test(name)) return !0;
			if ("innerhtml" === lowerCasedName) return console.error("Directly setting property `innerHTML` is not permitted. For more information, lookup documentation on `dangerouslySetInnerHTML`."), warnedProperties[name] = !0;
			if ("aria" === lowerCasedName) return console.error("The `aria` attribute is reserved for future use in React. Pass individual `aria-` attributes instead."), warnedProperties[name] = !0;
			if ("is" === lowerCasedName && null !== value && void 0 !== value && "string" !== typeof value) return console.error("Received a `%s` for a string attribute `is`. If this is expected, cast the value to a string.", typeof value), warnedProperties[name] = !0;
			if ("number" === typeof value && isNaN(value)) return console.error("Received NaN for the `%s` attribute. If this is expected, cast the value to a string.", name), warnedProperties[name] = !0;
			if (possibleStandardNames.hasOwnProperty(lowerCasedName)) {
				if (lowerCasedName = possibleStandardNames[lowerCasedName], lowerCasedName !== name) return console.error("Invalid DOM property `%s`. Did you mean `%s`?", name, lowerCasedName), warnedProperties[name] = !0;
			} else if (name !== lowerCasedName) return console.error("React does not recognize the `%s` prop on a DOM element. If you intentionally want it to appear in the DOM as a custom attribute, spell it as lowercase `%s` instead. If you accidentally passed it from a parent component, remove it from the DOM element.", name, lowerCasedName), warnedProperties[name] = !0;
			switch (name) {
				case "dangerouslySetInnerHTML":
				case "children":
				case "style":
				case "suppressContentEditableWarning":
				case "suppressHydrationWarning":
				case "defaultValue":
				case "defaultChecked":
				case "innerHTML":
				case "ref": return !0;
				case "innerText":
				case "textContent": return !0;
			}
			switch (typeof value) {
				case "boolean": switch (name) {
					case "autoFocus":
					case "checked":
					case "multiple":
					case "muted":
					case "selected":
					case "contentEditable":
					case "spellCheck":
					case "draggable":
					case "value":
					case "autoReverse":
					case "externalResourcesRequired":
					case "focusable":
					case "preserveAlpha":
					case "allowFullScreen":
					case "async":
					case "autoPlay":
					case "controls":
					case "default":
					case "defer":
					case "disabled":
					case "disablePictureInPicture":
					case "disableRemotePlayback":
					case "formNoValidate":
					case "hidden":
					case "loop":
					case "noModule":
					case "noValidate":
					case "open":
					case "playsInline":
					case "readOnly":
					case "required":
					case "reversed":
					case "scoped":
					case "seamless":
					case "itemScope":
					case "capture":
					case "download":
					case "inert": return !0;
					default:
						lowerCasedName = name.toLowerCase().slice(0, 5);
						if ("data-" === lowerCasedName || "aria-" === lowerCasedName) return !0;
						value ? console.error("Received `%s` for a non-boolean attribute `%s`.\n\nIf you want to write it to the DOM, pass a string instead: %s=\"%s\" or %s={value.toString()}.", value, name, name, value, name) : console.error("Received `%s` for a non-boolean attribute `%s`.\n\nIf you want to write it to the DOM, pass a string instead: %s=\"%s\" or %s={value.toString()}.\n\nIf you used to conditionally omit it with %s={condition && value}, pass %s={condition ? value : undefined} instead.", value, name, name, value, name, name, name);
						return warnedProperties[name] = !0;
				}
				case "function":
				case "symbol": return warnedProperties[name] = !0, !1;
				case "string": if ("false" === value || "true" === value) {
					switch (name) {
						case "checked":
						case "selected":
						case "multiple":
						case "muted":
						case "allowFullScreen":
						case "async":
						case "autoPlay":
						case "controls":
						case "default":
						case "defer":
						case "disabled":
						case "disablePictureInPicture":
						case "disableRemotePlayback":
						case "formNoValidate":
						case "hidden":
						case "loop":
						case "noModule":
						case "noValidate":
						case "open":
						case "playsInline":
						case "readOnly":
						case "required":
						case "reversed":
						case "scoped":
						case "seamless":
						case "itemScope":
						case "inert": break;
						default: return !0;
					}
					console.error("Received the string `%s` for the boolean attribute `%s`. %s Did you mean %s={%s}?", value, name, "false" === value ? "The browser will interpret it as a truthy value." : "Although this works, it will not work as expected if you pass the string \"false\".", name, value);
					warnedProperties[name] = !0;
				}
			}
			return !0;
		}
		function warnUnknownProperties(type, props, eventRegistry) {
			var unknownProps = [], key;
			for (key in props) validateProperty(type, key, props[key], eventRegistry) || unknownProps.push(key);
			props = unknownProps.map(function(prop) {
				return "`" + prop + "`";
			}).join(", ");
			1 === unknownProps.length ? console.error("Invalid value for prop %s on <%s> tag. Either remove it from the element, or pass a string or number value to keep it in the DOM. For details, see https://react.dev/link/attribute-behavior ", props, type) : 1 < unknownProps.length && console.error("Invalid values for props %s on <%s> tag. Either remove them from the element, or pass a string or number value to keep them in the DOM. For details, see https://react.dev/link/attribute-behavior ", props, type);
		}
		function sanitizeURL(url) {
			return isJavaScriptProtocol.test("" + url) ? "javascript:throw new Error('React has blocked a javascript: URL as a security precaution.')" : url;
		}
		function noop$1() {}
		function getEventTarget(nativeEvent) {
			nativeEvent = nativeEvent.target || nativeEvent.srcElement || window;
			nativeEvent.correspondingUseElement && (nativeEvent = nativeEvent.correspondingUseElement);
			return 3 === nativeEvent.nodeType ? nativeEvent.parentNode : nativeEvent;
		}
		function restoreStateOfTarget(target) {
			var internalInstance = getInstanceFromNode(target);
			if (internalInstance && (target = internalInstance.stateNode)) {
				var props = target[internalPropsKey] || null;
				a: switch (target = internalInstance.stateNode, internalInstance.type) {
					case "input":
						updateInput(target, props.value, props.defaultValue, props.defaultValue, props.checked, props.defaultChecked, props.type, props.name);
						internalInstance = props.name;
						if ("radio" === props.type && null != internalInstance) {
							for (props = target; props.parentNode;) props = props.parentNode;
							checkAttributeStringCoercion(internalInstance, "name");
							props = props.querySelectorAll("input[name=\"" + escapeSelectorAttributeValueInsideDoubleQuotes("" + internalInstance) + "\"][type=\"radio\"]");
							for (internalInstance = 0; internalInstance < props.length; internalInstance++) {
								var otherNode = props[internalInstance];
								if (otherNode !== target && otherNode.form === target.form) {
									var otherProps = otherNode[internalPropsKey] || null;
									if (!otherProps) throw Error("ReactDOMInput: Mixing React and non-React radio inputs with the same `name` is not supported.");
									updateInput(otherNode, otherProps.value, otherProps.defaultValue, otherProps.defaultValue, otherProps.checked, otherProps.defaultChecked, otherProps.type, otherProps.name);
								}
							}
							for (internalInstance = 0; internalInstance < props.length; internalInstance++) otherNode = props[internalInstance], otherNode.form === target.form && updateValueIfChanged(otherNode);
						}
						break a;
					case "textarea":
						updateTextarea(target, props.value, props.defaultValue);
						break a;
					case "select": internalInstance = props.value, null != internalInstance && updateOptions(target, !!props.multiple, internalInstance, !1);
				}
			}
		}
		function batchedUpdates$1(fn, a, b) {
			if (isInsideEventHandler) return fn(a, b);
			isInsideEventHandler = !0;
			try {
				return fn(a);
			} finally {
				if (isInsideEventHandler = !1, null !== restoreTarget || null !== restoreQueue) {
					if (flushSyncWork$1(), restoreTarget && (a = restoreTarget, fn = restoreQueue, restoreQueue = restoreTarget = null, restoreStateOfTarget(a), fn)) for (a = 0; a < fn.length; a++) restoreStateOfTarget(fn[a]);
				}
			}
		}
		function getListener(inst, registrationName) {
			var stateNode = inst.stateNode;
			if (null === stateNode) return null;
			var props = stateNode[internalPropsKey] || null;
			if (null === props) return null;
			stateNode = props[registrationName];
			a: switch (registrationName) {
				case "onClick":
				case "onClickCapture":
				case "onDoubleClick":
				case "onDoubleClickCapture":
				case "onMouseDown":
				case "onMouseDownCapture":
				case "onMouseMove":
				case "onMouseMoveCapture":
				case "onMouseUp":
				case "onMouseUpCapture":
				case "onMouseEnter":
					(props = !props.disabled) || (inst = inst.type, props = !("button" === inst || "input" === inst || "select" === inst || "textarea" === inst));
					inst = !props;
					break a;
				default: inst = !1;
			}
			if (inst) return null;
			if (stateNode && "function" !== typeof stateNode) throw Error("Expected `" + registrationName + "` listener to be a function, instead got a value of `" + typeof stateNode + "` type.");
			return stateNode;
		}
		function getData() {
			if (fallbackText) return fallbackText;
			var start, startValue = startText, startLength = startValue.length, end, endValue = "value" in root ? root.value : root.textContent, endLength = endValue.length;
			for (start = 0; start < startLength && startValue[start] === endValue[start]; start++);
			var minEnd = startLength - start;
			for (end = 1; end <= minEnd && startValue[startLength - end] === endValue[endLength - end]; end++);
			return fallbackText = endValue.slice(start, 1 < end ? 1 - end : void 0);
		}
		function getEventCharCode(nativeEvent) {
			var keyCode = nativeEvent.keyCode;
			"charCode" in nativeEvent ? (nativeEvent = nativeEvent.charCode, 0 === nativeEvent && 13 === keyCode && (nativeEvent = 13)) : nativeEvent = keyCode;
			10 === nativeEvent && (nativeEvent = 13);
			return 32 <= nativeEvent || 13 === nativeEvent ? nativeEvent : 0;
		}
		function functionThatReturnsTrue() {
			return !0;
		}
		function functionThatReturnsFalse() {
			return !1;
		}
		function createSyntheticEvent(Interface) {
			function SyntheticBaseEvent(reactName, reactEventType, targetInst, nativeEvent, nativeEventTarget) {
				this._reactName = reactName;
				this._targetInst = targetInst;
				this.type = reactEventType;
				this.nativeEvent = nativeEvent;
				this.target = nativeEventTarget;
				this.currentTarget = null;
				for (var propName in Interface) Interface.hasOwnProperty(propName) && (reactName = Interface[propName], this[propName] = reactName ? reactName(nativeEvent) : nativeEvent[propName]);
				this.isDefaultPrevented = (null != nativeEvent.defaultPrevented ? nativeEvent.defaultPrevented : !1 === nativeEvent.returnValue) ? functionThatReturnsTrue : functionThatReturnsFalse;
				this.isPropagationStopped = functionThatReturnsFalse;
				return this;
			}
			assign(SyntheticBaseEvent.prototype, {
				preventDefault: function() {
					this.defaultPrevented = !0;
					var event = this.nativeEvent;
					event && (event.preventDefault ? event.preventDefault() : "unknown" !== typeof event.returnValue && (event.returnValue = !1), this.isDefaultPrevented = functionThatReturnsTrue);
				},
				stopPropagation: function() {
					var event = this.nativeEvent;
					event && (event.stopPropagation ? event.stopPropagation() : "unknown" !== typeof event.cancelBubble && (event.cancelBubble = !0), this.isPropagationStopped = functionThatReturnsTrue);
				},
				persist: function() {},
				isPersistent: functionThatReturnsTrue
			});
			return SyntheticBaseEvent;
		}
		function modifierStateGetter(keyArg) {
			var nativeEvent = this.nativeEvent;
			return nativeEvent.getModifierState ? nativeEvent.getModifierState(keyArg) : (keyArg = modifierKeyToProp[keyArg]) ? !!nativeEvent[keyArg] : !1;
		}
		function getEventModifierState() {
			return modifierStateGetter;
		}
		function isFallbackCompositionEnd(domEventName, nativeEvent) {
			switch (domEventName) {
				case "keyup": return -1 !== END_KEYCODES.indexOf(nativeEvent.keyCode);
				case "keydown": return nativeEvent.keyCode !== START_KEYCODE;
				case "keypress":
				case "mousedown":
				case "focusout": return !0;
				default: return !1;
			}
		}
		function getDataFromCustomEvent(nativeEvent) {
			nativeEvent = nativeEvent.detail;
			return "object" === typeof nativeEvent && "data" in nativeEvent ? nativeEvent.data : null;
		}
		function getNativeBeforeInputChars(domEventName, nativeEvent) {
			switch (domEventName) {
				case "compositionend": return getDataFromCustomEvent(nativeEvent);
				case "keypress":
					if (nativeEvent.which !== SPACEBAR_CODE) return null;
					hasSpaceKeypress = !0;
					return SPACEBAR_CHAR;
				case "textInput": return domEventName = nativeEvent.data, domEventName === SPACEBAR_CHAR && hasSpaceKeypress ? null : domEventName;
				default: return null;
			}
		}
		function getFallbackBeforeInputChars(domEventName, nativeEvent) {
			if (isComposing) return "compositionend" === domEventName || !canUseCompositionEvent && isFallbackCompositionEnd(domEventName, nativeEvent) ? (domEventName = getData(), fallbackText = startText = root = null, isComposing = !1, domEventName) : null;
			switch (domEventName) {
				case "paste": return null;
				case "keypress":
					if (!(nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) || nativeEvent.ctrlKey && nativeEvent.altKey) {
						if (nativeEvent.char && 1 < nativeEvent.char.length) return nativeEvent.char;
						if (nativeEvent.which) return String.fromCharCode(nativeEvent.which);
					}
					return null;
				case "compositionend": return useFallbackCompositionData && "ko" !== nativeEvent.locale ? null : nativeEvent.data;
				default: return null;
			}
		}
		function isTextInputElement(elem) {
			var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
			return "input" === nodeName ? !!supportedInputTypes[elem.type] : "textarea" === nodeName ? !0 : !1;
		}
		function isEventSupported(eventNameSuffix) {
			if (!canUseDOM) return !1;
			eventNameSuffix = "on" + eventNameSuffix;
			var isSupported = eventNameSuffix in document;
			isSupported || (isSupported = document.createElement("div"), isSupported.setAttribute(eventNameSuffix, "return;"), isSupported = "function" === typeof isSupported[eventNameSuffix]);
			return isSupported;
		}
		function createAndAccumulateChangeEvent(dispatchQueue, inst, nativeEvent, target) {
			restoreTarget ? restoreQueue ? restoreQueue.push(target) : restoreQueue = [target] : restoreTarget = target;
			inst = accumulateTwoPhaseListeners(inst, "onChange");
			0 < inst.length && (nativeEvent = new SyntheticEvent("onChange", "change", null, nativeEvent, target), dispatchQueue.push({
				event: nativeEvent,
				listeners: inst
			}));
		}
		function runEventInBatch(dispatchQueue) {
			processDispatchQueue(dispatchQueue, 0);
		}
		function getInstIfValueChanged(targetInst) {
			if (updateValueIfChanged(getNodeFromInstance(targetInst))) return targetInst;
		}
		function getTargetInstForChangeEvent(domEventName, targetInst) {
			if ("change" === domEventName) return targetInst;
		}
		function stopWatchingForValueChange() {
			activeElement$1 && (activeElement$1.detachEvent("onpropertychange", handlePropertyChange), activeElementInst$1 = activeElement$1 = null);
		}
		function handlePropertyChange(nativeEvent) {
			if ("value" === nativeEvent.propertyName && getInstIfValueChanged(activeElementInst$1)) {
				var dispatchQueue = [];
				createAndAccumulateChangeEvent(dispatchQueue, activeElementInst$1, nativeEvent, getEventTarget(nativeEvent));
				batchedUpdates$1(runEventInBatch, dispatchQueue);
			}
		}
		function handleEventsForInputEventPolyfill(domEventName, target, targetInst) {
			"focusin" === domEventName ? (stopWatchingForValueChange(), activeElement$1 = target, activeElementInst$1 = targetInst, activeElement$1.attachEvent("onpropertychange", handlePropertyChange)) : "focusout" === domEventName && stopWatchingForValueChange();
		}
		function getTargetInstForInputEventPolyfill(domEventName) {
			if ("selectionchange" === domEventName || "keyup" === domEventName || "keydown" === domEventName) return getInstIfValueChanged(activeElementInst$1);
		}
		function getTargetInstForClickEvent(domEventName, targetInst) {
			if ("click" === domEventName) return getInstIfValueChanged(targetInst);
		}
		function getTargetInstForInputOrChangeEvent(domEventName, targetInst) {
			if ("input" === domEventName || "change" === domEventName) return getInstIfValueChanged(targetInst);
		}
		function is(x, y) {
			return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
		}
		function shallowEqual(objA, objB) {
			if (objectIs(objA, objB)) return !0;
			if ("object" !== typeof objA || null === objA || "object" !== typeof objB || null === objB) return !1;
			var keysA = Object.keys(objA), keysB = Object.keys(objB);
			if (keysA.length !== keysB.length) return !1;
			for (keysB = 0; keysB < keysA.length; keysB++) {
				var currentKey = keysA[keysB];
				if (!hasOwnProperty.call(objB, currentKey) || !objectIs(objA[currentKey], objB[currentKey])) return !1;
			}
			return !0;
		}
		function getLeafNode(node) {
			for (; node && node.firstChild;) node = node.firstChild;
			return node;
		}
		function getNodeForCharacterOffset(root, offset) {
			var node = getLeafNode(root);
			root = 0;
			for (var nodeEnd; node;) {
				if (3 === node.nodeType) {
					nodeEnd = root + node.textContent.length;
					if (root <= offset && nodeEnd >= offset) return {
						node,
						offset: offset - root
					};
					root = nodeEnd;
				}
				a: {
					for (; node;) {
						if (node.nextSibling) {
							node = node.nextSibling;
							break a;
						}
						node = node.parentNode;
					}
					node = void 0;
				}
				node = getLeafNode(node);
			}
		}
		function containsNode(outerNode, innerNode) {
			return outerNode && innerNode ? outerNode === innerNode ? !0 : outerNode && 3 === outerNode.nodeType ? !1 : innerNode && 3 === innerNode.nodeType ? containsNode(outerNode, innerNode.parentNode) : "contains" in outerNode ? outerNode.contains(innerNode) : outerNode.compareDocumentPosition ? !!(outerNode.compareDocumentPosition(innerNode) & 16) : !1 : !1;
		}
		function getActiveElementDeep(containerInfo) {
			containerInfo = null != containerInfo && null != containerInfo.ownerDocument && null != containerInfo.ownerDocument.defaultView ? containerInfo.ownerDocument.defaultView : window;
			for (var element = getActiveElement(containerInfo.document); element instanceof containerInfo.HTMLIFrameElement;) {
				try {
					var JSCompiler_inline_result = "string" === typeof element.contentWindow.location.href;
				} catch (err) {
					JSCompiler_inline_result = !1;
				}
				if (JSCompiler_inline_result) containerInfo = element.contentWindow;
				else break;
				element = getActiveElement(containerInfo.document);
			}
			return element;
		}
		function hasSelectionCapabilities(elem) {
			var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
			return nodeName && ("input" === nodeName && ("text" === elem.type || "search" === elem.type || "tel" === elem.type || "url" === elem.type || "password" === elem.type) || "textarea" === nodeName || "true" === elem.contentEditable);
		}
		function constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget) {
			var doc = nativeEventTarget.window === nativeEventTarget ? nativeEventTarget.document : 9 === nativeEventTarget.nodeType ? nativeEventTarget : nativeEventTarget.ownerDocument;
			mouseDown || null == activeElement || activeElement !== getActiveElement(doc) || (doc = activeElement, "selectionStart" in doc && hasSelectionCapabilities(doc) ? doc = {
				start: doc.selectionStart,
				end: doc.selectionEnd
			} : (doc = (doc.ownerDocument && doc.ownerDocument.defaultView || window).getSelection(), doc = {
				anchorNode: doc.anchorNode,
				anchorOffset: doc.anchorOffset,
				focusNode: doc.focusNode,
				focusOffset: doc.focusOffset
			}), lastSelection && shallowEqual(lastSelection, doc) || (lastSelection = doc, doc = accumulateTwoPhaseListeners(activeElementInst, "onSelect"), 0 < doc.length && (nativeEvent = new SyntheticEvent("onSelect", "select", null, nativeEvent, nativeEventTarget), dispatchQueue.push({
				event: nativeEvent,
				listeners: doc
			}), nativeEvent.target = activeElement)));
		}
		function makePrefixMap(styleProp, eventName) {
			var prefixes = {};
			prefixes[styleProp.toLowerCase()] = eventName.toLowerCase();
			prefixes["Webkit" + styleProp] = "webkit" + eventName;
			prefixes["Moz" + styleProp] = "moz" + eventName;
			return prefixes;
		}
		function getVendorPrefixedEventName(eventName) {
			if (prefixedEventNames[eventName]) return prefixedEventNames[eventName];
			if (!vendorPrefixes[eventName]) return eventName;
			var prefixMap = vendorPrefixes[eventName], styleProp;
			for (styleProp in prefixMap) if (prefixMap.hasOwnProperty(styleProp) && styleProp in style) return prefixedEventNames[eventName] = prefixMap[styleProp];
			return eventName;
		}
		function registerSimpleEvent(domEventName, reactName) {
			topLevelEventsToReactNames.set(domEventName, reactName);
			registerTwoPhaseEvent(reactName, [domEventName]);
		}
		function getArrayKind(array) {
			for (var kind = EMPTY_ARRAY, i = 0; i < array.length; i++) {
				var value = array[i];
				if ("object" === typeof value && null !== value) if (isArrayImpl(value) && 2 === value.length && "string" === typeof value[0]) {
					if (kind !== EMPTY_ARRAY && kind !== ENTRIES_ARRAY) return COMPLEX_ARRAY;
					kind = ENTRIES_ARRAY;
				} else return COMPLEX_ARRAY;
				else {
					if ("function" === typeof value || "string" === typeof value && 50 < value.length || kind !== EMPTY_ARRAY && kind !== PRIMITIVE_ARRAY) return COMPLEX_ARRAY;
					kind = PRIMITIVE_ARRAY;
				}
			}
			return kind;
		}
		function addObjectToProperties(object, properties, indent, prefix) {
			for (var key in object) hasOwnProperty.call(object, key) && "_" !== key[0] && addValueToProperties(key, object[key], properties, indent, prefix);
		}
		function addValueToProperties(propertyName, value, properties, indent, prefix) {
			switch (typeof value) {
				case "object": if (null === value) {
					value = "null";
					break;
				} else {
					if (value.$$typeof === REACT_ELEMENT_TYPE) {
						var typeName = getComponentNameFromType(value.type) || "…", key = value.key;
						value = value.props;
						var propsKeys = Object.keys(value), propsLength = propsKeys.length;
						if (null == key && 0 === propsLength) {
							value = "<" + typeName + " />";
							break;
						}
						if (3 > indent || 1 === propsLength && "children" === propsKeys[0] && null == key) {
							value = "<" + typeName + " … />";
							break;
						}
						properties.push([prefix + "\xA0\xA0".repeat(indent) + propertyName, "<" + typeName]);
						null !== key && addValueToProperties("key", key, properties, indent + 1, prefix);
						propertyName = !1;
						for (var propKey in value) "children" === propKey ? null != value.children && (!isArrayImpl(value.children) || 0 < value.children.length) && (propertyName = !0) : hasOwnProperty.call(value, propKey) && "_" !== propKey[0] && addValueToProperties(propKey, value[propKey], properties, indent + 1, prefix);
						properties.push(["", propertyName ? ">…</" + typeName + ">" : "/>"]);
						return;
					}
					typeName = Object.prototype.toString.call(value);
					typeName = typeName.slice(8, typeName.length - 1);
					if ("Array" === typeName) {
						if (propKey = getArrayKind(value), propKey === PRIMITIVE_ARRAY || propKey === EMPTY_ARRAY) {
							value = JSON.stringify(value);
							break;
						} else if (propKey === ENTRIES_ARRAY) {
							properties.push([prefix + "\xA0\xA0".repeat(indent) + propertyName, ""]);
							for (propertyName = 0; propertyName < value.length; propertyName++) typeName = value[propertyName], addValueToProperties(typeName[0], typeName[1], properties, indent + 1, prefix);
							return;
						}
					}
					if ("Promise" === typeName) {
						if ("fulfilled" === value.status) {
							if (typeName = properties.length, addValueToProperties(propertyName, value.value, properties, indent, prefix), properties.length > typeName) {
								properties = properties[typeName];
								properties[1] = "Promise<" + (properties[1] || "Object") + ">";
								return;
							}
						} else if ("rejected" === value.status && (typeName = properties.length, addValueToProperties(propertyName, value.reason, properties, indent, prefix), properties.length > typeName)) {
							properties = properties[typeName];
							properties[1] = "Rejected Promise<" + properties[1] + ">";
							return;
						}
						properties.push(["\xA0\xA0".repeat(indent) + propertyName, "Promise"]);
						return;
					}
					"Object" === typeName && (propKey = Object.getPrototypeOf(value)) && "function" === typeof propKey.constructor && (typeName = propKey.constructor.name);
					properties.push([prefix + "\xA0\xA0".repeat(indent) + propertyName, "Object" === typeName ? 3 > indent ? "" : "…" : typeName]);
					3 > indent && addObjectToProperties(value, properties, indent + 1, prefix);
					return;
				}
				case "function":
					value = "" === value.name ? "() => {}" : value.name + "() {}";
					break;
				case "string":
					value = value === OMITTED_PROP_ERROR ? "…" : JSON.stringify(value);
					break;
				case "undefined":
					value = "undefined";
					break;
				case "boolean":
					value = value ? "true" : "false";
					break;
				default: value = String(value);
			}
			properties.push([prefix + "\xA0\xA0".repeat(indent) + propertyName, value]);
		}
		function addObjectDiffToProperties(prev, next, properties, indent) {
			var isDeeplyEqual = !0;
			for (key in prev) key in next || (properties.push([REMOVED + "\xA0\xA0".repeat(indent) + key, "…"]), isDeeplyEqual = !1);
			for (var _key in next) if (_key in prev) {
				var key = prev[_key];
				var nextValue = next[_key];
				if (key !== nextValue) {
					if (0 === indent && "children" === _key) isDeeplyEqual = "\xA0\xA0".repeat(indent) + _key, properties.push([REMOVED + isDeeplyEqual, "…"], [ADDED + isDeeplyEqual, "…"]);
					else {
						if (!(3 <= indent)) {
							if ("object" === typeof key && "object" === typeof nextValue && null !== key && null !== nextValue && key.$$typeof === nextValue.$$typeof) if (nextValue.$$typeof === REACT_ELEMENT_TYPE) {
								if (key.type === nextValue.type && key.key === nextValue.key) {
									key = getComponentNameFromType(nextValue.type) || "…";
									isDeeplyEqual = "\xA0\xA0".repeat(indent) + _key;
									key = "<" + key + " … />";
									properties.push([REMOVED + isDeeplyEqual, key], [ADDED + isDeeplyEqual, key]);
									isDeeplyEqual = !1;
									continue;
								}
							} else {
								var prevKind = Object.prototype.toString.call(key), nextKind = Object.prototype.toString.call(nextValue);
								if (prevKind === nextKind && ("[object Object]" === nextKind || "[object Array]" === nextKind)) {
									prevKind = [UNCHANGED + "\xA0\xA0".repeat(indent) + _key, "[object Array]" === nextKind ? "Array" : ""];
									properties.push(prevKind);
									nextKind = properties.length;
									addObjectDiffToProperties(key, nextValue, properties, indent + 1) ? nextKind === properties.length && (prevKind[1] = "Referentially unequal but deeply equal objects. Consider memoization.") : isDeeplyEqual = !1;
									continue;
								}
							}
							else if ("function" === typeof key && "function" === typeof nextValue && key.name === nextValue.name && key.length === nextValue.length && (prevKind = Function.prototype.toString.call(key), nextKind = Function.prototype.toString.call(nextValue), prevKind === nextKind)) {
								key = "" === nextValue.name ? "() => {}" : nextValue.name + "() {}";
								properties.push([UNCHANGED + "\xA0\xA0".repeat(indent) + _key, key + " Referentially unequal function closure. Consider memoization."]);
								continue;
							}
						}
						addValueToProperties(_key, key, properties, indent, REMOVED);
						addValueToProperties(_key, nextValue, properties, indent, ADDED);
					}
					isDeeplyEqual = !1;
				}
			} else properties.push([ADDED + "\xA0\xA0".repeat(indent) + _key, "…"]), isDeeplyEqual = !1;
			return isDeeplyEqual;
		}
		function setCurrentTrackFromLanes(lanes) {
			currentTrack = lanes & 63 ? "Blocking" : lanes & 64 ? "Gesture" : lanes & 4194176 ? "Transition" : lanes & 62914560 ? "Suspense" : lanes & 2080374784 ? "Idle" : "Other";
		}
		function logComponentTrigger(fiber, startTime, endTime, trigger) {
			supportsUserTiming && (reusableComponentOptions.start = startTime, reusableComponentOptions.end = endTime, reusableComponentDevToolDetails.color = "warning", reusableComponentDevToolDetails.tooltipText = trigger, reusableComponentDevToolDetails.properties = null, (fiber = fiber._debugTask) ? fiber.run(performance.measure.bind(performance, trigger, reusableComponentOptions)) : performance.measure(trigger, reusableComponentOptions));
		}
		function logComponentReappeared(fiber, startTime, endTime) {
			logComponentTrigger(fiber, startTime, endTime, "Reconnect");
		}
		function logComponentRender(fiber, startTime, endTime, wasHydrated, committedLanes) {
			var name = getComponentNameFromFiber(fiber);
			if (null !== name && supportsUserTiming) {
				var alternate = fiber.alternate, selfTime = fiber.actualDuration;
				if (null === alternate || alternate.child !== fiber.child) for (var child = fiber.child; null !== child; child = child.sibling) selfTime -= child.actualDuration;
				wasHydrated = .5 > selfTime ? wasHydrated ? "tertiary-light" : "primary-light" : 10 > selfTime ? wasHydrated ? "tertiary" : "primary" : 100 > selfTime ? wasHydrated ? "tertiary-dark" : "primary-dark" : "error";
				var props = fiber.memoizedProps;
				selfTime = fiber._debugTask;
				null !== props && null !== alternate && alternate.memoizedProps !== props ? (child = [resuableChangedPropsEntry], props = addObjectDiffToProperties(alternate.memoizedProps, props, child, 0), 1 < child.length && (props && !alreadyWarnedForDeepEquality && 0 === (alternate.lanes & committedLanes) && 100 < fiber.actualDuration ? (alreadyWarnedForDeepEquality = !0, child[0] = reusableDeeplyEqualPropsEntry, reusableComponentDevToolDetails.color = "warning", reusableComponentDevToolDetails.tooltipText = DEEP_EQUALITY_WARNING) : (reusableComponentDevToolDetails.color = wasHydrated, reusableComponentDevToolDetails.tooltipText = name), reusableComponentDevToolDetails.properties = child, reusableComponentOptions.start = startTime, reusableComponentOptions.end = endTime, null != selfTime ? selfTime.run(performance.measure.bind(performance, "​" + name, reusableComponentOptions)) : performance.measure("​" + name, reusableComponentOptions))) : null != selfTime ? selfTime.run(console.timeStamp.bind(console, name, startTime, endTime, COMPONENTS_TRACK, void 0, wasHydrated)) : console.timeStamp(name, startTime, endTime, COMPONENTS_TRACK, void 0, wasHydrated);
			}
		}
		function logComponentErrored(fiber, startTime, endTime, errors) {
			if (supportsUserTiming) {
				var name = getComponentNameFromFiber(fiber);
				if (null !== name) {
					for (var debugTask = null, properties = [], i = 0; i < errors.length; i++) {
						var capturedValue = errors[i];
						null == debugTask && null !== capturedValue.source && (debugTask = capturedValue.source._debugTask);
						capturedValue = capturedValue.value;
						properties.push(["Error", "object" === typeof capturedValue && null !== capturedValue && "string" === typeof capturedValue.message ? String(capturedValue.message) : String(capturedValue)]);
					}
					null !== fiber.key && addValueToProperties("key", fiber.key, properties, 0, "");
					null !== fiber.memoizedProps && addObjectToProperties(fiber.memoizedProps, properties, 0, "");
					debugTask ??= fiber._debugTask;
					fiber = {
						start: startTime,
						end: endTime,
						detail: { devtools: {
							color: "error",
							track: COMPONENTS_TRACK,
							tooltipText: 13 === fiber.tag ? "Hydration failed" : "Error boundary caught an error",
							properties
						} }
					};
					debugTask ? debugTask.run(performance.measure.bind(performance, "​" + name, fiber)) : performance.measure("​" + name, fiber);
				}
			}
		}
		function logComponentEffect(fiber, startTime, endTime, selfTime, errors) {
			if (null !== errors) {
				if (supportsUserTiming) {
					var name = getComponentNameFromFiber(fiber);
					if (null !== name) {
						selfTime = [];
						for (var i = 0; i < errors.length; i++) {
							var error = errors[i].value;
							selfTime.push(["Error", "object" === typeof error && null !== error && "string" === typeof error.message ? String(error.message) : String(error)]);
						}
						null !== fiber.key && addValueToProperties("key", fiber.key, selfTime, 0, "");
						null !== fiber.memoizedProps && addObjectToProperties(fiber.memoizedProps, selfTime, 0, "");
						startTime = {
							start: startTime,
							end: endTime,
							detail: { devtools: {
								color: "error",
								track: COMPONENTS_TRACK,
								tooltipText: "A lifecycle or effect errored",
								properties: selfTime
							} }
						};
						(fiber = fiber._debugTask) ? fiber.run(performance.measure.bind(performance, "​" + name, startTime)) : performance.measure("​" + name, startTime);
					}
				}
			} else name = getComponentNameFromFiber(fiber), null !== name && supportsUserTiming && (errors = 1 > selfTime ? "secondary-light" : 100 > selfTime ? "secondary" : 500 > selfTime ? "secondary-dark" : "error", (fiber = fiber._debugTask) ? fiber.run(console.timeStamp.bind(console, name, startTime, endTime, COMPONENTS_TRACK, void 0, errors)) : console.timeStamp(name, startTime, endTime, COMPONENTS_TRACK, void 0, errors));
		}
		function logRenderPhase(startTime, endTime, lanes, debugTask) {
			if (supportsUserTiming && !(endTime <= startTime)) {
				var color = (lanes & 738197653) === lanes ? "tertiary-dark" : "primary-dark";
				lanes = (lanes & 536870912) === lanes ? "Prepared" : (lanes & 201326741) === lanes ? "Hydrated" : "Render";
				debugTask ? debugTask.run(console.timeStamp.bind(console, lanes, startTime, endTime, currentTrack, LANES_TRACK_GROUP, color)) : console.timeStamp(lanes, startTime, endTime, currentTrack, LANES_TRACK_GROUP, color);
			}
		}
		function logSuspendedRenderPhase(startTime, endTime, lanes, debugTask) {
			!supportsUserTiming || endTime <= startTime || (lanes = (lanes & 738197653) === lanes ? "tertiary-dark" : "primary-dark", debugTask ? debugTask.run(console.timeStamp.bind(console, "Prewarm", startTime, endTime, currentTrack, LANES_TRACK_GROUP, lanes)) : console.timeStamp("Prewarm", startTime, endTime, currentTrack, LANES_TRACK_GROUP, lanes));
		}
		function logSuspendedWithDelayPhase(startTime, endTime, lanes, debugTask) {
			!supportsUserTiming || endTime <= startTime || (lanes = (lanes & 738197653) === lanes ? "tertiary-dark" : "primary-dark", debugTask ? debugTask.run(console.timeStamp.bind(console, "Suspended", startTime, endTime, currentTrack, LANES_TRACK_GROUP, lanes)) : console.timeStamp("Suspended", startTime, endTime, currentTrack, LANES_TRACK_GROUP, lanes));
		}
		function logRecoveredRenderPhase(startTime, endTime, lanes, recoverableErrors, hydrationFailed, debugTask) {
			if (supportsUserTiming && !(endTime <= startTime)) {
				lanes = [];
				for (var i = 0; i < recoverableErrors.length; i++) {
					var error = recoverableErrors[i].value;
					lanes.push(["Recoverable Error", "object" === typeof error && null !== error && "string" === typeof error.message ? String(error.message) : String(error)]);
				}
				startTime = {
					start: startTime,
					end: endTime,
					detail: { devtools: {
						color: "primary-dark",
						track: currentTrack,
						trackGroup: LANES_TRACK_GROUP,
						tooltipText: hydrationFailed ? "Hydration Failed" : "Recovered after Error",
						properties: lanes
					} }
				};
				debugTask ? debugTask.run(performance.measure.bind(performance, "Recovered", startTime)) : performance.measure("Recovered", startTime);
			}
		}
		function logErroredRenderPhase(startTime, endTime, lanes, debugTask) {
			!supportsUserTiming || endTime <= startTime || (debugTask ? debugTask.run(console.timeStamp.bind(console, "Errored", startTime, endTime, currentTrack, LANES_TRACK_GROUP, "error")) : console.timeStamp("Errored", startTime, endTime, currentTrack, LANES_TRACK_GROUP, "error"));
		}
		function logSuspendedCommitPhase(startTime, endTime, reason, debugTask) {
			!supportsUserTiming || endTime <= startTime || (debugTask ? debugTask.run(console.timeStamp.bind(console, reason, startTime, endTime, currentTrack, LANES_TRACK_GROUP, "secondary-light")) : console.timeStamp(reason, startTime, endTime, currentTrack, LANES_TRACK_GROUP, "secondary-light"));
		}
		function logCommitErrored(startTime, endTime, errors, passive, debugTask) {
			if (supportsUserTiming && !(endTime <= startTime)) {
				for (var properties = [], i = 0; i < errors.length; i++) {
					var error = errors[i].value;
					properties.push(["Error", "object" === typeof error && null !== error && "string" === typeof error.message ? String(error.message) : String(error)]);
				}
				startTime = {
					start: startTime,
					end: endTime,
					detail: { devtools: {
						color: "error",
						track: currentTrack,
						trackGroup: LANES_TRACK_GROUP,
						tooltipText: passive ? "Remaining Effects Errored" : "Commit Errored",
						properties
					} }
				};
				debugTask ? debugTask.run(performance.measure.bind(performance, "Errored", startTime)) : performance.measure("Errored", startTime);
			}
		}
		function logAnimatingPhase(startTime, endTime, debugTask) {
			!supportsUserTiming || endTime <= startTime || (debugTask ? debugTask.run(console.timeStamp.bind(console, "Animating", startTime, endTime, currentTrack, LANES_TRACK_GROUP, "secondary-dark")) : console.timeStamp("Animating", startTime, endTime, currentTrack, LANES_TRACK_GROUP, "secondary-dark"));
		}
		function finishQueueingConcurrentUpdates() {
			for (var endIndex = concurrentQueuesIndex, i = concurrentlyUpdatedLanes = concurrentQueuesIndex = 0; i < endIndex;) {
				var fiber = concurrentQueues[i];
				concurrentQueues[i++] = null;
				var queue = concurrentQueues[i];
				concurrentQueues[i++] = null;
				var update = concurrentQueues[i];
				concurrentQueues[i++] = null;
				var lane = concurrentQueues[i];
				concurrentQueues[i++] = null;
				if (null !== queue && null !== update) {
					var pending = queue.pending;
					null === pending ? update.next = update : (update.next = pending.next, pending.next = update);
					queue.pending = update;
				}
				0 !== lane && markUpdateLaneFromFiberToRoot(fiber, update, lane);
			}
		}
		function enqueueUpdate$1(fiber, queue, update, lane) {
			concurrentQueues[concurrentQueuesIndex++] = fiber;
			concurrentQueues[concurrentQueuesIndex++] = queue;
			concurrentQueues[concurrentQueuesIndex++] = update;
			concurrentQueues[concurrentQueuesIndex++] = lane;
			concurrentlyUpdatedLanes |= lane;
			fiber.lanes |= lane;
			fiber = fiber.alternate;
			null !== fiber && (fiber.lanes |= lane);
		}
		function enqueueConcurrentHookUpdate(fiber, queue, update, lane) {
			enqueueUpdate$1(fiber, queue, update, lane);
			return getRootForUpdatedFiber(fiber);
		}
		function enqueueConcurrentRenderForLane(fiber, lane) {
			enqueueUpdate$1(fiber, null, null, lane);
			return getRootForUpdatedFiber(fiber);
		}
		function markUpdateLaneFromFiberToRoot(sourceFiber, update, lane) {
			sourceFiber.lanes |= lane;
			var alternate = sourceFiber.alternate;
			null !== alternate && (alternate.lanes |= lane);
			for (var isHidden = !1, parent = sourceFiber.return; null !== parent;) parent.childLanes |= lane, alternate = parent.alternate, null !== alternate && (alternate.childLanes |= lane), 22 === parent.tag && (sourceFiber = parent.stateNode, null === sourceFiber || sourceFiber._visibility & OffscreenVisible || (isHidden = !0)), sourceFiber = parent, parent = parent.return;
			return 3 === sourceFiber.tag ? (parent = sourceFiber.stateNode, isHidden && null !== update && (isHidden = 31 - clz32(lane), sourceFiber = parent.hiddenUpdates, alternate = sourceFiber[isHidden], null === alternate ? sourceFiber[isHidden] = [update] : alternate.push(update), update.lane = lane | 536870912), parent) : null;
		}
		function getRootForUpdatedFiber(sourceFiber) {
			if (nestedUpdateCount > NESTED_UPDATE_LIMIT) throw nestedPassiveUpdateCount = nestedUpdateCount = 0, rootWithPassiveNestedUpdates = rootWithNestedUpdates = null, Error("Maximum update depth exceeded. This can happen when a component repeatedly calls setState inside componentWillUpdate or componentDidUpdate. React limits the number of nested updates to prevent infinite loops.");
			nestedPassiveUpdateCount > NESTED_PASSIVE_UPDATE_LIMIT && (nestedPassiveUpdateCount = 0, rootWithPassiveNestedUpdates = null, console.error("Maximum update depth exceeded. This can happen when a component calls setState inside useEffect, but useEffect either doesn't have a dependency array, or one of the dependencies changes on every render."));
			null === sourceFiber.alternate && 0 !== (sourceFiber.flags & 4098) && warnAboutUpdateOnNotYetMountedFiberInDEV(sourceFiber);
			for (var node = sourceFiber, parent = node.return; null !== parent;) null === node.alternate && 0 !== (node.flags & 4098) && warnAboutUpdateOnNotYetMountedFiberInDEV(sourceFiber), node = parent, parent = node.return;
			return 3 === node.tag ? node.stateNode : null;
		}
		function resolveFunctionForHotReloading(type) {
			if (null === resolveFamily) return type;
			var family = resolveFamily(type);
			return void 0 === family ? type : family.current;
		}
		function resolveForwardRefForHotReloading(type) {
			if (null === resolveFamily) return type;
			var family = resolveFamily(type);
			return void 0 === family ? null !== type && void 0 !== type && "function" === typeof type.render && (family = resolveFunctionForHotReloading(type.render), type.render !== family) ? (family = {
				$$typeof: REACT_FORWARD_REF_TYPE,
				render: family
			}, void 0 !== type.displayName && (family.displayName = type.displayName), family) : type : family.current;
		}
		function isCompatibleFamilyForHotReloading(fiber, element) {
			if (null === resolveFamily) return !1;
			var prevType = fiber.elementType;
			element = element.type;
			var needsCompareFamilies = !1, $$typeofNextType = "object" === typeof element && null !== element ? element.$$typeof : null;
			switch (fiber.tag) {
				case 1:
					"function" === typeof element && (needsCompareFamilies = !0);
					break;
				case 0:
					"function" === typeof element ? needsCompareFamilies = !0 : $$typeofNextType === REACT_LAZY_TYPE && (needsCompareFamilies = !0);
					break;
				case 11:
					$$typeofNextType === REACT_FORWARD_REF_TYPE ? needsCompareFamilies = !0 : $$typeofNextType === REACT_LAZY_TYPE && (needsCompareFamilies = !0);
					break;
				case 14:
				case 15:
					$$typeofNextType === REACT_MEMO_TYPE ? needsCompareFamilies = !0 : $$typeofNextType === REACT_LAZY_TYPE && (needsCompareFamilies = !0);
					break;
				default: return !1;
			}
			return needsCompareFamilies && (fiber = resolveFamily(prevType), void 0 !== fiber && fiber === resolveFamily(element)) ? !0 : !1;
		}
		function markFailedErrorBoundaryForHotReloading(fiber) {
			null !== resolveFamily && "function" === typeof WeakSet && (null === failedBoundaries && (failedBoundaries = /* @__PURE__ */ new WeakSet()), failedBoundaries.add(fiber));
		}
		function scheduleFibersWithFamiliesRecursively(fiber, updatedFamilies, staleFamilies) {
			do {
				var _fiber = fiber, alternate = _fiber.alternate, child = _fiber.child, sibling = _fiber.sibling, tag = _fiber.tag;
				_fiber = _fiber.type;
				var candidateType = null;
				switch (tag) {
					case 0:
					case 15:
					case 1:
						candidateType = _fiber;
						break;
					case 11: candidateType = _fiber.render;
				}
				if (null === resolveFamily) throw Error("Expected resolveFamily to be set during hot reload.");
				var needsRender = !1;
				_fiber = !1;
				null !== candidateType && (candidateType = resolveFamily(candidateType), void 0 !== candidateType && (staleFamilies.has(candidateType) ? _fiber = !0 : updatedFamilies.has(candidateType) && (1 === tag ? _fiber = !0 : needsRender = !0)));
				null !== failedBoundaries && (failedBoundaries.has(fiber) || null !== alternate && failedBoundaries.has(alternate)) && (_fiber = !0);
				_fiber && (fiber._debugNeedsRemount = !0);
				if (_fiber || needsRender) alternate = enqueueConcurrentRenderForLane(fiber, 2), null !== alternate && scheduleUpdateOnFiber(alternate, fiber, 2);
				null === child || _fiber || scheduleFibersWithFamiliesRecursively(child, updatedFamilies, staleFamilies);
				if (null === sibling) break;
				fiber = sibling;
			} while (1);
		}
		function FiberNode(tag, pendingProps, key, mode) {
			this.tag = tag;
			this.key = key;
			this.sibling = this.child = this.return = this.stateNode = this.type = this.elementType = null;
			this.index = 0;
			this.refCleanup = this.ref = null;
			this.pendingProps = pendingProps;
			this.dependencies = this.memoizedState = this.updateQueue = this.memoizedProps = null;
			this.mode = mode;
			this.subtreeFlags = this.flags = 0;
			this.deletions = null;
			this.childLanes = this.lanes = 0;
			this.alternate = null;
			this.actualDuration = -0;
			this.actualStartTime = -1.1;
			this.treeBaseDuration = this.selfBaseDuration = -0;
			this._debugTask = this._debugStack = this._debugOwner = this._debugInfo = null;
			this._debugNeedsRemount = !1;
			this._debugHookTypes = null;
			hasBadMapPolyfill || "function" !== typeof Object.preventExtensions || Object.preventExtensions(this);
		}
		function shouldConstruct(Component) {
			Component = Component.prototype;
			return !(!Component || !Component.isReactComponent);
		}
		function createWorkInProgress(current, pendingProps) {
			var workInProgress = current.alternate;
			null === workInProgress ? (workInProgress = createFiber(current.tag, pendingProps, current.key, current.mode), workInProgress.elementType = current.elementType, workInProgress.type = current.type, workInProgress.stateNode = current.stateNode, workInProgress._debugOwner = current._debugOwner, workInProgress._debugStack = current._debugStack, workInProgress._debugTask = current._debugTask, workInProgress._debugHookTypes = current._debugHookTypes, workInProgress.alternate = current, current.alternate = workInProgress) : (workInProgress.pendingProps = pendingProps, workInProgress.type = current.type, workInProgress.flags = 0, workInProgress.subtreeFlags = 0, workInProgress.deletions = null, workInProgress.actualDuration = -0, workInProgress.actualStartTime = -1.1);
			workInProgress.flags = current.flags & 65011712;
			workInProgress.childLanes = current.childLanes;
			workInProgress.lanes = current.lanes;
			workInProgress.child = current.child;
			workInProgress.memoizedProps = current.memoizedProps;
			workInProgress.memoizedState = current.memoizedState;
			workInProgress.updateQueue = current.updateQueue;
			pendingProps = current.dependencies;
			workInProgress.dependencies = null === pendingProps ? null : {
				lanes: pendingProps.lanes,
				firstContext: pendingProps.firstContext,
				_debugThenableState: pendingProps._debugThenableState
			};
			workInProgress.sibling = current.sibling;
			workInProgress.index = current.index;
			workInProgress.ref = current.ref;
			workInProgress.refCleanup = current.refCleanup;
			workInProgress.selfBaseDuration = current.selfBaseDuration;
			workInProgress.treeBaseDuration = current.treeBaseDuration;
			workInProgress._debugInfo = current._debugInfo;
			workInProgress._debugNeedsRemount = current._debugNeedsRemount;
			switch (workInProgress.tag) {
				case 0:
				case 15:
					workInProgress.type = resolveFunctionForHotReloading(current.type);
					break;
				case 1:
					workInProgress.type = resolveFunctionForHotReloading(current.type);
					break;
				case 11: workInProgress.type = resolveForwardRefForHotReloading(current.type);
			}
			return workInProgress;
		}
		function resetWorkInProgress(workInProgress, renderLanes) {
			workInProgress.flags &= 65011714;
			var current = workInProgress.alternate;
			null === current ? (workInProgress.childLanes = 0, workInProgress.lanes = renderLanes, workInProgress.child = null, workInProgress.subtreeFlags = 0, workInProgress.memoizedProps = null, workInProgress.memoizedState = null, workInProgress.updateQueue = null, workInProgress.dependencies = null, workInProgress.stateNode = null, workInProgress.selfBaseDuration = 0, workInProgress.treeBaseDuration = 0) : (workInProgress.childLanes = current.childLanes, workInProgress.lanes = current.lanes, workInProgress.child = current.child, workInProgress.subtreeFlags = 0, workInProgress.deletions = null, workInProgress.memoizedProps = current.memoizedProps, workInProgress.memoizedState = current.memoizedState, workInProgress.updateQueue = current.updateQueue, workInProgress.type = current.type, renderLanes = current.dependencies, workInProgress.dependencies = null === renderLanes ? null : {
				lanes: renderLanes.lanes,
				firstContext: renderLanes.firstContext,
				_debugThenableState: renderLanes._debugThenableState
			}, workInProgress.selfBaseDuration = current.selfBaseDuration, workInProgress.treeBaseDuration = current.treeBaseDuration);
			return workInProgress;
		}
		function createFiberFromTypeAndProps(type, key, pendingProps, owner, mode, lanes) {
			var fiberTag = 0, resolvedType = type;
			if ("function" === typeof type) shouldConstruct(type) && (fiberTag = 1), resolvedType = resolveFunctionForHotReloading(resolvedType);
			else if ("string" === typeof type) fiberTag = getHostContext(), fiberTag = isHostHoistableType(type, pendingProps, fiberTag) ? 26 : "html" === type || "head" === type || "body" === type ? 27 : 5;
			else a: switch (type) {
				case REACT_ACTIVITY_TYPE: return key = createFiber(31, pendingProps, key, mode), key.elementType = REACT_ACTIVITY_TYPE, key.lanes = lanes, key;
				case REACT_FRAGMENT_TYPE: return createFiberFromFragment(pendingProps.children, mode, lanes, key);
				case REACT_STRICT_MODE_TYPE:
					fiberTag = 8;
					mode |= StrictLegacyMode;
					mode |= StrictEffectsMode;
					break;
				case REACT_PROFILER_TYPE: return type = pendingProps, owner = mode, "string" !== typeof type.id && console.error("Profiler must specify an \"id\" of type `string` as a prop. Received the type `%s` instead.", typeof type.id), key = createFiber(12, type, key, owner | ProfileMode), key.elementType = REACT_PROFILER_TYPE, key.lanes = lanes, key.stateNode = {
					effectDuration: 0,
					passiveEffectDuration: 0
				}, key;
				case REACT_SUSPENSE_TYPE: return key = createFiber(13, pendingProps, key, mode), key.elementType = REACT_SUSPENSE_TYPE, key.lanes = lanes, key;
				case REACT_SUSPENSE_LIST_TYPE: return key = createFiber(19, pendingProps, key, mode), key.elementType = REACT_SUSPENSE_LIST_TYPE, key.lanes = lanes, key;
				default:
					if ("object" === typeof type && null !== type) switch (type.$$typeof) {
						case REACT_CONTEXT_TYPE:
							fiberTag = 10;
							break a;
						case REACT_CONSUMER_TYPE:
							fiberTag = 9;
							break a;
						case REACT_FORWARD_REF_TYPE:
							fiberTag = 11;
							resolvedType = resolveForwardRefForHotReloading(resolvedType);
							break a;
						case REACT_MEMO_TYPE:
							fiberTag = 14;
							break a;
						case REACT_LAZY_TYPE:
							fiberTag = 16;
							resolvedType = null;
							break a;
					}
					resolvedType = "";
					if (void 0 === type || "object" === typeof type && null !== type && 0 === Object.keys(type).length) resolvedType += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.";
					null === type ? pendingProps = "null" : isArrayImpl(type) ? pendingProps = "array" : void 0 !== type && type.$$typeof === REACT_ELEMENT_TYPE ? (pendingProps = "<" + (getComponentNameFromType(type.type) || "Unknown") + " />", resolvedType = " Did you accidentally export a JSX literal instead of a component?") : pendingProps = typeof type;
					(fiberTag = owner ? getComponentNameFromOwner(owner) : null) && (resolvedType += "\n\nCheck the render method of `" + fiberTag + "`.");
					fiberTag = 29;
					pendingProps = Error("Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: " + (pendingProps + "." + resolvedType));
					resolvedType = null;
			}
			key = createFiber(fiberTag, pendingProps, key, mode);
			key.elementType = type;
			key.type = resolvedType;
			key.lanes = lanes;
			key._debugOwner = owner;
			return key;
		}
		function createFiberFromElement(element, mode, lanes) {
			mode = createFiberFromTypeAndProps(element.type, element.key, element.props, element._owner, mode, lanes);
			mode._debugOwner = element._owner;
			mode._debugStack = element._debugStack;
			mode._debugTask = element._debugTask;
			return mode;
		}
		function createFiberFromFragment(elements, mode, lanes, key) {
			elements = createFiber(7, elements, key, mode);
			elements.lanes = lanes;
			return elements;
		}
		function createFiberFromText(content, mode, lanes) {
			content = createFiber(6, content, null, mode);
			content.lanes = lanes;
			return content;
		}
		function createFiberFromDehydratedFragment(dehydratedNode) {
			var fiber = createFiber(18, null, null, NoMode);
			fiber.stateNode = dehydratedNode;
			return fiber;
		}
		function createFiberFromPortal(portal, mode, lanes) {
			mode = createFiber(4, null !== portal.children ? portal.children : [], portal.key, mode);
			mode.lanes = lanes;
			mode.stateNode = {
				containerInfo: portal.containerInfo,
				pendingChildren: null,
				implementation: portal.implementation
			};
			return mode;
		}
		function createCapturedValueAtFiber(value, source) {
			if ("object" === typeof value && null !== value) {
				var existing = CapturedStacks.get(value);
				if (void 0 !== existing) return existing;
				source = {
					value,
					source,
					stack: getStackByFiberInDevAndProd(source)
				};
				CapturedStacks.set(value, source);
				return source;
			}
			return {
				value,
				source,
				stack: getStackByFiberInDevAndProd(source)
			};
		}
		function pushTreeFork(workInProgress, totalChildren) {
			warnIfNotHydrating();
			forkStack[forkStackIndex++] = treeForkCount;
			forkStack[forkStackIndex++] = treeForkProvider;
			treeForkProvider = workInProgress;
			treeForkCount = totalChildren;
		}
		function pushTreeId(workInProgress, totalChildren, index) {
			warnIfNotHydrating();
			idStack[idStackIndex++] = treeContextId;
			idStack[idStackIndex++] = treeContextOverflow;
			idStack[idStackIndex++] = treeContextProvider;
			treeContextProvider = workInProgress;
			var baseIdWithLeadingBit = treeContextId;
			workInProgress = treeContextOverflow;
			var baseLength = 32 - clz32(baseIdWithLeadingBit) - 1;
			baseIdWithLeadingBit &= ~(1 << baseLength);
			index += 1;
			var length = 32 - clz32(totalChildren) + baseLength;
			if (30 < length) {
				var numberOfOverflowBits = baseLength - baseLength % 5;
				length = (baseIdWithLeadingBit & (1 << numberOfOverflowBits) - 1).toString(32);
				baseIdWithLeadingBit >>= numberOfOverflowBits;
				baseLength -= numberOfOverflowBits;
				treeContextId = 1 << 32 - clz32(totalChildren) + baseLength | index << baseLength | baseIdWithLeadingBit;
				treeContextOverflow = length + workInProgress;
			} else treeContextId = 1 << length | index << baseLength | baseIdWithLeadingBit, treeContextOverflow = workInProgress;
		}
		function pushMaterializedTreeId(workInProgress) {
			warnIfNotHydrating();
			null !== workInProgress.return && (pushTreeFork(workInProgress, 1), pushTreeId(workInProgress, 1, 0));
		}
		function popTreeContext(workInProgress) {
			for (; workInProgress === treeForkProvider;) treeForkProvider = forkStack[--forkStackIndex], forkStack[forkStackIndex] = null, treeForkCount = forkStack[--forkStackIndex], forkStack[forkStackIndex] = null;
			for (; workInProgress === treeContextProvider;) treeContextProvider = idStack[--idStackIndex], idStack[idStackIndex] = null, treeContextOverflow = idStack[--idStackIndex], idStack[idStackIndex] = null, treeContextId = idStack[--idStackIndex], idStack[idStackIndex] = null;
		}
		function getSuspendedTreeContext() {
			warnIfNotHydrating();
			return null !== treeContextProvider ? {
				id: treeContextId,
				overflow: treeContextOverflow
			} : null;
		}
		function restoreSuspendedTreeContext(workInProgress, suspendedContext) {
			warnIfNotHydrating();
			idStack[idStackIndex++] = treeContextId;
			idStack[idStackIndex++] = treeContextOverflow;
			idStack[idStackIndex++] = treeContextProvider;
			treeContextId = suspendedContext.id;
			treeContextOverflow = suspendedContext.overflow;
			treeContextProvider = workInProgress;
		}
		function warnIfNotHydrating() {
			isHydrating || console.error("Expected to be hydrating. This is a bug in React. Please file an issue.");
		}
		function buildHydrationDiffNode(fiber, distanceFromLeaf) {
			if (null === fiber.return) {
				if (null === hydrationDiffRootDEV) hydrationDiffRootDEV = {
					fiber,
					children: [],
					serverProps: void 0,
					serverTail: [],
					distanceFromLeaf
				};
				else {
					if (hydrationDiffRootDEV.fiber !== fiber) throw Error("Saw multiple hydration diff roots in a pass. This is a bug in React.");
					hydrationDiffRootDEV.distanceFromLeaf > distanceFromLeaf && (hydrationDiffRootDEV.distanceFromLeaf = distanceFromLeaf);
				}
				return hydrationDiffRootDEV;
			}
			var siblings = buildHydrationDiffNode(fiber.return, distanceFromLeaf + 1).children;
			if (0 < siblings.length && siblings[siblings.length - 1].fiber === fiber) return siblings = siblings[siblings.length - 1], siblings.distanceFromLeaf > distanceFromLeaf && (siblings.distanceFromLeaf = distanceFromLeaf), siblings;
			distanceFromLeaf = {
				fiber,
				children: [],
				serverProps: void 0,
				serverTail: [],
				distanceFromLeaf
			};
			siblings.push(distanceFromLeaf);
			return distanceFromLeaf;
		}
		function warnIfHydrating() {
			isHydrating && console.error("We should not be hydrating here. This is a bug in React. Please file a bug.");
		}
		function warnNonHydratedInstance(fiber, rejectedCandidate) {
			didSuspendOrErrorDEV || (fiber = buildHydrationDiffNode(fiber, 0), fiber.serverProps = null, null !== rejectedCandidate && (rejectedCandidate = describeHydratableInstanceForDevWarnings(rejectedCandidate), fiber.serverTail.push(rejectedCandidate)));
		}
		function throwOnHydrationMismatch(fiber) {
			var fromText = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : !1, diff = "", diffRoot = hydrationDiffRootDEV;
			null !== diffRoot && (hydrationDiffRootDEV = null, diff = describeDiff(diffRoot));
			queueHydrationError(createCapturedValueAtFiber(Error("Hydration failed because the server rendered " + (fromText ? "text" : "HTML") + " didn't match the client. As a result this tree will be regenerated on the client. This can happen if a SSR-ed Client Component used:\n\n- A server/client branch `if (typeof window !== 'undefined')`.\n- Variable input such as `Date.now()` or `Math.random()` which changes each time it's called.\n- Date formatting in a user's locale which doesn't match the server.\n- External changing data without sending a snapshot of it along with the HTML.\n- Invalid HTML tag nesting.\n\nIt can also happen if the client has a browser extension installed which messes with the HTML before React loaded.\n\nhttps://react.dev/link/hydration-mismatch" + diff), fiber));
			throw HydrationMismatchException;
		}
		function prepareToHydrateHostInstance(fiber) {
			var didHydrate = fiber.stateNode;
			var type = fiber.type, props = fiber.memoizedProps;
			didHydrate[internalInstanceKey] = fiber;
			didHydrate[internalPropsKey] = props;
			validatePropertiesInDevelopment(type, props);
			switch (type) {
				case "dialog":
					listenToNonDelegatedEvent("cancel", didHydrate);
					listenToNonDelegatedEvent("close", didHydrate);
					break;
				case "iframe":
				case "object":
				case "embed":
					listenToNonDelegatedEvent("load", didHydrate);
					break;
				case "video":
				case "audio":
					for (type = 0; type < mediaEventTypes.length; type++) listenToNonDelegatedEvent(mediaEventTypes[type], didHydrate);
					break;
				case "source":
					listenToNonDelegatedEvent("error", didHydrate);
					break;
				case "img":
				case "image":
				case "link":
					listenToNonDelegatedEvent("error", didHydrate);
					listenToNonDelegatedEvent("load", didHydrate);
					break;
				case "details":
					listenToNonDelegatedEvent("toggle", didHydrate);
					break;
				case "input":
					checkControlledValueProps("input", props);
					listenToNonDelegatedEvent("invalid", didHydrate);
					validateInputProps(didHydrate, props);
					initInput(didHydrate, props.value, props.defaultValue, props.checked, props.defaultChecked, props.type, props.name, !0);
					break;
				case "option":
					validateOptionProps(didHydrate, props);
					break;
				case "select":
					checkControlledValueProps("select", props);
					listenToNonDelegatedEvent("invalid", didHydrate);
					validateSelectProps(didHydrate, props);
					break;
				case "textarea": checkControlledValueProps("textarea", props), listenToNonDelegatedEvent("invalid", didHydrate), validateTextareaProps(didHydrate, props), initTextarea(didHydrate, props.value, props.defaultValue, props.children);
			}
			type = props.children;
			"string" !== typeof type && "number" !== typeof type && "bigint" !== typeof type || didHydrate.textContent === "" + type || !0 === props.suppressHydrationWarning || checkForUnmatchedText(didHydrate.textContent, type) ? (null != props.popover && (listenToNonDelegatedEvent("beforetoggle", didHydrate), listenToNonDelegatedEvent("toggle", didHydrate)), null != props.onScroll && listenToNonDelegatedEvent("scroll", didHydrate), null != props.onScrollEnd && listenToNonDelegatedEvent("scrollend", didHydrate), null != props.onClick && (didHydrate.onclick = noop$1), didHydrate = !0) : didHydrate = !1;
			didHydrate || throwOnHydrationMismatch(fiber, !0);
		}
		function popToNextHostParent(fiber) {
			for (hydrationParentFiber = fiber.return; hydrationParentFiber;) switch (hydrationParentFiber.tag) {
				case 5:
				case 31:
				case 13:
					rootOrSingletonContext = !1;
					return;
				case 27:
				case 3:
					rootOrSingletonContext = !0;
					return;
				default: hydrationParentFiber = hydrationParentFiber.return;
			}
		}
		function popHydrationState(fiber) {
			if (fiber !== hydrationParentFiber) return !1;
			if (!isHydrating) return popToNextHostParent(fiber), isHydrating = !0, !1;
			var tag = fiber.tag, JSCompiler_temp;
			if (JSCompiler_temp = 3 !== tag && 27 !== tag) {
				if (JSCompiler_temp = 5 === tag) JSCompiler_temp = fiber.type, JSCompiler_temp = !("form" !== JSCompiler_temp && "button" !== JSCompiler_temp) || shouldSetTextContent(fiber.type, fiber.memoizedProps);
				JSCompiler_temp = !JSCompiler_temp;
			}
			if (JSCompiler_temp && nextHydratableInstance) {
				for (JSCompiler_temp = nextHydratableInstance; JSCompiler_temp;) {
					var diffNode = buildHydrationDiffNode(fiber, 0), description = describeHydratableInstanceForDevWarnings(JSCompiler_temp);
					diffNode.serverTail.push(description);
					JSCompiler_temp = "Suspense" === description.type ? getNextHydratableInstanceAfterHydrationBoundary(JSCompiler_temp) : getNextHydratable(JSCompiler_temp.nextSibling);
				}
				throwOnHydrationMismatch(fiber);
			}
			popToNextHostParent(fiber);
			if (13 === tag) {
				fiber = fiber.memoizedState;
				fiber = null !== fiber ? fiber.dehydrated : null;
				if (!fiber) throw Error("Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue.");
				nextHydratableInstance = getNextHydratableInstanceAfterHydrationBoundary(fiber);
			} else if (31 === tag) {
				fiber = fiber.memoizedState;
				fiber = null !== fiber ? fiber.dehydrated : null;
				if (!fiber) throw Error("Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue.");
				nextHydratableInstance = getNextHydratableInstanceAfterHydrationBoundary(fiber);
			} else 27 === tag ? (tag = nextHydratableInstance, isSingletonScope(fiber.type) ? (fiber = previousHydratableOnEnteringScopedSingleton, previousHydratableOnEnteringScopedSingleton = null, nextHydratableInstance = fiber) : nextHydratableInstance = tag) : nextHydratableInstance = hydrationParentFiber ? getNextHydratable(fiber.stateNode.nextSibling) : null;
			return !0;
		}
		function resetHydrationState() {
			nextHydratableInstance = hydrationParentFiber = null;
			didSuspendOrErrorDEV = isHydrating = !1;
		}
		function upgradeHydrationErrorsToRecoverable() {
			var queuedErrors = hydrationErrors;
			null !== queuedErrors && (null === workInProgressRootRecoverableErrors ? workInProgressRootRecoverableErrors = queuedErrors : workInProgressRootRecoverableErrors.push.apply(workInProgressRootRecoverableErrors, queuedErrors), hydrationErrors = null);
			return queuedErrors;
		}
		function queueHydrationError(error) {
			null === hydrationErrors ? hydrationErrors = [error] : hydrationErrors.push(error);
		}
		function emitPendingHydrationWarnings() {
			var diffRoot = hydrationDiffRootDEV;
			if (null !== diffRoot) {
				hydrationDiffRootDEV = null;
				for (var diff = describeDiff(diffRoot); 0 < diffRoot.children.length;) diffRoot = diffRoot.children[0];
				runWithFiberInDEV(diffRoot.fiber, function() {
					console.error("A tree hydrated but some attributes of the server rendered HTML didn't match the client properties. This won't be patched up. This can happen if a SSR-ed Client Component used:\n\n- A server/client branch `if (typeof window !== 'undefined')`.\n- Variable input such as `Date.now()` or `Math.random()` which changes each time it's called.\n- Date formatting in a user's locale which doesn't match the server.\n- External changing data without sending a snapshot of it along with the HTML.\n- Invalid HTML tag nesting.\n\nIt can also happen if the client has a browser extension installed which messes with the HTML before React loaded.\n\n%s%s", "https://react.dev/link/hydration-mismatch", diff);
				});
			}
		}
		function resetContextDependencies() {
			lastContextDependency = currentlyRenderingFiber$1 = null;
			isDisallowedContextReadInDEV = !1;
		}
		function pushProvider(providerFiber, context, nextValue) {
			push(valueCursor, context._currentValue, providerFiber);
			context._currentValue = nextValue;
			push(rendererCursorDEV, context._currentRenderer, providerFiber);
			void 0 !== context._currentRenderer && null !== context._currentRenderer && context._currentRenderer !== rendererSigil && console.error("Detected multiple renderers concurrently rendering the same context provider. This is currently unsupported.");
			context._currentRenderer = rendererSigil;
		}
		function popProvider(context, providerFiber) {
			context._currentValue = valueCursor.current;
			var currentRenderer = rendererCursorDEV.current;
			pop(rendererCursorDEV, providerFiber);
			context._currentRenderer = currentRenderer;
			pop(valueCursor, providerFiber);
		}
		function scheduleContextWorkOnParentPath(parent, renderLanes, propagationRoot) {
			for (; null !== parent;) {
				var alternate = parent.alternate;
				(parent.childLanes & renderLanes) !== renderLanes ? (parent.childLanes |= renderLanes, null !== alternate && (alternate.childLanes |= renderLanes)) : null !== alternate && (alternate.childLanes & renderLanes) !== renderLanes && (alternate.childLanes |= renderLanes);
				if (parent === propagationRoot) break;
				parent = parent.return;
			}
			parent !== propagationRoot && console.error("Expected to find the propagation root when scheduling context work. This error is likely caused by a bug in React. Please file an issue.");
		}
		function propagateContextChanges(workInProgress, contexts, renderLanes, forcePropagateEntireTree) {
			var fiber = workInProgress.child;
			null !== fiber && (fiber.return = workInProgress);
			for (; null !== fiber;) {
				var list = fiber.dependencies;
				if (null !== list) {
					var nextFiber = fiber.child;
					list = list.firstContext;
					a: for (; null !== list;) {
						var dependency = list;
						list = fiber;
						for (var i = 0; i < contexts.length; i++) if (dependency.context === contexts[i]) {
							list.lanes |= renderLanes;
							dependency = list.alternate;
							null !== dependency && (dependency.lanes |= renderLanes);
							scheduleContextWorkOnParentPath(list.return, renderLanes, workInProgress);
							forcePropagateEntireTree || (nextFiber = null);
							break a;
						}
						list = dependency.next;
					}
				} else if (18 === fiber.tag) {
					nextFiber = fiber.return;
					if (null === nextFiber) throw Error("We just came from a parent so we must have had a parent. This is a bug in React.");
					nextFiber.lanes |= renderLanes;
					list = nextFiber.alternate;
					null !== list && (list.lanes |= renderLanes);
					scheduleContextWorkOnParentPath(nextFiber, renderLanes, workInProgress);
					nextFiber = null;
				} else nextFiber = fiber.child;
				if (null !== nextFiber) nextFiber.return = fiber;
				else for (nextFiber = fiber; null !== nextFiber;) {
					if (nextFiber === workInProgress) {
						nextFiber = null;
						break;
					}
					fiber = nextFiber.sibling;
					if (null !== fiber) {
						fiber.return = nextFiber.return;
						nextFiber = fiber;
						break;
					}
					nextFiber = nextFiber.return;
				}
				fiber = nextFiber;
			}
		}
		function propagateParentContextChanges(current, workInProgress, renderLanes, forcePropagateEntireTree) {
			current = null;
			for (var parent = workInProgress, isInsidePropagationBailout = !1; null !== parent;) {
				if (!isInsidePropagationBailout) {
					if (0 !== (parent.flags & 524288)) isInsidePropagationBailout = !0;
					else if (0 !== (parent.flags & 262144)) break;
				}
				if (10 === parent.tag) {
					var currentParent = parent.alternate;
					if (null === currentParent) throw Error("Should have a current fiber. This is a bug in React.");
					currentParent = currentParent.memoizedProps;
					if (null !== currentParent) {
						var context = parent.type;
						objectIs(parent.pendingProps.value, currentParent.value) || (null !== current ? current.push(context) : current = [context]);
					}
				} else if (parent === hostTransitionProviderCursor.current) {
					currentParent = parent.alternate;
					if (null === currentParent) throw Error("Should have a current fiber. This is a bug in React.");
					currentParent.memoizedState.memoizedState !== parent.memoizedState.memoizedState && (null !== current ? current.push(HostTransitionContext) : current = [HostTransitionContext]);
				}
				parent = parent.return;
			}
			null !== current && propagateContextChanges(workInProgress, current, renderLanes, forcePropagateEntireTree);
			workInProgress.flags |= 262144;
		}
		function checkIfContextChanged(currentDependencies) {
			for (currentDependencies = currentDependencies.firstContext; null !== currentDependencies;) {
				if (!objectIs(currentDependencies.context._currentValue, currentDependencies.memoizedValue)) return !0;
				currentDependencies = currentDependencies.next;
			}
			return !1;
		}
		function prepareToReadContext(workInProgress) {
			currentlyRenderingFiber$1 = workInProgress;
			lastContextDependency = null;
			workInProgress = workInProgress.dependencies;
			null !== workInProgress && (workInProgress.firstContext = null);
		}
		function readContext(context) {
			isDisallowedContextReadInDEV && console.error("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo().");
			return readContextForConsumer(currentlyRenderingFiber$1, context);
		}
		function readContextDuringReconciliation(consumer, context) {
			null === currentlyRenderingFiber$1 && prepareToReadContext(consumer);
			return readContextForConsumer(consumer, context);
		}
		function readContextForConsumer(consumer, context) {
			var value = context._currentValue;
			context = {
				context,
				memoizedValue: value,
				next: null
			};
			if (null === lastContextDependency) {
				if (null === consumer) throw Error("Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo().");
				lastContextDependency = context;
				consumer.dependencies = {
					lanes: 0,
					firstContext: context,
					_debugThenableState: null
				};
				consumer.flags |= 524288;
			} else lastContextDependency = lastContextDependency.next = context;
			return value;
		}
		function createCache() {
			return {
				controller: new AbortControllerLocal(),
				data: /* @__PURE__ */ new Map(),
				refCount: 0
			};
		}
		function retainCache(cache) {
			cache.controller.signal.aborted && console.warn("A cache instance was retained after it was already freed. This likely indicates a bug in React.");
			cache.refCount++;
		}
		function releaseCache(cache) {
			cache.refCount--;
			0 > cache.refCount && console.warn("A cache instance was released after it was already freed. This likely indicates a bug in React.");
			0 === cache.refCount && scheduleCallback$2(NormalPriority, function() {
				cache.controller.abort();
			});
		}
		function startUpdateTimerByLane(lane, method, fiber) {
			if (0 !== (lane & 127)) 0 > blockingUpdateTime && (blockingUpdateTime = now(), blockingUpdateTask = createTask(method), blockingUpdateMethodName = method, null != fiber && (blockingUpdateComponentName = getComponentNameFromFiber(fiber)), (executionContext & (RenderContext | CommitContext)) !== NoContext && (componentEffectSpawnedUpdate = !0, blockingUpdateType = SPAWNED_UPDATE), lane = resolveEventTimeStamp(), method = resolveEventType(), lane !== blockingEventRepeatTime || method !== blockingEventType ? blockingEventRepeatTime = -1.1 : null !== method && (blockingUpdateType = SPAWNED_UPDATE), blockingEventTime = lane, blockingEventType = method);
			else if (0 !== (lane & 4194048) && 0 > transitionUpdateTime && (transitionUpdateTime = now(), transitionUpdateTask = createTask(method), transitionUpdateMethodName = method, null != fiber && (transitionUpdateComponentName = getComponentNameFromFiber(fiber)), 0 > transitionStartTime)) {
				lane = resolveEventTimeStamp();
				method = resolveEventType();
				if (lane !== transitionEventRepeatTime || method !== transitionEventType) transitionEventRepeatTime = -1.1;
				transitionEventTime = lane;
				transitionEventType = method;
			}
		}
		function startHostActionTimer(fiber) {
			if (0 > blockingUpdateTime) {
				blockingUpdateTime = now();
				blockingUpdateTask = null != fiber._debugTask ? fiber._debugTask : null;
				(executionContext & (RenderContext | CommitContext)) !== NoContext && (blockingUpdateType = SPAWNED_UPDATE);
				var newEventTime = resolveEventTimeStamp(), newEventType = resolveEventType();
				newEventTime !== blockingEventRepeatTime || newEventType !== blockingEventType ? blockingEventRepeatTime = -1.1 : null !== newEventType && (blockingUpdateType = SPAWNED_UPDATE);
				blockingEventTime = newEventTime;
				blockingEventType = newEventType;
			}
			if (0 > transitionUpdateTime && (transitionUpdateTime = now(), transitionUpdateTask = null != fiber._debugTask ? fiber._debugTask : null, 0 > transitionStartTime)) {
				fiber = resolveEventTimeStamp();
				newEventTime = resolveEventType();
				if (fiber !== transitionEventRepeatTime || newEventTime !== transitionEventType) transitionEventRepeatTime = -1.1;
				transitionEventTime = fiber;
				transitionEventType = newEventTime;
			}
		}
		function pushNestedEffectDurations() {
			var prevEffectDuration = profilerEffectDuration;
			profilerEffectDuration = 0;
			return prevEffectDuration;
		}
		function popNestedEffectDurations(prevEffectDuration) {
			var elapsedTime = profilerEffectDuration;
			profilerEffectDuration = prevEffectDuration;
			return elapsedTime;
		}
		function bubbleNestedEffectDurations(prevEffectDuration) {
			var elapsedTime = profilerEffectDuration;
			profilerEffectDuration += prevEffectDuration;
			return elapsedTime;
		}
		function resetComponentEffectTimers() {
			componentEffectEndTime = componentEffectStartTime = -1.1;
		}
		function pushComponentEffectStart() {
			var prevEffectStart = componentEffectStartTime;
			componentEffectStartTime = -1.1;
			return prevEffectStart;
		}
		function popComponentEffectStart(prevEffectStart) {
			0 <= prevEffectStart && (componentEffectStartTime = prevEffectStart);
		}
		function pushComponentEffectDuration() {
			var prevEffectDuration = componentEffectDuration;
			componentEffectDuration = -0;
			return prevEffectDuration;
		}
		function popComponentEffectDuration(prevEffectDuration) {
			0 <= prevEffectDuration && (componentEffectDuration = prevEffectDuration);
		}
		function pushComponentEffectErrors() {
			var prevErrors = componentEffectErrors;
			componentEffectErrors = null;
			return prevErrors;
		}
		function pushComponentEffectDidSpawnUpdate() {
			var prev = componentEffectSpawnedUpdate;
			componentEffectSpawnedUpdate = !1;
			return prev;
		}
		function startProfilerTimer(fiber) {
			profilerStartTime = now();
			0 > fiber.actualStartTime && (fiber.actualStartTime = profilerStartTime);
		}
		function stopProfilerTimerIfRunningAndRecordDuration(fiber) {
			if (0 <= profilerStartTime) {
				var elapsedTime = now() - profilerStartTime;
				fiber.actualDuration += elapsedTime;
				fiber.selfBaseDuration = elapsedTime;
				profilerStartTime = -1;
			}
		}
		function stopProfilerTimerIfRunningAndRecordIncompleteDuration(fiber) {
			if (0 <= profilerStartTime) {
				var elapsedTime = now() - profilerStartTime;
				fiber.actualDuration += elapsedTime;
				profilerStartTime = -1;
			}
		}
		function recordEffectDuration() {
			if (0 <= profilerStartTime) {
				var endTime = now(), elapsedTime = endTime - profilerStartTime;
				profilerStartTime = -1;
				profilerEffectDuration += elapsedTime;
				componentEffectDuration += elapsedTime;
				componentEffectEndTime = endTime;
			}
		}
		function recordEffectError(errorInfo) {
			null === componentEffectErrors && (componentEffectErrors = []);
			componentEffectErrors.push(errorInfo);
			null === commitErrors && (commitErrors = []);
			commitErrors.push(errorInfo);
		}
		function startEffectTimer() {
			profilerStartTime = now();
			0 > componentEffectStartTime && (componentEffectStartTime = profilerStartTime);
		}
		function transferActualDuration(fiber) {
			for (var child = fiber.child; child;) fiber.actualDuration += child.actualDuration, child = child.sibling;
		}
		function entangleAsyncAction(transition, thenable) {
			if (null === currentEntangledListeners) {
				var entangledListeners = currentEntangledListeners = [];
				currentEntangledPendingCount = 0;
				currentEntangledLane = requestTransitionLane();
				currentEntangledActionThenable = {
					status: "pending",
					value: void 0,
					then: function(resolve) {
						entangledListeners.push(resolve);
					}
				};
			}
			currentEntangledPendingCount++;
			thenable.then(pingEngtangledActionScope, pingEngtangledActionScope);
			return thenable;
		}
		function pingEngtangledActionScope() {
			if (0 === --currentEntangledPendingCount && (-1 < transitionUpdateTime || (transitionStartTime = -1.1), null !== currentEntangledListeners)) {
				null !== currentEntangledActionThenable && (currentEntangledActionThenable.status = "fulfilled");
				var listeners = currentEntangledListeners;
				currentEntangledListeners = null;
				currentEntangledLane = 0;
				currentEntangledActionThenable = null;
				for (var i = 0; i < listeners.length; i++) (0, listeners[i])();
			}
		}
		function chainThenableValue(thenable, result) {
			var listeners = [], thenableWithOverride = {
				status: "pending",
				value: null,
				reason: null,
				then: function(resolve) {
					listeners.push(resolve);
				}
			};
			thenable.then(function() {
				thenableWithOverride.status = "fulfilled";
				thenableWithOverride.value = result;
				for (var i = 0; i < listeners.length; i++) (0, listeners[i])(result);
			}, function(error) {
				thenableWithOverride.status = "rejected";
				thenableWithOverride.reason = error;
				for (error = 0; error < listeners.length; error++) (0, listeners[error])(void 0);
			});
			return thenableWithOverride;
		}
		function peekCacheFromPool() {
			var cacheResumedFromPreviousRender = resumedCache.current;
			return null !== cacheResumedFromPreviousRender ? cacheResumedFromPreviousRender : workInProgressRoot.pooledCache;
		}
		function pushTransition(offscreenWorkInProgress, prevCachePool) {
			null === prevCachePool ? push(resumedCache, resumedCache.current, offscreenWorkInProgress) : push(resumedCache, prevCachePool.pool, offscreenWorkInProgress);
		}
		function getSuspendedCache() {
			var cacheFromPool = peekCacheFromPool();
			return null === cacheFromPool ? null : {
				parent: CacheContext._currentValue,
				pool: cacheFromPool
			};
		}
		function createThenableState() {
			return {
				didWarnAboutUncachedPromise: !1,
				thenables: []
			};
		}
		function isThenableResolved(thenable) {
			thenable = thenable.status;
			return "fulfilled" === thenable || "rejected" === thenable;
		}
		function trackUsedThenable(thenableState, thenable, index) {
			null !== ReactSharedInternals.actQueue && (ReactSharedInternals.didUsePromise = !0);
			var trackedThenables = thenableState.thenables;
			index = trackedThenables[index];
			void 0 === index ? trackedThenables.push(thenable) : index !== thenable && (thenableState.didWarnAboutUncachedPromise || (thenableState.didWarnAboutUncachedPromise = !0, console.error("A component was suspended by an uncached promise. Creating promises inside a Client Component or hook is not yet supported, except via a Suspense-compatible library or framework.")), thenable.then(noop$1, noop$1), thenable = index);
			if (void 0 === thenable._debugInfo) {
				thenableState = performance.now();
				trackedThenables = thenable.displayName;
				var ioInfo = {
					name: "string" === typeof trackedThenables ? trackedThenables : "Promise",
					start: thenableState,
					end: thenableState,
					value: thenable
				};
				thenable._debugInfo = [{ awaited: ioInfo }];
				"fulfilled" !== thenable.status && "rejected" !== thenable.status && (thenableState = function() {
					ioInfo.end = performance.now();
				}, thenable.then(thenableState, thenableState));
			}
			switch (thenable.status) {
				case "fulfilled": return thenable.value;
				case "rejected": throw thenableState = thenable.reason, checkIfUseWrappedInAsyncCatch(thenableState), thenableState;
				default:
					if ("string" === typeof thenable.status) thenable.then(noop$1, noop$1);
					else {
						thenableState = workInProgressRoot;
						if (null !== thenableState && 100 < thenableState.shellSuspendCounter) throw Error("An unknown Component is an async Client Component. Only Server Components can be async at the moment. This error is often caused by accidentally adding `'use client'` to a module that was originally written for the server.");
						thenableState = thenable;
						thenableState.status = "pending";
						thenableState.then(function(fulfilledValue) {
							if ("pending" === thenable.status) {
								var fulfilledThenable = thenable;
								fulfilledThenable.status = "fulfilled";
								fulfilledThenable.value = fulfilledValue;
							}
						}, function(error) {
							if ("pending" === thenable.status) {
								var rejectedThenable = thenable;
								rejectedThenable.status = "rejected";
								rejectedThenable.reason = error;
							}
						});
					}
					switch (thenable.status) {
						case "fulfilled": return thenable.value;
						case "rejected": throw thenableState = thenable.reason, checkIfUseWrappedInAsyncCatch(thenableState), thenableState;
					}
					suspendedThenable = thenable;
					needsToResetSuspendedThenableDEV = !0;
					throw SuspenseException;
			}
		}
		function resolveLazy(lazyType) {
			try {
				return callLazyInitInDEV(lazyType);
			} catch (x) {
				if (null !== x && "object" === typeof x && "function" === typeof x.then) throw suspendedThenable = x, needsToResetSuspendedThenableDEV = !0, SuspenseException;
				throw x;
			}
		}
		function getSuspendedThenable() {
			if (null === suspendedThenable) throw Error("Expected a suspended thenable. This is a bug in React. Please file an issue.");
			var thenable = suspendedThenable;
			suspendedThenable = null;
			needsToResetSuspendedThenableDEV = !1;
			return thenable;
		}
		function checkIfUseWrappedInAsyncCatch(rejectedReason) {
			if (rejectedReason === SuspenseException || rejectedReason === SuspenseActionException) throw Error("Hooks are not supported inside an async component. This error is often caused by accidentally adding `'use client'` to a module that was originally written for the server.");
		}
		function pushDebugInfo(debugInfo) {
			var previousDebugInfo = currentDebugInfo;
			null != debugInfo && (currentDebugInfo = null === previousDebugInfo ? debugInfo : previousDebugInfo.concat(debugInfo));
			return previousDebugInfo;
		}
		function getCurrentDebugTask() {
			var debugInfo = currentDebugInfo;
			if (null != debugInfo) {
				for (var i = debugInfo.length - 1; 0 <= i; i--) if (null != debugInfo[i].name) {
					var debugTask = debugInfo[i].debugTask;
					if (null != debugTask) return debugTask;
				}
			}
			return null;
		}
		function validateFragmentProps(element, fiber, returnFiber) {
			for (var keys = Object.keys(element.props), i = 0; i < keys.length; i++) {
				var key = keys[i];
				if ("children" !== key && "key" !== key) {
					null === fiber && (fiber = createFiberFromElement(element, returnFiber.mode, 0), fiber._debugInfo = currentDebugInfo, fiber.return = returnFiber);
					runWithFiberInDEV(fiber, function(erroredKey) {
						console.error("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", erroredKey);
					}, key);
					break;
				}
			}
		}
		function unwrapThenable(thenable) {
			var index = thenableIndexCounter$1;
			thenableIndexCounter$1 += 1;
			null === thenableState$1 && (thenableState$1 = createThenableState());
			return trackUsedThenable(thenableState$1, thenable, index);
		}
		function coerceRef(workInProgress, element) {
			element = element.props.ref;
			workInProgress.ref = void 0 !== element ? element : null;
		}
		function throwOnInvalidObjectTypeImpl(returnFiber, newChild) {
			if (newChild.$$typeof === REACT_LEGACY_ELEMENT_TYPE) throw Error("A React Element from an older version of React was rendered. This is not supported. It can happen if:\n- Multiple copies of the \"react\" package is used.\n- A library pre-bundled an old copy of \"react\" or \"react/jsx-runtime\".\n- A compiler tries to \"inline\" JSX instead of using the runtime.");
			returnFiber = Object.prototype.toString.call(newChild);
			throw Error("Objects are not valid as a React child (found: " + ("[object Object]" === returnFiber ? "object with keys {" + Object.keys(newChild).join(", ") + "}" : returnFiber) + "). If you meant to render a collection of children, use an array instead.");
		}
		function throwOnInvalidObjectType(returnFiber, newChild) {
			var debugTask = getCurrentDebugTask();
			null !== debugTask ? debugTask.run(throwOnInvalidObjectTypeImpl.bind(null, returnFiber, newChild)) : throwOnInvalidObjectTypeImpl(returnFiber, newChild);
		}
		function warnOnFunctionTypeImpl(returnFiber, invalidChild) {
			var parentName = getComponentNameFromFiber(returnFiber) || "Component";
			ownerHasFunctionTypeWarning[parentName] || (ownerHasFunctionTypeWarning[parentName] = !0, invalidChild = invalidChild.displayName || invalidChild.name || "Component", 3 === returnFiber.tag ? console.error("Functions are not valid as a React child. This may happen if you return %s instead of <%s /> from render. Or maybe you meant to call this function rather than return it.\n  root.render(%s)", invalidChild, invalidChild, invalidChild) : console.error("Functions are not valid as a React child. This may happen if you return %s instead of <%s /> from render. Or maybe you meant to call this function rather than return it.\n  <%s>{%s}</%s>", invalidChild, invalidChild, parentName, invalidChild, parentName));
		}
		function warnOnFunctionType(returnFiber, invalidChild) {
			var debugTask = getCurrentDebugTask();
			null !== debugTask ? debugTask.run(warnOnFunctionTypeImpl.bind(null, returnFiber, invalidChild)) : warnOnFunctionTypeImpl(returnFiber, invalidChild);
		}
		function warnOnSymbolTypeImpl(returnFiber, invalidChild) {
			var parentName = getComponentNameFromFiber(returnFiber) || "Component";
			ownerHasSymbolTypeWarning[parentName] || (ownerHasSymbolTypeWarning[parentName] = !0, invalidChild = String(invalidChild), 3 === returnFiber.tag ? console.error("Symbols are not valid as a React child.\n  root.render(%s)", invalidChild) : console.error("Symbols are not valid as a React child.\n  <%s>%s</%s>", parentName, invalidChild, parentName));
		}
		function warnOnSymbolType(returnFiber, invalidChild) {
			var debugTask = getCurrentDebugTask();
			null !== debugTask ? debugTask.run(warnOnSymbolTypeImpl.bind(null, returnFiber, invalidChild)) : warnOnSymbolTypeImpl(returnFiber, invalidChild);
		}
		function createChildReconciler(shouldTrackSideEffects) {
			function deleteChild(returnFiber, childToDelete) {
				if (shouldTrackSideEffects) {
					var deletions = returnFiber.deletions;
					null === deletions ? (returnFiber.deletions = [childToDelete], returnFiber.flags |= 16) : deletions.push(childToDelete);
				}
			}
			function deleteRemainingChildren(returnFiber, currentFirstChild) {
				if (!shouldTrackSideEffects) return null;
				for (; null !== currentFirstChild;) deleteChild(returnFiber, currentFirstChild), currentFirstChild = currentFirstChild.sibling;
				return null;
			}
			function mapRemainingChildren(currentFirstChild) {
				for (var existingChildren = /* @__PURE__ */ new Map(); null !== currentFirstChild;) null !== currentFirstChild.key ? existingChildren.set(currentFirstChild.key, currentFirstChild) : existingChildren.set(currentFirstChild.index, currentFirstChild), currentFirstChild = currentFirstChild.sibling;
				return existingChildren;
			}
			function useFiber(fiber, pendingProps) {
				fiber = createWorkInProgress(fiber, pendingProps);
				fiber.index = 0;
				fiber.sibling = null;
				return fiber;
			}
			function placeChild(newFiber, lastPlacedIndex, newIndex) {
				newFiber.index = newIndex;
				if (!shouldTrackSideEffects) return newFiber.flags |= 1048576, lastPlacedIndex;
				newIndex = newFiber.alternate;
				if (null !== newIndex) return newIndex = newIndex.index, newIndex < lastPlacedIndex ? (newFiber.flags |= 67108866, lastPlacedIndex) : newIndex;
				newFiber.flags |= 67108866;
				return lastPlacedIndex;
			}
			function placeSingleChild(newFiber) {
				shouldTrackSideEffects && null === newFiber.alternate && (newFiber.flags |= 67108866);
				return newFiber;
			}
			function updateTextNode(returnFiber, current, textContent, lanes) {
				if (null === current || 6 !== current.tag) return current = createFiberFromText(textContent, returnFiber.mode, lanes), current.return = returnFiber, current._debugOwner = returnFiber, current._debugTask = returnFiber._debugTask, current._debugInfo = currentDebugInfo, current;
				current = useFiber(current, textContent);
				current.return = returnFiber;
				current._debugInfo = currentDebugInfo;
				return current;
			}
			function updateElement(returnFiber, current, element, lanes) {
				var elementType = element.type;
				if (elementType === REACT_FRAGMENT_TYPE) return current = updateFragment(returnFiber, current, element.props.children, lanes, element.key), validateFragmentProps(element, current, returnFiber), current;
				if (null !== current && (current.elementType === elementType || isCompatibleFamilyForHotReloading(current, element) || "object" === typeof elementType && null !== elementType && elementType.$$typeof === REACT_LAZY_TYPE && resolveLazy(elementType) === current.type)) return current = useFiber(current, element.props), coerceRef(current, element), current.return = returnFiber, current._debugOwner = element._owner, current._debugInfo = currentDebugInfo, current;
				current = createFiberFromElement(element, returnFiber.mode, lanes);
				coerceRef(current, element);
				current.return = returnFiber;
				current._debugInfo = currentDebugInfo;
				return current;
			}
			function updatePortal(returnFiber, current, portal, lanes) {
				if (null === current || 4 !== current.tag || current.stateNode.containerInfo !== portal.containerInfo || current.stateNode.implementation !== portal.implementation) return current = createFiberFromPortal(portal, returnFiber.mode, lanes), current.return = returnFiber, current._debugInfo = currentDebugInfo, current;
				current = useFiber(current, portal.children || []);
				current.return = returnFiber;
				current._debugInfo = currentDebugInfo;
				return current;
			}
			function updateFragment(returnFiber, current, fragment, lanes, key) {
				if (null === current || 7 !== current.tag) return current = createFiberFromFragment(fragment, returnFiber.mode, lanes, key), current.return = returnFiber, current._debugOwner = returnFiber, current._debugTask = returnFiber._debugTask, current._debugInfo = currentDebugInfo, current;
				current = useFiber(current, fragment);
				current.return = returnFiber;
				current._debugInfo = currentDebugInfo;
				return current;
			}
			function createChild(returnFiber, newChild, lanes) {
				if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild) return newChild = createFiberFromText("" + newChild, returnFiber.mode, lanes), newChild.return = returnFiber, newChild._debugOwner = returnFiber, newChild._debugTask = returnFiber._debugTask, newChild._debugInfo = currentDebugInfo, newChild;
				if ("object" === typeof newChild && null !== newChild) {
					switch (newChild.$$typeof) {
						case REACT_ELEMENT_TYPE: return lanes = createFiberFromElement(newChild, returnFiber.mode, lanes), coerceRef(lanes, newChild), lanes.return = returnFiber, returnFiber = pushDebugInfo(newChild._debugInfo), lanes._debugInfo = currentDebugInfo, currentDebugInfo = returnFiber, lanes;
						case REACT_PORTAL_TYPE: return newChild = createFiberFromPortal(newChild, returnFiber.mode, lanes), newChild.return = returnFiber, newChild._debugInfo = currentDebugInfo, newChild;
						case REACT_LAZY_TYPE:
							var _prevDebugInfo = pushDebugInfo(newChild._debugInfo);
							newChild = resolveLazy(newChild);
							returnFiber = createChild(returnFiber, newChild, lanes);
							currentDebugInfo = _prevDebugInfo;
							return returnFiber;
					}
					if (isArrayImpl(newChild) || getIteratorFn(newChild)) return lanes = createFiberFromFragment(newChild, returnFiber.mode, lanes, null), lanes.return = returnFiber, lanes._debugOwner = returnFiber, lanes._debugTask = returnFiber._debugTask, returnFiber = pushDebugInfo(newChild._debugInfo), lanes._debugInfo = currentDebugInfo, currentDebugInfo = returnFiber, lanes;
					if ("function" === typeof newChild.then) return _prevDebugInfo = pushDebugInfo(newChild._debugInfo), returnFiber = createChild(returnFiber, unwrapThenable(newChild), lanes), currentDebugInfo = _prevDebugInfo, returnFiber;
					if (newChild.$$typeof === REACT_CONTEXT_TYPE) return createChild(returnFiber, readContextDuringReconciliation(returnFiber, newChild), lanes);
					throwOnInvalidObjectType(returnFiber, newChild);
				}
				"function" === typeof newChild && warnOnFunctionType(returnFiber, newChild);
				"symbol" === typeof newChild && warnOnSymbolType(returnFiber, newChild);
				return null;
			}
			function updateSlot(returnFiber, oldFiber, newChild, lanes) {
				var key = null !== oldFiber ? oldFiber.key : null;
				if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild) return null !== key ? null : updateTextNode(returnFiber, oldFiber, "" + newChild, lanes);
				if ("object" === typeof newChild && null !== newChild) {
					switch (newChild.$$typeof) {
						case REACT_ELEMENT_TYPE: return newChild.key === key ? (key = pushDebugInfo(newChild._debugInfo), returnFiber = updateElement(returnFiber, oldFiber, newChild, lanes), currentDebugInfo = key, returnFiber) : null;
						case REACT_PORTAL_TYPE: return newChild.key === key ? updatePortal(returnFiber, oldFiber, newChild, lanes) : null;
						case REACT_LAZY_TYPE: return key = pushDebugInfo(newChild._debugInfo), newChild = resolveLazy(newChild), returnFiber = updateSlot(returnFiber, oldFiber, newChild, lanes), currentDebugInfo = key, returnFiber;
					}
					if (isArrayImpl(newChild) || getIteratorFn(newChild)) {
						if (null !== key) return null;
						key = pushDebugInfo(newChild._debugInfo);
						returnFiber = updateFragment(returnFiber, oldFiber, newChild, lanes, null);
						currentDebugInfo = key;
						return returnFiber;
					}
					if ("function" === typeof newChild.then) return key = pushDebugInfo(newChild._debugInfo), returnFiber = updateSlot(returnFiber, oldFiber, unwrapThenable(newChild), lanes), currentDebugInfo = key, returnFiber;
					if (newChild.$$typeof === REACT_CONTEXT_TYPE) return updateSlot(returnFiber, oldFiber, readContextDuringReconciliation(returnFiber, newChild), lanes);
					throwOnInvalidObjectType(returnFiber, newChild);
				}
				"function" === typeof newChild && warnOnFunctionType(returnFiber, newChild);
				"symbol" === typeof newChild && warnOnSymbolType(returnFiber, newChild);
				return null;
			}
			function updateFromMap(existingChildren, returnFiber, newIdx, newChild, lanes) {
				if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild) return existingChildren = existingChildren.get(newIdx) || null, updateTextNode(returnFiber, existingChildren, "" + newChild, lanes);
				if ("object" === typeof newChild && null !== newChild) {
					switch (newChild.$$typeof) {
						case REACT_ELEMENT_TYPE: return newIdx = existingChildren.get(null === newChild.key ? newIdx : newChild.key) || null, existingChildren = pushDebugInfo(newChild._debugInfo), returnFiber = updateElement(returnFiber, newIdx, newChild, lanes), currentDebugInfo = existingChildren, returnFiber;
						case REACT_PORTAL_TYPE: return existingChildren = existingChildren.get(null === newChild.key ? newIdx : newChild.key) || null, updatePortal(returnFiber, existingChildren, newChild, lanes);
						case REACT_LAZY_TYPE:
							var _prevDebugInfo7 = pushDebugInfo(newChild._debugInfo);
							newChild = resolveLazy(newChild);
							returnFiber = updateFromMap(existingChildren, returnFiber, newIdx, newChild, lanes);
							currentDebugInfo = _prevDebugInfo7;
							return returnFiber;
					}
					if (isArrayImpl(newChild) || getIteratorFn(newChild)) return newIdx = existingChildren.get(newIdx) || null, existingChildren = pushDebugInfo(newChild._debugInfo), returnFiber = updateFragment(returnFiber, newIdx, newChild, lanes, null), currentDebugInfo = existingChildren, returnFiber;
					if ("function" === typeof newChild.then) return _prevDebugInfo7 = pushDebugInfo(newChild._debugInfo), returnFiber = updateFromMap(existingChildren, returnFiber, newIdx, unwrapThenable(newChild), lanes), currentDebugInfo = _prevDebugInfo7, returnFiber;
					if (newChild.$$typeof === REACT_CONTEXT_TYPE) return updateFromMap(existingChildren, returnFiber, newIdx, readContextDuringReconciliation(returnFiber, newChild), lanes);
					throwOnInvalidObjectType(returnFiber, newChild);
				}
				"function" === typeof newChild && warnOnFunctionType(returnFiber, newChild);
				"symbol" === typeof newChild && warnOnSymbolType(returnFiber, newChild);
				return null;
			}
			function warnOnInvalidKey(returnFiber, workInProgress, child, knownKeys) {
				if ("object" !== typeof child || null === child) return knownKeys;
				switch (child.$$typeof) {
					case REACT_ELEMENT_TYPE:
					case REACT_PORTAL_TYPE:
						warnForMissingKey(returnFiber, workInProgress, child);
						var key = child.key;
						if ("string" !== typeof key) break;
						if (null === knownKeys) {
							knownKeys = /* @__PURE__ */ new Set();
							knownKeys.add(key);
							break;
						}
						if (!knownKeys.has(key)) {
							knownKeys.add(key);
							break;
						}
						runWithFiberInDEV(workInProgress, function() {
							console.error("Encountered two children with the same key, `%s`. Keys should be unique so that components maintain their identity across updates. Non-unique keys may cause children to be duplicated and/or omitted — the behavior is unsupported and could change in a future version.", key);
						});
						break;
					case REACT_LAZY_TYPE: child = resolveLazy(child), warnOnInvalidKey(returnFiber, workInProgress, child, knownKeys);
				}
				return knownKeys;
			}
			function reconcileChildrenArray(returnFiber, currentFirstChild, newChildren, lanes) {
				for (var knownKeys = null, resultingFirstChild = null, previousNewFiber = null, oldFiber = currentFirstChild, newIdx = currentFirstChild = 0, nextOldFiber = null; null !== oldFiber && newIdx < newChildren.length; newIdx++) {
					oldFiber.index > newIdx ? (nextOldFiber = oldFiber, oldFiber = null) : nextOldFiber = oldFiber.sibling;
					var newFiber = updateSlot(returnFiber, oldFiber, newChildren[newIdx], lanes);
					if (null === newFiber) {
						null === oldFiber && (oldFiber = nextOldFiber);
						break;
					}
					knownKeys = warnOnInvalidKey(returnFiber, newFiber, newChildren[newIdx], knownKeys);
					shouldTrackSideEffects && oldFiber && null === newFiber.alternate && deleteChild(returnFiber, oldFiber);
					currentFirstChild = placeChild(newFiber, currentFirstChild, newIdx);
					null === previousNewFiber ? resultingFirstChild = newFiber : previousNewFiber.sibling = newFiber;
					previousNewFiber = newFiber;
					oldFiber = nextOldFiber;
				}
				if (newIdx === newChildren.length) return deleteRemainingChildren(returnFiber, oldFiber), isHydrating && pushTreeFork(returnFiber, newIdx), resultingFirstChild;
				if (null === oldFiber) {
					for (; newIdx < newChildren.length; newIdx++) oldFiber = createChild(returnFiber, newChildren[newIdx], lanes), null !== oldFiber && (knownKeys = warnOnInvalidKey(returnFiber, oldFiber, newChildren[newIdx], knownKeys), currentFirstChild = placeChild(oldFiber, currentFirstChild, newIdx), null === previousNewFiber ? resultingFirstChild = oldFiber : previousNewFiber.sibling = oldFiber, previousNewFiber = oldFiber);
					isHydrating && pushTreeFork(returnFiber, newIdx);
					return resultingFirstChild;
				}
				for (oldFiber = mapRemainingChildren(oldFiber); newIdx < newChildren.length; newIdx++) nextOldFiber = updateFromMap(oldFiber, returnFiber, newIdx, newChildren[newIdx], lanes), null !== nextOldFiber && (knownKeys = warnOnInvalidKey(returnFiber, nextOldFiber, newChildren[newIdx], knownKeys), shouldTrackSideEffects && null !== nextOldFiber.alternate && oldFiber.delete(null === nextOldFiber.key ? newIdx : nextOldFiber.key), currentFirstChild = placeChild(nextOldFiber, currentFirstChild, newIdx), null === previousNewFiber ? resultingFirstChild = nextOldFiber : previousNewFiber.sibling = nextOldFiber, previousNewFiber = nextOldFiber);
				shouldTrackSideEffects && oldFiber.forEach(function(child) {
					return deleteChild(returnFiber, child);
				});
				isHydrating && pushTreeFork(returnFiber, newIdx);
				return resultingFirstChild;
			}
			function reconcileChildrenIterator(returnFiber, currentFirstChild, newChildren, lanes) {
				if (null == newChildren) throw Error("An iterable object provided no iterator.");
				for (var resultingFirstChild = null, previousNewFiber = null, oldFiber = currentFirstChild, newIdx = currentFirstChild = 0, nextOldFiber = null, knownKeys = null, step = newChildren.next(); null !== oldFiber && !step.done; newIdx++, step = newChildren.next()) {
					oldFiber.index > newIdx ? (nextOldFiber = oldFiber, oldFiber = null) : nextOldFiber = oldFiber.sibling;
					var newFiber = updateSlot(returnFiber, oldFiber, step.value, lanes);
					if (null === newFiber) {
						null === oldFiber && (oldFiber = nextOldFiber);
						break;
					}
					knownKeys = warnOnInvalidKey(returnFiber, newFiber, step.value, knownKeys);
					shouldTrackSideEffects && oldFiber && null === newFiber.alternate && deleteChild(returnFiber, oldFiber);
					currentFirstChild = placeChild(newFiber, currentFirstChild, newIdx);
					null === previousNewFiber ? resultingFirstChild = newFiber : previousNewFiber.sibling = newFiber;
					previousNewFiber = newFiber;
					oldFiber = nextOldFiber;
				}
				if (step.done) return deleteRemainingChildren(returnFiber, oldFiber), isHydrating && pushTreeFork(returnFiber, newIdx), resultingFirstChild;
				if (null === oldFiber) {
					for (; !step.done; newIdx++, step = newChildren.next()) oldFiber = createChild(returnFiber, step.value, lanes), null !== oldFiber && (knownKeys = warnOnInvalidKey(returnFiber, oldFiber, step.value, knownKeys), currentFirstChild = placeChild(oldFiber, currentFirstChild, newIdx), null === previousNewFiber ? resultingFirstChild = oldFiber : previousNewFiber.sibling = oldFiber, previousNewFiber = oldFiber);
					isHydrating && pushTreeFork(returnFiber, newIdx);
					return resultingFirstChild;
				}
				for (oldFiber = mapRemainingChildren(oldFiber); !step.done; newIdx++, step = newChildren.next()) nextOldFiber = updateFromMap(oldFiber, returnFiber, newIdx, step.value, lanes), null !== nextOldFiber && (knownKeys = warnOnInvalidKey(returnFiber, nextOldFiber, step.value, knownKeys), shouldTrackSideEffects && null !== nextOldFiber.alternate && oldFiber.delete(null === nextOldFiber.key ? newIdx : nextOldFiber.key), currentFirstChild = placeChild(nextOldFiber, currentFirstChild, newIdx), null === previousNewFiber ? resultingFirstChild = nextOldFiber : previousNewFiber.sibling = nextOldFiber, previousNewFiber = nextOldFiber);
				shouldTrackSideEffects && oldFiber.forEach(function(child) {
					return deleteChild(returnFiber, child);
				});
				isHydrating && pushTreeFork(returnFiber, newIdx);
				return resultingFirstChild;
			}
			function reconcileChildFibersImpl(returnFiber, currentFirstChild, newChild, lanes) {
				"object" === typeof newChild && null !== newChild && newChild.type === REACT_FRAGMENT_TYPE && null === newChild.key && (validateFragmentProps(newChild, null, returnFiber), newChild = newChild.props.children);
				if ("object" === typeof newChild && null !== newChild) {
					switch (newChild.$$typeof) {
						case REACT_ELEMENT_TYPE:
							var prevDebugInfo = pushDebugInfo(newChild._debugInfo);
							a: {
								for (var key = newChild.key; null !== currentFirstChild;) {
									if (currentFirstChild.key === key) {
										key = newChild.type;
										if (key === REACT_FRAGMENT_TYPE) {
											if (7 === currentFirstChild.tag) {
												deleteRemainingChildren(returnFiber, currentFirstChild.sibling);
												lanes = useFiber(currentFirstChild, newChild.props.children);
												lanes.return = returnFiber;
												lanes._debugOwner = newChild._owner;
												lanes._debugInfo = currentDebugInfo;
												validateFragmentProps(newChild, lanes, returnFiber);
												returnFiber = lanes;
												break a;
											}
										} else if (currentFirstChild.elementType === key || isCompatibleFamilyForHotReloading(currentFirstChild, newChild) || "object" === typeof key && null !== key && key.$$typeof === REACT_LAZY_TYPE && resolveLazy(key) === currentFirstChild.type) {
											deleteRemainingChildren(returnFiber, currentFirstChild.sibling);
											lanes = useFiber(currentFirstChild, newChild.props);
											coerceRef(lanes, newChild);
											lanes.return = returnFiber;
											lanes._debugOwner = newChild._owner;
											lanes._debugInfo = currentDebugInfo;
											returnFiber = lanes;
											break a;
										}
										deleteRemainingChildren(returnFiber, currentFirstChild);
										break;
									} else deleteChild(returnFiber, currentFirstChild);
									currentFirstChild = currentFirstChild.sibling;
								}
								newChild.type === REACT_FRAGMENT_TYPE ? (lanes = createFiberFromFragment(newChild.props.children, returnFiber.mode, lanes, newChild.key), lanes.return = returnFiber, lanes._debugOwner = returnFiber, lanes._debugTask = returnFiber._debugTask, lanes._debugInfo = currentDebugInfo, validateFragmentProps(newChild, lanes, returnFiber), returnFiber = lanes) : (lanes = createFiberFromElement(newChild, returnFiber.mode, lanes), coerceRef(lanes, newChild), lanes.return = returnFiber, lanes._debugInfo = currentDebugInfo, returnFiber = lanes);
							}
							returnFiber = placeSingleChild(returnFiber);
							currentDebugInfo = prevDebugInfo;
							return returnFiber;
						case REACT_PORTAL_TYPE:
							a: {
								prevDebugInfo = newChild;
								for (newChild = prevDebugInfo.key; null !== currentFirstChild;) {
									if (currentFirstChild.key === newChild) if (4 === currentFirstChild.tag && currentFirstChild.stateNode.containerInfo === prevDebugInfo.containerInfo && currentFirstChild.stateNode.implementation === prevDebugInfo.implementation) {
										deleteRemainingChildren(returnFiber, currentFirstChild.sibling);
										lanes = useFiber(currentFirstChild, prevDebugInfo.children || []);
										lanes.return = returnFiber;
										returnFiber = lanes;
										break a;
									} else {
										deleteRemainingChildren(returnFiber, currentFirstChild);
										break;
									}
									else deleteChild(returnFiber, currentFirstChild);
									currentFirstChild = currentFirstChild.sibling;
								}
								lanes = createFiberFromPortal(prevDebugInfo, returnFiber.mode, lanes);
								lanes.return = returnFiber;
								returnFiber = lanes;
							}
							return placeSingleChild(returnFiber);
						case REACT_LAZY_TYPE: return prevDebugInfo = pushDebugInfo(newChild._debugInfo), newChild = resolveLazy(newChild), returnFiber = reconcileChildFibersImpl(returnFiber, currentFirstChild, newChild, lanes), currentDebugInfo = prevDebugInfo, returnFiber;
					}
					if (isArrayImpl(newChild)) return prevDebugInfo = pushDebugInfo(newChild._debugInfo), returnFiber = reconcileChildrenArray(returnFiber, currentFirstChild, newChild, lanes), currentDebugInfo = prevDebugInfo, returnFiber;
					if (getIteratorFn(newChild)) {
						prevDebugInfo = pushDebugInfo(newChild._debugInfo);
						key = getIteratorFn(newChild);
						if ("function" !== typeof key) throw Error("An object is not an iterable. This error is likely caused by a bug in React. Please file an issue.");
						var newChildren = key.call(newChild);
						if (newChildren === newChild) {
							if (0 !== returnFiber.tag || "[object GeneratorFunction]" !== Object.prototype.toString.call(returnFiber.type) || "[object Generator]" !== Object.prototype.toString.call(newChildren)) didWarnAboutGenerators || console.error("Using Iterators as children is unsupported and will likely yield unexpected results because enumerating a generator mutates it. You may convert it to an array with `Array.from()` or the `[...spread]` operator before rendering. You can also use an Iterable that can iterate multiple times over the same items."), didWarnAboutGenerators = !0;
						} else newChild.entries !== key || didWarnAboutMaps || (console.error("Using Maps as children is not supported. Use an array of keyed ReactElements instead."), didWarnAboutMaps = !0);
						returnFiber = reconcileChildrenIterator(returnFiber, currentFirstChild, newChildren, lanes);
						currentDebugInfo = prevDebugInfo;
						return returnFiber;
					}
					if ("function" === typeof newChild.then) return prevDebugInfo = pushDebugInfo(newChild._debugInfo), returnFiber = reconcileChildFibersImpl(returnFiber, currentFirstChild, unwrapThenable(newChild), lanes), currentDebugInfo = prevDebugInfo, returnFiber;
					if (newChild.$$typeof === REACT_CONTEXT_TYPE) return reconcileChildFibersImpl(returnFiber, currentFirstChild, readContextDuringReconciliation(returnFiber, newChild), lanes);
					throwOnInvalidObjectType(returnFiber, newChild);
				}
				if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild) return prevDebugInfo = "" + newChild, null !== currentFirstChild && 6 === currentFirstChild.tag ? (deleteRemainingChildren(returnFiber, currentFirstChild.sibling), lanes = useFiber(currentFirstChild, prevDebugInfo), lanes.return = returnFiber, returnFiber = lanes) : (deleteRemainingChildren(returnFiber, currentFirstChild), lanes = createFiberFromText(prevDebugInfo, returnFiber.mode, lanes), lanes.return = returnFiber, lanes._debugOwner = returnFiber, lanes._debugTask = returnFiber._debugTask, lanes._debugInfo = currentDebugInfo, returnFiber = lanes), placeSingleChild(returnFiber);
				"function" === typeof newChild && warnOnFunctionType(returnFiber, newChild);
				"symbol" === typeof newChild && warnOnSymbolType(returnFiber, newChild);
				return deleteRemainingChildren(returnFiber, currentFirstChild);
			}
			return function(returnFiber, currentFirstChild, newChild, lanes) {
				var prevDebugInfo = currentDebugInfo;
				currentDebugInfo = null;
				try {
					thenableIndexCounter$1 = 0;
					var firstChildFiber = reconcileChildFibersImpl(returnFiber, currentFirstChild, newChild, lanes);
					thenableState$1 = null;
					return firstChildFiber;
				} catch (x) {
					if (x === SuspenseException || x === SuspenseActionException) throw x;
					var fiber = createFiber(29, x, null, returnFiber.mode);
					fiber.lanes = lanes;
					fiber.return = returnFiber;
					var debugInfo = fiber._debugInfo = currentDebugInfo;
					fiber._debugOwner = returnFiber._debugOwner;
					fiber._debugTask = returnFiber._debugTask;
					if (null != debugInfo) {
						for (var i = debugInfo.length - 1; 0 <= i; i--) if ("string" === typeof debugInfo[i].stack) {
							fiber._debugOwner = debugInfo[i];
							fiber._debugTask = debugInfo[i].debugTask;
							break;
						}
					}
					return fiber;
				} finally {
					currentDebugInfo = prevDebugInfo;
				}
			};
		}
		function validateSuspenseListNestedChild(childSlot, index) {
			var isAnArray = isArrayImpl(childSlot);
			childSlot = !isAnArray && "function" === typeof getIteratorFn(childSlot);
			return isAnArray || childSlot ? (isAnArray = isAnArray ? "array" : "iterable", console.error("A nested %s was passed to row #%s in <SuspenseList />. Wrap it in an additional SuspenseList to configure its revealOrder: <SuspenseList revealOrder=...> ... <SuspenseList revealOrder=...>{%s}</SuspenseList> ... </SuspenseList>", isAnArray, index, isAnArray), !1) : !0;
		}
		function initializeUpdateQueue(fiber) {
			fiber.updateQueue = {
				baseState: fiber.memoizedState,
				firstBaseUpdate: null,
				lastBaseUpdate: null,
				shared: {
					pending: null,
					lanes: 0,
					hiddenCallbacks: null
				},
				callbacks: null
			};
		}
		function cloneUpdateQueue(current, workInProgress) {
			current = current.updateQueue;
			workInProgress.updateQueue === current && (workInProgress.updateQueue = {
				baseState: current.baseState,
				firstBaseUpdate: current.firstBaseUpdate,
				lastBaseUpdate: current.lastBaseUpdate,
				shared: current.shared,
				callbacks: null
			});
		}
		function createUpdate(lane) {
			return {
				lane,
				tag: UpdateState,
				payload: null,
				callback: null,
				next: null
			};
		}
		function enqueueUpdate(fiber, update, lane) {
			var updateQueue = fiber.updateQueue;
			if (null === updateQueue) return null;
			updateQueue = updateQueue.shared;
			if (currentlyProcessingQueue === updateQueue && !didWarnUpdateInsideUpdate) {
				var componentName = getComponentNameFromFiber(fiber);
				console.error("An update (setState, replaceState, or forceUpdate) was scheduled from inside an update function. Update functions should be pure, with zero side-effects. Consider using componentDidUpdate or a callback.\n\nPlease update the following component: %s", componentName);
				didWarnUpdateInsideUpdate = !0;
			}
			if ((executionContext & RenderContext) !== NoContext) return componentName = updateQueue.pending, null === componentName ? update.next = update : (update.next = componentName.next, componentName.next = update), updateQueue.pending = update, update = getRootForUpdatedFiber(fiber), markUpdateLaneFromFiberToRoot(fiber, null, lane), update;
			enqueueUpdate$1(fiber, updateQueue, update, lane);
			return getRootForUpdatedFiber(fiber);
		}
		function entangleTransitions(root, fiber, lane) {
			fiber = fiber.updateQueue;
			if (null !== fiber && (fiber = fiber.shared, 0 !== (lane & 4194048))) {
				var queueLanes = fiber.lanes;
				queueLanes &= root.pendingLanes;
				lane |= queueLanes;
				fiber.lanes = lane;
				markRootEntangled(root, lane);
			}
		}
		function enqueueCapturedUpdate(workInProgress, capturedUpdate) {
			var queue = workInProgress.updateQueue, current = workInProgress.alternate;
			if (null !== current && (current = current.updateQueue, queue === current)) {
				var newFirst = null, newLast = null;
				queue = queue.firstBaseUpdate;
				if (null !== queue) {
					do {
						var clone = {
							lane: queue.lane,
							tag: queue.tag,
							payload: queue.payload,
							callback: null,
							next: null
						};
						null === newLast ? newFirst = newLast = clone : newLast = newLast.next = clone;
						queue = queue.next;
					} while (null !== queue);
					null === newLast ? newFirst = newLast = capturedUpdate : newLast = newLast.next = capturedUpdate;
				} else newFirst = newLast = capturedUpdate;
				queue = {
					baseState: current.baseState,
					firstBaseUpdate: newFirst,
					lastBaseUpdate: newLast,
					shared: current.shared,
					callbacks: current.callbacks
				};
				workInProgress.updateQueue = queue;
				return;
			}
			workInProgress = queue.lastBaseUpdate;
			null === workInProgress ? queue.firstBaseUpdate = capturedUpdate : workInProgress.next = capturedUpdate;
			queue.lastBaseUpdate = capturedUpdate;
		}
		function suspendIfUpdateReadFromEntangledAsyncAction() {
			if (didReadFromEntangledAsyncAction) {
				var entangledActionThenable = currentEntangledActionThenable;
				if (null !== entangledActionThenable) throw entangledActionThenable;
			}
		}
		function processUpdateQueue(workInProgress, props, instance$jscomp$0, renderLanes) {
			didReadFromEntangledAsyncAction = !1;
			var queue = workInProgress.updateQueue;
			hasForceUpdate = !1;
			currentlyProcessingQueue = queue.shared;
			var firstBaseUpdate = queue.firstBaseUpdate, lastBaseUpdate = queue.lastBaseUpdate, pendingQueue = queue.shared.pending;
			if (null !== pendingQueue) {
				queue.shared.pending = null;
				var lastPendingUpdate = pendingQueue, firstPendingUpdate = lastPendingUpdate.next;
				lastPendingUpdate.next = null;
				null === lastBaseUpdate ? firstBaseUpdate = firstPendingUpdate : lastBaseUpdate.next = firstPendingUpdate;
				lastBaseUpdate = lastPendingUpdate;
				var current = workInProgress.alternate;
				null !== current && (current = current.updateQueue, pendingQueue = current.lastBaseUpdate, pendingQueue !== lastBaseUpdate && (null === pendingQueue ? current.firstBaseUpdate = firstPendingUpdate : pendingQueue.next = firstPendingUpdate, current.lastBaseUpdate = lastPendingUpdate));
			}
			if (null !== firstBaseUpdate) {
				var newState = queue.baseState;
				lastBaseUpdate = 0;
				current = firstPendingUpdate = lastPendingUpdate = null;
				pendingQueue = firstBaseUpdate;
				do {
					var updateLane = pendingQueue.lane & -536870913, isHiddenUpdate = updateLane !== pendingQueue.lane;
					if (isHiddenUpdate ? (workInProgressRootRenderLanes & updateLane) === updateLane : (renderLanes & updateLane) === updateLane) {
						0 !== updateLane && updateLane === currentEntangledLane && (didReadFromEntangledAsyncAction = !0);
						null !== current && (current = current.next = {
							lane: 0,
							tag: pendingQueue.tag,
							payload: pendingQueue.payload,
							callback: null,
							next: null
						});
						a: {
							updateLane = workInProgress;
							var partialState = pendingQueue;
							var nextProps = props, instance = instance$jscomp$0;
							switch (partialState.tag) {
								case ReplaceState:
									partialState = partialState.payload;
									if ("function" === typeof partialState) {
										isDisallowedContextReadInDEV = !0;
										var nextState = partialState.call(instance, newState, nextProps);
										if (updateLane.mode & StrictLegacyMode) {
											setIsStrictModeForDevtools(!0);
											try {
												partialState.call(instance, newState, nextProps);
											} finally {
												setIsStrictModeForDevtools(!1);
											}
										}
										isDisallowedContextReadInDEV = !1;
										newState = nextState;
										break a;
									}
									newState = partialState;
									break a;
								case CaptureUpdate: updateLane.flags = updateLane.flags & -65537 | 128;
								case UpdateState:
									nextState = partialState.payload;
									if ("function" === typeof nextState) {
										isDisallowedContextReadInDEV = !0;
										partialState = nextState.call(instance, newState, nextProps);
										if (updateLane.mode & StrictLegacyMode) {
											setIsStrictModeForDevtools(!0);
											try {
												nextState.call(instance, newState, nextProps);
											} finally {
												setIsStrictModeForDevtools(!1);
											}
										}
										isDisallowedContextReadInDEV = !1;
									} else partialState = nextState;
									if (null === partialState || void 0 === partialState) break a;
									newState = assign({}, newState, partialState);
									break a;
								case ForceUpdate: hasForceUpdate = !0;
							}
						}
						updateLane = pendingQueue.callback;
						null !== updateLane && (workInProgress.flags |= 64, isHiddenUpdate && (workInProgress.flags |= 8192), isHiddenUpdate = queue.callbacks, null === isHiddenUpdate ? queue.callbacks = [updateLane] : isHiddenUpdate.push(updateLane));
					} else isHiddenUpdate = {
						lane: updateLane,
						tag: pendingQueue.tag,
						payload: pendingQueue.payload,
						callback: pendingQueue.callback,
						next: null
					}, null === current ? (firstPendingUpdate = current = isHiddenUpdate, lastPendingUpdate = newState) : current = current.next = isHiddenUpdate, lastBaseUpdate |= updateLane;
					pendingQueue = pendingQueue.next;
					if (null === pendingQueue) if (pendingQueue = queue.shared.pending, null === pendingQueue) break;
					else isHiddenUpdate = pendingQueue, pendingQueue = isHiddenUpdate.next, isHiddenUpdate.next = null, queue.lastBaseUpdate = isHiddenUpdate, queue.shared.pending = null;
				} while (1);
				null === current && (lastPendingUpdate = newState);
				queue.baseState = lastPendingUpdate;
				queue.firstBaseUpdate = firstPendingUpdate;
				queue.lastBaseUpdate = current;
				null === firstBaseUpdate && (queue.shared.lanes = 0);
				workInProgressRootSkippedLanes |= lastBaseUpdate;
				workInProgress.lanes = lastBaseUpdate;
				workInProgress.memoizedState = newState;
			}
			currentlyProcessingQueue = null;
		}
		function callCallback(callback, context) {
			if ("function" !== typeof callback) throw Error("Invalid argument passed as callback. Expected a function. Instead received: " + callback);
			callback.call(context);
		}
		function commitHiddenCallbacks(updateQueue, context) {
			var hiddenCallbacks = updateQueue.shared.hiddenCallbacks;
			if (null !== hiddenCallbacks) for (updateQueue.shared.hiddenCallbacks = null, updateQueue = 0; updateQueue < hiddenCallbacks.length; updateQueue++) callCallback(hiddenCallbacks[updateQueue], context);
		}
		function commitCallbacks(updateQueue, context) {
			var callbacks = updateQueue.callbacks;
			if (null !== callbacks) for (updateQueue.callbacks = null, updateQueue = 0; updateQueue < callbacks.length; updateQueue++) callCallback(callbacks[updateQueue], context);
		}
		function pushHiddenContext(fiber, context) {
			var prevEntangledRenderLanes = entangledRenderLanes;
			push(prevEntangledRenderLanesCursor, prevEntangledRenderLanes, fiber);
			push(currentTreeHiddenStackCursor, context, fiber);
			entangledRenderLanes = prevEntangledRenderLanes | context.baseLanes;
		}
		function reuseHiddenContextOnStack(fiber) {
			push(prevEntangledRenderLanesCursor, entangledRenderLanes, fiber);
			push(currentTreeHiddenStackCursor, currentTreeHiddenStackCursor.current, fiber);
		}
		function popHiddenContext(fiber) {
			entangledRenderLanes = prevEntangledRenderLanesCursor.current;
			pop(currentTreeHiddenStackCursor, fiber);
			pop(prevEntangledRenderLanesCursor, fiber);
		}
		function pushPrimaryTreeSuspenseHandler(handler) {
			var current = handler.alternate;
			push(suspenseStackCursor, suspenseStackCursor.current & SubtreeSuspenseContextMask, handler);
			push(suspenseHandlerStackCursor, handler, handler);
			null === shellBoundary && (null === current || null !== currentTreeHiddenStackCursor.current ? shellBoundary = handler : null !== current.memoizedState && (shellBoundary = handler));
		}
		function pushDehydratedActivitySuspenseHandler(fiber) {
			push(suspenseStackCursor, suspenseStackCursor.current, fiber);
			push(suspenseHandlerStackCursor, fiber, fiber);
			null === shellBoundary && (shellBoundary = fiber);
		}
		function pushOffscreenSuspenseHandler(fiber) {
			22 === fiber.tag ? (push(suspenseStackCursor, suspenseStackCursor.current, fiber), push(suspenseHandlerStackCursor, fiber, fiber), null === shellBoundary && (shellBoundary = fiber)) : reuseSuspenseHandlerOnStack(fiber);
		}
		function reuseSuspenseHandlerOnStack(fiber) {
			push(suspenseStackCursor, suspenseStackCursor.current, fiber);
			push(suspenseHandlerStackCursor, suspenseHandlerStackCursor.current, fiber);
		}
		function popSuspenseHandler(fiber) {
			pop(suspenseHandlerStackCursor, fiber);
			shellBoundary === fiber && (shellBoundary = null);
			pop(suspenseStackCursor, fiber);
		}
		function findFirstSuspended(row) {
			for (var node = row; null !== node;) {
				if (13 === node.tag) {
					var state = node.memoizedState;
					if (null !== state && (state = state.dehydrated, null === state || isSuspenseInstancePending(state) || isSuspenseInstanceFallback(state))) return node;
				} else if (19 === node.tag && ("forwards" === node.memoizedProps.revealOrder || "backwards" === node.memoizedProps.revealOrder || "unstable_legacy-backwards" === node.memoizedProps.revealOrder || "together" === node.memoizedProps.revealOrder)) {
					if (0 !== (node.flags & 128)) return node;
				} else if (null !== node.child) {
					node.child.return = node;
					node = node.child;
					continue;
				}
				if (node === row) break;
				for (; null === node.sibling;) {
					if (null === node.return || node.return === row) return null;
					node = node.return;
				}
				node.sibling.return = node.return;
				node = node.sibling;
			}
			return null;
		}
		function mountHookTypesDev() {
			var hookName = currentHookNameInDev;
			null === hookTypesDev ? hookTypesDev = [hookName] : hookTypesDev.push(hookName);
		}
		function updateHookTypesDev() {
			var hookName = currentHookNameInDev;
			if (null !== hookTypesDev && (hookTypesUpdateIndexDev++, hookTypesDev[hookTypesUpdateIndexDev] !== hookName)) {
				var componentName = getComponentNameFromFiber(currentlyRenderingFiber);
				if (!didWarnAboutMismatchedHooksForComponent.has(componentName) && (didWarnAboutMismatchedHooksForComponent.add(componentName), null !== hookTypesDev)) {
					for (var table = "", i = 0; i <= hookTypesUpdateIndexDev; i++) {
						var oldHookName = hookTypesDev[i], newHookName = i === hookTypesUpdateIndexDev ? hookName : oldHookName;
						for (oldHookName = i + 1 + ". " + oldHookName; 30 > oldHookName.length;) oldHookName += " ";
						oldHookName += newHookName + "\n";
						table += oldHookName;
					}
					console.error("React has detected a change in the order of Hooks called by %s. This will lead to bugs and errors if not fixed. For more information, read the Rules of Hooks: https://react.dev/link/rules-of-hooks\n\n   Previous render            Next render\n   ------------------------------------------------------\n%s   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n", componentName, table);
				}
			}
		}
		function checkDepsAreArrayDev(deps) {
			void 0 === deps || null === deps || isArrayImpl(deps) || console.error("%s received a final argument that is not an array (instead, received `%s`). When specified, the final argument must be an array.", currentHookNameInDev, typeof deps);
		}
		function warnOnUseFormStateInDev() {
			var componentName = getComponentNameFromFiber(currentlyRenderingFiber);
			didWarnAboutUseFormState.has(componentName) || (didWarnAboutUseFormState.add(componentName), console.error("ReactDOM.useFormState has been renamed to React.useActionState. Please update %s to use React.useActionState.", componentName));
		}
		function throwInvalidHookError() {
			throw Error("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://react.dev/link/invalid-hook-call for tips about how to debug and fix this problem.");
		}
		function areHookInputsEqual(nextDeps, prevDeps) {
			if (ignorePreviousDependencies) return !1;
			if (null === prevDeps) return console.error("%s received a final argument during this render, but not during the previous render. Even though the final argument is optional, its type cannot change between renders.", currentHookNameInDev), !1;
			nextDeps.length !== prevDeps.length && console.error("The final argument passed to %s changed size between renders. The order and size of this array must remain constant.\n\nPrevious: %s\nIncoming: %s", currentHookNameInDev, "[" + prevDeps.join(", ") + "]", "[" + nextDeps.join(", ") + "]");
			for (var i = 0; i < prevDeps.length && i < nextDeps.length; i++) if (!objectIs(nextDeps[i], prevDeps[i])) return !1;
			return !0;
		}
		function renderWithHooks(current, workInProgress, Component, props, secondArg, nextRenderLanes) {
			renderLanes = nextRenderLanes;
			currentlyRenderingFiber = workInProgress;
			hookTypesDev = null !== current ? current._debugHookTypes : null;
			hookTypesUpdateIndexDev = -1;
			ignorePreviousDependencies = null !== current && current.type !== workInProgress.type;
			if ("[object AsyncFunction]" === Object.prototype.toString.call(Component) || "[object AsyncGeneratorFunction]" === Object.prototype.toString.call(Component)) nextRenderLanes = getComponentNameFromFiber(currentlyRenderingFiber), didWarnAboutAsyncClientComponent.has(nextRenderLanes) || (didWarnAboutAsyncClientComponent.add(nextRenderLanes), console.error("%s is an async Client Component. Only Server Components can be async at the moment. This error is often caused by accidentally adding `'use client'` to a module that was originally written for the server.", null === nextRenderLanes ? "An unknown Component" : "<" + nextRenderLanes + ">"));
			workInProgress.memoizedState = null;
			workInProgress.updateQueue = null;
			workInProgress.lanes = 0;
			ReactSharedInternals.H = null !== current && null !== current.memoizedState ? HooksDispatcherOnUpdateInDEV : null !== hookTypesDev ? HooksDispatcherOnMountWithHookTypesInDEV : HooksDispatcherOnMountInDEV;
			shouldDoubleInvokeUserFnsInHooksDEV = nextRenderLanes = (workInProgress.mode & StrictLegacyMode) !== NoMode;
			var children = callComponentInDEV(Component, props, secondArg);
			shouldDoubleInvokeUserFnsInHooksDEV = !1;
			didScheduleRenderPhaseUpdateDuringThisPass && (children = renderWithHooksAgain(workInProgress, Component, props, secondArg));
			if (nextRenderLanes) {
				setIsStrictModeForDevtools(!0);
				try {
					children = renderWithHooksAgain(workInProgress, Component, props, secondArg);
				} finally {
					setIsStrictModeForDevtools(!1);
				}
			}
			finishRenderingHooks(current, workInProgress);
			return children;
		}
		function finishRenderingHooks(current, workInProgress) {
			workInProgress._debugHookTypes = hookTypesDev;
			null === workInProgress.dependencies ? null !== thenableState && (workInProgress.dependencies = {
				lanes: 0,
				firstContext: null,
				_debugThenableState: thenableState
			}) : workInProgress.dependencies._debugThenableState = thenableState;
			ReactSharedInternals.H = ContextOnlyDispatcher;
			var didRenderTooFewHooks = null !== currentHook && null !== currentHook.next;
			renderLanes = 0;
			hookTypesDev = currentHookNameInDev = workInProgressHook = currentHook = currentlyRenderingFiber = null;
			hookTypesUpdateIndexDev = -1;
			null !== current && (current.flags & 65011712) !== (workInProgress.flags & 65011712) && console.error("Internal React error: Expected static flag was missing. Please notify the React team.");
			didScheduleRenderPhaseUpdate = !1;
			thenableIndexCounter = 0;
			thenableState = null;
			if (didRenderTooFewHooks) throw Error("Rendered fewer hooks than expected. This may be caused by an accidental early return statement.");
			null === current || didReceiveUpdate || (current = current.dependencies, null !== current && checkIfContextChanged(current) && (didReceiveUpdate = !0));
			needsToResetSuspendedThenableDEV ? (needsToResetSuspendedThenableDEV = !1, current = !0) : current = !1;
			current && (workInProgress = getComponentNameFromFiber(workInProgress) || "Unknown", didWarnAboutUseWrappedInTryCatch.has(workInProgress) || didWarnAboutAsyncClientComponent.has(workInProgress) || (didWarnAboutUseWrappedInTryCatch.add(workInProgress), console.error("`use` was called from inside a try/catch block. This is not allowed and can lead to unexpected behavior. To handle errors triggered by `use`, wrap your component in a error boundary.")));
		}
		function renderWithHooksAgain(workInProgress, Component, props, secondArg) {
			currentlyRenderingFiber = workInProgress;
			var numberOfReRenders = 0;
			do {
				didScheduleRenderPhaseUpdateDuringThisPass && (thenableState = null);
				thenableIndexCounter = 0;
				didScheduleRenderPhaseUpdateDuringThisPass = !1;
				if (numberOfReRenders >= RE_RENDER_LIMIT) throw Error("Too many re-renders. React limits the number of renders to prevent an infinite loop.");
				numberOfReRenders += 1;
				ignorePreviousDependencies = !1;
				workInProgressHook = currentHook = null;
				if (null != workInProgress.updateQueue) {
					var children = workInProgress.updateQueue;
					children.lastEffect = null;
					children.events = null;
					children.stores = null;
					null != children.memoCache && (children.memoCache.index = 0);
				}
				hookTypesUpdateIndexDev = -1;
				ReactSharedInternals.H = HooksDispatcherOnRerenderInDEV;
				children = callComponentInDEV(Component, props, secondArg);
			} while (didScheduleRenderPhaseUpdateDuringThisPass);
			return children;
		}
		function TransitionAwareHostComponent() {
			var dispatcher = ReactSharedInternals.H, maybeThenable = dispatcher.useState()[0];
			maybeThenable = "function" === typeof maybeThenable.then ? useThenable(maybeThenable) : maybeThenable;
			dispatcher = dispatcher.useState()[0];
			(null !== currentHook ? currentHook.memoizedState : null) !== dispatcher && (currentlyRenderingFiber.flags |= 1024);
			return maybeThenable;
		}
		function checkDidRenderIdHook() {
			var didRenderIdHook = 0 !== localIdCounter;
			localIdCounter = 0;
			return didRenderIdHook;
		}
		function bailoutHooks(current, workInProgress, lanes) {
			workInProgress.updateQueue = current.updateQueue;
			workInProgress.flags = (workInProgress.mode & StrictEffectsMode) !== NoMode ? workInProgress.flags & -402655237 : workInProgress.flags & -2053;
			current.lanes &= ~lanes;
		}
		function resetHooksOnUnwind(workInProgress) {
			if (didScheduleRenderPhaseUpdate) {
				for (workInProgress = workInProgress.memoizedState; null !== workInProgress;) {
					var queue = workInProgress.queue;
					null !== queue && (queue.pending = null);
					workInProgress = workInProgress.next;
				}
				didScheduleRenderPhaseUpdate = !1;
			}
			renderLanes = 0;
			hookTypesDev = workInProgressHook = currentHook = currentlyRenderingFiber = null;
			hookTypesUpdateIndexDev = -1;
			currentHookNameInDev = null;
			didScheduleRenderPhaseUpdateDuringThisPass = !1;
			thenableIndexCounter = localIdCounter = 0;
			thenableState = null;
		}
		function mountWorkInProgressHook() {
			var hook = {
				memoizedState: null,
				baseState: null,
				baseQueue: null,
				queue: null,
				next: null
			};
			null === workInProgressHook ? currentlyRenderingFiber.memoizedState = workInProgressHook = hook : workInProgressHook = workInProgressHook.next = hook;
			return workInProgressHook;
		}
		function updateWorkInProgressHook() {
			if (null === currentHook) {
				var nextCurrentHook = currentlyRenderingFiber.alternate;
				nextCurrentHook = null !== nextCurrentHook ? nextCurrentHook.memoizedState : null;
			} else nextCurrentHook = currentHook.next;
			var nextWorkInProgressHook = null === workInProgressHook ? currentlyRenderingFiber.memoizedState : workInProgressHook.next;
			if (null !== nextWorkInProgressHook) workInProgressHook = nextWorkInProgressHook, currentHook = nextCurrentHook;
			else {
				if (null === nextCurrentHook) {
					if (null === currentlyRenderingFiber.alternate) throw Error("Update hook called on initial render. This is likely a bug in React. Please file an issue.");
					throw Error("Rendered more hooks than during the previous render.");
				}
				currentHook = nextCurrentHook;
				nextCurrentHook = {
					memoizedState: currentHook.memoizedState,
					baseState: currentHook.baseState,
					baseQueue: currentHook.baseQueue,
					queue: currentHook.queue,
					next: null
				};
				null === workInProgressHook ? currentlyRenderingFiber.memoizedState = workInProgressHook = nextCurrentHook : workInProgressHook = workInProgressHook.next = nextCurrentHook;
			}
			return workInProgressHook;
		}
		function createFunctionComponentUpdateQueue() {
			return {
				lastEffect: null,
				events: null,
				stores: null,
				memoCache: null
			};
		}
		function useThenable(thenable) {
			var index = thenableIndexCounter;
			thenableIndexCounter += 1;
			null === thenableState && (thenableState = createThenableState());
			thenable = trackUsedThenable(thenableState, thenable, index);
			index = currentlyRenderingFiber;
			null === (null === workInProgressHook ? index.memoizedState : workInProgressHook.next) && (index = index.alternate, ReactSharedInternals.H = null !== index && null !== index.memoizedState ? HooksDispatcherOnUpdateInDEV : HooksDispatcherOnMountInDEV);
			return thenable;
		}
		function use(usable) {
			if (null !== usable && "object" === typeof usable) {
				if ("function" === typeof usable.then) return useThenable(usable);
				if (usable.$$typeof === REACT_CONTEXT_TYPE) return readContext(usable);
			}
			throw Error("An unsupported type was passed to use(): " + String(usable));
		}
		function useMemoCache(size) {
			var memoCache = null, updateQueue = currentlyRenderingFiber.updateQueue;
			null !== updateQueue && (memoCache = updateQueue.memoCache);
			if (null == memoCache) {
				var current = currentlyRenderingFiber.alternate;
				null !== current && (current = current.updateQueue, null !== current && (current = current.memoCache, null != current && (memoCache = {
					data: current.data.map(function(array) {
						return array.slice();
					}),
					index: 0
				})));
			}
			memoCache ??= {
				data: [],
				index: 0
			};
			null === updateQueue && (updateQueue = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = updateQueue);
			updateQueue.memoCache = memoCache;
			updateQueue = memoCache.data[memoCache.index];
			if (void 0 === updateQueue || ignorePreviousDependencies) for (updateQueue = memoCache.data[memoCache.index] = Array(size), current = 0; current < size; current++) updateQueue[current] = REACT_MEMO_CACHE_SENTINEL;
			else updateQueue.length !== size && console.error("Expected a constant size argument for each invocation of useMemoCache. The previous cache was allocated with size %s but size %s was requested.", updateQueue.length, size);
			memoCache.index++;
			return updateQueue;
		}
		function basicStateReducer(state, action) {
			return "function" === typeof action ? action(state) : action;
		}
		function mountReducer(reducer, initialArg, init) {
			var hook = mountWorkInProgressHook();
			if (void 0 !== init) {
				var initialState = init(initialArg);
				if (shouldDoubleInvokeUserFnsInHooksDEV) {
					setIsStrictModeForDevtools(!0);
					try {
						init(initialArg);
					} finally {
						setIsStrictModeForDevtools(!1);
					}
				}
			} else initialState = initialArg;
			hook.memoizedState = hook.baseState = initialState;
			reducer = {
				pending: null,
				lanes: 0,
				dispatch: null,
				lastRenderedReducer: reducer,
				lastRenderedState: initialState
			};
			hook.queue = reducer;
			reducer = reducer.dispatch = dispatchReducerAction.bind(null, currentlyRenderingFiber, reducer);
			return [hook.memoizedState, reducer];
		}
		function updateReducer(reducer) {
			return updateReducerImpl(updateWorkInProgressHook(), currentHook, reducer);
		}
		function updateReducerImpl(hook, current, reducer) {
			var queue = hook.queue;
			if (null === queue) throw Error("Should have a queue. You are likely calling Hooks conditionally, which is not allowed. (https://react.dev/link/invalid-hook-call)");
			queue.lastRenderedReducer = reducer;
			var baseQueue = hook.baseQueue, pendingQueue = queue.pending;
			if (null !== pendingQueue) {
				if (null !== baseQueue) {
					var baseFirst = baseQueue.next;
					baseQueue.next = pendingQueue.next;
					pendingQueue.next = baseFirst;
				}
				current.baseQueue !== baseQueue && console.error("Internal error: Expected work-in-progress queue to be a clone. This is a bug in React.");
				current.baseQueue = baseQueue = pendingQueue;
				queue.pending = null;
			}
			pendingQueue = hook.baseState;
			if (null === baseQueue) hook.memoizedState = pendingQueue;
			else {
				current = baseQueue.next;
				var newBaseQueueFirst = baseFirst = null, newBaseQueueLast = null, update = current, didReadFromEntangledAsyncAction = !1;
				do {
					var updateLane = update.lane & -536870913;
					if (updateLane !== update.lane ? (workInProgressRootRenderLanes & updateLane) === updateLane : (renderLanes & updateLane) === updateLane) {
						var revertLane = update.revertLane;
						if (0 === revertLane) null !== newBaseQueueLast && (newBaseQueueLast = newBaseQueueLast.next = {
							lane: 0,
							revertLane: 0,
							gesture: null,
							action: update.action,
							hasEagerState: update.hasEagerState,
							eagerState: update.eagerState,
							next: null
						}), updateLane === currentEntangledLane && (didReadFromEntangledAsyncAction = !0);
						else if ((renderLanes & revertLane) === revertLane) {
							update = update.next;
							revertLane === currentEntangledLane && (didReadFromEntangledAsyncAction = !0);
							continue;
						} else updateLane = {
							lane: 0,
							revertLane: update.revertLane,
							gesture: null,
							action: update.action,
							hasEagerState: update.hasEagerState,
							eagerState: update.eagerState,
							next: null
						}, null === newBaseQueueLast ? (newBaseQueueFirst = newBaseQueueLast = updateLane, baseFirst = pendingQueue) : newBaseQueueLast = newBaseQueueLast.next = updateLane, currentlyRenderingFiber.lanes |= revertLane, workInProgressRootSkippedLanes |= revertLane;
						updateLane = update.action;
						shouldDoubleInvokeUserFnsInHooksDEV && reducer(pendingQueue, updateLane);
						pendingQueue = update.hasEagerState ? update.eagerState : reducer(pendingQueue, updateLane);
					} else revertLane = {
						lane: updateLane,
						revertLane: update.revertLane,
						gesture: update.gesture,
						action: update.action,
						hasEagerState: update.hasEagerState,
						eagerState: update.eagerState,
						next: null
					}, null === newBaseQueueLast ? (newBaseQueueFirst = newBaseQueueLast = revertLane, baseFirst = pendingQueue) : newBaseQueueLast = newBaseQueueLast.next = revertLane, currentlyRenderingFiber.lanes |= updateLane, workInProgressRootSkippedLanes |= updateLane;
					update = update.next;
				} while (null !== update && update !== current);
				null === newBaseQueueLast ? baseFirst = pendingQueue : newBaseQueueLast.next = newBaseQueueFirst;
				if (!objectIs(pendingQueue, hook.memoizedState) && (didReceiveUpdate = !0, didReadFromEntangledAsyncAction && (reducer = currentEntangledActionThenable, null !== reducer))) throw reducer;
				hook.memoizedState = pendingQueue;
				hook.baseState = baseFirst;
				hook.baseQueue = newBaseQueueLast;
				queue.lastRenderedState = pendingQueue;
			}
			null === baseQueue && (queue.lanes = 0);
			return [hook.memoizedState, queue.dispatch];
		}
		function rerenderReducer(reducer) {
			var hook = updateWorkInProgressHook(), queue = hook.queue;
			if (null === queue) throw Error("Should have a queue. You are likely calling Hooks conditionally, which is not allowed. (https://react.dev/link/invalid-hook-call)");
			queue.lastRenderedReducer = reducer;
			var dispatch = queue.dispatch, lastRenderPhaseUpdate = queue.pending, newState = hook.memoizedState;
			if (null !== lastRenderPhaseUpdate) {
				queue.pending = null;
				var update = lastRenderPhaseUpdate = lastRenderPhaseUpdate.next;
				do
					newState = reducer(newState, update.action), update = update.next;
				while (update !== lastRenderPhaseUpdate);
				objectIs(newState, hook.memoizedState) || (didReceiveUpdate = !0);
				hook.memoizedState = newState;
				null === hook.baseQueue && (hook.baseState = newState);
				queue.lastRenderedState = newState;
			}
			return [newState, dispatch];
		}
		function mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
			var fiber = currentlyRenderingFiber, hook = mountWorkInProgressHook();
			if (isHydrating) {
				if (void 0 === getServerSnapshot) throw Error("Missing getServerSnapshot, which is required for server-rendered content. Will revert to client rendering.");
				var nextSnapshot = getServerSnapshot();
				didWarnUncachedGetSnapshot || nextSnapshot === getServerSnapshot() || (console.error("The result of getServerSnapshot should be cached to avoid an infinite loop"), didWarnUncachedGetSnapshot = !0);
			} else {
				nextSnapshot = getSnapshot();
				didWarnUncachedGetSnapshot || (getServerSnapshot = getSnapshot(), objectIs(nextSnapshot, getServerSnapshot) || (console.error("The result of getSnapshot should be cached to avoid an infinite loop"), didWarnUncachedGetSnapshot = !0));
				if (null === workInProgressRoot) throw Error("Expected a work-in-progress root. This is a bug in React. Please file an issue.");
				0 !== (workInProgressRootRenderLanes & 127) || pushStoreConsistencyCheck(fiber, getSnapshot, nextSnapshot);
			}
			hook.memoizedState = nextSnapshot;
			getServerSnapshot = {
				value: nextSnapshot,
				getSnapshot
			};
			hook.queue = getServerSnapshot;
			mountEffect(subscribeToStore.bind(null, fiber, getServerSnapshot, subscribe), [subscribe]);
			fiber.flags |= 2048;
			pushSimpleEffect(HasEffect | Passive, { destroy: void 0 }, updateStoreInstance.bind(null, fiber, getServerSnapshot, nextSnapshot, getSnapshot), null);
			return nextSnapshot;
		}
		function updateSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
			var fiber = currentlyRenderingFiber, hook = updateWorkInProgressHook(), isHydrating$jscomp$0 = isHydrating;
			if (isHydrating$jscomp$0) {
				if (void 0 === getServerSnapshot) throw Error("Missing getServerSnapshot, which is required for server-rendered content. Will revert to client rendering.");
				getServerSnapshot = getServerSnapshot();
			} else if (getServerSnapshot = getSnapshot(), !didWarnUncachedGetSnapshot) {
				var cachedSnapshot = getSnapshot();
				objectIs(getServerSnapshot, cachedSnapshot) || (console.error("The result of getSnapshot should be cached to avoid an infinite loop"), didWarnUncachedGetSnapshot = !0);
			}
			if (cachedSnapshot = !objectIs((currentHook || hook).memoizedState, getServerSnapshot)) hook.memoizedState = getServerSnapshot, didReceiveUpdate = !0;
			hook = hook.queue;
			updateEffectImpl(2048, Passive, subscribeToStore.bind(null, fiber, hook, subscribe), [subscribe]);
			if (hook.getSnapshot !== getSnapshot || cachedSnapshot || null !== workInProgressHook && workInProgressHook.memoizedState.tag & HasEffect) {
				fiber.flags |= 2048;
				pushSimpleEffect(HasEffect | Passive, { destroy: void 0 }, updateStoreInstance.bind(null, fiber, hook, getServerSnapshot, getSnapshot), null);
				if (null === workInProgressRoot) throw Error("Expected a work-in-progress root. This is a bug in React. Please file an issue.");
				isHydrating$jscomp$0 || 0 !== (renderLanes & 127) || pushStoreConsistencyCheck(fiber, getSnapshot, getServerSnapshot);
			}
			return getServerSnapshot;
		}
		function pushStoreConsistencyCheck(fiber, getSnapshot, renderedSnapshot) {
			fiber.flags |= 16384;
			fiber = {
				getSnapshot,
				value: renderedSnapshot
			};
			getSnapshot = currentlyRenderingFiber.updateQueue;
			null === getSnapshot ? (getSnapshot = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = getSnapshot, getSnapshot.stores = [fiber]) : (renderedSnapshot = getSnapshot.stores, null === renderedSnapshot ? getSnapshot.stores = [fiber] : renderedSnapshot.push(fiber));
		}
		function updateStoreInstance(fiber, inst, nextSnapshot, getSnapshot) {
			inst.value = nextSnapshot;
			inst.getSnapshot = getSnapshot;
			checkIfSnapshotChanged(inst) && forceStoreRerender(fiber);
		}
		function subscribeToStore(fiber, inst, subscribe) {
			return subscribe(function() {
				checkIfSnapshotChanged(inst) && (startUpdateTimerByLane(2, "updateSyncExternalStore()", fiber), forceStoreRerender(fiber));
			});
		}
		function checkIfSnapshotChanged(inst) {
			var latestGetSnapshot = inst.getSnapshot;
			inst = inst.value;
			try {
				var nextValue = latestGetSnapshot();
				return !objectIs(inst, nextValue);
			} catch (error) {
				return !0;
			}
		}
		function forceStoreRerender(fiber) {
			var root = enqueueConcurrentRenderForLane(fiber, 2);
			null !== root && scheduleUpdateOnFiber(root, fiber, 2);
		}
		function mountStateImpl(initialState) {
			var hook = mountWorkInProgressHook();
			if ("function" === typeof initialState) {
				var initialStateInitializer = initialState;
				initialState = initialStateInitializer();
				if (shouldDoubleInvokeUserFnsInHooksDEV) {
					setIsStrictModeForDevtools(!0);
					try {
						initialStateInitializer();
					} finally {
						setIsStrictModeForDevtools(!1);
					}
				}
			}
			hook.memoizedState = hook.baseState = initialState;
			hook.queue = {
				pending: null,
				lanes: 0,
				dispatch: null,
				lastRenderedReducer: basicStateReducer,
				lastRenderedState: initialState
			};
			return hook;
		}
		function mountState(initialState) {
			initialState = mountStateImpl(initialState);
			var queue = initialState.queue, dispatch = dispatchSetState.bind(null, currentlyRenderingFiber, queue);
			queue.dispatch = dispatch;
			return [initialState.memoizedState, dispatch];
		}
		function mountOptimistic(passthrough) {
			var hook = mountWorkInProgressHook();
			hook.memoizedState = hook.baseState = passthrough;
			var queue = {
				pending: null,
				lanes: 0,
				dispatch: null,
				lastRenderedReducer: null,
				lastRenderedState: null
			};
			hook.queue = queue;
			hook = dispatchOptimisticSetState.bind(null, currentlyRenderingFiber, !0, queue);
			queue.dispatch = hook;
			return [passthrough, hook];
		}
		function updateOptimistic(passthrough, reducer) {
			return updateOptimisticImpl(updateWorkInProgressHook(), currentHook, passthrough, reducer);
		}
		function updateOptimisticImpl(hook, current, passthrough, reducer) {
			hook.baseState = passthrough;
			return updateReducerImpl(hook, currentHook, "function" === typeof reducer ? reducer : basicStateReducer);
		}
		function rerenderOptimistic(passthrough, reducer) {
			var hook = updateWorkInProgressHook();
			if (null !== currentHook) return updateOptimisticImpl(hook, currentHook, passthrough, reducer);
			hook.baseState = passthrough;
			return [passthrough, hook.queue.dispatch];
		}
		function dispatchActionState(fiber, actionQueue, setPendingState, setState, payload) {
			if (isRenderPhaseUpdate(fiber)) throw Error("Cannot update form state while rendering.");
			fiber = actionQueue.action;
			if (null !== fiber) {
				var actionNode = {
					payload,
					action: fiber,
					next: null,
					isTransition: !0,
					status: "pending",
					value: null,
					reason: null,
					listeners: [],
					then: function(listener) {
						actionNode.listeners.push(listener);
					}
				};
				null !== ReactSharedInternals.T ? setPendingState(!0) : actionNode.isTransition = !1;
				setState(actionNode);
				setPendingState = actionQueue.pending;
				null === setPendingState ? (actionNode.next = actionQueue.pending = actionNode, runActionStateAction(actionQueue, actionNode)) : (actionNode.next = setPendingState.next, actionQueue.pending = setPendingState.next = actionNode);
			}
		}
		function runActionStateAction(actionQueue, node) {
			var action = node.action, payload = node.payload, prevState = actionQueue.state;
			if (node.isTransition) {
				var prevTransition = ReactSharedInternals.T, currentTransition = {};
				currentTransition._updatedFibers = /* @__PURE__ */ new Set();
				ReactSharedInternals.T = currentTransition;
				try {
					var returnValue = action(prevState, payload), onStartTransitionFinish = ReactSharedInternals.S;
					null !== onStartTransitionFinish && onStartTransitionFinish(currentTransition, returnValue);
					handleActionReturnValue(actionQueue, node, returnValue);
				} catch (error) {
					onActionError(actionQueue, node, error);
				} finally {
					null !== prevTransition && null !== currentTransition.types && (null !== prevTransition.types && prevTransition.types !== currentTransition.types && console.error("We expected inner Transitions to have transferred the outer types set and that you cannot add to the outer Transition while inside the inner.This is a bug in React."), prevTransition.types = currentTransition.types), ReactSharedInternals.T = prevTransition, null === prevTransition && currentTransition._updatedFibers && (actionQueue = currentTransition._updatedFibers.size, currentTransition._updatedFibers.clear(), 10 < actionQueue && console.warn("Detected a large number of updates inside startTransition. If this is due to a subscription please re-write it to use React provided hooks. Otherwise concurrent mode guarantees are off the table."));
				}
			} else try {
				currentTransition = action(prevState, payload), handleActionReturnValue(actionQueue, node, currentTransition);
			} catch (error$4) {
				onActionError(actionQueue, node, error$4);
			}
		}
		function handleActionReturnValue(actionQueue, node, returnValue) {
			null !== returnValue && "object" === typeof returnValue && "function" === typeof returnValue.then ? (ReactSharedInternals.asyncTransitions++, returnValue.then(releaseAsyncTransition, releaseAsyncTransition), returnValue.then(function(nextState) {
				onActionSuccess(actionQueue, node, nextState);
			}, function(error) {
				return onActionError(actionQueue, node, error);
			}), node.isTransition || console.error("An async function with useActionState was called outside of a transition. This is likely not what you intended (for example, isPending will not update correctly). Either call the returned function inside startTransition, or pass it to an `action` or `formAction` prop.")) : onActionSuccess(actionQueue, node, returnValue);
		}
		function onActionSuccess(actionQueue, actionNode, nextState) {
			actionNode.status = "fulfilled";
			actionNode.value = nextState;
			notifyActionListeners(actionNode);
			actionQueue.state = nextState;
			actionNode = actionQueue.pending;
			null !== actionNode && (nextState = actionNode.next, nextState === actionNode ? actionQueue.pending = null : (nextState = nextState.next, actionNode.next = nextState, runActionStateAction(actionQueue, nextState)));
		}
		function onActionError(actionQueue, actionNode, error) {
			var last = actionQueue.pending;
			actionQueue.pending = null;
			if (null !== last) {
				last = last.next;
				do
					actionNode.status = "rejected", actionNode.reason = error, notifyActionListeners(actionNode), actionNode = actionNode.next;
				while (actionNode !== last);
			}
			actionQueue.action = null;
		}
		function notifyActionListeners(actionNode) {
			actionNode = actionNode.listeners;
			for (var i = 0; i < actionNode.length; i++) (0, actionNode[i])();
		}
		function actionStateReducer(oldState, newState) {
			return newState;
		}
		function mountActionState(action, initialStateProp) {
			if (isHydrating) {
				var ssrFormState = workInProgressRoot.formState;
				if (null !== ssrFormState) {
					a: {
						var isMatching = currentlyRenderingFiber;
						if (isHydrating) {
							if (nextHydratableInstance) {
								b: {
									var markerInstance = nextHydratableInstance;
									for (var inRootOrSingleton = rootOrSingletonContext; 8 !== markerInstance.nodeType;) {
										if (!inRootOrSingleton) {
											markerInstance = null;
											break b;
										}
										markerInstance = getNextHydratable(markerInstance.nextSibling);
										if (null === markerInstance) {
											markerInstance = null;
											break b;
										}
									}
									inRootOrSingleton = markerInstance.data;
									markerInstance = inRootOrSingleton === FORM_STATE_IS_MATCHING || inRootOrSingleton === FORM_STATE_IS_NOT_MATCHING ? markerInstance : null;
								}
								if (markerInstance) {
									nextHydratableInstance = getNextHydratable(markerInstance.nextSibling);
									isMatching = markerInstance.data === FORM_STATE_IS_MATCHING;
									break a;
								}
							}
							throwOnHydrationMismatch(isMatching);
						}
						isMatching = !1;
					}
					isMatching && (initialStateProp = ssrFormState[0]);
				}
			}
			ssrFormState = mountWorkInProgressHook();
			ssrFormState.memoizedState = ssrFormState.baseState = initialStateProp;
			isMatching = {
				pending: null,
				lanes: 0,
				dispatch: null,
				lastRenderedReducer: actionStateReducer,
				lastRenderedState: initialStateProp
			};
			ssrFormState.queue = isMatching;
			ssrFormState = dispatchSetState.bind(null, currentlyRenderingFiber, isMatching);
			isMatching.dispatch = ssrFormState;
			isMatching = mountStateImpl(!1);
			inRootOrSingleton = dispatchOptimisticSetState.bind(null, currentlyRenderingFiber, !1, isMatching.queue);
			isMatching = mountWorkInProgressHook();
			markerInstance = {
				state: initialStateProp,
				dispatch: null,
				action,
				pending: null
			};
			isMatching.queue = markerInstance;
			ssrFormState = dispatchActionState.bind(null, currentlyRenderingFiber, markerInstance, inRootOrSingleton, ssrFormState);
			markerInstance.dispatch = ssrFormState;
			isMatching.memoizedState = action;
			return [
				initialStateProp,
				ssrFormState,
				!1
			];
		}
		function updateActionState(action) {
			return updateActionStateImpl(updateWorkInProgressHook(), currentHook, action);
		}
		function updateActionStateImpl(stateHook, currentStateHook, action) {
			currentStateHook = updateReducerImpl(stateHook, currentStateHook, actionStateReducer)[0];
			stateHook = updateReducer(basicStateReducer)[0];
			if ("object" === typeof currentStateHook && null !== currentStateHook && "function" === typeof currentStateHook.then) try {
				var state = useThenable(currentStateHook);
			} catch (x) {
				if (x === SuspenseException) throw SuspenseActionException;
				throw x;
			}
			else state = currentStateHook;
			currentStateHook = updateWorkInProgressHook();
			var actionQueue = currentStateHook.queue, dispatch = actionQueue.dispatch;
			action !== currentStateHook.memoizedState && (currentlyRenderingFiber.flags |= 2048, pushSimpleEffect(HasEffect | Passive, { destroy: void 0 }, actionStateActionEffect.bind(null, actionQueue, action), null));
			return [
				state,
				dispatch,
				stateHook
			];
		}
		function actionStateActionEffect(actionQueue, action) {
			actionQueue.action = action;
		}
		function rerenderActionState(action) {
			var stateHook = updateWorkInProgressHook(), currentStateHook = currentHook;
			if (null !== currentStateHook) return updateActionStateImpl(stateHook, currentStateHook, action);
			updateWorkInProgressHook();
			stateHook = stateHook.memoizedState;
			currentStateHook = updateWorkInProgressHook();
			var dispatch = currentStateHook.queue.dispatch;
			currentStateHook.memoizedState = action;
			return [
				stateHook,
				dispatch,
				!1
			];
		}
		function pushSimpleEffect(tag, inst, create, deps) {
			tag = {
				tag,
				create,
				deps,
				inst,
				next: null
			};
			inst = currentlyRenderingFiber.updateQueue;
			null === inst && (inst = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = inst);
			create = inst.lastEffect;
			null === create ? inst.lastEffect = tag.next = tag : (deps = create.next, create.next = tag, tag.next = deps, inst.lastEffect = tag);
			return tag;
		}
		function mountRef(initialValue) {
			var hook = mountWorkInProgressHook();
			initialValue = { current: initialValue };
			return hook.memoizedState = initialValue;
		}
		function mountEffectImpl(fiberFlags, hookFlags, create, deps) {
			var hook = mountWorkInProgressHook();
			currentlyRenderingFiber.flags |= fiberFlags;
			hook.memoizedState = pushSimpleEffect(HasEffect | hookFlags, { destroy: void 0 }, create, void 0 === deps ? null : deps);
		}
		function updateEffectImpl(fiberFlags, hookFlags, create, deps) {
			var hook = updateWorkInProgressHook();
			deps = void 0 === deps ? null : deps;
			var inst = hook.memoizedState.inst;
			null !== currentHook && null !== deps && areHookInputsEqual(deps, currentHook.memoizedState.deps) ? hook.memoizedState = pushSimpleEffect(hookFlags, inst, create, deps) : (currentlyRenderingFiber.flags |= fiberFlags, hook.memoizedState = pushSimpleEffect(HasEffect | hookFlags, inst, create, deps));
		}
		function mountEffect(create, deps) {
			(currentlyRenderingFiber.mode & StrictEffectsMode) !== NoMode ? mountEffectImpl(276826112, Passive, create, deps) : mountEffectImpl(8390656, Passive, create, deps);
		}
		function useEffectEventImpl(payload) {
			currentlyRenderingFiber.flags |= 4;
			var componentUpdateQueue = currentlyRenderingFiber.updateQueue;
			if (null === componentUpdateQueue) componentUpdateQueue = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = componentUpdateQueue, componentUpdateQueue.events = [payload];
			else {
				var events = componentUpdateQueue.events;
				null === events ? componentUpdateQueue.events = [payload] : events.push(payload);
			}
		}
		function mountEvent(callback) {
			var hook = mountWorkInProgressHook(), ref = { impl: callback };
			hook.memoizedState = ref;
			return function() {
				if ((executionContext & RenderContext) !== NoContext) throw Error("A function wrapped in useEffectEvent can't be called during rendering.");
				return ref.impl.apply(void 0, arguments);
			};
		}
		function updateEvent(callback) {
			var ref = updateWorkInProgressHook().memoizedState;
			useEffectEventImpl({
				ref,
				nextImpl: callback
			});
			return function() {
				if ((executionContext & RenderContext) !== NoContext) throw Error("A function wrapped in useEffectEvent can't be called during rendering.");
				return ref.impl.apply(void 0, arguments);
			};
		}
		function mountLayoutEffect(create, deps) {
			var fiberFlags = 4194308;
			(currentlyRenderingFiber.mode & StrictEffectsMode) !== NoMode && (fiberFlags |= 134217728);
			return mountEffectImpl(fiberFlags, Layout, create, deps);
		}
		function imperativeHandleEffect(create, ref) {
			if ("function" === typeof ref) {
				create = create();
				var refCleanup = ref(create);
				return function() {
					"function" === typeof refCleanup ? refCleanup() : ref(null);
				};
			}
			if (null !== ref && void 0 !== ref) return ref.hasOwnProperty("current") || console.error("Expected useImperativeHandle() first argument to either be a ref callback or React.createRef() object. Instead received: %s.", "an object with keys {" + Object.keys(ref).join(", ") + "}"), create = create(), ref.current = create, function() {
				ref.current = null;
			};
		}
		function mountImperativeHandle(ref, create, deps) {
			"function" !== typeof create && console.error("Expected useImperativeHandle() second argument to be a function that creates a handle. Instead received: %s.", null !== create ? typeof create : "null");
			deps = null !== deps && void 0 !== deps ? deps.concat([ref]) : null;
			var fiberFlags = 4194308;
			(currentlyRenderingFiber.mode & StrictEffectsMode) !== NoMode && (fiberFlags |= 134217728);
			mountEffectImpl(fiberFlags, Layout, imperativeHandleEffect.bind(null, create, ref), deps);
		}
		function updateImperativeHandle(ref, create, deps) {
			"function" !== typeof create && console.error("Expected useImperativeHandle() second argument to be a function that creates a handle. Instead received: %s.", null !== create ? typeof create : "null");
			deps = null !== deps && void 0 !== deps ? deps.concat([ref]) : null;
			updateEffectImpl(4, Layout, imperativeHandleEffect.bind(null, create, ref), deps);
		}
		function mountCallback(callback, deps) {
			mountWorkInProgressHook().memoizedState = [callback, void 0 === deps ? null : deps];
			return callback;
		}
		function updateCallback(callback, deps) {
			var hook = updateWorkInProgressHook();
			deps = void 0 === deps ? null : deps;
			var prevState = hook.memoizedState;
			if (null !== deps && areHookInputsEqual(deps, prevState[1])) return prevState[0];
			hook.memoizedState = [callback, deps];
			return callback;
		}
		function mountMemo(nextCreate, deps) {
			var hook = mountWorkInProgressHook();
			deps = void 0 === deps ? null : deps;
			var nextValue = nextCreate();
			if (shouldDoubleInvokeUserFnsInHooksDEV) {
				setIsStrictModeForDevtools(!0);
				try {
					nextCreate();
				} finally {
					setIsStrictModeForDevtools(!1);
				}
			}
			hook.memoizedState = [nextValue, deps];
			return nextValue;
		}
		function updateMemo(nextCreate, deps) {
			var hook = updateWorkInProgressHook();
			deps = void 0 === deps ? null : deps;
			var prevState = hook.memoizedState;
			if (null !== deps && areHookInputsEqual(deps, prevState[1])) return prevState[0];
			prevState = nextCreate();
			if (shouldDoubleInvokeUserFnsInHooksDEV) {
				setIsStrictModeForDevtools(!0);
				try {
					nextCreate();
				} finally {
					setIsStrictModeForDevtools(!1);
				}
			}
			hook.memoizedState = [prevState, deps];
			return prevState;
		}
		function mountDeferredValue(value, initialValue) {
			return mountDeferredValueImpl(mountWorkInProgressHook(), value, initialValue);
		}
		function updateDeferredValue(value, initialValue) {
			return updateDeferredValueImpl(updateWorkInProgressHook(), currentHook.memoizedState, value, initialValue);
		}
		function rerenderDeferredValue(value, initialValue) {
			var hook = updateWorkInProgressHook();
			return null === currentHook ? mountDeferredValueImpl(hook, value, initialValue) : updateDeferredValueImpl(hook, currentHook.memoizedState, value, initialValue);
		}
		function mountDeferredValueImpl(hook, value, initialValue) {
			if (void 0 === initialValue || 0 !== (renderLanes & 1073741824) && 0 === (workInProgressRootRenderLanes & 261930)) return hook.memoizedState = value;
			hook.memoizedState = initialValue;
			hook = requestDeferredLane();
			currentlyRenderingFiber.lanes |= hook;
			workInProgressRootSkippedLanes |= hook;
			return initialValue;
		}
		function updateDeferredValueImpl(hook, prevValue, value, initialValue) {
			if (objectIs(value, prevValue)) return value;
			if (null !== currentTreeHiddenStackCursor.current) return hook = mountDeferredValueImpl(hook, value, initialValue), objectIs(hook, prevValue) || (didReceiveUpdate = !0), hook;
			if (0 === (renderLanes & 42) || 0 !== (renderLanes & 1073741824) && 0 === (workInProgressRootRenderLanes & 261930)) return didReceiveUpdate = !0, hook.memoizedState = value;
			hook = requestDeferredLane();
			currentlyRenderingFiber.lanes |= hook;
			workInProgressRootSkippedLanes |= hook;
			return prevValue;
		}
		function releaseAsyncTransition() {
			ReactSharedInternals.asyncTransitions--;
		}
		function startTransition(fiber, queue, pendingState, finishedState, callback) {
			var previousPriority = ReactDOMSharedInternals.p;
			ReactDOMSharedInternals.p = 0 !== previousPriority && previousPriority < ContinuousEventPriority ? previousPriority : ContinuousEventPriority;
			var prevTransition = ReactSharedInternals.T, currentTransition = {};
			currentTransition._updatedFibers = /* @__PURE__ */ new Set();
			ReactSharedInternals.T = currentTransition;
			dispatchOptimisticSetState(fiber, !1, queue, pendingState);
			try {
				var returnValue = callback(), onStartTransitionFinish = ReactSharedInternals.S;
				null !== onStartTransitionFinish && onStartTransitionFinish(currentTransition, returnValue);
				if (null !== returnValue && "object" === typeof returnValue && "function" === typeof returnValue.then) {
					ReactSharedInternals.asyncTransitions++;
					returnValue.then(releaseAsyncTransition, releaseAsyncTransition);
					var thenableForFinishedState = chainThenableValue(returnValue, finishedState);
					dispatchSetStateInternal(fiber, queue, thenableForFinishedState, requestUpdateLane(fiber));
				} else dispatchSetStateInternal(fiber, queue, finishedState, requestUpdateLane(fiber));
			} catch (error) {
				dispatchSetStateInternal(fiber, queue, {
					then: function() {},
					status: "rejected",
					reason: error
				}, requestUpdateLane(fiber));
			} finally {
				ReactDOMSharedInternals.p = previousPriority, null !== prevTransition && null !== currentTransition.types && (null !== prevTransition.types && prevTransition.types !== currentTransition.types && console.error("We expected inner Transitions to have transferred the outer types set and that you cannot add to the outer Transition while inside the inner.This is a bug in React."), prevTransition.types = currentTransition.types), ReactSharedInternals.T = prevTransition, null === prevTransition && currentTransition._updatedFibers && (fiber = currentTransition._updatedFibers.size, currentTransition._updatedFibers.clear(), 10 < fiber && console.warn("Detected a large number of updates inside startTransition. If this is due to a subscription please re-write it to use React provided hooks. Otherwise concurrent mode guarantees are off the table."));
			}
		}
		function startHostTransition(formFiber, pendingState, action, formData) {
			if (5 !== formFiber.tag) throw Error("Expected the form instance to be a HostComponent. This is a bug in React.");
			var queue = ensureFormComponentIsStateful(formFiber).queue;
			startHostActionTimer(formFiber);
			startTransition(formFiber, queue, pendingState, NotPendingTransition, null === action ? noop : function() {
				requestFormReset$1(formFiber);
				return action(formData);
			});
		}
		function ensureFormComponentIsStateful(formFiber) {
			var existingStateHook = formFiber.memoizedState;
			if (null !== existingStateHook) return existingStateHook;
			existingStateHook = {
				memoizedState: NotPendingTransition,
				baseState: NotPendingTransition,
				baseQueue: null,
				queue: {
					pending: null,
					lanes: 0,
					dispatch: null,
					lastRenderedReducer: basicStateReducer,
					lastRenderedState: NotPendingTransition
				},
				next: null
			};
			var initialResetState = {};
			existingStateHook.next = {
				memoizedState: initialResetState,
				baseState: initialResetState,
				baseQueue: null,
				queue: {
					pending: null,
					lanes: 0,
					dispatch: null,
					lastRenderedReducer: basicStateReducer,
					lastRenderedState: initialResetState
				},
				next: null
			};
			formFiber.memoizedState = existingStateHook;
			formFiber = formFiber.alternate;
			null !== formFiber && (formFiber.memoizedState = existingStateHook);
			return existingStateHook;
		}
		function requestFormReset$1(formFiber) {
			null === ReactSharedInternals.T && console.error("requestFormReset was called outside a transition or action. To fix, move to an action, or wrap with startTransition.");
			var stateHook = ensureFormComponentIsStateful(formFiber);
			null === stateHook.next && (stateHook = formFiber.alternate.memoizedState);
			dispatchSetStateInternal(formFiber, stateHook.next.queue, {}, requestUpdateLane(formFiber));
		}
		function mountTransition() {
			var stateHook = mountStateImpl(!1);
			stateHook = startTransition.bind(null, currentlyRenderingFiber, stateHook.queue, !0, !1);
			mountWorkInProgressHook().memoizedState = stateHook;
			return [!1, stateHook];
		}
		function updateTransition() {
			var booleanOrThenable = updateReducer(basicStateReducer)[0], start = updateWorkInProgressHook().memoizedState;
			return ["boolean" === typeof booleanOrThenable ? booleanOrThenable : useThenable(booleanOrThenable), start];
		}
		function rerenderTransition() {
			var booleanOrThenable = rerenderReducer(basicStateReducer)[0], start = updateWorkInProgressHook().memoizedState;
			return ["boolean" === typeof booleanOrThenable ? booleanOrThenable : useThenable(booleanOrThenable), start];
		}
		function useHostTransitionStatus() {
			return readContext(HostTransitionContext);
		}
		function mountId() {
			var hook = mountWorkInProgressHook(), identifierPrefix = workInProgressRoot.identifierPrefix;
			if (isHydrating) {
				var treeId = treeContextOverflow;
				var idWithLeadingBit = treeContextId;
				treeId = (idWithLeadingBit & ~(1 << 32 - clz32(idWithLeadingBit) - 1)).toString(32) + treeId;
				identifierPrefix = "_" + identifierPrefix + "R_" + treeId;
				treeId = localIdCounter++;
				0 < treeId && (identifierPrefix += "H" + treeId.toString(32));
				identifierPrefix += "_";
			} else treeId = globalClientIdCounter++, identifierPrefix = "_" + identifierPrefix + "r_" + treeId.toString(32) + "_";
			return hook.memoizedState = identifierPrefix;
		}
		function mountRefresh() {
			return mountWorkInProgressHook().memoizedState = refreshCache.bind(null, currentlyRenderingFiber);
		}
		function refreshCache(fiber, seedKey) {
			for (var provider = fiber.return; null !== provider;) {
				switch (provider.tag) {
					case 24:
					case 3:
						var lane = requestUpdateLane(provider), refreshUpdate = createUpdate(lane), root = enqueueUpdate(provider, refreshUpdate, lane);
						null !== root && (startUpdateTimerByLane(lane, "refresh()", fiber), scheduleUpdateOnFiber(root, provider, lane), entangleTransitions(root, provider, lane));
						fiber = createCache();
						null !== seedKey && void 0 !== seedKey && null !== root && console.error("The seed argument is not enabled outside experimental channels.");
						refreshUpdate.payload = { cache: fiber };
						return;
				}
				provider = provider.return;
			}
		}
		function dispatchReducerAction(fiber, queue, action) {
			var args = arguments;
			"function" === typeof args[3] && console.error("State updates from the useState() and useReducer() Hooks don't support the second callback argument. To execute a side effect after rendering, declare it in the component body with useEffect().");
			args = requestUpdateLane(fiber);
			var update = {
				lane: args,
				revertLane: 0,
				gesture: null,
				action,
				hasEagerState: !1,
				eagerState: null,
				next: null
			};
			isRenderPhaseUpdate(fiber) ? enqueueRenderPhaseUpdate(queue, update) : (update = enqueueConcurrentHookUpdate(fiber, queue, update, args), null !== update && (startUpdateTimerByLane(args, "dispatch()", fiber), scheduleUpdateOnFiber(update, fiber, args), entangleTransitionUpdate(update, queue, args)));
		}
		function dispatchSetState(fiber, queue, action) {
			var args = arguments;
			"function" === typeof args[3] && console.error("State updates from the useState() and useReducer() Hooks don't support the second callback argument. To execute a side effect after rendering, declare it in the component body with useEffect().");
			args = requestUpdateLane(fiber);
			dispatchSetStateInternal(fiber, queue, action, args) && startUpdateTimerByLane(args, "setState()", fiber);
		}
		function dispatchSetStateInternal(fiber, queue, action, lane) {
			var update = {
				lane,
				revertLane: 0,
				gesture: null,
				action,
				hasEagerState: !1,
				eagerState: null,
				next: null
			};
			if (isRenderPhaseUpdate(fiber)) enqueueRenderPhaseUpdate(queue, update);
			else {
				var alternate = fiber.alternate;
				if (0 === fiber.lanes && (null === alternate || 0 === alternate.lanes) && (alternate = queue.lastRenderedReducer, null !== alternate)) {
					var prevDispatcher = ReactSharedInternals.H;
					ReactSharedInternals.H = InvalidNestedHooksDispatcherOnUpdateInDEV;
					try {
						var currentState = queue.lastRenderedState, eagerState = alternate(currentState, action);
						update.hasEagerState = !0;
						update.eagerState = eagerState;
						if (objectIs(eagerState, currentState)) return enqueueUpdate$1(fiber, queue, update, 0), null === workInProgressRoot && finishQueueingConcurrentUpdates(), !1;
					} catch (error) {} finally {
						ReactSharedInternals.H = prevDispatcher;
					}
				}
				action = enqueueConcurrentHookUpdate(fiber, queue, update, lane);
				if (null !== action) return scheduleUpdateOnFiber(action, fiber, lane), entangleTransitionUpdate(action, queue, lane), !0;
			}
			return !1;
		}
		function dispatchOptimisticSetState(fiber, throwIfDuringRender, queue, action) {
			null === ReactSharedInternals.T && 0 === currentEntangledLane && console.error("An optimistic state update occurred outside a transition or action. To fix, move the update to an action, or wrap with startTransition.");
			action = {
				lane: 2,
				revertLane: requestTransitionLane(),
				gesture: null,
				action,
				hasEagerState: !1,
				eagerState: null,
				next: null
			};
			if (isRenderPhaseUpdate(fiber)) {
				if (throwIfDuringRender) throw Error("Cannot update optimistic state while rendering.");
				console.error("Cannot call startTransition while rendering.");
			} else throwIfDuringRender = enqueueConcurrentHookUpdate(fiber, queue, action, 2), null !== throwIfDuringRender && (startUpdateTimerByLane(2, "setOptimistic()", fiber), scheduleUpdateOnFiber(throwIfDuringRender, fiber, 2));
		}
		function isRenderPhaseUpdate(fiber) {
			var alternate = fiber.alternate;
			return fiber === currentlyRenderingFiber || null !== alternate && alternate === currentlyRenderingFiber;
		}
		function enqueueRenderPhaseUpdate(queue, update) {
			didScheduleRenderPhaseUpdateDuringThisPass = didScheduleRenderPhaseUpdate = !0;
			var pending = queue.pending;
			null === pending ? update.next = update : (update.next = pending.next, pending.next = update);
			queue.pending = update;
		}
		function entangleTransitionUpdate(root, queue, lane) {
			if (0 !== (lane & 4194048)) {
				var queueLanes = queue.lanes;
				queueLanes &= root.pendingLanes;
				lane |= queueLanes;
				queue.lanes = lane;
				markRootEntangled(root, lane);
			}
		}
		function warnOnInvalidCallback(callback) {
			if (null !== callback && "function" !== typeof callback) {
				var key = String(callback);
				didWarnOnInvalidCallback.has(key) || (didWarnOnInvalidCallback.add(key), console.error("Expected the last optional `callback` argument to be a function. Instead received: %s.", callback));
			}
		}
		function applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, nextProps) {
			var prevState = workInProgress.memoizedState, partialState = getDerivedStateFromProps(nextProps, prevState);
			if (workInProgress.mode & StrictLegacyMode) {
				setIsStrictModeForDevtools(!0);
				try {
					partialState = getDerivedStateFromProps(nextProps, prevState);
				} finally {
					setIsStrictModeForDevtools(!1);
				}
			}
			void 0 === partialState && (ctor = getComponentNameFromType(ctor) || "Component", didWarnAboutUndefinedDerivedState.has(ctor) || (didWarnAboutUndefinedDerivedState.add(ctor), console.error("%s.getDerivedStateFromProps(): A valid state object (or null) must be returned. You have returned undefined.", ctor)));
			prevState = null === partialState || void 0 === partialState ? prevState : assign({}, prevState, partialState);
			workInProgress.memoizedState = prevState;
			0 === workInProgress.lanes && (workInProgress.updateQueue.baseState = prevState);
		}
		function checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext) {
			var instance = workInProgress.stateNode;
			if ("function" === typeof instance.shouldComponentUpdate) {
				oldProps = instance.shouldComponentUpdate(newProps, newState, nextContext);
				if (workInProgress.mode & StrictLegacyMode) {
					setIsStrictModeForDevtools(!0);
					try {
						oldProps = instance.shouldComponentUpdate(newProps, newState, nextContext);
					} finally {
						setIsStrictModeForDevtools(!1);
					}
				}
				void 0 === oldProps && console.error("%s.shouldComponentUpdate(): Returned undefined instead of a boolean value. Make sure to return true or false.", getComponentNameFromType(ctor) || "Component");
				return oldProps;
			}
			return ctor.prototype && ctor.prototype.isPureReactComponent ? !shallowEqual(oldProps, newProps) || !shallowEqual(oldState, newState) : !0;
		}
		function callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext) {
			var oldState = instance.state;
			"function" === typeof instance.componentWillReceiveProps && instance.componentWillReceiveProps(newProps, nextContext);
			"function" === typeof instance.UNSAFE_componentWillReceiveProps && instance.UNSAFE_componentWillReceiveProps(newProps, nextContext);
			instance.state !== oldState && (workInProgress = getComponentNameFromFiber(workInProgress) || "Component", didWarnAboutStateAssignmentForComponent.has(workInProgress) || (didWarnAboutStateAssignmentForComponent.add(workInProgress), console.error("%s.componentWillReceiveProps(): Assigning directly to this.state is deprecated (except inside a component's constructor). Use setState instead.", workInProgress)), classComponentUpdater.enqueueReplaceState(instance, instance.state, null));
		}
		function resolveClassComponentProps(Component, baseProps) {
			var newProps = baseProps;
			if ("ref" in baseProps) {
				newProps = {};
				for (var propName in baseProps) "ref" !== propName && (newProps[propName] = baseProps[propName]);
			}
			if (Component = Component.defaultProps) {
				newProps === baseProps && (newProps = assign({}, newProps));
				for (var _propName in Component) void 0 === newProps[_propName] && (newProps[_propName] = Component[_propName]);
			}
			return newProps;
		}
		function defaultOnUncaughtError(error) {
			reportGlobalError(error);
			console.warn("%s\n\n%s\n", componentName ? "An error occurred in the <" + componentName + "> component." : "An error occurred in one of your React components.", "Consider adding an error boundary to your tree to customize error handling behavior.\nVisit https://react.dev/link/error-boundaries to learn more about error boundaries.");
		}
		function defaultOnCaughtError(error) {
			var componentNameMessage = componentName ? "The above error occurred in the <" + componentName + "> component." : "The above error occurred in one of your React components.", recreateMessage = "React will try to recreate this component tree from scratch using the error boundary you provided, " + ((errorBoundaryName || "Anonymous") + ".");
			if ("object" === typeof error && null !== error && "string" === typeof error.environmentName) {
				var JSCompiler_inline_result = error.environmentName;
				error = [
					"%o\n\n%s\n\n%s\n",
					error,
					componentNameMessage,
					recreateMessage
				].slice(0);
				"string" === typeof error[0] ? error.splice(0, 1, badgeFormat + " " + error[0], badgeStyle, pad + JSCompiler_inline_result + pad, resetStyle) : error.splice(0, 0, badgeFormat, badgeStyle, pad + JSCompiler_inline_result + pad, resetStyle);
				error.unshift(console);
				JSCompiler_inline_result = bind.apply(console.error, error);
				JSCompiler_inline_result();
			} else console.error("%o\n\n%s\n\n%s\n", error, componentNameMessage, recreateMessage);
		}
		function defaultOnRecoverableError(error) {
			reportGlobalError(error);
		}
		function logUncaughtError(root, errorInfo) {
			try {
				componentName = errorInfo.source ? getComponentNameFromFiber(errorInfo.source) : null;
				errorBoundaryName = null;
				var error = errorInfo.value;
				if (null !== ReactSharedInternals.actQueue) ReactSharedInternals.thrownErrors.push(error);
				else {
					var onUncaughtError = root.onUncaughtError;
					onUncaughtError(error, { componentStack: errorInfo.stack });
				}
			} catch (e$5) {
				setTimeout(function() {
					throw e$5;
				});
			}
		}
		function logCaughtError(root, boundary, errorInfo) {
			try {
				componentName = errorInfo.source ? getComponentNameFromFiber(errorInfo.source) : null;
				errorBoundaryName = getComponentNameFromFiber(boundary);
				var onCaughtError = root.onCaughtError;
				onCaughtError(errorInfo.value, {
					componentStack: errorInfo.stack,
					errorBoundary: 1 === boundary.tag ? boundary.stateNode : null
				});
			} catch (e$6) {
				setTimeout(function() {
					throw e$6;
				});
			}
		}
		function createRootErrorUpdate(root, errorInfo, lane) {
			lane = createUpdate(lane);
			lane.tag = CaptureUpdate;
			lane.payload = { element: null };
			lane.callback = function() {
				runWithFiberInDEV(errorInfo.source, logUncaughtError, root, errorInfo);
			};
			return lane;
		}
		function createClassErrorUpdate(lane) {
			lane = createUpdate(lane);
			lane.tag = CaptureUpdate;
			return lane;
		}
		function initializeClassErrorUpdate(update, root, fiber, errorInfo) {
			var getDerivedStateFromError = fiber.type.getDerivedStateFromError;
			if ("function" === typeof getDerivedStateFromError) {
				var error = errorInfo.value;
				update.payload = function() {
					return getDerivedStateFromError(error);
				};
				update.callback = function() {
					markFailedErrorBoundaryForHotReloading(fiber);
					runWithFiberInDEV(errorInfo.source, logCaughtError, root, fiber, errorInfo);
				};
			}
			var inst = fiber.stateNode;
			null !== inst && "function" === typeof inst.componentDidCatch && (update.callback = function() {
				markFailedErrorBoundaryForHotReloading(fiber);
				runWithFiberInDEV(errorInfo.source, logCaughtError, root, fiber, errorInfo);
				"function" !== typeof getDerivedStateFromError && (null === legacyErrorBoundariesThatAlreadyFailed ? legacyErrorBoundariesThatAlreadyFailed = /* @__PURE__ */ new Set([this]) : legacyErrorBoundariesThatAlreadyFailed.add(this));
				callComponentDidCatchInDEV(this, errorInfo);
				"function" === typeof getDerivedStateFromError || 0 === (fiber.lanes & 2) && console.error("%s: Error boundaries should implement getDerivedStateFromError(). In that method, return a state update to display an error message or fallback UI.", getComponentNameFromFiber(fiber) || "Unknown");
			});
		}
		function throwException(root, returnFiber, sourceFiber, value, rootRenderLanes) {
			sourceFiber.flags |= 32768;
			isDevToolsPresent && restorePendingUpdaters(root, rootRenderLanes);
			if (null !== value && "object" === typeof value && "function" === typeof value.then) {
				returnFiber = sourceFiber.alternate;
				null !== returnFiber && propagateParentContextChanges(returnFiber, sourceFiber, rootRenderLanes, !0);
				isHydrating && (didSuspendOrErrorDEV = !0);
				sourceFiber = suspenseHandlerStackCursor.current;
				if (null !== sourceFiber) {
					switch (sourceFiber.tag) {
						case 31:
						case 13: return null === shellBoundary ? renderDidSuspendDelayIfPossible() : null === sourceFiber.alternate && workInProgressRootExitStatus === RootInProgress && (workInProgressRootExitStatus = RootSuspended), sourceFiber.flags &= -257, sourceFiber.flags |= 65536, sourceFiber.lanes = rootRenderLanes, value === noopSuspenseyCommitThenable ? sourceFiber.flags |= 16384 : (returnFiber = sourceFiber.updateQueue, null === returnFiber ? sourceFiber.updateQueue = /* @__PURE__ */ new Set([value]) : returnFiber.add(value), attachPingListener(root, value, rootRenderLanes)), !1;
						case 22: return sourceFiber.flags |= 65536, value === noopSuspenseyCommitThenable ? sourceFiber.flags |= 16384 : (returnFiber = sourceFiber.updateQueue, null === returnFiber ? (returnFiber = {
							transitions: null,
							markerInstances: null,
							retryQueue: /* @__PURE__ */ new Set([value])
						}, sourceFiber.updateQueue = returnFiber) : (sourceFiber = returnFiber.retryQueue, null === sourceFiber ? returnFiber.retryQueue = /* @__PURE__ */ new Set([value]) : sourceFiber.add(value)), attachPingListener(root, value, rootRenderLanes)), !1;
					}
					throw Error("Unexpected Suspense handler tag (" + sourceFiber.tag + "). This is a bug in React.");
				}
				attachPingListener(root, value, rootRenderLanes);
				renderDidSuspendDelayIfPossible();
				return !1;
			}
			if (isHydrating) return didSuspendOrErrorDEV = !0, returnFiber = suspenseHandlerStackCursor.current, null !== returnFiber ? (0 === (returnFiber.flags & 65536) && (returnFiber.flags |= 256), returnFiber.flags |= 65536, returnFiber.lanes = rootRenderLanes, value !== HydrationMismatchException && queueHydrationError(createCapturedValueAtFiber(Error("There was an error while hydrating but React was able to recover by instead client rendering from the nearest Suspense boundary.", { cause: value }), sourceFiber))) : (value !== HydrationMismatchException && queueHydrationError(createCapturedValueAtFiber(Error("There was an error while hydrating but React was able to recover by instead client rendering the entire root.", { cause: value }), sourceFiber)), root = root.current.alternate, root.flags |= 65536, rootRenderLanes &= -rootRenderLanes, root.lanes |= rootRenderLanes, value = createCapturedValueAtFiber(value, sourceFiber), rootRenderLanes = createRootErrorUpdate(root.stateNode, value, rootRenderLanes), enqueueCapturedUpdate(root, rootRenderLanes), workInProgressRootExitStatus !== RootSuspendedWithDelay && (workInProgressRootExitStatus = RootErrored)), !1;
			var error = createCapturedValueAtFiber(Error("There was an error during concurrent rendering but React was able to recover by instead synchronously rendering the entire root.", { cause: value }), sourceFiber);
			null === workInProgressRootConcurrentErrors ? workInProgressRootConcurrentErrors = [error] : workInProgressRootConcurrentErrors.push(error);
			workInProgressRootExitStatus !== RootSuspendedWithDelay && (workInProgressRootExitStatus = RootErrored);
			if (null === returnFiber) return !0;
			value = createCapturedValueAtFiber(value, sourceFiber);
			sourceFiber = returnFiber;
			do {
				switch (sourceFiber.tag) {
					case 3: return sourceFiber.flags |= 65536, root = rootRenderLanes & -rootRenderLanes, sourceFiber.lanes |= root, root = createRootErrorUpdate(sourceFiber.stateNode, value, root), enqueueCapturedUpdate(sourceFiber, root), !1;
					case 1: if (returnFiber = sourceFiber.type, error = sourceFiber.stateNode, 0 === (sourceFiber.flags & 128) && ("function" === typeof returnFiber.getDerivedStateFromError || null !== error && "function" === typeof error.componentDidCatch && (null === legacyErrorBoundariesThatAlreadyFailed || !legacyErrorBoundariesThatAlreadyFailed.has(error)))) return sourceFiber.flags |= 65536, rootRenderLanes &= -rootRenderLanes, sourceFiber.lanes |= rootRenderLanes, rootRenderLanes = createClassErrorUpdate(rootRenderLanes), initializeClassErrorUpdate(rootRenderLanes, root, sourceFiber, value), enqueueCapturedUpdate(sourceFiber, rootRenderLanes), !1;
				}
				sourceFiber = sourceFiber.return;
			} while (null !== sourceFiber);
			return !1;
		}
		function reconcileChildren(current, workInProgress, nextChildren, renderLanes) {
			workInProgress.child = null === current ? mountChildFibers(workInProgress, null, nextChildren, renderLanes) : reconcileChildFibers(workInProgress, current.child, nextChildren, renderLanes);
		}
		function updateForwardRef(current, workInProgress, Component, nextProps, renderLanes) {
			Component = Component.render;
			var ref = workInProgress.ref;
			if ("ref" in nextProps) {
				var propsWithoutRef = {};
				for (var key in nextProps) "ref" !== key && (propsWithoutRef[key] = nextProps[key]);
			} else propsWithoutRef = nextProps;
			prepareToReadContext(workInProgress);
			nextProps = renderWithHooks(current, workInProgress, Component, propsWithoutRef, ref, renderLanes);
			key = checkDidRenderIdHook();
			if (null !== current && !didReceiveUpdate) return bailoutHooks(current, workInProgress, renderLanes), bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
			isHydrating && key && pushMaterializedTreeId(workInProgress);
			workInProgress.flags |= 1;
			reconcileChildren(current, workInProgress, nextProps, renderLanes);
			return workInProgress.child;
		}
		function updateMemoComponent(current, workInProgress, Component, nextProps, renderLanes) {
			if (null === current) {
				var type = Component.type;
				if ("function" === typeof type && !shouldConstruct(type) && void 0 === type.defaultProps && null === Component.compare) return Component = resolveFunctionForHotReloading(type), workInProgress.tag = 15, workInProgress.type = Component, validateFunctionComponentInDev(workInProgress, type), updateSimpleMemoComponent(current, workInProgress, Component, nextProps, renderLanes);
				current = createFiberFromTypeAndProps(Component.type, null, nextProps, workInProgress, workInProgress.mode, renderLanes);
				current.ref = workInProgress.ref;
				current.return = workInProgress;
				return workInProgress.child = current;
			}
			type = current.child;
			if (!checkScheduledUpdateOrContext(current, renderLanes)) {
				var prevProps = type.memoizedProps;
				Component = Component.compare;
				Component = null !== Component ? Component : shallowEqual;
				if (Component(prevProps, nextProps) && current.ref === workInProgress.ref) return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
			}
			workInProgress.flags |= 1;
			current = createWorkInProgress(type, nextProps);
			current.ref = workInProgress.ref;
			current.return = workInProgress;
			return workInProgress.child = current;
		}
		function updateSimpleMemoComponent(current, workInProgress, Component, nextProps, renderLanes) {
			if (null !== current) {
				var prevProps = current.memoizedProps;
				if (shallowEqual(prevProps, nextProps) && current.ref === workInProgress.ref && workInProgress.type === current.type) if (didReceiveUpdate = !1, workInProgress.pendingProps = nextProps = prevProps, checkScheduledUpdateOrContext(current, renderLanes)) 0 !== (current.flags & 131072) && (didReceiveUpdate = !0);
				else return workInProgress.lanes = current.lanes, bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
			}
			return updateFunctionComponent(current, workInProgress, Component, nextProps, renderLanes);
		}
		function updateOffscreenComponent(current, workInProgress, renderLanes, nextProps) {
			var nextChildren = nextProps.children, prevState = null !== current ? current.memoizedState : null;
			null === current && null === workInProgress.stateNode && (workInProgress.stateNode = {
				_visibility: OffscreenVisible,
				_pendingMarkers: null,
				_retryCache: null,
				_transitions: null
			});
			if ("hidden" === nextProps.mode) {
				if (0 !== (workInProgress.flags & 128)) {
					prevState = null !== prevState ? prevState.baseLanes | renderLanes : renderLanes;
					if (null !== current) {
						nextProps = workInProgress.child = current.child;
						for (nextChildren = 0; null !== nextProps;) nextChildren = nextChildren | nextProps.lanes | nextProps.childLanes, nextProps = nextProps.sibling;
						nextProps = nextChildren & ~prevState;
					} else nextProps = 0, workInProgress.child = null;
					return deferHiddenOffscreenComponent(current, workInProgress, prevState, renderLanes, nextProps);
				}
				if (0 !== (renderLanes & 536870912)) workInProgress.memoizedState = {
					baseLanes: 0,
					cachePool: null
				}, null !== current && pushTransition(workInProgress, null !== prevState ? prevState.cachePool : null), null !== prevState ? pushHiddenContext(workInProgress, prevState) : reuseHiddenContextOnStack(workInProgress), pushOffscreenSuspenseHandler(workInProgress);
				else return nextProps = workInProgress.lanes = 536870912, deferHiddenOffscreenComponent(current, workInProgress, null !== prevState ? prevState.baseLanes | renderLanes : renderLanes, renderLanes, nextProps);
			} else null !== prevState ? (pushTransition(workInProgress, prevState.cachePool), pushHiddenContext(workInProgress, prevState), reuseSuspenseHandlerOnStack(workInProgress), workInProgress.memoizedState = null) : (null !== current && pushTransition(workInProgress, null), reuseHiddenContextOnStack(workInProgress), reuseSuspenseHandlerOnStack(workInProgress));
			reconcileChildren(current, workInProgress, nextChildren, renderLanes);
			return workInProgress.child;
		}
		function bailoutOffscreenComponent(current, workInProgress) {
			null !== current && 22 === current.tag || null !== workInProgress.stateNode || (workInProgress.stateNode = {
				_visibility: OffscreenVisible,
				_pendingMarkers: null,
				_retryCache: null,
				_transitions: null
			});
			return workInProgress.sibling;
		}
		function deferHiddenOffscreenComponent(current, workInProgress, nextBaseLanes, renderLanes, remainingChildLanes) {
			var JSCompiler_inline_result = peekCacheFromPool();
			JSCompiler_inline_result = null === JSCompiler_inline_result ? null : {
				parent: CacheContext._currentValue,
				pool: JSCompiler_inline_result
			};
			workInProgress.memoizedState = {
				baseLanes: nextBaseLanes,
				cachePool: JSCompiler_inline_result
			};
			null !== current && pushTransition(workInProgress, null);
			reuseHiddenContextOnStack(workInProgress);
			pushOffscreenSuspenseHandler(workInProgress);
			null !== current && propagateParentContextChanges(current, workInProgress, renderLanes, !0);
			workInProgress.childLanes = remainingChildLanes;
			return null;
		}
		function mountActivityChildren(workInProgress, nextProps) {
			var hiddenProp = nextProps.hidden;
			void 0 !== hiddenProp && console.error("<Activity> doesn't accept a hidden prop. Use mode=\"hidden\" instead.\n- <Activity %s>\n+ <Activity %s>", !0 === hiddenProp ? "hidden" : !1 === hiddenProp ? "hidden={false}" : "hidden={...}", hiddenProp ? "mode=\"hidden\"" : "mode=\"visible\"");
			nextProps = mountWorkInProgressOffscreenFiber({
				mode: nextProps.mode,
				children: nextProps.children
			}, workInProgress.mode);
			nextProps.ref = workInProgress.ref;
			workInProgress.child = nextProps;
			nextProps.return = workInProgress;
			return nextProps;
		}
		function retryActivityComponentWithoutHydrating(current, workInProgress, renderLanes) {
			reconcileChildFibers(workInProgress, current.child, null, renderLanes);
			current = mountActivityChildren(workInProgress, workInProgress.pendingProps);
			current.flags |= 2;
			popSuspenseHandler(workInProgress);
			workInProgress.memoizedState = null;
			return current;
		}
		function updateActivityComponent(current, workInProgress, renderLanes) {
			var nextProps = workInProgress.pendingProps, didSuspend = 0 !== (workInProgress.flags & 128);
			workInProgress.flags &= -129;
			if (null === current) {
				if (isHydrating) {
					if ("hidden" === nextProps.mode) return current = mountActivityChildren(workInProgress, nextProps), workInProgress.lanes = 536870912, bailoutOffscreenComponent(null, current);
					pushDehydratedActivitySuspenseHandler(workInProgress);
					(current = nextHydratableInstance) ? (renderLanes = canHydrateHydrationBoundary(current, rootOrSingletonContext), renderLanes = null !== renderLanes && renderLanes.data === ACTIVITY_START_DATA ? renderLanes : null, null !== renderLanes && (nextProps = {
						dehydrated: renderLanes,
						treeContext: getSuspendedTreeContext(),
						retryLane: 536870912,
						hydrationErrors: null
					}, workInProgress.memoizedState = nextProps, nextProps = createFiberFromDehydratedFragment(renderLanes), nextProps.return = workInProgress, workInProgress.child = nextProps, hydrationParentFiber = workInProgress, nextHydratableInstance = null)) : renderLanes = null;
					if (null === renderLanes) throw warnNonHydratedInstance(workInProgress, current), throwOnHydrationMismatch(workInProgress);
					workInProgress.lanes = 536870912;
					return null;
				}
				return mountActivityChildren(workInProgress, nextProps);
			}
			var prevState = current.memoizedState;
			if (null !== prevState) {
				var activityInstance = prevState.dehydrated;
				pushDehydratedActivitySuspenseHandler(workInProgress);
				if (didSuspend) if (workInProgress.flags & 256) workInProgress.flags &= -257, workInProgress = retryActivityComponentWithoutHydrating(current, workInProgress, renderLanes);
				else if (null !== workInProgress.memoizedState) workInProgress.child = current.child, workInProgress.flags |= 128, workInProgress = null;
				else throw Error("Client rendering an Activity suspended it again. This is a bug in React.");
				else if (warnIfHydrating(), 0 !== (renderLanes & 536870912) && markRenderDerivedCause(workInProgress), didReceiveUpdate || propagateParentContextChanges(current, workInProgress, renderLanes, !1), didSuspend = 0 !== (renderLanes & current.childLanes), didReceiveUpdate || didSuspend) {
					nextProps = workInProgressRoot;
					if (null !== nextProps && (activityInstance = getBumpedLaneForHydration(nextProps, renderLanes), 0 !== activityInstance && activityInstance !== prevState.retryLane)) throw prevState.retryLane = activityInstance, enqueueConcurrentRenderForLane(current, activityInstance), scheduleUpdateOnFiber(nextProps, current, activityInstance), SelectiveHydrationException;
					renderDidSuspendDelayIfPossible();
					workInProgress = retryActivityComponentWithoutHydrating(current, workInProgress, renderLanes);
				} else current = prevState.treeContext, nextHydratableInstance = getNextHydratable(activityInstance.nextSibling), hydrationParentFiber = workInProgress, isHydrating = !0, hydrationErrors = null, didSuspendOrErrorDEV = !1, hydrationDiffRootDEV = null, rootOrSingletonContext = !1, null !== current && restoreSuspendedTreeContext(workInProgress, current), workInProgress = mountActivityChildren(workInProgress, nextProps), workInProgress.flags |= 4096;
				return workInProgress;
			}
			prevState = current.child;
			nextProps = {
				mode: nextProps.mode,
				children: nextProps.children
			};
			0 !== (renderLanes & 536870912) && 0 !== (renderLanes & current.lanes) && markRenderDerivedCause(workInProgress);
			current = createWorkInProgress(prevState, nextProps);
			current.ref = workInProgress.ref;
			workInProgress.child = current;
			current.return = workInProgress;
			return current;
		}
		function markRef(current, workInProgress) {
			var ref = workInProgress.ref;
			if (null === ref) null !== current && null !== current.ref && (workInProgress.flags |= 4194816);
			else {
				if ("function" !== typeof ref && "object" !== typeof ref) throw Error("Expected ref to be a function, an object returned by React.createRef(), or undefined/null.");
				if (null === current || current.ref !== ref) workInProgress.flags |= 4194816;
			}
		}
		function updateFunctionComponent(current, workInProgress, Component, nextProps, renderLanes) {
			if (Component.prototype && "function" === typeof Component.prototype.render) {
				var componentName = getComponentNameFromType(Component) || "Unknown";
				didWarnAboutBadClass[componentName] || (console.error("The <%s /> component appears to have a render method, but doesn't extend React.Component. This is likely to cause errors. Change %s to extend React.Component instead.", componentName, componentName), didWarnAboutBadClass[componentName] = !0);
			}
			workInProgress.mode & StrictLegacyMode && ReactStrictModeWarnings.recordLegacyContextWarning(workInProgress, null);
			null === current && (validateFunctionComponentInDev(workInProgress, workInProgress.type), Component.contextTypes && (componentName = getComponentNameFromType(Component) || "Unknown", didWarnAboutContextTypes[componentName] || (didWarnAboutContextTypes[componentName] = !0, console.error("%s uses the legacy contextTypes API which was removed in React 19. Use React.createContext() with React.useContext() instead. (https://react.dev/link/legacy-context)", componentName))));
			prepareToReadContext(workInProgress);
			Component = renderWithHooks(current, workInProgress, Component, nextProps, void 0, renderLanes);
			nextProps = checkDidRenderIdHook();
			if (null !== current && !didReceiveUpdate) return bailoutHooks(current, workInProgress, renderLanes), bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
			isHydrating && nextProps && pushMaterializedTreeId(workInProgress);
			workInProgress.flags |= 1;
			reconcileChildren(current, workInProgress, Component, renderLanes);
			return workInProgress.child;
		}
		function replayFunctionComponent(current, workInProgress, nextProps, Component, secondArg, renderLanes) {
			prepareToReadContext(workInProgress);
			hookTypesUpdateIndexDev = -1;
			ignorePreviousDependencies = null !== current && current.type !== workInProgress.type;
			workInProgress.updateQueue = null;
			nextProps = renderWithHooksAgain(workInProgress, Component, nextProps, secondArg);
			finishRenderingHooks(current, workInProgress);
			Component = checkDidRenderIdHook();
			if (null !== current && !didReceiveUpdate) return bailoutHooks(current, workInProgress, renderLanes), bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
			isHydrating && Component && pushMaterializedTreeId(workInProgress);
			workInProgress.flags |= 1;
			reconcileChildren(current, workInProgress, nextProps, renderLanes);
			return workInProgress.child;
		}
		function updateClassComponent(current, workInProgress, Component, nextProps, renderLanes) {
			switch (shouldErrorImpl(workInProgress)) {
				case !1:
					var _instance = workInProgress.stateNode, state = new workInProgress.type(workInProgress.memoizedProps, _instance.context).state;
					_instance.updater.enqueueSetState(_instance, state, null);
					break;
				case !0:
					workInProgress.flags |= 128;
					workInProgress.flags |= 65536;
					_instance = Error("Simulated error coming from DevTools");
					var lane = renderLanes & -renderLanes;
					workInProgress.lanes |= lane;
					state = workInProgressRoot;
					if (null === state) throw Error("Expected a work-in-progress root. This is a bug in React. Please file an issue.");
					lane = createClassErrorUpdate(lane);
					initializeClassErrorUpdate(lane, state, workInProgress, createCapturedValueAtFiber(_instance, workInProgress));
					enqueueCapturedUpdate(workInProgress, lane);
			}
			prepareToReadContext(workInProgress);
			if (null === workInProgress.stateNode) {
				state = emptyContextObject;
				_instance = Component.contextType;
				"contextType" in Component && null !== _instance && (void 0 === _instance || _instance.$$typeof !== REACT_CONTEXT_TYPE) && !didWarnAboutInvalidateContextType.has(Component) && (didWarnAboutInvalidateContextType.add(Component), lane = void 0 === _instance ? " However, it is set to undefined. This can be caused by a typo or by mixing up named and default imports. This can also happen due to a circular dependency, so try moving the createContext() call to a separate file." : "object" !== typeof _instance ? " However, it is set to a " + typeof _instance + "." : _instance.$$typeof === REACT_CONSUMER_TYPE ? " Did you accidentally pass the Context.Consumer instead?" : " However, it is set to an object with keys {" + Object.keys(_instance).join(", ") + "}.", console.error("%s defines an invalid contextType. contextType should point to the Context object returned by React.createContext().%s", getComponentNameFromType(Component) || "Component", lane));
				"object" === typeof _instance && null !== _instance && (state = readContext(_instance));
				_instance = new Component(nextProps, state);
				if (workInProgress.mode & StrictLegacyMode) {
					setIsStrictModeForDevtools(!0);
					try {
						_instance = new Component(nextProps, state);
					} finally {
						setIsStrictModeForDevtools(!1);
					}
				}
				state = workInProgress.memoizedState = null !== _instance.state && void 0 !== _instance.state ? _instance.state : null;
				_instance.updater = classComponentUpdater;
				workInProgress.stateNode = _instance;
				_instance._reactInternals = workInProgress;
				_instance._reactInternalInstance = fakeInternalInstance;
				"function" === typeof Component.getDerivedStateFromProps && null === state && (state = getComponentNameFromType(Component) || "Component", didWarnAboutUninitializedState.has(state) || (didWarnAboutUninitializedState.add(state), console.error("`%s` uses `getDerivedStateFromProps` but its initial state is %s. This is not recommended. Instead, define the initial state by assigning an object to `this.state` in the constructor of `%s`. This ensures that `getDerivedStateFromProps` arguments have a consistent shape.", state, null === _instance.state ? "null" : "undefined", state)));
				if ("function" === typeof Component.getDerivedStateFromProps || "function" === typeof _instance.getSnapshotBeforeUpdate) {
					var foundWillUpdateName = lane = state = null;
					"function" === typeof _instance.componentWillMount && !0 !== _instance.componentWillMount.__suppressDeprecationWarning ? state = "componentWillMount" : "function" === typeof _instance.UNSAFE_componentWillMount && (state = "UNSAFE_componentWillMount");
					"function" === typeof _instance.componentWillReceiveProps && !0 !== _instance.componentWillReceiveProps.__suppressDeprecationWarning ? lane = "componentWillReceiveProps" : "function" === typeof _instance.UNSAFE_componentWillReceiveProps && (lane = "UNSAFE_componentWillReceiveProps");
					"function" === typeof _instance.componentWillUpdate && !0 !== _instance.componentWillUpdate.__suppressDeprecationWarning ? foundWillUpdateName = "componentWillUpdate" : "function" === typeof _instance.UNSAFE_componentWillUpdate && (foundWillUpdateName = "UNSAFE_componentWillUpdate");
					if (null !== state || null !== lane || null !== foundWillUpdateName) {
						_instance = getComponentNameFromType(Component) || "Component";
						var newApiName = "function" === typeof Component.getDerivedStateFromProps ? "getDerivedStateFromProps()" : "getSnapshotBeforeUpdate()";
						didWarnAboutLegacyLifecyclesAndDerivedState.has(_instance) || (didWarnAboutLegacyLifecyclesAndDerivedState.add(_instance), console.error("Unsafe legacy lifecycles will not be called for components using new component APIs.\n\n%s uses %s but also contains the following legacy lifecycles:%s%s%s\n\nThe above lifecycles should be removed. Learn more about this warning here:\nhttps://react.dev/link/unsafe-component-lifecycles", _instance, newApiName, null !== state ? "\n  " + state : "", null !== lane ? "\n  " + lane : "", null !== foundWillUpdateName ? "\n  " + foundWillUpdateName : ""));
					}
				}
				_instance = workInProgress.stateNode;
				state = getComponentNameFromType(Component) || "Component";
				_instance.render || (Component.prototype && "function" === typeof Component.prototype.render ? console.error("No `render` method found on the %s instance: did you accidentally return an object from the constructor?", state) : console.error("No `render` method found on the %s instance: you may have forgotten to define `render`.", state));
				!_instance.getInitialState || _instance.getInitialState.isReactClassApproved || _instance.state || console.error("getInitialState was defined on %s, a plain JavaScript class. This is only supported for classes created using React.createClass. Did you mean to define a state property instead?", state);
				_instance.getDefaultProps && !_instance.getDefaultProps.isReactClassApproved && console.error("getDefaultProps was defined on %s, a plain JavaScript class. This is only supported for classes created using React.createClass. Use a static property to define defaultProps instead.", state);
				_instance.contextType && console.error("contextType was defined as an instance property on %s. Use a static property to define contextType instead.", state);
				Component.childContextTypes && !didWarnAboutChildContextTypes.has(Component) && (didWarnAboutChildContextTypes.add(Component), console.error("%s uses the legacy childContextTypes API which was removed in React 19. Use React.createContext() instead. (https://react.dev/link/legacy-context)", state));
				Component.contextTypes && !didWarnAboutContextTypes$1.has(Component) && (didWarnAboutContextTypes$1.add(Component), console.error("%s uses the legacy contextTypes API which was removed in React 19. Use React.createContext() with static contextType instead. (https://react.dev/link/legacy-context)", state));
				"function" === typeof _instance.componentShouldUpdate && console.error("%s has a method called componentShouldUpdate(). Did you mean shouldComponentUpdate()? The name is phrased as a question because the function is expected to return a value.", state);
				Component.prototype && Component.prototype.isPureReactComponent && "undefined" !== typeof _instance.shouldComponentUpdate && console.error("%s has a method called shouldComponentUpdate(). shouldComponentUpdate should not be used when extending React.PureComponent. Please extend React.Component if shouldComponentUpdate is used.", getComponentNameFromType(Component) || "A pure component");
				"function" === typeof _instance.componentDidUnmount && console.error("%s has a method called componentDidUnmount(). But there is no such lifecycle method. Did you mean componentWillUnmount()?", state);
				"function" === typeof _instance.componentDidReceiveProps && console.error("%s has a method called componentDidReceiveProps(). But there is no such lifecycle method. If you meant to update the state in response to changing props, use componentWillReceiveProps(). If you meant to fetch data or run side-effects or mutations after React has updated the UI, use componentDidUpdate().", state);
				"function" === typeof _instance.componentWillRecieveProps && console.error("%s has a method called componentWillRecieveProps(). Did you mean componentWillReceiveProps()?", state);
				"function" === typeof _instance.UNSAFE_componentWillRecieveProps && console.error("%s has a method called UNSAFE_componentWillRecieveProps(). Did you mean UNSAFE_componentWillReceiveProps()?", state);
				lane = _instance.props !== nextProps;
				void 0 !== _instance.props && lane && console.error("When calling super() in `%s`, make sure to pass up the same props that your component's constructor was passed.", state);
				_instance.defaultProps && console.error("Setting defaultProps as an instance property on %s is not supported and will be ignored. Instead, define defaultProps as a static property on %s.", state, state);
				"function" !== typeof _instance.getSnapshotBeforeUpdate || "function" === typeof _instance.componentDidUpdate || didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.has(Component) || (didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.add(Component), console.error("%s: getSnapshotBeforeUpdate() should be used with componentDidUpdate(). This component defines getSnapshotBeforeUpdate() only.", getComponentNameFromType(Component)));
				"function" === typeof _instance.getDerivedStateFromProps && console.error("%s: getDerivedStateFromProps() is defined as an instance method and will be ignored. Instead, declare it as a static method.", state);
				"function" === typeof _instance.getDerivedStateFromError && console.error("%s: getDerivedStateFromError() is defined as an instance method and will be ignored. Instead, declare it as a static method.", state);
				"function" === typeof Component.getSnapshotBeforeUpdate && console.error("%s: getSnapshotBeforeUpdate() is defined as a static method and will be ignored. Instead, declare it as an instance method.", state);
				(lane = _instance.state) && ("object" !== typeof lane || isArrayImpl(lane)) && console.error("%s.state: must be set to an object or null", state);
				"function" === typeof _instance.getChildContext && "object" !== typeof Component.childContextTypes && console.error("%s.getChildContext(): childContextTypes must be defined in order to use getChildContext().", state);
				_instance = workInProgress.stateNode;
				_instance.props = nextProps;
				_instance.state = workInProgress.memoizedState;
				_instance.refs = {};
				initializeUpdateQueue(workInProgress);
				state = Component.contextType;
				_instance.context = "object" === typeof state && null !== state ? readContext(state) : emptyContextObject;
				_instance.state === nextProps && (state = getComponentNameFromType(Component) || "Component", didWarnAboutDirectlyAssigningPropsToState.has(state) || (didWarnAboutDirectlyAssigningPropsToState.add(state), console.error("%s: It is not recommended to assign props directly to state because updates to props won't be reflected in state. In most cases, it is better to use props directly.", state)));
				workInProgress.mode & StrictLegacyMode && ReactStrictModeWarnings.recordLegacyContextWarning(workInProgress, _instance);
				ReactStrictModeWarnings.recordUnsafeLifecycleWarnings(workInProgress, _instance);
				_instance.state = workInProgress.memoizedState;
				state = Component.getDerivedStateFromProps;
				"function" === typeof state && (applyDerivedStateFromProps(workInProgress, Component, state, nextProps), _instance.state = workInProgress.memoizedState);
				"function" === typeof Component.getDerivedStateFromProps || "function" === typeof _instance.getSnapshotBeforeUpdate || "function" !== typeof _instance.UNSAFE_componentWillMount && "function" !== typeof _instance.componentWillMount || (state = _instance.state, "function" === typeof _instance.componentWillMount && _instance.componentWillMount(), "function" === typeof _instance.UNSAFE_componentWillMount && _instance.UNSAFE_componentWillMount(), state !== _instance.state && (console.error("%s.componentWillMount(): Assigning directly to this.state is deprecated (except inside a component's constructor). Use setState instead.", getComponentNameFromFiber(workInProgress) || "Component"), classComponentUpdater.enqueueReplaceState(_instance, _instance.state, null)), processUpdateQueue(workInProgress, nextProps, _instance, renderLanes), suspendIfUpdateReadFromEntangledAsyncAction(), _instance.state = workInProgress.memoizedState);
				"function" === typeof _instance.componentDidMount && (workInProgress.flags |= 4194308);
				(workInProgress.mode & StrictEffectsMode) !== NoMode && (workInProgress.flags |= 134217728);
				_instance = !0;
			} else if (null === current) {
				_instance = workInProgress.stateNode;
				var unresolvedOldProps = workInProgress.memoizedProps;
				lane = resolveClassComponentProps(Component, unresolvedOldProps);
				_instance.props = lane;
				var oldContext = _instance.context;
				foundWillUpdateName = Component.contextType;
				state = emptyContextObject;
				"object" === typeof foundWillUpdateName && null !== foundWillUpdateName && (state = readContext(foundWillUpdateName));
				newApiName = Component.getDerivedStateFromProps;
				foundWillUpdateName = "function" === typeof newApiName || "function" === typeof _instance.getSnapshotBeforeUpdate;
				unresolvedOldProps = workInProgress.pendingProps !== unresolvedOldProps;
				foundWillUpdateName || "function" !== typeof _instance.UNSAFE_componentWillReceiveProps && "function" !== typeof _instance.componentWillReceiveProps || (unresolvedOldProps || oldContext !== state) && callComponentWillReceiveProps(workInProgress, _instance, nextProps, state);
				hasForceUpdate = !1;
				var oldState = workInProgress.memoizedState;
				_instance.state = oldState;
				processUpdateQueue(workInProgress, nextProps, _instance, renderLanes);
				suspendIfUpdateReadFromEntangledAsyncAction();
				oldContext = workInProgress.memoizedState;
				unresolvedOldProps || oldState !== oldContext || hasForceUpdate ? ("function" === typeof newApiName && (applyDerivedStateFromProps(workInProgress, Component, newApiName, nextProps), oldContext = workInProgress.memoizedState), (lane = hasForceUpdate || checkShouldComponentUpdate(workInProgress, Component, lane, nextProps, oldState, oldContext, state)) ? (foundWillUpdateName || "function" !== typeof _instance.UNSAFE_componentWillMount && "function" !== typeof _instance.componentWillMount || ("function" === typeof _instance.componentWillMount && _instance.componentWillMount(), "function" === typeof _instance.UNSAFE_componentWillMount && _instance.UNSAFE_componentWillMount()), "function" === typeof _instance.componentDidMount && (workInProgress.flags |= 4194308), (workInProgress.mode & StrictEffectsMode) !== NoMode && (workInProgress.flags |= 134217728)) : ("function" === typeof _instance.componentDidMount && (workInProgress.flags |= 4194308), (workInProgress.mode & StrictEffectsMode) !== NoMode && (workInProgress.flags |= 134217728), workInProgress.memoizedProps = nextProps, workInProgress.memoizedState = oldContext), _instance.props = nextProps, _instance.state = oldContext, _instance.context = state, _instance = lane) : ("function" === typeof _instance.componentDidMount && (workInProgress.flags |= 4194308), (workInProgress.mode & StrictEffectsMode) !== NoMode && (workInProgress.flags |= 134217728), _instance = !1);
			} else {
				_instance = workInProgress.stateNode;
				cloneUpdateQueue(current, workInProgress);
				state = workInProgress.memoizedProps;
				foundWillUpdateName = resolveClassComponentProps(Component, state);
				_instance.props = foundWillUpdateName;
				newApiName = workInProgress.pendingProps;
				oldState = _instance.context;
				oldContext = Component.contextType;
				lane = emptyContextObject;
				"object" === typeof oldContext && null !== oldContext && (lane = readContext(oldContext));
				unresolvedOldProps = Component.getDerivedStateFromProps;
				(oldContext = "function" === typeof unresolvedOldProps || "function" === typeof _instance.getSnapshotBeforeUpdate) || "function" !== typeof _instance.UNSAFE_componentWillReceiveProps && "function" !== typeof _instance.componentWillReceiveProps || (state !== newApiName || oldState !== lane) && callComponentWillReceiveProps(workInProgress, _instance, nextProps, lane);
				hasForceUpdate = !1;
				oldState = workInProgress.memoizedState;
				_instance.state = oldState;
				processUpdateQueue(workInProgress, nextProps, _instance, renderLanes);
				suspendIfUpdateReadFromEntangledAsyncAction();
				var newState = workInProgress.memoizedState;
				state !== newApiName || oldState !== newState || hasForceUpdate || null !== current && null !== current.dependencies && checkIfContextChanged(current.dependencies) ? ("function" === typeof unresolvedOldProps && (applyDerivedStateFromProps(workInProgress, Component, unresolvedOldProps, nextProps), newState = workInProgress.memoizedState), (foundWillUpdateName = hasForceUpdate || checkShouldComponentUpdate(workInProgress, Component, foundWillUpdateName, nextProps, oldState, newState, lane) || null !== current && null !== current.dependencies && checkIfContextChanged(current.dependencies)) ? (oldContext || "function" !== typeof _instance.UNSAFE_componentWillUpdate && "function" !== typeof _instance.componentWillUpdate || ("function" === typeof _instance.componentWillUpdate && _instance.componentWillUpdate(nextProps, newState, lane), "function" === typeof _instance.UNSAFE_componentWillUpdate && _instance.UNSAFE_componentWillUpdate(nextProps, newState, lane)), "function" === typeof _instance.componentDidUpdate && (workInProgress.flags |= 4), "function" === typeof _instance.getSnapshotBeforeUpdate && (workInProgress.flags |= 1024)) : ("function" !== typeof _instance.componentDidUpdate || state === current.memoizedProps && oldState === current.memoizedState || (workInProgress.flags |= 4), "function" !== typeof _instance.getSnapshotBeforeUpdate || state === current.memoizedProps && oldState === current.memoizedState || (workInProgress.flags |= 1024), workInProgress.memoizedProps = nextProps, workInProgress.memoizedState = newState), _instance.props = nextProps, _instance.state = newState, _instance.context = lane, _instance = foundWillUpdateName) : ("function" !== typeof _instance.componentDidUpdate || state === current.memoizedProps && oldState === current.memoizedState || (workInProgress.flags |= 4), "function" !== typeof _instance.getSnapshotBeforeUpdate || state === current.memoizedProps && oldState === current.memoizedState || (workInProgress.flags |= 1024), _instance = !1);
			}
			lane = _instance;
			markRef(current, workInProgress);
			state = 0 !== (workInProgress.flags & 128);
			if (lane || state) {
				lane = workInProgress.stateNode;
				setCurrentFiber(workInProgress);
				if (state && "function" !== typeof Component.getDerivedStateFromError) Component = null, profilerStartTime = -1;
				else if (Component = callRenderInDEV(lane), workInProgress.mode & StrictLegacyMode) {
					setIsStrictModeForDevtools(!0);
					try {
						callRenderInDEV(lane);
					} finally {
						setIsStrictModeForDevtools(!1);
					}
				}
				workInProgress.flags |= 1;
				null !== current && state ? (workInProgress.child = reconcileChildFibers(workInProgress, current.child, null, renderLanes), workInProgress.child = reconcileChildFibers(workInProgress, null, Component, renderLanes)) : reconcileChildren(current, workInProgress, Component, renderLanes);
				workInProgress.memoizedState = lane.state;
				current = workInProgress.child;
			} else current = bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
			renderLanes = workInProgress.stateNode;
			_instance && renderLanes.props !== nextProps && (didWarnAboutReassigningProps || console.error("It looks like %s is reassigning its own `this.props` while rendering. This is not supported and can lead to confusing bugs.", getComponentNameFromFiber(workInProgress) || "a component"), didWarnAboutReassigningProps = !0);
			return current;
		}
		function mountHostRootWithoutHydrating(current, workInProgress, nextChildren, renderLanes) {
			resetHydrationState();
			workInProgress.flags |= 256;
			reconcileChildren(current, workInProgress, nextChildren, renderLanes);
			return workInProgress.child;
		}
		function validateFunctionComponentInDev(workInProgress, Component) {
			Component && Component.childContextTypes && console.error("childContextTypes cannot be defined on a function component.\n  %s.childContextTypes = ...", Component.displayName || Component.name || "Component");
			"function" === typeof Component.getDerivedStateFromProps && (workInProgress = getComponentNameFromType(Component) || "Unknown", didWarnAboutGetDerivedStateOnFunctionComponent[workInProgress] || (console.error("%s: Function components do not support getDerivedStateFromProps.", workInProgress), didWarnAboutGetDerivedStateOnFunctionComponent[workInProgress] = !0));
			"object" === typeof Component.contextType && null !== Component.contextType && (Component = getComponentNameFromType(Component) || "Unknown", didWarnAboutContextTypeOnFunctionComponent[Component] || (console.error("%s: Function components do not support contextType.", Component), didWarnAboutContextTypeOnFunctionComponent[Component] = !0));
		}
		function mountSuspenseOffscreenState(renderLanes) {
			return {
				baseLanes: renderLanes,
				cachePool: getSuspendedCache()
			};
		}
		function getRemainingWorkInPrimaryTree(current, primaryTreeDidDefer, renderLanes) {
			current = null !== current ? current.childLanes & ~renderLanes : 0;
			primaryTreeDidDefer && (current |= workInProgressDeferredLane);
			return current;
		}
		function updateSuspenseComponent(current, workInProgress, renderLanes) {
			var JSCompiler_object_inline_digest_2724;
			var JSCompiler_object_inline_stack_2725 = workInProgress.pendingProps;
			shouldSuspendImpl(workInProgress) && (workInProgress.flags |= 128);
			var JSCompiler_object_inline_message_2723 = !1;
			var didSuspend = 0 !== (workInProgress.flags & 128);
			(JSCompiler_object_inline_digest_2724 = didSuspend) || (JSCompiler_object_inline_digest_2724 = null !== current && null === current.memoizedState ? !1 : 0 !== (suspenseStackCursor.current & ForceSuspenseFallback));
			JSCompiler_object_inline_digest_2724 && (JSCompiler_object_inline_message_2723 = !0, workInProgress.flags &= -129);
			JSCompiler_object_inline_digest_2724 = 0 !== (workInProgress.flags & 32);
			workInProgress.flags &= -33;
			if (null === current) {
				if (isHydrating) {
					JSCompiler_object_inline_message_2723 ? pushPrimaryTreeSuspenseHandler(workInProgress) : reuseSuspenseHandlerOnStack(workInProgress);
					(current = nextHydratableInstance) ? (renderLanes = canHydrateHydrationBoundary(current, rootOrSingletonContext), renderLanes = null !== renderLanes && renderLanes.data !== ACTIVITY_START_DATA ? renderLanes : null, null !== renderLanes && (JSCompiler_object_inline_digest_2724 = {
						dehydrated: renderLanes,
						treeContext: getSuspendedTreeContext(),
						retryLane: 536870912,
						hydrationErrors: null
					}, workInProgress.memoizedState = JSCompiler_object_inline_digest_2724, JSCompiler_object_inline_digest_2724 = createFiberFromDehydratedFragment(renderLanes), JSCompiler_object_inline_digest_2724.return = workInProgress, workInProgress.child = JSCompiler_object_inline_digest_2724, hydrationParentFiber = workInProgress, nextHydratableInstance = null)) : renderLanes = null;
					if (null === renderLanes) throw warnNonHydratedInstance(workInProgress, current), throwOnHydrationMismatch(workInProgress);
					isSuspenseInstanceFallback(renderLanes) ? workInProgress.lanes = 32 : workInProgress.lanes = 536870912;
					return null;
				}
				var nextPrimaryChildren = JSCompiler_object_inline_stack_2725.children;
				JSCompiler_object_inline_stack_2725 = JSCompiler_object_inline_stack_2725.fallback;
				if (JSCompiler_object_inline_message_2723) {
					reuseSuspenseHandlerOnStack(workInProgress);
					var mode = workInProgress.mode;
					nextPrimaryChildren = mountWorkInProgressOffscreenFiber({
						mode: "hidden",
						children: nextPrimaryChildren
					}, mode);
					JSCompiler_object_inline_stack_2725 = createFiberFromFragment(JSCompiler_object_inline_stack_2725, mode, renderLanes, null);
					nextPrimaryChildren.return = workInProgress;
					JSCompiler_object_inline_stack_2725.return = workInProgress;
					nextPrimaryChildren.sibling = JSCompiler_object_inline_stack_2725;
					workInProgress.child = nextPrimaryChildren;
					JSCompiler_object_inline_stack_2725 = workInProgress.child;
					JSCompiler_object_inline_stack_2725.memoizedState = mountSuspenseOffscreenState(renderLanes);
					JSCompiler_object_inline_stack_2725.childLanes = getRemainingWorkInPrimaryTree(current, JSCompiler_object_inline_digest_2724, renderLanes);
					workInProgress.memoizedState = SUSPENDED_MARKER;
					return bailoutOffscreenComponent(null, JSCompiler_object_inline_stack_2725);
				}
				pushPrimaryTreeSuspenseHandler(workInProgress);
				return mountSuspensePrimaryChildren(workInProgress, nextPrimaryChildren);
			}
			var prevState = current.memoizedState;
			if (null !== prevState) {
				var JSCompiler_object_inline_componentStack_2726 = prevState.dehydrated;
				if (null !== JSCompiler_object_inline_componentStack_2726) {
					if (didSuspend) workInProgress.flags & 256 ? (pushPrimaryTreeSuspenseHandler(workInProgress), workInProgress.flags &= -257, workInProgress = retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes)) : null !== workInProgress.memoizedState ? (reuseSuspenseHandlerOnStack(workInProgress), workInProgress.child = current.child, workInProgress.flags |= 128, workInProgress = null) : (reuseSuspenseHandlerOnStack(workInProgress), nextPrimaryChildren = JSCompiler_object_inline_stack_2725.fallback, mode = workInProgress.mode, JSCompiler_object_inline_stack_2725 = mountWorkInProgressOffscreenFiber({
						mode: "visible",
						children: JSCompiler_object_inline_stack_2725.children
					}, mode), nextPrimaryChildren = createFiberFromFragment(nextPrimaryChildren, mode, renderLanes, null), nextPrimaryChildren.flags |= 2, JSCompiler_object_inline_stack_2725.return = workInProgress, nextPrimaryChildren.return = workInProgress, JSCompiler_object_inline_stack_2725.sibling = nextPrimaryChildren, workInProgress.child = JSCompiler_object_inline_stack_2725, reconcileChildFibers(workInProgress, current.child, null, renderLanes), JSCompiler_object_inline_stack_2725 = workInProgress.child, JSCompiler_object_inline_stack_2725.memoizedState = mountSuspenseOffscreenState(renderLanes), JSCompiler_object_inline_stack_2725.childLanes = getRemainingWorkInPrimaryTree(current, JSCompiler_object_inline_digest_2724, renderLanes), workInProgress.memoizedState = SUSPENDED_MARKER, workInProgress = bailoutOffscreenComponent(null, JSCompiler_object_inline_stack_2725));
					else if (pushPrimaryTreeSuspenseHandler(workInProgress), warnIfHydrating(), 0 !== (renderLanes & 536870912) && markRenderDerivedCause(workInProgress), isSuspenseInstanceFallback(JSCompiler_object_inline_componentStack_2726)) {
						JSCompiler_object_inline_digest_2724 = JSCompiler_object_inline_componentStack_2726.nextSibling && JSCompiler_object_inline_componentStack_2726.nextSibling.dataset;
						if (JSCompiler_object_inline_digest_2724) {
							nextPrimaryChildren = JSCompiler_object_inline_digest_2724.dgst;
							var message = JSCompiler_object_inline_digest_2724.msg;
							mode = JSCompiler_object_inline_digest_2724.stck;
							var componentStack = JSCompiler_object_inline_digest_2724.cstck;
						}
						JSCompiler_object_inline_message_2723 = message;
						JSCompiler_object_inline_digest_2724 = nextPrimaryChildren;
						JSCompiler_object_inline_stack_2725 = mode;
						JSCompiler_object_inline_componentStack_2726 = componentStack;
						nextPrimaryChildren = JSCompiler_object_inline_message_2723;
						mode = JSCompiler_object_inline_componentStack_2726;
						nextPrimaryChildren = nextPrimaryChildren ? Error(nextPrimaryChildren) : Error("The server could not finish this Suspense boundary, likely due to an error during server rendering. Switched to client rendering.");
						nextPrimaryChildren.stack = JSCompiler_object_inline_stack_2725 || "";
						nextPrimaryChildren.digest = JSCompiler_object_inline_digest_2724;
						JSCompiler_object_inline_digest_2724 = void 0 === mode ? null : mode;
						JSCompiler_object_inline_stack_2725 = {
							value: nextPrimaryChildren,
							source: null,
							stack: JSCompiler_object_inline_digest_2724
						};
						"string" === typeof JSCompiler_object_inline_digest_2724 && CapturedStacks.set(nextPrimaryChildren, JSCompiler_object_inline_stack_2725);
						queueHydrationError(JSCompiler_object_inline_stack_2725);
						workInProgress = retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes);
					} else if (didReceiveUpdate || propagateParentContextChanges(current, workInProgress, renderLanes, !1), JSCompiler_object_inline_digest_2724 = 0 !== (renderLanes & current.childLanes), didReceiveUpdate || JSCompiler_object_inline_digest_2724) {
						JSCompiler_object_inline_digest_2724 = workInProgressRoot;
						if (null !== JSCompiler_object_inline_digest_2724 && (JSCompiler_object_inline_stack_2725 = getBumpedLaneForHydration(JSCompiler_object_inline_digest_2724, renderLanes), 0 !== JSCompiler_object_inline_stack_2725 && JSCompiler_object_inline_stack_2725 !== prevState.retryLane)) throw prevState.retryLane = JSCompiler_object_inline_stack_2725, enqueueConcurrentRenderForLane(current, JSCompiler_object_inline_stack_2725), scheduleUpdateOnFiber(JSCompiler_object_inline_digest_2724, current, JSCompiler_object_inline_stack_2725), SelectiveHydrationException;
						isSuspenseInstancePending(JSCompiler_object_inline_componentStack_2726) || renderDidSuspendDelayIfPossible();
						workInProgress = retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes);
					} else isSuspenseInstancePending(JSCompiler_object_inline_componentStack_2726) ? (workInProgress.flags |= 192, workInProgress.child = current.child, workInProgress = null) : (current = prevState.treeContext, nextHydratableInstance = getNextHydratable(JSCompiler_object_inline_componentStack_2726.nextSibling), hydrationParentFiber = workInProgress, isHydrating = !0, hydrationErrors = null, didSuspendOrErrorDEV = !1, hydrationDiffRootDEV = null, rootOrSingletonContext = !1, null !== current && restoreSuspendedTreeContext(workInProgress, current), workInProgress = mountSuspensePrimaryChildren(workInProgress, JSCompiler_object_inline_stack_2725.children), workInProgress.flags |= 4096);
					return workInProgress;
				}
			}
			if (JSCompiler_object_inline_message_2723) return reuseSuspenseHandlerOnStack(workInProgress), nextPrimaryChildren = JSCompiler_object_inline_stack_2725.fallback, mode = workInProgress.mode, componentStack = current.child, JSCompiler_object_inline_componentStack_2726 = componentStack.sibling, JSCompiler_object_inline_stack_2725 = createWorkInProgress(componentStack, {
				mode: "hidden",
				children: JSCompiler_object_inline_stack_2725.children
			}), JSCompiler_object_inline_stack_2725.subtreeFlags = componentStack.subtreeFlags & 65011712, null !== JSCompiler_object_inline_componentStack_2726 ? nextPrimaryChildren = createWorkInProgress(JSCompiler_object_inline_componentStack_2726, nextPrimaryChildren) : (nextPrimaryChildren = createFiberFromFragment(nextPrimaryChildren, mode, renderLanes, null), nextPrimaryChildren.flags |= 2), nextPrimaryChildren.return = workInProgress, JSCompiler_object_inline_stack_2725.return = workInProgress, JSCompiler_object_inline_stack_2725.sibling = nextPrimaryChildren, workInProgress.child = JSCompiler_object_inline_stack_2725, bailoutOffscreenComponent(null, JSCompiler_object_inline_stack_2725), JSCompiler_object_inline_stack_2725 = workInProgress.child, nextPrimaryChildren = current.child.memoizedState, null === nextPrimaryChildren ? nextPrimaryChildren = mountSuspenseOffscreenState(renderLanes) : (mode = nextPrimaryChildren.cachePool, null !== mode ? (componentStack = CacheContext._currentValue, mode = mode.parent !== componentStack ? {
				parent: componentStack,
				pool: componentStack
			} : mode) : mode = getSuspendedCache(), nextPrimaryChildren = {
				baseLanes: nextPrimaryChildren.baseLanes | renderLanes,
				cachePool: mode
			}), JSCompiler_object_inline_stack_2725.memoizedState = nextPrimaryChildren, JSCompiler_object_inline_stack_2725.childLanes = getRemainingWorkInPrimaryTree(current, JSCompiler_object_inline_digest_2724, renderLanes), workInProgress.memoizedState = SUSPENDED_MARKER, bailoutOffscreenComponent(current.child, JSCompiler_object_inline_stack_2725);
			null !== prevState && (renderLanes & 62914560) === renderLanes && 0 !== (renderLanes & current.lanes) && markRenderDerivedCause(workInProgress);
			pushPrimaryTreeSuspenseHandler(workInProgress);
			renderLanes = current.child;
			current = renderLanes.sibling;
			renderLanes = createWorkInProgress(renderLanes, {
				mode: "visible",
				children: JSCompiler_object_inline_stack_2725.children
			});
			renderLanes.return = workInProgress;
			renderLanes.sibling = null;
			null !== current && (JSCompiler_object_inline_digest_2724 = workInProgress.deletions, null === JSCompiler_object_inline_digest_2724 ? (workInProgress.deletions = [current], workInProgress.flags |= 16) : JSCompiler_object_inline_digest_2724.push(current));
			workInProgress.child = renderLanes;
			workInProgress.memoizedState = null;
			return renderLanes;
		}
		function mountSuspensePrimaryChildren(workInProgress, primaryChildren) {
			primaryChildren = mountWorkInProgressOffscreenFiber({
				mode: "visible",
				children: primaryChildren
			}, workInProgress.mode);
			primaryChildren.return = workInProgress;
			return workInProgress.child = primaryChildren;
		}
		function mountWorkInProgressOffscreenFiber(offscreenProps, mode) {
			offscreenProps = createFiber(22, offscreenProps, null, mode);
			offscreenProps.lanes = 0;
			return offscreenProps;
		}
		function retrySuspenseComponentWithoutHydrating(current, workInProgress, renderLanes) {
			reconcileChildFibers(workInProgress, current.child, null, renderLanes);
			current = mountSuspensePrimaryChildren(workInProgress, workInProgress.pendingProps.children);
			current.flags |= 2;
			workInProgress.memoizedState = null;
			return current;
		}
		function scheduleSuspenseWorkOnFiber(fiber, renderLanes, propagationRoot) {
			fiber.lanes |= renderLanes;
			var alternate = fiber.alternate;
			null !== alternate && (alternate.lanes |= renderLanes);
			scheduleContextWorkOnParentPath(fiber.return, renderLanes, propagationRoot);
		}
		function initSuspenseListRenderState(workInProgress, isBackwards, tail, lastContentRow, tailMode, treeForkCount) {
			var renderState = workInProgress.memoizedState;
			null === renderState ? workInProgress.memoizedState = {
				isBackwards,
				rendering: null,
				renderingStartTime: 0,
				last: lastContentRow,
				tail,
				tailMode,
				treeForkCount
			} : (renderState.isBackwards = isBackwards, renderState.rendering = null, renderState.renderingStartTime = 0, renderState.last = lastContentRow, renderState.tail = tail, renderState.tailMode = tailMode, renderState.treeForkCount = treeForkCount);
		}
		function updateSuspenseListComponent(current, workInProgress, renderLanes) {
			var nextProps = workInProgress.pendingProps, revealOrder = nextProps.revealOrder, tailMode = nextProps.tail, newChildren = nextProps.children, suspenseContext = suspenseStackCursor.current;
			(nextProps = 0 !== (suspenseContext & ForceSuspenseFallback)) ? (suspenseContext = suspenseContext & SubtreeSuspenseContextMask | ForceSuspenseFallback, workInProgress.flags |= 128) : suspenseContext &= SubtreeSuspenseContextMask;
			push(suspenseStackCursor, suspenseContext, workInProgress);
			suspenseContext = null == revealOrder ? "null" : revealOrder;
			if ("forwards" !== revealOrder && "unstable_legacy-backwards" !== revealOrder && "together" !== revealOrder && "independent" !== revealOrder && !didWarnAboutRevealOrder[suspenseContext]) if (didWarnAboutRevealOrder[suspenseContext] = !0, null == revealOrder) console.error("The default for the <SuspenseList revealOrder=\"...\"> prop is changing. To be future compatible you must explictly specify either \"independent\" (the current default), \"together\", \"forwards\" or \"legacy_unstable-backwards\".");
			else if ("backwards" === revealOrder) console.error("The rendering order of <SuspenseList revealOrder=\"backwards\"> is changing. To be future compatible you must specify revealOrder=\"legacy_unstable-backwards\" instead.");
			else if ("string" === typeof revealOrder) switch (revealOrder.toLowerCase()) {
				case "together":
				case "forwards":
				case "backwards":
				case "independent":
					console.error("\"%s\" is not a valid value for revealOrder on <SuspenseList />. Use lowercase \"%s\" instead.", revealOrder, revealOrder.toLowerCase());
					break;
				case "forward":
				case "backward":
					console.error("\"%s\" is not a valid value for revealOrder on <SuspenseList />. React uses the -s suffix in the spelling. Use \"%ss\" instead.", revealOrder, revealOrder.toLowerCase());
					break;
				default: console.error("\"%s\" is not a supported revealOrder on <SuspenseList />. Did you mean \"independent\", \"together\", \"forwards\" or \"backwards\"?", revealOrder);
			}
			else console.error("%s is not a supported value for revealOrder on <SuspenseList />. Did you mean \"independent\", \"together\", \"forwards\" or \"backwards\"?", revealOrder);
			suspenseContext = null == tailMode ? "null" : tailMode;
			if (!didWarnAboutTailOptions[suspenseContext]) if (null == tailMode) {
				if ("forwards" === revealOrder || "backwards" === revealOrder || "unstable_legacy-backwards" === revealOrder) didWarnAboutTailOptions[suspenseContext] = !0, console.error("The default for the <SuspenseList tail=\"...\"> prop is changing. To be future compatible you must explictly specify either \"visible\" (the current default), \"collapsed\" or \"hidden\".");
			} else "visible" !== tailMode && "collapsed" !== tailMode && "hidden" !== tailMode ? (didWarnAboutTailOptions[suspenseContext] = !0, console.error("\"%s\" is not a supported value for tail on <SuspenseList />. Did you mean \"visible\", \"collapsed\" or \"hidden\"?", tailMode)) : "forwards" !== revealOrder && "backwards" !== revealOrder && "unstable_legacy-backwards" !== revealOrder && (didWarnAboutTailOptions[suspenseContext] = !0, console.error("<SuspenseList tail=\"%s\" /> is only valid if revealOrder is \"forwards\" or \"backwards\". Did you mean to specify revealOrder=\"forwards\"?", tailMode));
			a: if (("forwards" === revealOrder || "backwards" === revealOrder || "unstable_legacy-backwards" === revealOrder) && void 0 !== newChildren && null !== newChildren && !1 !== newChildren) if (isArrayImpl(newChildren)) {
				for (suspenseContext = 0; suspenseContext < newChildren.length; suspenseContext++) if (!validateSuspenseListNestedChild(newChildren[suspenseContext], suspenseContext)) break a;
			} else if (suspenseContext = getIteratorFn(newChildren), "function" === typeof suspenseContext) {
				if (suspenseContext = suspenseContext.call(newChildren)) for (var step = suspenseContext.next(), _i = 0; !step.done; step = suspenseContext.next()) {
					if (!validateSuspenseListNestedChild(step.value, _i)) break a;
					_i++;
				}
			} else console.error("A single row was passed to a <SuspenseList revealOrder=\"%s\" />. This is not useful since it needs multiple rows. Did you mean to pass multiple children or an array?", revealOrder);
			reconcileChildren(current, workInProgress, newChildren, renderLanes);
			isHydrating ? (warnIfNotHydrating(), newChildren = treeForkCount) : newChildren = 0;
			if (!nextProps && null !== current && 0 !== (current.flags & 128)) a: for (current = workInProgress.child; null !== current;) {
				if (13 === current.tag) null !== current.memoizedState && scheduleSuspenseWorkOnFiber(current, renderLanes, workInProgress);
				else if (19 === current.tag) scheduleSuspenseWorkOnFiber(current, renderLanes, workInProgress);
				else if (null !== current.child) {
					current.child.return = current;
					current = current.child;
					continue;
				}
				if (current === workInProgress) break a;
				for (; null === current.sibling;) {
					if (null === current.return || current.return === workInProgress) break a;
					current = current.return;
				}
				current.sibling.return = current.return;
				current = current.sibling;
			}
			switch (revealOrder) {
				case "forwards":
					renderLanes = workInProgress.child;
					for (revealOrder = null; null !== renderLanes;) current = renderLanes.alternate, null !== current && null === findFirstSuspended(current) && (revealOrder = renderLanes), renderLanes = renderLanes.sibling;
					renderLanes = revealOrder;
					null === renderLanes ? (revealOrder = workInProgress.child, workInProgress.child = null) : (revealOrder = renderLanes.sibling, renderLanes.sibling = null);
					initSuspenseListRenderState(workInProgress, !1, revealOrder, renderLanes, tailMode, newChildren);
					break;
				case "backwards":
				case "unstable_legacy-backwards":
					renderLanes = null;
					revealOrder = workInProgress.child;
					for (workInProgress.child = null; null !== revealOrder;) {
						current = revealOrder.alternate;
						if (null !== current && null === findFirstSuspended(current)) {
							workInProgress.child = revealOrder;
							break;
						}
						current = revealOrder.sibling;
						revealOrder.sibling = renderLanes;
						renderLanes = revealOrder;
						revealOrder = current;
					}
					initSuspenseListRenderState(workInProgress, !0, renderLanes, null, tailMode, newChildren);
					break;
				case "together":
					initSuspenseListRenderState(workInProgress, !1, null, null, void 0, newChildren);
					break;
				default: workInProgress.memoizedState = null;
			}
			return workInProgress.child;
		}
		function bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes) {
			null !== current && (workInProgress.dependencies = current.dependencies);
			profilerStartTime = -1;
			workInProgressRootSkippedLanes |= workInProgress.lanes;
			if (0 === (renderLanes & workInProgress.childLanes)) if (null !== current) {
				if (propagateParentContextChanges(current, workInProgress, renderLanes, !1), 0 === (renderLanes & workInProgress.childLanes)) return null;
			} else return null;
			if (null !== current && workInProgress.child !== current.child) throw Error("Resuming work not yet implemented.");
			if (null !== workInProgress.child) {
				current = workInProgress.child;
				renderLanes = createWorkInProgress(current, current.pendingProps);
				workInProgress.child = renderLanes;
				for (renderLanes.return = workInProgress; null !== current.sibling;) current = current.sibling, renderLanes = renderLanes.sibling = createWorkInProgress(current, current.pendingProps), renderLanes.return = workInProgress;
				renderLanes.sibling = null;
			}
			return workInProgress.child;
		}
		function checkScheduledUpdateOrContext(current, renderLanes) {
			if (0 !== (current.lanes & renderLanes)) return !0;
			current = current.dependencies;
			return null !== current && checkIfContextChanged(current) ? !0 : !1;
		}
		function attemptEarlyBailoutIfNoScheduledUpdate(current, workInProgress, renderLanes) {
			switch (workInProgress.tag) {
				case 3:
					pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
					pushProvider(workInProgress, CacheContext, current.memoizedState.cache);
					resetHydrationState();
					break;
				case 27:
				case 5:
					pushHostContext(workInProgress);
					break;
				case 4:
					pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
					break;
				case 10:
					pushProvider(workInProgress, workInProgress.type, workInProgress.memoizedProps.value);
					break;
				case 12:
					0 !== (renderLanes & workInProgress.childLanes) && (workInProgress.flags |= 4);
					workInProgress.flags |= 2048;
					var stateNode = workInProgress.stateNode;
					stateNode.effectDuration = -0;
					stateNode.passiveEffectDuration = -0;
					break;
				case 31:
					if (null !== workInProgress.memoizedState) return workInProgress.flags |= 128, pushDehydratedActivitySuspenseHandler(workInProgress), null;
					break;
				case 13:
					stateNode = workInProgress.memoizedState;
					if (null !== stateNode) {
						if (null !== stateNode.dehydrated) return pushPrimaryTreeSuspenseHandler(workInProgress), workInProgress.flags |= 128, null;
						if (0 !== (renderLanes & workInProgress.child.childLanes)) return updateSuspenseComponent(current, workInProgress, renderLanes);
						pushPrimaryTreeSuspenseHandler(workInProgress);
						current = bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
						return null !== current ? current.sibling : null;
					}
					pushPrimaryTreeSuspenseHandler(workInProgress);
					break;
				case 19:
					var didSuspendBefore = 0 !== (current.flags & 128);
					stateNode = 0 !== (renderLanes & workInProgress.childLanes);
					stateNode || (propagateParentContextChanges(current, workInProgress, renderLanes, !1), stateNode = 0 !== (renderLanes & workInProgress.childLanes));
					if (didSuspendBefore) {
						if (stateNode) return updateSuspenseListComponent(current, workInProgress, renderLanes);
						workInProgress.flags |= 128;
					}
					didSuspendBefore = workInProgress.memoizedState;
					null !== didSuspendBefore && (didSuspendBefore.rendering = null, didSuspendBefore.tail = null, didSuspendBefore.lastEffect = null);
					push(suspenseStackCursor, suspenseStackCursor.current, workInProgress);
					if (stateNode) break;
					else return null;
				case 22: return workInProgress.lanes = 0, updateOffscreenComponent(current, workInProgress, renderLanes, workInProgress.pendingProps);
				case 24: pushProvider(workInProgress, CacheContext, current.memoizedState.cache);
			}
			return bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
		}
		function beginWork(current, workInProgress, renderLanes) {
			if (workInProgress._debugNeedsRemount && null !== current) {
				renderLanes = createFiberFromTypeAndProps(workInProgress.type, workInProgress.key, workInProgress.pendingProps, workInProgress._debugOwner || null, workInProgress.mode, workInProgress.lanes);
				renderLanes._debugStack = workInProgress._debugStack;
				renderLanes._debugTask = workInProgress._debugTask;
				var returnFiber = workInProgress.return;
				if (null === returnFiber) throw Error("Cannot swap the root fiber.");
				current.alternate = null;
				workInProgress.alternate = null;
				renderLanes.index = workInProgress.index;
				renderLanes.sibling = workInProgress.sibling;
				renderLanes.return = workInProgress.return;
				renderLanes.ref = workInProgress.ref;
				renderLanes._debugInfo = workInProgress._debugInfo;
				if (workInProgress === returnFiber.child) returnFiber.child = renderLanes;
				else {
					var prevSibling = returnFiber.child;
					if (null === prevSibling) throw Error("Expected parent to have a child.");
					for (; prevSibling.sibling !== workInProgress;) if (prevSibling = prevSibling.sibling, null === prevSibling) throw Error("Expected to find the previous sibling.");
					prevSibling.sibling = renderLanes;
				}
				workInProgress = returnFiber.deletions;
				null === workInProgress ? (returnFiber.deletions = [current], returnFiber.flags |= 16) : workInProgress.push(current);
				renderLanes.flags |= 2;
				return renderLanes;
			}
			if (null !== current) if (current.memoizedProps !== workInProgress.pendingProps || workInProgress.type !== current.type) didReceiveUpdate = !0;
			else {
				if (!checkScheduledUpdateOrContext(current, renderLanes) && 0 === (workInProgress.flags & 128)) return didReceiveUpdate = !1, attemptEarlyBailoutIfNoScheduledUpdate(current, workInProgress, renderLanes);
				didReceiveUpdate = 0 !== (current.flags & 131072) ? !0 : !1;
			}
			else {
				didReceiveUpdate = !1;
				if (returnFiber = isHydrating) warnIfNotHydrating(), returnFiber = 0 !== (workInProgress.flags & 1048576);
				returnFiber && (returnFiber = workInProgress.index, warnIfNotHydrating(), pushTreeId(workInProgress, treeForkCount, returnFiber));
			}
			workInProgress.lanes = 0;
			switch (workInProgress.tag) {
				case 16:
					a: if (returnFiber = workInProgress.pendingProps, current = resolveLazy(workInProgress.elementType), workInProgress.type = current, "function" === typeof current) shouldConstruct(current) ? (returnFiber = resolveClassComponentProps(current, returnFiber), workInProgress.tag = 1, workInProgress.type = current = resolveFunctionForHotReloading(current), workInProgress = updateClassComponent(null, workInProgress, current, returnFiber, renderLanes)) : (workInProgress.tag = 0, validateFunctionComponentInDev(workInProgress, current), workInProgress.type = current = resolveFunctionForHotReloading(current), workInProgress = updateFunctionComponent(null, workInProgress, current, returnFiber, renderLanes));
					else {
						if (void 0 !== current && null !== current) {
							if (prevSibling = current.$$typeof, prevSibling === REACT_FORWARD_REF_TYPE) {
								workInProgress.tag = 11;
								workInProgress.type = current = resolveForwardRefForHotReloading(current);
								workInProgress = updateForwardRef(null, workInProgress, current, returnFiber, renderLanes);
								break a;
							} else if (prevSibling === REACT_MEMO_TYPE) {
								workInProgress.tag = 14;
								workInProgress = updateMemoComponent(null, workInProgress, current, returnFiber, renderLanes);
								break a;
							}
						}
						workInProgress = "";
						null !== current && "object" === typeof current && current.$$typeof === REACT_LAZY_TYPE && (workInProgress = " Did you wrap a component in React.lazy() more than once?");
						renderLanes = getComponentNameFromType(current) || current;
						throw Error("Element type is invalid. Received a promise that resolves to: " + renderLanes + ". Lazy element type must resolve to a class or function." + workInProgress);
					}
					return workInProgress;
				case 0: return updateFunctionComponent(current, workInProgress, workInProgress.type, workInProgress.pendingProps, renderLanes);
				case 1: return returnFiber = workInProgress.type, prevSibling = resolveClassComponentProps(returnFiber, workInProgress.pendingProps), updateClassComponent(current, workInProgress, returnFiber, prevSibling, renderLanes);
				case 3:
					a: {
						pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
						if (null === current) throw Error("Should have a current fiber. This is a bug in React.");
						returnFiber = workInProgress.pendingProps;
						var prevState = workInProgress.memoizedState;
						prevSibling = prevState.element;
						cloneUpdateQueue(current, workInProgress);
						processUpdateQueue(workInProgress, returnFiber, null, renderLanes);
						var nextState = workInProgress.memoizedState;
						returnFiber = nextState.cache;
						pushProvider(workInProgress, CacheContext, returnFiber);
						returnFiber !== prevState.cache && propagateContextChanges(workInProgress, [CacheContext], renderLanes, !0);
						suspendIfUpdateReadFromEntangledAsyncAction();
						returnFiber = nextState.element;
						if (prevState.isDehydrated) if (prevState = {
							element: returnFiber,
							isDehydrated: !1,
							cache: nextState.cache
						}, workInProgress.updateQueue.baseState = prevState, workInProgress.memoizedState = prevState, workInProgress.flags & 256) {
							workInProgress = mountHostRootWithoutHydrating(current, workInProgress, returnFiber, renderLanes);
							break a;
						} else if (returnFiber !== prevSibling) {
							prevSibling = createCapturedValueAtFiber(Error("This root received an early update, before anything was able hydrate. Switched the entire root to client rendering."), workInProgress);
							queueHydrationError(prevSibling);
							workInProgress = mountHostRootWithoutHydrating(current, workInProgress, returnFiber, renderLanes);
							break a;
						} else {
							current = workInProgress.stateNode.containerInfo;
							switch (current.nodeType) {
								case 9:
									current = current.body;
									break;
								default: current = "HTML" === current.nodeName ? current.ownerDocument.body : current;
							}
							nextHydratableInstance = getNextHydratable(current.firstChild);
							hydrationParentFiber = workInProgress;
							isHydrating = !0;
							hydrationErrors = null;
							didSuspendOrErrorDEV = !1;
							hydrationDiffRootDEV = null;
							rootOrSingletonContext = !0;
							renderLanes = mountChildFibers(workInProgress, null, returnFiber, renderLanes);
							for (workInProgress.child = renderLanes; renderLanes;) renderLanes.flags = renderLanes.flags & -3 | 4096, renderLanes = renderLanes.sibling;
						}
						else {
							resetHydrationState();
							if (returnFiber === prevSibling) {
								workInProgress = bailoutOnAlreadyFinishedWork(current, workInProgress, renderLanes);
								break a;
							}
							reconcileChildren(current, workInProgress, returnFiber, renderLanes);
						}
						workInProgress = workInProgress.child;
					}
					return workInProgress;
				case 26: return markRef(current, workInProgress), null === current ? (renderLanes = getResource(workInProgress.type, null, workInProgress.pendingProps, null)) ? workInProgress.memoizedState = renderLanes : isHydrating || (renderLanes = workInProgress.type, current = workInProgress.pendingProps, returnFiber = requiredContext(rootInstanceStackCursor.current), returnFiber = getOwnerDocumentFromRootContainer(returnFiber).createElement(renderLanes), returnFiber[internalInstanceKey] = workInProgress, returnFiber[internalPropsKey] = current, setInitialProperties(returnFiber, renderLanes, current), markNodeAsHoistable(returnFiber), workInProgress.stateNode = returnFiber) : workInProgress.memoizedState = getResource(workInProgress.type, current.memoizedProps, workInProgress.pendingProps, current.memoizedState), null;
				case 27: return pushHostContext(workInProgress), null === current && isHydrating && (returnFiber = requiredContext(rootInstanceStackCursor.current), prevSibling = getHostContext(), returnFiber = workInProgress.stateNode = resolveSingletonInstance(workInProgress.type, workInProgress.pendingProps, returnFiber, prevSibling, !1), didSuspendOrErrorDEV || (prevSibling = diffHydratedProperties(returnFiber, workInProgress.type, workInProgress.pendingProps, prevSibling), null !== prevSibling && (buildHydrationDiffNode(workInProgress, 0).serverProps = prevSibling)), hydrationParentFiber = workInProgress, rootOrSingletonContext = !0, prevSibling = nextHydratableInstance, isSingletonScope(workInProgress.type) ? (previousHydratableOnEnteringScopedSingleton = prevSibling, nextHydratableInstance = getNextHydratable(returnFiber.firstChild)) : nextHydratableInstance = prevSibling), reconcileChildren(current, workInProgress, workInProgress.pendingProps.children, renderLanes), markRef(current, workInProgress), null === current && (workInProgress.flags |= 4194304), workInProgress.child;
				case 5: return null === current && isHydrating && (prevState = getHostContext(), returnFiber = validateDOMNesting(workInProgress.type, prevState.ancestorInfo), prevSibling = nextHydratableInstance, (nextState = !prevSibling) || (nextState = canHydrateInstance(prevSibling, workInProgress.type, workInProgress.pendingProps, rootOrSingletonContext), null !== nextState ? (workInProgress.stateNode = nextState, didSuspendOrErrorDEV || (prevState = diffHydratedProperties(nextState, workInProgress.type, workInProgress.pendingProps, prevState), null !== prevState && (buildHydrationDiffNode(workInProgress, 0).serverProps = prevState)), hydrationParentFiber = workInProgress, nextHydratableInstance = getNextHydratable(nextState.firstChild), rootOrSingletonContext = !1, prevState = !0) : prevState = !1, nextState = !prevState), nextState && (returnFiber && warnNonHydratedInstance(workInProgress, prevSibling), throwOnHydrationMismatch(workInProgress))), pushHostContext(workInProgress), prevSibling = workInProgress.type, prevState = workInProgress.pendingProps, nextState = null !== current ? current.memoizedProps : null, returnFiber = prevState.children, shouldSetTextContent(prevSibling, prevState) ? returnFiber = null : null !== nextState && shouldSetTextContent(prevSibling, nextState) && (workInProgress.flags |= 32), null !== workInProgress.memoizedState && (prevSibling = renderWithHooks(current, workInProgress, TransitionAwareHostComponent, null, null, renderLanes), HostTransitionContext._currentValue = prevSibling), markRef(current, workInProgress), reconcileChildren(current, workInProgress, returnFiber, renderLanes), workInProgress.child;
				case 6: return null === current && isHydrating && (renderLanes = workInProgress.pendingProps, current = getHostContext(), returnFiber = current.ancestorInfo.current, renderLanes = null != returnFiber ? validateTextNesting(renderLanes, returnFiber.tag, current.ancestorInfo.implicitRootScope) : !0, current = nextHydratableInstance, (returnFiber = !current) || (returnFiber = canHydrateTextInstance(current, workInProgress.pendingProps, rootOrSingletonContext), null !== returnFiber ? (workInProgress.stateNode = returnFiber, hydrationParentFiber = workInProgress, nextHydratableInstance = null, returnFiber = !0) : returnFiber = !1, returnFiber = !returnFiber), returnFiber && (renderLanes && warnNonHydratedInstance(workInProgress, current), throwOnHydrationMismatch(workInProgress))), null;
				case 13: return updateSuspenseComponent(current, workInProgress, renderLanes);
				case 4: return pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo), returnFiber = workInProgress.pendingProps, null === current ? workInProgress.child = reconcileChildFibers(workInProgress, null, returnFiber, renderLanes) : reconcileChildren(current, workInProgress, returnFiber, renderLanes), workInProgress.child;
				case 11: return updateForwardRef(current, workInProgress, workInProgress.type, workInProgress.pendingProps, renderLanes);
				case 7: return reconcileChildren(current, workInProgress, workInProgress.pendingProps, renderLanes), workInProgress.child;
				case 8: return reconcileChildren(current, workInProgress, workInProgress.pendingProps.children, renderLanes), workInProgress.child;
				case 12: return workInProgress.flags |= 4, workInProgress.flags |= 2048, returnFiber = workInProgress.stateNode, returnFiber.effectDuration = -0, returnFiber.passiveEffectDuration = -0, reconcileChildren(current, workInProgress, workInProgress.pendingProps.children, renderLanes), workInProgress.child;
				case 10: return returnFiber = workInProgress.type, prevSibling = workInProgress.pendingProps, prevState = prevSibling.value, "value" in prevSibling || hasWarnedAboutUsingNoValuePropOnContextProvider || (hasWarnedAboutUsingNoValuePropOnContextProvider = !0, console.error("The `value` prop is required for the `<Context.Provider>`. Did you misspell it or forget to pass it?")), pushProvider(workInProgress, returnFiber, prevState), reconcileChildren(current, workInProgress, prevSibling.children, renderLanes), workInProgress.child;
				case 9: return prevSibling = workInProgress.type._context, returnFiber = workInProgress.pendingProps.children, "function" !== typeof returnFiber && console.error("A context consumer was rendered with multiple children, or a child that isn't a function. A context consumer expects a single child that is a function. If you did pass a function, make sure there is no trailing or leading whitespace around it."), prepareToReadContext(workInProgress), prevSibling = readContext(prevSibling), returnFiber = callComponentInDEV(returnFiber, prevSibling, void 0), workInProgress.flags |= 1, reconcileChildren(current, workInProgress, returnFiber, renderLanes), workInProgress.child;
				case 14: return updateMemoComponent(current, workInProgress, workInProgress.type, workInProgress.pendingProps, renderLanes);
				case 15: return updateSimpleMemoComponent(current, workInProgress, workInProgress.type, workInProgress.pendingProps, renderLanes);
				case 19: return updateSuspenseListComponent(current, workInProgress, renderLanes);
				case 31: return updateActivityComponent(current, workInProgress, renderLanes);
				case 22: return updateOffscreenComponent(current, workInProgress, renderLanes, workInProgress.pendingProps);
				case 24: return prepareToReadContext(workInProgress), returnFiber = readContext(CacheContext), null === current ? (prevSibling = peekCacheFromPool(), null === prevSibling && (prevSibling = workInProgressRoot, prevState = createCache(), prevSibling.pooledCache = prevState, retainCache(prevState), null !== prevState && (prevSibling.pooledCacheLanes |= renderLanes), prevSibling = prevState), workInProgress.memoizedState = {
					parent: returnFiber,
					cache: prevSibling
				}, initializeUpdateQueue(workInProgress), pushProvider(workInProgress, CacheContext, prevSibling)) : (0 !== (current.lanes & renderLanes) && (cloneUpdateQueue(current, workInProgress), processUpdateQueue(workInProgress, null, null, renderLanes), suspendIfUpdateReadFromEntangledAsyncAction()), prevSibling = current.memoizedState, prevState = workInProgress.memoizedState, prevSibling.parent !== returnFiber ? (prevSibling = {
					parent: returnFiber,
					cache: returnFiber
				}, workInProgress.memoizedState = prevSibling, 0 === workInProgress.lanes && (workInProgress.memoizedState = workInProgress.updateQueue.baseState = prevSibling), pushProvider(workInProgress, CacheContext, returnFiber)) : (returnFiber = prevState.cache, pushProvider(workInProgress, CacheContext, returnFiber), returnFiber !== prevSibling.cache && propagateContextChanges(workInProgress, [CacheContext], renderLanes, !0))), reconcileChildren(current, workInProgress, workInProgress.pendingProps.children, renderLanes), workInProgress.child;
				case 29: throw workInProgress.pendingProps;
			}
			throw Error("Unknown unit of work tag (" + workInProgress.tag + "). This error is likely caused by a bug in React. Please file an issue.");
		}
		function markUpdate(workInProgress) {
			workInProgress.flags |= 4;
		}
		function preloadInstanceAndSuspendIfNeeded(workInProgress, type, oldProps, newProps, renderLanes) {
			if (type = (workInProgress.mode & SuspenseyImagesMode) !== NoMode) type = !1;
			if (type) {
				if (workInProgress.flags |= 16777216, (renderLanes & 335544128) === renderLanes) if (workInProgress.stateNode.complete) workInProgress.flags |= 8192;
				else if (shouldRemainOnPreviousScreen()) workInProgress.flags |= 8192;
				else throw suspendedThenable = noopSuspenseyCommitThenable, SuspenseyCommitException;
			} else workInProgress.flags &= -16777217;
		}
		function preloadResourceAndSuspendIfNeeded(workInProgress, resource) {
			if ("stylesheet" !== resource.type || (resource.state.loading & Inserted) !== NotLoaded) workInProgress.flags &= -16777217;
			else if (workInProgress.flags |= 16777216, !preloadResource(resource)) if (shouldRemainOnPreviousScreen()) workInProgress.flags |= 8192;
			else throw suspendedThenable = noopSuspenseyCommitThenable, SuspenseyCommitException;
		}
		function scheduleRetryEffect(workInProgress, retryQueue) {
			null !== retryQueue && (workInProgress.flags |= 4);
			workInProgress.flags & 16384 && (retryQueue = 22 !== workInProgress.tag ? claimNextRetryLane() : 536870912, workInProgress.lanes |= retryQueue, workInProgressSuspendedRetryLanes |= retryQueue);
		}
		function cutOffTailIfNeeded(renderState, hasRenderedATailFallback) {
			if (!isHydrating) switch (renderState.tailMode) {
				case "hidden":
					hasRenderedATailFallback = renderState.tail;
					for (var lastTailNode = null; null !== hasRenderedATailFallback;) null !== hasRenderedATailFallback.alternate && (lastTailNode = hasRenderedATailFallback), hasRenderedATailFallback = hasRenderedATailFallback.sibling;
					null === lastTailNode ? renderState.tail = null : lastTailNode.sibling = null;
					break;
				case "collapsed":
					lastTailNode = renderState.tail;
					for (var _lastTailNode = null; null !== lastTailNode;) null !== lastTailNode.alternate && (_lastTailNode = lastTailNode), lastTailNode = lastTailNode.sibling;
					null === _lastTailNode ? hasRenderedATailFallback || null === renderState.tail ? renderState.tail = null : renderState.tail.sibling = null : _lastTailNode.sibling = null;
			}
		}
		function bubbleProperties(completedWork) {
			var didBailout = null !== completedWork.alternate && completedWork.alternate.child === completedWork.child, newChildLanes = 0, subtreeFlags = 0;
			if (didBailout) if ((completedWork.mode & ProfileMode) !== NoMode) {
				for (var _treeBaseDuration = completedWork.selfBaseDuration, _child2 = completedWork.child; null !== _child2;) newChildLanes |= _child2.lanes | _child2.childLanes, subtreeFlags |= _child2.subtreeFlags & 65011712, subtreeFlags |= _child2.flags & 65011712, _treeBaseDuration += _child2.treeBaseDuration, _child2 = _child2.sibling;
				completedWork.treeBaseDuration = _treeBaseDuration;
			} else for (_treeBaseDuration = completedWork.child; null !== _treeBaseDuration;) newChildLanes |= _treeBaseDuration.lanes | _treeBaseDuration.childLanes, subtreeFlags |= _treeBaseDuration.subtreeFlags & 65011712, subtreeFlags |= _treeBaseDuration.flags & 65011712, _treeBaseDuration.return = completedWork, _treeBaseDuration = _treeBaseDuration.sibling;
			else if ((completedWork.mode & ProfileMode) !== NoMode) {
				_treeBaseDuration = completedWork.actualDuration;
				_child2 = completedWork.selfBaseDuration;
				for (var child = completedWork.child; null !== child;) newChildLanes |= child.lanes | child.childLanes, subtreeFlags |= child.subtreeFlags, subtreeFlags |= child.flags, _treeBaseDuration += child.actualDuration, _child2 += child.treeBaseDuration, child = child.sibling;
				completedWork.actualDuration = _treeBaseDuration;
				completedWork.treeBaseDuration = _child2;
			} else for (_treeBaseDuration = completedWork.child; null !== _treeBaseDuration;) newChildLanes |= _treeBaseDuration.lanes | _treeBaseDuration.childLanes, subtreeFlags |= _treeBaseDuration.subtreeFlags, subtreeFlags |= _treeBaseDuration.flags, _treeBaseDuration.return = completedWork, _treeBaseDuration = _treeBaseDuration.sibling;
			completedWork.subtreeFlags |= subtreeFlags;
			completedWork.childLanes = newChildLanes;
			return didBailout;
		}
		function completeWork(current, workInProgress, renderLanes) {
			var newProps = workInProgress.pendingProps;
			popTreeContext(workInProgress);
			switch (workInProgress.tag) {
				case 16:
				case 15:
				case 0:
				case 11:
				case 7:
				case 8:
				case 12:
				case 9:
				case 14: return bubbleProperties(workInProgress), null;
				case 1: return bubbleProperties(workInProgress), null;
				case 3:
					renderLanes = workInProgress.stateNode;
					newProps = null;
					null !== current && (newProps = current.memoizedState.cache);
					workInProgress.memoizedState.cache !== newProps && (workInProgress.flags |= 2048);
					popProvider(CacheContext, workInProgress);
					popHostContainer(workInProgress);
					renderLanes.pendingContext && (renderLanes.context = renderLanes.pendingContext, renderLanes.pendingContext = null);
					if (null === current || null === current.child) popHydrationState(workInProgress) ? (emitPendingHydrationWarnings(), markUpdate(workInProgress)) : null === current || current.memoizedState.isDehydrated && 0 === (workInProgress.flags & 256) || (workInProgress.flags |= 1024, upgradeHydrationErrorsToRecoverable());
					bubbleProperties(workInProgress);
					return null;
				case 26:
					var type = workInProgress.type, nextResource = workInProgress.memoizedState;
					null === current ? (markUpdate(workInProgress), null !== nextResource ? (bubbleProperties(workInProgress), preloadResourceAndSuspendIfNeeded(workInProgress, nextResource)) : (bubbleProperties(workInProgress), preloadInstanceAndSuspendIfNeeded(workInProgress, type, null, newProps, renderLanes))) : nextResource ? nextResource !== current.memoizedState ? (markUpdate(workInProgress), bubbleProperties(workInProgress), preloadResourceAndSuspendIfNeeded(workInProgress, nextResource)) : (bubbleProperties(workInProgress), workInProgress.flags &= -16777217) : (current = current.memoizedProps, current !== newProps && markUpdate(workInProgress), bubbleProperties(workInProgress), preloadInstanceAndSuspendIfNeeded(workInProgress, type, current, newProps, renderLanes));
					return null;
				case 27:
					popHostContext(workInProgress);
					renderLanes = requiredContext(rootInstanceStackCursor.current);
					type = workInProgress.type;
					if (null !== current && null != workInProgress.stateNode) current.memoizedProps !== newProps && markUpdate(workInProgress);
					else {
						if (!newProps) {
							if (null === workInProgress.stateNode) throw Error("We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.");
							bubbleProperties(workInProgress);
							return null;
						}
						current = getHostContext();
						popHydrationState(workInProgress) ? prepareToHydrateHostInstance(workInProgress, current) : (current = resolveSingletonInstance(type, newProps, renderLanes, current, !0), workInProgress.stateNode = current, markUpdate(workInProgress));
					}
					bubbleProperties(workInProgress);
					return null;
				case 5:
					popHostContext(workInProgress);
					type = workInProgress.type;
					if (null !== current && null != workInProgress.stateNode) current.memoizedProps !== newProps && markUpdate(workInProgress);
					else {
						if (!newProps) {
							if (null === workInProgress.stateNode) throw Error("We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.");
							bubbleProperties(workInProgress);
							return null;
						}
						var _currentHostContext = getHostContext();
						if (popHydrationState(workInProgress)) prepareToHydrateHostInstance(workInProgress, _currentHostContext);
						else {
							nextResource = requiredContext(rootInstanceStackCursor.current);
							validateDOMNesting(type, _currentHostContext.ancestorInfo);
							_currentHostContext = _currentHostContext.context;
							nextResource = getOwnerDocumentFromRootContainer(nextResource);
							switch (_currentHostContext) {
								case HostContextNamespaceSvg:
									nextResource = nextResource.createElementNS(SVG_NAMESPACE, type);
									break;
								case HostContextNamespaceMath:
									nextResource = nextResource.createElementNS(MATH_NAMESPACE, type);
									break;
								default: switch (type) {
									case "svg":
										nextResource = nextResource.createElementNS(SVG_NAMESPACE, type);
										break;
									case "math":
										nextResource = nextResource.createElementNS(MATH_NAMESPACE, type);
										break;
									case "script":
										nextResource = nextResource.createElement("div");
										nextResource.innerHTML = "<script><\/script>";
										nextResource = nextResource.removeChild(nextResource.firstChild);
										break;
									case "select":
										nextResource = "string" === typeof newProps.is ? nextResource.createElement("select", { is: newProps.is }) : nextResource.createElement("select");
										newProps.multiple ? nextResource.multiple = !0 : newProps.size && (nextResource.size = newProps.size);
										break;
									default: nextResource = "string" === typeof newProps.is ? nextResource.createElement(type, { is: newProps.is }) : nextResource.createElement(type), -1 === type.indexOf("-") && (type !== type.toLowerCase() && console.error("<%s /> is using incorrect casing. Use PascalCase for React components, or lowercase for HTML elements.", type), "[object HTMLUnknownElement]" !== Object.prototype.toString.call(nextResource) || hasOwnProperty.call(warnedUnknownTags, type) || (warnedUnknownTags[type] = !0, console.error("The tag <%s> is unrecognized in this browser. If you meant to render a React component, start its name with an uppercase letter.", type)));
								}
							}
							nextResource[internalInstanceKey] = workInProgress;
							nextResource[internalPropsKey] = newProps;
							a: for (_currentHostContext = workInProgress.child; null !== _currentHostContext;) {
								if (5 === _currentHostContext.tag || 6 === _currentHostContext.tag) nextResource.appendChild(_currentHostContext.stateNode);
								else if (4 !== _currentHostContext.tag && 27 !== _currentHostContext.tag && null !== _currentHostContext.child) {
									_currentHostContext.child.return = _currentHostContext;
									_currentHostContext = _currentHostContext.child;
									continue;
								}
								if (_currentHostContext === workInProgress) break a;
								for (; null === _currentHostContext.sibling;) {
									if (null === _currentHostContext.return || _currentHostContext.return === workInProgress) break a;
									_currentHostContext = _currentHostContext.return;
								}
								_currentHostContext.sibling.return = _currentHostContext.return;
								_currentHostContext = _currentHostContext.sibling;
							}
							workInProgress.stateNode = nextResource;
							a: switch (setInitialProperties(nextResource, type, newProps), type) {
								case "button":
								case "input":
								case "select":
								case "textarea":
									newProps = !!newProps.autoFocus;
									break a;
								case "img":
									newProps = !0;
									break a;
								default: newProps = !1;
							}
							newProps && markUpdate(workInProgress);
						}
					}
					bubbleProperties(workInProgress);
					preloadInstanceAndSuspendIfNeeded(workInProgress, workInProgress.type, null === current ? null : current.memoizedProps, workInProgress.pendingProps, renderLanes);
					return null;
				case 6:
					if (current && null != workInProgress.stateNode) current.memoizedProps !== newProps && markUpdate(workInProgress);
					else {
						if ("string" !== typeof newProps && null === workInProgress.stateNode) throw Error("We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.");
						current = requiredContext(rootInstanceStackCursor.current);
						renderLanes = getHostContext();
						if (popHydrationState(workInProgress)) {
							current = workInProgress.stateNode;
							renderLanes = workInProgress.memoizedProps;
							type = !didSuspendOrErrorDEV;
							newProps = null;
							nextResource = hydrationParentFiber;
							if (null !== nextResource) switch (nextResource.tag) {
								case 3:
									type && (type = diffHydratedTextForDevWarnings(current, renderLanes, newProps), null !== type && (buildHydrationDiffNode(workInProgress, 0).serverProps = type));
									break;
								case 27:
								case 5: newProps = nextResource.memoizedProps, type && (type = diffHydratedTextForDevWarnings(current, renderLanes, newProps), null !== type && (buildHydrationDiffNode(workInProgress, 0).serverProps = type));
							}
							current[internalInstanceKey] = workInProgress;
							current = current.nodeValue === renderLanes || null !== newProps && !0 === newProps.suppressHydrationWarning || checkForUnmatchedText(current.nodeValue, renderLanes) ? !0 : !1;
							current || throwOnHydrationMismatch(workInProgress, !0);
						} else type = renderLanes.ancestorInfo.current, null != type && validateTextNesting(newProps, type.tag, renderLanes.ancestorInfo.implicitRootScope), current = getOwnerDocumentFromRootContainer(current).createTextNode(newProps), current[internalInstanceKey] = workInProgress, workInProgress.stateNode = current;
					}
					bubbleProperties(workInProgress);
					return null;
				case 31:
					renderLanes = workInProgress.memoizedState;
					if (null === current || null !== current.memoizedState) {
						newProps = popHydrationState(workInProgress);
						if (null !== renderLanes) {
							if (null === current) {
								if (!newProps) throw Error("A dehydrated suspense component was completed without a hydrated node. This is probably a bug in React.");
								current = workInProgress.memoizedState;
								current = null !== current ? current.dehydrated : null;
								if (!current) throw Error("Expected to have a hydrated activity instance. This error is likely caused by a bug in React. Please file an issue.");
								current[internalInstanceKey] = workInProgress;
								bubbleProperties(workInProgress);
								(workInProgress.mode & ProfileMode) !== NoMode && null !== renderLanes && (current = workInProgress.child, null !== current && (workInProgress.treeBaseDuration -= current.treeBaseDuration));
							} else emitPendingHydrationWarnings(), resetHydrationState(), 0 === (workInProgress.flags & 128) && (renderLanes = workInProgress.memoizedState = null), workInProgress.flags |= 4, bubbleProperties(workInProgress), (workInProgress.mode & ProfileMode) !== NoMode && null !== renderLanes && (current = workInProgress.child, null !== current && (workInProgress.treeBaseDuration -= current.treeBaseDuration));
							current = !1;
						} else renderLanes = upgradeHydrationErrorsToRecoverable(), null !== current && null !== current.memoizedState && (current.memoizedState.hydrationErrors = renderLanes), current = !0;
						if (!current) {
							if (workInProgress.flags & 256) return popSuspenseHandler(workInProgress), workInProgress;
							popSuspenseHandler(workInProgress);
							return null;
						}
						if (0 !== (workInProgress.flags & 128)) throw Error("Client rendering an Activity suspended it again. This is a bug in React.");
					}
					bubbleProperties(workInProgress);
					return null;
				case 13:
					newProps = workInProgress.memoizedState;
					if (null === current || null !== current.memoizedState && null !== current.memoizedState.dehydrated) {
						type = newProps;
						nextResource = popHydrationState(workInProgress);
						if (null !== type && null !== type.dehydrated) {
							if (null === current) {
								if (!nextResource) throw Error("A dehydrated suspense component was completed without a hydrated node. This is probably a bug in React.");
								nextResource = workInProgress.memoizedState;
								nextResource = null !== nextResource ? nextResource.dehydrated : null;
								if (!nextResource) throw Error("Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue.");
								nextResource[internalInstanceKey] = workInProgress;
								bubbleProperties(workInProgress);
								(workInProgress.mode & ProfileMode) !== NoMode && null !== type && (type = workInProgress.child, null !== type && (workInProgress.treeBaseDuration -= type.treeBaseDuration));
							} else emitPendingHydrationWarnings(), resetHydrationState(), 0 === (workInProgress.flags & 128) && (type = workInProgress.memoizedState = null), workInProgress.flags |= 4, bubbleProperties(workInProgress), (workInProgress.mode & ProfileMode) !== NoMode && null !== type && (type = workInProgress.child, null !== type && (workInProgress.treeBaseDuration -= type.treeBaseDuration));
							type = !1;
						} else type = upgradeHydrationErrorsToRecoverable(), null !== current && null !== current.memoizedState && (current.memoizedState.hydrationErrors = type), type = !0;
						if (!type) {
							if (workInProgress.flags & 256) return popSuspenseHandler(workInProgress), workInProgress;
							popSuspenseHandler(workInProgress);
							return null;
						}
					}
					popSuspenseHandler(workInProgress);
					if (0 !== (workInProgress.flags & 128)) return workInProgress.lanes = renderLanes, (workInProgress.mode & ProfileMode) !== NoMode && transferActualDuration(workInProgress), workInProgress;
					renderLanes = null !== newProps;
					current = null !== current && null !== current.memoizedState;
					renderLanes && (newProps = workInProgress.child, type = null, null !== newProps.alternate && null !== newProps.alternate.memoizedState && null !== newProps.alternate.memoizedState.cachePool && (type = newProps.alternate.memoizedState.cachePool.pool), nextResource = null, null !== newProps.memoizedState && null !== newProps.memoizedState.cachePool && (nextResource = newProps.memoizedState.cachePool.pool), nextResource !== type && (newProps.flags |= 2048));
					renderLanes !== current && renderLanes && (workInProgress.child.flags |= 8192);
					scheduleRetryEffect(workInProgress, workInProgress.updateQueue);
					bubbleProperties(workInProgress);
					(workInProgress.mode & ProfileMode) !== NoMode && renderLanes && (current = workInProgress.child, null !== current && (workInProgress.treeBaseDuration -= current.treeBaseDuration));
					return null;
				case 4: return popHostContainer(workInProgress), null === current && listenToAllSupportedEvents(workInProgress.stateNode.containerInfo), bubbleProperties(workInProgress), null;
				case 10: return popProvider(workInProgress.type, workInProgress), bubbleProperties(workInProgress), null;
				case 19:
					pop(suspenseStackCursor, workInProgress);
					newProps = workInProgress.memoizedState;
					if (null === newProps) return bubbleProperties(workInProgress), null;
					type = 0 !== (workInProgress.flags & 128);
					nextResource = newProps.rendering;
					if (null === nextResource) if (type) cutOffTailIfNeeded(newProps, !1);
					else {
						if (workInProgressRootExitStatus !== RootInProgress || null !== current && 0 !== (current.flags & 128)) for (current = workInProgress.child; null !== current;) {
							nextResource = findFirstSuspended(current);
							if (null !== nextResource) {
								workInProgress.flags |= 128;
								cutOffTailIfNeeded(newProps, !1);
								current = nextResource.updateQueue;
								workInProgress.updateQueue = current;
								scheduleRetryEffect(workInProgress, current);
								workInProgress.subtreeFlags = 0;
								current = renderLanes;
								for (renderLanes = workInProgress.child; null !== renderLanes;) resetWorkInProgress(renderLanes, current), renderLanes = renderLanes.sibling;
								push(suspenseStackCursor, suspenseStackCursor.current & SubtreeSuspenseContextMask | ForceSuspenseFallback, workInProgress);
								isHydrating && pushTreeFork(workInProgress, newProps.treeForkCount);
								return workInProgress.child;
							}
							current = current.sibling;
						}
						null !== newProps.tail && now$1() > workInProgressRootRenderTargetTime && (workInProgress.flags |= 128, type = !0, cutOffTailIfNeeded(newProps, !1), workInProgress.lanes = 4194304);
					}
					else {
						if (!type) if (current = findFirstSuspended(nextResource), null !== current) {
							if (workInProgress.flags |= 128, type = !0, current = current.updateQueue, workInProgress.updateQueue = current, scheduleRetryEffect(workInProgress, current), cutOffTailIfNeeded(newProps, !0), null === newProps.tail && "hidden" === newProps.tailMode && !nextResource.alternate && !isHydrating) return bubbleProperties(workInProgress), null;
						} else 2 * now$1() - newProps.renderingStartTime > workInProgressRootRenderTargetTime && 536870912 !== renderLanes && (workInProgress.flags |= 128, type = !0, cutOffTailIfNeeded(newProps, !1), workInProgress.lanes = 4194304);
						newProps.isBackwards ? (nextResource.sibling = workInProgress.child, workInProgress.child = nextResource) : (current = newProps.last, null !== current ? current.sibling = nextResource : workInProgress.child = nextResource, newProps.last = nextResource);
					}
					if (null !== newProps.tail) return current = newProps.tail, newProps.rendering = current, newProps.tail = current.sibling, newProps.renderingStartTime = now$1(), current.sibling = null, renderLanes = suspenseStackCursor.current, renderLanes = type ? renderLanes & SubtreeSuspenseContextMask | ForceSuspenseFallback : renderLanes & SubtreeSuspenseContextMask, push(suspenseStackCursor, renderLanes, workInProgress), isHydrating && pushTreeFork(workInProgress, newProps.treeForkCount), current;
					bubbleProperties(workInProgress);
					return null;
				case 22:
				case 23: return popSuspenseHandler(workInProgress), popHiddenContext(workInProgress), newProps = null !== workInProgress.memoizedState, null !== current ? null !== current.memoizedState !== newProps && (workInProgress.flags |= 8192) : newProps && (workInProgress.flags |= 8192), newProps ? 0 !== (renderLanes & 536870912) && 0 === (workInProgress.flags & 128) && (bubbleProperties(workInProgress), workInProgress.subtreeFlags & 6 && (workInProgress.flags |= 8192)) : bubbleProperties(workInProgress), renderLanes = workInProgress.updateQueue, null !== renderLanes && scheduleRetryEffect(workInProgress, renderLanes.retryQueue), renderLanes = null, null !== current && null !== current.memoizedState && null !== current.memoizedState.cachePool && (renderLanes = current.memoizedState.cachePool.pool), newProps = null, null !== workInProgress.memoizedState && null !== workInProgress.memoizedState.cachePool && (newProps = workInProgress.memoizedState.cachePool.pool), newProps !== renderLanes && (workInProgress.flags |= 2048), null !== current && pop(resumedCache, workInProgress), null;
				case 24: return renderLanes = null, null !== current && (renderLanes = current.memoizedState.cache), workInProgress.memoizedState.cache !== renderLanes && (workInProgress.flags |= 2048), popProvider(CacheContext, workInProgress), bubbleProperties(workInProgress), null;
				case 25: return null;
				case 30: return null;
			}
			throw Error("Unknown unit of work tag (" + workInProgress.tag + "). This error is likely caused by a bug in React. Please file an issue.");
		}
		function unwindWork(current, workInProgress) {
			popTreeContext(workInProgress);
			switch (workInProgress.tag) {
				case 1: return current = workInProgress.flags, current & 65536 ? (workInProgress.flags = current & -65537 | 128, (workInProgress.mode & ProfileMode) !== NoMode && transferActualDuration(workInProgress), workInProgress) : null;
				case 3: return popProvider(CacheContext, workInProgress), popHostContainer(workInProgress), current = workInProgress.flags, 0 !== (current & 65536) && 0 === (current & 128) ? (workInProgress.flags = current & -65537 | 128, workInProgress) : null;
				case 26:
				case 27:
				case 5: return popHostContext(workInProgress), null;
				case 31:
					if (null !== workInProgress.memoizedState) {
						popSuspenseHandler(workInProgress);
						if (null === workInProgress.alternate) throw Error("Threw in newly mounted dehydrated component. This is likely a bug in React. Please file an issue.");
						resetHydrationState();
					}
					current = workInProgress.flags;
					return current & 65536 ? (workInProgress.flags = current & -65537 | 128, (workInProgress.mode & ProfileMode) !== NoMode && transferActualDuration(workInProgress), workInProgress) : null;
				case 13:
					popSuspenseHandler(workInProgress);
					current = workInProgress.memoizedState;
					if (null !== current && null !== current.dehydrated) {
						if (null === workInProgress.alternate) throw Error("Threw in newly mounted dehydrated component. This is likely a bug in React. Please file an issue.");
						resetHydrationState();
					}
					current = workInProgress.flags;
					return current & 65536 ? (workInProgress.flags = current & -65537 | 128, (workInProgress.mode & ProfileMode) !== NoMode && transferActualDuration(workInProgress), workInProgress) : null;
				case 19: return pop(suspenseStackCursor, workInProgress), null;
				case 4: return popHostContainer(workInProgress), null;
				case 10: return popProvider(workInProgress.type, workInProgress), null;
				case 22:
				case 23: return popSuspenseHandler(workInProgress), popHiddenContext(workInProgress), null !== current && pop(resumedCache, workInProgress), current = workInProgress.flags, current & 65536 ? (workInProgress.flags = current & -65537 | 128, (workInProgress.mode & ProfileMode) !== NoMode && transferActualDuration(workInProgress), workInProgress) : null;
				case 24: return popProvider(CacheContext, workInProgress), null;
				case 25: return null;
				default: return null;
			}
		}
		function unwindInterruptedWork(current, interruptedWork) {
			popTreeContext(interruptedWork);
			switch (interruptedWork.tag) {
				case 3:
					popProvider(CacheContext, interruptedWork);
					popHostContainer(interruptedWork);
					break;
				case 26:
				case 27:
				case 5:
					popHostContext(interruptedWork);
					break;
				case 4:
					popHostContainer(interruptedWork);
					break;
				case 31:
					null !== interruptedWork.memoizedState && popSuspenseHandler(interruptedWork);
					break;
				case 13:
					popSuspenseHandler(interruptedWork);
					break;
				case 19:
					pop(suspenseStackCursor, interruptedWork);
					break;
				case 10:
					popProvider(interruptedWork.type, interruptedWork);
					break;
				case 22:
				case 23:
					popSuspenseHandler(interruptedWork);
					popHiddenContext(interruptedWork);
					null !== current && pop(resumedCache, interruptedWork);
					break;
				case 24: popProvider(CacheContext, interruptedWork);
			}
		}
		function shouldProfile(current) {
			return (current.mode & ProfileMode) !== NoMode;
		}
		function commitHookLayoutEffects(finishedWork, hookFlags) {
			shouldProfile(finishedWork) ? (startEffectTimer(), commitHookEffectListMount(hookFlags, finishedWork), recordEffectDuration()) : commitHookEffectListMount(hookFlags, finishedWork);
		}
		function commitHookLayoutUnmountEffects(finishedWork, nearestMountedAncestor, hookFlags) {
			shouldProfile(finishedWork) ? (startEffectTimer(), commitHookEffectListUnmount(hookFlags, finishedWork, nearestMountedAncestor), recordEffectDuration()) : commitHookEffectListUnmount(hookFlags, finishedWork, nearestMountedAncestor);
		}
		function commitHookEffectListMount(flags, finishedWork) {
			try {
				var updateQueue = finishedWork.updateQueue, lastEffect = null !== updateQueue ? updateQueue.lastEffect : null;
				if (null !== lastEffect) {
					var firstEffect = lastEffect.next;
					updateQueue = firstEffect;
					do {
						if ((updateQueue.tag & flags) === flags && (lastEffect = void 0, (flags & Insertion) !== NoFlags && (isRunningInsertionEffect = !0), lastEffect = runWithFiberInDEV(finishedWork, callCreateInDEV, updateQueue), (flags & Insertion) !== NoFlags && (isRunningInsertionEffect = !1), void 0 !== lastEffect && "function" !== typeof lastEffect)) {
							var hookName = void 0;
							hookName = 0 !== (updateQueue.tag & Layout) ? "useLayoutEffect" : 0 !== (updateQueue.tag & Insertion) ? "useInsertionEffect" : "useEffect";
							var addendum = void 0;
							addendum = null === lastEffect ? " You returned null. If your effect does not require clean up, return undefined (or nothing)." : "function" === typeof lastEffect.then ? "\n\nIt looks like you wrote " + hookName + "(async () => ...) or returned a Promise. Instead, write the async function inside your effect and call it immediately:\n\n" + hookName + "(() => {\n  async function fetchData() {\n    // You can await here\n    const response = await MyAPI.getData(someId);\n    // ...\n  }\n  fetchData();\n}, [someId]); // Or [] if effect doesn't need props or state\n\nLearn more about data fetching with Hooks: https://react.dev/link/hooks-data-fetching" : " You returned: " + lastEffect;
							runWithFiberInDEV(finishedWork, function(n, a) {
								console.error("%s must not return anything besides a function, which is used for clean-up.%s", n, a);
							}, hookName, addendum);
						}
						updateQueue = updateQueue.next;
					} while (updateQueue !== firstEffect);
				}
			} catch (error) {
				captureCommitPhaseError(finishedWork, finishedWork.return, error);
			}
		}
		function commitHookEffectListUnmount(flags, finishedWork, nearestMountedAncestor) {
			try {
				var updateQueue = finishedWork.updateQueue, lastEffect = null !== updateQueue ? updateQueue.lastEffect : null;
				if (null !== lastEffect) {
					var firstEffect = lastEffect.next;
					updateQueue = firstEffect;
					do {
						if ((updateQueue.tag & flags) === flags) {
							var inst = updateQueue.inst, destroy = inst.destroy;
							void 0 !== destroy && (inst.destroy = void 0, (flags & Insertion) !== NoFlags && (isRunningInsertionEffect = !0), lastEffect = finishedWork, runWithFiberInDEV(lastEffect, callDestroyInDEV, lastEffect, nearestMountedAncestor, destroy), (flags & Insertion) !== NoFlags && (isRunningInsertionEffect = !1));
						}
						updateQueue = updateQueue.next;
					} while (updateQueue !== firstEffect);
				}
			} catch (error) {
				captureCommitPhaseError(finishedWork, finishedWork.return, error);
			}
		}
		function commitHookPassiveMountEffects(finishedWork, hookFlags) {
			shouldProfile(finishedWork) ? (startEffectTimer(), commitHookEffectListMount(hookFlags, finishedWork), recordEffectDuration()) : commitHookEffectListMount(hookFlags, finishedWork);
		}
		function commitHookPassiveUnmountEffects(finishedWork, nearestMountedAncestor, hookFlags) {
			shouldProfile(finishedWork) ? (startEffectTimer(), commitHookEffectListUnmount(hookFlags, finishedWork, nearestMountedAncestor), recordEffectDuration()) : commitHookEffectListUnmount(hookFlags, finishedWork, nearestMountedAncestor);
		}
		function commitClassCallbacks(finishedWork) {
			var updateQueue = finishedWork.updateQueue;
			if (null !== updateQueue) {
				var instance = finishedWork.stateNode;
				finishedWork.type.defaultProps || "ref" in finishedWork.memoizedProps || didWarnAboutReassigningProps || (instance.props !== finishedWork.memoizedProps && console.error("Expected %s props to match memoized props before processing the update queue. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance"), instance.state !== finishedWork.memoizedState && console.error("Expected %s state to match memoized state before processing the update queue. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance"));
				try {
					runWithFiberInDEV(finishedWork, commitCallbacks, updateQueue, instance);
				} catch (error) {
					captureCommitPhaseError(finishedWork, finishedWork.return, error);
				}
			}
		}
		function callGetSnapshotBeforeUpdates(instance, prevProps, prevState) {
			return instance.getSnapshotBeforeUpdate(prevProps, prevState);
		}
		function commitClassSnapshot(finishedWork, current) {
			var prevProps = current.memoizedProps, prevState = current.memoizedState;
			current = finishedWork.stateNode;
			finishedWork.type.defaultProps || "ref" in finishedWork.memoizedProps || didWarnAboutReassigningProps || (current.props !== finishedWork.memoizedProps && console.error("Expected %s props to match memoized props before getSnapshotBeforeUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance"), current.state !== finishedWork.memoizedState && console.error("Expected %s state to match memoized state before getSnapshotBeforeUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance"));
			try {
				var resolvedPrevProps = resolveClassComponentProps(finishedWork.type, prevProps);
				var snapshot = runWithFiberInDEV(finishedWork, callGetSnapshotBeforeUpdates, current, resolvedPrevProps, prevState);
				prevProps = didWarnAboutUndefinedSnapshotBeforeUpdate;
				void 0 !== snapshot || prevProps.has(finishedWork.type) || (prevProps.add(finishedWork.type), runWithFiberInDEV(finishedWork, function() {
					console.error("%s.getSnapshotBeforeUpdate(): A snapshot value (or null) must be returned. You have returned undefined.", getComponentNameFromFiber(finishedWork));
				}));
				current.__reactInternalSnapshotBeforeUpdate = snapshot;
			} catch (error) {
				captureCommitPhaseError(finishedWork, finishedWork.return, error);
			}
		}
		function safelyCallComponentWillUnmount(current, nearestMountedAncestor, instance) {
			instance.props = resolveClassComponentProps(current.type, current.memoizedProps);
			instance.state = current.memoizedState;
			shouldProfile(current) ? (startEffectTimer(), runWithFiberInDEV(current, callComponentWillUnmountInDEV, current, nearestMountedAncestor, instance), recordEffectDuration()) : runWithFiberInDEV(current, callComponentWillUnmountInDEV, current, nearestMountedAncestor, instance);
		}
		function commitAttachRef(finishedWork) {
			var ref = finishedWork.ref;
			if (null !== ref) {
				switch (finishedWork.tag) {
					case 26:
					case 27:
					case 5:
						var instanceToUse = finishedWork.stateNode;
						break;
					case 30:
						instanceToUse = finishedWork.stateNode;
						break;
					default: instanceToUse = finishedWork.stateNode;
				}
				if ("function" === typeof ref) if (shouldProfile(finishedWork)) try {
					startEffectTimer(), finishedWork.refCleanup = ref(instanceToUse);
				} finally {
					recordEffectDuration();
				}
				else finishedWork.refCleanup = ref(instanceToUse);
				else "string" === typeof ref ? console.error("String refs are no longer supported.") : ref.hasOwnProperty("current") || console.error("Unexpected ref object provided for %s. Use either a ref-setter function or React.createRef().", getComponentNameFromFiber(finishedWork)), ref.current = instanceToUse;
			}
		}
		function safelyAttachRef(current, nearestMountedAncestor) {
			try {
				runWithFiberInDEV(current, commitAttachRef, current);
			} catch (error) {
				captureCommitPhaseError(current, nearestMountedAncestor, error);
			}
		}
		function safelyDetachRef(current, nearestMountedAncestor) {
			var ref = current.ref, refCleanup = current.refCleanup;
			if (null !== ref) if ("function" === typeof refCleanup) try {
				if (shouldProfile(current)) try {
					startEffectTimer(), runWithFiberInDEV(current, refCleanup);
				} finally {
					recordEffectDuration(current);
				}
				else runWithFiberInDEV(current, refCleanup);
			} catch (error) {
				captureCommitPhaseError(current, nearestMountedAncestor, error);
			} finally {
				current.refCleanup = null, current = current.alternate, null != current && (current.refCleanup = null);
			}
			else if ("function" === typeof ref) try {
				if (shouldProfile(current)) try {
					startEffectTimer(), runWithFiberInDEV(current, ref, null);
				} finally {
					recordEffectDuration(current);
				}
				else runWithFiberInDEV(current, ref, null);
			} catch (error$7) {
				captureCommitPhaseError(current, nearestMountedAncestor, error$7);
			}
			else ref.current = null;
		}
		function commitProfiler(finishedWork, current, commitStartTime, effectDuration) {
			var _finishedWork$memoize = finishedWork.memoizedProps, id = _finishedWork$memoize.id, onCommit = _finishedWork$memoize.onCommit;
			_finishedWork$memoize = _finishedWork$memoize.onRender;
			current = null === current ? "mount" : "update";
			currentUpdateIsNested && (current = "nested-update");
			"function" === typeof _finishedWork$memoize && _finishedWork$memoize(id, current, finishedWork.actualDuration, finishedWork.treeBaseDuration, finishedWork.actualStartTime, commitStartTime);
			"function" === typeof onCommit && onCommit(id, current, effectDuration, commitStartTime);
		}
		function commitProfilerPostCommitImpl(finishedWork, current, commitStartTime, passiveEffectDuration) {
			var _finishedWork$memoize2 = finishedWork.memoizedProps;
			finishedWork = _finishedWork$memoize2.id;
			_finishedWork$memoize2 = _finishedWork$memoize2.onPostCommit;
			current = null === current ? "mount" : "update";
			currentUpdateIsNested && (current = "nested-update");
			"function" === typeof _finishedWork$memoize2 && _finishedWork$memoize2(finishedWork, current, passiveEffectDuration, commitStartTime);
		}
		function commitHostMount(finishedWork) {
			var type = finishedWork.type, props = finishedWork.memoizedProps, instance = finishedWork.stateNode;
			try {
				runWithFiberInDEV(finishedWork, commitMount, instance, type, props, finishedWork);
			} catch (error) {
				captureCommitPhaseError(finishedWork, finishedWork.return, error);
			}
		}
		function commitHostUpdate(finishedWork, newProps, oldProps) {
			try {
				runWithFiberInDEV(finishedWork, commitUpdate, finishedWork.stateNode, finishedWork.type, oldProps, newProps, finishedWork);
			} catch (error) {
				captureCommitPhaseError(finishedWork, finishedWork.return, error);
			}
		}
		function isHostParent(fiber) {
			return 5 === fiber.tag || 3 === fiber.tag || 26 === fiber.tag || 27 === fiber.tag && isSingletonScope(fiber.type) || 4 === fiber.tag;
		}
		function getHostSibling(fiber) {
			a: for (;;) {
				for (; null === fiber.sibling;) {
					if (null === fiber.return || isHostParent(fiber.return)) return null;
					fiber = fiber.return;
				}
				fiber.sibling.return = fiber.return;
				for (fiber = fiber.sibling; 5 !== fiber.tag && 6 !== fiber.tag && 18 !== fiber.tag;) {
					if (27 === fiber.tag && isSingletonScope(fiber.type)) continue a;
					if (fiber.flags & 2) continue a;
					if (null === fiber.child || 4 === fiber.tag) continue a;
					else fiber.child.return = fiber, fiber = fiber.child;
				}
				if (!(fiber.flags & 2)) return fiber.stateNode;
			}
		}
		function insertOrAppendPlacementNodeIntoContainer(node, before, parent) {
			var tag = node.tag;
			if (5 === tag || 6 === tag) node = node.stateNode, before ? (warnForReactChildrenConflict(parent), (9 === parent.nodeType ? parent.body : "HTML" === parent.nodeName ? parent.ownerDocument.body : parent).insertBefore(node, before)) : (warnForReactChildrenConflict(parent), before = 9 === parent.nodeType ? parent.body : "HTML" === parent.nodeName ? parent.ownerDocument.body : parent, before.appendChild(node), parent = parent._reactRootContainer, null !== parent && void 0 !== parent || null !== before.onclick || (before.onclick = noop$1));
			else if (4 !== tag && (27 === tag && isSingletonScope(node.type) && (parent = node.stateNode, before = null), node = node.child, null !== node)) for (insertOrAppendPlacementNodeIntoContainer(node, before, parent), node = node.sibling; null !== node;) insertOrAppendPlacementNodeIntoContainer(node, before, parent), node = node.sibling;
		}
		function insertOrAppendPlacementNode(node, before, parent) {
			var tag = node.tag;
			if (5 === tag || 6 === tag) node = node.stateNode, before ? parent.insertBefore(node, before) : parent.appendChild(node);
			else if (4 !== tag && (27 === tag && isSingletonScope(node.type) && (parent = node.stateNode), node = node.child, null !== node)) for (insertOrAppendPlacementNode(node, before, parent), node = node.sibling; null !== node;) insertOrAppendPlacementNode(node, before, parent), node = node.sibling;
		}
		function commitPlacement(finishedWork) {
			for (var hostParentFiber, parentFiber = finishedWork.return; null !== parentFiber;) {
				if (isHostParent(parentFiber)) {
					hostParentFiber = parentFiber;
					break;
				}
				parentFiber = parentFiber.return;
			}
			if (null == hostParentFiber) throw Error("Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.");
			switch (hostParentFiber.tag) {
				case 27:
					hostParentFiber = hostParentFiber.stateNode;
					parentFiber = getHostSibling(finishedWork);
					insertOrAppendPlacementNode(finishedWork, parentFiber, hostParentFiber);
					break;
				case 5:
					parentFiber = hostParentFiber.stateNode;
					hostParentFiber.flags & 32 && (resetTextContent(parentFiber), hostParentFiber.flags &= -33);
					hostParentFiber = getHostSibling(finishedWork);
					insertOrAppendPlacementNode(finishedWork, hostParentFiber, parentFiber);
					break;
				case 3:
				case 4:
					hostParentFiber = hostParentFiber.stateNode.containerInfo;
					parentFiber = getHostSibling(finishedWork);
					insertOrAppendPlacementNodeIntoContainer(finishedWork, parentFiber, hostParentFiber);
					break;
				default: throw Error("Invalid host parent fiber. This error is likely caused by a bug in React. Please file an issue.");
			}
		}
		function commitHostSingletonAcquisition(finishedWork) {
			var singleton = finishedWork.stateNode, props = finishedWork.memoizedProps;
			try {
				runWithFiberInDEV(finishedWork, acquireSingletonInstance, finishedWork.type, props, singleton, finishedWork);
			} catch (error) {
				captureCommitPhaseError(finishedWork, finishedWork.return, error);
			}
		}
		function isHydratingParent(current, finishedWork) {
			return 31 === finishedWork.tag ? (finishedWork = finishedWork.memoizedState, null !== current.memoizedState && null === finishedWork) : 13 === finishedWork.tag ? (current = current.memoizedState, finishedWork = finishedWork.memoizedState, null !== current && null !== current.dehydrated && (null === finishedWork || null === finishedWork.dehydrated)) : 3 === finishedWork.tag ? current.memoizedState.isDehydrated && 0 === (finishedWork.flags & 256) : !1;
		}
		function commitBeforeMutationEffects(root, firstChild) {
			root = root.containerInfo;
			eventsEnabled = _enabled;
			root = getActiveElementDeep(root);
			if (hasSelectionCapabilities(root)) {
				if ("selectionStart" in root) var JSCompiler_temp = {
					start: root.selectionStart,
					end: root.selectionEnd
				};
				else a: {
					JSCompiler_temp = (JSCompiler_temp = root.ownerDocument) && JSCompiler_temp.defaultView || window;
					var selection = JSCompiler_temp.getSelection && JSCompiler_temp.getSelection();
					if (selection && 0 !== selection.rangeCount) {
						JSCompiler_temp = selection.anchorNode;
						var anchorOffset = selection.anchorOffset, focusNode = selection.focusNode;
						selection = selection.focusOffset;
						try {
							JSCompiler_temp.nodeType, focusNode.nodeType;
						} catch (e$2) {
							JSCompiler_temp = null;
							break a;
						}
						var length = 0, start = -1, end = -1, indexWithinAnchor = 0, indexWithinFocus = 0, node = root, parentNode = null;
						b: for (;;) {
							for (var next;;) {
								node !== JSCompiler_temp || 0 !== anchorOffset && 3 !== node.nodeType || (start = length + anchorOffset);
								node !== focusNode || 0 !== selection && 3 !== node.nodeType || (end = length + selection);
								3 === node.nodeType && (length += node.nodeValue.length);
								if (null === (next = node.firstChild)) break;
								parentNode = node;
								node = next;
							}
							for (;;) {
								if (node === root) break b;
								parentNode === JSCompiler_temp && ++indexWithinAnchor === anchorOffset && (start = length);
								parentNode === focusNode && ++indexWithinFocus === selection && (end = length);
								if (null !== (next = node.nextSibling)) break;
								node = parentNode;
								parentNode = node.parentNode;
							}
							node = next;
						}
						JSCompiler_temp = -1 === start || -1 === end ? null : {
							start,
							end
						};
					} else JSCompiler_temp = null;
				}
				JSCompiler_temp = JSCompiler_temp || {
					start: 0,
					end: 0
				};
			} else JSCompiler_temp = null;
			selectionInformation = {
				focusedElem: root,
				selectionRange: JSCompiler_temp
			};
			_enabled = !1;
			for (nextEffect = firstChild; null !== nextEffect;) if (firstChild = nextEffect, root = firstChild.child, 0 !== (firstChild.subtreeFlags & 1028) && null !== root) root.return = firstChild, nextEffect = root;
			else for (; null !== nextEffect;) {
				root = firstChild = nextEffect;
				JSCompiler_temp = root.alternate;
				anchorOffset = root.flags;
				switch (root.tag) {
					case 0:
						if (0 !== (anchorOffset & 4) && (root = root.updateQueue, root = null !== root ? root.events : null, null !== root)) for (JSCompiler_temp = 0; JSCompiler_temp < root.length; JSCompiler_temp++) anchorOffset = root[JSCompiler_temp], anchorOffset.ref.impl = anchorOffset.nextImpl;
						break;
					case 11:
					case 15: break;
					case 1:
						0 !== (anchorOffset & 1024) && null !== JSCompiler_temp && commitClassSnapshot(root, JSCompiler_temp);
						break;
					case 3:
						if (0 !== (anchorOffset & 1024)) {
							if (root = root.stateNode.containerInfo, JSCompiler_temp = root.nodeType, 9 === JSCompiler_temp) clearContainerSparingly(root);
							else if (1 === JSCompiler_temp) switch (root.nodeName) {
								case "HEAD":
								case "HTML":
								case "BODY":
									clearContainerSparingly(root);
									break;
								default: root.textContent = "";
							}
						}
						break;
					case 5:
					case 26:
					case 27:
					case 6:
					case 4:
					case 17: break;
					default: if (0 !== (anchorOffset & 1024)) throw Error("This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.");
				}
				root = firstChild.sibling;
				if (null !== root) {
					root.return = firstChild.return;
					nextEffect = root;
					break;
				}
				nextEffect = firstChild.return;
			}
		}
		function commitLayoutEffectOnFiber(finishedRoot, current, finishedWork) {
			var prevEffectStart = pushComponentEffectStart(), prevEffectDuration = pushComponentEffectDuration(), prevEffectErrors = pushComponentEffectErrors(), prevEffectDidSpawnUpdate = pushComponentEffectDidSpawnUpdate(), flags = finishedWork.flags;
			switch (finishedWork.tag) {
				case 0:
				case 11:
				case 15:
					recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
					flags & 4 && commitHookLayoutEffects(finishedWork, Layout | HasEffect);
					break;
				case 1:
					recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
					if (flags & 4) if (finishedRoot = finishedWork.stateNode, null === current) finishedWork.type.defaultProps || "ref" in finishedWork.memoizedProps || didWarnAboutReassigningProps || (finishedRoot.props !== finishedWork.memoizedProps && console.error("Expected %s props to match memoized props before componentDidMount. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance"), finishedRoot.state !== finishedWork.memoizedState && console.error("Expected %s state to match memoized state before componentDidMount. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance")), shouldProfile(finishedWork) ? (startEffectTimer(), runWithFiberInDEV(finishedWork, callComponentDidMountInDEV, finishedWork, finishedRoot), recordEffectDuration()) : runWithFiberInDEV(finishedWork, callComponentDidMountInDEV, finishedWork, finishedRoot);
					else {
						var prevProps = resolveClassComponentProps(finishedWork.type, current.memoizedProps);
						current = current.memoizedState;
						finishedWork.type.defaultProps || "ref" in finishedWork.memoizedProps || didWarnAboutReassigningProps || (finishedRoot.props !== finishedWork.memoizedProps && console.error("Expected %s props to match memoized props before componentDidUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.props`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance"), finishedRoot.state !== finishedWork.memoizedState && console.error("Expected %s state to match memoized state before componentDidUpdate. This might either be because of a bug in React, or because a component reassigns its own `this.state`. Please file an issue.", getComponentNameFromFiber(finishedWork) || "instance"));
						shouldProfile(finishedWork) ? (startEffectTimer(), runWithFiberInDEV(finishedWork, callComponentDidUpdateInDEV, finishedWork, finishedRoot, prevProps, current, finishedRoot.__reactInternalSnapshotBeforeUpdate), recordEffectDuration()) : runWithFiberInDEV(finishedWork, callComponentDidUpdateInDEV, finishedWork, finishedRoot, prevProps, current, finishedRoot.__reactInternalSnapshotBeforeUpdate);
					}
					flags & 64 && commitClassCallbacks(finishedWork);
					flags & 512 && safelyAttachRef(finishedWork, finishedWork.return);
					break;
				case 3:
					current = pushNestedEffectDurations();
					recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
					if (flags & 64 && (flags = finishedWork.updateQueue, null !== flags)) {
						prevProps = null;
						if (null !== finishedWork.child) switch (finishedWork.child.tag) {
							case 27:
							case 5:
								prevProps = finishedWork.child.stateNode;
								break;
							case 1: prevProps = finishedWork.child.stateNode;
						}
						try {
							runWithFiberInDEV(finishedWork, commitCallbacks, flags, prevProps);
						} catch (error) {
							captureCommitPhaseError(finishedWork, finishedWork.return, error);
						}
					}
					finishedRoot.effectDuration += popNestedEffectDurations(current);
					break;
				case 27: null === current && flags & 4 && commitHostSingletonAcquisition(finishedWork);
				case 26:
				case 5:
					recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
					if (null === current) {
						if (flags & 4) commitHostMount(finishedWork);
						else if (flags & 64) {
							finishedRoot = finishedWork.type;
							current = finishedWork.memoizedProps;
							prevProps = finishedWork.stateNode;
							try {
								runWithFiberInDEV(finishedWork, commitHydratedInstance, prevProps, finishedRoot, current, finishedWork);
							} catch (error) {
								captureCommitPhaseError(finishedWork, finishedWork.return, error);
							}
						}
					}
					flags & 512 && safelyAttachRef(finishedWork, finishedWork.return);
					break;
				case 12:
					if (flags & 4) {
						flags = pushNestedEffectDurations();
						recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
						finishedRoot = finishedWork.stateNode;
						finishedRoot.effectDuration += bubbleNestedEffectDurations(flags);
						try {
							runWithFiberInDEV(finishedWork, commitProfiler, finishedWork, current, commitStartTime, finishedRoot.effectDuration);
						} catch (error) {
							captureCommitPhaseError(finishedWork, finishedWork.return, error);
						}
					} else recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
					break;
				case 31:
					recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
					flags & 4 && commitActivityHydrationCallbacks(finishedRoot, finishedWork);
					break;
				case 13:
					recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
					flags & 4 && commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
					flags & 64 && (finishedRoot = finishedWork.memoizedState, null !== finishedRoot && (finishedRoot = finishedRoot.dehydrated, null !== finishedRoot && (flags = retryDehydratedSuspenseBoundary.bind(null, finishedWork), registerSuspenseInstanceRetry(finishedRoot, flags))));
					break;
				case 22:
					flags = null !== finishedWork.memoizedState || offscreenSubtreeIsHidden;
					if (!flags) {
						current = null !== current && null !== current.memoizedState || offscreenSubtreeWasHidden;
						prevProps = offscreenSubtreeIsHidden;
						var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
						offscreenSubtreeIsHidden = flags;
						(offscreenSubtreeWasHidden = current) && !prevOffscreenSubtreeWasHidden ? (recursivelyTraverseReappearLayoutEffects(finishedRoot, finishedWork, 0 !== (finishedWork.subtreeFlags & 8772)), (finishedWork.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && .05 < componentEffectEndTime - componentEffectStartTime && logComponentReappeared(finishedWork, componentEffectStartTime, componentEffectEndTime)) : recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
						offscreenSubtreeIsHidden = prevProps;
						offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
					}
					break;
				case 30: break;
				default: recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
			}
			(finishedWork.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && ((componentEffectSpawnedUpdate || .05 < componentEffectDuration) && logComponentEffect(finishedWork, componentEffectStartTime, componentEffectEndTime, componentEffectDuration, componentEffectErrors), null === finishedWork.alternate && null !== finishedWork.return && null !== finishedWork.return.alternate && .05 < componentEffectEndTime - componentEffectStartTime && (isHydratingParent(finishedWork.return.alternate, finishedWork.return) || logComponentTrigger(finishedWork, componentEffectStartTime, componentEffectEndTime, "Mount")));
			popComponentEffectStart(prevEffectStart);
			popComponentEffectDuration(prevEffectDuration);
			componentEffectErrors = prevEffectErrors;
			componentEffectSpawnedUpdate = prevEffectDidSpawnUpdate;
		}
		function detachFiberAfterEffects(fiber) {
			var alternate = fiber.alternate;
			null !== alternate && (fiber.alternate = null, detachFiberAfterEffects(alternate));
			fiber.child = null;
			fiber.deletions = null;
			fiber.sibling = null;
			5 === fiber.tag && (alternate = fiber.stateNode, null !== alternate && detachDeletedInstance(alternate));
			fiber.stateNode = null;
			fiber._debugOwner = null;
			fiber.return = null;
			fiber.dependencies = null;
			fiber.memoizedProps = null;
			fiber.memoizedState = null;
			fiber.pendingProps = null;
			fiber.stateNode = null;
			fiber.updateQueue = null;
		}
		function recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, parent) {
			for (parent = parent.child; null !== parent;) commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, parent), parent = parent.sibling;
		}
		function commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, deletedFiber) {
			if (injectedHook && "function" === typeof injectedHook.onCommitFiberUnmount) try {
				injectedHook.onCommitFiberUnmount(rendererID, deletedFiber);
			} catch (err) {
				hasLoggedError || (hasLoggedError = !0, console.error("React instrumentation encountered an error: %o", err));
			}
			var prevEffectStart = pushComponentEffectStart(), prevEffectDuration = pushComponentEffectDuration(), prevEffectErrors = pushComponentEffectErrors(), prevEffectDidSpawnUpdate = pushComponentEffectDidSpawnUpdate();
			switch (deletedFiber.tag) {
				case 26:
					offscreenSubtreeWasHidden || safelyDetachRef(deletedFiber, nearestMountedAncestor);
					recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
					deletedFiber.memoizedState ? deletedFiber.memoizedState.count-- : deletedFiber.stateNode && (finishedRoot = deletedFiber.stateNode, finishedRoot.parentNode.removeChild(finishedRoot));
					break;
				case 27:
					offscreenSubtreeWasHidden || safelyDetachRef(deletedFiber, nearestMountedAncestor);
					var prevHostParent = hostParent, prevHostParentIsContainer = hostParentIsContainer;
					isSingletonScope(deletedFiber.type) && (hostParent = deletedFiber.stateNode, hostParentIsContainer = !1);
					recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
					runWithFiberInDEV(deletedFiber, releaseSingletonInstance, deletedFiber.stateNode);
					hostParent = prevHostParent;
					hostParentIsContainer = prevHostParentIsContainer;
					break;
				case 5: offscreenSubtreeWasHidden || safelyDetachRef(deletedFiber, nearestMountedAncestor);
				case 6:
					prevHostParent = hostParent;
					prevHostParentIsContainer = hostParentIsContainer;
					hostParent = null;
					recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
					hostParent = prevHostParent;
					hostParentIsContainer = prevHostParentIsContainer;
					if (null !== hostParent) if (hostParentIsContainer) try {
						runWithFiberInDEV(deletedFiber, removeChildFromContainer, hostParent, deletedFiber.stateNode);
					} catch (error) {
						captureCommitPhaseError(deletedFiber, nearestMountedAncestor, error);
					}
					else try {
						runWithFiberInDEV(deletedFiber, removeChild, hostParent, deletedFiber.stateNode);
					} catch (error) {
						captureCommitPhaseError(deletedFiber, nearestMountedAncestor, error);
					}
					break;
				case 18:
					null !== hostParent && (hostParentIsContainer ? (finishedRoot = hostParent, clearHydrationBoundary(9 === finishedRoot.nodeType ? finishedRoot.body : "HTML" === finishedRoot.nodeName ? finishedRoot.ownerDocument.body : finishedRoot, deletedFiber.stateNode), retryIfBlockedOn(finishedRoot)) : clearHydrationBoundary(hostParent, deletedFiber.stateNode));
					break;
				case 4:
					prevHostParent = hostParent;
					prevHostParentIsContainer = hostParentIsContainer;
					hostParent = deletedFiber.stateNode.containerInfo;
					hostParentIsContainer = !0;
					recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
					hostParent = prevHostParent;
					hostParentIsContainer = prevHostParentIsContainer;
					break;
				case 0:
				case 11:
				case 14:
				case 15:
					commitHookEffectListUnmount(Insertion, deletedFiber, nearestMountedAncestor);
					offscreenSubtreeWasHidden || commitHookLayoutUnmountEffects(deletedFiber, nearestMountedAncestor, Layout);
					recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
					break;
				case 1:
					offscreenSubtreeWasHidden || (safelyDetachRef(deletedFiber, nearestMountedAncestor), prevHostParent = deletedFiber.stateNode, "function" === typeof prevHostParent.componentWillUnmount && safelyCallComponentWillUnmount(deletedFiber, nearestMountedAncestor, prevHostParent));
					recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
					break;
				case 21:
					recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
					break;
				case 22:
					offscreenSubtreeWasHidden = (prevHostParent = offscreenSubtreeWasHidden) || null !== deletedFiber.memoizedState;
					recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
					offscreenSubtreeWasHidden = prevHostParent;
					break;
				default: recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, deletedFiber);
			}
			(deletedFiber.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && (componentEffectSpawnedUpdate || .05 < componentEffectDuration) && logComponentEffect(deletedFiber, componentEffectStartTime, componentEffectEndTime, componentEffectDuration, componentEffectErrors);
			popComponentEffectStart(prevEffectStart);
			popComponentEffectDuration(prevEffectDuration);
			componentEffectErrors = prevEffectErrors;
			componentEffectSpawnedUpdate = prevEffectDidSpawnUpdate;
		}
		function commitActivityHydrationCallbacks(finishedRoot, finishedWork) {
			if (null === finishedWork.memoizedState && (finishedRoot = finishedWork.alternate, null !== finishedRoot && (finishedRoot = finishedRoot.memoizedState, null !== finishedRoot))) {
				finishedRoot = finishedRoot.dehydrated;
				try {
					runWithFiberInDEV(finishedWork, commitHydratedActivityInstance, finishedRoot);
				} catch (error) {
					captureCommitPhaseError(finishedWork, finishedWork.return, error);
				}
			}
		}
		function commitSuspenseHydrationCallbacks(finishedRoot, finishedWork) {
			if (null === finishedWork.memoizedState && (finishedRoot = finishedWork.alternate, null !== finishedRoot && (finishedRoot = finishedRoot.memoizedState, null !== finishedRoot && (finishedRoot = finishedRoot.dehydrated, null !== finishedRoot)))) try {
				runWithFiberInDEV(finishedWork, commitHydratedSuspenseInstance, finishedRoot);
			} catch (error) {
				captureCommitPhaseError(finishedWork, finishedWork.return, error);
			}
		}
		function getRetryCache(finishedWork) {
			switch (finishedWork.tag) {
				case 31:
				case 13:
				case 19:
					var retryCache = finishedWork.stateNode;
					null === retryCache && (retryCache = finishedWork.stateNode = new PossiblyWeakSet());
					return retryCache;
				case 22: return finishedWork = finishedWork.stateNode, retryCache = finishedWork._retryCache, null === retryCache && (retryCache = finishedWork._retryCache = new PossiblyWeakSet()), retryCache;
				default: throw Error("Unexpected Suspense handler tag (" + finishedWork.tag + "). This is a bug in React.");
			}
		}
		function attachSuspenseRetryListeners(finishedWork, wakeables) {
			var retryCache = getRetryCache(finishedWork);
			wakeables.forEach(function(wakeable) {
				if (!retryCache.has(wakeable)) {
					retryCache.add(wakeable);
					if (isDevToolsPresent) if (null !== inProgressLanes && null !== inProgressRoot) restorePendingUpdaters(inProgressRoot, inProgressLanes);
					else throw Error("Expected finished root and lanes to be set. This is a bug in React.");
					var retry = resolveRetryWakeable.bind(null, finishedWork, wakeable);
					wakeable.then(retry, retry);
				}
			});
		}
		function recursivelyTraverseMutationEffects(root$jscomp$0, parentFiber) {
			var deletions = parentFiber.deletions;
			if (null !== deletions) for (var i = 0; i < deletions.length; i++) {
				var root = root$jscomp$0, returnFiber = parentFiber, deletedFiber = deletions[i], prevEffectStart = pushComponentEffectStart(), parent = returnFiber;
				a: for (; null !== parent;) {
					switch (parent.tag) {
						case 27:
							if (isSingletonScope(parent.type)) {
								hostParent = parent.stateNode;
								hostParentIsContainer = !1;
								break a;
							}
							break;
						case 5:
							hostParent = parent.stateNode;
							hostParentIsContainer = !1;
							break a;
						case 3:
						case 4:
							hostParent = parent.stateNode.containerInfo;
							hostParentIsContainer = !0;
							break a;
					}
					parent = parent.return;
				}
				if (null === hostParent) throw Error("Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.");
				commitDeletionEffectsOnFiber(root, returnFiber, deletedFiber);
				hostParent = null;
				hostParentIsContainer = !1;
				(deletedFiber.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && .05 < componentEffectEndTime - componentEffectStartTime && logComponentTrigger(deletedFiber, componentEffectStartTime, componentEffectEndTime, "Unmount");
				popComponentEffectStart(prevEffectStart);
				root = deletedFiber;
				returnFiber = root.alternate;
				null !== returnFiber && (returnFiber.return = null);
				root.return = null;
			}
			if (parentFiber.subtreeFlags & 13886) for (parentFiber = parentFiber.child; null !== parentFiber;) commitMutationEffectsOnFiber(parentFiber, root$jscomp$0), parentFiber = parentFiber.sibling;
		}
		function commitMutationEffectsOnFiber(finishedWork, root) {
			var prevEffectStart = pushComponentEffectStart(), prevEffectDuration = pushComponentEffectDuration(), prevEffectErrors = pushComponentEffectErrors(), prevEffectDidSpawnUpdate = pushComponentEffectDidSpawnUpdate(), current = finishedWork.alternate, flags = finishedWork.flags;
			switch (finishedWork.tag) {
				case 0:
				case 11:
				case 14:
				case 15:
					recursivelyTraverseMutationEffects(root, finishedWork);
					commitReconciliationEffects(finishedWork);
					flags & 4 && (commitHookEffectListUnmount(Insertion | HasEffect, finishedWork, finishedWork.return), commitHookEffectListMount(Insertion | HasEffect, finishedWork), commitHookLayoutUnmountEffects(finishedWork, finishedWork.return, Layout | HasEffect));
					break;
				case 1:
					recursivelyTraverseMutationEffects(root, finishedWork);
					commitReconciliationEffects(finishedWork);
					flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
					if (flags & 64 && offscreenSubtreeIsHidden && (flags = finishedWork.updateQueue, null !== flags && (current = flags.callbacks, null !== current))) {
						var existingHiddenCallbacks = flags.shared.hiddenCallbacks;
						flags.shared.hiddenCallbacks = null === existingHiddenCallbacks ? current : existingHiddenCallbacks.concat(current);
					}
					break;
				case 26:
					existingHiddenCallbacks = currentHoistableRoot;
					recursivelyTraverseMutationEffects(root, finishedWork);
					commitReconciliationEffects(finishedWork);
					flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
					if (flags & 4) {
						var currentResource = null !== current ? current.memoizedState : null;
						flags = finishedWork.memoizedState;
						if (null === current) if (null === flags) if (null === finishedWork.stateNode) {
							a: {
								flags = finishedWork.type;
								current = finishedWork.memoizedProps;
								existingHiddenCallbacks = existingHiddenCallbacks.ownerDocument || existingHiddenCallbacks;
								b: switch (flags) {
									case "title":
										currentResource = existingHiddenCallbacks.getElementsByTagName("title")[0];
										if (!currentResource || currentResource[internalHoistableMarker] || currentResource[internalInstanceKey] || currentResource.namespaceURI === SVG_NAMESPACE || currentResource.hasAttribute("itemprop")) currentResource = existingHiddenCallbacks.createElement(flags), existingHiddenCallbacks.head.insertBefore(currentResource, existingHiddenCallbacks.querySelector("head > title"));
										setInitialProperties(currentResource, flags, current);
										currentResource[internalInstanceKey] = finishedWork;
										markNodeAsHoistable(currentResource);
										flags = currentResource;
										break a;
									case "link":
										var maybeNodes = getHydratableHoistableCache("link", "href", existingHiddenCallbacks).get(flags + (current.href || ""));
										if (maybeNodes) {
											for (var i = 0; i < maybeNodes.length; i++) if (currentResource = maybeNodes[i], currentResource.getAttribute("href") === (null == current.href || "" === current.href ? null : current.href) && currentResource.getAttribute("rel") === (null == current.rel ? null : current.rel) && currentResource.getAttribute("title") === (null == current.title ? null : current.title) && currentResource.getAttribute("crossorigin") === (null == current.crossOrigin ? null : current.crossOrigin)) {
												maybeNodes.splice(i, 1);
												break b;
											}
										}
										currentResource = existingHiddenCallbacks.createElement(flags);
										setInitialProperties(currentResource, flags, current);
										existingHiddenCallbacks.head.appendChild(currentResource);
										break;
									case "meta":
										if (maybeNodes = getHydratableHoistableCache("meta", "content", existingHiddenCallbacks).get(flags + (current.content || ""))) {
											for (i = 0; i < maybeNodes.length; i++) if (currentResource = maybeNodes[i], checkAttributeStringCoercion(current.content, "content"), currentResource.getAttribute("content") === (null == current.content ? null : "" + current.content) && currentResource.getAttribute("name") === (null == current.name ? null : current.name) && currentResource.getAttribute("property") === (null == current.property ? null : current.property) && currentResource.getAttribute("http-equiv") === (null == current.httpEquiv ? null : current.httpEquiv) && currentResource.getAttribute("charset") === (null == current.charSet ? null : current.charSet)) {
												maybeNodes.splice(i, 1);
												break b;
											}
										}
										currentResource = existingHiddenCallbacks.createElement(flags);
										setInitialProperties(currentResource, flags, current);
										existingHiddenCallbacks.head.appendChild(currentResource);
										break;
									default: throw Error("getNodesForType encountered a type it did not expect: \"" + flags + "\". This is a bug in React.");
								}
								currentResource[internalInstanceKey] = finishedWork;
								markNodeAsHoistable(currentResource);
								flags = currentResource;
							}
							finishedWork.stateNode = flags;
						} else mountHoistable(existingHiddenCallbacks, finishedWork.type, finishedWork.stateNode);
						else finishedWork.stateNode = acquireResource(existingHiddenCallbacks, flags, finishedWork.memoizedProps);
						else currentResource !== flags ? (null === currentResource ? null !== current.stateNode && (current = current.stateNode, current.parentNode.removeChild(current)) : currentResource.count--, null === flags ? mountHoistable(existingHiddenCallbacks, finishedWork.type, finishedWork.stateNode) : acquireResource(existingHiddenCallbacks, flags, finishedWork.memoizedProps)) : null === flags && null !== finishedWork.stateNode && commitHostUpdate(finishedWork, finishedWork.memoizedProps, current.memoizedProps);
					}
					break;
				case 27:
					recursivelyTraverseMutationEffects(root, finishedWork);
					commitReconciliationEffects(finishedWork);
					flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
					null !== current && flags & 4 && commitHostUpdate(finishedWork, finishedWork.memoizedProps, current.memoizedProps);
					break;
				case 5:
					recursivelyTraverseMutationEffects(root, finishedWork);
					commitReconciliationEffects(finishedWork);
					flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
					if (finishedWork.flags & 32) {
						existingHiddenCallbacks = finishedWork.stateNode;
						try {
							runWithFiberInDEV(finishedWork, resetTextContent, existingHiddenCallbacks);
						} catch (error) {
							captureCommitPhaseError(finishedWork, finishedWork.return, error);
						}
					}
					flags & 4 && null != finishedWork.stateNode && (existingHiddenCallbacks = finishedWork.memoizedProps, commitHostUpdate(finishedWork, existingHiddenCallbacks, null !== current ? current.memoizedProps : existingHiddenCallbacks));
					flags & 1024 && (needsFormReset = !0, "form" !== finishedWork.type && console.error("Unexpected host component type. Expected a form. This is a bug in React."));
					break;
				case 6:
					recursivelyTraverseMutationEffects(root, finishedWork);
					commitReconciliationEffects(finishedWork);
					if (flags & 4) {
						if (null === finishedWork.stateNode) throw Error("This should have a text node initialized. This error is likely caused by a bug in React. Please file an issue.");
						flags = finishedWork.memoizedProps;
						current = null !== current ? current.memoizedProps : flags;
						existingHiddenCallbacks = finishedWork.stateNode;
						try {
							runWithFiberInDEV(finishedWork, commitTextUpdate, existingHiddenCallbacks, current, flags);
						} catch (error) {
							captureCommitPhaseError(finishedWork, finishedWork.return, error);
						}
					}
					break;
				case 3:
					existingHiddenCallbacks = pushNestedEffectDurations();
					tagCaches = null;
					currentResource = currentHoistableRoot;
					currentHoistableRoot = getHoistableRoot(root.containerInfo);
					recursivelyTraverseMutationEffects(root, finishedWork);
					currentHoistableRoot = currentResource;
					commitReconciliationEffects(finishedWork);
					if (flags & 4 && null !== current && current.memoizedState.isDehydrated) try {
						runWithFiberInDEV(finishedWork, commitHydratedContainer, root.containerInfo);
					} catch (error) {
						captureCommitPhaseError(finishedWork, finishedWork.return, error);
					}
					needsFormReset && (needsFormReset = !1, recursivelyResetForms(finishedWork));
					root.effectDuration += popNestedEffectDurations(existingHiddenCallbacks);
					break;
				case 4:
					flags = currentHoistableRoot;
					currentHoistableRoot = getHoistableRoot(finishedWork.stateNode.containerInfo);
					recursivelyTraverseMutationEffects(root, finishedWork);
					commitReconciliationEffects(finishedWork);
					currentHoistableRoot = flags;
					break;
				case 12:
					flags = pushNestedEffectDurations();
					recursivelyTraverseMutationEffects(root, finishedWork);
					commitReconciliationEffects(finishedWork);
					finishedWork.stateNode.effectDuration += bubbleNestedEffectDurations(flags);
					break;
				case 31:
					recursivelyTraverseMutationEffects(root, finishedWork);
					commitReconciliationEffects(finishedWork);
					flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (finishedWork.updateQueue = null, attachSuspenseRetryListeners(finishedWork, flags)));
					break;
				case 13:
					recursivelyTraverseMutationEffects(root, finishedWork);
					commitReconciliationEffects(finishedWork);
					finishedWork.child.flags & 8192 && null !== finishedWork.memoizedState !== (null !== current && null !== current.memoizedState) && (globalMostRecentFallbackTime = now$1());
					flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (finishedWork.updateQueue = null, attachSuspenseRetryListeners(finishedWork, flags)));
					break;
				case 22:
					existingHiddenCallbacks = null !== finishedWork.memoizedState;
					var wasHidden = null !== current && null !== current.memoizedState, prevOffscreenSubtreeIsHidden = offscreenSubtreeIsHidden, prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
					offscreenSubtreeIsHidden = prevOffscreenSubtreeIsHidden || existingHiddenCallbacks;
					offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden || wasHidden;
					recursivelyTraverseMutationEffects(root, finishedWork);
					offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
					offscreenSubtreeIsHidden = prevOffscreenSubtreeIsHidden;
					wasHidden && !existingHiddenCallbacks && !prevOffscreenSubtreeIsHidden && !prevOffscreenSubtreeWasHidden && (finishedWork.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && .05 < componentEffectEndTime - componentEffectStartTime && logComponentReappeared(finishedWork, componentEffectStartTime, componentEffectEndTime);
					commitReconciliationEffects(finishedWork);
					if (flags & 8192) a: for (root = finishedWork.stateNode, root._visibility = existingHiddenCallbacks ? root._visibility & ~OffscreenVisible : root._visibility | OffscreenVisible, !existingHiddenCallbacks || null === current || wasHidden || offscreenSubtreeIsHidden || offscreenSubtreeWasHidden || (recursivelyTraverseDisappearLayoutEffects(finishedWork), (finishedWork.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && .05 < componentEffectEndTime - componentEffectStartTime && logComponentTrigger(finishedWork, componentEffectStartTime, componentEffectEndTime, "Disconnect")), current = null, root = finishedWork;;) {
						if (5 === root.tag || 26 === root.tag) {
							if (null === current) {
								wasHidden = current = root;
								try {
									currentResource = wasHidden.stateNode, existingHiddenCallbacks ? runWithFiberInDEV(wasHidden, hideInstance, currentResource) : runWithFiberInDEV(wasHidden, unhideInstance, wasHidden.stateNode, wasHidden.memoizedProps);
								} catch (error) {
									captureCommitPhaseError(wasHidden, wasHidden.return, error);
								}
							}
						} else if (6 === root.tag) {
							if (null === current) {
								wasHidden = root;
								try {
									maybeNodes = wasHidden.stateNode, existingHiddenCallbacks ? runWithFiberInDEV(wasHidden, hideTextInstance, maybeNodes) : runWithFiberInDEV(wasHidden, unhideTextInstance, maybeNodes, wasHidden.memoizedProps);
								} catch (error) {
									captureCommitPhaseError(wasHidden, wasHidden.return, error);
								}
							}
						} else if (18 === root.tag) {
							if (null === current) {
								wasHidden = root;
								try {
									i = wasHidden.stateNode, existingHiddenCallbacks ? runWithFiberInDEV(wasHidden, hideDehydratedBoundary, i) : runWithFiberInDEV(wasHidden, unhideDehydratedBoundary, wasHidden.stateNode);
								} catch (error) {
									captureCommitPhaseError(wasHidden, wasHidden.return, error);
								}
							}
						} else if ((22 !== root.tag && 23 !== root.tag || null === root.memoizedState || root === finishedWork) && null !== root.child) {
							root.child.return = root;
							root = root.child;
							continue;
						}
						if (root === finishedWork) break a;
						for (; null === root.sibling;) {
							if (null === root.return || root.return === finishedWork) break a;
							current === root && (current = null);
							root = root.return;
						}
						current === root && (current = null);
						root.sibling.return = root.return;
						root = root.sibling;
					}
					flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (current = flags.retryQueue, null !== current && (flags.retryQueue = null, attachSuspenseRetryListeners(finishedWork, current))));
					break;
				case 19:
					recursivelyTraverseMutationEffects(root, finishedWork);
					commitReconciliationEffects(finishedWork);
					flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (finishedWork.updateQueue = null, attachSuspenseRetryListeners(finishedWork, flags)));
					break;
				case 30: break;
				case 21: break;
				default: recursivelyTraverseMutationEffects(root, finishedWork), commitReconciliationEffects(finishedWork);
			}
			(finishedWork.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && ((componentEffectSpawnedUpdate || .05 < componentEffectDuration) && logComponentEffect(finishedWork, componentEffectStartTime, componentEffectEndTime, componentEffectDuration, componentEffectErrors), null === finishedWork.alternate && null !== finishedWork.return && null !== finishedWork.return.alternate && .05 < componentEffectEndTime - componentEffectStartTime && (isHydratingParent(finishedWork.return.alternate, finishedWork.return) || logComponentTrigger(finishedWork, componentEffectStartTime, componentEffectEndTime, "Mount")));
			popComponentEffectStart(prevEffectStart);
			popComponentEffectDuration(prevEffectDuration);
			componentEffectErrors = prevEffectErrors;
			componentEffectSpawnedUpdate = prevEffectDidSpawnUpdate;
		}
		function commitReconciliationEffects(finishedWork) {
			var flags = finishedWork.flags;
			if (flags & 2) {
				try {
					runWithFiberInDEV(finishedWork, commitPlacement, finishedWork);
				} catch (error) {
					captureCommitPhaseError(finishedWork, finishedWork.return, error);
				}
				finishedWork.flags &= -3;
			}
			flags & 4096 && (finishedWork.flags &= -4097);
		}
		function recursivelyResetForms(parentFiber) {
			if (parentFiber.subtreeFlags & 1024) for (parentFiber = parentFiber.child; null !== parentFiber;) {
				var fiber = parentFiber;
				recursivelyResetForms(fiber);
				5 === fiber.tag && fiber.flags & 1024 && fiber.stateNode.reset();
				parentFiber = parentFiber.sibling;
			}
		}
		function recursivelyTraverseLayoutEffects(root, parentFiber) {
			if (parentFiber.subtreeFlags & 8772) for (parentFiber = parentFiber.child; null !== parentFiber;) commitLayoutEffectOnFiber(root, parentFiber.alternate, parentFiber), parentFiber = parentFiber.sibling;
		}
		function disappearLayoutEffects(finishedWork) {
			var prevEffectStart = pushComponentEffectStart(), prevEffectDuration = pushComponentEffectDuration(), prevEffectErrors = pushComponentEffectErrors(), prevEffectDidSpawnUpdate = pushComponentEffectDidSpawnUpdate();
			switch (finishedWork.tag) {
				case 0:
				case 11:
				case 14:
				case 15:
					commitHookLayoutUnmountEffects(finishedWork, finishedWork.return, Layout);
					recursivelyTraverseDisappearLayoutEffects(finishedWork);
					break;
				case 1:
					safelyDetachRef(finishedWork, finishedWork.return);
					var instance = finishedWork.stateNode;
					"function" === typeof instance.componentWillUnmount && safelyCallComponentWillUnmount(finishedWork, finishedWork.return, instance);
					recursivelyTraverseDisappearLayoutEffects(finishedWork);
					break;
				case 27: runWithFiberInDEV(finishedWork, releaseSingletonInstance, finishedWork.stateNode);
				case 26:
				case 5:
					safelyDetachRef(finishedWork, finishedWork.return);
					recursivelyTraverseDisappearLayoutEffects(finishedWork);
					break;
				case 22:
					null === finishedWork.memoizedState && recursivelyTraverseDisappearLayoutEffects(finishedWork);
					break;
				case 30:
					recursivelyTraverseDisappearLayoutEffects(finishedWork);
					break;
				default: recursivelyTraverseDisappearLayoutEffects(finishedWork);
			}
			(finishedWork.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && (componentEffectSpawnedUpdate || .05 < componentEffectDuration) && logComponentEffect(finishedWork, componentEffectStartTime, componentEffectEndTime, componentEffectDuration, componentEffectErrors);
			popComponentEffectStart(prevEffectStart);
			popComponentEffectDuration(prevEffectDuration);
			componentEffectErrors = prevEffectErrors;
			componentEffectSpawnedUpdate = prevEffectDidSpawnUpdate;
		}
		function recursivelyTraverseDisappearLayoutEffects(parentFiber) {
			for (parentFiber = parentFiber.child; null !== parentFiber;) disappearLayoutEffects(parentFiber), parentFiber = parentFiber.sibling;
		}
		function reappearLayoutEffects(finishedRoot, current, finishedWork, includeWorkInProgressEffects) {
			var prevEffectStart = pushComponentEffectStart(), prevEffectDuration = pushComponentEffectDuration(), prevEffectErrors = pushComponentEffectErrors(), prevEffectDidSpawnUpdate = pushComponentEffectDidSpawnUpdate(), flags = finishedWork.flags;
			switch (finishedWork.tag) {
				case 0:
				case 11:
				case 15:
					recursivelyTraverseReappearLayoutEffects(finishedRoot, finishedWork, includeWorkInProgressEffects);
					commitHookLayoutEffects(finishedWork, Layout);
					break;
				case 1:
					recursivelyTraverseReappearLayoutEffects(finishedRoot, finishedWork, includeWorkInProgressEffects);
					current = finishedWork.stateNode;
					"function" === typeof current.componentDidMount && runWithFiberInDEV(finishedWork, callComponentDidMountInDEV, finishedWork, current);
					current = finishedWork.updateQueue;
					if (null !== current) {
						finishedRoot = finishedWork.stateNode;
						try {
							runWithFiberInDEV(finishedWork, commitHiddenCallbacks, current, finishedRoot);
						} catch (error) {
							captureCommitPhaseError(finishedWork, finishedWork.return, error);
						}
					}
					includeWorkInProgressEffects && flags & 64 && commitClassCallbacks(finishedWork);
					safelyAttachRef(finishedWork, finishedWork.return);
					break;
				case 27: commitHostSingletonAcquisition(finishedWork);
				case 26:
				case 5:
					recursivelyTraverseReappearLayoutEffects(finishedRoot, finishedWork, includeWorkInProgressEffects);
					includeWorkInProgressEffects && null === current && flags & 4 && commitHostMount(finishedWork);
					safelyAttachRef(finishedWork, finishedWork.return);
					break;
				case 12:
					if (includeWorkInProgressEffects && flags & 4) {
						flags = pushNestedEffectDurations();
						recursivelyTraverseReappearLayoutEffects(finishedRoot, finishedWork, includeWorkInProgressEffects);
						includeWorkInProgressEffects = finishedWork.stateNode;
						includeWorkInProgressEffects.effectDuration += bubbleNestedEffectDurations(flags);
						try {
							runWithFiberInDEV(finishedWork, commitProfiler, finishedWork, current, commitStartTime, includeWorkInProgressEffects.effectDuration);
						} catch (error) {
							captureCommitPhaseError(finishedWork, finishedWork.return, error);
						}
					} else recursivelyTraverseReappearLayoutEffects(finishedRoot, finishedWork, includeWorkInProgressEffects);
					break;
				case 31:
					recursivelyTraverseReappearLayoutEffects(finishedRoot, finishedWork, includeWorkInProgressEffects);
					includeWorkInProgressEffects && flags & 4 && commitActivityHydrationCallbacks(finishedRoot, finishedWork);
					break;
				case 13:
					recursivelyTraverseReappearLayoutEffects(finishedRoot, finishedWork, includeWorkInProgressEffects);
					includeWorkInProgressEffects && flags & 4 && commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
					break;
				case 22:
					null === finishedWork.memoizedState && recursivelyTraverseReappearLayoutEffects(finishedRoot, finishedWork, includeWorkInProgressEffects);
					safelyAttachRef(finishedWork, finishedWork.return);
					break;
				case 30: break;
				default: recursivelyTraverseReappearLayoutEffects(finishedRoot, finishedWork, includeWorkInProgressEffects);
			}
			(finishedWork.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && (componentEffectSpawnedUpdate || .05 < componentEffectDuration) && logComponentEffect(finishedWork, componentEffectStartTime, componentEffectEndTime, componentEffectDuration, componentEffectErrors);
			popComponentEffectStart(prevEffectStart);
			popComponentEffectDuration(prevEffectDuration);
			componentEffectErrors = prevEffectErrors;
			componentEffectSpawnedUpdate = prevEffectDidSpawnUpdate;
		}
		function recursivelyTraverseReappearLayoutEffects(finishedRoot, parentFiber, includeWorkInProgressEffects) {
			includeWorkInProgressEffects = includeWorkInProgressEffects && 0 !== (parentFiber.subtreeFlags & 8772);
			for (parentFiber = parentFiber.child; null !== parentFiber;) reappearLayoutEffects(finishedRoot, parentFiber.alternate, parentFiber, includeWorkInProgressEffects), parentFiber = parentFiber.sibling;
		}
		function commitOffscreenPassiveMountEffects(current, finishedWork) {
			var previousCache = null;
			null !== current && null !== current.memoizedState && null !== current.memoizedState.cachePool && (previousCache = current.memoizedState.cachePool.pool);
			current = null;
			null !== finishedWork.memoizedState && null !== finishedWork.memoizedState.cachePool && (current = finishedWork.memoizedState.cachePool.pool);
			current !== previousCache && (null != current && retainCache(current), null != previousCache && releaseCache(previousCache));
		}
		function commitCachePassiveMountEffect(current, finishedWork) {
			current = null;
			null !== finishedWork.alternate && (current = finishedWork.alternate.memoizedState.cache);
			finishedWork = finishedWork.memoizedState.cache;
			finishedWork !== current && (retainCache(finishedWork), null != current && releaseCache(current));
		}
		function recursivelyTraversePassiveMountEffects(root, parentFiber, committedLanes, committedTransitions, endTime) {
			if (parentFiber.subtreeFlags & 10256 || 0 !== parentFiber.actualDuration && (null === parentFiber.alternate || parentFiber.alternate.child !== parentFiber.child)) for (parentFiber = parentFiber.child; null !== parentFiber;) {
				var nextSibling = parentFiber.sibling;
				commitPassiveMountOnFiber(root, parentFiber, committedLanes, committedTransitions, null !== nextSibling ? nextSibling.actualStartTime : endTime);
				parentFiber = nextSibling;
			}
		}
		function commitPassiveMountOnFiber(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime) {
			var prevEffectStart = pushComponentEffectStart(), prevEffectDuration = pushComponentEffectDuration(), prevEffectErrors = pushComponentEffectErrors(), prevEffectDidSpawnUpdate = pushComponentEffectDidSpawnUpdate(), prevDeepEquality = alreadyWarnedForDeepEquality, flags = finishedWork.flags;
			switch (finishedWork.tag) {
				case 0:
				case 11:
				case 15:
					(finishedWork.mode & ProfileMode) !== NoMode && 0 < finishedWork.actualStartTime && 0 !== (finishedWork.flags & 1) && logComponentRender(finishedWork, finishedWork.actualStartTime, endTime, inHydratedSubtree, committedLanes);
					recursivelyTraversePassiveMountEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime);
					flags & 2048 && commitHookPassiveMountEffects(finishedWork, Passive | HasEffect);
					break;
				case 1:
					(finishedWork.mode & ProfileMode) !== NoMode && 0 < finishedWork.actualStartTime && (0 !== (finishedWork.flags & 128) ? logComponentErrored(finishedWork, finishedWork.actualStartTime, endTime, []) : 0 !== (finishedWork.flags & 1) && logComponentRender(finishedWork, finishedWork.actualStartTime, endTime, inHydratedSubtree, committedLanes));
					recursivelyTraversePassiveMountEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime);
					break;
				case 3:
					var prevProfilerEffectDuration = pushNestedEffectDurations(), wasInHydratedSubtree = inHydratedSubtree;
					inHydratedSubtree = null !== finishedWork.alternate && finishedWork.alternate.memoizedState.isDehydrated && 0 === (finishedWork.flags & 256);
					recursivelyTraversePassiveMountEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime);
					inHydratedSubtree = wasInHydratedSubtree;
					flags & 2048 && (committedLanes = null, null !== finishedWork.alternate && (committedLanes = finishedWork.alternate.memoizedState.cache), committedTransitions = finishedWork.memoizedState.cache, committedTransitions !== committedLanes && (retainCache(committedTransitions), null != committedLanes && releaseCache(committedLanes)));
					finishedRoot.passiveEffectDuration += popNestedEffectDurations(prevProfilerEffectDuration);
					break;
				case 12:
					if (flags & 2048) {
						flags = pushNestedEffectDurations();
						recursivelyTraversePassiveMountEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime);
						finishedRoot = finishedWork.stateNode;
						finishedRoot.passiveEffectDuration += bubbleNestedEffectDurations(flags);
						try {
							runWithFiberInDEV(finishedWork, commitProfilerPostCommitImpl, finishedWork, finishedWork.alternate, commitStartTime, finishedRoot.passiveEffectDuration);
						} catch (error) {
							captureCommitPhaseError(finishedWork, finishedWork.return, error);
						}
					} else recursivelyTraversePassiveMountEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime);
					break;
				case 31:
					flags = inHydratedSubtree;
					prevProfilerEffectDuration = null !== finishedWork.alternate ? finishedWork.alternate.memoizedState : null;
					wasInHydratedSubtree = finishedWork.memoizedState;
					null !== prevProfilerEffectDuration && null === wasInHydratedSubtree ? (wasInHydratedSubtree = finishedWork.deletions, null !== wasInHydratedSubtree && 0 < wasInHydratedSubtree.length && 18 === wasInHydratedSubtree[0].tag ? (inHydratedSubtree = !1, prevProfilerEffectDuration = prevProfilerEffectDuration.hydrationErrors, null !== prevProfilerEffectDuration && logComponentErrored(finishedWork, finishedWork.actualStartTime, endTime, prevProfilerEffectDuration)) : inHydratedSubtree = !0) : inHydratedSubtree = !1;
					recursivelyTraversePassiveMountEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime);
					inHydratedSubtree = flags;
					break;
				case 13:
					flags = inHydratedSubtree;
					prevProfilerEffectDuration = null !== finishedWork.alternate ? finishedWork.alternate.memoizedState : null;
					wasInHydratedSubtree = finishedWork.memoizedState;
					null === prevProfilerEffectDuration || null === prevProfilerEffectDuration.dehydrated || null !== wasInHydratedSubtree && null !== wasInHydratedSubtree.dehydrated ? inHydratedSubtree = !1 : (wasInHydratedSubtree = finishedWork.deletions, null !== wasInHydratedSubtree && 0 < wasInHydratedSubtree.length && 18 === wasInHydratedSubtree[0].tag ? (inHydratedSubtree = !1, prevProfilerEffectDuration = prevProfilerEffectDuration.hydrationErrors, null !== prevProfilerEffectDuration && logComponentErrored(finishedWork, finishedWork.actualStartTime, endTime, prevProfilerEffectDuration)) : inHydratedSubtree = !0);
					recursivelyTraversePassiveMountEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime);
					inHydratedSubtree = flags;
					break;
				case 23: break;
				case 22:
					wasInHydratedSubtree = finishedWork.stateNode;
					prevProfilerEffectDuration = finishedWork.alternate;
					null !== finishedWork.memoizedState ? wasInHydratedSubtree._visibility & OffscreenPassiveEffectsConnected ? recursivelyTraversePassiveMountEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime) : recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime) : wasInHydratedSubtree._visibility & OffscreenPassiveEffectsConnected ? recursivelyTraversePassiveMountEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime) : (wasInHydratedSubtree._visibility |= OffscreenPassiveEffectsConnected, recursivelyTraverseReconnectPassiveEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, 0 !== (finishedWork.subtreeFlags & 10256) || 0 !== finishedWork.actualDuration && (null === finishedWork.alternate || finishedWork.alternate.child !== finishedWork.child), endTime), (finishedWork.mode & ProfileMode) === NoMode || inHydratedSubtree || (finishedRoot = finishedWork.actualStartTime, 0 <= finishedRoot && .05 < endTime - finishedRoot && logComponentReappeared(finishedWork, finishedRoot, endTime), 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && .05 < componentEffectEndTime - componentEffectStartTime && logComponentReappeared(finishedWork, componentEffectStartTime, componentEffectEndTime)));
					flags & 2048 && commitOffscreenPassiveMountEffects(prevProfilerEffectDuration, finishedWork);
					break;
				case 24:
					recursivelyTraversePassiveMountEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime);
					flags & 2048 && commitCachePassiveMountEffect(finishedWork.alternate, finishedWork);
					break;
				default: recursivelyTraversePassiveMountEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime);
			}
			if ((finishedWork.mode & ProfileMode) !== NoMode) {
				if (finishedRoot = !inHydratedSubtree && null === finishedWork.alternate && null !== finishedWork.return && null !== finishedWork.return.alternate) committedLanes = finishedWork.actualStartTime, 0 <= committedLanes && .05 < endTime - committedLanes && logComponentTrigger(finishedWork, committedLanes, endTime, "Mount");
				0 <= componentEffectStartTime && 0 <= componentEffectEndTime && ((componentEffectSpawnedUpdate || .05 < componentEffectDuration) && logComponentEffect(finishedWork, componentEffectStartTime, componentEffectEndTime, componentEffectDuration, componentEffectErrors), finishedRoot && .05 < componentEffectEndTime - componentEffectStartTime && logComponentTrigger(finishedWork, componentEffectStartTime, componentEffectEndTime, "Mount"));
			}
			popComponentEffectStart(prevEffectStart);
			popComponentEffectDuration(prevEffectDuration);
			componentEffectErrors = prevEffectErrors;
			componentEffectSpawnedUpdate = prevEffectDidSpawnUpdate;
			alreadyWarnedForDeepEquality = prevDeepEquality;
		}
		function recursivelyTraverseReconnectPassiveEffects(finishedRoot, parentFiber, committedLanes, committedTransitions, includeWorkInProgressEffects, endTime) {
			includeWorkInProgressEffects = includeWorkInProgressEffects && (0 !== (parentFiber.subtreeFlags & 10256) || 0 !== parentFiber.actualDuration && (null === parentFiber.alternate || parentFiber.alternate.child !== parentFiber.child));
			for (parentFiber = parentFiber.child; null !== parentFiber;) {
				var nextSibling = parentFiber.sibling;
				reconnectPassiveEffects(finishedRoot, parentFiber, committedLanes, committedTransitions, includeWorkInProgressEffects, null !== nextSibling ? nextSibling.actualStartTime : endTime);
				parentFiber = nextSibling;
			}
		}
		function reconnectPassiveEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, includeWorkInProgressEffects, endTime) {
			var prevEffectStart = pushComponentEffectStart(), prevEffectDuration = pushComponentEffectDuration(), prevEffectErrors = pushComponentEffectErrors(), prevEffectDidSpawnUpdate = pushComponentEffectDidSpawnUpdate(), prevDeepEquality = alreadyWarnedForDeepEquality;
			includeWorkInProgressEffects && (finishedWork.mode & ProfileMode) !== NoMode && 0 < finishedWork.actualStartTime && 0 !== (finishedWork.flags & 1) && logComponentRender(finishedWork, finishedWork.actualStartTime, endTime, inHydratedSubtree, committedLanes);
			var flags = finishedWork.flags;
			switch (finishedWork.tag) {
				case 0:
				case 11:
				case 15:
					recursivelyTraverseReconnectPassiveEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, includeWorkInProgressEffects, endTime);
					commitHookPassiveMountEffects(finishedWork, Passive);
					break;
				case 23: break;
				case 22:
					var _instance2 = finishedWork.stateNode;
					null !== finishedWork.memoizedState ? _instance2._visibility & OffscreenPassiveEffectsConnected ? recursivelyTraverseReconnectPassiveEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, includeWorkInProgressEffects, endTime) : recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, endTime) : (_instance2._visibility |= OffscreenPassiveEffectsConnected, recursivelyTraverseReconnectPassiveEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, includeWorkInProgressEffects, endTime));
					includeWorkInProgressEffects && flags & 2048 && commitOffscreenPassiveMountEffects(finishedWork.alternate, finishedWork);
					break;
				case 24:
					recursivelyTraverseReconnectPassiveEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, includeWorkInProgressEffects, endTime);
					includeWorkInProgressEffects && flags & 2048 && commitCachePassiveMountEffect(finishedWork.alternate, finishedWork);
					break;
				default: recursivelyTraverseReconnectPassiveEffects(finishedRoot, finishedWork, committedLanes, committedTransitions, includeWorkInProgressEffects, endTime);
			}
			(finishedWork.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && (componentEffectSpawnedUpdate || .05 < componentEffectDuration) && logComponentEffect(finishedWork, componentEffectStartTime, componentEffectEndTime, componentEffectDuration, componentEffectErrors);
			popComponentEffectStart(prevEffectStart);
			popComponentEffectDuration(prevEffectDuration);
			componentEffectErrors = prevEffectErrors;
			componentEffectSpawnedUpdate = prevEffectDidSpawnUpdate;
			alreadyWarnedForDeepEquality = prevDeepEquality;
		}
		function recursivelyTraverseAtomicPassiveEffects(finishedRoot$jscomp$0, parentFiber, committedLanes$jscomp$0, committedTransitions$jscomp$0, endTime$jscomp$0) {
			if (parentFiber.subtreeFlags & 10256 || 0 !== parentFiber.actualDuration && (null === parentFiber.alternate || parentFiber.alternate.child !== parentFiber.child)) for (var child = parentFiber.child; null !== child;) {
				parentFiber = child.sibling;
				var finishedRoot = finishedRoot$jscomp$0, committedLanes = committedLanes$jscomp$0, committedTransitions = committedTransitions$jscomp$0, endTime = null !== parentFiber ? parentFiber.actualStartTime : endTime$jscomp$0, prevDeepEquality = alreadyWarnedForDeepEquality;
				(child.mode & ProfileMode) !== NoMode && 0 < child.actualStartTime && 0 !== (child.flags & 1) && logComponentRender(child, child.actualStartTime, endTime, inHydratedSubtree, committedLanes);
				var flags = child.flags;
				switch (child.tag) {
					case 22:
						recursivelyTraverseAtomicPassiveEffects(finishedRoot, child, committedLanes, committedTransitions, endTime);
						flags & 2048 && commitOffscreenPassiveMountEffects(child.alternate, child);
						break;
					case 24:
						recursivelyTraverseAtomicPassiveEffects(finishedRoot, child, committedLanes, committedTransitions, endTime);
						flags & 2048 && commitCachePassiveMountEffect(child.alternate, child);
						break;
					default: recursivelyTraverseAtomicPassiveEffects(finishedRoot, child, committedLanes, committedTransitions, endTime);
				}
				alreadyWarnedForDeepEquality = prevDeepEquality;
				child = parentFiber;
			}
		}
		function recursivelyAccumulateSuspenseyCommit(parentFiber, committedLanes, suspendedState) {
			if (parentFiber.subtreeFlags & suspenseyCommitFlag) for (parentFiber = parentFiber.child; null !== parentFiber;) accumulateSuspenseyCommitOnFiber(parentFiber, committedLanes, suspendedState), parentFiber = parentFiber.sibling;
		}
		function accumulateSuspenseyCommitOnFiber(fiber, committedLanes, suspendedState) {
			switch (fiber.tag) {
				case 26:
					recursivelyAccumulateSuspenseyCommit(fiber, committedLanes, suspendedState);
					fiber.flags & suspenseyCommitFlag && null !== fiber.memoizedState && suspendResource(suspendedState, currentHoistableRoot, fiber.memoizedState, fiber.memoizedProps);
					break;
				case 5:
					recursivelyAccumulateSuspenseyCommit(fiber, committedLanes, suspendedState);
					break;
				case 3:
				case 4:
					var previousHoistableRoot = currentHoistableRoot;
					currentHoistableRoot = getHoistableRoot(fiber.stateNode.containerInfo);
					recursivelyAccumulateSuspenseyCommit(fiber, committedLanes, suspendedState);
					currentHoistableRoot = previousHoistableRoot;
					break;
				case 22:
					null === fiber.memoizedState && (previousHoistableRoot = fiber.alternate, null !== previousHoistableRoot && null !== previousHoistableRoot.memoizedState ? (previousHoistableRoot = suspenseyCommitFlag, suspenseyCommitFlag = 16777216, recursivelyAccumulateSuspenseyCommit(fiber, committedLanes, suspendedState), suspenseyCommitFlag = previousHoistableRoot) : recursivelyAccumulateSuspenseyCommit(fiber, committedLanes, suspendedState));
					break;
				default: recursivelyAccumulateSuspenseyCommit(fiber, committedLanes, suspendedState);
			}
		}
		function detachAlternateSiblings(parentFiber) {
			var previousFiber = parentFiber.alternate;
			if (null !== previousFiber && (parentFiber = previousFiber.child, null !== parentFiber)) {
				previousFiber.child = null;
				do
					previousFiber = parentFiber.sibling, parentFiber.sibling = null, parentFiber = previousFiber;
				while (null !== parentFiber);
			}
		}
		function recursivelyTraversePassiveUnmountEffects(parentFiber) {
			var deletions = parentFiber.deletions;
			if (0 !== (parentFiber.flags & 16)) {
				if (null !== deletions) for (var i = 0; i < deletions.length; i++) {
					var childToDelete = deletions[i], prevEffectStart = pushComponentEffectStart();
					nextEffect = childToDelete;
					commitPassiveUnmountEffectsInsideOfDeletedTree_begin(childToDelete, parentFiber);
					(childToDelete.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && .05 < componentEffectEndTime - componentEffectStartTime && logComponentTrigger(childToDelete, componentEffectStartTime, componentEffectEndTime, "Unmount");
					popComponentEffectStart(prevEffectStart);
				}
				detachAlternateSiblings(parentFiber);
			}
			if (parentFiber.subtreeFlags & 10256) for (parentFiber = parentFiber.child; null !== parentFiber;) commitPassiveUnmountOnFiber(parentFiber), parentFiber = parentFiber.sibling;
		}
		function commitPassiveUnmountOnFiber(finishedWork) {
			var prevEffectStart = pushComponentEffectStart(), prevEffectDuration = pushComponentEffectDuration(), prevEffectErrors = pushComponentEffectErrors(), prevEffectDidSpawnUpdate = pushComponentEffectDidSpawnUpdate();
			switch (finishedWork.tag) {
				case 0:
				case 11:
				case 15:
					recursivelyTraversePassiveUnmountEffects(finishedWork);
					finishedWork.flags & 2048 && commitHookPassiveUnmountEffects(finishedWork, finishedWork.return, Passive | HasEffect);
					break;
				case 3:
					var prevProfilerEffectDuration = pushNestedEffectDurations();
					recursivelyTraversePassiveUnmountEffects(finishedWork);
					finishedWork.stateNode.passiveEffectDuration += popNestedEffectDurations(prevProfilerEffectDuration);
					break;
				case 12:
					prevProfilerEffectDuration = pushNestedEffectDurations();
					recursivelyTraversePassiveUnmountEffects(finishedWork);
					finishedWork.stateNode.passiveEffectDuration += bubbleNestedEffectDurations(prevProfilerEffectDuration);
					break;
				case 22:
					prevProfilerEffectDuration = finishedWork.stateNode;
					null !== finishedWork.memoizedState && prevProfilerEffectDuration._visibility & OffscreenPassiveEffectsConnected && (null === finishedWork.return || 13 !== finishedWork.return.tag) ? (prevProfilerEffectDuration._visibility &= ~OffscreenPassiveEffectsConnected, recursivelyTraverseDisconnectPassiveEffects(finishedWork), (finishedWork.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && .05 < componentEffectEndTime - componentEffectStartTime && logComponentTrigger(finishedWork, componentEffectStartTime, componentEffectEndTime, "Disconnect")) : recursivelyTraversePassiveUnmountEffects(finishedWork);
					break;
				default: recursivelyTraversePassiveUnmountEffects(finishedWork);
			}
			(finishedWork.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && (componentEffectSpawnedUpdate || .05 < componentEffectDuration) && logComponentEffect(finishedWork, componentEffectStartTime, componentEffectEndTime, componentEffectDuration, componentEffectErrors);
			popComponentEffectStart(prevEffectStart);
			popComponentEffectDuration(prevEffectDuration);
			componentEffectSpawnedUpdate = prevEffectDidSpawnUpdate;
			componentEffectErrors = prevEffectErrors;
		}
		function recursivelyTraverseDisconnectPassiveEffects(parentFiber) {
			var deletions = parentFiber.deletions;
			if (0 !== (parentFiber.flags & 16)) {
				if (null !== deletions) for (var i = 0; i < deletions.length; i++) {
					var childToDelete = deletions[i], prevEffectStart = pushComponentEffectStart();
					nextEffect = childToDelete;
					commitPassiveUnmountEffectsInsideOfDeletedTree_begin(childToDelete, parentFiber);
					(childToDelete.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && .05 < componentEffectEndTime - componentEffectStartTime && logComponentTrigger(childToDelete, componentEffectStartTime, componentEffectEndTime, "Unmount");
					popComponentEffectStart(prevEffectStart);
				}
				detachAlternateSiblings(parentFiber);
			}
			for (parentFiber = parentFiber.child; null !== parentFiber;) disconnectPassiveEffect(parentFiber), parentFiber = parentFiber.sibling;
		}
		function disconnectPassiveEffect(finishedWork) {
			var prevEffectStart = pushComponentEffectStart(), prevEffectDuration = pushComponentEffectDuration(), prevEffectErrors = pushComponentEffectErrors(), prevEffectDidSpawnUpdate = pushComponentEffectDidSpawnUpdate();
			switch (finishedWork.tag) {
				case 0:
				case 11:
				case 15:
					commitHookPassiveUnmountEffects(finishedWork, finishedWork.return, Passive);
					recursivelyTraverseDisconnectPassiveEffects(finishedWork);
					break;
				case 22:
					var instance = finishedWork.stateNode;
					instance._visibility & OffscreenPassiveEffectsConnected && (instance._visibility &= ~OffscreenPassiveEffectsConnected, recursivelyTraverseDisconnectPassiveEffects(finishedWork));
					break;
				default: recursivelyTraverseDisconnectPassiveEffects(finishedWork);
			}
			(finishedWork.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && (componentEffectSpawnedUpdate || .05 < componentEffectDuration) && logComponentEffect(finishedWork, componentEffectStartTime, componentEffectEndTime, componentEffectDuration, componentEffectErrors);
			popComponentEffectStart(prevEffectStart);
			popComponentEffectDuration(prevEffectDuration);
			componentEffectSpawnedUpdate = prevEffectDidSpawnUpdate;
			componentEffectErrors = prevEffectErrors;
		}
		function commitPassiveUnmountEffectsInsideOfDeletedTree_begin(deletedSubtreeRoot, nearestMountedAncestor$jscomp$0) {
			for (; null !== nextEffect;) {
				var fiber = nextEffect, current = fiber, nearestMountedAncestor = nearestMountedAncestor$jscomp$0, prevEffectStart = pushComponentEffectStart(), prevEffectDuration = pushComponentEffectDuration(), prevEffectErrors = pushComponentEffectErrors(), prevEffectDidSpawnUpdate = pushComponentEffectDidSpawnUpdate();
				switch (current.tag) {
					case 0:
					case 11:
					case 15:
						commitHookPassiveUnmountEffects(current, nearestMountedAncestor, Passive);
						break;
					case 23:
					case 22:
						null !== current.memoizedState && null !== current.memoizedState.cachePool && (nearestMountedAncestor = current.memoizedState.cachePool.pool, null != nearestMountedAncestor && retainCache(nearestMountedAncestor));
						break;
					case 24: releaseCache(current.memoizedState.cache);
				}
				(current.mode & ProfileMode) !== NoMode && 0 <= componentEffectStartTime && 0 <= componentEffectEndTime && (componentEffectSpawnedUpdate || .05 < componentEffectDuration) && logComponentEffect(current, componentEffectStartTime, componentEffectEndTime, componentEffectDuration, componentEffectErrors);
				popComponentEffectStart(prevEffectStart);
				popComponentEffectDuration(prevEffectDuration);
				componentEffectSpawnedUpdate = prevEffectDidSpawnUpdate;
				componentEffectErrors = prevEffectErrors;
				current = fiber.child;
				if (null !== current) current.return = fiber, nextEffect = current;
				else a: for (fiber = deletedSubtreeRoot; null !== nextEffect;) {
					current = nextEffect;
					prevEffectStart = current.sibling;
					prevEffectDuration = current.return;
					detachFiberAfterEffects(current);
					if (current === fiber) {
						nextEffect = null;
						break a;
					}
					if (null !== prevEffectStart) {
						prevEffectStart.return = prevEffectDuration;
						nextEffect = prevEffectStart;
						break a;
					}
					nextEffect = prevEffectDuration;
				}
			}
		}
		function onCommitRoot() {
			commitHooks.forEach(function(commitHook) {
				return commitHook();
			});
		}
		function isConcurrentActEnvironment() {
			var isReactActEnvironmentGlobal = "undefined" !== typeof IS_REACT_ACT_ENVIRONMENT ? IS_REACT_ACT_ENVIRONMENT : void 0;
			isReactActEnvironmentGlobal || null === ReactSharedInternals.actQueue || console.error("The current testing environment is not configured to support act(...)");
			return isReactActEnvironmentGlobal;
		}
		function requestUpdateLane(fiber) {
			if ((executionContext & RenderContext) !== NoContext && 0 !== workInProgressRootRenderLanes) return workInProgressRootRenderLanes & -workInProgressRootRenderLanes;
			var transition = ReactSharedInternals.T;
			return null !== transition ? (transition._updatedFibers || (transition._updatedFibers = /* @__PURE__ */ new Set()), transition._updatedFibers.add(fiber), requestTransitionLane()) : resolveUpdatePriority();
		}
		function requestDeferredLane() {
			if (0 === workInProgressDeferredLane) if (0 === (workInProgressRootRenderLanes & 536870912) || isHydrating) {
				var lane = nextTransitionDeferredLane;
				nextTransitionDeferredLane <<= 1;
				0 === (nextTransitionDeferredLane & 3932160) && (nextTransitionDeferredLane = 262144);
				workInProgressDeferredLane = lane;
			} else workInProgressDeferredLane = 536870912;
			lane = suspenseHandlerStackCursor.current;
			null !== lane && (lane.flags |= 32);
			return workInProgressDeferredLane;
		}
		function scheduleUpdateOnFiber(root, fiber, lane) {
			isRunningInsertionEffect && console.error("useInsertionEffect must not schedule updates.");
			isFlushingPassiveEffects && (didScheduleUpdateDuringPassiveEffects = !0);
			if (root === workInProgressRoot && (workInProgressSuspendedReason === SuspendedOnData || workInProgressSuspendedReason === SuspendedOnAction) || null !== root.cancelPendingCommit) prepareFreshStack(root, 0), markRootSuspended(root, workInProgressRootRenderLanes, workInProgressDeferredLane, !1);
			markRootUpdated$1(root, lane);
			if ((executionContext & RenderContext) !== NoContext && root === workInProgressRoot) {
				if (isRendering) switch (fiber.tag) {
					case 0:
					case 11:
					case 15:
						root = workInProgress && getComponentNameFromFiber(workInProgress) || "Unknown";
						didWarnAboutUpdateInRenderForAnotherComponent.has(root) || (didWarnAboutUpdateInRenderForAnotherComponent.add(root), fiber = getComponentNameFromFiber(fiber) || "Unknown", console.error("Cannot update a component (`%s`) while rendering a different component (`%s`). To locate the bad setState() call inside `%s`, follow the stack trace as described in https://react.dev/link/setstate-in-render", fiber, root, root));
						break;
					case 1: didWarnAboutUpdateInRender || (console.error("Cannot update during an existing state transition (such as within `render`). Render methods should be a pure function of props and state."), didWarnAboutUpdateInRender = !0);
				}
			} else isDevToolsPresent && addFiberToLanesMap(root, fiber, lane), warnIfUpdatesNotWrappedWithActDEV(fiber), root === workInProgressRoot && ((executionContext & RenderContext) === NoContext && (workInProgressRootInterleavedUpdatedLanes |= lane), workInProgressRootExitStatus === RootSuspendedWithDelay && markRootSuspended(root, workInProgressRootRenderLanes, workInProgressDeferredLane, !1)), ensureRootIsScheduled(root);
		}
		function performWorkOnRoot(root, lanes, forceSync) {
			if ((executionContext & (RenderContext | CommitContext)) !== NoContext) throw Error("Should not already be working.");
			if (0 !== workInProgressRootRenderLanes && null !== workInProgress) {
				var yieldedFiber = workInProgress, yieldEndTime = now$1();
				switch (yieldReason) {
					case SuspendedOnImmediate:
					case SuspendedOnData:
						var startTime = yieldStartTime;
						supportsUserTiming && ((yieldedFiber = yieldedFiber._debugTask) ? yieldedFiber.run(console.timeStamp.bind(console, "Suspended", startTime, yieldEndTime, COMPONENTS_TRACK, void 0, "primary-light")) : console.timeStamp("Suspended", startTime, yieldEndTime, COMPONENTS_TRACK, void 0, "primary-light"));
						break;
					case SuspendedOnAction:
						startTime = yieldStartTime;
						supportsUserTiming && ((yieldedFiber = yieldedFiber._debugTask) ? yieldedFiber.run(console.timeStamp.bind(console, "Action", startTime, yieldEndTime, COMPONENTS_TRACK, void 0, "primary-light")) : console.timeStamp("Action", startTime, yieldEndTime, COMPONENTS_TRACK, void 0, "primary-light"));
						break;
					default: supportsUserTiming && (yieldedFiber = yieldEndTime - yieldStartTime, 3 > yieldedFiber || console.timeStamp("Blocked", yieldStartTime, yieldEndTime, COMPONENTS_TRACK, void 0, 5 > yieldedFiber ? "primary-light" : 10 > yieldedFiber ? "primary" : 100 > yieldedFiber ? "primary-dark" : "error"));
				}
			}
			startTime = (forceSync = !forceSync && 0 === (lanes & 127) && 0 === (lanes & root.expiredLanes) || checkIfRootIsPrerendering(root, lanes)) ? renderRootConcurrent(root, lanes) : renderRootSync(root, lanes, !0);
			var renderWasConcurrent = forceSync;
			do {
				if (startTime === RootInProgress) {
					workInProgressRootIsPrerendering && !forceSync && markRootSuspended(root, lanes, 0, !1);
					lanes = workInProgressSuspendedReason;
					yieldStartTime = now();
					yieldReason = lanes;
					break;
				} else {
					yieldedFiber = now$1();
					yieldEndTime = root.current.alternate;
					if (renderWasConcurrent && !isRenderConsistentWithExternalStores(yieldEndTime)) {
						setCurrentTrackFromLanes(lanes);
						yieldEndTime = renderStartTime;
						startTime = yieldedFiber;
						!supportsUserTiming || startTime <= yieldEndTime || (workInProgressUpdateTask ? workInProgressUpdateTask.run(console.timeStamp.bind(console, "Teared Render", yieldEndTime, startTime, currentTrack, LANES_TRACK_GROUP, "error")) : console.timeStamp("Teared Render", yieldEndTime, startTime, currentTrack, LANES_TRACK_GROUP, "error"));
						finalizeRender(lanes, yieldedFiber);
						startTime = renderRootSync(root, lanes, !1);
						renderWasConcurrent = !1;
						continue;
					}
					if (startTime === RootErrored) {
						renderWasConcurrent = lanes;
						if (root.errorRecoveryDisabledLanes & renderWasConcurrent) var errorRetryLanes = 0;
						else errorRetryLanes = root.pendingLanes & -536870913, errorRetryLanes = 0 !== errorRetryLanes ? errorRetryLanes : errorRetryLanes & 536870912 ? 536870912 : 0;
						if (0 !== errorRetryLanes) {
							setCurrentTrackFromLanes(lanes);
							logErroredRenderPhase(renderStartTime, yieldedFiber, lanes, workInProgressUpdateTask);
							finalizeRender(lanes, yieldedFiber);
							lanes = errorRetryLanes;
							a: {
								yieldedFiber = root;
								startTime = renderWasConcurrent;
								renderWasConcurrent = workInProgressRootConcurrentErrors;
								var wasRootDehydrated = yieldedFiber.current.memoizedState.isDehydrated;
								wasRootDehydrated && (prepareFreshStack(yieldedFiber, errorRetryLanes).flags |= 256);
								errorRetryLanes = renderRootSync(yieldedFiber, errorRetryLanes, !1);
								if (errorRetryLanes !== RootErrored) {
									if (workInProgressRootDidAttachPingListener && !wasRootDehydrated) {
										yieldedFiber.errorRecoveryDisabledLanes |= startTime;
										workInProgressRootInterleavedUpdatedLanes |= startTime;
										startTime = RootSuspendedWithDelay;
										break a;
									}
									yieldedFiber = workInProgressRootRecoverableErrors;
									workInProgressRootRecoverableErrors = renderWasConcurrent;
									null !== yieldedFiber && (null === workInProgressRootRecoverableErrors ? workInProgressRootRecoverableErrors = yieldedFiber : workInProgressRootRecoverableErrors.push.apply(workInProgressRootRecoverableErrors, yieldedFiber));
								}
								startTime = errorRetryLanes;
							}
							renderWasConcurrent = !1;
							if (startTime !== RootErrored) continue;
							else yieldedFiber = now$1();
						}
					}
					if (startTime === RootFatalErrored) {
						setCurrentTrackFromLanes(lanes);
						logErroredRenderPhase(renderStartTime, yieldedFiber, lanes, workInProgressUpdateTask);
						finalizeRender(lanes, yieldedFiber);
						prepareFreshStack(root, 0);
						markRootSuspended(root, lanes, 0, !0);
						break;
					}
					a: {
						forceSync = root;
						switch (startTime) {
							case RootInProgress:
							case RootFatalErrored: throw Error("Root did not complete. This is a bug in React.");
							case RootSuspendedWithDelay: if ((lanes & 4194048) !== lanes) break;
							case RootSuspendedAtTheShell:
								setCurrentTrackFromLanes(lanes);
								logSuspendedRenderPhase(renderStartTime, yieldedFiber, lanes, workInProgressUpdateTask);
								finalizeRender(lanes, yieldedFiber);
								yieldEndTime = lanes;
								0 !== (yieldEndTime & 127) ? blockingSuspendedTime = yieldedFiber : 0 !== (yieldEndTime & 4194048) && (transitionSuspendedTime = yieldedFiber);
								markRootSuspended(forceSync, lanes, workInProgressDeferredLane, !workInProgressRootDidSkipSuspendedSiblings);
								break a;
							case RootErrored:
								workInProgressRootRecoverableErrors = null;
								break;
							case RootSuspended:
							case RootCompleted: break;
							default: throw Error("Unknown root exit status.");
						}
						if (null !== ReactSharedInternals.actQueue) commitRoot(forceSync, yieldEndTime, lanes, workInProgressRootRecoverableErrors, workInProgressTransitions, workInProgressRootDidIncludeRecursiveRenderUpdate, workInProgressDeferredLane, workInProgressRootInterleavedUpdatedLanes, workInProgressSuspendedRetryLanes, startTime, null, null, renderStartTime, yieldedFiber);
						else {
							if ((lanes & 62914560) === lanes && (renderWasConcurrent = globalMostRecentFallbackTime + FALLBACK_THROTTLE_MS - now$1(), 10 < renderWasConcurrent)) {
								markRootSuspended(forceSync, lanes, workInProgressDeferredLane, !workInProgressRootDidSkipSuspendedSiblings);
								if (0 !== getNextLanes(forceSync, 0, !0)) break a;
								pendingEffectsLanes = lanes;
								forceSync.timeoutHandle = scheduleTimeout(commitRootWhenReady.bind(null, forceSync, yieldEndTime, workInProgressRootRecoverableErrors, workInProgressTransitions, workInProgressRootDidIncludeRecursiveRenderUpdate, lanes, workInProgressDeferredLane, workInProgressRootInterleavedUpdatedLanes, workInProgressSuspendedRetryLanes, workInProgressRootDidSkipSuspendedSiblings, startTime, "Throttled", renderStartTime, yieldedFiber), renderWasConcurrent);
								break a;
							}
							commitRootWhenReady(forceSync, yieldEndTime, workInProgressRootRecoverableErrors, workInProgressTransitions, workInProgressRootDidIncludeRecursiveRenderUpdate, lanes, workInProgressDeferredLane, workInProgressRootInterleavedUpdatedLanes, workInProgressSuspendedRetryLanes, workInProgressRootDidSkipSuspendedSiblings, startTime, null, renderStartTime, yieldedFiber);
						}
					}
				}
				break;
			} while (1);
			ensureRootIsScheduled(root);
		}
		function commitRootWhenReady(root, finishedWork, recoverableErrors, transitions, didIncludeRenderPhaseUpdate, lanes, spawnedLane, updatedLanes, suspendedRetryLanes, didSkipSuspendedSiblings, exitStatus, suspendedCommitReason, completedRenderStartTime, completedRenderEndTime) {
			root.timeoutHandle = noTimeout;
			var subtreeFlags = finishedWork.subtreeFlags, suspendedState = null;
			if (subtreeFlags & 8192 || 16785408 === (subtreeFlags & 16785408)) {
				if (suspendedState = {
					stylesheets: null,
					count: 0,
					imgCount: 0,
					imgBytes: 0,
					suspenseyImages: [],
					waitingForImages: !0,
					waitingForViewTransition: !1,
					unsuspend: noop$1
				}, accumulateSuspenseyCommitOnFiber(finishedWork, lanes, suspendedState), subtreeFlags = (lanes & 62914560) === lanes ? globalMostRecentFallbackTime - now$1() : (lanes & 4194048) === lanes ? globalMostRecentTransitionTime - now$1() : 0, subtreeFlags = waitForCommitToBeReady(suspendedState, subtreeFlags), null !== subtreeFlags) {
					pendingEffectsLanes = lanes;
					root.cancelPendingCommit = subtreeFlags(commitRoot.bind(null, root, finishedWork, lanes, recoverableErrors, transitions, didIncludeRenderPhaseUpdate, spawnedLane, updatedLanes, suspendedRetryLanes, exitStatus, suspendedState, suspendedState.waitingForViewTransition ? "Waiting for the previous Animation" : 0 < suspendedState.count ? 0 < suspendedState.imgCount ? "Suspended on CSS and Images" : "Suspended on CSS" : 1 === suspendedState.imgCount ? "Suspended on an Image" : 0 < suspendedState.imgCount ? "Suspended on Images" : null, completedRenderStartTime, completedRenderEndTime));
					markRootSuspended(root, lanes, spawnedLane, !didSkipSuspendedSiblings);
					return;
				}
			}
			commitRoot(root, finishedWork, lanes, recoverableErrors, transitions, didIncludeRenderPhaseUpdate, spawnedLane, updatedLanes, suspendedRetryLanes, exitStatus, suspendedState, suspendedCommitReason, completedRenderStartTime, completedRenderEndTime);
		}
		function isRenderConsistentWithExternalStores(finishedWork) {
			for (var node = finishedWork;;) {
				var tag = node.tag;
				if ((0 === tag || 11 === tag || 15 === tag) && node.flags & 16384 && (tag = node.updateQueue, null !== tag && (tag = tag.stores, null !== tag))) for (var i = 0; i < tag.length; i++) {
					var check = tag[i], getSnapshot = check.getSnapshot;
					check = check.value;
					try {
						if (!objectIs(getSnapshot(), check)) return !1;
					} catch (error) {
						return !1;
					}
				}
				tag = node.child;
				if (node.subtreeFlags & 16384 && null !== tag) tag.return = node, node = tag;
				else {
					if (node === finishedWork) break;
					for (; null === node.sibling;) {
						if (null === node.return || node.return === finishedWork) return !0;
						node = node.return;
					}
					node.sibling.return = node.return;
					node = node.sibling;
				}
			}
			return !0;
		}
		function markRootSuspended(root, suspendedLanes, spawnedLane, didAttemptEntireTree) {
			suspendedLanes &= ~workInProgressRootPingedLanes;
			suspendedLanes &= ~workInProgressRootInterleavedUpdatedLanes;
			root.suspendedLanes |= suspendedLanes;
			root.pingedLanes &= ~suspendedLanes;
			didAttemptEntireTree && (root.warmLanes |= suspendedLanes);
			didAttemptEntireTree = root.expirationTimes;
			for (var lanes = suspendedLanes; 0 < lanes;) {
				var index = 31 - clz32(lanes), lane = 1 << index;
				didAttemptEntireTree[index] = -1;
				lanes &= ~lane;
			}
			0 !== spawnedLane && markSpawnedDeferredLane(root, spawnedLane, suspendedLanes);
		}
		function flushSyncWork$1() {
			return (executionContext & (RenderContext | CommitContext)) === NoContext ? (flushSyncWorkAcrossRoots_impl(0, !1), !1) : !0;
		}
		function resetWorkInProgressStack() {
			if (null !== workInProgress) {
				if (workInProgressSuspendedReason === NotSuspended) var interruptedWork = workInProgress.return;
				else interruptedWork = workInProgress, resetContextDependencies(), resetHooksOnUnwind(interruptedWork), thenableState$1 = null, thenableIndexCounter$1 = 0, interruptedWork = workInProgress;
				for (; null !== interruptedWork;) unwindInterruptedWork(interruptedWork.alternate, interruptedWork), interruptedWork = interruptedWork.return;
				workInProgress = null;
			}
		}
		function finalizeRender(lanes, finalizationTime) {
			0 !== (lanes & 127) && (blockingClampTime = finalizationTime);
			0 !== (lanes & 4194048) && (transitionClampTime = finalizationTime);
			0 !== (lanes & 62914560) && (retryClampTime = finalizationTime);
			0 !== (lanes & 2080374784) && (idleClampTime = finalizationTime);
		}
		function prepareFreshStack(root, lanes) {
			supportsUserTiming && (console.timeStamp("Blocking Track", .003, .003, "Blocking", LANES_TRACK_GROUP, "primary-light"), console.timeStamp("Transition Track", .003, .003, "Transition", LANES_TRACK_GROUP, "primary-light"), console.timeStamp("Suspense Track", .003, .003, "Suspense", LANES_TRACK_GROUP, "primary-light"), console.timeStamp("Idle Track", .003, .003, "Idle", LANES_TRACK_GROUP, "primary-light"));
			var previousRenderStartTime = renderStartTime;
			renderStartTime = now();
			if (0 !== workInProgressRootRenderLanes && 0 < previousRenderStartTime) {
				setCurrentTrackFromLanes(workInProgressRootRenderLanes);
				if (workInProgressRootExitStatus === RootSuspended || workInProgressRootExitStatus === RootSuspendedWithDelay) logSuspendedRenderPhase(previousRenderStartTime, renderStartTime, lanes, workInProgressUpdateTask);
				else {
					var endTime = renderStartTime, debugTask = workInProgressUpdateTask;
					if (supportsUserTiming && !(endTime <= previousRenderStartTime)) {
						var color = (lanes & 738197653) === lanes ? "tertiary-dark" : "primary-dark", label = (lanes & 536870912) === lanes ? "Prewarm" : (lanes & 201326741) === lanes ? "Interrupted Hydration" : "Interrupted Render";
						debugTask ? debugTask.run(console.timeStamp.bind(console, label, previousRenderStartTime, endTime, currentTrack, LANES_TRACK_GROUP, color)) : console.timeStamp(label, previousRenderStartTime, endTime, currentTrack, LANES_TRACK_GROUP, color);
					}
				}
				finalizeRender(workInProgressRootRenderLanes, renderStartTime);
			}
			previousRenderStartTime = workInProgressUpdateTask;
			workInProgressUpdateTask = null;
			if (0 !== (lanes & 127)) {
				workInProgressUpdateTask = blockingUpdateTask;
				debugTask = 0 <= blockingUpdateTime && blockingUpdateTime < blockingClampTime ? blockingClampTime : blockingUpdateTime;
				endTime = 0 <= blockingEventTime && blockingEventTime < blockingClampTime ? blockingClampTime : blockingEventTime;
				color = 0 <= endTime ? endTime : 0 <= debugTask ? debugTask : renderStartTime;
				0 <= blockingSuspendedTime ? (setCurrentTrackFromLanes(2), logSuspendedWithDelayPhase(blockingSuspendedTime, color, lanes, previousRenderStartTime)) : 0 !== (animatingLanes & 127) && (setCurrentTrackFromLanes(2), logAnimatingPhase(blockingClampTime, color, animatingTask));
				previousRenderStartTime = debugTask;
				var eventTime = endTime, eventType = blockingEventType, eventIsRepeat = 0 < blockingEventRepeatTime, isSpawnedUpdate = blockingUpdateType === SPAWNED_UPDATE, isPingedUpdate = blockingUpdateType === PINGED_UPDATE;
				debugTask = renderStartTime;
				endTime = blockingUpdateTask;
				color = blockingUpdateMethodName;
				label = blockingUpdateComponentName;
				if (supportsUserTiming) {
					currentTrack = "Blocking";
					0 < previousRenderStartTime ? previousRenderStartTime > debugTask && (previousRenderStartTime = debugTask) : previousRenderStartTime = debugTask;
					0 < eventTime ? eventTime > previousRenderStartTime && (eventTime = previousRenderStartTime) : eventTime = previousRenderStartTime;
					if (null !== eventType && previousRenderStartTime > eventTime) {
						var color$jscomp$0 = eventIsRepeat ? "secondary-light" : "warning";
						endTime ? endTime.run(console.timeStamp.bind(console, eventIsRepeat ? "Consecutive" : "Event: " + eventType, eventTime, previousRenderStartTime, currentTrack, LANES_TRACK_GROUP, color$jscomp$0)) : console.timeStamp(eventIsRepeat ? "Consecutive" : "Event: " + eventType, eventTime, previousRenderStartTime, currentTrack, LANES_TRACK_GROUP, color$jscomp$0);
					}
					debugTask > previousRenderStartTime && (eventTime = isSpawnedUpdate ? "error" : (lanes & 738197653) === lanes ? "tertiary-light" : "primary-light", isSpawnedUpdate = isPingedUpdate ? "Promise Resolved" : isSpawnedUpdate ? "Cascading Update" : 5 < debugTask - previousRenderStartTime ? "Update Blocked" : "Update", isPingedUpdate = [], null != label && isPingedUpdate.push(["Component name", label]), null != color && isPingedUpdate.push(["Method name", color]), previousRenderStartTime = {
						start: previousRenderStartTime,
						end: debugTask,
						detail: { devtools: {
							properties: isPingedUpdate,
							track: currentTrack,
							trackGroup: LANES_TRACK_GROUP,
							color: eventTime
						} }
					}, endTime ? endTime.run(performance.measure.bind(performance, isSpawnedUpdate, previousRenderStartTime)) : performance.measure(isSpawnedUpdate, previousRenderStartTime));
				}
				blockingUpdateTime = -1.1;
				blockingUpdateType = 0;
				blockingUpdateComponentName = blockingUpdateMethodName = null;
				blockingSuspendedTime = -1.1;
				blockingEventRepeatTime = blockingEventTime;
				blockingEventTime = -1.1;
				blockingClampTime = now();
			}
			0 !== (lanes & 4194048) && (workInProgressUpdateTask = transitionUpdateTask, debugTask = 0 <= transitionStartTime && transitionStartTime < transitionClampTime ? transitionClampTime : transitionStartTime, previousRenderStartTime = 0 <= transitionUpdateTime && transitionUpdateTime < transitionClampTime ? transitionClampTime : transitionUpdateTime, endTime = 0 <= transitionEventTime && transitionEventTime < transitionClampTime ? transitionClampTime : transitionEventTime, color = 0 <= endTime ? endTime : 0 <= previousRenderStartTime ? previousRenderStartTime : renderStartTime, 0 <= transitionSuspendedTime ? (setCurrentTrackFromLanes(256), logSuspendedWithDelayPhase(transitionSuspendedTime, color, lanes, workInProgressUpdateTask)) : 0 !== (animatingLanes & 4194048) && (setCurrentTrackFromLanes(256), logAnimatingPhase(transitionClampTime, color, animatingTask)), isPingedUpdate = endTime, eventTime = transitionEventType, eventType = 0 < transitionEventRepeatTime, eventIsRepeat = transitionUpdateType === PINGED_UPDATE, color = renderStartTime, endTime = transitionUpdateTask, label = transitionUpdateMethodName, isSpawnedUpdate = transitionUpdateComponentName, supportsUserTiming && (currentTrack = "Transition", 0 < previousRenderStartTime ? previousRenderStartTime > color && (previousRenderStartTime = color) : previousRenderStartTime = color, 0 < debugTask ? debugTask > previousRenderStartTime && (debugTask = previousRenderStartTime) : debugTask = previousRenderStartTime, 0 < isPingedUpdate ? isPingedUpdate > debugTask && (isPingedUpdate = debugTask) : isPingedUpdate = debugTask, debugTask > isPingedUpdate && null !== eventTime && (color$jscomp$0 = eventType ? "secondary-light" : "warning", endTime ? endTime.run(console.timeStamp.bind(console, eventType ? "Consecutive" : "Event: " + eventTime, isPingedUpdate, debugTask, currentTrack, LANES_TRACK_GROUP, color$jscomp$0)) : console.timeStamp(eventType ? "Consecutive" : "Event: " + eventTime, isPingedUpdate, debugTask, currentTrack, LANES_TRACK_GROUP, color$jscomp$0)), previousRenderStartTime > debugTask && (endTime ? endTime.run(console.timeStamp.bind(console, "Action", debugTask, previousRenderStartTime, currentTrack, LANES_TRACK_GROUP, "primary-dark")) : console.timeStamp("Action", debugTask, previousRenderStartTime, currentTrack, LANES_TRACK_GROUP, "primary-dark")), color > previousRenderStartTime && (debugTask = eventIsRepeat ? "Promise Resolved" : 5 < color - previousRenderStartTime ? "Update Blocked" : "Update", isPingedUpdate = [], null != isSpawnedUpdate && isPingedUpdate.push(["Component name", isSpawnedUpdate]), null != label && isPingedUpdate.push(["Method name", label]), previousRenderStartTime = {
				start: previousRenderStartTime,
				end: color,
				detail: { devtools: {
					properties: isPingedUpdate,
					track: currentTrack,
					trackGroup: LANES_TRACK_GROUP,
					color: "primary-light"
				} }
			}, endTime ? endTime.run(performance.measure.bind(performance, debugTask, previousRenderStartTime)) : performance.measure(debugTask, previousRenderStartTime))), transitionUpdateTime = transitionStartTime = -1.1, transitionUpdateType = 0, transitionSuspendedTime = -1.1, transitionEventRepeatTime = transitionEventTime, transitionEventTime = -1.1, transitionClampTime = now());
			0 !== (lanes & 62914560) && 0 !== (animatingLanes & 62914560) && (setCurrentTrackFromLanes(4194304), logAnimatingPhase(retryClampTime, renderStartTime, animatingTask));
			0 !== (lanes & 2080374784) && 0 !== (animatingLanes & 2080374784) && (setCurrentTrackFromLanes(268435456), logAnimatingPhase(idleClampTime, renderStartTime, animatingTask));
			previousRenderStartTime = root.timeoutHandle;
			previousRenderStartTime !== noTimeout && (root.timeoutHandle = noTimeout, cancelTimeout(previousRenderStartTime));
			previousRenderStartTime = root.cancelPendingCommit;
			null !== previousRenderStartTime && (root.cancelPendingCommit = null, previousRenderStartTime());
			pendingEffectsLanes = 0;
			resetWorkInProgressStack();
			workInProgressRoot = root;
			workInProgress = previousRenderStartTime = createWorkInProgress(root.current, null);
			workInProgressRootRenderLanes = lanes;
			workInProgressSuspendedReason = NotSuspended;
			workInProgressThrownValue = null;
			workInProgressRootDidSkipSuspendedSiblings = !1;
			workInProgressRootIsPrerendering = checkIfRootIsPrerendering(root, lanes);
			workInProgressRootDidAttachPingListener = !1;
			workInProgressRootExitStatus = RootInProgress;
			workInProgressSuspendedRetryLanes = workInProgressDeferredLane = workInProgressRootPingedLanes = workInProgressRootInterleavedUpdatedLanes = workInProgressRootSkippedLanes = 0;
			workInProgressRootRecoverableErrors = workInProgressRootConcurrentErrors = null;
			workInProgressRootDidIncludeRecursiveRenderUpdate = !1;
			0 !== (lanes & 8) && (lanes |= lanes & 32);
			endTime = root.entangledLanes;
			if (0 !== endTime) for (root = root.entanglements, endTime &= lanes; 0 < endTime;) debugTask = 31 - clz32(endTime), color = 1 << debugTask, lanes |= root[debugTask], endTime &= ~color;
			entangledRenderLanes = lanes;
			finishQueueingConcurrentUpdates();
			root = getCurrentTime();
			1e3 < root - lastResetTime && (ReactSharedInternals.recentlyCreatedOwnerStacks = 0, lastResetTime = root);
			ReactStrictModeWarnings.discardPendingWarnings();
			return previousRenderStartTime;
		}
		function handleThrow(root, thrownValue) {
			currentlyRenderingFiber = null;
			ReactSharedInternals.H = ContextOnlyDispatcher;
			ReactSharedInternals.getCurrentStack = null;
			isRendering = !1;
			current = null;
			thrownValue === SuspenseException || thrownValue === SuspenseActionException ? (thrownValue = getSuspendedThenable(), workInProgressSuspendedReason = SuspendedOnImmediate) : thrownValue === SuspenseyCommitException ? (thrownValue = getSuspendedThenable(), workInProgressSuspendedReason = SuspendedOnInstance) : workInProgressSuspendedReason = thrownValue === SelectiveHydrationException ? SuspendedOnHydration : null !== thrownValue && "object" === typeof thrownValue && "function" === typeof thrownValue.then ? SuspendedOnDeprecatedThrowPromise : SuspendedOnError;
			workInProgressThrownValue = thrownValue;
			var erroredWork = workInProgress;
			null === erroredWork ? (workInProgressRootExitStatus = RootFatalErrored, logUncaughtError(root, createCapturedValueAtFiber(thrownValue, root.current))) : erroredWork.mode & ProfileMode && stopProfilerTimerIfRunningAndRecordDuration(erroredWork);
		}
		function shouldRemainOnPreviousScreen() {
			var handler = suspenseHandlerStackCursor.current;
			return null === handler ? !0 : (workInProgressRootRenderLanes & 4194048) === workInProgressRootRenderLanes ? null === shellBoundary ? !0 : !1 : (workInProgressRootRenderLanes & 62914560) === workInProgressRootRenderLanes || 0 !== (workInProgressRootRenderLanes & 536870912) ? handler === shellBoundary : !1;
		}
		function pushDispatcher() {
			var prevDispatcher = ReactSharedInternals.H;
			ReactSharedInternals.H = ContextOnlyDispatcher;
			return null === prevDispatcher ? ContextOnlyDispatcher : prevDispatcher;
		}
		function pushAsyncDispatcher() {
			var prevAsyncDispatcher = ReactSharedInternals.A;
			ReactSharedInternals.A = DefaultAsyncDispatcher;
			return prevAsyncDispatcher;
		}
		function markRenderDerivedCause(fiber) {
			null === workInProgressUpdateTask && (workInProgressUpdateTask = null == fiber._debugTask ? null : fiber._debugTask);
		}
		function renderDidSuspendDelayIfPossible() {
			workInProgressRootExitStatus = RootSuspendedWithDelay;
			workInProgressRootDidSkipSuspendedSiblings || (workInProgressRootRenderLanes & 4194048) !== workInProgressRootRenderLanes && null !== suspenseHandlerStackCursor.current || (workInProgressRootIsPrerendering = !0);
			0 === (workInProgressRootSkippedLanes & 134217727) && 0 === (workInProgressRootInterleavedUpdatedLanes & 134217727) || null === workInProgressRoot || markRootSuspended(workInProgressRoot, workInProgressRootRenderLanes, workInProgressDeferredLane, !1);
		}
		function renderRootSync(root, lanes, shouldYieldForPrerendering) {
			var prevExecutionContext = executionContext;
			executionContext |= RenderContext;
			var prevDispatcher = pushDispatcher(), prevAsyncDispatcher = pushAsyncDispatcher();
			if (workInProgressRoot !== root || workInProgressRootRenderLanes !== lanes) {
				if (isDevToolsPresent) {
					var memoizedUpdaters = root.memoizedUpdaters;
					0 < memoizedUpdaters.size && (restorePendingUpdaters(root, workInProgressRootRenderLanes), memoizedUpdaters.clear());
					movePendingFibersToMemoized(root, lanes);
				}
				workInProgressTransitions = null;
				prepareFreshStack(root, lanes);
			}
			lanes = !1;
			memoizedUpdaters = workInProgressRootExitStatus;
			a: do
				try {
					if (workInProgressSuspendedReason !== NotSuspended && null !== workInProgress) {
						var unitOfWork = workInProgress, thrownValue = workInProgressThrownValue;
						switch (workInProgressSuspendedReason) {
							case SuspendedOnHydration:
								resetWorkInProgressStack();
								memoizedUpdaters = RootSuspendedAtTheShell;
								break a;
							case SuspendedOnImmediate:
							case SuspendedOnData:
							case SuspendedOnAction:
							case SuspendedOnDeprecatedThrowPromise:
								null === suspenseHandlerStackCursor.current && (lanes = !0);
								var reason = workInProgressSuspendedReason;
								workInProgressSuspendedReason = NotSuspended;
								workInProgressThrownValue = null;
								throwAndUnwindWorkLoop(root, unitOfWork, thrownValue, reason);
								if (shouldYieldForPrerendering && workInProgressRootIsPrerendering) {
									memoizedUpdaters = RootInProgress;
									break a;
								}
								break;
							default: reason = workInProgressSuspendedReason, workInProgressSuspendedReason = NotSuspended, workInProgressThrownValue = null, throwAndUnwindWorkLoop(root, unitOfWork, thrownValue, reason);
						}
					}
					workLoopSync();
					memoizedUpdaters = workInProgressRootExitStatus;
					break;
				} catch (thrownValue$8) {
					handleThrow(root, thrownValue$8);
				}
			while (1);
			lanes && root.shellSuspendCounter++;
			resetContextDependencies();
			executionContext = prevExecutionContext;
			ReactSharedInternals.H = prevDispatcher;
			ReactSharedInternals.A = prevAsyncDispatcher;
			null === workInProgress && (workInProgressRoot = null, workInProgressRootRenderLanes = 0, finishQueueingConcurrentUpdates());
			return memoizedUpdaters;
		}
		function workLoopSync() {
			for (; null !== workInProgress;) performUnitOfWork(workInProgress);
		}
		function renderRootConcurrent(root, lanes) {
			var prevExecutionContext = executionContext;
			executionContext |= RenderContext;
			var prevDispatcher = pushDispatcher(), prevAsyncDispatcher = pushAsyncDispatcher();
			if (workInProgressRoot !== root || workInProgressRootRenderLanes !== lanes) {
				if (isDevToolsPresent) {
					var memoizedUpdaters = root.memoizedUpdaters;
					0 < memoizedUpdaters.size && (restorePendingUpdaters(root, workInProgressRootRenderLanes), memoizedUpdaters.clear());
					movePendingFibersToMemoized(root, lanes);
				}
				workInProgressTransitions = null;
				workInProgressRootRenderTargetTime = now$1() + RENDER_TIMEOUT_MS;
				prepareFreshStack(root, lanes);
			} else workInProgressRootIsPrerendering = checkIfRootIsPrerendering(root, lanes);
			a: do
				try {
					if (workInProgressSuspendedReason !== NotSuspended && null !== workInProgress) b: switch (lanes = workInProgress, memoizedUpdaters = workInProgressThrownValue, workInProgressSuspendedReason) {
						case SuspendedOnError:
							workInProgressSuspendedReason = NotSuspended;
							workInProgressThrownValue = null;
							throwAndUnwindWorkLoop(root, lanes, memoizedUpdaters, SuspendedOnError);
							break;
						case SuspendedOnData:
						case SuspendedOnAction:
							if (isThenableResolved(memoizedUpdaters)) {
								workInProgressSuspendedReason = NotSuspended;
								workInProgressThrownValue = null;
								replaySuspendedUnitOfWork(lanes);
								break;
							}
							lanes = function() {
								workInProgressSuspendedReason !== SuspendedOnData && workInProgressSuspendedReason !== SuspendedOnAction || workInProgressRoot !== root || (workInProgressSuspendedReason = SuspendedAndReadyToContinue);
								ensureRootIsScheduled(root);
							};
							memoizedUpdaters.then(lanes, lanes);
							break a;
						case SuspendedOnImmediate:
							workInProgressSuspendedReason = SuspendedAndReadyToContinue;
							break a;
						case SuspendedOnInstance:
							workInProgressSuspendedReason = SuspendedOnInstanceAndReadyToContinue;
							break a;
						case SuspendedAndReadyToContinue:
							isThenableResolved(memoizedUpdaters) ? (workInProgressSuspendedReason = NotSuspended, workInProgressThrownValue = null, replaySuspendedUnitOfWork(lanes)) : (workInProgressSuspendedReason = NotSuspended, workInProgressThrownValue = null, throwAndUnwindWorkLoop(root, lanes, memoizedUpdaters, SuspendedAndReadyToContinue));
							break;
						case SuspendedOnInstanceAndReadyToContinue:
							var resource = null;
							switch (workInProgress.tag) {
								case 26: resource = workInProgress.memoizedState;
								case 5:
								case 27:
									var hostFiber = workInProgress;
									if (resource ? preloadResource(resource) : hostFiber.stateNode.complete) {
										workInProgressSuspendedReason = NotSuspended;
										workInProgressThrownValue = null;
										var sibling = hostFiber.sibling;
										if (null !== sibling) workInProgress = sibling;
										else {
											var returnFiber = hostFiber.return;
											null !== returnFiber ? (workInProgress = returnFiber, completeUnitOfWork(returnFiber)) : workInProgress = null;
										}
										break b;
									}
									break;
								default: console.error("Unexpected type of fiber triggered a suspensey commit. This is a bug in React.");
							}
							workInProgressSuspendedReason = NotSuspended;
							workInProgressThrownValue = null;
							throwAndUnwindWorkLoop(root, lanes, memoizedUpdaters, SuspendedOnInstanceAndReadyToContinue);
							break;
						case SuspendedOnDeprecatedThrowPromise:
							workInProgressSuspendedReason = NotSuspended;
							workInProgressThrownValue = null;
							throwAndUnwindWorkLoop(root, lanes, memoizedUpdaters, SuspendedOnDeprecatedThrowPromise);
							break;
						case SuspendedOnHydration:
							resetWorkInProgressStack();
							workInProgressRootExitStatus = RootSuspendedAtTheShell;
							break a;
						default: throw Error("Unexpected SuspendedReason. This is a bug in React.");
					}
					null !== ReactSharedInternals.actQueue ? workLoopSync() : workLoopConcurrentByScheduler();
					break;
				} catch (thrownValue$9) {
					handleThrow(root, thrownValue$9);
				}
			while (1);
			resetContextDependencies();
			ReactSharedInternals.H = prevDispatcher;
			ReactSharedInternals.A = prevAsyncDispatcher;
			executionContext = prevExecutionContext;
			if (null !== workInProgress) return RootInProgress;
			workInProgressRoot = null;
			workInProgressRootRenderLanes = 0;
			finishQueueingConcurrentUpdates();
			return workInProgressRootExitStatus;
		}
		function workLoopConcurrentByScheduler() {
			for (; null !== workInProgress && !shouldYield();) performUnitOfWork(workInProgress);
		}
		function performUnitOfWork(unitOfWork) {
			var current = unitOfWork.alternate;
			(unitOfWork.mode & ProfileMode) !== NoMode ? (startProfilerTimer(unitOfWork), current = runWithFiberInDEV(unitOfWork, beginWork, current, unitOfWork, entangledRenderLanes), stopProfilerTimerIfRunningAndRecordDuration(unitOfWork)) : current = runWithFiberInDEV(unitOfWork, beginWork, current, unitOfWork, entangledRenderLanes);
			unitOfWork.memoizedProps = unitOfWork.pendingProps;
			null === current ? completeUnitOfWork(unitOfWork) : workInProgress = current;
		}
		function replaySuspendedUnitOfWork(unitOfWork) {
			var next = runWithFiberInDEV(unitOfWork, replayBeginWork, unitOfWork);
			unitOfWork.memoizedProps = unitOfWork.pendingProps;
			null === next ? completeUnitOfWork(unitOfWork) : workInProgress = next;
		}
		function replayBeginWork(unitOfWork) {
			var current = unitOfWork.alternate, isProfilingMode = (unitOfWork.mode & ProfileMode) !== NoMode;
			isProfilingMode && startProfilerTimer(unitOfWork);
			switch (unitOfWork.tag) {
				case 15:
				case 0:
					current = replayFunctionComponent(current, unitOfWork, unitOfWork.pendingProps, unitOfWork.type, void 0, workInProgressRootRenderLanes);
					break;
				case 11:
					current = replayFunctionComponent(current, unitOfWork, unitOfWork.pendingProps, unitOfWork.type.render, unitOfWork.ref, workInProgressRootRenderLanes);
					break;
				case 5: resetHooksOnUnwind(unitOfWork);
				default: unwindInterruptedWork(current, unitOfWork), unitOfWork = workInProgress = resetWorkInProgress(unitOfWork, entangledRenderLanes), current = beginWork(current, unitOfWork, entangledRenderLanes);
			}
			isProfilingMode && stopProfilerTimerIfRunningAndRecordDuration(unitOfWork);
			return current;
		}
		function throwAndUnwindWorkLoop(root, unitOfWork, thrownValue, suspendedReason) {
			resetContextDependencies();
			resetHooksOnUnwind(unitOfWork);
			thenableState$1 = null;
			thenableIndexCounter$1 = 0;
			var returnFiber = unitOfWork.return;
			try {
				if (throwException(root, returnFiber, unitOfWork, thrownValue, workInProgressRootRenderLanes)) {
					workInProgressRootExitStatus = RootFatalErrored;
					logUncaughtError(root, createCapturedValueAtFiber(thrownValue, root.current));
					workInProgress = null;
					return;
				}
			} catch (error) {
				if (null !== returnFiber) throw workInProgress = returnFiber, error;
				workInProgressRootExitStatus = RootFatalErrored;
				logUncaughtError(root, createCapturedValueAtFiber(thrownValue, root.current));
				workInProgress = null;
				return;
			}
			if (unitOfWork.flags & 32768) {
				if (isHydrating || suspendedReason === SuspendedOnError) root = !0;
				else if (workInProgressRootIsPrerendering || 0 !== (workInProgressRootRenderLanes & 536870912)) root = !1;
				else if (workInProgressRootDidSkipSuspendedSiblings = root = !0, suspendedReason === SuspendedOnData || suspendedReason === SuspendedOnAction || suspendedReason === SuspendedOnImmediate || suspendedReason === SuspendedOnDeprecatedThrowPromise) suspendedReason = suspenseHandlerStackCursor.current, null !== suspendedReason && 13 === suspendedReason.tag && (suspendedReason.flags |= 16384);
				unwindUnitOfWork(unitOfWork, root);
			} else completeUnitOfWork(unitOfWork);
		}
		function completeUnitOfWork(unitOfWork) {
			var completedWork = unitOfWork;
			do {
				if (0 !== (completedWork.flags & 32768)) {
					unwindUnitOfWork(completedWork, workInProgressRootDidSkipSuspendedSiblings);
					return;
				}
				var current = completedWork.alternate;
				unitOfWork = completedWork.return;
				startProfilerTimer(completedWork);
				current = runWithFiberInDEV(completedWork, completeWork, current, completedWork, entangledRenderLanes);
				(completedWork.mode & ProfileMode) !== NoMode && stopProfilerTimerIfRunningAndRecordIncompleteDuration(completedWork);
				if (null !== current) {
					workInProgress = current;
					return;
				}
				completedWork = completedWork.sibling;
				if (null !== completedWork) {
					workInProgress = completedWork;
					return;
				}
				workInProgress = completedWork = unitOfWork;
			} while (null !== completedWork);
			workInProgressRootExitStatus === RootInProgress && (workInProgressRootExitStatus = RootCompleted);
		}
		function unwindUnitOfWork(unitOfWork, skipSiblings) {
			do {
				var next = unwindWork(unitOfWork.alternate, unitOfWork);
				if (null !== next) {
					next.flags &= 32767;
					workInProgress = next;
					return;
				}
				if ((unitOfWork.mode & ProfileMode) !== NoMode) {
					stopProfilerTimerIfRunningAndRecordIncompleteDuration(unitOfWork);
					next = unitOfWork.actualDuration;
					for (var child = unitOfWork.child; null !== child;) next += child.actualDuration, child = child.sibling;
					unitOfWork.actualDuration = next;
				}
				next = unitOfWork.return;
				null !== next && (next.flags |= 32768, next.subtreeFlags = 0, next.deletions = null);
				if (!skipSiblings && (unitOfWork = unitOfWork.sibling, null !== unitOfWork)) {
					workInProgress = unitOfWork;
					return;
				}
				workInProgress = unitOfWork = next;
			} while (null !== unitOfWork);
			workInProgressRootExitStatus = RootSuspendedAtTheShell;
			workInProgress = null;
		}
		function commitRoot(root, finishedWork, lanes, recoverableErrors, transitions, didIncludeRenderPhaseUpdate, spawnedLane, updatedLanes, suspendedRetryLanes, exitStatus, suspendedState, suspendedCommitReason, completedRenderStartTime, completedRenderEndTime) {
			root.cancelPendingCommit = null;
			do
				flushPendingEffects();
			while (pendingEffectsStatus !== NO_PENDING_EFFECTS);
			ReactStrictModeWarnings.flushLegacyContextWarning();
			ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings();
			if ((executionContext & (RenderContext | CommitContext)) !== NoContext) throw Error("Should not already be working.");
			setCurrentTrackFromLanes(lanes);
			exitStatus === RootErrored ? logErroredRenderPhase(completedRenderStartTime, completedRenderEndTime, lanes, workInProgressUpdateTask) : null !== recoverableErrors ? logRecoveredRenderPhase(completedRenderStartTime, completedRenderEndTime, lanes, recoverableErrors, null !== finishedWork && null !== finishedWork.alternate && finishedWork.alternate.memoizedState.isDehydrated && 0 !== (finishedWork.flags & 256), workInProgressUpdateTask) : logRenderPhase(completedRenderStartTime, completedRenderEndTime, lanes, workInProgressUpdateTask);
			if (null !== finishedWork) {
				0 === lanes && console.error("finishedLanes should not be empty during a commit. This is a bug in React.");
				if (finishedWork === root.current) throw Error("Cannot commit the same tree as before. This error is likely caused by a bug in React. Please file an issue.");
				didIncludeRenderPhaseUpdate = finishedWork.lanes | finishedWork.childLanes;
				didIncludeRenderPhaseUpdate |= concurrentlyUpdatedLanes;
				markRootFinished(root, lanes, didIncludeRenderPhaseUpdate, spawnedLane, updatedLanes, suspendedRetryLanes);
				root === workInProgressRoot && (workInProgress = workInProgressRoot = null, workInProgressRootRenderLanes = 0);
				pendingFinishedWork = finishedWork;
				pendingEffectsRoot = root;
				pendingEffectsLanes = lanes;
				pendingEffectsRemainingLanes = didIncludeRenderPhaseUpdate;
				pendingPassiveTransitions = transitions;
				pendingRecoverableErrors = recoverableErrors;
				pendingEffectsRenderEndTime = completedRenderEndTime;
				pendingSuspendedCommitReason = suspendedCommitReason;
				pendingDelayedCommitReason = IMMEDIATE_COMMIT;
				pendingSuspendedViewTransitionReason = null;
				0 !== finishedWork.actualDuration || 0 !== (finishedWork.subtreeFlags & 10256) || 0 !== (finishedWork.flags & 10256) ? (root.callbackNode = null, root.callbackPriority = 0, scheduleCallback$1(NormalPriority$1, function() {
					schedulerEvent = window.event;
					pendingDelayedCommitReason === IMMEDIATE_COMMIT && (pendingDelayedCommitReason = DELAYED_PASSIVE_COMMIT);
					flushPassiveEffects();
					return null;
				})) : (root.callbackNode = null, root.callbackPriority = 0);
				commitErrors = null;
				commitStartTime = now();
				null !== suspendedCommitReason && logSuspendedCommitPhase(completedRenderEndTime, commitStartTime, suspendedCommitReason, workInProgressUpdateTask);
				recoverableErrors = 0 !== (finishedWork.flags & 13878);
				if (0 !== (finishedWork.subtreeFlags & 13878) || recoverableErrors) {
					recoverableErrors = ReactSharedInternals.T;
					ReactSharedInternals.T = null;
					transitions = ReactDOMSharedInternals.p;
					ReactDOMSharedInternals.p = DiscreteEventPriority;
					spawnedLane = executionContext;
					executionContext |= CommitContext;
					try {
						commitBeforeMutationEffects(root, finishedWork, lanes);
					} finally {
						executionContext = spawnedLane, ReactDOMSharedInternals.p = transitions, ReactSharedInternals.T = recoverableErrors;
					}
				}
				pendingEffectsStatus = PENDING_MUTATION_PHASE;
				flushMutationEffects();
				flushLayoutEffects();
				flushSpawnedWork();
			}
		}
		function flushMutationEffects() {
			if (pendingEffectsStatus === PENDING_MUTATION_PHASE) {
				pendingEffectsStatus = NO_PENDING_EFFECTS;
				var root = pendingEffectsRoot, finishedWork = pendingFinishedWork, lanes = pendingEffectsLanes, rootMutationHasEffect = 0 !== (finishedWork.flags & 13878);
				if (0 !== (finishedWork.subtreeFlags & 13878) || rootMutationHasEffect) {
					rootMutationHasEffect = ReactSharedInternals.T;
					ReactSharedInternals.T = null;
					var previousPriority = ReactDOMSharedInternals.p;
					ReactDOMSharedInternals.p = DiscreteEventPriority;
					var prevExecutionContext = executionContext;
					executionContext |= CommitContext;
					try {
						inProgressLanes = lanes;
						inProgressRoot = root;
						resetComponentEffectTimers();
						commitMutationEffectsOnFiber(finishedWork, root);
						inProgressRoot = inProgressLanes = null;
						lanes = selectionInformation;
						var curFocusedElem = getActiveElementDeep(root.containerInfo), priorFocusedElem = lanes.focusedElem, priorSelectionRange = lanes.selectionRange;
						if (curFocusedElem !== priorFocusedElem && priorFocusedElem && priorFocusedElem.ownerDocument && containsNode(priorFocusedElem.ownerDocument.documentElement, priorFocusedElem)) {
							if (null !== priorSelectionRange && hasSelectionCapabilities(priorFocusedElem)) {
								var start = priorSelectionRange.start, end = priorSelectionRange.end;
								void 0 === end && (end = start);
								if ("selectionStart" in priorFocusedElem) priorFocusedElem.selectionStart = start, priorFocusedElem.selectionEnd = Math.min(end, priorFocusedElem.value.length);
								else {
									var doc = priorFocusedElem.ownerDocument || document, win = doc && doc.defaultView || window;
									if (win.getSelection) {
										var selection = win.getSelection(), length = priorFocusedElem.textContent.length, start$jscomp$0 = Math.min(priorSelectionRange.start, length), end$jscomp$0 = void 0 === priorSelectionRange.end ? start$jscomp$0 : Math.min(priorSelectionRange.end, length);
										!selection.extend && start$jscomp$0 > end$jscomp$0 && (curFocusedElem = end$jscomp$0, end$jscomp$0 = start$jscomp$0, start$jscomp$0 = curFocusedElem);
										var startMarker = getNodeForCharacterOffset(priorFocusedElem, start$jscomp$0), endMarker = getNodeForCharacterOffset(priorFocusedElem, end$jscomp$0);
										if (startMarker && endMarker && (1 !== selection.rangeCount || selection.anchorNode !== startMarker.node || selection.anchorOffset !== startMarker.offset || selection.focusNode !== endMarker.node || selection.focusOffset !== endMarker.offset)) {
											var range = doc.createRange();
											range.setStart(startMarker.node, startMarker.offset);
											selection.removeAllRanges();
											start$jscomp$0 > end$jscomp$0 ? (selection.addRange(range), selection.extend(endMarker.node, endMarker.offset)) : (range.setEnd(endMarker.node, endMarker.offset), selection.addRange(range));
										}
									}
								}
							}
							doc = [];
							for (selection = priorFocusedElem; selection = selection.parentNode;) 1 === selection.nodeType && doc.push({
								element: selection,
								left: selection.scrollLeft,
								top: selection.scrollTop
							});
							"function" === typeof priorFocusedElem.focus && priorFocusedElem.focus();
							for (priorFocusedElem = 0; priorFocusedElem < doc.length; priorFocusedElem++) {
								var info = doc[priorFocusedElem];
								info.element.scrollLeft = info.left;
								info.element.scrollTop = info.top;
							}
						}
						_enabled = !!eventsEnabled;
						selectionInformation = eventsEnabled = null;
					} finally {
						executionContext = prevExecutionContext, ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = rootMutationHasEffect;
					}
				}
				root.current = finishedWork;
				pendingEffectsStatus = PENDING_LAYOUT_PHASE;
			}
		}
		function flushLayoutEffects() {
			if (pendingEffectsStatus === PENDING_LAYOUT_PHASE) {
				pendingEffectsStatus = NO_PENDING_EFFECTS;
				var suspendedViewTransitionReason = pendingSuspendedViewTransitionReason;
				if (null !== suspendedViewTransitionReason) {
					commitStartTime = now();
					var startTime = commitEndTime, endTime = commitStartTime;
					!supportsUserTiming || endTime <= startTime || (animatingTask ? animatingTask.run(console.timeStamp.bind(console, suspendedViewTransitionReason, startTime, endTime, currentTrack, LANES_TRACK_GROUP, "secondary-light")) : console.timeStamp(suspendedViewTransitionReason, startTime, endTime, currentTrack, LANES_TRACK_GROUP, "secondary-light"));
				}
				suspendedViewTransitionReason = pendingEffectsRoot;
				startTime = pendingFinishedWork;
				endTime = pendingEffectsLanes;
				var rootHasLayoutEffect = 0 !== (startTime.flags & 8772);
				if (0 !== (startTime.subtreeFlags & 8772) || rootHasLayoutEffect) {
					rootHasLayoutEffect = ReactSharedInternals.T;
					ReactSharedInternals.T = null;
					var _previousPriority = ReactDOMSharedInternals.p;
					ReactDOMSharedInternals.p = DiscreteEventPriority;
					var _prevExecutionContext = executionContext;
					executionContext |= CommitContext;
					try {
						inProgressLanes = endTime, inProgressRoot = suspendedViewTransitionReason, resetComponentEffectTimers(), commitLayoutEffectOnFiber(suspendedViewTransitionReason, startTime.alternate, startTime), inProgressRoot = inProgressLanes = null;
					} finally {
						executionContext = _prevExecutionContext, ReactDOMSharedInternals.p = _previousPriority, ReactSharedInternals.T = rootHasLayoutEffect;
					}
				}
				suspendedViewTransitionReason = pendingEffectsRenderEndTime;
				startTime = pendingSuspendedCommitReason;
				commitEndTime = now();
				suspendedViewTransitionReason = null === startTime ? suspendedViewTransitionReason : commitStartTime;
				startTime = commitEndTime;
				endTime = pendingDelayedCommitReason === ABORTED_VIEW_TRANSITION_COMMIT;
				rootHasLayoutEffect = workInProgressUpdateTask;
				null !== commitErrors ? logCommitErrored(suspendedViewTransitionReason, startTime, commitErrors, !1, rootHasLayoutEffect) : !supportsUserTiming || startTime <= suspendedViewTransitionReason || (rootHasLayoutEffect ? rootHasLayoutEffect.run(console.timeStamp.bind(console, endTime ? "Commit Interrupted View Transition" : "Commit", suspendedViewTransitionReason, startTime, currentTrack, LANES_TRACK_GROUP, endTime ? "error" : "secondary-dark")) : console.timeStamp(endTime ? "Commit Interrupted View Transition" : "Commit", suspendedViewTransitionReason, startTime, currentTrack, LANES_TRACK_GROUP, endTime ? "error" : "secondary-dark"));
				pendingEffectsStatus = PENDING_AFTER_MUTATION_PHASE;
			}
		}
		function flushSpawnedWork() {
			if (pendingEffectsStatus === PENDING_SPAWNED_WORK || pendingEffectsStatus === PENDING_AFTER_MUTATION_PHASE) {
				if (pendingEffectsStatus === PENDING_SPAWNED_WORK) {
					var startViewTransitionStartTime = commitEndTime;
					commitEndTime = now();
					var endTime = commitEndTime, abortedViewTransition = pendingDelayedCommitReason === ABORTED_VIEW_TRANSITION_COMMIT;
					!supportsUserTiming || endTime <= startViewTransitionStartTime || (animatingTask ? animatingTask.run(console.timeStamp.bind(console, abortedViewTransition ? "Interrupted View Transition" : "Starting Animation", startViewTransitionStartTime, endTime, currentTrack, LANES_TRACK_GROUP, abortedViewTransition ? "error" : "secondary-light")) : console.timeStamp(abortedViewTransition ? "Interrupted View Transition" : "Starting Animation", startViewTransitionStartTime, endTime, currentTrack, LANES_TRACK_GROUP, abortedViewTransition ? " error" : "secondary-light"));
					pendingDelayedCommitReason !== ABORTED_VIEW_TRANSITION_COMMIT && (pendingDelayedCommitReason = ANIMATION_STARTED_COMMIT);
				}
				pendingEffectsStatus = NO_PENDING_EFFECTS;
				requestPaint();
				startViewTransitionStartTime = pendingEffectsRoot;
				var finishedWork = pendingFinishedWork;
				endTime = pendingEffectsLanes;
				abortedViewTransition = pendingRecoverableErrors;
				var rootDidHavePassiveEffects = 0 !== finishedWork.actualDuration || 0 !== (finishedWork.subtreeFlags & 10256) || 0 !== (finishedWork.flags & 10256);
				rootDidHavePassiveEffects ? pendingEffectsStatus = PENDING_PASSIVE_PHASE : (pendingEffectsStatus = NO_PENDING_EFFECTS, pendingFinishedWork = pendingEffectsRoot = null, releaseRootPooledCache(startViewTransitionStartTime, startViewTransitionStartTime.pendingLanes), nestedPassiveUpdateCount = 0, rootWithPassiveNestedUpdates = null);
				var remainingLanes = startViewTransitionStartTime.pendingLanes;
				0 === remainingLanes && (legacyErrorBoundariesThatAlreadyFailed = null);
				rootDidHavePassiveEffects || commitDoubleInvokeEffectsInDEV(startViewTransitionStartTime);
				remainingLanes = lanesToEventPriority(endTime);
				finishedWork = finishedWork.stateNode;
				if (injectedHook && "function" === typeof injectedHook.onCommitFiberRoot) try {
					var didError = 128 === (finishedWork.current.flags & 128);
					switch (remainingLanes) {
						case DiscreteEventPriority:
							var schedulerPriority = ImmediatePriority;
							break;
						case ContinuousEventPriority:
							schedulerPriority = UserBlockingPriority;
							break;
						case DefaultEventPriority:
							schedulerPriority = NormalPriority$1;
							break;
						case IdleEventPriority:
							schedulerPriority = IdlePriority;
							break;
						default: schedulerPriority = NormalPriority$1;
					}
					injectedHook.onCommitFiberRoot(rendererID, finishedWork, schedulerPriority, didError);
				} catch (err) {
					hasLoggedError || (hasLoggedError = !0, console.error("React instrumentation encountered an error: %o", err));
				}
				isDevToolsPresent && startViewTransitionStartTime.memoizedUpdaters.clear();
				onCommitRoot();
				if (null !== abortedViewTransition) {
					didError = ReactSharedInternals.T;
					schedulerPriority = ReactDOMSharedInternals.p;
					ReactDOMSharedInternals.p = DiscreteEventPriority;
					ReactSharedInternals.T = null;
					try {
						var onRecoverableError = startViewTransitionStartTime.onRecoverableError;
						for (finishedWork = 0; finishedWork < abortedViewTransition.length; finishedWork++) {
							var recoverableError = abortedViewTransition[finishedWork], errorInfo = makeErrorInfo(recoverableError.stack);
							runWithFiberInDEV(recoverableError.source, onRecoverableError, recoverableError.value, errorInfo);
						}
					} finally {
						ReactSharedInternals.T = didError, ReactDOMSharedInternals.p = schedulerPriority;
					}
				}
				0 !== (pendingEffectsLanes & 3) && flushPendingEffects();
				ensureRootIsScheduled(startViewTransitionStartTime);
				remainingLanes = startViewTransitionStartTime.pendingLanes;
				0 !== (endTime & 261930) && 0 !== (remainingLanes & 42) ? (nestedUpdateScheduled = !0, startViewTransitionStartTime === rootWithNestedUpdates ? nestedUpdateCount++ : (nestedUpdateCount = 0, rootWithNestedUpdates = startViewTransitionStartTime)) : nestedUpdateCount = 0;
				rootDidHavePassiveEffects || finalizeRender(endTime, commitEndTime);
				flushSyncWorkAcrossRoots_impl(0, !1);
			}
		}
		function makeErrorInfo(componentStack) {
			componentStack = { componentStack };
			Object.defineProperty(componentStack, "digest", { get: function() {
				console.error("You are accessing \"digest\" from the errorInfo object passed to onRecoverableError. This property is no longer provided as part of errorInfo but can be accessed as a property of the Error instance itself.");
			} });
			return componentStack;
		}
		function releaseRootPooledCache(root, remainingLanes) {
			0 === (root.pooledCacheLanes &= remainingLanes) && (remainingLanes = root.pooledCache, null != remainingLanes && (root.pooledCache = null, releaseCache(remainingLanes)));
		}
		function flushPendingEffects() {
			flushMutationEffects();
			flushLayoutEffects();
			flushSpawnedWork();
			return flushPassiveEffects();
		}
		function flushPassiveEffects() {
			if (pendingEffectsStatus !== PENDING_PASSIVE_PHASE) return !1;
			var root = pendingEffectsRoot, remainingLanes = pendingEffectsRemainingLanes;
			pendingEffectsRemainingLanes = 0;
			var renderPriority = lanesToEventPriority(pendingEffectsLanes), priority = 0 === DefaultEventPriority || DefaultEventPriority > renderPriority ? DefaultEventPriority : renderPriority;
			renderPriority = ReactSharedInternals.T;
			var previousPriority = ReactDOMSharedInternals.p;
			try {
				ReactDOMSharedInternals.p = priority;
				ReactSharedInternals.T = null;
				var transitions = pendingPassiveTransitions;
				pendingPassiveTransitions = null;
				priority = pendingEffectsRoot;
				var lanes = pendingEffectsLanes;
				pendingEffectsStatus = NO_PENDING_EFFECTS;
				pendingFinishedWork = pendingEffectsRoot = null;
				pendingEffectsLanes = 0;
				if ((executionContext & (RenderContext | CommitContext)) !== NoContext) throw Error("Cannot flush passive effects while already rendering.");
				setCurrentTrackFromLanes(lanes);
				isFlushingPassiveEffects = !0;
				didScheduleUpdateDuringPassiveEffects = !1;
				var passiveEffectStartTime = 0;
				commitErrors = null;
				passiveEffectStartTime = now$1();
				if (pendingDelayedCommitReason === ANIMATION_STARTED_COMMIT) logAnimatingPhase(commitEndTime, passiveEffectStartTime, animatingTask);
				else {
					var startTime = commitEndTime, endTime = passiveEffectStartTime, delayedUntilPaint = pendingDelayedCommitReason === DELAYED_PASSIVE_COMMIT;
					!supportsUserTiming || endTime <= startTime || (workInProgressUpdateTask ? workInProgressUpdateTask.run(console.timeStamp.bind(console, delayedUntilPaint ? "Waiting for Paint" : "Waiting", startTime, endTime, currentTrack, LANES_TRACK_GROUP, "secondary-light")) : console.timeStamp(delayedUntilPaint ? "Waiting for Paint" : "Waiting", startTime, endTime, currentTrack, LANES_TRACK_GROUP, "secondary-light"));
				}
				startTime = executionContext;
				executionContext |= CommitContext;
				var finishedWork = priority.current;
				resetComponentEffectTimers();
				commitPassiveUnmountOnFiber(finishedWork);
				var finishedWork$jscomp$0 = priority.current;
				finishedWork = pendingEffectsRenderEndTime;
				resetComponentEffectTimers();
				commitPassiveMountOnFiber(priority, finishedWork$jscomp$0, lanes, transitions, finishedWork);
				commitDoubleInvokeEffectsInDEV(priority);
				executionContext = startTime;
				var passiveEffectsEndTime = now$1();
				finishedWork$jscomp$0 = passiveEffectStartTime;
				finishedWork = workInProgressUpdateTask;
				null !== commitErrors ? logCommitErrored(finishedWork$jscomp$0, passiveEffectsEndTime, commitErrors, !0, finishedWork) : !supportsUserTiming || passiveEffectsEndTime <= finishedWork$jscomp$0 || (finishedWork ? finishedWork.run(console.timeStamp.bind(console, "Remaining Effects", finishedWork$jscomp$0, passiveEffectsEndTime, currentTrack, LANES_TRACK_GROUP, "secondary-dark")) : console.timeStamp("Remaining Effects", finishedWork$jscomp$0, passiveEffectsEndTime, currentTrack, LANES_TRACK_GROUP, "secondary-dark"));
				finalizeRender(lanes, passiveEffectsEndTime);
				flushSyncWorkAcrossRoots_impl(0, !1);
				didScheduleUpdateDuringPassiveEffects ? priority === rootWithPassiveNestedUpdates ? nestedPassiveUpdateCount++ : (nestedPassiveUpdateCount = 0, rootWithPassiveNestedUpdates = priority) : nestedPassiveUpdateCount = 0;
				didScheduleUpdateDuringPassiveEffects = isFlushingPassiveEffects = !1;
				if (injectedHook && "function" === typeof injectedHook.onPostCommitFiberRoot) try {
					injectedHook.onPostCommitFiberRoot(rendererID, priority);
				} catch (err) {
					hasLoggedError || (hasLoggedError = !0, console.error("React instrumentation encountered an error: %o", err));
				}
				var stateNode = priority.current.stateNode;
				stateNode.effectDuration = 0;
				stateNode.passiveEffectDuration = 0;
				return !0;
			} finally {
				ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = renderPriority, releaseRootPooledCache(root, remainingLanes);
			}
		}
		function captureCommitPhaseErrorOnRoot(rootFiber, sourceFiber, error) {
			sourceFiber = createCapturedValueAtFiber(error, sourceFiber);
			recordEffectError(sourceFiber);
			sourceFiber = createRootErrorUpdate(rootFiber.stateNode, sourceFiber, 2);
			rootFiber = enqueueUpdate(rootFiber, sourceFiber, 2);
			null !== rootFiber && (markRootUpdated$1(rootFiber, 2), ensureRootIsScheduled(rootFiber));
		}
		function captureCommitPhaseError(sourceFiber, nearestMountedAncestor, error) {
			isRunningInsertionEffect = !1;
			if (3 === sourceFiber.tag) captureCommitPhaseErrorOnRoot(sourceFiber, sourceFiber, error);
			else {
				for (; null !== nearestMountedAncestor;) {
					if (3 === nearestMountedAncestor.tag) {
						captureCommitPhaseErrorOnRoot(nearestMountedAncestor, sourceFiber, error);
						return;
					}
					if (1 === nearestMountedAncestor.tag) {
						var instance = nearestMountedAncestor.stateNode;
						if ("function" === typeof nearestMountedAncestor.type.getDerivedStateFromError || "function" === typeof instance.componentDidCatch && (null === legacyErrorBoundariesThatAlreadyFailed || !legacyErrorBoundariesThatAlreadyFailed.has(instance))) {
							sourceFiber = createCapturedValueAtFiber(error, sourceFiber);
							recordEffectError(sourceFiber);
							error = createClassErrorUpdate(2);
							instance = enqueueUpdate(nearestMountedAncestor, error, 2);
							null !== instance && (initializeClassErrorUpdate(error, instance, nearestMountedAncestor, sourceFiber), markRootUpdated$1(instance, 2), ensureRootIsScheduled(instance));
							return;
						}
					}
					nearestMountedAncestor = nearestMountedAncestor.return;
				}
				console.error("Internal React error: Attempted to capture a commit phase error inside a detached tree. This indicates a bug in React. Potential causes include deleting the same fiber more than once, committing an already-finished tree, or an inconsistent return pointer.\n\nError message:\n\n%s", error);
			}
		}
		function attachPingListener(root, wakeable, lanes) {
			var pingCache = root.pingCache;
			if (null === pingCache) {
				pingCache = root.pingCache = new PossiblyWeakMap();
				var threadIDs = /* @__PURE__ */ new Set();
				pingCache.set(wakeable, threadIDs);
			} else threadIDs = pingCache.get(wakeable), void 0 === threadIDs && (threadIDs = /* @__PURE__ */ new Set(), pingCache.set(wakeable, threadIDs));
			threadIDs.has(lanes) || (workInProgressRootDidAttachPingListener = !0, threadIDs.add(lanes), pingCache = pingSuspendedRoot.bind(null, root, wakeable, lanes), isDevToolsPresent && restorePendingUpdaters(root, lanes), wakeable.then(pingCache, pingCache));
		}
		function pingSuspendedRoot(root, wakeable, pingedLanes) {
			var pingCache = root.pingCache;
			null !== pingCache && pingCache.delete(wakeable);
			root.pingedLanes |= root.suspendedLanes & pingedLanes;
			root.warmLanes &= ~pingedLanes;
			0 !== (pingedLanes & 127) ? 0 > blockingUpdateTime && (blockingClampTime = blockingUpdateTime = now(), blockingUpdateTask = createTask("Promise Resolved"), blockingUpdateType = PINGED_UPDATE) : 0 !== (pingedLanes & 4194048) && 0 > transitionUpdateTime && (transitionClampTime = transitionUpdateTime = now(), transitionUpdateTask = createTask("Promise Resolved"), transitionUpdateType = PINGED_UPDATE);
			isConcurrentActEnvironment() && null === ReactSharedInternals.actQueue && console.error("A suspended resource finished loading inside a test, but the event was not wrapped in act(...).\n\nWhen testing, code that resolves suspended data should be wrapped into act(...):\n\nact(() => {\n  /* finish loading suspended data */\n});\n/* assert on the output */\n\nThis ensures that you're testing the behavior the user would see in the browser. Learn more at https://react.dev/link/wrap-tests-with-act");
			workInProgressRoot === root && (workInProgressRootRenderLanes & pingedLanes) === pingedLanes && (workInProgressRootExitStatus === RootSuspendedWithDelay || workInProgressRootExitStatus === RootSuspended && (workInProgressRootRenderLanes & 62914560) === workInProgressRootRenderLanes && now$1() - globalMostRecentFallbackTime < FALLBACK_THROTTLE_MS ? (executionContext & RenderContext) === NoContext && prepareFreshStack(root, 0) : workInProgressRootPingedLanes |= pingedLanes, workInProgressSuspendedRetryLanes === workInProgressRootRenderLanes && (workInProgressSuspendedRetryLanes = 0));
			ensureRootIsScheduled(root);
		}
		function retryTimedOutBoundary(boundaryFiber, retryLane) {
			0 === retryLane && (retryLane = claimNextRetryLane());
			boundaryFiber = enqueueConcurrentRenderForLane(boundaryFiber, retryLane);
			null !== boundaryFiber && (markRootUpdated$1(boundaryFiber, retryLane), ensureRootIsScheduled(boundaryFiber));
		}
		function retryDehydratedSuspenseBoundary(boundaryFiber) {
			var suspenseState = boundaryFiber.memoizedState, retryLane = 0;
			null !== suspenseState && (retryLane = suspenseState.retryLane);
			retryTimedOutBoundary(boundaryFiber, retryLane);
		}
		function resolveRetryWakeable(boundaryFiber, wakeable) {
			var retryLane = 0;
			switch (boundaryFiber.tag) {
				case 31:
				case 13:
					var retryCache = boundaryFiber.stateNode;
					var suspenseState = boundaryFiber.memoizedState;
					null !== suspenseState && (retryLane = suspenseState.retryLane);
					break;
				case 19:
					retryCache = boundaryFiber.stateNode;
					break;
				case 22:
					retryCache = boundaryFiber.stateNode._retryCache;
					break;
				default: throw Error("Pinged unknown suspense boundary type. This is probably a bug in React.");
			}
			null !== retryCache && retryCache.delete(wakeable);
			retryTimedOutBoundary(boundaryFiber, retryLane);
		}
		function recursivelyTraverseAndDoubleInvokeEffectsInDEV(root$jscomp$0, parentFiber, isInStrictMode) {
			if (0 !== (parentFiber.subtreeFlags & 67117056)) for (parentFiber = parentFiber.child; null !== parentFiber;) {
				var root = root$jscomp$0, fiber = parentFiber, isStrictModeFiber = fiber.type === REACT_STRICT_MODE_TYPE;
				isStrictModeFiber = isInStrictMode || isStrictModeFiber;
				22 !== fiber.tag ? fiber.flags & 67108864 ? isStrictModeFiber && runWithFiberInDEV(fiber, doubleInvokeEffectsOnFiber, root, fiber) : recursivelyTraverseAndDoubleInvokeEffectsInDEV(root, fiber, isStrictModeFiber) : null === fiber.memoizedState && (isStrictModeFiber && fiber.flags & 8192 ? runWithFiberInDEV(fiber, doubleInvokeEffectsOnFiber, root, fiber) : fiber.subtreeFlags & 67108864 && runWithFiberInDEV(fiber, recursivelyTraverseAndDoubleInvokeEffectsInDEV, root, fiber, isStrictModeFiber));
				parentFiber = parentFiber.sibling;
			}
		}
		function doubleInvokeEffectsOnFiber(root, fiber) {
			setIsStrictModeForDevtools(!0);
			try {
				disappearLayoutEffects(fiber), disconnectPassiveEffect(fiber), reappearLayoutEffects(root, fiber.alternate, fiber, !1), reconnectPassiveEffects(root, fiber, 0, null, !1, 0);
			} finally {
				setIsStrictModeForDevtools(!1);
			}
		}
		function commitDoubleInvokeEffectsInDEV(root) {
			var doubleInvokeEffects = !0;
			root.current.mode & (StrictLegacyMode | StrictEffectsMode) || (doubleInvokeEffects = !1);
			recursivelyTraverseAndDoubleInvokeEffectsInDEV(root, root.current, doubleInvokeEffects);
		}
		function warnAboutUpdateOnNotYetMountedFiberInDEV(fiber) {
			if ((executionContext & RenderContext) === NoContext) {
				var tag = fiber.tag;
				if (3 === tag || 1 === tag || 0 === tag || 11 === tag || 14 === tag || 15 === tag) {
					tag = getComponentNameFromFiber(fiber) || "ReactComponent";
					if (null !== didWarnStateUpdateForNotYetMountedComponent) {
						if (didWarnStateUpdateForNotYetMountedComponent.has(tag)) return;
						didWarnStateUpdateForNotYetMountedComponent.add(tag);
					} else didWarnStateUpdateForNotYetMountedComponent = /* @__PURE__ */ new Set([tag]);
					runWithFiberInDEV(fiber, function() {
						console.error("Can't perform a React state update on a component that hasn't mounted yet. This indicates that you have a side-effect in your render function that asynchronously tries to update the component. Move this work to useEffect instead.");
					});
				}
			}
		}
		function restorePendingUpdaters(root, lanes) {
			isDevToolsPresent && root.memoizedUpdaters.forEach(function(schedulingFiber) {
				addFiberToLanesMap(root, schedulingFiber, lanes);
			});
		}
		function scheduleCallback$1(priorityLevel, callback) {
			var actQueue = ReactSharedInternals.actQueue;
			return null !== actQueue ? (actQueue.push(callback), fakeActCallbackNode$1) : scheduleCallback$3(priorityLevel, callback);
		}
		function warnIfUpdatesNotWrappedWithActDEV(fiber) {
			isConcurrentActEnvironment() && null === ReactSharedInternals.actQueue && runWithFiberInDEV(fiber, function() {
				console.error("An update to %s inside a test was not wrapped in act(...).\n\nWhen testing, code that causes React state updates should be wrapped into act(...):\n\nact(() => {\n  /* fire events that update state */\n});\n/* assert on the output */\n\nThis ensures that you're testing the behavior the user would see in the browser. Learn more at https://react.dev/link/wrap-tests-with-act", getComponentNameFromFiber(fiber));
			});
		}
		function ensureRootIsScheduled(root) {
			root !== lastScheduledRoot && null === root.next && (null === lastScheduledRoot ? firstScheduledRoot = lastScheduledRoot = root : lastScheduledRoot = lastScheduledRoot.next = root);
			mightHavePendingSyncWork = !0;
			null !== ReactSharedInternals.actQueue ? didScheduleMicrotask_act || (didScheduleMicrotask_act = !0, scheduleImmediateRootScheduleTask()) : didScheduleMicrotask || (didScheduleMicrotask = !0, scheduleImmediateRootScheduleTask());
		}
		function flushSyncWorkAcrossRoots_impl(syncTransitionLanes, onlyLegacy) {
			if (!isFlushingWork && mightHavePendingSyncWork) {
				isFlushingWork = !0;
				do {
					var didPerformSomeWork = !1;
					for (var root = firstScheduledRoot; null !== root;) {
						if (!onlyLegacy) if (0 !== syncTransitionLanes) {
							var pendingLanes = root.pendingLanes;
							if (0 === pendingLanes) var nextLanes = 0;
							else {
								var suspendedLanes = root.suspendedLanes, pingedLanes = root.pingedLanes;
								nextLanes = (1 << 31 - clz32(42 | syncTransitionLanes) + 1) - 1;
								nextLanes &= pendingLanes & ~(suspendedLanes & ~pingedLanes);
								nextLanes = nextLanes & 201326741 ? nextLanes & 201326741 | 1 : nextLanes ? nextLanes | 2 : 0;
							}
							0 !== nextLanes && (didPerformSomeWork = !0, performSyncWorkOnRoot(root, nextLanes));
						} else nextLanes = workInProgressRootRenderLanes, nextLanes = getNextLanes(root, root === workInProgressRoot ? nextLanes : 0, null !== root.cancelPendingCommit || root.timeoutHandle !== noTimeout), 0 === (nextLanes & 3) || checkIfRootIsPrerendering(root, nextLanes) || (didPerformSomeWork = !0, performSyncWorkOnRoot(root, nextLanes));
						root = root.next;
					}
				} while (didPerformSomeWork);
				isFlushingWork = !1;
			}
		}
		function processRootScheduleInImmediateTask() {
			schedulerEvent = window.event;
			processRootScheduleInMicrotask();
		}
		function processRootScheduleInMicrotask() {
			mightHavePendingSyncWork = didScheduleMicrotask_act = didScheduleMicrotask = !1;
			var syncTransitionLanes = 0;
			0 !== currentEventTransitionLane && shouldAttemptEagerTransition() && (syncTransitionLanes = currentEventTransitionLane);
			for (var currentTime = now$1(), prev = null, root = firstScheduledRoot; null !== root;) {
				var next = root.next, nextLanes = scheduleTaskForRootDuringMicrotask(root, currentTime);
				if (0 === nextLanes) root.next = null, null === prev ? firstScheduledRoot = next : prev.next = next, null === next && (lastScheduledRoot = prev);
				else if (prev = root, 0 !== syncTransitionLanes || 0 !== (nextLanes & 3)) mightHavePendingSyncWork = !0;
				root = next;
			}
			pendingEffectsStatus !== NO_PENDING_EFFECTS && pendingEffectsStatus !== PENDING_PASSIVE_PHASE || flushSyncWorkAcrossRoots_impl(syncTransitionLanes, !1);
			0 !== currentEventTransitionLane && (currentEventTransitionLane = 0);
		}
		function scheduleTaskForRootDuringMicrotask(root, currentTime) {
			for (var suspendedLanes = root.suspendedLanes, pingedLanes = root.pingedLanes, expirationTimes = root.expirationTimes, lanes = root.pendingLanes & -62914561; 0 < lanes;) {
				var index = 31 - clz32(lanes), lane = 1 << index, expirationTime = expirationTimes[index];
				if (-1 === expirationTime) {
					if (0 === (lane & suspendedLanes) || 0 !== (lane & pingedLanes)) expirationTimes[index] = computeExpirationTime(lane, currentTime);
				} else expirationTime <= currentTime && (root.expiredLanes |= lane);
				lanes &= ~lane;
			}
			currentTime = workInProgressRoot;
			suspendedLanes = workInProgressRootRenderLanes;
			suspendedLanes = getNextLanes(root, root === currentTime ? suspendedLanes : 0, null !== root.cancelPendingCommit || root.timeoutHandle !== noTimeout);
			pingedLanes = root.callbackNode;
			if (0 === suspendedLanes || root === currentTime && (workInProgressSuspendedReason === SuspendedOnData || workInProgressSuspendedReason === SuspendedOnAction) || null !== root.cancelPendingCommit) return null !== pingedLanes && cancelCallback(pingedLanes), root.callbackNode = null, root.callbackPriority = 0;
			if (0 === (suspendedLanes & 3) || checkIfRootIsPrerendering(root, suspendedLanes)) {
				currentTime = suspendedLanes & -suspendedLanes;
				if (currentTime !== root.callbackPriority || null !== ReactSharedInternals.actQueue && pingedLanes !== fakeActCallbackNode) cancelCallback(pingedLanes);
				else return currentTime;
				switch (lanesToEventPriority(suspendedLanes)) {
					case DiscreteEventPriority:
					case ContinuousEventPriority:
						suspendedLanes = UserBlockingPriority;
						break;
					case DefaultEventPriority:
						suspendedLanes = NormalPriority$1;
						break;
					case IdleEventPriority:
						suspendedLanes = IdlePriority;
						break;
					default: suspendedLanes = NormalPriority$1;
				}
				pingedLanes = performWorkOnRootViaSchedulerTask.bind(null, root);
				null !== ReactSharedInternals.actQueue ? (ReactSharedInternals.actQueue.push(pingedLanes), suspendedLanes = fakeActCallbackNode) : suspendedLanes = scheduleCallback$3(suspendedLanes, pingedLanes);
				root.callbackPriority = currentTime;
				root.callbackNode = suspendedLanes;
				return currentTime;
			}
			null !== pingedLanes && cancelCallback(pingedLanes);
			root.callbackPriority = 2;
			root.callbackNode = null;
			return 2;
		}
		function performWorkOnRootViaSchedulerTask(root, didTimeout) {
			nestedUpdateScheduled = currentUpdateIsNested = !1;
			schedulerEvent = window.event;
			if (pendingEffectsStatus !== NO_PENDING_EFFECTS && pendingEffectsStatus !== PENDING_PASSIVE_PHASE) return root.callbackNode = null, root.callbackPriority = 0, null;
			var originalCallbackNode = root.callbackNode;
			pendingDelayedCommitReason === IMMEDIATE_COMMIT && (pendingDelayedCommitReason = DELAYED_PASSIVE_COMMIT);
			if (flushPendingEffects() && root.callbackNode !== originalCallbackNode) return null;
			var workInProgressRootRenderLanes$jscomp$0 = workInProgressRootRenderLanes;
			workInProgressRootRenderLanes$jscomp$0 = getNextLanes(root, root === workInProgressRoot ? workInProgressRootRenderLanes$jscomp$0 : 0, null !== root.cancelPendingCommit || root.timeoutHandle !== noTimeout);
			if (0 === workInProgressRootRenderLanes$jscomp$0) return null;
			performWorkOnRoot(root, workInProgressRootRenderLanes$jscomp$0, didTimeout);
			scheduleTaskForRootDuringMicrotask(root, now$1());
			return null != root.callbackNode && root.callbackNode === originalCallbackNode ? performWorkOnRootViaSchedulerTask.bind(null, root) : null;
		}
		function performSyncWorkOnRoot(root, lanes) {
			if (flushPendingEffects()) return null;
			currentUpdateIsNested = nestedUpdateScheduled;
			nestedUpdateScheduled = !1;
			performWorkOnRoot(root, lanes, !0);
		}
		function cancelCallback(callbackNode) {
			callbackNode !== fakeActCallbackNode && null !== callbackNode && cancelCallback$1(callbackNode);
		}
		function scheduleImmediateRootScheduleTask() {
			null !== ReactSharedInternals.actQueue && ReactSharedInternals.actQueue.push(function() {
				processRootScheduleInMicrotask();
				return null;
			});
			scheduleMicrotask(function() {
				(executionContext & (RenderContext | CommitContext)) !== NoContext ? scheduleCallback$3(ImmediatePriority, processRootScheduleInImmediateTask) : processRootScheduleInMicrotask();
			});
		}
		function requestTransitionLane() {
			if (0 === currentEventTransitionLane) {
				var actionScopeLane = currentEntangledLane;
				0 === actionScopeLane && (actionScopeLane = nextTransitionUpdateLane, nextTransitionUpdateLane <<= 1, 0 === (nextTransitionUpdateLane & 261888) && (nextTransitionUpdateLane = 256));
				currentEventTransitionLane = actionScopeLane;
			}
			return currentEventTransitionLane;
		}
		function coerceFormActionProp(actionProp) {
			if (null == actionProp || "symbol" === typeof actionProp || "boolean" === typeof actionProp) return null;
			if ("function" === typeof actionProp) return actionProp;
			checkAttributeStringCoercion(actionProp, "action");
			return sanitizeURL("" + actionProp);
		}
		function createFormDataWithSubmitter(form, submitter) {
			var temp = submitter.ownerDocument.createElement("input");
			temp.name = submitter.name;
			temp.value = submitter.value;
			form.id && temp.setAttribute("form", form.id);
			submitter.parentNode.insertBefore(temp, submitter);
			form = new FormData(form);
			temp.parentNode.removeChild(temp);
			return form;
		}
		function extractEvents$1(dispatchQueue, domEventName, maybeTargetInst, nativeEvent, nativeEventTarget) {
			if ("submit" === domEventName && maybeTargetInst && maybeTargetInst.stateNode === nativeEventTarget) {
				var action = coerceFormActionProp((nativeEventTarget[internalPropsKey] || null).action), submitter = nativeEvent.submitter;
				submitter && (domEventName = (domEventName = submitter[internalPropsKey] || null) ? coerceFormActionProp(domEventName.formAction) : submitter.getAttribute("formAction"), null !== domEventName && (action = domEventName, submitter = null));
				var event = new SyntheticEvent("action", "action", null, nativeEvent, nativeEventTarget);
				dispatchQueue.push({
					event,
					listeners: [{
						instance: null,
						listener: function() {
							if (nativeEvent.defaultPrevented) {
								if (0 !== currentEventTransitionLane) {
									var formData = submitter ? createFormDataWithSubmitter(nativeEventTarget, submitter) : new FormData(nativeEventTarget), pendingState = {
										pending: !0,
										data: formData,
										method: nativeEventTarget.method,
										action
									};
									Object.freeze(pendingState);
									startHostTransition(maybeTargetInst, pendingState, null, formData);
								}
							} else "function" === typeof action && (event.preventDefault(), formData = submitter ? createFormDataWithSubmitter(nativeEventTarget, submitter) : new FormData(nativeEventTarget), pendingState = {
								pending: !0,
								data: formData,
								method: nativeEventTarget.method,
								action
							}, Object.freeze(pendingState), startHostTransition(maybeTargetInst, pendingState, action, formData));
						},
						currentTarget: nativeEventTarget
					}]
				});
			}
		}
		function executeDispatch(event, listener, currentTarget) {
			event.currentTarget = currentTarget;
			try {
				listener(event);
			} catch (error) {
				reportGlobalError(error);
			}
			event.currentTarget = null;
		}
		function processDispatchQueue(dispatchQueue, eventSystemFlags) {
			eventSystemFlags = 0 !== (eventSystemFlags & 4);
			for (var i = 0; i < dispatchQueue.length; i++) {
				var _dispatchQueue$i = dispatchQueue[i];
				a: {
					var previousInstance = void 0, event = _dispatchQueue$i.event;
					_dispatchQueue$i = _dispatchQueue$i.listeners;
					if (eventSystemFlags) for (var i$jscomp$0 = _dispatchQueue$i.length - 1; 0 <= i$jscomp$0; i$jscomp$0--) {
						var _dispatchListeners$i = _dispatchQueue$i[i$jscomp$0], instance = _dispatchListeners$i.instance, currentTarget = _dispatchListeners$i.currentTarget;
						_dispatchListeners$i = _dispatchListeners$i.listener;
						if (instance !== previousInstance && event.isPropagationStopped()) break a;
						null !== instance ? runWithFiberInDEV(instance, executeDispatch, event, _dispatchListeners$i, currentTarget) : executeDispatch(event, _dispatchListeners$i, currentTarget);
						previousInstance = instance;
					}
					else for (i$jscomp$0 = 0; i$jscomp$0 < _dispatchQueue$i.length; i$jscomp$0++) {
						_dispatchListeners$i = _dispatchQueue$i[i$jscomp$0];
						instance = _dispatchListeners$i.instance;
						currentTarget = _dispatchListeners$i.currentTarget;
						_dispatchListeners$i = _dispatchListeners$i.listener;
						if (instance !== previousInstance && event.isPropagationStopped()) break a;
						null !== instance ? runWithFiberInDEV(instance, executeDispatch, event, _dispatchListeners$i, currentTarget) : executeDispatch(event, _dispatchListeners$i, currentTarget);
						previousInstance = instance;
					}
				}
			}
		}
		function listenToNonDelegatedEvent(domEventName, targetElement) {
			nonDelegatedEvents.has(domEventName) || console.error("Did not expect a listenToNonDelegatedEvent() call for \"%s\". This is a bug in React. Please file an issue.", domEventName);
			var listenerSet = targetElement[internalEventHandlersKey];
			void 0 === listenerSet && (listenerSet = targetElement[internalEventHandlersKey] = /* @__PURE__ */ new Set());
			var listenerSetKey = domEventName + "__bubble";
			listenerSet.has(listenerSetKey) || (addTrappedEventListener(targetElement, domEventName, 2, !1), listenerSet.add(listenerSetKey));
		}
		function listenToNativeEvent(domEventName, isCapturePhaseListener, target) {
			nonDelegatedEvents.has(domEventName) && !isCapturePhaseListener && console.error("Did not expect a listenToNativeEvent() call for \"%s\" in the bubble phase. This is a bug in React. Please file an issue.", domEventName);
			var eventSystemFlags = 0;
			isCapturePhaseListener && (eventSystemFlags |= 4);
			addTrappedEventListener(target, domEventName, eventSystemFlags, isCapturePhaseListener);
		}
		function listenToAllSupportedEvents(rootContainerElement) {
			if (!rootContainerElement[listeningMarker]) {
				rootContainerElement[listeningMarker] = !0;
				allNativeEvents.forEach(function(domEventName) {
					"selectionchange" !== domEventName && (nonDelegatedEvents.has(domEventName) || listenToNativeEvent(domEventName, !1, rootContainerElement), listenToNativeEvent(domEventName, !0, rootContainerElement));
				});
				var ownerDocument = 9 === rootContainerElement.nodeType ? rootContainerElement : rootContainerElement.ownerDocument;
				null === ownerDocument || ownerDocument[listeningMarker] || (ownerDocument[listeningMarker] = !0, listenToNativeEvent("selectionchange", !1, ownerDocument));
			}
		}
		function addTrappedEventListener(targetContainer, domEventName, eventSystemFlags, isCapturePhaseListener) {
			switch (getEventPriority(domEventName)) {
				case DiscreteEventPriority:
					var listenerWrapper = dispatchDiscreteEvent;
					break;
				case ContinuousEventPriority:
					listenerWrapper = dispatchContinuousEvent;
					break;
				default: listenerWrapper = dispatchEvent;
			}
			eventSystemFlags = listenerWrapper.bind(null, domEventName, eventSystemFlags, targetContainer);
			listenerWrapper = void 0;
			!passiveBrowserEventsSupported || "touchstart" !== domEventName && "touchmove" !== domEventName && "wheel" !== domEventName || (listenerWrapper = !0);
			isCapturePhaseListener ? void 0 !== listenerWrapper ? targetContainer.addEventListener(domEventName, eventSystemFlags, {
				capture: !0,
				passive: listenerWrapper
			}) : targetContainer.addEventListener(domEventName, eventSystemFlags, !0) : void 0 !== listenerWrapper ? targetContainer.addEventListener(domEventName, eventSystemFlags, { passive: listenerWrapper }) : targetContainer.addEventListener(domEventName, eventSystemFlags, !1);
		}
		function dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, targetInst$jscomp$0, targetContainer) {
			var ancestorInst = targetInst$jscomp$0;
			if (0 === (eventSystemFlags & 1) && 0 === (eventSystemFlags & 2) && null !== targetInst$jscomp$0) a: for (;;) {
				if (null === targetInst$jscomp$0) return;
				var nodeTag = targetInst$jscomp$0.tag;
				if (3 === nodeTag || 4 === nodeTag) {
					var container = targetInst$jscomp$0.stateNode.containerInfo;
					if (container === targetContainer) break;
					if (4 === nodeTag) for (nodeTag = targetInst$jscomp$0.return; null !== nodeTag;) {
						var grandTag = nodeTag.tag;
						if ((3 === grandTag || 4 === grandTag) && nodeTag.stateNode.containerInfo === targetContainer) return;
						nodeTag = nodeTag.return;
					}
					for (; null !== container;) {
						nodeTag = getClosestInstanceFromNode(container);
						if (null === nodeTag) return;
						grandTag = nodeTag.tag;
						if (5 === grandTag || 6 === grandTag || 26 === grandTag || 27 === grandTag) {
							targetInst$jscomp$0 = ancestorInst = nodeTag;
							continue a;
						}
						container = container.parentNode;
					}
				}
				targetInst$jscomp$0 = targetInst$jscomp$0.return;
			}
			batchedUpdates$1(function() {
				var targetInst = ancestorInst, nativeEventTarget = getEventTarget(nativeEvent), dispatchQueue = [];
				a: {
					var reactName = topLevelEventsToReactNames.get(domEventName);
					if (void 0 !== reactName) {
						var SyntheticEventCtor = SyntheticEvent, reactEventType = domEventName;
						switch (domEventName) {
							case "keypress": if (0 === getEventCharCode(nativeEvent)) break a;
							case "keydown":
							case "keyup":
								SyntheticEventCtor = SyntheticKeyboardEvent;
								break;
							case "focusin":
								reactEventType = "focus";
								SyntheticEventCtor = SyntheticFocusEvent;
								break;
							case "focusout":
								reactEventType = "blur";
								SyntheticEventCtor = SyntheticFocusEvent;
								break;
							case "beforeblur":
							case "afterblur":
								SyntheticEventCtor = SyntheticFocusEvent;
								break;
							case "click": if (2 === nativeEvent.button) break a;
							case "auxclick":
							case "dblclick":
							case "mousedown":
							case "mousemove":
							case "mouseup":
							case "mouseout":
							case "mouseover":
							case "contextmenu":
								SyntheticEventCtor = SyntheticMouseEvent;
								break;
							case "drag":
							case "dragend":
							case "dragenter":
							case "dragexit":
							case "dragleave":
							case "dragover":
							case "dragstart":
							case "drop":
								SyntheticEventCtor = SyntheticDragEvent;
								break;
							case "touchcancel":
							case "touchend":
							case "touchmove":
							case "touchstart":
								SyntheticEventCtor = SyntheticTouchEvent;
								break;
							case ANIMATION_END:
							case ANIMATION_ITERATION:
							case ANIMATION_START:
								SyntheticEventCtor = SyntheticAnimationEvent;
								break;
							case TRANSITION_END:
								SyntheticEventCtor = SyntheticTransitionEvent;
								break;
							case "scroll":
							case "scrollend":
								SyntheticEventCtor = SyntheticUIEvent;
								break;
							case "wheel":
								SyntheticEventCtor = SyntheticWheelEvent;
								break;
							case "copy":
							case "cut":
							case "paste":
								SyntheticEventCtor = SyntheticClipboardEvent;
								break;
							case "gotpointercapture":
							case "lostpointercapture":
							case "pointercancel":
							case "pointerdown":
							case "pointermove":
							case "pointerout":
							case "pointerover":
							case "pointerup":
								SyntheticEventCtor = SyntheticPointerEvent;
								break;
							case "toggle":
							case "beforetoggle": SyntheticEventCtor = SyntheticToggleEvent;
						}
						var inCapturePhase = 0 !== (eventSystemFlags & 4), accumulateTargetOnly = !inCapturePhase && ("scroll" === domEventName || "scrollend" === domEventName), reactEventName = inCapturePhase ? null !== reactName ? reactName + "Capture" : null : reactName;
						inCapturePhase = [];
						for (var instance = targetInst, lastHostComponent; null !== instance;) {
							var _instance2 = instance;
							lastHostComponent = _instance2.stateNode;
							_instance2 = _instance2.tag;
							5 !== _instance2 && 26 !== _instance2 && 27 !== _instance2 || null === lastHostComponent || null === reactEventName || (_instance2 = getListener(instance, reactEventName), null != _instance2 && inCapturePhase.push(createDispatchListener(instance, _instance2, lastHostComponent)));
							if (accumulateTargetOnly) break;
							instance = instance.return;
						}
						0 < inCapturePhase.length && (reactName = new SyntheticEventCtor(reactName, reactEventType, null, nativeEvent, nativeEventTarget), dispatchQueue.push({
							event: reactName,
							listeners: inCapturePhase
						}));
					}
				}
				if (0 === (eventSystemFlags & 7)) {
					a: {
						reactName = "mouseover" === domEventName || "pointerover" === domEventName;
						SyntheticEventCtor = "mouseout" === domEventName || "pointerout" === domEventName;
						if (reactName && nativeEvent !== currentReplayingEvent && (reactEventType = nativeEvent.relatedTarget || nativeEvent.fromElement) && (getClosestInstanceFromNode(reactEventType) || reactEventType[internalContainerInstanceKey])) break a;
						if (SyntheticEventCtor || reactName) {
							reactName = nativeEventTarget.window === nativeEventTarget ? nativeEventTarget : (reactName = nativeEventTarget.ownerDocument) ? reactName.defaultView || reactName.parentWindow : window;
							if (SyntheticEventCtor) {
								if (reactEventType = nativeEvent.relatedTarget || nativeEvent.toElement, SyntheticEventCtor = targetInst, reactEventType = reactEventType ? getClosestInstanceFromNode(reactEventType) : null, null !== reactEventType && (accumulateTargetOnly = getNearestMountedFiber(reactEventType), inCapturePhase = reactEventType.tag, reactEventType !== accumulateTargetOnly || 5 !== inCapturePhase && 27 !== inCapturePhase && 6 !== inCapturePhase)) reactEventType = null;
							} else SyntheticEventCtor = null, reactEventType = targetInst;
							if (SyntheticEventCtor !== reactEventType) {
								inCapturePhase = SyntheticMouseEvent;
								_instance2 = "onMouseLeave";
								reactEventName = "onMouseEnter";
								instance = "mouse";
								if ("pointerout" === domEventName || "pointerover" === domEventName) inCapturePhase = SyntheticPointerEvent, _instance2 = "onPointerLeave", reactEventName = "onPointerEnter", instance = "pointer";
								accumulateTargetOnly = null == SyntheticEventCtor ? reactName : getNodeFromInstance(SyntheticEventCtor);
								lastHostComponent = null == reactEventType ? reactName : getNodeFromInstance(reactEventType);
								reactName = new inCapturePhase(_instance2, instance + "leave", SyntheticEventCtor, nativeEvent, nativeEventTarget);
								reactName.target = accumulateTargetOnly;
								reactName.relatedTarget = lastHostComponent;
								_instance2 = null;
								getClosestInstanceFromNode(nativeEventTarget) === targetInst && (inCapturePhase = new inCapturePhase(reactEventName, instance + "enter", reactEventType, nativeEvent, nativeEventTarget), inCapturePhase.target = lastHostComponent, inCapturePhase.relatedTarget = accumulateTargetOnly, _instance2 = inCapturePhase);
								accumulateTargetOnly = _instance2;
								if (SyntheticEventCtor && reactEventType) b: {
									inCapturePhase = getParent;
									reactEventName = SyntheticEventCtor;
									instance = reactEventType;
									lastHostComponent = 0;
									for (_instance2 = reactEventName; _instance2; _instance2 = inCapturePhase(_instance2)) lastHostComponent++;
									_instance2 = 0;
									for (var tempB = instance; tempB; tempB = inCapturePhase(tempB)) _instance2++;
									for (; 0 < lastHostComponent - _instance2;) reactEventName = inCapturePhase(reactEventName), lastHostComponent--;
									for (; 0 < _instance2 - lastHostComponent;) instance = inCapturePhase(instance), _instance2--;
									for (; lastHostComponent--;) {
										if (reactEventName === instance || null !== instance && reactEventName === instance.alternate) {
											inCapturePhase = reactEventName;
											break b;
										}
										reactEventName = inCapturePhase(reactEventName);
										instance = inCapturePhase(instance);
									}
									inCapturePhase = null;
								}
								else inCapturePhase = null;
								null !== SyntheticEventCtor && accumulateEnterLeaveListenersForEvent(dispatchQueue, reactName, SyntheticEventCtor, inCapturePhase, !1);
								null !== reactEventType && null !== accumulateTargetOnly && accumulateEnterLeaveListenersForEvent(dispatchQueue, accumulateTargetOnly, reactEventType, inCapturePhase, !0);
							}
						}
					}
					a: {
						reactName = targetInst ? getNodeFromInstance(targetInst) : window;
						SyntheticEventCtor = reactName.nodeName && reactName.nodeName.toLowerCase();
						if ("select" === SyntheticEventCtor || "input" === SyntheticEventCtor && "file" === reactName.type) var getTargetInstFunc = getTargetInstForChangeEvent;
						else if (isTextInputElement(reactName)) if (isInputEventSupported) getTargetInstFunc = getTargetInstForInputOrChangeEvent;
						else {
							getTargetInstFunc = getTargetInstForInputEventPolyfill;
							var handleEventFunc = handleEventsForInputEventPolyfill;
						}
						else SyntheticEventCtor = reactName.nodeName, !SyntheticEventCtor || "input" !== SyntheticEventCtor.toLowerCase() || "checkbox" !== reactName.type && "radio" !== reactName.type ? targetInst && isCustomElement(targetInst.elementType) && (getTargetInstFunc = getTargetInstForChangeEvent) : getTargetInstFunc = getTargetInstForClickEvent;
						if (getTargetInstFunc && (getTargetInstFunc = getTargetInstFunc(domEventName, targetInst))) {
							createAndAccumulateChangeEvent(dispatchQueue, getTargetInstFunc, nativeEvent, nativeEventTarget);
							break a;
						}
						handleEventFunc && handleEventFunc(domEventName, reactName, targetInst);
						"focusout" === domEventName && targetInst && "number" === reactName.type && null != targetInst.memoizedProps.value && setDefaultValue(reactName, "number", reactName.value);
					}
					handleEventFunc = targetInst ? getNodeFromInstance(targetInst) : window;
					switch (domEventName) {
						case "focusin":
							if (isTextInputElement(handleEventFunc) || "true" === handleEventFunc.contentEditable) activeElement = handleEventFunc, activeElementInst = targetInst, lastSelection = null;
							break;
						case "focusout":
							lastSelection = activeElementInst = activeElement = null;
							break;
						case "mousedown":
							mouseDown = !0;
							break;
						case "contextmenu":
						case "mouseup":
						case "dragend":
							mouseDown = !1;
							constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
							break;
						case "selectionchange": if (skipSelectionChangeEvent) break;
						case "keydown":
						case "keyup": constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
					}
					var fallbackData;
					if (canUseCompositionEvent) b: {
						switch (domEventName) {
							case "compositionstart":
								var eventType = "onCompositionStart";
								break b;
							case "compositionend":
								eventType = "onCompositionEnd";
								break b;
							case "compositionupdate":
								eventType = "onCompositionUpdate";
								break b;
						}
						eventType = void 0;
					}
					else isComposing ? isFallbackCompositionEnd(domEventName, nativeEvent) && (eventType = "onCompositionEnd") : "keydown" === domEventName && nativeEvent.keyCode === START_KEYCODE && (eventType = "onCompositionStart");
					eventType && (useFallbackCompositionData && "ko" !== nativeEvent.locale && (isComposing || "onCompositionStart" !== eventType ? "onCompositionEnd" === eventType && isComposing && (fallbackData = getData()) : (root = nativeEventTarget, startText = "value" in root ? root.value : root.textContent, isComposing = !0)), handleEventFunc = accumulateTwoPhaseListeners(targetInst, eventType), 0 < handleEventFunc.length && (eventType = new SyntheticCompositionEvent(eventType, domEventName, null, nativeEvent, nativeEventTarget), dispatchQueue.push({
						event: eventType,
						listeners: handleEventFunc
					}), fallbackData ? eventType.data = fallbackData : (fallbackData = getDataFromCustomEvent(nativeEvent), null !== fallbackData && (eventType.data = fallbackData))));
					if (fallbackData = canUseTextInputEvent ? getNativeBeforeInputChars(domEventName, nativeEvent) : getFallbackBeforeInputChars(domEventName, nativeEvent)) eventType = accumulateTwoPhaseListeners(targetInst, "onBeforeInput"), 0 < eventType.length && (handleEventFunc = new SyntheticInputEvent("onBeforeInput", "beforeinput", null, nativeEvent, nativeEventTarget), dispatchQueue.push({
						event: handleEventFunc,
						listeners: eventType
					}), handleEventFunc.data = fallbackData);
					extractEvents$1(dispatchQueue, domEventName, targetInst, nativeEvent, nativeEventTarget);
				}
				processDispatchQueue(dispatchQueue, eventSystemFlags);
			});
		}
		function createDispatchListener(instance, listener, currentTarget) {
			return {
				instance,
				listener,
				currentTarget
			};
		}
		function accumulateTwoPhaseListeners(targetFiber, reactName) {
			for (var captureName = reactName + "Capture", listeners = []; null !== targetFiber;) {
				var _instance3 = targetFiber, stateNode = _instance3.stateNode;
				_instance3 = _instance3.tag;
				5 !== _instance3 && 26 !== _instance3 && 27 !== _instance3 || null === stateNode || (_instance3 = getListener(targetFiber, captureName), null != _instance3 && listeners.unshift(createDispatchListener(targetFiber, _instance3, stateNode)), _instance3 = getListener(targetFiber, reactName), null != _instance3 && listeners.push(createDispatchListener(targetFiber, _instance3, stateNode)));
				if (3 === targetFiber.tag) return listeners;
				targetFiber = targetFiber.return;
			}
			return [];
		}
		function getParent(inst) {
			if (null === inst) return null;
			do
				inst = inst.return;
			while (inst && 5 !== inst.tag && 27 !== inst.tag);
			return inst ? inst : null;
		}
		function accumulateEnterLeaveListenersForEvent(dispatchQueue, event, target, common, inCapturePhase) {
			for (var registrationName = event._reactName, listeners = []; null !== target && target !== common;) {
				var _instance4 = target, alternate = _instance4.alternate, stateNode = _instance4.stateNode;
				_instance4 = _instance4.tag;
				if (null !== alternate && alternate === common) break;
				5 !== _instance4 && 26 !== _instance4 && 27 !== _instance4 || null === stateNode || (alternate = stateNode, inCapturePhase ? (stateNode = getListener(target, registrationName), null != stateNode && listeners.unshift(createDispatchListener(target, stateNode, alternate))) : inCapturePhase || (stateNode = getListener(target, registrationName), null != stateNode && listeners.push(createDispatchListener(target, stateNode, alternate))));
				target = target.return;
			}
			0 !== listeners.length && dispatchQueue.push({
				event,
				listeners
			});
		}
		function validatePropertiesInDevelopment(type, props) {
			validateProperties$2(type, props);
			"input" !== type && "textarea" !== type && "select" !== type || null == props || null !== props.value || didWarnValueNull || (didWarnValueNull = !0, "select" === type && props.multiple ? console.error("`value` prop on `%s` should not be null. Consider using an empty array when `multiple` is set to `true` to clear the component or `undefined` for uncontrolled components.", type) : console.error("`value` prop on `%s` should not be null. Consider using an empty string to clear the component or `undefined` for uncontrolled components.", type));
			var eventRegistry = {
				registrationNameDependencies,
				possibleRegistrationNames
			};
			isCustomElement(type) || "string" === typeof props.is || warnUnknownProperties(type, props, eventRegistry);
			props.contentEditable && !props.suppressContentEditableWarning && null != props.children && console.error("A component is `contentEditable` and contains `children` managed by React. It is now your responsibility to guarantee that none of those nodes are unexpectedly modified or duplicated. This is probably not intentional.");
		}
		function warnForPropDifference(propName, serverValue, clientValue, serverDifferences) {
			serverValue !== clientValue && (clientValue = normalizeMarkupForTextOrAttribute(clientValue), normalizeMarkupForTextOrAttribute(serverValue) !== clientValue && (serverDifferences[propName] = serverValue));
		}
		function warnForExtraAttributes(domElement, attributeNames, serverDifferences) {
			attributeNames.forEach(function(attributeName) {
				serverDifferences[getPropNameFromAttributeName(attributeName)] = "style" === attributeName ? getStylesObjectFromElement(domElement) : domElement.getAttribute(attributeName);
			});
		}
		function warnForInvalidEventListener(registrationName, listener) {
			!1 === listener ? console.error("Expected `%s` listener to be a function, instead got `false`.\n\nIf you used to conditionally omit it with %s={condition && value}, pass %s={condition ? value : undefined} instead.", registrationName, registrationName, registrationName) : console.error("Expected `%s` listener to be a function, instead got a value of `%s` type.", registrationName, typeof listener);
		}
		function normalizeHTML(parent, html) {
			parent = parent.namespaceURI === MATH_NAMESPACE || parent.namespaceURI === SVG_NAMESPACE ? parent.ownerDocument.createElementNS(parent.namespaceURI, parent.tagName) : parent.ownerDocument.createElement(parent.tagName);
			parent.innerHTML = html;
			return parent.innerHTML;
		}
		function normalizeMarkupForTextOrAttribute(markup) {
			willCoercionThrow(markup) && (console.error("The provided HTML markup uses a value of unsupported type %s. This value must be coerced to a string before using it here.", typeName(markup)), testStringCoercion(markup));
			return ("string" === typeof markup ? markup : "" + markup).replace(NORMALIZE_NEWLINES_REGEX, "\n").replace(NORMALIZE_NULL_AND_REPLACEMENT_REGEX, "");
		}
		function checkForUnmatchedText(serverText, clientText) {
			clientText = normalizeMarkupForTextOrAttribute(clientText);
			return normalizeMarkupForTextOrAttribute(serverText) === clientText ? !0 : !1;
		}
		function setProp(domElement, tag, key, value, props, prevValue) {
			switch (key) {
				case "children":
					if ("string" === typeof value) validateTextNesting(value, tag, !1), "body" === tag || "textarea" === tag && "" === value || setTextContent(domElement, value);
					else if ("number" === typeof value || "bigint" === typeof value) validateTextNesting("" + value, tag, !1), "body" !== tag && setTextContent(domElement, "" + value);
					break;
				case "className":
					setValueForKnownAttribute(domElement, "class", value);
					break;
				case "tabIndex":
					setValueForKnownAttribute(domElement, "tabindex", value);
					break;
				case "dir":
				case "role":
				case "viewBox":
				case "width":
				case "height":
					setValueForKnownAttribute(domElement, key, value);
					break;
				case "style":
					setValueForStyles(domElement, value, prevValue);
					break;
				case "data": if ("object" !== tag) {
					setValueForKnownAttribute(domElement, "data", value);
					break;
				}
				case "src":
				case "href":
					if ("" === value && ("a" !== tag || "href" !== key)) {
						"src" === key ? console.error("An empty string (\"\") was passed to the %s attribute. This may cause the browser to download the whole page again over the network. To fix this, either do not render the element at all or pass null to %s instead of an empty string.", key, key) : console.error("An empty string (\"\") was passed to the %s attribute. To fix this, either do not render the element at all or pass null to %s instead of an empty string.", key, key);
						domElement.removeAttribute(key);
						break;
					}
					if (null == value || "function" === typeof value || "symbol" === typeof value || "boolean" === typeof value) {
						domElement.removeAttribute(key);
						break;
					}
					checkAttributeStringCoercion(value, key);
					value = sanitizeURL("" + value);
					domElement.setAttribute(key, value);
					break;
				case "action":
				case "formAction":
					null != value && ("form" === tag ? "formAction" === key ? console.error("You can only pass the formAction prop to <input> or <button>. Use the action prop on <form>.") : "function" === typeof value && (null == props.encType && null == props.method || didWarnFormActionMethod || (didWarnFormActionMethod = !0, console.error("Cannot specify a encType or method for a form that specifies a function as the action. React provides those automatically. They will get overridden.")), null == props.target || didWarnFormActionTarget || (didWarnFormActionTarget = !0, console.error("Cannot specify a target for a form that specifies a function as the action. The function will always be executed in the same window."))) : "input" === tag || "button" === tag ? "action" === key ? console.error("You can only pass the action prop to <form>. Use the formAction prop on <input> or <button>.") : "input" !== tag || "submit" === props.type || "image" === props.type || didWarnFormActionType ? "button" !== tag || null == props.type || "submit" === props.type || didWarnFormActionType ? "function" === typeof value && (null == props.name || didWarnFormActionName || (didWarnFormActionName = !0, console.error("Cannot specify a \"name\" prop for a button that specifies a function as a formAction. React needs it to encode which action should be invoked. It will get overridden.")), null == props.formEncType && null == props.formMethod || didWarnFormActionMethod || (didWarnFormActionMethod = !0, console.error("Cannot specify a formEncType or formMethod for a button that specifies a function as a formAction. React provides those automatically. They will get overridden.")), null == props.formTarget || didWarnFormActionTarget || (didWarnFormActionTarget = !0, console.error("Cannot specify a formTarget for a button that specifies a function as a formAction. The function will always be executed in the same window."))) : (didWarnFormActionType = !0, console.error("A button can only specify a formAction along with type=\"submit\" or no type.")) : (didWarnFormActionType = !0, console.error("An input can only specify a formAction along with type=\"submit\" or type=\"image\".")) : "action" === key ? console.error("You can only pass the action prop to <form>.") : console.error("You can only pass the formAction prop to <input> or <button>."));
					if ("function" === typeof value) {
						domElement.setAttribute(key, "javascript:throw new Error('A React form was unexpectedly submitted. If you called form.submit() manually, consider using form.requestSubmit() instead. If you\\'re trying to use event.stopPropagation() in a submit event handler, consider also calling event.preventDefault().')");
						break;
					} else "function" === typeof prevValue && ("formAction" === key ? ("input" !== tag && setProp(domElement, tag, "name", props.name, props, null), setProp(domElement, tag, "formEncType", props.formEncType, props, null), setProp(domElement, tag, "formMethod", props.formMethod, props, null), setProp(domElement, tag, "formTarget", props.formTarget, props, null)) : (setProp(domElement, tag, "encType", props.encType, props, null), setProp(domElement, tag, "method", props.method, props, null), setProp(domElement, tag, "target", props.target, props, null)));
					if (null == value || "symbol" === typeof value || "boolean" === typeof value) {
						domElement.removeAttribute(key);
						break;
					}
					checkAttributeStringCoercion(value, key);
					value = sanitizeURL("" + value);
					domElement.setAttribute(key, value);
					break;
				case "onClick":
					null != value && ("function" !== typeof value && warnForInvalidEventListener(key, value), domElement.onclick = noop$1);
					break;
				case "onScroll":
					null != value && ("function" !== typeof value && warnForInvalidEventListener(key, value), listenToNonDelegatedEvent("scroll", domElement));
					break;
				case "onScrollEnd":
					null != value && ("function" !== typeof value && warnForInvalidEventListener(key, value), listenToNonDelegatedEvent("scrollend", domElement));
					break;
				case "dangerouslySetInnerHTML":
					if (null != value) {
						if ("object" !== typeof value || !("__html" in value)) throw Error("`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://react.dev/link/dangerously-set-inner-html for more information.");
						key = value.__html;
						if (null != key) {
							if (null != props.children) throw Error("Can only set one of `children` or `props.dangerouslySetInnerHTML`.");
							domElement.innerHTML = key;
						}
					}
					break;
				case "multiple":
					domElement.multiple = value && "function" !== typeof value && "symbol" !== typeof value;
					break;
				case "muted":
					domElement.muted = value && "function" !== typeof value && "symbol" !== typeof value;
					break;
				case "suppressContentEditableWarning":
				case "suppressHydrationWarning":
				case "defaultValue":
				case "defaultChecked":
				case "innerHTML":
				case "ref": break;
				case "autoFocus": break;
				case "xlinkHref":
					if (null == value || "function" === typeof value || "boolean" === typeof value || "symbol" === typeof value) {
						domElement.removeAttribute("xlink:href");
						break;
					}
					checkAttributeStringCoercion(value, key);
					key = sanitizeURL("" + value);
					domElement.setAttributeNS(xlinkNamespace, "xlink:href", key);
					break;
				case "contentEditable":
				case "spellCheck":
				case "draggable":
				case "value":
				case "autoReverse":
				case "externalResourcesRequired":
				case "focusable":
				case "preserveAlpha":
					null != value && "function" !== typeof value && "symbol" !== typeof value ? (checkAttributeStringCoercion(value, key), domElement.setAttribute(key, "" + value)) : domElement.removeAttribute(key);
					break;
				case "inert": "" !== value || didWarnForNewBooleanPropsWithEmptyValue[key] || (didWarnForNewBooleanPropsWithEmptyValue[key] = !0, console.error("Received an empty string for a boolean attribute `%s`. This will treat the attribute as if it were false. Either pass `false` to silence this warning, or pass `true` if you used an empty string in earlier versions of React to indicate this attribute is true.", key));
				case "allowFullScreen":
				case "async":
				case "autoPlay":
				case "controls":
				case "default":
				case "defer":
				case "disabled":
				case "disablePictureInPicture":
				case "disableRemotePlayback":
				case "formNoValidate":
				case "hidden":
				case "loop":
				case "noModule":
				case "noValidate":
				case "open":
				case "playsInline":
				case "readOnly":
				case "required":
				case "reversed":
				case "scoped":
				case "seamless":
				case "itemScope":
					value && "function" !== typeof value && "symbol" !== typeof value ? domElement.setAttribute(key, "") : domElement.removeAttribute(key);
					break;
				case "capture":
				case "download":
					!0 === value ? domElement.setAttribute(key, "") : !1 !== value && null != value && "function" !== typeof value && "symbol" !== typeof value ? (checkAttributeStringCoercion(value, key), domElement.setAttribute(key, value)) : domElement.removeAttribute(key);
					break;
				case "cols":
				case "rows":
				case "size":
				case "span":
					null != value && "function" !== typeof value && "symbol" !== typeof value && !isNaN(value) && 1 <= value ? (checkAttributeStringCoercion(value, key), domElement.setAttribute(key, value)) : domElement.removeAttribute(key);
					break;
				case "rowSpan":
				case "start":
					null == value || "function" === typeof value || "symbol" === typeof value || isNaN(value) ? domElement.removeAttribute(key) : (checkAttributeStringCoercion(value, key), domElement.setAttribute(key, value));
					break;
				case "popover":
					listenToNonDelegatedEvent("beforetoggle", domElement);
					listenToNonDelegatedEvent("toggle", domElement);
					setValueForAttribute(domElement, "popover", value);
					break;
				case "xlinkActuate":
					setValueForNamespacedAttribute(domElement, xlinkNamespace, "xlink:actuate", value);
					break;
				case "xlinkArcrole":
					setValueForNamespacedAttribute(domElement, xlinkNamespace, "xlink:arcrole", value);
					break;
				case "xlinkRole":
					setValueForNamespacedAttribute(domElement, xlinkNamespace, "xlink:role", value);
					break;
				case "xlinkShow":
					setValueForNamespacedAttribute(domElement, xlinkNamespace, "xlink:show", value);
					break;
				case "xlinkTitle":
					setValueForNamespacedAttribute(domElement, xlinkNamespace, "xlink:title", value);
					break;
				case "xlinkType":
					setValueForNamespacedAttribute(domElement, xlinkNamespace, "xlink:type", value);
					break;
				case "xmlBase":
					setValueForNamespacedAttribute(domElement, xmlNamespace, "xml:base", value);
					break;
				case "xmlLang":
					setValueForNamespacedAttribute(domElement, xmlNamespace, "xml:lang", value);
					break;
				case "xmlSpace":
					setValueForNamespacedAttribute(domElement, xmlNamespace, "xml:space", value);
					break;
				case "is":
					null != prevValue && console.error("Cannot update the \"is\" prop after it has been initialized.");
					setValueForAttribute(domElement, "is", value);
					break;
				case "innerText":
				case "textContent": break;
				case "popoverTarget": didWarnPopoverTargetObject || null == value || "object" !== typeof value || (didWarnPopoverTargetObject = !0, console.error("The `popoverTarget` prop expects the ID of an Element as a string. Received %s instead.", value));
				default: !(2 < key.length) || "o" !== key[0] && "O" !== key[0] || "n" !== key[1] && "N" !== key[1] ? (key = getAttributeAlias(key), setValueForAttribute(domElement, key, value)) : registrationNameDependencies.hasOwnProperty(key) && null != value && "function" !== typeof value && warnForInvalidEventListener(key, value);
			}
		}
		function setPropOnCustomElement(domElement, tag, key, value, props, prevValue) {
			switch (key) {
				case "style":
					setValueForStyles(domElement, value, prevValue);
					break;
				case "dangerouslySetInnerHTML":
					if (null != value) {
						if ("object" !== typeof value || !("__html" in value)) throw Error("`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://react.dev/link/dangerously-set-inner-html for more information.");
						key = value.__html;
						if (null != key) {
							if (null != props.children) throw Error("Can only set one of `children` or `props.dangerouslySetInnerHTML`.");
							domElement.innerHTML = key;
						}
					}
					break;
				case "children":
					"string" === typeof value ? setTextContent(domElement, value) : ("number" === typeof value || "bigint" === typeof value) && setTextContent(domElement, "" + value);
					break;
				case "onScroll":
					null != value && ("function" !== typeof value && warnForInvalidEventListener(key, value), listenToNonDelegatedEvent("scroll", domElement));
					break;
				case "onScrollEnd":
					null != value && ("function" !== typeof value && warnForInvalidEventListener(key, value), listenToNonDelegatedEvent("scrollend", domElement));
					break;
				case "onClick":
					null != value && ("function" !== typeof value && warnForInvalidEventListener(key, value), domElement.onclick = noop$1);
					break;
				case "suppressContentEditableWarning":
				case "suppressHydrationWarning":
				case "innerHTML":
				case "ref": break;
				case "innerText":
				case "textContent": break;
				default: if (registrationNameDependencies.hasOwnProperty(key)) null != value && "function" !== typeof value && warnForInvalidEventListener(key, value);
				else a: {
					if ("o" === key[0] && "n" === key[1] && (props = key.endsWith("Capture"), tag = key.slice(2, props ? key.length - 7 : void 0), prevValue = domElement[internalPropsKey] || null, prevValue = null != prevValue ? prevValue[key] : null, "function" === typeof prevValue && domElement.removeEventListener(tag, prevValue, props), "function" === typeof value)) {
						"function" !== typeof prevValue && null !== prevValue && (key in domElement ? domElement[key] = null : domElement.hasAttribute(key) && domElement.removeAttribute(key));
						domElement.addEventListener(tag, value, props);
						break a;
					}
					key in domElement ? domElement[key] = value : !0 === value ? domElement.setAttribute(key, "") : setValueForAttribute(domElement, key, value);
				}
			}
		}
		function setInitialProperties(domElement, tag, props) {
			validatePropertiesInDevelopment(tag, props);
			switch (tag) {
				case "div":
				case "span":
				case "svg":
				case "path":
				case "a":
				case "g":
				case "p":
				case "li": break;
				case "img":
					listenToNonDelegatedEvent("error", domElement);
					listenToNonDelegatedEvent("load", domElement);
					var hasSrc = !1, hasSrcSet = !1, propKey;
					for (propKey in props) if (props.hasOwnProperty(propKey)) {
						var propValue = props[propKey];
						if (null != propValue) switch (propKey) {
							case "src":
								hasSrc = !0;
								break;
							case "srcSet":
								hasSrcSet = !0;
								break;
							case "children":
							case "dangerouslySetInnerHTML": throw Error(tag + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`.");
							default: setProp(domElement, tag, propKey, propValue, props, null);
						}
					}
					hasSrcSet && setProp(domElement, tag, "srcSet", props.srcSet, props, null);
					hasSrc && setProp(domElement, tag, "src", props.src, props, null);
					return;
				case "input":
					checkControlledValueProps("input", props);
					listenToNonDelegatedEvent("invalid", domElement);
					var defaultValue = propKey = propValue = hasSrcSet = null, checked = null, defaultChecked = null;
					for (hasSrc in props) if (props.hasOwnProperty(hasSrc)) {
						var _propValue = props[hasSrc];
						if (null != _propValue) switch (hasSrc) {
							case "name":
								hasSrcSet = _propValue;
								break;
							case "type":
								propValue = _propValue;
								break;
							case "checked":
								checked = _propValue;
								break;
							case "defaultChecked":
								defaultChecked = _propValue;
								break;
							case "value":
								propKey = _propValue;
								break;
							case "defaultValue":
								defaultValue = _propValue;
								break;
							case "children":
							case "dangerouslySetInnerHTML":
								if (null != _propValue) throw Error(tag + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`.");
								break;
							default: setProp(domElement, tag, hasSrc, _propValue, props, null);
						}
					}
					validateInputProps(domElement, props);
					initInput(domElement, propKey, defaultValue, checked, defaultChecked, propValue, hasSrcSet, !1);
					return;
				case "select":
					checkControlledValueProps("select", props);
					listenToNonDelegatedEvent("invalid", domElement);
					hasSrc = propValue = propKey = null;
					for (hasSrcSet in props) if (props.hasOwnProperty(hasSrcSet) && (defaultValue = props[hasSrcSet], null != defaultValue)) switch (hasSrcSet) {
						case "value":
							propKey = defaultValue;
							break;
						case "defaultValue":
							propValue = defaultValue;
							break;
						case "multiple": hasSrc = defaultValue;
						default: setProp(domElement, tag, hasSrcSet, defaultValue, props, null);
					}
					validateSelectProps(domElement, props);
					tag = propKey;
					props = propValue;
					domElement.multiple = !!hasSrc;
					null != tag ? updateOptions(domElement, !!hasSrc, tag, !1) : null != props && updateOptions(domElement, !!hasSrc, props, !0);
					return;
				case "textarea":
					checkControlledValueProps("textarea", props);
					listenToNonDelegatedEvent("invalid", domElement);
					propKey = hasSrcSet = hasSrc = null;
					for (propValue in props) if (props.hasOwnProperty(propValue) && (defaultValue = props[propValue], null != defaultValue)) switch (propValue) {
						case "value":
							hasSrc = defaultValue;
							break;
						case "defaultValue":
							hasSrcSet = defaultValue;
							break;
						case "children":
							propKey = defaultValue;
							break;
						case "dangerouslySetInnerHTML":
							if (null != defaultValue) throw Error("`dangerouslySetInnerHTML` does not make sense on <textarea>.");
							break;
						default: setProp(domElement, tag, propValue, defaultValue, props, null);
					}
					validateTextareaProps(domElement, props);
					initTextarea(domElement, hasSrc, hasSrcSet, propKey);
					return;
				case "option":
					validateOptionProps(domElement, props);
					for (checked in props) if (props.hasOwnProperty(checked) && (hasSrc = props[checked], null != hasSrc)) switch (checked) {
						case "selected":
							domElement.selected = hasSrc && "function" !== typeof hasSrc && "symbol" !== typeof hasSrc;
							break;
						default: setProp(domElement, tag, checked, hasSrc, props, null);
					}
					return;
				case "dialog":
					listenToNonDelegatedEvent("beforetoggle", domElement);
					listenToNonDelegatedEvent("toggle", domElement);
					listenToNonDelegatedEvent("cancel", domElement);
					listenToNonDelegatedEvent("close", domElement);
					break;
				case "iframe":
				case "object":
					listenToNonDelegatedEvent("load", domElement);
					break;
				case "video":
				case "audio":
					for (hasSrc = 0; hasSrc < mediaEventTypes.length; hasSrc++) listenToNonDelegatedEvent(mediaEventTypes[hasSrc], domElement);
					break;
				case "image":
					listenToNonDelegatedEvent("error", domElement);
					listenToNonDelegatedEvent("load", domElement);
					break;
				case "details":
					listenToNonDelegatedEvent("toggle", domElement);
					break;
				case "embed":
				case "source":
				case "link": listenToNonDelegatedEvent("error", domElement), listenToNonDelegatedEvent("load", domElement);
				case "area":
				case "base":
				case "br":
				case "col":
				case "hr":
				case "keygen":
				case "meta":
				case "param":
				case "track":
				case "wbr":
				case "menuitem":
					for (defaultChecked in props) if (props.hasOwnProperty(defaultChecked) && (hasSrc = props[defaultChecked], null != hasSrc)) switch (defaultChecked) {
						case "children":
						case "dangerouslySetInnerHTML": throw Error(tag + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`.");
						default: setProp(domElement, tag, defaultChecked, hasSrc, props, null);
					}
					return;
				default: if (isCustomElement(tag)) {
					for (_propValue in props) props.hasOwnProperty(_propValue) && (hasSrc = props[_propValue], void 0 !== hasSrc && setPropOnCustomElement(domElement, tag, _propValue, hasSrc, props, void 0));
					return;
				}
			}
			for (defaultValue in props) props.hasOwnProperty(defaultValue) && (hasSrc = props[defaultValue], null != hasSrc && setProp(domElement, tag, defaultValue, hasSrc, props, null));
		}
		function updateProperties(domElement, tag, lastProps, nextProps) {
			validatePropertiesInDevelopment(tag, nextProps);
			switch (tag) {
				case "div":
				case "span":
				case "svg":
				case "path":
				case "a":
				case "g":
				case "p":
				case "li": break;
				case "input":
					var name = null, type = null, value = null, defaultValue = null, lastDefaultValue = null, checked = null, defaultChecked = null;
					for (propKey in lastProps) {
						var lastProp = lastProps[propKey];
						if (lastProps.hasOwnProperty(propKey) && null != lastProp) switch (propKey) {
							case "checked": break;
							case "value": break;
							case "defaultValue": lastDefaultValue = lastProp;
							default: nextProps.hasOwnProperty(propKey) || setProp(domElement, tag, propKey, null, nextProps, lastProp);
						}
					}
					for (var _propKey8 in nextProps) {
						var propKey = nextProps[_propKey8];
						lastProp = lastProps[_propKey8];
						if (nextProps.hasOwnProperty(_propKey8) && (null != propKey || null != lastProp)) switch (_propKey8) {
							case "type":
								type = propKey;
								break;
							case "name":
								name = propKey;
								break;
							case "checked":
								checked = propKey;
								break;
							case "defaultChecked":
								defaultChecked = propKey;
								break;
							case "value":
								value = propKey;
								break;
							case "defaultValue":
								defaultValue = propKey;
								break;
							case "children":
							case "dangerouslySetInnerHTML":
								if (null != propKey) throw Error(tag + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`.");
								break;
							default: propKey !== lastProp && setProp(domElement, tag, _propKey8, propKey, nextProps, lastProp);
						}
					}
					tag = "checkbox" === lastProps.type || "radio" === lastProps.type ? null != lastProps.checked : null != lastProps.value;
					nextProps = "checkbox" === nextProps.type || "radio" === nextProps.type ? null != nextProps.checked : null != nextProps.value;
					tag || !nextProps || didWarnUncontrolledToControlled || (console.error("A component is changing an uncontrolled input to be controlled. This is likely caused by the value changing from undefined to a defined value, which should not happen. Decide between using a controlled or uncontrolled input element for the lifetime of the component. More info: https://react.dev/link/controlled-components"), didWarnUncontrolledToControlled = !0);
					!tag || nextProps || didWarnControlledToUncontrolled || (console.error("A component is changing a controlled input to be uncontrolled. This is likely caused by the value changing from a defined to undefined, which should not happen. Decide between using a controlled or uncontrolled input element for the lifetime of the component. More info: https://react.dev/link/controlled-components"), didWarnControlledToUncontrolled = !0);
					updateInput(domElement, value, defaultValue, lastDefaultValue, checked, defaultChecked, type, name);
					return;
				case "select":
					propKey = value = defaultValue = _propKey8 = null;
					for (type in lastProps) if (lastDefaultValue = lastProps[type], lastProps.hasOwnProperty(type) && null != lastDefaultValue) switch (type) {
						case "value": break;
						case "multiple": propKey = lastDefaultValue;
						default: nextProps.hasOwnProperty(type) || setProp(domElement, tag, type, null, nextProps, lastDefaultValue);
					}
					for (name in nextProps) if (type = nextProps[name], lastDefaultValue = lastProps[name], nextProps.hasOwnProperty(name) && (null != type || null != lastDefaultValue)) switch (name) {
						case "value":
							_propKey8 = type;
							break;
						case "defaultValue":
							defaultValue = type;
							break;
						case "multiple": value = type;
						default: type !== lastDefaultValue && setProp(domElement, tag, name, type, nextProps, lastDefaultValue);
					}
					nextProps = defaultValue;
					tag = value;
					lastProps = propKey;
					null != _propKey8 ? updateOptions(domElement, !!tag, _propKey8, !1) : !!lastProps !== !!tag && (null != nextProps ? updateOptions(domElement, !!tag, nextProps, !0) : updateOptions(domElement, !!tag, tag ? [] : "", !1));
					return;
				case "textarea":
					propKey = _propKey8 = null;
					for (defaultValue in lastProps) if (name = lastProps[defaultValue], lastProps.hasOwnProperty(defaultValue) && null != name && !nextProps.hasOwnProperty(defaultValue)) switch (defaultValue) {
						case "value": break;
						case "children": break;
						default: setProp(domElement, tag, defaultValue, null, nextProps, name);
					}
					for (value in nextProps) if (name = nextProps[value], type = lastProps[value], nextProps.hasOwnProperty(value) && (null != name || null != type)) switch (value) {
						case "value":
							_propKey8 = name;
							break;
						case "defaultValue":
							propKey = name;
							break;
						case "children": break;
						case "dangerouslySetInnerHTML":
							if (null != name) throw Error("`dangerouslySetInnerHTML` does not make sense on <textarea>.");
							break;
						default: name !== type && setProp(domElement, tag, value, name, nextProps, type);
					}
					updateTextarea(domElement, _propKey8, propKey);
					return;
				case "option":
					for (var _propKey13 in lastProps) if (_propKey8 = lastProps[_propKey13], lastProps.hasOwnProperty(_propKey13) && null != _propKey8 && !nextProps.hasOwnProperty(_propKey13)) switch (_propKey13) {
						case "selected":
							domElement.selected = !1;
							break;
						default: setProp(domElement, tag, _propKey13, null, nextProps, _propKey8);
					}
					for (lastDefaultValue in nextProps) if (_propKey8 = nextProps[lastDefaultValue], propKey = lastProps[lastDefaultValue], nextProps.hasOwnProperty(lastDefaultValue) && _propKey8 !== propKey && (null != _propKey8 || null != propKey)) switch (lastDefaultValue) {
						case "selected":
							domElement.selected = _propKey8 && "function" !== typeof _propKey8 && "symbol" !== typeof _propKey8;
							break;
						default: setProp(domElement, tag, lastDefaultValue, _propKey8, nextProps, propKey);
					}
					return;
				case "img":
				case "link":
				case "area":
				case "base":
				case "br":
				case "col":
				case "embed":
				case "hr":
				case "keygen":
				case "meta":
				case "param":
				case "source":
				case "track":
				case "wbr":
				case "menuitem":
					for (var _propKey15 in lastProps) _propKey8 = lastProps[_propKey15], lastProps.hasOwnProperty(_propKey15) && null != _propKey8 && !nextProps.hasOwnProperty(_propKey15) && setProp(domElement, tag, _propKey15, null, nextProps, _propKey8);
					for (checked in nextProps) if (_propKey8 = nextProps[checked], propKey = lastProps[checked], nextProps.hasOwnProperty(checked) && _propKey8 !== propKey && (null != _propKey8 || null != propKey)) switch (checked) {
						case "children":
						case "dangerouslySetInnerHTML":
							if (null != _propKey8) throw Error(tag + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`.");
							break;
						default: setProp(domElement, tag, checked, _propKey8, nextProps, propKey);
					}
					return;
				default: if (isCustomElement(tag)) {
					for (var _propKey17 in lastProps) _propKey8 = lastProps[_propKey17], lastProps.hasOwnProperty(_propKey17) && void 0 !== _propKey8 && !nextProps.hasOwnProperty(_propKey17) && setPropOnCustomElement(domElement, tag, _propKey17, void 0, nextProps, _propKey8);
					for (defaultChecked in nextProps) _propKey8 = nextProps[defaultChecked], propKey = lastProps[defaultChecked], !nextProps.hasOwnProperty(defaultChecked) || _propKey8 === propKey || void 0 === _propKey8 && void 0 === propKey || setPropOnCustomElement(domElement, tag, defaultChecked, _propKey8, nextProps, propKey);
					return;
				}
			}
			for (var _propKey19 in lastProps) _propKey8 = lastProps[_propKey19], lastProps.hasOwnProperty(_propKey19) && null != _propKey8 && !nextProps.hasOwnProperty(_propKey19) && setProp(domElement, tag, _propKey19, null, nextProps, _propKey8);
			for (lastProp in nextProps) _propKey8 = nextProps[lastProp], propKey = lastProps[lastProp], !nextProps.hasOwnProperty(lastProp) || _propKey8 === propKey || null == _propKey8 && null == propKey || setProp(domElement, tag, lastProp, _propKey8, nextProps, propKey);
		}
		function getPropNameFromAttributeName(attrName) {
			switch (attrName) {
				case "class": return "className";
				case "for": return "htmlFor";
				default: return attrName;
			}
		}
		function getStylesObjectFromElement(domElement) {
			var serverValueInObjectForm = {};
			domElement = domElement.style;
			for (var i = 0; i < domElement.length; i++) {
				var styleName = domElement[i];
				serverValueInObjectForm[styleName] = domElement.getPropertyValue(styleName);
			}
			return serverValueInObjectForm;
		}
		function diffHydratedStyles(domElement, value$jscomp$0, serverDifferences) {
			if (null != value$jscomp$0 && "object" !== typeof value$jscomp$0) console.error("The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX.");
			else {
				var clientValue;
				var delimiter = clientValue = "", styleName;
				for (styleName in value$jscomp$0) if (value$jscomp$0.hasOwnProperty(styleName)) {
					var value = value$jscomp$0[styleName];
					null != value && "boolean" !== typeof value && "" !== value && (0 === styleName.indexOf("--") ? (checkCSSPropertyStringCoercion(value, styleName), clientValue += delimiter + styleName + ":" + ("" + value).trim()) : "number" !== typeof value || 0 === value || unitlessNumbers.has(styleName) ? (checkCSSPropertyStringCoercion(value, styleName), clientValue += delimiter + styleName.replace(uppercasePattern, "-$1").toLowerCase().replace(msPattern$1, "-ms-") + ":" + ("" + value).trim()) : clientValue += delimiter + styleName.replace(uppercasePattern, "-$1").toLowerCase().replace(msPattern$1, "-ms-") + ":" + value + "px", delimiter = ";");
				}
				clientValue = clientValue || null;
				value$jscomp$0 = domElement.getAttribute("style");
				value$jscomp$0 !== clientValue && (clientValue = normalizeMarkupForTextOrAttribute(clientValue), normalizeMarkupForTextOrAttribute(value$jscomp$0) !== clientValue && (serverDifferences.style = getStylesObjectFromElement(domElement)));
			}
		}
		function hydrateAttribute(domElement, propKey, attributeName, value, extraAttributes, serverDifferences) {
			extraAttributes.delete(attributeName);
			domElement = domElement.getAttribute(attributeName);
			if (null === domElement) switch (typeof value) {
				case "undefined":
				case "function":
				case "symbol":
				case "boolean": return;
			}
			else if (null != value) switch (typeof value) {
				case "function":
				case "symbol":
				case "boolean": break;
				default: if (checkAttributeStringCoercion(value, propKey), domElement === "" + value) return;
			}
			warnForPropDifference(propKey, domElement, value, serverDifferences);
		}
		function hydrateBooleanAttribute(domElement, propKey, attributeName, value, extraAttributes, serverDifferences) {
			extraAttributes.delete(attributeName);
			domElement = domElement.getAttribute(attributeName);
			if (null === domElement) {
				switch (typeof value) {
					case "function":
					case "symbol": return;
				}
				if (!value) return;
			} else switch (typeof value) {
				case "function":
				case "symbol": break;
				default: if (value) return;
			}
			warnForPropDifference(propKey, domElement, value, serverDifferences);
		}
		function hydrateBooleanishAttribute(domElement, propKey, attributeName, value, extraAttributes, serverDifferences) {
			extraAttributes.delete(attributeName);
			domElement = domElement.getAttribute(attributeName);
			if (null === domElement) switch (typeof value) {
				case "undefined":
				case "function":
				case "symbol": return;
			}
			else if (null != value) switch (typeof value) {
				case "function":
				case "symbol": break;
				default: if (checkAttributeStringCoercion(value, attributeName), domElement === "" + value) return;
			}
			warnForPropDifference(propKey, domElement, value, serverDifferences);
		}
		function hydrateNumericAttribute(domElement, propKey, attributeName, value, extraAttributes, serverDifferences) {
			extraAttributes.delete(attributeName);
			domElement = domElement.getAttribute(attributeName);
			if (null === domElement) switch (typeof value) {
				case "undefined":
				case "function":
				case "symbol":
				case "boolean": return;
				default: if (isNaN(value)) return;
			}
			else if (null != value) switch (typeof value) {
				case "function":
				case "symbol":
				case "boolean": break;
				default: if (!isNaN(value) && (checkAttributeStringCoercion(value, propKey), domElement === "" + value)) return;
			}
			warnForPropDifference(propKey, domElement, value, serverDifferences);
		}
		function hydrateSanitizedAttribute(domElement, propKey, attributeName, value, extraAttributes, serverDifferences) {
			extraAttributes.delete(attributeName);
			domElement = domElement.getAttribute(attributeName);
			if (null === domElement) switch (typeof value) {
				case "undefined":
				case "function":
				case "symbol":
				case "boolean": return;
			}
			else if (null != value) switch (typeof value) {
				case "function":
				case "symbol":
				case "boolean": break;
				default: if (checkAttributeStringCoercion(value, propKey), attributeName = sanitizeURL("" + value), domElement === attributeName) return;
			}
			warnForPropDifference(propKey, domElement, value, serverDifferences);
		}
		function diffHydratedProperties(domElement, tag, props, hostContext) {
			for (var serverDifferences = {}, extraAttributes = /* @__PURE__ */ new Set(), attributes = domElement.attributes, i = 0; i < attributes.length; i++) switch (attributes[i].name.toLowerCase()) {
				case "value": break;
				case "checked": break;
				case "selected": break;
				default: extraAttributes.add(attributes[i].name);
			}
			if (isCustomElement(tag)) {
				for (var propKey in props) if (props.hasOwnProperty(propKey)) {
					var value = props[propKey];
					if (null != value) {
						if (registrationNameDependencies.hasOwnProperty(propKey)) "function" !== typeof value && warnForInvalidEventListener(propKey, value);
						else if (!0 !== props.suppressHydrationWarning) switch (propKey) {
							case "children":
								"string" !== typeof value && "number" !== typeof value || warnForPropDifference("children", domElement.textContent, value, serverDifferences);
								continue;
							case "suppressContentEditableWarning":
							case "suppressHydrationWarning":
							case "defaultValue":
							case "defaultChecked":
							case "innerHTML":
							case "ref": continue;
							case "dangerouslySetInnerHTML":
								attributes = domElement.innerHTML;
								value = value ? value.__html : void 0;
								null != value && (value = normalizeHTML(domElement, value), warnForPropDifference(propKey, attributes, value, serverDifferences));
								continue;
							case "style":
								extraAttributes.delete(propKey);
								diffHydratedStyles(domElement, value, serverDifferences);
								continue;
							case "offsetParent":
							case "offsetTop":
							case "offsetLeft":
							case "offsetWidth":
							case "offsetHeight":
							case "isContentEditable":
							case "outerText":
							case "outerHTML":
								extraAttributes.delete(propKey.toLowerCase());
								console.error("Assignment to read-only property will result in a no-op: `%s`", propKey);
								continue;
							case "className":
								extraAttributes.delete("class");
								attributes = getValueForAttributeOnCustomComponent(domElement, "class", value);
								warnForPropDifference("className", attributes, value, serverDifferences);
								continue;
							default: hostContext.context === HostContextNamespaceNone && "svg" !== tag && "math" !== tag ? extraAttributes.delete(propKey.toLowerCase()) : extraAttributes.delete(propKey), attributes = getValueForAttributeOnCustomComponent(domElement, propKey, value), warnForPropDifference(propKey, attributes, value, serverDifferences);
						}
					}
				}
			} else for (value in props) if (props.hasOwnProperty(value) && (propKey = props[value], null != propKey)) {
				if (registrationNameDependencies.hasOwnProperty(value)) "function" !== typeof propKey && warnForInvalidEventListener(value, propKey);
				else if (!0 !== props.suppressHydrationWarning) switch (value) {
					case "children":
						"string" !== typeof propKey && "number" !== typeof propKey || warnForPropDifference("children", domElement.textContent, propKey, serverDifferences);
						continue;
					case "suppressContentEditableWarning":
					case "suppressHydrationWarning":
					case "value":
					case "checked":
					case "selected":
					case "defaultValue":
					case "defaultChecked":
					case "innerHTML":
					case "ref": continue;
					case "dangerouslySetInnerHTML":
						attributes = domElement.innerHTML;
						propKey = propKey ? propKey.__html : void 0;
						null != propKey && (propKey = normalizeHTML(domElement, propKey), attributes !== propKey && (serverDifferences[value] = { __html: attributes }));
						continue;
					case "className":
						hydrateAttribute(domElement, value, "class", propKey, extraAttributes, serverDifferences);
						continue;
					case "tabIndex":
						hydrateAttribute(domElement, value, "tabindex", propKey, extraAttributes, serverDifferences);
						continue;
					case "style":
						extraAttributes.delete(value);
						diffHydratedStyles(domElement, propKey, serverDifferences);
						continue;
					case "multiple":
						extraAttributes.delete(value);
						warnForPropDifference(value, domElement.multiple, propKey, serverDifferences);
						continue;
					case "muted":
						extraAttributes.delete(value);
						warnForPropDifference(value, domElement.muted, propKey, serverDifferences);
						continue;
					case "autoFocus":
						extraAttributes.delete("autofocus");
						warnForPropDifference(value, domElement.autofocus, propKey, serverDifferences);
						continue;
					case "data": if ("object" !== tag) {
						extraAttributes.delete(value);
						attributes = domElement.getAttribute("data");
						warnForPropDifference(value, attributes, propKey, serverDifferences);
						continue;
					}
					case "src":
					case "href":
						if (!("" !== propKey || "a" === tag && "href" === value || "object" === tag && "data" === value)) {
							"src" === value ? console.error("An empty string (\"\") was passed to the %s attribute. This may cause the browser to download the whole page again over the network. To fix this, either do not render the element at all or pass null to %s instead of an empty string.", value, value) : console.error("An empty string (\"\") was passed to the %s attribute. To fix this, either do not render the element at all or pass null to %s instead of an empty string.", value, value);
							continue;
						}
						hydrateSanitizedAttribute(domElement, value, value, propKey, extraAttributes, serverDifferences);
						continue;
					case "action":
					case "formAction":
						attributes = domElement.getAttribute(value);
						if ("function" === typeof propKey) {
							extraAttributes.delete(value.toLowerCase());
							"formAction" === value ? (extraAttributes.delete("name"), extraAttributes.delete("formenctype"), extraAttributes.delete("formmethod"), extraAttributes.delete("formtarget")) : (extraAttributes.delete("enctype"), extraAttributes.delete("method"), extraAttributes.delete("target"));
							continue;
						} else if (attributes === EXPECTED_FORM_ACTION_URL) {
							extraAttributes.delete(value.toLowerCase());
							warnForPropDifference(value, "function", propKey, serverDifferences);
							continue;
						}
						hydrateSanitizedAttribute(domElement, value, value.toLowerCase(), propKey, extraAttributes, serverDifferences);
						continue;
					case "xlinkHref":
						hydrateSanitizedAttribute(domElement, value, "xlink:href", propKey, extraAttributes, serverDifferences);
						continue;
					case "contentEditable":
						hydrateBooleanishAttribute(domElement, value, "contenteditable", propKey, extraAttributes, serverDifferences);
						continue;
					case "spellCheck":
						hydrateBooleanishAttribute(domElement, value, "spellcheck", propKey, extraAttributes, serverDifferences);
						continue;
					case "draggable":
					case "autoReverse":
					case "externalResourcesRequired":
					case "focusable":
					case "preserveAlpha":
						hydrateBooleanishAttribute(domElement, value, value, propKey, extraAttributes, serverDifferences);
						continue;
					case "allowFullScreen":
					case "async":
					case "autoPlay":
					case "controls":
					case "default":
					case "defer":
					case "disabled":
					case "disablePictureInPicture":
					case "disableRemotePlayback":
					case "formNoValidate":
					case "hidden":
					case "loop":
					case "noModule":
					case "noValidate":
					case "open":
					case "playsInline":
					case "readOnly":
					case "required":
					case "reversed":
					case "scoped":
					case "seamless":
					case "itemScope":
						hydrateBooleanAttribute(domElement, value, value.toLowerCase(), propKey, extraAttributes, serverDifferences);
						continue;
					case "capture":
					case "download":
						a: {
							i = domElement;
							var attributeName = attributes = value, serverDifferences$jscomp$0 = serverDifferences;
							extraAttributes.delete(attributeName);
							i = i.getAttribute(attributeName);
							if (null === i) switch (typeof propKey) {
								case "undefined":
								case "function":
								case "symbol": break a;
								default: if (!1 === propKey) break a;
							}
							else if (null != propKey) switch (typeof propKey) {
								case "function":
								case "symbol": break;
								case "boolean":
									if (!0 === propKey && "" === i) break a;
									break;
								default: if (checkAttributeStringCoercion(propKey, attributes), i === "" + propKey) break a;
							}
							warnForPropDifference(attributes, i, propKey, serverDifferences$jscomp$0);
						}
						continue;
					case "cols":
					case "rows":
					case "size":
					case "span":
						a: {
							i = domElement;
							attributeName = attributes = value;
							serverDifferences$jscomp$0 = serverDifferences;
							extraAttributes.delete(attributeName);
							i = i.getAttribute(attributeName);
							if (null === i) switch (typeof propKey) {
								case "undefined":
								case "function":
								case "symbol":
								case "boolean": break a;
								default: if (isNaN(propKey) || 1 > propKey) break a;
							}
							else if (null != propKey) switch (typeof propKey) {
								case "function":
								case "symbol":
								case "boolean": break;
								default: if (!(isNaN(propKey) || 1 > propKey) && (checkAttributeStringCoercion(propKey, attributes), i === "" + propKey)) break a;
							}
							warnForPropDifference(attributes, i, propKey, serverDifferences$jscomp$0);
						}
						continue;
					case "rowSpan":
						hydrateNumericAttribute(domElement, value, "rowspan", propKey, extraAttributes, serverDifferences);
						continue;
					case "start":
						hydrateNumericAttribute(domElement, value, value, propKey, extraAttributes, serverDifferences);
						continue;
					case "xHeight":
						hydrateAttribute(domElement, value, "x-height", propKey, extraAttributes, serverDifferences);
						continue;
					case "xlinkActuate":
						hydrateAttribute(domElement, value, "xlink:actuate", propKey, extraAttributes, serverDifferences);
						continue;
					case "xlinkArcrole":
						hydrateAttribute(domElement, value, "xlink:arcrole", propKey, extraAttributes, serverDifferences);
						continue;
					case "xlinkRole":
						hydrateAttribute(domElement, value, "xlink:role", propKey, extraAttributes, serverDifferences);
						continue;
					case "xlinkShow":
						hydrateAttribute(domElement, value, "xlink:show", propKey, extraAttributes, serverDifferences);
						continue;
					case "xlinkTitle":
						hydrateAttribute(domElement, value, "xlink:title", propKey, extraAttributes, serverDifferences);
						continue;
					case "xlinkType":
						hydrateAttribute(domElement, value, "xlink:type", propKey, extraAttributes, serverDifferences);
						continue;
					case "xmlBase":
						hydrateAttribute(domElement, value, "xml:base", propKey, extraAttributes, serverDifferences);
						continue;
					case "xmlLang":
						hydrateAttribute(domElement, value, "xml:lang", propKey, extraAttributes, serverDifferences);
						continue;
					case "xmlSpace":
						hydrateAttribute(domElement, value, "xml:space", propKey, extraAttributes, serverDifferences);
						continue;
					case "inert":
						"" !== propKey || didWarnForNewBooleanPropsWithEmptyValue[value] || (didWarnForNewBooleanPropsWithEmptyValue[value] = !0, console.error("Received an empty string for a boolean attribute `%s`. This will treat the attribute as if it were false. Either pass `false` to silence this warning, or pass `true` if you used an empty string in earlier versions of React to indicate this attribute is true.", value));
						hydrateBooleanAttribute(domElement, value, value, propKey, extraAttributes, serverDifferences);
						continue;
					default: if (!(2 < value.length) || "o" !== value[0] && "O" !== value[0] || "n" !== value[1] && "N" !== value[1]) {
						i = getAttributeAlias(value);
						attributes = !1;
						hostContext.context === HostContextNamespaceNone && "svg" !== tag && "math" !== tag ? extraAttributes.delete(i.toLowerCase()) : (attributeName = value.toLowerCase(), attributeName = possibleStandardNames.hasOwnProperty(attributeName) ? possibleStandardNames[attributeName] || null : null, null !== attributeName && attributeName !== value && (attributes = !0, extraAttributes.delete(attributeName)), extraAttributes.delete(i));
						a: if (attributeName = domElement, serverDifferences$jscomp$0 = i, i = propKey, isAttributeNameSafe(serverDifferences$jscomp$0)) if (attributeName.hasAttribute(serverDifferences$jscomp$0)) attributeName = attributeName.getAttribute(serverDifferences$jscomp$0), checkAttributeStringCoercion(i, serverDifferences$jscomp$0), i = attributeName === "" + i ? i : attributeName;
						else {
							switch (typeof i) {
								case "function":
								case "symbol": break a;
								case "boolean": if (attributeName = serverDifferences$jscomp$0.toLowerCase().slice(0, 5), "data-" !== attributeName && "aria-" !== attributeName) break a;
							}
							i = void 0 === i ? void 0 : null;
						}
						else i = void 0;
						attributes || warnForPropDifference(value, i, propKey, serverDifferences);
					}
				}
			}
			0 < extraAttributes.size && !0 !== props.suppressHydrationWarning && warnForExtraAttributes(domElement, extraAttributes, serverDifferences);
			return 0 === Object.keys(serverDifferences).length ? null : serverDifferences;
		}
		function propNamesListJoin(list, combinator) {
			switch (list.length) {
				case 0: return "";
				case 1: return list[0];
				case 2: return list[0] + " " + combinator + " " + list[1];
				default: return list.slice(0, -1).join(", ") + ", " + combinator + " " + list[list.length - 1];
			}
		}
		function isLikelyStaticResource(initiatorType) {
			switch (initiatorType) {
				case "css":
				case "script":
				case "font":
				case "img":
				case "image":
				case "input":
				case "link": return !0;
				default: return !1;
			}
		}
		function estimateBandwidth() {
			if ("function" === typeof performance.getEntriesByType) {
				for (var count = 0, bits = 0, resourceEntries = performance.getEntriesByType("resource"), i = 0; i < resourceEntries.length; i++) {
					var entry = resourceEntries[i], transferSize = entry.transferSize, initiatorType = entry.initiatorType, duration = entry.duration;
					if (transferSize && duration && isLikelyStaticResource(initiatorType)) {
						initiatorType = 0;
						duration = entry.responseEnd;
						for (i += 1; i < resourceEntries.length; i++) {
							var overlapEntry = resourceEntries[i], overlapStartTime = overlapEntry.startTime;
							if (overlapStartTime > duration) break;
							var overlapTransferSize = overlapEntry.transferSize, overlapInitiatorType = overlapEntry.initiatorType;
							overlapTransferSize && isLikelyStaticResource(overlapInitiatorType) && (overlapEntry = overlapEntry.responseEnd, initiatorType += overlapTransferSize * (overlapEntry < duration ? 1 : (duration - overlapStartTime) / (overlapEntry - overlapStartTime)));
						}
						--i;
						bits += 8 * (transferSize + initiatorType) / (entry.duration / 1e3);
						count++;
						if (10 < count) break;
					}
				}
				if (0 < count) return bits / count / 1e6;
			}
			return navigator.connection && (count = navigator.connection.downlink, "number" === typeof count) ? count : 5;
		}
		function getOwnerDocumentFromRootContainer(rootContainerElement) {
			return 9 === rootContainerElement.nodeType ? rootContainerElement : rootContainerElement.ownerDocument;
		}
		function getOwnHostContext(namespaceURI) {
			switch (namespaceURI) {
				case SVG_NAMESPACE: return HostContextNamespaceSvg;
				case MATH_NAMESPACE: return HostContextNamespaceMath;
				default: return HostContextNamespaceNone;
			}
		}
		function getChildHostContextProd(parentNamespace, type) {
			if (parentNamespace === HostContextNamespaceNone) switch (type) {
				case "svg": return HostContextNamespaceSvg;
				case "math": return HostContextNamespaceMath;
				default: return HostContextNamespaceNone;
			}
			return parentNamespace === HostContextNamespaceSvg && "foreignObject" === type ? HostContextNamespaceNone : parentNamespace;
		}
		function shouldSetTextContent(type, props) {
			return "textarea" === type || "noscript" === type || "string" === typeof props.children || "number" === typeof props.children || "bigint" === typeof props.children || "object" === typeof props.dangerouslySetInnerHTML && null !== props.dangerouslySetInnerHTML && null != props.dangerouslySetInnerHTML.__html;
		}
		function shouldAttemptEagerTransition() {
			var event = window.event;
			if (event && "popstate" === event.type) {
				if (event === currentPopstateTransitionEvent) return !1;
				currentPopstateTransitionEvent = event;
				return !0;
			}
			currentPopstateTransitionEvent = null;
			return !1;
		}
		function resolveEventType() {
			var event = window.event;
			return event && event !== schedulerEvent ? event.type : null;
		}
		function resolveEventTimeStamp() {
			var event = window.event;
			return event && event !== schedulerEvent ? event.timeStamp : -1.1;
		}
		function handleErrorInNextTick(error) {
			setTimeout(function() {
				throw error;
			});
		}
		function commitMount(domElement, type, newProps) {
			switch (type) {
				case "button":
				case "input":
				case "select":
				case "textarea":
					newProps.autoFocus && domElement.focus();
					break;
				case "img": newProps.src ? domElement.src = newProps.src : newProps.srcSet && (domElement.srcset = newProps.srcSet);
			}
		}
		function commitHydratedInstance() {}
		function commitUpdate(domElement, type, oldProps, newProps) {
			updateProperties(domElement, type, oldProps, newProps);
			domElement[internalPropsKey] = newProps;
		}
		function resetTextContent(domElement) {
			setTextContent(domElement, "");
		}
		function commitTextUpdate(textInstance, oldText, newText) {
			textInstance.nodeValue = newText;
		}
		function warnForReactChildrenConflict(container) {
			if (!container.__reactWarnedAboutChildrenConflict) {
				var props = container[internalPropsKey] || null;
				if (null !== props) {
					var fiber = getInstanceFromNode(container);
					null !== fiber && ("string" === typeof props.children || "number" === typeof props.children ? (container.__reactWarnedAboutChildrenConflict = !0, runWithFiberInDEV(fiber, function() {
						console.error("Cannot use a ref on a React element as a container to `createRoot` or `createPortal` if that element also sets \"children\" text content using React. It should be a leaf with no children. Otherwise it's ambiguous which children should be used.");
					})) : null != props.dangerouslySetInnerHTML && (container.__reactWarnedAboutChildrenConflict = !0, runWithFiberInDEV(fiber, function() {
						console.error("Cannot use a ref on a React element as a container to `createRoot` or `createPortal` if that element also sets \"dangerouslySetInnerHTML\" using React. It should be a leaf with no children. Otherwise it's ambiguous which children should be used.");
					})));
				}
			}
		}
		function isSingletonScope(type) {
			return "head" === type;
		}
		function removeChild(parentInstance, child) {
			parentInstance.removeChild(child);
		}
		function removeChildFromContainer(container, child) {
			(9 === container.nodeType ? container.body : "HTML" === container.nodeName ? container.ownerDocument.body : container).removeChild(child);
		}
		function clearHydrationBoundary(parentInstance, hydrationInstance) {
			var node = hydrationInstance, depth = 0;
			do {
				var nextNode = node.nextSibling;
				parentInstance.removeChild(node);
				if (nextNode && 8 === nextNode.nodeType) if (node = nextNode.data, node === SUSPENSE_END_DATA || node === ACTIVITY_END_DATA) {
					if (0 === depth) {
						parentInstance.removeChild(nextNode);
						retryIfBlockedOn(hydrationInstance);
						return;
					}
					depth--;
				} else if (node === SUSPENSE_START_DATA || node === SUSPENSE_PENDING_START_DATA || node === SUSPENSE_QUEUED_START_DATA || node === SUSPENSE_FALLBACK_START_DATA || node === ACTIVITY_START_DATA) depth++;
				else if (node === PREAMBLE_CONTRIBUTION_HTML) releaseSingletonInstance(parentInstance.ownerDocument.documentElement);
				else if (node === PREAMBLE_CONTRIBUTION_HEAD) {
					node = parentInstance.ownerDocument.head;
					releaseSingletonInstance(node);
					for (var node$jscomp$0 = node.firstChild; node$jscomp$0;) {
						var nextNode$jscomp$0 = node$jscomp$0.nextSibling, nodeName = node$jscomp$0.nodeName;
						node$jscomp$0[internalHoistableMarker] || "SCRIPT" === nodeName || "STYLE" === nodeName || "LINK" === nodeName && "stylesheet" === node$jscomp$0.rel.toLowerCase() || node.removeChild(node$jscomp$0);
						node$jscomp$0 = nextNode$jscomp$0;
					}
				} else node === PREAMBLE_CONTRIBUTION_BODY && releaseSingletonInstance(parentInstance.ownerDocument.body);
				node = nextNode;
			} while (node);
			retryIfBlockedOn(hydrationInstance);
		}
		function hideOrUnhideDehydratedBoundary(suspenseInstance, isHidden) {
			var node = suspenseInstance;
			suspenseInstance = 0;
			do {
				var nextNode = node.nextSibling;
				1 === node.nodeType ? isHidden ? (node._stashedDisplay = node.style.display, node.style.display = "none") : (node.style.display = node._stashedDisplay || "", "" === node.getAttribute("style") && node.removeAttribute("style")) : 3 === node.nodeType && (isHidden ? (node._stashedText = node.nodeValue, node.nodeValue = "") : node.nodeValue = node._stashedText || "");
				if (nextNode && 8 === nextNode.nodeType) if (node = nextNode.data, node === SUSPENSE_END_DATA) if (0 === suspenseInstance) break;
				else suspenseInstance--;
				else node !== SUSPENSE_START_DATA && node !== SUSPENSE_PENDING_START_DATA && node !== SUSPENSE_QUEUED_START_DATA && node !== SUSPENSE_FALLBACK_START_DATA || suspenseInstance++;
				node = nextNode;
			} while (node);
		}
		function hideDehydratedBoundary(suspenseInstance) {
			hideOrUnhideDehydratedBoundary(suspenseInstance, !0);
		}
		function hideInstance(instance) {
			instance = instance.style;
			"function" === typeof instance.setProperty ? instance.setProperty("display", "none", "important") : instance.display = "none";
		}
		function hideTextInstance(textInstance) {
			textInstance.nodeValue = "";
		}
		function unhideDehydratedBoundary(dehydratedInstance) {
			hideOrUnhideDehydratedBoundary(dehydratedInstance, !1);
		}
		function unhideInstance(instance, props) {
			props = props[STYLE];
			props = void 0 !== props && null !== props && props.hasOwnProperty("display") ? props.display : null;
			instance.style.display = null == props || "boolean" === typeof props ? "" : ("" + props).trim();
		}
		function unhideTextInstance(textInstance, text) {
			textInstance.nodeValue = text;
		}
		function clearContainerSparingly(container) {
			var nextNode = container.firstChild;
			nextNode && 10 === nextNode.nodeType && (nextNode = nextNode.nextSibling);
			for (; nextNode;) {
				var node = nextNode;
				nextNode = nextNode.nextSibling;
				switch (node.nodeName) {
					case "HTML":
					case "HEAD":
					case "BODY":
						clearContainerSparingly(node);
						detachDeletedInstance(node);
						continue;
					case "SCRIPT":
					case "STYLE": continue;
					case "LINK": if ("stylesheet" === node.rel.toLowerCase()) continue;
				}
				container.removeChild(node);
			}
		}
		function canHydrateInstance(instance, type, props, inRootOrSingleton) {
			for (; 1 === instance.nodeType;) {
				var anyProps = props;
				if (instance.nodeName.toLowerCase() !== type.toLowerCase()) {
					if (!inRootOrSingleton && ("INPUT" !== instance.nodeName || "hidden" !== instance.type)) break;
				} else if (!inRootOrSingleton) if ("input" === type && "hidden" === instance.type) {
					checkAttributeStringCoercion(anyProps.name, "name");
					var name = null == anyProps.name ? null : "" + anyProps.name;
					if ("hidden" === anyProps.type && instance.getAttribute("name") === name) return instance;
				} else return instance;
				else if (!instance[internalHoistableMarker]) switch (type) {
					case "meta":
						if (!instance.hasAttribute("itemprop")) break;
						return instance;
					case "link":
						name = instance.getAttribute("rel");
						if ("stylesheet" === name && instance.hasAttribute("data-precedence")) break;
						else if (name !== anyProps.rel || instance.getAttribute("href") !== (null == anyProps.href || "" === anyProps.href ? null : anyProps.href) || instance.getAttribute("crossorigin") !== (null == anyProps.crossOrigin ? null : anyProps.crossOrigin) || instance.getAttribute("title") !== (null == anyProps.title ? null : anyProps.title)) break;
						return instance;
					case "style":
						if (instance.hasAttribute("data-precedence")) break;
						return instance;
					case "script":
						name = instance.getAttribute("src");
						if ((name !== (null == anyProps.src ? null : anyProps.src) || instance.getAttribute("type") !== (null == anyProps.type ? null : anyProps.type) || instance.getAttribute("crossorigin") !== (null == anyProps.crossOrigin ? null : anyProps.crossOrigin)) && name && instance.hasAttribute("async") && !instance.hasAttribute("itemprop")) break;
						return instance;
					default: return instance;
				}
				instance = getNextHydratable(instance.nextSibling);
				if (null === instance) break;
			}
			return null;
		}
		function canHydrateTextInstance(instance, text, inRootOrSingleton) {
			if ("" === text) return null;
			for (; 3 !== instance.nodeType;) {
				if ((1 !== instance.nodeType || "INPUT" !== instance.nodeName || "hidden" !== instance.type) && !inRootOrSingleton) return null;
				instance = getNextHydratable(instance.nextSibling);
				if (null === instance) return null;
			}
			return instance;
		}
		function canHydrateHydrationBoundary(instance, inRootOrSingleton) {
			for (; 8 !== instance.nodeType;) {
				if ((1 !== instance.nodeType || "INPUT" !== instance.nodeName || "hidden" !== instance.type) && !inRootOrSingleton) return null;
				instance = getNextHydratable(instance.nextSibling);
				if (null === instance) return null;
			}
			return instance;
		}
		function isSuspenseInstancePending(instance) {
			return instance.data === SUSPENSE_PENDING_START_DATA || instance.data === SUSPENSE_QUEUED_START_DATA;
		}
		function isSuspenseInstanceFallback(instance) {
			return instance.data === SUSPENSE_FALLBACK_START_DATA || instance.data === SUSPENSE_PENDING_START_DATA && instance.ownerDocument.readyState !== DOCUMENT_READY_STATE_LOADING;
		}
		function registerSuspenseInstanceRetry(instance, callback) {
			var ownerDocument = instance.ownerDocument;
			if (instance.data === SUSPENSE_QUEUED_START_DATA) instance._reactRetry = callback;
			else if (instance.data !== SUSPENSE_PENDING_START_DATA || ownerDocument.readyState !== DOCUMENT_READY_STATE_LOADING) callback();
			else {
				var listener = function() {
					callback();
					ownerDocument.removeEventListener("DOMContentLoaded", listener);
				};
				ownerDocument.addEventListener("DOMContentLoaded", listener);
				instance._reactRetry = listener;
			}
		}
		function getNextHydratable(node) {
			for (; null != node; node = node.nextSibling) {
				var nodeType = node.nodeType;
				if (1 === nodeType || 3 === nodeType) break;
				if (8 === nodeType) {
					nodeType = node.data;
					if (nodeType === SUSPENSE_START_DATA || nodeType === SUSPENSE_FALLBACK_START_DATA || nodeType === SUSPENSE_PENDING_START_DATA || nodeType === SUSPENSE_QUEUED_START_DATA || nodeType === ACTIVITY_START_DATA || nodeType === FORM_STATE_IS_MATCHING || nodeType === FORM_STATE_IS_NOT_MATCHING) break;
					if (nodeType === SUSPENSE_END_DATA || nodeType === ACTIVITY_END_DATA) return null;
				}
			}
			return node;
		}
		function describeHydratableInstanceForDevWarnings(instance) {
			if (1 === instance.nodeType) {
				for (var JSCompiler_temp_const = instance.nodeName.toLowerCase(), serverDifferences = {}, attributes = instance.attributes, i = 0; i < attributes.length; i++) {
					var attr = attributes[i];
					serverDifferences[getPropNameFromAttributeName(attr.name)] = "style" === attr.name.toLowerCase() ? getStylesObjectFromElement(instance) : attr.value;
				}
				return {
					type: JSCompiler_temp_const,
					props: serverDifferences
				};
			}
			return 8 === instance.nodeType ? instance.data === ACTIVITY_START_DATA ? {
				type: "Activity",
				props: {}
			} : {
				type: "Suspense",
				props: {}
			} : instance.nodeValue;
		}
		function diffHydratedTextForDevWarnings(textInstance, text, parentProps) {
			return null === parentProps || !0 !== parentProps[SUPPRESS_HYDRATION_WARNING] ? (textInstance.nodeValue === text ? textInstance = null : (text = normalizeMarkupForTextOrAttribute(text), textInstance = normalizeMarkupForTextOrAttribute(textInstance.nodeValue) === text ? null : textInstance.nodeValue), textInstance) : null;
		}
		function getNextHydratableInstanceAfterHydrationBoundary(hydrationInstance) {
			hydrationInstance = hydrationInstance.nextSibling;
			for (var depth = 0; hydrationInstance;) {
				if (8 === hydrationInstance.nodeType) {
					var data = hydrationInstance.data;
					if (data === SUSPENSE_END_DATA || data === ACTIVITY_END_DATA) {
						if (0 === depth) return getNextHydratable(hydrationInstance.nextSibling);
						depth--;
					} else data !== SUSPENSE_START_DATA && data !== SUSPENSE_FALLBACK_START_DATA && data !== SUSPENSE_PENDING_START_DATA && data !== SUSPENSE_QUEUED_START_DATA && data !== ACTIVITY_START_DATA || depth++;
				}
				hydrationInstance = hydrationInstance.nextSibling;
			}
			return null;
		}
		function getParentHydrationBoundary(targetInstance) {
			targetInstance = targetInstance.previousSibling;
			for (var depth = 0; targetInstance;) {
				if (8 === targetInstance.nodeType) {
					var data = targetInstance.data;
					if (data === SUSPENSE_START_DATA || data === SUSPENSE_FALLBACK_START_DATA || data === SUSPENSE_PENDING_START_DATA || data === SUSPENSE_QUEUED_START_DATA || data === ACTIVITY_START_DATA) {
						if (0 === depth) return targetInstance;
						depth--;
					} else data !== SUSPENSE_END_DATA && data !== ACTIVITY_END_DATA || depth++;
				}
				targetInstance = targetInstance.previousSibling;
			}
			return null;
		}
		function commitHydratedContainer(container) {
			retryIfBlockedOn(container);
		}
		function commitHydratedActivityInstance(activityInstance) {
			retryIfBlockedOn(activityInstance);
		}
		function commitHydratedSuspenseInstance(suspenseInstance) {
			retryIfBlockedOn(suspenseInstance);
		}
		function resolveSingletonInstance(type, props, rootContainerInstance, hostContext, validateDOMNestingDev) {
			validateDOMNestingDev && validateDOMNesting(type, hostContext.ancestorInfo);
			props = getOwnerDocumentFromRootContainer(rootContainerInstance);
			switch (type) {
				case "html":
					type = props.documentElement;
					if (!type) throw Error("React expected an <html> element (document.documentElement) to exist in the Document but one was not found. React never removes the documentElement for any Document it renders into so the cause is likely in some other script running on this page.");
					return type;
				case "head":
					type = props.head;
					if (!type) throw Error("React expected a <head> element (document.head) to exist in the Document but one was not found. React never removes the head for any Document it renders into so the cause is likely in some other script running on this page.");
					return type;
				case "body":
					type = props.body;
					if (!type) throw Error("React expected a <body> element (document.body) to exist in the Document but one was not found. React never removes the body for any Document it renders into so the cause is likely in some other script running on this page.");
					return type;
				default: throw Error("resolveSingletonInstance was called with an element type that is not supported. This is a bug in React.");
			}
		}
		function acquireSingletonInstance(type, props, instance, internalInstanceHandle) {
			if (!instance[internalContainerInstanceKey] && getInstanceFromNode(instance)) {
				var tagName = instance.tagName.toLowerCase();
				console.error("You are mounting a new %s component when a previous one has not first unmounted. It is an error to render more than one %s component at a time and attributes and children of these components will likely fail in unpredictable ways. Please only render a single instance of <%s> and if you need to mount a new one, ensure any previous ones have unmounted first.", tagName, tagName, tagName);
			}
			switch (type) {
				case "html":
				case "head":
				case "body": break;
				default: console.error("acquireSingletonInstance was called with an element type that is not supported. This is a bug in React.");
			}
			for (tagName = instance.attributes; tagName.length;) instance.removeAttributeNode(tagName[0]);
			setInitialProperties(instance, type, props);
			instance[internalInstanceKey] = internalInstanceHandle;
			instance[internalPropsKey] = props;
		}
		function releaseSingletonInstance(instance) {
			for (var attributes = instance.attributes; attributes.length;) instance.removeAttributeNode(attributes[0]);
			detachDeletedInstance(instance);
		}
		function getHoistableRoot(container) {
			return "function" === typeof container.getRootNode ? container.getRootNode() : 9 === container.nodeType ? container : container.ownerDocument;
		}
		function preconnectAs(rel, href, crossOrigin) {
			var ownerDocument = globalDocument;
			if (ownerDocument && "string" === typeof href && href) {
				var limitedEscapedHref = escapeSelectorAttributeValueInsideDoubleQuotes(href);
				limitedEscapedHref = "link[rel=\"" + rel + "\"][href=\"" + limitedEscapedHref + "\"]";
				"string" === typeof crossOrigin && (limitedEscapedHref += "[crossorigin=\"" + crossOrigin + "\"]");
				preconnectsSet.has(limitedEscapedHref) || (preconnectsSet.add(limitedEscapedHref), rel = {
					rel,
					crossOrigin,
					href
				}, null === ownerDocument.querySelector(limitedEscapedHref) && (href = ownerDocument.createElement("link"), setInitialProperties(href, "link", rel), markNodeAsHoistable(href), ownerDocument.head.appendChild(href)));
			}
		}
		function getResource(type, currentProps, pendingProps, currentResource) {
			var resourceRoot = (resourceRoot = rootInstanceStackCursor.current) ? getHoistableRoot(resourceRoot) : null;
			if (!resourceRoot) throw Error("\"resourceRoot\" was expected to exist. This is a bug in React.");
			switch (type) {
				case "meta":
				case "title": return null;
				case "style": return "string" === typeof pendingProps.precedence && "string" === typeof pendingProps.href ? (pendingProps = getStyleKey(pendingProps.href), currentProps = getResourcesFromRoot(resourceRoot).hoistableStyles, currentResource = currentProps.get(pendingProps), currentResource || (currentResource = {
					type: "style",
					instance: null,
					count: 0,
					state: null
				}, currentProps.set(pendingProps, currentResource)), currentResource) : {
					type: "void",
					instance: null,
					count: 0,
					state: null
				};
				case "link":
					if ("stylesheet" === pendingProps.rel && "string" === typeof pendingProps.href && "string" === typeof pendingProps.precedence) {
						type = getStyleKey(pendingProps.href);
						var _styles = getResourcesFromRoot(resourceRoot).hoistableStyles, _resource = _styles.get(type);
						if (!_resource && (resourceRoot = resourceRoot.ownerDocument || resourceRoot, _resource = {
							type: "stylesheet",
							instance: null,
							count: 0,
							state: {
								loading: NotLoaded,
								preload: null
							}
						}, _styles.set(type, _resource), (_styles = resourceRoot.querySelector(getStylesheetSelectorFromKey(type))) && !_styles._p && (_resource.instance = _styles, _resource.state.loading = Loaded | Inserted), !preloadPropsMap.has(type))) {
							var preloadProps = {
								rel: "preload",
								as: "style",
								href: pendingProps.href,
								crossOrigin: pendingProps.crossOrigin,
								integrity: pendingProps.integrity,
								media: pendingProps.media,
								hrefLang: pendingProps.hrefLang,
								referrerPolicy: pendingProps.referrerPolicy
							};
							preloadPropsMap.set(type, preloadProps);
							_styles || preloadStylesheet(resourceRoot, type, preloadProps, _resource.state);
						}
						if (currentProps && null === currentResource) throw pendingProps = "\n\n  - " + describeLinkForResourceErrorDEV(currentProps) + "\n  + " + describeLinkForResourceErrorDEV(pendingProps), Error("Expected <link> not to update to be updated to a stylesheet with precedence. Check the `rel`, `href`, and `precedence` props of this component. Alternatively, check whether two different <link> components render in the same slot or share the same key." + pendingProps);
						return _resource;
					}
					if (currentProps && null !== currentResource) throw pendingProps = "\n\n  - " + describeLinkForResourceErrorDEV(currentProps) + "\n  + " + describeLinkForResourceErrorDEV(pendingProps), Error("Expected stylesheet with precedence to not be updated to a different kind of <link>. Check the `rel`, `href`, and `precedence` props of this component. Alternatively, check whether two different <link> components render in the same slot or share the same key." + pendingProps);
					return null;
				case "script": return currentProps = pendingProps.async, pendingProps = pendingProps.src, "string" === typeof pendingProps && currentProps && "function" !== typeof currentProps && "symbol" !== typeof currentProps ? (pendingProps = getScriptKey(pendingProps), currentProps = getResourcesFromRoot(resourceRoot).hoistableScripts, currentResource = currentProps.get(pendingProps), currentResource || (currentResource = {
					type: "script",
					instance: null,
					count: 0,
					state: null
				}, currentProps.set(pendingProps, currentResource)), currentResource) : {
					type: "void",
					instance: null,
					count: 0,
					state: null
				};
				default: throw Error("getResource encountered a type it did not expect: \"" + type + "\". this is a bug in React.");
			}
		}
		function describeLinkForResourceErrorDEV(props) {
			var describedProps = 0, description = "<link";
			"string" === typeof props.rel ? (describedProps++, description += " rel=\"" + props.rel + "\"") : hasOwnProperty.call(props, "rel") && (describedProps++, description += " rel=\"" + (null === props.rel ? "null" : "invalid type " + typeof props.rel) + "\"");
			"string" === typeof props.href ? (describedProps++, description += " href=\"" + props.href + "\"") : hasOwnProperty.call(props, "href") && (describedProps++, description += " href=\"" + (null === props.href ? "null" : "invalid type " + typeof props.href) + "\"");
			"string" === typeof props.precedence ? (describedProps++, description += " precedence=\"" + props.precedence + "\"") : hasOwnProperty.call(props, "precedence") && (describedProps++, description += " precedence={" + (null === props.precedence ? "null" : "invalid type " + typeof props.precedence) + "}");
			Object.getOwnPropertyNames(props).length > describedProps && (description += " ...");
			return description + " />";
		}
		function getStyleKey(href) {
			return "href=\"" + escapeSelectorAttributeValueInsideDoubleQuotes(href) + "\"";
		}
		function getStylesheetSelectorFromKey(key) {
			return "link[rel=\"stylesheet\"][" + key + "]";
		}
		function stylesheetPropsFromRawProps(rawProps) {
			return assign({}, rawProps, {
				"data-precedence": rawProps.precedence,
				precedence: null
			});
		}
		function preloadStylesheet(ownerDocument, key, preloadProps, state) {
			ownerDocument.querySelector("link[rel=\"preload\"][as=\"style\"][" + key + "]") ? state.loading = Loaded : (key = ownerDocument.createElement("link"), state.preload = key, key.addEventListener("load", function() {
				return state.loading |= Loaded;
			}), key.addEventListener("error", function() {
				return state.loading |= Errored;
			}), setInitialProperties(key, "link", preloadProps), markNodeAsHoistable(key), ownerDocument.head.appendChild(key));
		}
		function getScriptKey(src) {
			return "[src=\"" + escapeSelectorAttributeValueInsideDoubleQuotes(src) + "\"]";
		}
		function getScriptSelectorFromKey(key) {
			return "script[async]" + key;
		}
		function acquireResource(hoistableRoot, resource, props) {
			resource.count++;
			if (null === resource.instance) switch (resource.type) {
				case "style":
					var instance = hoistableRoot.querySelector("style[data-href~=\"" + escapeSelectorAttributeValueInsideDoubleQuotes(props.href) + "\"]");
					if (instance) return resource.instance = instance, markNodeAsHoistable(instance), instance;
					var styleProps = assign({}, props, {
						"data-href": props.href,
						"data-precedence": props.precedence,
						href: null,
						precedence: null
					});
					instance = (hoistableRoot.ownerDocument || hoistableRoot).createElement("style");
					markNodeAsHoistable(instance);
					setInitialProperties(instance, "style", styleProps);
					insertStylesheet(instance, props.precedence, hoistableRoot);
					return resource.instance = instance;
				case "stylesheet":
					styleProps = getStyleKey(props.href);
					var _instance = hoistableRoot.querySelector(getStylesheetSelectorFromKey(styleProps));
					if (_instance) return resource.state.loading |= Inserted, resource.instance = _instance, markNodeAsHoistable(_instance), _instance;
					instance = stylesheetPropsFromRawProps(props);
					(styleProps = preloadPropsMap.get(styleProps)) && adoptPreloadPropsForStylesheet(instance, styleProps);
					_instance = (hoistableRoot.ownerDocument || hoistableRoot).createElement("link");
					markNodeAsHoistable(_instance);
					var linkInstance = _instance;
					linkInstance._p = new Promise(function(resolve, reject) {
						linkInstance.onload = resolve;
						linkInstance.onerror = reject;
					});
					setInitialProperties(_instance, "link", instance);
					resource.state.loading |= Inserted;
					insertStylesheet(_instance, props.precedence, hoistableRoot);
					return resource.instance = _instance;
				case "script":
					_instance = getScriptKey(props.src);
					if (styleProps = hoistableRoot.querySelector(getScriptSelectorFromKey(_instance))) return resource.instance = styleProps, markNodeAsHoistable(styleProps), styleProps;
					instance = props;
					if (styleProps = preloadPropsMap.get(_instance)) instance = assign({}, props), adoptPreloadPropsForScript(instance, styleProps);
					hoistableRoot = hoistableRoot.ownerDocument || hoistableRoot;
					styleProps = hoistableRoot.createElement("script");
					markNodeAsHoistable(styleProps);
					setInitialProperties(styleProps, "link", instance);
					hoistableRoot.head.appendChild(styleProps);
					return resource.instance = styleProps;
				case "void": return null;
				default: throw Error("acquireResource encountered a resource type it did not expect: \"" + resource.type + "\". this is a bug in React.");
			}
			else "stylesheet" === resource.type && (resource.state.loading & Inserted) === NotLoaded && (instance = resource.instance, resource.state.loading |= Inserted, insertStylesheet(instance, props.precedence, hoistableRoot));
			return resource.instance;
		}
		function insertStylesheet(instance, precedence, root) {
			for (var nodes = root.querySelectorAll("link[rel=\"stylesheet\"][data-precedence],style[data-precedence]"), last = nodes.length ? nodes[nodes.length - 1] : null, prior = last, i = 0; i < nodes.length; i++) {
				var node = nodes[i];
				if (node.dataset.precedence === precedence) prior = node;
				else if (prior !== last) break;
			}
			prior ? prior.parentNode.insertBefore(instance, prior.nextSibling) : (precedence = 9 === root.nodeType ? root.head : root, precedence.insertBefore(instance, precedence.firstChild));
		}
		function adoptPreloadPropsForStylesheet(stylesheetProps, preloadProps) {
			stylesheetProps.crossOrigin ??= preloadProps.crossOrigin;
			stylesheetProps.referrerPolicy ??= preloadProps.referrerPolicy;
			stylesheetProps.title ??= preloadProps.title;
		}
		function adoptPreloadPropsForScript(scriptProps, preloadProps) {
			scriptProps.crossOrigin ??= preloadProps.crossOrigin;
			scriptProps.referrerPolicy ??= preloadProps.referrerPolicy;
			scriptProps.integrity ??= preloadProps.integrity;
		}
		function getHydratableHoistableCache(type, keyAttribute, ownerDocument) {
			if (null === tagCaches) {
				var cache = /* @__PURE__ */ new Map();
				var caches = tagCaches = /* @__PURE__ */ new Map();
				caches.set(ownerDocument, cache);
			} else caches = tagCaches, cache = caches.get(ownerDocument), cache || (cache = /* @__PURE__ */ new Map(), caches.set(ownerDocument, cache));
			if (cache.has(type)) return cache;
			cache.set(type, null);
			ownerDocument = ownerDocument.getElementsByTagName(type);
			for (caches = 0; caches < ownerDocument.length; caches++) {
				var node = ownerDocument[caches];
				if (!(node[internalHoistableMarker] || node[internalInstanceKey] || "link" === type && "stylesheet" === node.getAttribute("rel")) && node.namespaceURI !== SVG_NAMESPACE) {
					var nodeKey = node.getAttribute(keyAttribute) || "";
					nodeKey = type + nodeKey;
					var existing = cache.get(nodeKey);
					existing ? existing.push(node) : cache.set(nodeKey, [node]);
				}
			}
			return cache;
		}
		function mountHoistable(hoistableRoot, type, instance) {
			hoistableRoot = hoistableRoot.ownerDocument || hoistableRoot;
			hoistableRoot.head.insertBefore(instance, "title" === type ? hoistableRoot.querySelector("head > title") : null);
		}
		function isHostHoistableType(type, props, hostContext) {
			var outsideHostContainerContext = !hostContext.ancestorInfo.containerTagInScope;
			if (hostContext.context === HostContextNamespaceSvg || null != props.itemProp) return !outsideHostContainerContext || null == props.itemProp || "meta" !== type && "title" !== type && "style" !== type && "link" !== type && "script" !== type || console.error("Cannot render a <%s> outside the main document if it has an `itemProp` prop. `itemProp` suggests the tag belongs to an `itemScope` which can appear anywhere in the DOM. If you were intending for React to hoist this <%s> remove the `itemProp` prop. Otherwise, try moving this tag into the <head> or <body> of the Document.", type, type), !1;
			switch (type) {
				case "meta":
				case "title": return !0;
				case "style":
					if ("string" !== typeof props.precedence || "string" !== typeof props.href || "" === props.href) {
						outsideHostContainerContext && console.error("Cannot render a <style> outside the main document without knowing its precedence and a unique href key. React can hoist and deduplicate <style> tags if you provide a `precedence` prop along with an `href` prop that does not conflict with the `href` values used in any other hoisted <style> or <link rel=\"stylesheet\" ...> tags.  Note that hoisting <style> tags is considered an advanced feature that most will not use directly. Consider moving the <style> tag to the <head> or consider adding a `precedence=\"default\"` and `href=\"some unique resource identifier\"`.");
						break;
					}
					return !0;
				case "link":
					if ("string" !== typeof props.rel || "string" !== typeof props.href || "" === props.href || props.onLoad || props.onError) {
						if ("stylesheet" === props.rel && "string" === typeof props.precedence) {
							type = props.href;
							var onError = props.onError, disabled = props.disabled;
							hostContext = [];
							props.onLoad && hostContext.push("`onLoad`");
							onError && hostContext.push("`onError`");
							null != disabled && hostContext.push("`disabled`");
							onError = propNamesListJoin(hostContext, "and");
							onError += 1 === hostContext.length ? " prop" : " props";
							disabled = 1 === hostContext.length ? "an " + onError : "the " + onError;
							hostContext.length && console.error("React encountered a <link rel=\"stylesheet\" href=\"%s\" ... /> with a `precedence` prop that also included %s. The presence of loading and error handlers indicates an intent to manage the stylesheet loading state from your from your Component code and React will not hoist or deduplicate this stylesheet. If your intent was to have React hoist and deduplciate this stylesheet using the `precedence` prop remove the %s, otherwise remove the `precedence` prop.", type, disabled, onError);
						}
						outsideHostContainerContext && ("string" !== typeof props.rel || "string" !== typeof props.href || "" === props.href ? console.error("Cannot render a <link> outside the main document without a `rel` and `href` prop. Try adding a `rel` and/or `href` prop to this <link> or moving the link into the <head> tag") : (props.onError || props.onLoad) && console.error("Cannot render a <link> with onLoad or onError listeners outside the main document. Try removing onLoad={...} and onError={...} or moving it into the root <head> tag or somewhere in the <body>."));
						break;
					}
					switch (props.rel) {
						case "stylesheet": return type = props.precedence, props = props.disabled, "string" !== typeof type && outsideHostContainerContext && console.error("Cannot render a <link rel=\"stylesheet\" /> outside the main document without knowing its precedence. Consider adding precedence=\"default\" or moving it into the root <head> tag."), "string" === typeof type && null == props;
						default: return !0;
					}
				case "script":
					type = props.async && "function" !== typeof props.async && "symbol" !== typeof props.async;
					if (!type || props.onLoad || props.onError || !props.src || "string" !== typeof props.src) {
						outsideHostContainerContext && (type ? props.onLoad || props.onError ? console.error("Cannot render a <script> with onLoad or onError listeners outside the main document. Try removing onLoad={...} and onError={...} or moving it into the root <head> tag or somewhere in the <body>.") : console.error("Cannot render a <script> outside the main document without `async={true}` and a non-empty `src` prop. Ensure there is a valid `src` and either make the script async or move it into the root <head> tag or somewhere in the <body>.") : console.error("Cannot render a sync or defer <script> outside the main document without knowing its order. Try adding async=\"\" or moving it into the root <head> tag."));
						break;
					}
					return !0;
				case "noscript":
				case "template": outsideHostContainerContext && console.error("Cannot render <%s> outside the main document. Try moving it into the root <head> tag.", type);
			}
			return !1;
		}
		function preloadResource(resource) {
			return "stylesheet" === resource.type && (resource.state.loading & Settled) === NotLoaded ? !1 : !0;
		}
		function suspendResource(state, hoistableRoot, resource, props) {
			if ("stylesheet" === resource.type && ("string" !== typeof props.media || !1 !== matchMedia(props.media).matches) && (resource.state.loading & Inserted) === NotLoaded) {
				if (null === resource.instance) {
					var key = getStyleKey(props.href), instance = hoistableRoot.querySelector(getStylesheetSelectorFromKey(key));
					if (instance) {
						hoistableRoot = instance._p;
						null !== hoistableRoot && "object" === typeof hoistableRoot && "function" === typeof hoistableRoot.then && (state.count++, state = onUnsuspend.bind(state), hoistableRoot.then(state, state));
						resource.state.loading |= Inserted;
						resource.instance = instance;
						markNodeAsHoistable(instance);
						return;
					}
					instance = hoistableRoot.ownerDocument || hoistableRoot;
					props = stylesheetPropsFromRawProps(props);
					(key = preloadPropsMap.get(key)) && adoptPreloadPropsForStylesheet(props, key);
					instance = instance.createElement("link");
					markNodeAsHoistable(instance);
					var linkInstance = instance;
					linkInstance._p = new Promise(function(resolve, reject) {
						linkInstance.onload = resolve;
						linkInstance.onerror = reject;
					});
					setInitialProperties(instance, "link", props);
					resource.instance = instance;
				}
				null === state.stylesheets && (state.stylesheets = /* @__PURE__ */ new Map());
				state.stylesheets.set(resource, hoistableRoot);
				(hoistableRoot = resource.state.preload) && (resource.state.loading & Settled) === NotLoaded && (state.count++, resource = onUnsuspend.bind(state), hoistableRoot.addEventListener("load", resource), hoistableRoot.addEventListener("error", resource));
			}
		}
		function waitForCommitToBeReady(state, timeoutOffset) {
			state.stylesheets && 0 === state.count && insertSuspendedStylesheets(state, state.stylesheets);
			return 0 < state.count || 0 < state.imgCount ? function(commit) {
				var stylesheetTimer = setTimeout(function() {
					state.stylesheets && insertSuspendedStylesheets(state, state.stylesheets);
					if (state.unsuspend) {
						var unsuspend = state.unsuspend;
						state.unsuspend = null;
						unsuspend();
					}
				}, SUSPENSEY_STYLESHEET_TIMEOUT + timeoutOffset);
				0 < state.imgBytes && 0 === estimatedBytesWithinLimit && (estimatedBytesWithinLimit = 125 * estimateBandwidth() * SUSPENSEY_IMAGE_TIME_ESTIMATE);
				var imgTimer = setTimeout(function() {
					state.waitingForImages = !1;
					if (0 === state.count && (state.stylesheets && insertSuspendedStylesheets(state, state.stylesheets), state.unsuspend)) {
						var unsuspend = state.unsuspend;
						state.unsuspend = null;
						unsuspend();
					}
				}, (state.imgBytes > estimatedBytesWithinLimit ? 50 : SUSPENSEY_IMAGE_TIMEOUT) + timeoutOffset);
				state.unsuspend = commit;
				return function() {
					state.unsuspend = null;
					clearTimeout(stylesheetTimer);
					clearTimeout(imgTimer);
				};
			} : null;
		}
		function onUnsuspend() {
			this.count--;
			if (0 === this.count && (0 === this.imgCount || !this.waitingForImages)) {
				if (this.stylesheets) insertSuspendedStylesheets(this, this.stylesheets);
				else if (this.unsuspend) {
					var unsuspend = this.unsuspend;
					this.unsuspend = null;
					unsuspend();
				}
			}
		}
		function insertSuspendedStylesheets(state, resources) {
			state.stylesheets = null;
			null !== state.unsuspend && (state.count++, precedencesByRoot = /* @__PURE__ */ new Map(), resources.forEach(insertStylesheetIntoRoot, state), precedencesByRoot = null, onUnsuspend.call(state));
		}
		function insertStylesheetIntoRoot(root, resource) {
			if (!(resource.state.loading & Inserted)) {
				var precedences = precedencesByRoot.get(root);
				if (precedences) var last = precedences.get(LAST_PRECEDENCE);
				else {
					precedences = /* @__PURE__ */ new Map();
					precedencesByRoot.set(root, precedences);
					for (var nodes = root.querySelectorAll("link[data-precedence],style[data-precedence]"), i = 0; i < nodes.length; i++) {
						var node = nodes[i];
						if ("LINK" === node.nodeName || "not all" !== node.getAttribute("media")) precedences.set(node.dataset.precedence, node), last = node;
					}
					last && precedences.set(LAST_PRECEDENCE, last);
				}
				nodes = resource.instance;
				node = nodes.getAttribute("data-precedence");
				i = precedences.get(node) || last;
				i === last && precedences.set(LAST_PRECEDENCE, nodes);
				precedences.set(node, nodes);
				this.count++;
				last = onUnsuspend.bind(this);
				nodes.addEventListener("load", last);
				nodes.addEventListener("error", last);
				i ? i.parentNode.insertBefore(nodes, i.nextSibling) : (root = 9 === root.nodeType ? root.head : root, root.insertBefore(nodes, root.firstChild));
				resource.state.loading |= Inserted;
			}
		}
		function FiberRootNode(containerInfo, tag, hydrate, identifierPrefix, onUncaughtError, onCaughtError, onRecoverableError, onDefaultTransitionIndicator, formState) {
			this.tag = 1;
			this.containerInfo = containerInfo;
			this.pingCache = this.current = this.pendingChildren = null;
			this.timeoutHandle = noTimeout;
			this.callbackNode = this.next = this.pendingContext = this.context = this.cancelPendingCommit = null;
			this.callbackPriority = 0;
			this.expirationTimes = createLaneMap(-1);
			this.entangledLanes = this.shellSuspendCounter = this.errorRecoveryDisabledLanes = this.expiredLanes = this.warmLanes = this.pingedLanes = this.suspendedLanes = this.pendingLanes = 0;
			this.entanglements = createLaneMap(0);
			this.hiddenUpdates = createLaneMap(null);
			this.identifierPrefix = identifierPrefix;
			this.onUncaughtError = onUncaughtError;
			this.onCaughtError = onCaughtError;
			this.onRecoverableError = onRecoverableError;
			this.pooledCache = null;
			this.pooledCacheLanes = 0;
			this.formState = formState;
			this.incompleteTransitions = /* @__PURE__ */ new Map();
			this.passiveEffectDuration = this.effectDuration = -0;
			this.memoizedUpdaters = /* @__PURE__ */ new Set();
			containerInfo = this.pendingUpdatersLaneMap = [];
			for (tag = 0; 31 > tag; tag++) containerInfo.push(/* @__PURE__ */ new Set());
			this._debugRootType = hydrate ? "hydrateRoot()" : "createRoot()";
		}
		function createFiberRoot(containerInfo, tag, hydrate, initialChildren, hydrationCallbacks, isStrictMode, identifierPrefix, formState, onUncaughtError, onCaughtError, onRecoverableError, onDefaultTransitionIndicator) {
			containerInfo = new FiberRootNode(containerInfo, tag, hydrate, identifierPrefix, onUncaughtError, onCaughtError, onRecoverableError, onDefaultTransitionIndicator, formState);
			tag = ConcurrentMode;
			!0 === isStrictMode && (tag |= StrictLegacyMode | StrictEffectsMode);
			tag |= ProfileMode;
			isStrictMode = createFiber(3, null, null, tag);
			containerInfo.current = isStrictMode;
			isStrictMode.stateNode = containerInfo;
			tag = createCache();
			retainCache(tag);
			containerInfo.pooledCache = tag;
			retainCache(tag);
			isStrictMode.memoizedState = {
				element: initialChildren,
				isDehydrated: hydrate,
				cache: tag
			};
			initializeUpdateQueue(isStrictMode);
			return containerInfo;
		}
		function getContextForSubtree(parentComponent) {
			if (!parentComponent) return emptyContextObject;
			parentComponent = emptyContextObject;
			return parentComponent;
		}
		function updateContainerImpl(rootFiber, lane, element, container, parentComponent, callback) {
			if (injectedHook && "function" === typeof injectedHook.onScheduleFiberRoot) try {
				injectedHook.onScheduleFiberRoot(rendererID, container, element);
			} catch (err) {
				hasLoggedError || (hasLoggedError = !0, console.error("React instrumentation encountered an error: %o", err));
			}
			parentComponent = getContextForSubtree(parentComponent);
			null === container.context ? container.context = parentComponent : container.pendingContext = parentComponent;
			isRendering && null !== current && !didWarnAboutNestedUpdates && (didWarnAboutNestedUpdates = !0, console.error("Render methods should be a pure function of props and state; triggering nested component updates from render is not allowed. If necessary, trigger nested updates in componentDidUpdate.\n\nCheck the render method of %s.", getComponentNameFromFiber(current) || "Unknown"));
			container = createUpdate(lane);
			container.payload = { element };
			callback = void 0 === callback ? null : callback;
			null !== callback && ("function" !== typeof callback && console.error("Expected the last optional `callback` argument to be a function. Instead received: %s.", callback), container.callback = callback);
			element = enqueueUpdate(rootFiber, container, lane);
			null !== element && (startUpdateTimerByLane(lane, "root.render()", null), scheduleUpdateOnFiber(element, rootFiber, lane), entangleTransitions(element, rootFiber, lane));
		}
		function markRetryLaneImpl(fiber, retryLane) {
			fiber = fiber.memoizedState;
			if (null !== fiber && null !== fiber.dehydrated) {
				var a = fiber.retryLane;
				fiber.retryLane = 0 !== a && a < retryLane ? a : retryLane;
			}
		}
		function markRetryLaneIfNotHydrated(fiber, retryLane) {
			markRetryLaneImpl(fiber, retryLane);
			(fiber = fiber.alternate) && markRetryLaneImpl(fiber, retryLane);
		}
		function attemptContinuousHydration(fiber) {
			if (13 === fiber.tag || 31 === fiber.tag) {
				var root = enqueueConcurrentRenderForLane(fiber, 67108864);
				null !== root && scheduleUpdateOnFiber(root, fiber, 67108864);
				markRetryLaneIfNotHydrated(fiber, 67108864);
			}
		}
		function attemptHydrationAtCurrentPriority(fiber) {
			if (13 === fiber.tag || 31 === fiber.tag) {
				var lane = requestUpdateLane(fiber);
				lane = getBumpedLaneForHydrationByLane(lane);
				var root = enqueueConcurrentRenderForLane(fiber, lane);
				null !== root && scheduleUpdateOnFiber(root, fiber, lane);
				markRetryLaneIfNotHydrated(fiber, lane);
			}
		}
		function getCurrentFiberForDevTools() {
			return current;
		}
		function dispatchDiscreteEvent(domEventName, eventSystemFlags, container, nativeEvent) {
			var prevTransition = ReactSharedInternals.T;
			ReactSharedInternals.T = null;
			var previousPriority = ReactDOMSharedInternals.p;
			try {
				ReactDOMSharedInternals.p = DiscreteEventPriority, dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
			} finally {
				ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = prevTransition;
			}
		}
		function dispatchContinuousEvent(domEventName, eventSystemFlags, container, nativeEvent) {
			var prevTransition = ReactSharedInternals.T;
			ReactSharedInternals.T = null;
			var previousPriority = ReactDOMSharedInternals.p;
			try {
				ReactDOMSharedInternals.p = ContinuousEventPriority, dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
			} finally {
				ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = prevTransition;
			}
		}
		function dispatchEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
			if (_enabled) {
				var blockedOn = findInstanceBlockingEvent(nativeEvent);
				if (null === blockedOn) dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, return_targetInst, targetContainer), clearIfContinuousEvent(domEventName, nativeEvent);
				else if (queueIfContinuousEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent)) nativeEvent.stopPropagation();
				else if (clearIfContinuousEvent(domEventName, nativeEvent), eventSystemFlags & 4 && -1 < discreteReplayableEvents.indexOf(domEventName)) {
					for (; null !== blockedOn;) {
						var fiber = getInstanceFromNode(blockedOn);
						if (null !== fiber) switch (fiber.tag) {
							case 3:
								fiber = fiber.stateNode;
								if (fiber.current.memoizedState.isDehydrated) {
									var lanes = getHighestPriorityLanes(fiber.pendingLanes);
									if (0 !== lanes) {
										var root = fiber;
										root.pendingLanes |= 2;
										for (root.entangledLanes |= 2; lanes;) {
											var lane = 1 << 31 - clz32(lanes);
											root.entanglements[1] |= lane;
											lanes &= ~lane;
										}
										ensureRootIsScheduled(fiber);
										(executionContext & (RenderContext | CommitContext)) === NoContext && (workInProgressRootRenderTargetTime = now$1() + RENDER_TIMEOUT_MS, flushSyncWorkAcrossRoots_impl(0, !1));
									}
								}
								break;
							case 31:
							case 13: root = enqueueConcurrentRenderForLane(fiber, 2), null !== root && scheduleUpdateOnFiber(root, fiber, 2), flushSyncWork$1(), markRetryLaneIfNotHydrated(fiber, 2);
						}
						fiber = findInstanceBlockingEvent(nativeEvent);
						null === fiber && dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, return_targetInst, targetContainer);
						if (fiber === blockedOn) break;
						blockedOn = fiber;
					}
					null !== blockedOn && nativeEvent.stopPropagation();
				} else dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, null, targetContainer);
			}
		}
		function findInstanceBlockingEvent(nativeEvent) {
			nativeEvent = getEventTarget(nativeEvent);
			return findInstanceBlockingTarget(nativeEvent);
		}
		function findInstanceBlockingTarget(targetNode) {
			return_targetInst = null;
			targetNode = getClosestInstanceFromNode(targetNode);
			if (null !== targetNode) {
				var nearestMounted = getNearestMountedFiber(targetNode);
				if (null === nearestMounted) targetNode = null;
				else {
					var tag = nearestMounted.tag;
					if (13 === tag) {
						targetNode = getSuspenseInstanceFromFiber(nearestMounted);
						if (null !== targetNode) return targetNode;
						targetNode = null;
					} else if (31 === tag) {
						targetNode = getActivityInstanceFromFiber(nearestMounted);
						if (null !== targetNode) return targetNode;
						targetNode = null;
					} else if (3 === tag) {
						if (nearestMounted.stateNode.current.memoizedState.isDehydrated) return 3 === nearestMounted.tag ? nearestMounted.stateNode.containerInfo : null;
						targetNode = null;
					} else nearestMounted !== targetNode && (targetNode = null);
				}
			}
			return_targetInst = targetNode;
			return null;
		}
		function getEventPriority(domEventName) {
			switch (domEventName) {
				case "beforetoggle":
				case "cancel":
				case "click":
				case "close":
				case "contextmenu":
				case "copy":
				case "cut":
				case "auxclick":
				case "dblclick":
				case "dragend":
				case "dragstart":
				case "drop":
				case "focusin":
				case "focusout":
				case "input":
				case "invalid":
				case "keydown":
				case "keypress":
				case "keyup":
				case "mousedown":
				case "mouseup":
				case "paste":
				case "pause":
				case "play":
				case "pointercancel":
				case "pointerdown":
				case "pointerup":
				case "ratechange":
				case "reset":
				case "resize":
				case "seeked":
				case "submit":
				case "toggle":
				case "touchcancel":
				case "touchend":
				case "touchstart":
				case "volumechange":
				case "change":
				case "selectionchange":
				case "textInput":
				case "compositionstart":
				case "compositionend":
				case "compositionupdate":
				case "beforeblur":
				case "afterblur":
				case "beforeinput":
				case "blur":
				case "fullscreenchange":
				case "focus":
				case "hashchange":
				case "popstate":
				case "select":
				case "selectstart": return DiscreteEventPriority;
				case "drag":
				case "dragenter":
				case "dragexit":
				case "dragleave":
				case "dragover":
				case "mousemove":
				case "mouseout":
				case "mouseover":
				case "pointermove":
				case "pointerout":
				case "pointerover":
				case "scroll":
				case "touchmove":
				case "wheel":
				case "mouseenter":
				case "mouseleave":
				case "pointerenter":
				case "pointerleave": return ContinuousEventPriority;
				case "message": switch (getCurrentPriorityLevel()) {
					case ImmediatePriority: return DiscreteEventPriority;
					case UserBlockingPriority: return ContinuousEventPriority;
					case NormalPriority$1:
					case LowPriority: return DefaultEventPriority;
					case IdlePriority: return IdleEventPriority;
					default: return DefaultEventPriority;
				}
				default: return DefaultEventPriority;
			}
		}
		function clearIfContinuousEvent(domEventName, nativeEvent) {
			switch (domEventName) {
				case "focusin":
				case "focusout":
					queuedFocus = null;
					break;
				case "dragenter":
				case "dragleave":
					queuedDrag = null;
					break;
				case "mouseover":
				case "mouseout":
					queuedMouse = null;
					break;
				case "pointerover":
				case "pointerout":
					queuedPointers.delete(nativeEvent.pointerId);
					break;
				case "gotpointercapture":
				case "lostpointercapture": queuedPointerCaptures.delete(nativeEvent.pointerId);
			}
		}
		function accumulateOrCreateContinuousQueuedReplayableEvent(existingQueuedEvent, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
			if (null === existingQueuedEvent || existingQueuedEvent.nativeEvent !== nativeEvent) return existingQueuedEvent = {
				blockedOn,
				domEventName,
				eventSystemFlags,
				nativeEvent,
				targetContainers: [targetContainer]
			}, null !== blockedOn && (blockedOn = getInstanceFromNode(blockedOn), null !== blockedOn && attemptContinuousHydration(blockedOn)), existingQueuedEvent;
			existingQueuedEvent.eventSystemFlags |= eventSystemFlags;
			blockedOn = existingQueuedEvent.targetContainers;
			null !== targetContainer && -1 === blockedOn.indexOf(targetContainer) && blockedOn.push(targetContainer);
			return existingQueuedEvent;
		}
		function queueIfContinuousEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
			switch (domEventName) {
				case "focusin": return queuedFocus = accumulateOrCreateContinuousQueuedReplayableEvent(queuedFocus, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent), !0;
				case "dragenter": return queuedDrag = accumulateOrCreateContinuousQueuedReplayableEvent(queuedDrag, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent), !0;
				case "mouseover": return queuedMouse = accumulateOrCreateContinuousQueuedReplayableEvent(queuedMouse, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent), !0;
				case "pointerover":
					var pointerId = nativeEvent.pointerId;
					queuedPointers.set(pointerId, accumulateOrCreateContinuousQueuedReplayableEvent(queuedPointers.get(pointerId) || null, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent));
					return !0;
				case "gotpointercapture": return pointerId = nativeEvent.pointerId, queuedPointerCaptures.set(pointerId, accumulateOrCreateContinuousQueuedReplayableEvent(queuedPointerCaptures.get(pointerId) || null, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent)), !0;
			}
			return !1;
		}
		function attemptExplicitHydrationTarget(queuedTarget) {
			var targetInst = getClosestInstanceFromNode(queuedTarget.target);
			if (null !== targetInst) {
				var nearestMounted = getNearestMountedFiber(targetInst);
				if (null !== nearestMounted) {
					if (targetInst = nearestMounted.tag, 13 === targetInst) {
						if (targetInst = getSuspenseInstanceFromFiber(nearestMounted), null !== targetInst) {
							queuedTarget.blockedOn = targetInst;
							runWithPriority(queuedTarget.priority, function() {
								attemptHydrationAtCurrentPriority(nearestMounted);
							});
							return;
						}
					} else if (31 === targetInst) {
						if (targetInst = getActivityInstanceFromFiber(nearestMounted), null !== targetInst) {
							queuedTarget.blockedOn = targetInst;
							runWithPriority(queuedTarget.priority, function() {
								attemptHydrationAtCurrentPriority(nearestMounted);
							});
							return;
						}
					} else if (3 === targetInst && nearestMounted.stateNode.current.memoizedState.isDehydrated) {
						queuedTarget.blockedOn = 3 === nearestMounted.tag ? nearestMounted.stateNode.containerInfo : null;
						return;
					}
				}
			}
			queuedTarget.blockedOn = null;
		}
		function attemptReplayContinuousQueuedEvent(queuedEvent) {
			if (null !== queuedEvent.blockedOn) return !1;
			for (var targetContainers = queuedEvent.targetContainers; 0 < targetContainers.length;) {
				var nextBlockedOn = findInstanceBlockingEvent(queuedEvent.nativeEvent);
				if (null === nextBlockedOn) {
					nextBlockedOn = queuedEvent.nativeEvent;
					var nativeEventClone = new nextBlockedOn.constructor(nextBlockedOn.type, nextBlockedOn), event = nativeEventClone;
					null !== currentReplayingEvent && console.error("Expected currently replaying event to be null. This error is likely caused by a bug in React. Please file an issue.");
					currentReplayingEvent = event;
					nextBlockedOn.target.dispatchEvent(nativeEventClone);
					null === currentReplayingEvent && console.error("Expected currently replaying event to not be null. This error is likely caused by a bug in React. Please file an issue.");
					currentReplayingEvent = null;
				} else return targetContainers = getInstanceFromNode(nextBlockedOn), null !== targetContainers && attemptContinuousHydration(targetContainers), queuedEvent.blockedOn = nextBlockedOn, !1;
				targetContainers.shift();
			}
			return !0;
		}
		function attemptReplayContinuousQueuedEventInMap(queuedEvent, key, map) {
			attemptReplayContinuousQueuedEvent(queuedEvent) && map.delete(key);
		}
		function replayUnblockedEvents() {
			hasScheduledReplayAttempt = !1;
			null !== queuedFocus && attemptReplayContinuousQueuedEvent(queuedFocus) && (queuedFocus = null);
			null !== queuedDrag && attemptReplayContinuousQueuedEvent(queuedDrag) && (queuedDrag = null);
			null !== queuedMouse && attemptReplayContinuousQueuedEvent(queuedMouse) && (queuedMouse = null);
			queuedPointers.forEach(attemptReplayContinuousQueuedEventInMap);
			queuedPointerCaptures.forEach(attemptReplayContinuousQueuedEventInMap);
		}
		function scheduleCallbackIfUnblocked(queuedEvent, unblocked) {
			queuedEvent.blockedOn === unblocked && (queuedEvent.blockedOn = null, hasScheduledReplayAttempt || (hasScheduledReplayAttempt = !0, Scheduler.unstable_scheduleCallback(Scheduler.unstable_NormalPriority, replayUnblockedEvents)));
		}
		function scheduleReplayQueueIfNeeded(formReplayingQueue) {
			lastScheduledReplayQueue !== formReplayingQueue && (lastScheduledReplayQueue = formReplayingQueue, Scheduler.unstable_scheduleCallback(Scheduler.unstable_NormalPriority, function() {
				lastScheduledReplayQueue === formReplayingQueue && (lastScheduledReplayQueue = null);
				for (var i = 0; i < formReplayingQueue.length; i += 3) {
					var form = formReplayingQueue[i], submitterOrAction = formReplayingQueue[i + 1], formData = formReplayingQueue[i + 2];
					if ("function" !== typeof submitterOrAction) if (null === findInstanceBlockingTarget(submitterOrAction || form)) continue;
					else break;
					var formInst = getInstanceFromNode(form);
					null !== formInst && (formReplayingQueue.splice(i, 3), i -= 3, form = {
						pending: !0,
						data: formData,
						method: form.method,
						action: submitterOrAction
					}, Object.freeze(form), startHostTransition(formInst, form, submitterOrAction, formData));
				}
			}));
		}
		function retryIfBlockedOn(unblocked) {
			function unblock(queuedEvent) {
				return scheduleCallbackIfUnblocked(queuedEvent, unblocked);
			}
			null !== queuedFocus && scheduleCallbackIfUnblocked(queuedFocus, unblocked);
			null !== queuedDrag && scheduleCallbackIfUnblocked(queuedDrag, unblocked);
			null !== queuedMouse && scheduleCallbackIfUnblocked(queuedMouse, unblocked);
			queuedPointers.forEach(unblock);
			queuedPointerCaptures.forEach(unblock);
			for (var i = 0; i < queuedExplicitHydrationTargets.length; i++) {
				var queuedTarget = queuedExplicitHydrationTargets[i];
				queuedTarget.blockedOn === unblocked && (queuedTarget.blockedOn = null);
			}
			for (; 0 < queuedExplicitHydrationTargets.length && (i = queuedExplicitHydrationTargets[0], null === i.blockedOn);) attemptExplicitHydrationTarget(i), null === i.blockedOn && queuedExplicitHydrationTargets.shift();
			i = (unblocked.ownerDocument || unblocked).$$reactFormReplay;
			if (null != i) for (queuedTarget = 0; queuedTarget < i.length; queuedTarget += 3) {
				var form = i[queuedTarget], submitterOrAction = i[queuedTarget + 1], formProps = form[internalPropsKey] || null;
				if ("function" === typeof submitterOrAction) formProps || scheduleReplayQueueIfNeeded(i);
				else if (formProps) {
					var action = null;
					if (submitterOrAction && submitterOrAction.hasAttribute("formAction")) {
						if (form = submitterOrAction, formProps = submitterOrAction[internalPropsKey] || null) action = formProps.formAction;
						else if (null !== findInstanceBlockingTarget(form)) continue;
					} else action = formProps.action;
					"function" === typeof action ? i[queuedTarget + 1] = action : (i.splice(queuedTarget, 3), queuedTarget -= 3);
					scheduleReplayQueueIfNeeded(i);
				}
			}
		}
		function defaultOnDefaultTransitionIndicator() {
			function handleNavigate(event) {
				event.canIntercept && "react-transition" === event.info && event.intercept({
					handler: function() {
						return new Promise(function(resolve) {
							return pendingResolve = resolve;
						});
					},
					focusReset: "manual",
					scroll: "manual"
				});
			}
			function handleNavigateComplete() {
				null !== pendingResolve && (pendingResolve(), pendingResolve = null);
				isCancelled || setTimeout(startFakeNavigation, 20);
			}
			function startFakeNavigation() {
				if (!isCancelled && !navigation.transition) {
					var currentEntry = navigation.currentEntry;
					currentEntry && null != currentEntry.url && navigation.navigate(currentEntry.url, {
						state: currentEntry.getState(),
						info: "react-transition",
						history: "replace"
					});
				}
			}
			if ("object" === typeof navigation) {
				var isCancelled = !1, pendingResolve = null;
				navigation.addEventListener("navigate", handleNavigate);
				navigation.addEventListener("navigatesuccess", handleNavigateComplete);
				navigation.addEventListener("navigateerror", handleNavigateComplete);
				setTimeout(startFakeNavigation, 100);
				return function() {
					isCancelled = !0;
					navigation.removeEventListener("navigate", handleNavigate);
					navigation.removeEventListener("navigatesuccess", handleNavigateComplete);
					navigation.removeEventListener("navigateerror", handleNavigateComplete);
					null !== pendingResolve && (pendingResolve(), pendingResolve = null);
				};
			}
		}
		function ReactDOMRoot(internalRoot) {
			this._internalRoot = internalRoot;
		}
		function ReactDOMHydrationRoot(internalRoot) {
			this._internalRoot = internalRoot;
		}
		function warnIfReactDOMContainerInDEV(container) {
			container[internalContainerInstanceKey] && (container._reactRootContainer ? console.error("You are calling ReactDOMClient.createRoot() on a container that was previously passed to ReactDOM.render(). This is not supported.") : console.error("You are calling ReactDOMClient.createRoot() on a container that has already been passed to createRoot() before. Instead, call root.render() on the existing root instead if you want to update it."));
		}
		"undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
		var Scheduler = require_scheduler(), React = require_react(), ReactDOM = require_react_dom(), assign = Object.assign, REACT_LEGACY_ELEMENT_TYPE = Symbol.for("react.element"), REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element"), REACT_PORTAL_TYPE = Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = Symbol.for("react.profiler"), REACT_CONSUMER_TYPE = Symbol.for("react.consumer"), REACT_CONTEXT_TYPE = Symbol.for("react.context"), REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = Symbol.for("react.suspense"), REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list"), REACT_MEMO_TYPE = Symbol.for("react.memo"), REACT_LAZY_TYPE = Symbol.for("react.lazy");
		var REACT_ACTIVITY_TYPE = Symbol.for("react.activity");
		var REACT_MEMO_CACHE_SENTINEL = Symbol.for("react.memo_cache_sentinel");
		var MAYBE_ITERATOR_SYMBOL = Symbol.iterator, REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference"), isArrayImpl = Array.isArray, ReactSharedInternals = React.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, ReactDOMSharedInternals = ReactDOM.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, NotPending = Object.freeze({
			pending: !1,
			data: null,
			method: null,
			action: null
		}), valueStack = [];
		var fiberStack = [];
		var index$jscomp$0 = -1, contextStackCursor = createCursor(null), contextFiberStackCursor = createCursor(null), rootInstanceStackCursor = createCursor(null), hostTransitionProviderCursor = createCursor(null), disabledDepth = 0, prevLog, prevInfo, prevWarn, prevError, prevGroup, prevGroupCollapsed, prevGroupEnd;
		disabledLog.__reactDisabledLog = !0;
		var prefix, suffix, reentry = !1;
		var componentFrameCache = new ("function" === typeof WeakMap ? WeakMap : Map)();
		var current = null, isRendering = !1, hasOwnProperty = Object.prototype.hasOwnProperty, scheduleCallback$3 = Scheduler.unstable_scheduleCallback, cancelCallback$1 = Scheduler.unstable_cancelCallback, shouldYield = Scheduler.unstable_shouldYield, requestPaint = Scheduler.unstable_requestPaint, now$1 = Scheduler.unstable_now, getCurrentPriorityLevel = Scheduler.unstable_getCurrentPriorityLevel, ImmediatePriority = Scheduler.unstable_ImmediatePriority, UserBlockingPriority = Scheduler.unstable_UserBlockingPriority, NormalPriority$1 = Scheduler.unstable_NormalPriority, LowPriority = Scheduler.unstable_LowPriority, IdlePriority = Scheduler.unstable_IdlePriority, log$1 = Scheduler.log, unstable_setDisableYieldValue = Scheduler.unstable_setDisableYieldValue, rendererID = null, injectedHook = null, hasLoggedError = !1, isDevToolsPresent = "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__, clz32 = Math.clz32 ? Math.clz32 : clz32Fallback, log = Math.log, LN2 = Math.LN2, nextTransitionUpdateLane = 256, nextTransitionDeferredLane = 262144, nextRetryLane = 4194304, DiscreteEventPriority = 2, ContinuousEventPriority = 8, DefaultEventPriority = 32, IdleEventPriority = 268435456, randomKey = Math.random().toString(36).slice(2), internalInstanceKey = "__reactFiber$" + randomKey, internalPropsKey = "__reactProps$" + randomKey, internalContainerInstanceKey = "__reactContainer$" + randomKey, internalEventHandlersKey = "__reactEvents$" + randomKey, internalEventHandlerListenersKey = "__reactListeners$" + randomKey, internalEventHandlesSetKey = "__reactHandles$" + randomKey, internalRootNodeResourcesKey = "__reactResources$" + randomKey, internalHoistableMarker = "__reactMarker$" + randomKey, allNativeEvents = /* @__PURE__ */ new Set(), registrationNameDependencies = {}, possibleRegistrationNames = {}, hasReadOnlyValue = {
			button: !0,
			checkbox: !0,
			image: !0,
			hidden: !0,
			radio: !0,
			reset: !0,
			submit: !0
		}, VALID_ATTRIBUTE_NAME_REGEX = RegExp("^[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"), illegalAttributeNameCache = {}, validatedAttributeNameCache = {}, escapeSelectorAttributeValueInsideDoubleQuotesRegex = /[\n"\\]/g, didWarnValueDefaultValue$1 = !1, didWarnCheckedDefaultChecked = !1, didWarnSelectedSetOnOption = !1, didWarnInvalidChild = !1, didWarnInvalidInnerHTML = !1;
		var didWarnValueDefaultValue = !1;
		var valuePropNames = ["value", "defaultValue"], didWarnValDefaultVal = !1, needsEscaping = /["'&<>\n\t]|^\s|\s$/, specialTags = "address applet area article aside base basefont bgsound blockquote body br button caption center col colgroup dd details dir div dl dt embed fieldset figcaption figure footer form frame frameset h1 h2 h3 h4 h5 h6 head header hgroup hr html iframe img input isindex li link listing main marquee menu menuitem meta nav noembed noframes noscript object ol p param plaintext pre script section select source style summary table tbody td template textarea tfoot th thead title tr track ul wbr xmp".split(" "), inScopeTags = "applet caption html table td th marquee object template foreignObject desc title".split(" "), buttonScopeTags = inScopeTags.concat(["button"]), impliedEndTags = "dd dt li option optgroup p rp rt".split(" "), emptyAncestorInfoDev = {
			current: null,
			formTag: null,
			aTagInScope: null,
			buttonTagInScope: null,
			nobrTagInScope: null,
			pTagInButtonScope: null,
			listItemTagAutoclosing: null,
			dlItemTagAutoclosing: null,
			containerTagInScope: null,
			implicitRootScope: !1
		}, didWarn = {}, shorthandToLonghand = {
			animation: "animationDelay animationDirection animationDuration animationFillMode animationIterationCount animationName animationPlayState animationTimingFunction".split(" "),
			background: "backgroundAttachment backgroundClip backgroundColor backgroundImage backgroundOrigin backgroundPositionX backgroundPositionY backgroundRepeat backgroundSize".split(" "),
			backgroundPosition: ["backgroundPositionX", "backgroundPositionY"],
			border: "borderBottomColor borderBottomStyle borderBottomWidth borderImageOutset borderImageRepeat borderImageSlice borderImageSource borderImageWidth borderLeftColor borderLeftStyle borderLeftWidth borderRightColor borderRightStyle borderRightWidth borderTopColor borderTopStyle borderTopWidth".split(" "),
			borderBlockEnd: [
				"borderBlockEndColor",
				"borderBlockEndStyle",
				"borderBlockEndWidth"
			],
			borderBlockStart: [
				"borderBlockStartColor",
				"borderBlockStartStyle",
				"borderBlockStartWidth"
			],
			borderBottom: [
				"borderBottomColor",
				"borderBottomStyle",
				"borderBottomWidth"
			],
			borderColor: [
				"borderBottomColor",
				"borderLeftColor",
				"borderRightColor",
				"borderTopColor"
			],
			borderImage: [
				"borderImageOutset",
				"borderImageRepeat",
				"borderImageSlice",
				"borderImageSource",
				"borderImageWidth"
			],
			borderInlineEnd: [
				"borderInlineEndColor",
				"borderInlineEndStyle",
				"borderInlineEndWidth"
			],
			borderInlineStart: [
				"borderInlineStartColor",
				"borderInlineStartStyle",
				"borderInlineStartWidth"
			],
			borderLeft: [
				"borderLeftColor",
				"borderLeftStyle",
				"borderLeftWidth"
			],
			borderRadius: [
				"borderBottomLeftRadius",
				"borderBottomRightRadius",
				"borderTopLeftRadius",
				"borderTopRightRadius"
			],
			borderRight: [
				"borderRightColor",
				"borderRightStyle",
				"borderRightWidth"
			],
			borderStyle: [
				"borderBottomStyle",
				"borderLeftStyle",
				"borderRightStyle",
				"borderTopStyle"
			],
			borderTop: [
				"borderTopColor",
				"borderTopStyle",
				"borderTopWidth"
			],
			borderWidth: [
				"borderBottomWidth",
				"borderLeftWidth",
				"borderRightWidth",
				"borderTopWidth"
			],
			columnRule: [
				"columnRuleColor",
				"columnRuleStyle",
				"columnRuleWidth"
			],
			columns: ["columnCount", "columnWidth"],
			flex: [
				"flexBasis",
				"flexGrow",
				"flexShrink"
			],
			flexFlow: ["flexDirection", "flexWrap"],
			font: "fontFamily fontFeatureSettings fontKerning fontLanguageOverride fontSize fontSizeAdjust fontStretch fontStyle fontVariant fontVariantAlternates fontVariantCaps fontVariantEastAsian fontVariantLigatures fontVariantNumeric fontVariantPosition fontWeight lineHeight".split(" "),
			fontVariant: "fontVariantAlternates fontVariantCaps fontVariantEastAsian fontVariantLigatures fontVariantNumeric fontVariantPosition".split(" "),
			gap: ["columnGap", "rowGap"],
			grid: "gridAutoColumns gridAutoFlow gridAutoRows gridTemplateAreas gridTemplateColumns gridTemplateRows".split(" "),
			gridArea: [
				"gridColumnEnd",
				"gridColumnStart",
				"gridRowEnd",
				"gridRowStart"
			],
			gridColumn: ["gridColumnEnd", "gridColumnStart"],
			gridColumnGap: ["columnGap"],
			gridGap: ["columnGap", "rowGap"],
			gridRow: ["gridRowEnd", "gridRowStart"],
			gridRowGap: ["rowGap"],
			gridTemplate: [
				"gridTemplateAreas",
				"gridTemplateColumns",
				"gridTemplateRows"
			],
			listStyle: [
				"listStyleImage",
				"listStylePosition",
				"listStyleType"
			],
			margin: [
				"marginBottom",
				"marginLeft",
				"marginRight",
				"marginTop"
			],
			marker: [
				"markerEnd",
				"markerMid",
				"markerStart"
			],
			mask: "maskClip maskComposite maskImage maskMode maskOrigin maskPositionX maskPositionY maskRepeat maskSize".split(" "),
			maskPosition: ["maskPositionX", "maskPositionY"],
			outline: [
				"outlineColor",
				"outlineStyle",
				"outlineWidth"
			],
			overflow: ["overflowX", "overflowY"],
			padding: [
				"paddingBottom",
				"paddingLeft",
				"paddingRight",
				"paddingTop"
			],
			placeContent: ["alignContent", "justifyContent"],
			placeItems: ["alignItems", "justifyItems"],
			placeSelf: ["alignSelf", "justifySelf"],
			textDecoration: [
				"textDecorationColor",
				"textDecorationLine",
				"textDecorationStyle"
			],
			textEmphasis: ["textEmphasisColor", "textEmphasisStyle"],
			transition: [
				"transitionDelay",
				"transitionDuration",
				"transitionProperty",
				"transitionTimingFunction"
			],
			wordWrap: ["overflowWrap"]
		}, uppercasePattern = /([A-Z])/g, msPattern$1 = /^ms-/, badVendoredStyleNamePattern = /^(?:webkit|moz|o)[A-Z]/, msPattern = /^-ms-/, hyphenPattern = /-(.)/g, badStyleValueWithSemicolonPattern = /;\s*$/, warnedStyleNames = {}, warnedStyleValues = {}, warnedForNaNValue = !1, warnedForInfinityValue = !1, unitlessNumbers = new Set("animationIterationCount aspectRatio borderImageOutset borderImageSlice borderImageWidth boxFlex boxFlexGroup boxOrdinalGroup columnCount columns flex flexGrow flexPositive flexShrink flexNegative flexOrder gridArea gridRow gridRowEnd gridRowSpan gridRowStart gridColumn gridColumnEnd gridColumnSpan gridColumnStart fontWeight lineClamp lineHeight opacity order orphans scale tabSize widows zIndex zoom fillOpacity floodOpacity stopOpacity strokeDasharray strokeDashoffset strokeMiterlimit strokeOpacity strokeWidth MozAnimationIterationCount MozBoxFlex MozBoxFlexGroup MozLineClamp msAnimationIterationCount msFlex msZoom msFlexGrow msFlexNegative msFlexOrder msFlexPositive msFlexShrink msGridColumn msGridColumnSpan msGridRow msGridRowSpan WebkitAnimationIterationCount WebkitBoxFlex WebKitBoxFlexGroup WebkitBoxOrdinalGroup WebkitColumnCount WebkitColumns WebkitFlex WebkitFlexGrow WebkitFlexPositive WebkitFlexShrink WebkitLineClamp".split(" ")), MATH_NAMESPACE = "http://www.w3.org/1998/Math/MathML", SVG_NAMESPACE = "http://www.w3.org/2000/svg", aliases = /* @__PURE__ */ new Map([
			["acceptCharset", "accept-charset"],
			["htmlFor", "for"],
			["httpEquiv", "http-equiv"],
			["crossOrigin", "crossorigin"],
			["accentHeight", "accent-height"],
			["alignmentBaseline", "alignment-baseline"],
			["arabicForm", "arabic-form"],
			["baselineShift", "baseline-shift"],
			["capHeight", "cap-height"],
			["clipPath", "clip-path"],
			["clipRule", "clip-rule"],
			["colorInterpolation", "color-interpolation"],
			["colorInterpolationFilters", "color-interpolation-filters"],
			["colorProfile", "color-profile"],
			["colorRendering", "color-rendering"],
			["dominantBaseline", "dominant-baseline"],
			["enableBackground", "enable-background"],
			["fillOpacity", "fill-opacity"],
			["fillRule", "fill-rule"],
			["floodColor", "flood-color"],
			["floodOpacity", "flood-opacity"],
			["fontFamily", "font-family"],
			["fontSize", "font-size"],
			["fontSizeAdjust", "font-size-adjust"],
			["fontStretch", "font-stretch"],
			["fontStyle", "font-style"],
			["fontVariant", "font-variant"],
			["fontWeight", "font-weight"],
			["glyphName", "glyph-name"],
			["glyphOrientationHorizontal", "glyph-orientation-horizontal"],
			["glyphOrientationVertical", "glyph-orientation-vertical"],
			["horizAdvX", "horiz-adv-x"],
			["horizOriginX", "horiz-origin-x"],
			["imageRendering", "image-rendering"],
			["letterSpacing", "letter-spacing"],
			["lightingColor", "lighting-color"],
			["markerEnd", "marker-end"],
			["markerMid", "marker-mid"],
			["markerStart", "marker-start"],
			["overlinePosition", "overline-position"],
			["overlineThickness", "overline-thickness"],
			["paintOrder", "paint-order"],
			["panose-1", "panose-1"],
			["pointerEvents", "pointer-events"],
			["renderingIntent", "rendering-intent"],
			["shapeRendering", "shape-rendering"],
			["stopColor", "stop-color"],
			["stopOpacity", "stop-opacity"],
			["strikethroughPosition", "strikethrough-position"],
			["strikethroughThickness", "strikethrough-thickness"],
			["strokeDasharray", "stroke-dasharray"],
			["strokeDashoffset", "stroke-dashoffset"],
			["strokeLinecap", "stroke-linecap"],
			["strokeLinejoin", "stroke-linejoin"],
			["strokeMiterlimit", "stroke-miterlimit"],
			["strokeOpacity", "stroke-opacity"],
			["strokeWidth", "stroke-width"],
			["textAnchor", "text-anchor"],
			["textDecoration", "text-decoration"],
			["textRendering", "text-rendering"],
			["transformOrigin", "transform-origin"],
			["underlinePosition", "underline-position"],
			["underlineThickness", "underline-thickness"],
			["unicodeBidi", "unicode-bidi"],
			["unicodeRange", "unicode-range"],
			["unitsPerEm", "units-per-em"],
			["vAlphabetic", "v-alphabetic"],
			["vHanging", "v-hanging"],
			["vIdeographic", "v-ideographic"],
			["vMathematical", "v-mathematical"],
			["vectorEffect", "vector-effect"],
			["vertAdvY", "vert-adv-y"],
			["vertOriginX", "vert-origin-x"],
			["vertOriginY", "vert-origin-y"],
			["wordSpacing", "word-spacing"],
			["writingMode", "writing-mode"],
			["xmlnsXlink", "xmlns:xlink"],
			["xHeight", "x-height"]
		]), possibleStandardNames = {
			accept: "accept",
			acceptcharset: "acceptCharset",
			"accept-charset": "acceptCharset",
			accesskey: "accessKey",
			action: "action",
			allowfullscreen: "allowFullScreen",
			alt: "alt",
			as: "as",
			async: "async",
			autocapitalize: "autoCapitalize",
			autocomplete: "autoComplete",
			autocorrect: "autoCorrect",
			autofocus: "autoFocus",
			autoplay: "autoPlay",
			autosave: "autoSave",
			capture: "capture",
			cellpadding: "cellPadding",
			cellspacing: "cellSpacing",
			challenge: "challenge",
			charset: "charSet",
			checked: "checked",
			children: "children",
			cite: "cite",
			class: "className",
			classid: "classID",
			classname: "className",
			cols: "cols",
			colspan: "colSpan",
			content: "content",
			contenteditable: "contentEditable",
			contextmenu: "contextMenu",
			controls: "controls",
			controlslist: "controlsList",
			coords: "coords",
			crossorigin: "crossOrigin",
			dangerouslysetinnerhtml: "dangerouslySetInnerHTML",
			data: "data",
			datetime: "dateTime",
			default: "default",
			defaultchecked: "defaultChecked",
			defaultvalue: "defaultValue",
			defer: "defer",
			dir: "dir",
			disabled: "disabled",
			disablepictureinpicture: "disablePictureInPicture",
			disableremoteplayback: "disableRemotePlayback",
			download: "download",
			draggable: "draggable",
			enctype: "encType",
			enterkeyhint: "enterKeyHint",
			fetchpriority: "fetchPriority",
			for: "htmlFor",
			form: "form",
			formmethod: "formMethod",
			formaction: "formAction",
			formenctype: "formEncType",
			formnovalidate: "formNoValidate",
			formtarget: "formTarget",
			frameborder: "frameBorder",
			headers: "headers",
			height: "height",
			hidden: "hidden",
			high: "high",
			href: "href",
			hreflang: "hrefLang",
			htmlfor: "htmlFor",
			httpequiv: "httpEquiv",
			"http-equiv": "httpEquiv",
			icon: "icon",
			id: "id",
			imagesizes: "imageSizes",
			imagesrcset: "imageSrcSet",
			inert: "inert",
			innerhtml: "innerHTML",
			inputmode: "inputMode",
			integrity: "integrity",
			is: "is",
			itemid: "itemID",
			itemprop: "itemProp",
			itemref: "itemRef",
			itemscope: "itemScope",
			itemtype: "itemType",
			keyparams: "keyParams",
			keytype: "keyType",
			kind: "kind",
			label: "label",
			lang: "lang",
			list: "list",
			loop: "loop",
			low: "low",
			manifest: "manifest",
			marginwidth: "marginWidth",
			marginheight: "marginHeight",
			max: "max",
			maxlength: "maxLength",
			media: "media",
			mediagroup: "mediaGroup",
			method: "method",
			min: "min",
			minlength: "minLength",
			multiple: "multiple",
			muted: "muted",
			name: "name",
			nomodule: "noModule",
			nonce: "nonce",
			novalidate: "noValidate",
			open: "open",
			optimum: "optimum",
			pattern: "pattern",
			placeholder: "placeholder",
			playsinline: "playsInline",
			poster: "poster",
			preload: "preload",
			profile: "profile",
			radiogroup: "radioGroup",
			readonly: "readOnly",
			referrerpolicy: "referrerPolicy",
			rel: "rel",
			required: "required",
			reversed: "reversed",
			role: "role",
			rows: "rows",
			rowspan: "rowSpan",
			sandbox: "sandbox",
			scope: "scope",
			scoped: "scoped",
			scrolling: "scrolling",
			seamless: "seamless",
			selected: "selected",
			shape: "shape",
			size: "size",
			sizes: "sizes",
			span: "span",
			spellcheck: "spellCheck",
			src: "src",
			srcdoc: "srcDoc",
			srclang: "srcLang",
			srcset: "srcSet",
			start: "start",
			step: "step",
			style: "style",
			summary: "summary",
			tabindex: "tabIndex",
			target: "target",
			title: "title",
			type: "type",
			usemap: "useMap",
			value: "value",
			width: "width",
			wmode: "wmode",
			wrap: "wrap",
			about: "about",
			accentheight: "accentHeight",
			"accent-height": "accentHeight",
			accumulate: "accumulate",
			additive: "additive",
			alignmentbaseline: "alignmentBaseline",
			"alignment-baseline": "alignmentBaseline",
			allowreorder: "allowReorder",
			alphabetic: "alphabetic",
			amplitude: "amplitude",
			arabicform: "arabicForm",
			"arabic-form": "arabicForm",
			ascent: "ascent",
			attributename: "attributeName",
			attributetype: "attributeType",
			autoreverse: "autoReverse",
			azimuth: "azimuth",
			basefrequency: "baseFrequency",
			baselineshift: "baselineShift",
			"baseline-shift": "baselineShift",
			baseprofile: "baseProfile",
			bbox: "bbox",
			begin: "begin",
			bias: "bias",
			by: "by",
			calcmode: "calcMode",
			capheight: "capHeight",
			"cap-height": "capHeight",
			clip: "clip",
			clippath: "clipPath",
			"clip-path": "clipPath",
			clippathunits: "clipPathUnits",
			cliprule: "clipRule",
			"clip-rule": "clipRule",
			color: "color",
			colorinterpolation: "colorInterpolation",
			"color-interpolation": "colorInterpolation",
			colorinterpolationfilters: "colorInterpolationFilters",
			"color-interpolation-filters": "colorInterpolationFilters",
			colorprofile: "colorProfile",
			"color-profile": "colorProfile",
			colorrendering: "colorRendering",
			"color-rendering": "colorRendering",
			contentscripttype: "contentScriptType",
			contentstyletype: "contentStyleType",
			cursor: "cursor",
			cx: "cx",
			cy: "cy",
			d: "d",
			datatype: "datatype",
			decelerate: "decelerate",
			descent: "descent",
			diffuseconstant: "diffuseConstant",
			direction: "direction",
			display: "display",
			divisor: "divisor",
			dominantbaseline: "dominantBaseline",
			"dominant-baseline": "dominantBaseline",
			dur: "dur",
			dx: "dx",
			dy: "dy",
			edgemode: "edgeMode",
			elevation: "elevation",
			enablebackground: "enableBackground",
			"enable-background": "enableBackground",
			end: "end",
			exponent: "exponent",
			externalresourcesrequired: "externalResourcesRequired",
			fill: "fill",
			fillopacity: "fillOpacity",
			"fill-opacity": "fillOpacity",
			fillrule: "fillRule",
			"fill-rule": "fillRule",
			filter: "filter",
			filterres: "filterRes",
			filterunits: "filterUnits",
			floodopacity: "floodOpacity",
			"flood-opacity": "floodOpacity",
			floodcolor: "floodColor",
			"flood-color": "floodColor",
			focusable: "focusable",
			fontfamily: "fontFamily",
			"font-family": "fontFamily",
			fontsize: "fontSize",
			"font-size": "fontSize",
			fontsizeadjust: "fontSizeAdjust",
			"font-size-adjust": "fontSizeAdjust",
			fontstretch: "fontStretch",
			"font-stretch": "fontStretch",
			fontstyle: "fontStyle",
			"font-style": "fontStyle",
			fontvariant: "fontVariant",
			"font-variant": "fontVariant",
			fontweight: "fontWeight",
			"font-weight": "fontWeight",
			format: "format",
			from: "from",
			fx: "fx",
			fy: "fy",
			g1: "g1",
			g2: "g2",
			glyphname: "glyphName",
			"glyph-name": "glyphName",
			glyphorientationhorizontal: "glyphOrientationHorizontal",
			"glyph-orientation-horizontal": "glyphOrientationHorizontal",
			glyphorientationvertical: "glyphOrientationVertical",
			"glyph-orientation-vertical": "glyphOrientationVertical",
			glyphref: "glyphRef",
			gradienttransform: "gradientTransform",
			gradientunits: "gradientUnits",
			hanging: "hanging",
			horizadvx: "horizAdvX",
			"horiz-adv-x": "horizAdvX",
			horizoriginx: "horizOriginX",
			"horiz-origin-x": "horizOriginX",
			ideographic: "ideographic",
			imagerendering: "imageRendering",
			"image-rendering": "imageRendering",
			in2: "in2",
			in: "in",
			inlist: "inlist",
			intercept: "intercept",
			k1: "k1",
			k2: "k2",
			k3: "k3",
			k4: "k4",
			k: "k",
			kernelmatrix: "kernelMatrix",
			kernelunitlength: "kernelUnitLength",
			kerning: "kerning",
			keypoints: "keyPoints",
			keysplines: "keySplines",
			keytimes: "keyTimes",
			lengthadjust: "lengthAdjust",
			letterspacing: "letterSpacing",
			"letter-spacing": "letterSpacing",
			lightingcolor: "lightingColor",
			"lighting-color": "lightingColor",
			limitingconeangle: "limitingConeAngle",
			local: "local",
			markerend: "markerEnd",
			"marker-end": "markerEnd",
			markerheight: "markerHeight",
			markermid: "markerMid",
			"marker-mid": "markerMid",
			markerstart: "markerStart",
			"marker-start": "markerStart",
			markerunits: "markerUnits",
			markerwidth: "markerWidth",
			mask: "mask",
			maskcontentunits: "maskContentUnits",
			maskunits: "maskUnits",
			mathematical: "mathematical",
			mode: "mode",
			numoctaves: "numOctaves",
			offset: "offset",
			opacity: "opacity",
			operator: "operator",
			order: "order",
			orient: "orient",
			orientation: "orientation",
			origin: "origin",
			overflow: "overflow",
			overlineposition: "overlinePosition",
			"overline-position": "overlinePosition",
			overlinethickness: "overlineThickness",
			"overline-thickness": "overlineThickness",
			paintorder: "paintOrder",
			"paint-order": "paintOrder",
			panose1: "panose1",
			"panose-1": "panose1",
			pathlength: "pathLength",
			patterncontentunits: "patternContentUnits",
			patterntransform: "patternTransform",
			patternunits: "patternUnits",
			pointerevents: "pointerEvents",
			"pointer-events": "pointerEvents",
			points: "points",
			pointsatx: "pointsAtX",
			pointsaty: "pointsAtY",
			pointsatz: "pointsAtZ",
			popover: "popover",
			popovertarget: "popoverTarget",
			popovertargetaction: "popoverTargetAction",
			prefix: "prefix",
			preservealpha: "preserveAlpha",
			preserveaspectratio: "preserveAspectRatio",
			primitiveunits: "primitiveUnits",
			property: "property",
			r: "r",
			radius: "radius",
			refx: "refX",
			refy: "refY",
			renderingintent: "renderingIntent",
			"rendering-intent": "renderingIntent",
			repeatcount: "repeatCount",
			repeatdur: "repeatDur",
			requiredextensions: "requiredExtensions",
			requiredfeatures: "requiredFeatures",
			resource: "resource",
			restart: "restart",
			result: "result",
			results: "results",
			rotate: "rotate",
			rx: "rx",
			ry: "ry",
			scale: "scale",
			security: "security",
			seed: "seed",
			shaperendering: "shapeRendering",
			"shape-rendering": "shapeRendering",
			slope: "slope",
			spacing: "spacing",
			specularconstant: "specularConstant",
			specularexponent: "specularExponent",
			speed: "speed",
			spreadmethod: "spreadMethod",
			startoffset: "startOffset",
			stddeviation: "stdDeviation",
			stemh: "stemh",
			stemv: "stemv",
			stitchtiles: "stitchTiles",
			stopcolor: "stopColor",
			"stop-color": "stopColor",
			stopopacity: "stopOpacity",
			"stop-opacity": "stopOpacity",
			strikethroughposition: "strikethroughPosition",
			"strikethrough-position": "strikethroughPosition",
			strikethroughthickness: "strikethroughThickness",
			"strikethrough-thickness": "strikethroughThickness",
			string: "string",
			stroke: "stroke",
			strokedasharray: "strokeDasharray",
			"stroke-dasharray": "strokeDasharray",
			strokedashoffset: "strokeDashoffset",
			"stroke-dashoffset": "strokeDashoffset",
			strokelinecap: "strokeLinecap",
			"stroke-linecap": "strokeLinecap",
			strokelinejoin: "strokeLinejoin",
			"stroke-linejoin": "strokeLinejoin",
			strokemiterlimit: "strokeMiterlimit",
			"stroke-miterlimit": "strokeMiterlimit",
			strokewidth: "strokeWidth",
			"stroke-width": "strokeWidth",
			strokeopacity: "strokeOpacity",
			"stroke-opacity": "strokeOpacity",
			suppresscontenteditablewarning: "suppressContentEditableWarning",
			suppresshydrationwarning: "suppressHydrationWarning",
			surfacescale: "surfaceScale",
			systemlanguage: "systemLanguage",
			tablevalues: "tableValues",
			targetx: "targetX",
			targety: "targetY",
			textanchor: "textAnchor",
			"text-anchor": "textAnchor",
			textdecoration: "textDecoration",
			"text-decoration": "textDecoration",
			textlength: "textLength",
			textrendering: "textRendering",
			"text-rendering": "textRendering",
			to: "to",
			transform: "transform",
			transformorigin: "transformOrigin",
			"transform-origin": "transformOrigin",
			typeof: "typeof",
			u1: "u1",
			u2: "u2",
			underlineposition: "underlinePosition",
			"underline-position": "underlinePosition",
			underlinethickness: "underlineThickness",
			"underline-thickness": "underlineThickness",
			unicode: "unicode",
			unicodebidi: "unicodeBidi",
			"unicode-bidi": "unicodeBidi",
			unicoderange: "unicodeRange",
			"unicode-range": "unicodeRange",
			unitsperem: "unitsPerEm",
			"units-per-em": "unitsPerEm",
			unselectable: "unselectable",
			valphabetic: "vAlphabetic",
			"v-alphabetic": "vAlphabetic",
			values: "values",
			vectoreffect: "vectorEffect",
			"vector-effect": "vectorEffect",
			version: "version",
			vertadvy: "vertAdvY",
			"vert-adv-y": "vertAdvY",
			vertoriginx: "vertOriginX",
			"vert-origin-x": "vertOriginX",
			vertoriginy: "vertOriginY",
			"vert-origin-y": "vertOriginY",
			vhanging: "vHanging",
			"v-hanging": "vHanging",
			videographic: "vIdeographic",
			"v-ideographic": "vIdeographic",
			viewbox: "viewBox",
			viewtarget: "viewTarget",
			visibility: "visibility",
			vmathematical: "vMathematical",
			"v-mathematical": "vMathematical",
			vocab: "vocab",
			widths: "widths",
			wordspacing: "wordSpacing",
			"word-spacing": "wordSpacing",
			writingmode: "writingMode",
			"writing-mode": "writingMode",
			x1: "x1",
			x2: "x2",
			x: "x",
			xchannelselector: "xChannelSelector",
			xheight: "xHeight",
			"x-height": "xHeight",
			xlinkactuate: "xlinkActuate",
			"xlink:actuate": "xlinkActuate",
			xlinkarcrole: "xlinkArcrole",
			"xlink:arcrole": "xlinkArcrole",
			xlinkhref: "xlinkHref",
			"xlink:href": "xlinkHref",
			xlinkrole: "xlinkRole",
			"xlink:role": "xlinkRole",
			xlinkshow: "xlinkShow",
			"xlink:show": "xlinkShow",
			xlinktitle: "xlinkTitle",
			"xlink:title": "xlinkTitle",
			xlinktype: "xlinkType",
			"xlink:type": "xlinkType",
			xmlbase: "xmlBase",
			"xml:base": "xmlBase",
			xmllang: "xmlLang",
			"xml:lang": "xmlLang",
			xmlns: "xmlns",
			"xml:space": "xmlSpace",
			xmlnsxlink: "xmlnsXlink",
			"xmlns:xlink": "xmlnsXlink",
			xmlspace: "xmlSpace",
			y1: "y1",
			y2: "y2",
			y: "y",
			ychannelselector: "yChannelSelector",
			z: "z",
			zoomandpan: "zoomAndPan"
		}, ariaProperties = {
			"aria-current": 0,
			"aria-description": 0,
			"aria-details": 0,
			"aria-disabled": 0,
			"aria-hidden": 0,
			"aria-invalid": 0,
			"aria-keyshortcuts": 0,
			"aria-label": 0,
			"aria-roledescription": 0,
			"aria-autocomplete": 0,
			"aria-checked": 0,
			"aria-expanded": 0,
			"aria-haspopup": 0,
			"aria-level": 0,
			"aria-modal": 0,
			"aria-multiline": 0,
			"aria-multiselectable": 0,
			"aria-orientation": 0,
			"aria-placeholder": 0,
			"aria-pressed": 0,
			"aria-readonly": 0,
			"aria-required": 0,
			"aria-selected": 0,
			"aria-sort": 0,
			"aria-valuemax": 0,
			"aria-valuemin": 0,
			"aria-valuenow": 0,
			"aria-valuetext": 0,
			"aria-atomic": 0,
			"aria-busy": 0,
			"aria-live": 0,
			"aria-relevant": 0,
			"aria-dropeffect": 0,
			"aria-grabbed": 0,
			"aria-activedescendant": 0,
			"aria-colcount": 0,
			"aria-colindex": 0,
			"aria-colspan": 0,
			"aria-controls": 0,
			"aria-describedby": 0,
			"aria-errormessage": 0,
			"aria-flowto": 0,
			"aria-labelledby": 0,
			"aria-owns": 0,
			"aria-posinset": 0,
			"aria-rowcount": 0,
			"aria-rowindex": 0,
			"aria-rowspan": 0,
			"aria-setsize": 0,
			"aria-braillelabel": 0,
			"aria-brailleroledescription": 0,
			"aria-colindextext": 0,
			"aria-rowindextext": 0
		}, warnedProperties$1 = {}, rARIA$1 = RegExp("^(aria)-[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"), rARIACamel$1 = RegExp("^(aria)[A-Z][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"), didWarnValueNull = !1, warnedProperties = {}, EVENT_NAME_REGEX = /^on./, INVALID_EVENT_NAME_REGEX = /^on[^A-Z]/, rARIA = RegExp("^(aria)-[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"), rARIACamel = RegExp("^(aria)[A-Z][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"), isJavaScriptProtocol = /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*:/i, currentReplayingEvent = null, restoreTarget = null, restoreQueue = null, isInsideEventHandler = !1, canUseDOM = !("undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement), passiveBrowserEventsSupported = !1;
		if (canUseDOM) try {
			var options$jscomp$0 = {};
			Object.defineProperty(options$jscomp$0, "passive", { get: function() {
				passiveBrowserEventsSupported = !0;
			} });
			window.addEventListener("test", options$jscomp$0, options$jscomp$0);
			window.removeEventListener("test", options$jscomp$0, options$jscomp$0);
		} catch (e) {
			passiveBrowserEventsSupported = !1;
		}
		var root = null, startText = null, fallbackText = null, EventInterface = {
			eventPhase: 0,
			bubbles: 0,
			cancelable: 0,
			timeStamp: function(event) {
				return event.timeStamp || Date.now();
			},
			defaultPrevented: 0,
			isTrusted: 0
		}, SyntheticEvent = createSyntheticEvent(EventInterface), UIEventInterface = assign({}, EventInterface, {
			view: 0,
			detail: 0
		}), SyntheticUIEvent = createSyntheticEvent(UIEventInterface), lastMovementX, lastMovementY, lastMouseEvent, MouseEventInterface = assign({}, UIEventInterface, {
			screenX: 0,
			screenY: 0,
			clientX: 0,
			clientY: 0,
			pageX: 0,
			pageY: 0,
			ctrlKey: 0,
			shiftKey: 0,
			altKey: 0,
			metaKey: 0,
			getModifierState: getEventModifierState,
			button: 0,
			buttons: 0,
			relatedTarget: function(event) {
				return void 0 === event.relatedTarget ? event.fromElement === event.srcElement ? event.toElement : event.fromElement : event.relatedTarget;
			},
			movementX: function(event) {
				if ("movementX" in event) return event.movementX;
				event !== lastMouseEvent && (lastMouseEvent && "mousemove" === event.type ? (lastMovementX = event.screenX - lastMouseEvent.screenX, lastMovementY = event.screenY - lastMouseEvent.screenY) : lastMovementY = lastMovementX = 0, lastMouseEvent = event);
				return lastMovementX;
			},
			movementY: function(event) {
				return "movementY" in event ? event.movementY : lastMovementY;
			}
		}), SyntheticMouseEvent = createSyntheticEvent(MouseEventInterface), SyntheticDragEvent = createSyntheticEvent(assign({}, MouseEventInterface, { dataTransfer: 0 })), SyntheticFocusEvent = createSyntheticEvent(assign({}, UIEventInterface, { relatedTarget: 0 })), SyntheticAnimationEvent = createSyntheticEvent(assign({}, EventInterface, {
			animationName: 0,
			elapsedTime: 0,
			pseudoElement: 0
		})), SyntheticClipboardEvent = createSyntheticEvent(assign({}, EventInterface, { clipboardData: function(event) {
			return "clipboardData" in event ? event.clipboardData : window.clipboardData;
		} })), SyntheticCompositionEvent = createSyntheticEvent(assign({}, EventInterface, { data: 0 })), SyntheticInputEvent = SyntheticCompositionEvent, normalizeKey = {
			Esc: "Escape",
			Spacebar: " ",
			Left: "ArrowLeft",
			Up: "ArrowUp",
			Right: "ArrowRight",
			Down: "ArrowDown",
			Del: "Delete",
			Win: "OS",
			Menu: "ContextMenu",
			Apps: "ContextMenu",
			Scroll: "ScrollLock",
			MozPrintableKey: "Unidentified"
		}, translateToKey = {
			8: "Backspace",
			9: "Tab",
			12: "Clear",
			13: "Enter",
			16: "Shift",
			17: "Control",
			18: "Alt",
			19: "Pause",
			20: "CapsLock",
			27: "Escape",
			32: " ",
			33: "PageUp",
			34: "PageDown",
			35: "End",
			36: "Home",
			37: "ArrowLeft",
			38: "ArrowUp",
			39: "ArrowRight",
			40: "ArrowDown",
			45: "Insert",
			46: "Delete",
			112: "F1",
			113: "F2",
			114: "F3",
			115: "F4",
			116: "F5",
			117: "F6",
			118: "F7",
			119: "F8",
			120: "F9",
			121: "F10",
			122: "F11",
			123: "F12",
			144: "NumLock",
			145: "ScrollLock",
			224: "Meta"
		}, modifierKeyToProp = {
			Alt: "altKey",
			Control: "ctrlKey",
			Meta: "metaKey",
			Shift: "shiftKey"
		}, SyntheticKeyboardEvent = createSyntheticEvent(assign({}, UIEventInterface, {
			key: function(nativeEvent) {
				if (nativeEvent.key) {
					var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
					if ("Unidentified" !== key) return key;
				}
				return "keypress" === nativeEvent.type ? (nativeEvent = getEventCharCode(nativeEvent), 13 === nativeEvent ? "Enter" : String.fromCharCode(nativeEvent)) : "keydown" === nativeEvent.type || "keyup" === nativeEvent.type ? translateToKey[nativeEvent.keyCode] || "Unidentified" : "";
			},
			code: 0,
			location: 0,
			ctrlKey: 0,
			shiftKey: 0,
			altKey: 0,
			metaKey: 0,
			repeat: 0,
			locale: 0,
			getModifierState: getEventModifierState,
			charCode: function(event) {
				return "keypress" === event.type ? getEventCharCode(event) : 0;
			},
			keyCode: function(event) {
				return "keydown" === event.type || "keyup" === event.type ? event.keyCode : 0;
			},
			which: function(event) {
				return "keypress" === event.type ? getEventCharCode(event) : "keydown" === event.type || "keyup" === event.type ? event.keyCode : 0;
			}
		})), SyntheticPointerEvent = createSyntheticEvent(assign({}, MouseEventInterface, {
			pointerId: 0,
			width: 0,
			height: 0,
			pressure: 0,
			tangentialPressure: 0,
			tiltX: 0,
			tiltY: 0,
			twist: 0,
			pointerType: 0,
			isPrimary: 0
		})), SyntheticTouchEvent = createSyntheticEvent(assign({}, UIEventInterface, {
			touches: 0,
			targetTouches: 0,
			changedTouches: 0,
			altKey: 0,
			metaKey: 0,
			ctrlKey: 0,
			shiftKey: 0,
			getModifierState: getEventModifierState
		})), SyntheticTransitionEvent = createSyntheticEvent(assign({}, EventInterface, {
			propertyName: 0,
			elapsedTime: 0,
			pseudoElement: 0
		})), SyntheticWheelEvent = createSyntheticEvent(assign({}, MouseEventInterface, {
			deltaX: function(event) {
				return "deltaX" in event ? event.deltaX : "wheelDeltaX" in event ? -event.wheelDeltaX : 0;
			},
			deltaY: function(event) {
				return "deltaY" in event ? event.deltaY : "wheelDeltaY" in event ? -event.wheelDeltaY : "wheelDelta" in event ? -event.wheelDelta : 0;
			},
			deltaZ: 0,
			deltaMode: 0
		})), SyntheticToggleEvent = createSyntheticEvent(assign({}, EventInterface, {
			newState: 0,
			oldState: 0
		})), END_KEYCODES = [
			9,
			13,
			27,
			32
		], START_KEYCODE = 229, canUseCompositionEvent = canUseDOM && "CompositionEvent" in window, documentMode = null;
		canUseDOM && "documentMode" in document && (documentMode = document.documentMode);
		var canUseTextInputEvent = canUseDOM && "TextEvent" in window && !documentMode, useFallbackCompositionData = canUseDOM && (!canUseCompositionEvent || documentMode && 8 < documentMode && 11 >= documentMode), SPACEBAR_CODE = 32, SPACEBAR_CHAR = String.fromCharCode(SPACEBAR_CODE), hasSpaceKeypress = !1, isComposing = !1, supportedInputTypes = {
			color: !0,
			date: !0,
			datetime: !0,
			"datetime-local": !0,
			email: !0,
			month: !0,
			number: !0,
			password: !0,
			range: !0,
			search: !0,
			tel: !0,
			text: !0,
			time: !0,
			url: !0,
			week: !0
		}, activeElement$1 = null, activeElementInst$1 = null, isInputEventSupported = !1;
		canUseDOM && (isInputEventSupported = isEventSupported("input") && (!document.documentMode || 9 < document.documentMode));
		var objectIs = "function" === typeof Object.is ? Object.is : is, skipSelectionChangeEvent = canUseDOM && "documentMode" in document && 11 >= document.documentMode, activeElement = null, activeElementInst = null, lastSelection = null, mouseDown = !1, vendorPrefixes = {
			animationend: makePrefixMap("Animation", "AnimationEnd"),
			animationiteration: makePrefixMap("Animation", "AnimationIteration"),
			animationstart: makePrefixMap("Animation", "AnimationStart"),
			transitionrun: makePrefixMap("Transition", "TransitionRun"),
			transitionstart: makePrefixMap("Transition", "TransitionStart"),
			transitioncancel: makePrefixMap("Transition", "TransitionCancel"),
			transitionend: makePrefixMap("Transition", "TransitionEnd")
		}, prefixedEventNames = {}, style = {};
		canUseDOM && (style = document.createElement("div").style, "AnimationEvent" in window || (delete vendorPrefixes.animationend.animation, delete vendorPrefixes.animationiteration.animation, delete vendorPrefixes.animationstart.animation), "TransitionEvent" in window || delete vendorPrefixes.transitionend.transition);
		var ANIMATION_END = getVendorPrefixedEventName("animationend"), ANIMATION_ITERATION = getVendorPrefixedEventName("animationiteration"), ANIMATION_START = getVendorPrefixedEventName("animationstart"), TRANSITION_RUN = getVendorPrefixedEventName("transitionrun"), TRANSITION_START = getVendorPrefixedEventName("transitionstart"), TRANSITION_CANCEL = getVendorPrefixedEventName("transitioncancel"), TRANSITION_END = getVendorPrefixedEventName("transitionend"), topLevelEventsToReactNames = /* @__PURE__ */ new Map(), simpleEventPluginEvents = "abort auxClick beforeToggle cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(" ");
		simpleEventPluginEvents.push("scrollEnd");
		var lastResetTime = 0;
		if ("object" === typeof performance && "function" === typeof performance.now) {
			var localPerformance = performance;
			var getCurrentTime = function() {
				return localPerformance.now();
			};
		} else {
			var localDate = Date;
			getCurrentTime = function() {
				return localDate.now();
			};
		}
		var reportGlobalError = "function" === typeof reportError ? reportError : function(error) {
			if ("object" === typeof window && "function" === typeof window.ErrorEvent) {
				var event = new window.ErrorEvent("error", {
					bubbles: !0,
					cancelable: !0,
					message: "object" === typeof error && null !== error && "string" === typeof error.message ? String(error.message) : String(error),
					error
				});
				if (!window.dispatchEvent(event)) return;
			} else if ("object" === typeof process && "function" === typeof process.emit) {
				process.emit("uncaughtException", error);
				return;
			}
			console.error(error);
		}, OMITTED_PROP_ERROR = "This object has been omitted by React in the console log to avoid sending too much data from the server. Try logging smaller or more specific objects.", EMPTY_ARRAY = 0, COMPLEX_ARRAY = 1, PRIMITIVE_ARRAY = 2, ENTRIES_ARRAY = 3, REMOVED = "–\xA0", ADDED = "+\xA0", UNCHANGED = " \xA0", supportsUserTiming = "undefined" !== typeof console && "function" === typeof console.timeStamp && "undefined" !== typeof performance && "function" === typeof performance.measure, COMPONENTS_TRACK = "Components ⚛", LANES_TRACK_GROUP = "Scheduler ⚛", currentTrack = "Blocking", alreadyWarnedForDeepEquality = !1, reusableComponentDevToolDetails = {
			color: "primary",
			properties: null,
			tooltipText: "",
			track: COMPONENTS_TRACK
		}, reusableComponentOptions = {
			start: -0,
			end: -0,
			detail: { devtools: reusableComponentDevToolDetails }
		}, resuableChangedPropsEntry = ["Changed Props", ""], DEEP_EQUALITY_WARNING = "This component received deeply equal props. It might benefit from useMemo or the React Compiler in its owner.", reusableDeeplyEqualPropsEntry = ["Changed Props", DEEP_EQUALITY_WARNING], OffscreenVisible = 1, OffscreenPassiveEffectsConnected = 2, concurrentQueues = [], concurrentQueuesIndex = 0, concurrentlyUpdatedLanes = 0, emptyContextObject = {};
		Object.freeze(emptyContextObject);
		var resolveFamily = null, failedBoundaries = null, NoMode = 0, ConcurrentMode = 1, ProfileMode = 2, StrictLegacyMode = 8, StrictEffectsMode = 16, SuspenseyImagesMode = 32;
		var hasBadMapPolyfill = !1;
		try {
			Object.preventExtensions({});
		} catch (e$3) {
			hasBadMapPolyfill = !0;
		}
		var CapturedStacks = /* @__PURE__ */ new WeakMap(), forkStack = [], forkStackIndex = 0, treeForkProvider = null, treeForkCount = 0, idStack = [], idStackIndex = 0, treeContextProvider = null, treeContextId = 1, treeContextOverflow = "", hydrationParentFiber = null, nextHydratableInstance = null, isHydrating = !1, didSuspendOrErrorDEV = !1, hydrationDiffRootDEV = null, hydrationErrors = null, rootOrSingletonContext = !1, HydrationMismatchException = Error("Hydration Mismatch Exception: This is not a real error, and should not leak into userspace. If you're seeing this, it's likely a bug in React."), valueCursor = createCursor(null);
		var rendererCursorDEV = createCursor(null);
		var rendererSigil = {};
		var currentlyRenderingFiber$1 = null, lastContextDependency = null, isDisallowedContextReadInDEV = !1, AbortControllerLocal = "undefined" !== typeof AbortController ? AbortController : function() {
			var listeners = [], signal = this.signal = {
				aborted: !1,
				addEventListener: function(type, listener) {
					listeners.push(listener);
				}
			};
			this.abort = function() {
				signal.aborted = !0;
				listeners.forEach(function(listener) {
					return listener();
				});
			};
		}, scheduleCallback$2 = Scheduler.unstable_scheduleCallback, NormalPriority = Scheduler.unstable_NormalPriority, CacheContext = {
			$$typeof: REACT_CONTEXT_TYPE,
			Consumer: null,
			Provider: null,
			_currentValue: null,
			_currentValue2: null,
			_threadCount: 0,
			_currentRenderer: null,
			_currentRenderer2: null
		}, now = Scheduler.unstable_now, createTask = console.createTask ? console.createTask : function() {
			return null;
		}, SPAWNED_UPDATE = 1, PINGED_UPDATE = 2, renderStartTime = -0, commitStartTime = -0, commitEndTime = -0, commitErrors = null, profilerStartTime = -1.1, profilerEffectDuration = -0, componentEffectDuration = -0, componentEffectStartTime = -1.1, componentEffectEndTime = -1.1, componentEffectErrors = null, componentEffectSpawnedUpdate = !1, blockingClampTime = -0, blockingUpdateTime = -1.1, blockingUpdateTask = null, blockingUpdateType = 0, blockingUpdateMethodName = null, blockingUpdateComponentName = null, blockingEventTime = -1.1, blockingEventType = null, blockingEventRepeatTime = -1.1, blockingSuspendedTime = -1.1, transitionClampTime = -0, transitionStartTime = -1.1, transitionUpdateTime = -1.1, transitionUpdateType = 0, transitionUpdateTask = null, transitionUpdateMethodName = null, transitionUpdateComponentName = null, transitionEventTime = -1.1, transitionEventType = null, transitionEventRepeatTime = -1.1, transitionSuspendedTime = -1.1, retryClampTime = -0, idleClampTime = -0, animatingLanes = 0, animatingTask = null, yieldReason = 0, yieldStartTime = -1.1, currentUpdateIsNested = !1, nestedUpdateScheduled = !1, currentEntangledListeners = null, currentEntangledPendingCount = 0, currentEntangledLane = 0, currentEntangledActionThenable = null, prevOnStartTransitionFinish = ReactSharedInternals.S;
		ReactSharedInternals.S = function(transition, returnValue) {
			globalMostRecentTransitionTime = now$1();
			if ("object" === typeof returnValue && null !== returnValue && "function" === typeof returnValue.then) {
				if (0 > transitionStartTime && 0 > transitionUpdateTime) {
					transitionStartTime = now();
					var newEventTime = resolveEventTimeStamp(), newEventType = resolveEventType();
					if (newEventTime !== transitionEventRepeatTime || newEventType !== transitionEventType) transitionEventRepeatTime = -1.1;
					transitionEventTime = newEventTime;
					transitionEventType = newEventType;
				}
				entangleAsyncAction(transition, returnValue);
			}
			null !== prevOnStartTransitionFinish && prevOnStartTransitionFinish(transition, returnValue);
		};
		var resumedCache = createCursor(null), ReactStrictModeWarnings = {
			recordUnsafeLifecycleWarnings: function() {},
			flushPendingUnsafeLifecycleWarnings: function() {},
			recordLegacyContextWarning: function() {},
			flushLegacyContextWarning: function() {},
			discardPendingWarnings: function() {}
		}, pendingComponentWillMountWarnings = [], pendingUNSAFE_ComponentWillMountWarnings = [], pendingComponentWillReceivePropsWarnings = [], pendingUNSAFE_ComponentWillReceivePropsWarnings = [], pendingComponentWillUpdateWarnings = [], pendingUNSAFE_ComponentWillUpdateWarnings = [], didWarnAboutUnsafeLifecycles = /* @__PURE__ */ new Set();
		ReactStrictModeWarnings.recordUnsafeLifecycleWarnings = function(fiber, instance) {
			didWarnAboutUnsafeLifecycles.has(fiber.type) || ("function" === typeof instance.componentWillMount && !0 !== instance.componentWillMount.__suppressDeprecationWarning && pendingComponentWillMountWarnings.push(fiber), fiber.mode & StrictLegacyMode && "function" === typeof instance.UNSAFE_componentWillMount && pendingUNSAFE_ComponentWillMountWarnings.push(fiber), "function" === typeof instance.componentWillReceiveProps && !0 !== instance.componentWillReceiveProps.__suppressDeprecationWarning && pendingComponentWillReceivePropsWarnings.push(fiber), fiber.mode & StrictLegacyMode && "function" === typeof instance.UNSAFE_componentWillReceiveProps && pendingUNSAFE_ComponentWillReceivePropsWarnings.push(fiber), "function" === typeof instance.componentWillUpdate && !0 !== instance.componentWillUpdate.__suppressDeprecationWarning && pendingComponentWillUpdateWarnings.push(fiber), fiber.mode & StrictLegacyMode && "function" === typeof instance.UNSAFE_componentWillUpdate && pendingUNSAFE_ComponentWillUpdateWarnings.push(fiber));
		};
		ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings = function() {
			var componentWillMountUniqueNames = /* @__PURE__ */ new Set();
			0 < pendingComponentWillMountWarnings.length && (pendingComponentWillMountWarnings.forEach(function(fiber) {
				componentWillMountUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
				didWarnAboutUnsafeLifecycles.add(fiber.type);
			}), pendingComponentWillMountWarnings = []);
			var UNSAFE_componentWillMountUniqueNames = /* @__PURE__ */ new Set();
			0 < pendingUNSAFE_ComponentWillMountWarnings.length && (pendingUNSAFE_ComponentWillMountWarnings.forEach(function(fiber) {
				UNSAFE_componentWillMountUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
				didWarnAboutUnsafeLifecycles.add(fiber.type);
			}), pendingUNSAFE_ComponentWillMountWarnings = []);
			var componentWillReceivePropsUniqueNames = /* @__PURE__ */ new Set();
			0 < pendingComponentWillReceivePropsWarnings.length && (pendingComponentWillReceivePropsWarnings.forEach(function(fiber) {
				componentWillReceivePropsUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
				didWarnAboutUnsafeLifecycles.add(fiber.type);
			}), pendingComponentWillReceivePropsWarnings = []);
			var UNSAFE_componentWillReceivePropsUniqueNames = /* @__PURE__ */ new Set();
			0 < pendingUNSAFE_ComponentWillReceivePropsWarnings.length && (pendingUNSAFE_ComponentWillReceivePropsWarnings.forEach(function(fiber) {
				UNSAFE_componentWillReceivePropsUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
				didWarnAboutUnsafeLifecycles.add(fiber.type);
			}), pendingUNSAFE_ComponentWillReceivePropsWarnings = []);
			var componentWillUpdateUniqueNames = /* @__PURE__ */ new Set();
			0 < pendingComponentWillUpdateWarnings.length && (pendingComponentWillUpdateWarnings.forEach(function(fiber) {
				componentWillUpdateUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
				didWarnAboutUnsafeLifecycles.add(fiber.type);
			}), pendingComponentWillUpdateWarnings = []);
			var UNSAFE_componentWillUpdateUniqueNames = /* @__PURE__ */ new Set();
			0 < pendingUNSAFE_ComponentWillUpdateWarnings.length && (pendingUNSAFE_ComponentWillUpdateWarnings.forEach(function(fiber) {
				UNSAFE_componentWillUpdateUniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
				didWarnAboutUnsafeLifecycles.add(fiber.type);
			}), pendingUNSAFE_ComponentWillUpdateWarnings = []);
			if (0 < UNSAFE_componentWillMountUniqueNames.size) {
				var sortedNames = setToSortedString(UNSAFE_componentWillMountUniqueNames);
				console.error("Using UNSAFE_componentWillMount in strict mode is not recommended and may indicate bugs in your code. See https://react.dev/link/unsafe-component-lifecycles for details.\n\n* Move code with side effects to componentDidMount, and set initial state in the constructor.\n\nPlease update the following components: %s", sortedNames);
			}
			0 < UNSAFE_componentWillReceivePropsUniqueNames.size && (sortedNames = setToSortedString(UNSAFE_componentWillReceivePropsUniqueNames), console.error("Using UNSAFE_componentWillReceiveProps in strict mode is not recommended and may indicate bugs in your code. See https://react.dev/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n* If you're updating state whenever props change, refactor your code to use memoization techniques or move it to static getDerivedStateFromProps. Learn more at: https://react.dev/link/derived-state\n\nPlease update the following components: %s", sortedNames));
			0 < UNSAFE_componentWillUpdateUniqueNames.size && (sortedNames = setToSortedString(UNSAFE_componentWillUpdateUniqueNames), console.error("Using UNSAFE_componentWillUpdate in strict mode is not recommended and may indicate bugs in your code. See https://react.dev/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n\nPlease update the following components: %s", sortedNames));
			0 < componentWillMountUniqueNames.size && (sortedNames = setToSortedString(componentWillMountUniqueNames), console.warn("componentWillMount has been renamed, and is not recommended for use. See https://react.dev/link/unsafe-component-lifecycles for details.\n\n* Move code with side effects to componentDidMount, and set initial state in the constructor.\n* Rename componentWillMount to UNSAFE_componentWillMount to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run `npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n\nPlease update the following components: %s", sortedNames));
			0 < componentWillReceivePropsUniqueNames.size && (sortedNames = setToSortedString(componentWillReceivePropsUniqueNames), console.warn("componentWillReceiveProps has been renamed, and is not recommended for use. See https://react.dev/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n* If you're updating state whenever props change, refactor your code to use memoization techniques or move it to static getDerivedStateFromProps. Learn more at: https://react.dev/link/derived-state\n* Rename componentWillReceiveProps to UNSAFE_componentWillReceiveProps to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run `npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n\nPlease update the following components: %s", sortedNames));
			0 < componentWillUpdateUniqueNames.size && (sortedNames = setToSortedString(componentWillUpdateUniqueNames), console.warn("componentWillUpdate has been renamed, and is not recommended for use. See https://react.dev/link/unsafe-component-lifecycles for details.\n\n* Move data fetching code or side effects to componentDidUpdate.\n* Rename componentWillUpdate to UNSAFE_componentWillUpdate to suppress this warning in non-strict mode. In React 18.x, only the UNSAFE_ name will work. To rename all deprecated lifecycles to their new names, you can run `npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n\nPlease update the following components: %s", sortedNames));
		};
		var pendingLegacyContextWarning = /* @__PURE__ */ new Map(), didWarnAboutLegacyContext = /* @__PURE__ */ new Set();
		ReactStrictModeWarnings.recordLegacyContextWarning = function(fiber, instance) {
			var strictRoot = null;
			for (var node = fiber; null !== node;) node.mode & StrictLegacyMode && (strictRoot = node), node = node.return;
			null === strictRoot ? console.error("Expected to find a StrictMode component in a strict mode tree. This error is likely caused by a bug in React. Please file an issue.") : !didWarnAboutLegacyContext.has(fiber.type) && (node = pendingLegacyContextWarning.get(strictRoot), null != fiber.type.contextTypes || null != fiber.type.childContextTypes || null !== instance && "function" === typeof instance.getChildContext) && (void 0 === node && (node = [], pendingLegacyContextWarning.set(strictRoot, node)), node.push(fiber));
		};
		ReactStrictModeWarnings.flushLegacyContextWarning = function() {
			pendingLegacyContextWarning.forEach(function(fiberArray) {
				if (0 !== fiberArray.length) {
					var firstFiber = fiberArray[0], uniqueNames = /* @__PURE__ */ new Set();
					fiberArray.forEach(function(fiber) {
						uniqueNames.add(getComponentNameFromFiber(fiber) || "Component");
						didWarnAboutLegacyContext.add(fiber.type);
					});
					var sortedNames = setToSortedString(uniqueNames);
					runWithFiberInDEV(firstFiber, function() {
						console.error("Legacy context API has been detected within a strict-mode tree.\n\nThe old API will be supported in all 16.x releases, but applications using it should migrate to the new version.\n\nPlease update the following components: %s\n\nLearn more about this warning here: https://react.dev/link/legacy-context", sortedNames);
					});
				}
			});
		};
		ReactStrictModeWarnings.discardPendingWarnings = function() {
			pendingComponentWillMountWarnings = [];
			pendingUNSAFE_ComponentWillMountWarnings = [];
			pendingComponentWillReceivePropsWarnings = [];
			pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
			pendingComponentWillUpdateWarnings = [];
			pendingUNSAFE_ComponentWillUpdateWarnings = [];
			pendingLegacyContextWarning = /* @__PURE__ */ new Map();
		};
		var callComponent = { react_stack_bottom_frame: function(Component, props, secondArg) {
			var wasRendering = isRendering;
			isRendering = !0;
			try {
				return Component(props, secondArg);
			} finally {
				isRendering = wasRendering;
			}
		} }, callComponentInDEV = callComponent.react_stack_bottom_frame.bind(callComponent), callRender = { react_stack_bottom_frame: function(instance) {
			var wasRendering = isRendering;
			isRendering = !0;
			try {
				return instance.render();
			} finally {
				isRendering = wasRendering;
			}
		} }, callRenderInDEV = callRender.react_stack_bottom_frame.bind(callRender), callComponentDidMount = { react_stack_bottom_frame: function(finishedWork, instance) {
			try {
				instance.componentDidMount();
			} catch (error) {
				captureCommitPhaseError(finishedWork, finishedWork.return, error);
			}
		} }, callComponentDidMountInDEV = callComponentDidMount.react_stack_bottom_frame.bind(callComponentDidMount), callComponentDidUpdate = { react_stack_bottom_frame: function(finishedWork, instance, prevProps, prevState, snapshot) {
			try {
				instance.componentDidUpdate(prevProps, prevState, snapshot);
			} catch (error) {
				captureCommitPhaseError(finishedWork, finishedWork.return, error);
			}
		} }, callComponentDidUpdateInDEV = callComponentDidUpdate.react_stack_bottom_frame.bind(callComponentDidUpdate), callComponentDidCatch = { react_stack_bottom_frame: function(instance, errorInfo) {
			var stack = errorInfo.stack;
			instance.componentDidCatch(errorInfo.value, { componentStack: null !== stack ? stack : "" });
		} }, callComponentDidCatchInDEV = callComponentDidCatch.react_stack_bottom_frame.bind(callComponentDidCatch), callComponentWillUnmount = { react_stack_bottom_frame: function(current, nearestMountedAncestor, instance) {
			try {
				instance.componentWillUnmount();
			} catch (error) {
				captureCommitPhaseError(current, nearestMountedAncestor, error);
			}
		} }, callComponentWillUnmountInDEV = callComponentWillUnmount.react_stack_bottom_frame.bind(callComponentWillUnmount), callCreate = { react_stack_bottom_frame: function(effect) {
			var create = effect.create;
			effect = effect.inst;
			create = create();
			return effect.destroy = create;
		} }, callCreateInDEV = callCreate.react_stack_bottom_frame.bind(callCreate), callDestroy = { react_stack_bottom_frame: function(current, nearestMountedAncestor, destroy) {
			try {
				destroy();
			} catch (error) {
				captureCommitPhaseError(current, nearestMountedAncestor, error);
			}
		} }, callDestroyInDEV = callDestroy.react_stack_bottom_frame.bind(callDestroy), callLazyInit = { react_stack_bottom_frame: function(lazy) {
			var init = lazy._init;
			return init(lazy._payload);
		} }, callLazyInitInDEV = callLazyInit.react_stack_bottom_frame.bind(callLazyInit), SuspenseException = Error("Suspense Exception: This is not a real error! It's an implementation detail of `use` to interrupt the current render. You must either rethrow it immediately, or move the `use` call outside of the `try/catch` block. Capturing without rethrowing will lead to unexpected behavior.\n\nTo handle async errors, wrap your component in an error boundary, or call the promise's `.catch` method and pass the result to `use`."), SuspenseyCommitException = Error("Suspense Exception: This is not a real error, and should not leak into userspace. If you're seeing this, it's likely a bug in React."), SuspenseActionException = Error("Suspense Exception: This is not a real error! It's an implementation detail of `useActionState` to interrupt the current render. You must either rethrow it immediately, or move the `useActionState` call outside of the `try/catch` block. Capturing without rethrowing will lead to unexpected behavior.\n\nTo handle async errors, wrap your component in an error boundary."), noopSuspenseyCommitThenable = { then: function() {
			console.error("Internal React error: A listener was unexpectedly attached to a \"noop\" thenable. This is a bug in React. Please file an issue.");
		} }, suspendedThenable = null, needsToResetSuspendedThenableDEV = !1, thenableState$1 = null, thenableIndexCounter$1 = 0, currentDebugInfo = null, didWarnAboutMaps;
		var didWarnAboutGenerators = didWarnAboutMaps = !1;
		var ownerHasKeyUseWarning = {};
		var ownerHasFunctionTypeWarning = {};
		var ownerHasSymbolTypeWarning = {};
		warnForMissingKey = function(returnFiber, workInProgress, child) {
			if (null !== child && "object" === typeof child && child._store && (!child._store.validated && null == child.key || 2 === child._store.validated)) {
				if ("object" !== typeof child._store) throw Error("React Component in warnForMissingKey should have a _store. This error is likely caused by a bug in React. Please file an issue.");
				child._store.validated = 1;
				var componentName = getComponentNameFromFiber(returnFiber), componentKey = componentName || "null";
				if (!ownerHasKeyUseWarning[componentKey]) {
					ownerHasKeyUseWarning[componentKey] = !0;
					child = child._owner;
					returnFiber = returnFiber._debugOwner;
					var currentComponentErrorInfo = "";
					returnFiber && "number" === typeof returnFiber.tag && (componentKey = getComponentNameFromFiber(returnFiber)) && (currentComponentErrorInfo = "\n\nCheck the render method of `" + componentKey + "`.");
					currentComponentErrorInfo || componentName && (currentComponentErrorInfo = "\n\nCheck the top-level render call using <" + componentName + ">.");
					var childOwnerAppendix = "";
					null != child && returnFiber !== child && (componentName = null, "number" === typeof child.tag ? componentName = getComponentNameFromFiber(child) : "string" === typeof child.name && (componentName = child.name), componentName && (childOwnerAppendix = " It was passed a child from " + componentName + "."));
					runWithFiberInDEV(workInProgress, function() {
						console.error("Each child in a list should have a unique \"key\" prop.%s%s See https://react.dev/link/warning-keys for more information.", currentComponentErrorInfo, childOwnerAppendix);
					});
				}
			}
		};
		var reconcileChildFibers = createChildReconciler(!0), mountChildFibers = createChildReconciler(!1), UpdateState = 0, ReplaceState = 1, ForceUpdate = 2, CaptureUpdate = 3, hasForceUpdate = !1;
		var didWarnUpdateInsideUpdate = !1;
		var currentlyProcessingQueue = null;
		var didReadFromEntangledAsyncAction = !1, currentTreeHiddenStackCursor = createCursor(null), prevEntangledRenderLanesCursor = createCursor(0), suspenseHandlerStackCursor = createCursor(null), shellBoundary = null, SubtreeSuspenseContextMask = 1, ForceSuspenseFallback = 2, suspenseStackCursor = createCursor(0), NoFlags = 0, HasEffect = 1, Insertion = 2, Layout = 4, Passive = 8, didWarnUncachedGetSnapshot;
		var didWarnAboutMismatchedHooksForComponent = /* @__PURE__ */ new Set();
		var didWarnAboutUseWrappedInTryCatch = /* @__PURE__ */ new Set();
		var didWarnAboutAsyncClientComponent = /* @__PURE__ */ new Set();
		var didWarnAboutUseFormState = /* @__PURE__ */ new Set();
		var renderLanes = 0, currentlyRenderingFiber = null, currentHook = null, workInProgressHook = null, didScheduleRenderPhaseUpdate = !1, didScheduleRenderPhaseUpdateDuringThisPass = !1, shouldDoubleInvokeUserFnsInHooksDEV = !1, localIdCounter = 0, thenableIndexCounter = 0, thenableState = null, globalClientIdCounter = 0, RE_RENDER_LIMIT = 25, currentHookNameInDev = null, hookTypesDev = null, hookTypesUpdateIndexDev = -1, ignorePreviousDependencies = !1, ContextOnlyDispatcher = {
			readContext,
			use,
			useCallback: throwInvalidHookError,
			useContext: throwInvalidHookError,
			useEffect: throwInvalidHookError,
			useImperativeHandle: throwInvalidHookError,
			useLayoutEffect: throwInvalidHookError,
			useInsertionEffect: throwInvalidHookError,
			useMemo: throwInvalidHookError,
			useReducer: throwInvalidHookError,
			useRef: throwInvalidHookError,
			useState: throwInvalidHookError,
			useDebugValue: throwInvalidHookError,
			useDeferredValue: throwInvalidHookError,
			useTransition: throwInvalidHookError,
			useSyncExternalStore: throwInvalidHookError,
			useId: throwInvalidHookError,
			useHostTransitionStatus: throwInvalidHookError,
			useFormState: throwInvalidHookError,
			useActionState: throwInvalidHookError,
			useOptimistic: throwInvalidHookError,
			useMemoCache: throwInvalidHookError,
			useCacheRefresh: throwInvalidHookError
		};
		ContextOnlyDispatcher.useEffectEvent = throwInvalidHookError;
		var HooksDispatcherOnMountInDEV = null, HooksDispatcherOnMountWithHookTypesInDEV = null, HooksDispatcherOnUpdateInDEV = null, HooksDispatcherOnRerenderInDEV = null, InvalidNestedHooksDispatcherOnMountInDEV = null, InvalidNestedHooksDispatcherOnUpdateInDEV = null, InvalidNestedHooksDispatcherOnRerenderInDEV = null;
		HooksDispatcherOnMountInDEV = {
			readContext: function(context) {
				return readContext(context);
			},
			use,
			useCallback: function(callback, deps) {
				currentHookNameInDev = "useCallback";
				mountHookTypesDev();
				checkDepsAreArrayDev(deps);
				return mountCallback(callback, deps);
			},
			useContext: function(context) {
				currentHookNameInDev = "useContext";
				mountHookTypesDev();
				return readContext(context);
			},
			useEffect: function(create, deps) {
				currentHookNameInDev = "useEffect";
				mountHookTypesDev();
				checkDepsAreArrayDev(deps);
				return mountEffect(create, deps);
			},
			useImperativeHandle: function(ref, create, deps) {
				currentHookNameInDev = "useImperativeHandle";
				mountHookTypesDev();
				checkDepsAreArrayDev(deps);
				return mountImperativeHandle(ref, create, deps);
			},
			useInsertionEffect: function(create, deps) {
				currentHookNameInDev = "useInsertionEffect";
				mountHookTypesDev();
				checkDepsAreArrayDev(deps);
				mountEffectImpl(4, Insertion, create, deps);
			},
			useLayoutEffect: function(create, deps) {
				currentHookNameInDev = "useLayoutEffect";
				mountHookTypesDev();
				checkDepsAreArrayDev(deps);
				return mountLayoutEffect(create, deps);
			},
			useMemo: function(create, deps) {
				currentHookNameInDev = "useMemo";
				mountHookTypesDev();
				checkDepsAreArrayDev(deps);
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnMountInDEV;
				try {
					return mountMemo(create, deps);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useReducer: function(reducer, initialArg, init) {
				currentHookNameInDev = "useReducer";
				mountHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnMountInDEV;
				try {
					return mountReducer(reducer, initialArg, init);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useRef: function(initialValue) {
				currentHookNameInDev = "useRef";
				mountHookTypesDev();
				return mountRef(initialValue);
			},
			useState: function(initialState) {
				currentHookNameInDev = "useState";
				mountHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnMountInDEV;
				try {
					return mountState(initialState);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useDebugValue: function() {
				currentHookNameInDev = "useDebugValue";
				mountHookTypesDev();
			},
			useDeferredValue: function(value, initialValue) {
				currentHookNameInDev = "useDeferredValue";
				mountHookTypesDev();
				return mountDeferredValue(value, initialValue);
			},
			useTransition: function() {
				currentHookNameInDev = "useTransition";
				mountHookTypesDev();
				return mountTransition();
			},
			useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
				currentHookNameInDev = "useSyncExternalStore";
				mountHookTypesDev();
				return mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
			},
			useId: function() {
				currentHookNameInDev = "useId";
				mountHookTypesDev();
				return mountId();
			},
			useFormState: function(action, initialState) {
				currentHookNameInDev = "useFormState";
				mountHookTypesDev();
				warnOnUseFormStateInDev();
				return mountActionState(action, initialState);
			},
			useActionState: function(action, initialState) {
				currentHookNameInDev = "useActionState";
				mountHookTypesDev();
				return mountActionState(action, initialState);
			},
			useOptimistic: function(passthrough) {
				currentHookNameInDev = "useOptimistic";
				mountHookTypesDev();
				return mountOptimistic(passthrough);
			},
			useHostTransitionStatus,
			useMemoCache,
			useCacheRefresh: function() {
				currentHookNameInDev = "useCacheRefresh";
				mountHookTypesDev();
				return mountRefresh();
			},
			useEffectEvent: function(callback) {
				currentHookNameInDev = "useEffectEvent";
				mountHookTypesDev();
				return mountEvent(callback);
			}
		};
		HooksDispatcherOnMountWithHookTypesInDEV = {
			readContext: function(context) {
				return readContext(context);
			},
			use,
			useCallback: function(callback, deps) {
				currentHookNameInDev = "useCallback";
				updateHookTypesDev();
				return mountCallback(callback, deps);
			},
			useContext: function(context) {
				currentHookNameInDev = "useContext";
				updateHookTypesDev();
				return readContext(context);
			},
			useEffect: function(create, deps) {
				currentHookNameInDev = "useEffect";
				updateHookTypesDev();
				return mountEffect(create, deps);
			},
			useImperativeHandle: function(ref, create, deps) {
				currentHookNameInDev = "useImperativeHandle";
				updateHookTypesDev();
				return mountImperativeHandle(ref, create, deps);
			},
			useInsertionEffect: function(create, deps) {
				currentHookNameInDev = "useInsertionEffect";
				updateHookTypesDev();
				mountEffectImpl(4, Insertion, create, deps);
			},
			useLayoutEffect: function(create, deps) {
				currentHookNameInDev = "useLayoutEffect";
				updateHookTypesDev();
				return mountLayoutEffect(create, deps);
			},
			useMemo: function(create, deps) {
				currentHookNameInDev = "useMemo";
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnMountInDEV;
				try {
					return mountMemo(create, deps);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useReducer: function(reducer, initialArg, init) {
				currentHookNameInDev = "useReducer";
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnMountInDEV;
				try {
					return mountReducer(reducer, initialArg, init);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useRef: function(initialValue) {
				currentHookNameInDev = "useRef";
				updateHookTypesDev();
				return mountRef(initialValue);
			},
			useState: function(initialState) {
				currentHookNameInDev = "useState";
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnMountInDEV;
				try {
					return mountState(initialState);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useDebugValue: function() {
				currentHookNameInDev = "useDebugValue";
				updateHookTypesDev();
			},
			useDeferredValue: function(value, initialValue) {
				currentHookNameInDev = "useDeferredValue";
				updateHookTypesDev();
				return mountDeferredValue(value, initialValue);
			},
			useTransition: function() {
				currentHookNameInDev = "useTransition";
				updateHookTypesDev();
				return mountTransition();
			},
			useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
				currentHookNameInDev = "useSyncExternalStore";
				updateHookTypesDev();
				return mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
			},
			useId: function() {
				currentHookNameInDev = "useId";
				updateHookTypesDev();
				return mountId();
			},
			useActionState: function(action, initialState) {
				currentHookNameInDev = "useActionState";
				updateHookTypesDev();
				return mountActionState(action, initialState);
			},
			useFormState: function(action, initialState) {
				currentHookNameInDev = "useFormState";
				updateHookTypesDev();
				warnOnUseFormStateInDev();
				return mountActionState(action, initialState);
			},
			useOptimistic: function(passthrough) {
				currentHookNameInDev = "useOptimistic";
				updateHookTypesDev();
				return mountOptimistic(passthrough);
			},
			useHostTransitionStatus,
			useMemoCache,
			useCacheRefresh: function() {
				currentHookNameInDev = "useCacheRefresh";
				updateHookTypesDev();
				return mountRefresh();
			},
			useEffectEvent: function(callback) {
				currentHookNameInDev = "useEffectEvent";
				updateHookTypesDev();
				return mountEvent(callback);
			}
		};
		HooksDispatcherOnUpdateInDEV = {
			readContext: function(context) {
				return readContext(context);
			},
			use,
			useCallback: function(callback, deps) {
				currentHookNameInDev = "useCallback";
				updateHookTypesDev();
				return updateCallback(callback, deps);
			},
			useContext: function(context) {
				currentHookNameInDev = "useContext";
				updateHookTypesDev();
				return readContext(context);
			},
			useEffect: function(create, deps) {
				currentHookNameInDev = "useEffect";
				updateHookTypesDev();
				updateEffectImpl(2048, Passive, create, deps);
			},
			useImperativeHandle: function(ref, create, deps) {
				currentHookNameInDev = "useImperativeHandle";
				updateHookTypesDev();
				return updateImperativeHandle(ref, create, deps);
			},
			useInsertionEffect: function(create, deps) {
				currentHookNameInDev = "useInsertionEffect";
				updateHookTypesDev();
				return updateEffectImpl(4, Insertion, create, deps);
			},
			useLayoutEffect: function(create, deps) {
				currentHookNameInDev = "useLayoutEffect";
				updateHookTypesDev();
				return updateEffectImpl(4, Layout, create, deps);
			},
			useMemo: function(create, deps) {
				currentHookNameInDev = "useMemo";
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnUpdateInDEV;
				try {
					return updateMemo(create, deps);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useReducer: function(reducer, initialArg, init) {
				currentHookNameInDev = "useReducer";
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnUpdateInDEV;
				try {
					return updateReducer(reducer, initialArg, init);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useRef: function() {
				currentHookNameInDev = "useRef";
				updateHookTypesDev();
				return updateWorkInProgressHook().memoizedState;
			},
			useState: function() {
				currentHookNameInDev = "useState";
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnUpdateInDEV;
				try {
					return updateReducer(basicStateReducer);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useDebugValue: function() {
				currentHookNameInDev = "useDebugValue";
				updateHookTypesDev();
			},
			useDeferredValue: function(value, initialValue) {
				currentHookNameInDev = "useDeferredValue";
				updateHookTypesDev();
				return updateDeferredValue(value, initialValue);
			},
			useTransition: function() {
				currentHookNameInDev = "useTransition";
				updateHookTypesDev();
				return updateTransition();
			},
			useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
				currentHookNameInDev = "useSyncExternalStore";
				updateHookTypesDev();
				return updateSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
			},
			useId: function() {
				currentHookNameInDev = "useId";
				updateHookTypesDev();
				return updateWorkInProgressHook().memoizedState;
			},
			useFormState: function(action) {
				currentHookNameInDev = "useFormState";
				updateHookTypesDev();
				warnOnUseFormStateInDev();
				return updateActionState(action);
			},
			useActionState: function(action) {
				currentHookNameInDev = "useActionState";
				updateHookTypesDev();
				return updateActionState(action);
			},
			useOptimistic: function(passthrough, reducer) {
				currentHookNameInDev = "useOptimistic";
				updateHookTypesDev();
				return updateOptimistic(passthrough, reducer);
			},
			useHostTransitionStatus,
			useMemoCache,
			useCacheRefresh: function() {
				currentHookNameInDev = "useCacheRefresh";
				updateHookTypesDev();
				return updateWorkInProgressHook().memoizedState;
			},
			useEffectEvent: function(callback) {
				currentHookNameInDev = "useEffectEvent";
				updateHookTypesDev();
				return updateEvent(callback);
			}
		};
		HooksDispatcherOnRerenderInDEV = {
			readContext: function(context) {
				return readContext(context);
			},
			use,
			useCallback: function(callback, deps) {
				currentHookNameInDev = "useCallback";
				updateHookTypesDev();
				return updateCallback(callback, deps);
			},
			useContext: function(context) {
				currentHookNameInDev = "useContext";
				updateHookTypesDev();
				return readContext(context);
			},
			useEffect: function(create, deps) {
				currentHookNameInDev = "useEffect";
				updateHookTypesDev();
				updateEffectImpl(2048, Passive, create, deps);
			},
			useImperativeHandle: function(ref, create, deps) {
				currentHookNameInDev = "useImperativeHandle";
				updateHookTypesDev();
				return updateImperativeHandle(ref, create, deps);
			},
			useInsertionEffect: function(create, deps) {
				currentHookNameInDev = "useInsertionEffect";
				updateHookTypesDev();
				return updateEffectImpl(4, Insertion, create, deps);
			},
			useLayoutEffect: function(create, deps) {
				currentHookNameInDev = "useLayoutEffect";
				updateHookTypesDev();
				return updateEffectImpl(4, Layout, create, deps);
			},
			useMemo: function(create, deps) {
				currentHookNameInDev = "useMemo";
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnRerenderInDEV;
				try {
					return updateMemo(create, deps);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useReducer: function(reducer, initialArg, init) {
				currentHookNameInDev = "useReducer";
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnRerenderInDEV;
				try {
					return rerenderReducer(reducer, initialArg, init);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useRef: function() {
				currentHookNameInDev = "useRef";
				updateHookTypesDev();
				return updateWorkInProgressHook().memoizedState;
			},
			useState: function() {
				currentHookNameInDev = "useState";
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnRerenderInDEV;
				try {
					return rerenderReducer(basicStateReducer);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useDebugValue: function() {
				currentHookNameInDev = "useDebugValue";
				updateHookTypesDev();
			},
			useDeferredValue: function(value, initialValue) {
				currentHookNameInDev = "useDeferredValue";
				updateHookTypesDev();
				return rerenderDeferredValue(value, initialValue);
			},
			useTransition: function() {
				currentHookNameInDev = "useTransition";
				updateHookTypesDev();
				return rerenderTransition();
			},
			useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
				currentHookNameInDev = "useSyncExternalStore";
				updateHookTypesDev();
				return updateSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
			},
			useId: function() {
				currentHookNameInDev = "useId";
				updateHookTypesDev();
				return updateWorkInProgressHook().memoizedState;
			},
			useFormState: function(action) {
				currentHookNameInDev = "useFormState";
				updateHookTypesDev();
				warnOnUseFormStateInDev();
				return rerenderActionState(action);
			},
			useActionState: function(action) {
				currentHookNameInDev = "useActionState";
				updateHookTypesDev();
				return rerenderActionState(action);
			},
			useOptimistic: function(passthrough, reducer) {
				currentHookNameInDev = "useOptimistic";
				updateHookTypesDev();
				return rerenderOptimistic(passthrough, reducer);
			},
			useHostTransitionStatus,
			useMemoCache,
			useCacheRefresh: function() {
				currentHookNameInDev = "useCacheRefresh";
				updateHookTypesDev();
				return updateWorkInProgressHook().memoizedState;
			},
			useEffectEvent: function(callback) {
				currentHookNameInDev = "useEffectEvent";
				updateHookTypesDev();
				return updateEvent(callback);
			}
		};
		InvalidNestedHooksDispatcherOnMountInDEV = {
			readContext: function(context) {
				warnInvalidContextAccess();
				return readContext(context);
			},
			use: function(usable) {
				warnInvalidHookAccess();
				return use(usable);
			},
			useCallback: function(callback, deps) {
				currentHookNameInDev = "useCallback";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountCallback(callback, deps);
			},
			useContext: function(context) {
				currentHookNameInDev = "useContext";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return readContext(context);
			},
			useEffect: function(create, deps) {
				currentHookNameInDev = "useEffect";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountEffect(create, deps);
			},
			useImperativeHandle: function(ref, create, deps) {
				currentHookNameInDev = "useImperativeHandle";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountImperativeHandle(ref, create, deps);
			},
			useInsertionEffect: function(create, deps) {
				currentHookNameInDev = "useInsertionEffect";
				warnInvalidHookAccess();
				mountHookTypesDev();
				mountEffectImpl(4, Insertion, create, deps);
			},
			useLayoutEffect: function(create, deps) {
				currentHookNameInDev = "useLayoutEffect";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountLayoutEffect(create, deps);
			},
			useMemo: function(create, deps) {
				currentHookNameInDev = "useMemo";
				warnInvalidHookAccess();
				mountHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnMountInDEV;
				try {
					return mountMemo(create, deps);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useReducer: function(reducer, initialArg, init) {
				currentHookNameInDev = "useReducer";
				warnInvalidHookAccess();
				mountHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnMountInDEV;
				try {
					return mountReducer(reducer, initialArg, init);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useRef: function(initialValue) {
				currentHookNameInDev = "useRef";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountRef(initialValue);
			},
			useState: function(initialState) {
				currentHookNameInDev = "useState";
				warnInvalidHookAccess();
				mountHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnMountInDEV;
				try {
					return mountState(initialState);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useDebugValue: function() {
				currentHookNameInDev = "useDebugValue";
				warnInvalidHookAccess();
				mountHookTypesDev();
			},
			useDeferredValue: function(value, initialValue) {
				currentHookNameInDev = "useDeferredValue";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountDeferredValue(value, initialValue);
			},
			useTransition: function() {
				currentHookNameInDev = "useTransition";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountTransition();
			},
			useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
				currentHookNameInDev = "useSyncExternalStore";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
			},
			useId: function() {
				currentHookNameInDev = "useId";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountId();
			},
			useFormState: function(action, initialState) {
				currentHookNameInDev = "useFormState";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountActionState(action, initialState);
			},
			useActionState: function(action, initialState) {
				currentHookNameInDev = "useActionState";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountActionState(action, initialState);
			},
			useOptimistic: function(passthrough) {
				currentHookNameInDev = "useOptimistic";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountOptimistic(passthrough);
			},
			useMemoCache: function(size) {
				warnInvalidHookAccess();
				return useMemoCache(size);
			},
			useHostTransitionStatus,
			useCacheRefresh: function() {
				currentHookNameInDev = "useCacheRefresh";
				mountHookTypesDev();
				return mountRefresh();
			},
			useEffectEvent: function(callback) {
				currentHookNameInDev = "useEffectEvent";
				warnInvalidHookAccess();
				mountHookTypesDev();
				return mountEvent(callback);
			}
		};
		InvalidNestedHooksDispatcherOnUpdateInDEV = {
			readContext: function(context) {
				warnInvalidContextAccess();
				return readContext(context);
			},
			use: function(usable) {
				warnInvalidHookAccess();
				return use(usable);
			},
			useCallback: function(callback, deps) {
				currentHookNameInDev = "useCallback";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateCallback(callback, deps);
			},
			useContext: function(context) {
				currentHookNameInDev = "useContext";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return readContext(context);
			},
			useEffect: function(create, deps) {
				currentHookNameInDev = "useEffect";
				warnInvalidHookAccess();
				updateHookTypesDev();
				updateEffectImpl(2048, Passive, create, deps);
			},
			useImperativeHandle: function(ref, create, deps) {
				currentHookNameInDev = "useImperativeHandle";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateImperativeHandle(ref, create, deps);
			},
			useInsertionEffect: function(create, deps) {
				currentHookNameInDev = "useInsertionEffect";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateEffectImpl(4, Insertion, create, deps);
			},
			useLayoutEffect: function(create, deps) {
				currentHookNameInDev = "useLayoutEffect";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateEffectImpl(4, Layout, create, deps);
			},
			useMemo: function(create, deps) {
				currentHookNameInDev = "useMemo";
				warnInvalidHookAccess();
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnUpdateInDEV;
				try {
					return updateMemo(create, deps);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useReducer: function(reducer, initialArg, init) {
				currentHookNameInDev = "useReducer";
				warnInvalidHookAccess();
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnUpdateInDEV;
				try {
					return updateReducer(reducer, initialArg, init);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useRef: function() {
				currentHookNameInDev = "useRef";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateWorkInProgressHook().memoizedState;
			},
			useState: function() {
				currentHookNameInDev = "useState";
				warnInvalidHookAccess();
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnUpdateInDEV;
				try {
					return updateReducer(basicStateReducer);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useDebugValue: function() {
				currentHookNameInDev = "useDebugValue";
				warnInvalidHookAccess();
				updateHookTypesDev();
			},
			useDeferredValue: function(value, initialValue) {
				currentHookNameInDev = "useDeferredValue";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateDeferredValue(value, initialValue);
			},
			useTransition: function() {
				currentHookNameInDev = "useTransition";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateTransition();
			},
			useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
				currentHookNameInDev = "useSyncExternalStore";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
			},
			useId: function() {
				currentHookNameInDev = "useId";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateWorkInProgressHook().memoizedState;
			},
			useFormState: function(action) {
				currentHookNameInDev = "useFormState";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateActionState(action);
			},
			useActionState: function(action) {
				currentHookNameInDev = "useActionState";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateActionState(action);
			},
			useOptimistic: function(passthrough, reducer) {
				currentHookNameInDev = "useOptimistic";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateOptimistic(passthrough, reducer);
			},
			useMemoCache: function(size) {
				warnInvalidHookAccess();
				return useMemoCache(size);
			},
			useHostTransitionStatus,
			useCacheRefresh: function() {
				currentHookNameInDev = "useCacheRefresh";
				updateHookTypesDev();
				return updateWorkInProgressHook().memoizedState;
			},
			useEffectEvent: function(callback) {
				currentHookNameInDev = "useEffectEvent";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateEvent(callback);
			}
		};
		InvalidNestedHooksDispatcherOnRerenderInDEV = {
			readContext: function(context) {
				warnInvalidContextAccess();
				return readContext(context);
			},
			use: function(usable) {
				warnInvalidHookAccess();
				return use(usable);
			},
			useCallback: function(callback, deps) {
				currentHookNameInDev = "useCallback";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateCallback(callback, deps);
			},
			useContext: function(context) {
				currentHookNameInDev = "useContext";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return readContext(context);
			},
			useEffect: function(create, deps) {
				currentHookNameInDev = "useEffect";
				warnInvalidHookAccess();
				updateHookTypesDev();
				updateEffectImpl(2048, Passive, create, deps);
			},
			useImperativeHandle: function(ref, create, deps) {
				currentHookNameInDev = "useImperativeHandle";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateImperativeHandle(ref, create, deps);
			},
			useInsertionEffect: function(create, deps) {
				currentHookNameInDev = "useInsertionEffect";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateEffectImpl(4, Insertion, create, deps);
			},
			useLayoutEffect: function(create, deps) {
				currentHookNameInDev = "useLayoutEffect";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateEffectImpl(4, Layout, create, deps);
			},
			useMemo: function(create, deps) {
				currentHookNameInDev = "useMemo";
				warnInvalidHookAccess();
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnUpdateInDEV;
				try {
					return updateMemo(create, deps);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useReducer: function(reducer, initialArg, init) {
				currentHookNameInDev = "useReducer";
				warnInvalidHookAccess();
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnUpdateInDEV;
				try {
					return rerenderReducer(reducer, initialArg, init);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useRef: function() {
				currentHookNameInDev = "useRef";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateWorkInProgressHook().memoizedState;
			},
			useState: function() {
				currentHookNameInDev = "useState";
				warnInvalidHookAccess();
				updateHookTypesDev();
				var prevDispatcher = ReactSharedInternals.H;
				ReactSharedInternals.H = InvalidNestedHooksDispatcherOnUpdateInDEV;
				try {
					return rerenderReducer(basicStateReducer);
				} finally {
					ReactSharedInternals.H = prevDispatcher;
				}
			},
			useDebugValue: function() {
				currentHookNameInDev = "useDebugValue";
				warnInvalidHookAccess();
				updateHookTypesDev();
			},
			useDeferredValue: function(value, initialValue) {
				currentHookNameInDev = "useDeferredValue";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return rerenderDeferredValue(value, initialValue);
			},
			useTransition: function() {
				currentHookNameInDev = "useTransition";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return rerenderTransition();
			},
			useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
				currentHookNameInDev = "useSyncExternalStore";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
			},
			useId: function() {
				currentHookNameInDev = "useId";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateWorkInProgressHook().memoizedState;
			},
			useFormState: function(action) {
				currentHookNameInDev = "useFormState";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return rerenderActionState(action);
			},
			useActionState: function(action) {
				currentHookNameInDev = "useActionState";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return rerenderActionState(action);
			},
			useOptimistic: function(passthrough, reducer) {
				currentHookNameInDev = "useOptimistic";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return rerenderOptimistic(passthrough, reducer);
			},
			useMemoCache: function(size) {
				warnInvalidHookAccess();
				return useMemoCache(size);
			},
			useHostTransitionStatus,
			useCacheRefresh: function() {
				currentHookNameInDev = "useCacheRefresh";
				updateHookTypesDev();
				return updateWorkInProgressHook().memoizedState;
			},
			useEffectEvent: function(callback) {
				currentHookNameInDev = "useEffectEvent";
				warnInvalidHookAccess();
				updateHookTypesDev();
				return updateEvent(callback);
			}
		};
		var fakeInternalInstance = {};
		var didWarnAboutStateAssignmentForComponent = /* @__PURE__ */ new Set();
		var didWarnAboutUninitializedState = /* @__PURE__ */ new Set();
		var didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate = /* @__PURE__ */ new Set();
		var didWarnAboutLegacyLifecyclesAndDerivedState = /* @__PURE__ */ new Set();
		var didWarnAboutDirectlyAssigningPropsToState = /* @__PURE__ */ new Set();
		var didWarnAboutUndefinedDerivedState = /* @__PURE__ */ new Set();
		var didWarnAboutContextTypes$1 = /* @__PURE__ */ new Set();
		var didWarnAboutChildContextTypes = /* @__PURE__ */ new Set();
		var didWarnAboutInvalidateContextType = /* @__PURE__ */ new Set();
		var didWarnOnInvalidCallback = /* @__PURE__ */ new Set();
		Object.freeze(fakeInternalInstance);
		var classComponentUpdater = {
			enqueueSetState: function(inst, payload, callback) {
				inst = inst._reactInternals;
				var lane = requestUpdateLane(inst), update = createUpdate(lane);
				update.payload = payload;
				void 0 !== callback && null !== callback && (warnOnInvalidCallback(callback), update.callback = callback);
				payload = enqueueUpdate(inst, update, lane);
				null !== payload && (startUpdateTimerByLane(lane, "this.setState()", inst), scheduleUpdateOnFiber(payload, inst, lane), entangleTransitions(payload, inst, lane));
			},
			enqueueReplaceState: function(inst, payload, callback) {
				inst = inst._reactInternals;
				var lane = requestUpdateLane(inst), update = createUpdate(lane);
				update.tag = ReplaceState;
				update.payload = payload;
				void 0 !== callback && null !== callback && (warnOnInvalidCallback(callback), update.callback = callback);
				payload = enqueueUpdate(inst, update, lane);
				null !== payload && (startUpdateTimerByLane(lane, "this.replaceState()", inst), scheduleUpdateOnFiber(payload, inst, lane), entangleTransitions(payload, inst, lane));
			},
			enqueueForceUpdate: function(inst, callback) {
				inst = inst._reactInternals;
				var lane = requestUpdateLane(inst), update = createUpdate(lane);
				update.tag = ForceUpdate;
				void 0 !== callback && null !== callback && (warnOnInvalidCallback(callback), update.callback = callback);
				callback = enqueueUpdate(inst, update, lane);
				null !== callback && (startUpdateTimerByLane(lane, "this.forceUpdate()", inst), scheduleUpdateOnFiber(callback, inst, lane), entangleTransitions(callback, inst, lane));
			}
		}, componentName = null, errorBoundaryName = null, SelectiveHydrationException = Error("This is not a real error. It's an implementation detail of React's selective hydration feature. If this leaks into userspace, it's a bug in React. Please file an issue."), didReceiveUpdate = !1;
		var didWarnAboutBadClass = {};
		var didWarnAboutContextTypeOnFunctionComponent = {};
		var didWarnAboutContextTypes = {};
		var didWarnAboutGetDerivedStateOnFunctionComponent = {};
		var didWarnAboutReassigningProps = !1;
		var didWarnAboutRevealOrder = {};
		var didWarnAboutTailOptions = {};
		var SUSPENDED_MARKER = {
			dehydrated: null,
			treeContext: null,
			retryLane: 0,
			hydrationErrors: null
		}, hasWarnedAboutUsingNoValuePropOnContextProvider = !1, didWarnAboutUndefinedSnapshotBeforeUpdate = null;
		didWarnAboutUndefinedSnapshotBeforeUpdate = /* @__PURE__ */ new Set();
		var offscreenSubtreeIsHidden = !1, offscreenSubtreeWasHidden = !1, needsFormReset = !1, PossiblyWeakSet = "function" === typeof WeakSet ? WeakSet : Set, nextEffect = null, inProgressLanes = null, inProgressRoot = null, hostParent = null, hostParentIsContainer = !1, currentHoistableRoot = null, inHydratedSubtree = !1, suspenseyCommitFlag = 8192, DefaultAsyncDispatcher = {
			getCacheForType: function(resourceType) {
				var cache = readContext(CacheContext), cacheForType = cache.data.get(resourceType);
				void 0 === cacheForType && (cacheForType = resourceType(), cache.data.set(resourceType, cacheForType));
				return cacheForType;
			},
			cacheSignal: function() {
				return readContext(CacheContext).controller.signal;
			},
			getOwner: function() {
				return current;
			}
		};
		if ("function" === typeof Symbol && Symbol.for) {
			var symbolFor = Symbol.for;
			symbolFor("selector.component");
			symbolFor("selector.has_pseudo_class");
			symbolFor("selector.role");
			symbolFor("selector.test_id");
			symbolFor("selector.text");
		}
		var commitHooks = [], PossiblyWeakMap = "function" === typeof WeakMap ? WeakMap : Map, NoContext = 0, RenderContext = 2, CommitContext = 4, RootInProgress = 0, RootFatalErrored = 1, RootErrored = 2, RootSuspended = 3, RootSuspendedWithDelay = 4, RootSuspendedAtTheShell = 6, RootCompleted = 5, executionContext = NoContext, workInProgressRoot = null, workInProgress = null, workInProgressRootRenderLanes = 0, NotSuspended = 0, SuspendedOnError = 1, SuspendedOnData = 2, SuspendedOnImmediate = 3, SuspendedOnInstance = 4, SuspendedOnInstanceAndReadyToContinue = 5, SuspendedOnDeprecatedThrowPromise = 6, SuspendedAndReadyToContinue = 7, SuspendedOnHydration = 8, SuspendedOnAction = 9, workInProgressSuspendedReason = NotSuspended, workInProgressThrownValue = null, workInProgressRootDidSkipSuspendedSiblings = !1, workInProgressRootIsPrerendering = !1, workInProgressRootDidAttachPingListener = !1, entangledRenderLanes = 0, workInProgressRootExitStatus = RootInProgress, workInProgressRootSkippedLanes = 0, workInProgressRootInterleavedUpdatedLanes = 0, workInProgressRootPingedLanes = 0, workInProgressDeferredLane = 0, workInProgressSuspendedRetryLanes = 0, workInProgressRootConcurrentErrors = null, workInProgressRootRecoverableErrors = null, workInProgressRootDidIncludeRecursiveRenderUpdate = !1, globalMostRecentFallbackTime = 0, globalMostRecentTransitionTime = 0, FALLBACK_THROTTLE_MS = 300, workInProgressRootRenderTargetTime = Infinity, RENDER_TIMEOUT_MS = 500, workInProgressTransitions = null, workInProgressUpdateTask = null, legacyErrorBoundariesThatAlreadyFailed = null, IMMEDIATE_COMMIT = 0, ABORTED_VIEW_TRANSITION_COMMIT = 1, DELAYED_PASSIVE_COMMIT = 2, ANIMATION_STARTED_COMMIT = 3, NO_PENDING_EFFECTS = 0, PENDING_MUTATION_PHASE = 1, PENDING_LAYOUT_PHASE = 2, PENDING_AFTER_MUTATION_PHASE = 3, PENDING_SPAWNED_WORK = 4, PENDING_PASSIVE_PHASE = 5, pendingEffectsStatus = 0, pendingEffectsRoot = null, pendingFinishedWork = null, pendingEffectsLanes = 0, pendingEffectsRemainingLanes = 0, pendingEffectsRenderEndTime = -0, pendingPassiveTransitions = null, pendingRecoverableErrors = null, pendingSuspendedCommitReason = null, pendingDelayedCommitReason = IMMEDIATE_COMMIT, pendingSuspendedViewTransitionReason = null, NESTED_UPDATE_LIMIT = 50, nestedUpdateCount = 0, rootWithNestedUpdates = null, isFlushingPassiveEffects = !1, didScheduleUpdateDuringPassiveEffects = !1, NESTED_PASSIVE_UPDATE_LIMIT = 50, nestedPassiveUpdateCount = 0, rootWithPassiveNestedUpdates = null, isRunningInsertionEffect = !1, didWarnStateUpdateForNotYetMountedComponent = null, didWarnAboutUpdateInRender = !1;
		var didWarnAboutUpdateInRenderForAnotherComponent = /* @__PURE__ */ new Set();
		var fakeActCallbackNode$1 = {}, firstScheduledRoot = null, lastScheduledRoot = null, didScheduleMicrotask = !1, didScheduleMicrotask_act = !1, mightHavePendingSyncWork = !1, isFlushingWork = !1, currentEventTransitionLane = 0, fakeActCallbackNode = {};
		(function() {
			for (var i = 0; i < simpleEventPluginEvents.length; i++) {
				var eventName = simpleEventPluginEvents[i], domEventName = eventName.toLowerCase();
				eventName = eventName[0].toUpperCase() + eventName.slice(1);
				registerSimpleEvent(domEventName, "on" + eventName);
			}
			registerSimpleEvent(ANIMATION_END, "onAnimationEnd");
			registerSimpleEvent(ANIMATION_ITERATION, "onAnimationIteration");
			registerSimpleEvent(ANIMATION_START, "onAnimationStart");
			registerSimpleEvent("dblclick", "onDoubleClick");
			registerSimpleEvent("focusin", "onFocus");
			registerSimpleEvent("focusout", "onBlur");
			registerSimpleEvent(TRANSITION_RUN, "onTransitionRun");
			registerSimpleEvent(TRANSITION_START, "onTransitionStart");
			registerSimpleEvent(TRANSITION_CANCEL, "onTransitionCancel");
			registerSimpleEvent(TRANSITION_END, "onTransitionEnd");
		})();
		registerDirectEvent("onMouseEnter", ["mouseout", "mouseover"]);
		registerDirectEvent("onMouseLeave", ["mouseout", "mouseover"]);
		registerDirectEvent("onPointerEnter", ["pointerout", "pointerover"]);
		registerDirectEvent("onPointerLeave", ["pointerout", "pointerover"]);
		registerTwoPhaseEvent("onChange", "change click focusin focusout input keydown keyup selectionchange".split(" "));
		registerTwoPhaseEvent("onSelect", "focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(" "));
		registerTwoPhaseEvent("onBeforeInput", [
			"compositionend",
			"keypress",
			"textInput",
			"paste"
		]);
		registerTwoPhaseEvent("onCompositionEnd", "compositionend focusout keydown keypress keyup mousedown".split(" "));
		registerTwoPhaseEvent("onCompositionStart", "compositionstart focusout keydown keypress keyup mousedown".split(" "));
		registerTwoPhaseEvent("onCompositionUpdate", "compositionupdate focusout keydown keypress keyup mousedown".split(" "));
		var mediaEventTypes = "abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(" "), nonDelegatedEvents = new Set("beforetoggle cancel close invalid load scroll scrollend toggle".split(" ").concat(mediaEventTypes)), listeningMarker = "_reactListening" + Math.random().toString(36).slice(2), didWarnControlledToUncontrolled = !1, didWarnUncontrolledToControlled = !1, didWarnFormActionType = !1, didWarnFormActionName = !1, didWarnFormActionTarget = !1, didWarnFormActionMethod = !1, didWarnPopoverTargetObject = !1;
		var didWarnForNewBooleanPropsWithEmptyValue = {};
		var NORMALIZE_NEWLINES_REGEX = /\r\n?/g, NORMALIZE_NULL_AND_REPLACEMENT_REGEX = /\u0000|\uFFFD/g, xlinkNamespace = "http://www.w3.org/1999/xlink", xmlNamespace = "http://www.w3.org/XML/1998/namespace", EXPECTED_FORM_ACTION_URL = "javascript:throw new Error('React form unexpectedly submitted.')", SUPPRESS_HYDRATION_WARNING = "suppressHydrationWarning", ACTIVITY_START_DATA = "&", ACTIVITY_END_DATA = "/&", SUSPENSE_START_DATA = "$", SUSPENSE_END_DATA = "/$", SUSPENSE_PENDING_START_DATA = "$?", SUSPENSE_QUEUED_START_DATA = "$~", SUSPENSE_FALLBACK_START_DATA = "$!", PREAMBLE_CONTRIBUTION_HTML = "html", PREAMBLE_CONTRIBUTION_BODY = "body", PREAMBLE_CONTRIBUTION_HEAD = "head", FORM_STATE_IS_MATCHING = "F!", FORM_STATE_IS_NOT_MATCHING = "F", DOCUMENT_READY_STATE_LOADING = "loading", STYLE = "style", HostContextNamespaceNone = 0, HostContextNamespaceSvg = 1, HostContextNamespaceMath = 2, eventsEnabled = null, selectionInformation = null, warnedUnknownTags = {
			dialog: !0,
			webview: !0
		}, currentPopstateTransitionEvent = null, schedulerEvent = void 0, scheduleTimeout = "function" === typeof setTimeout ? setTimeout : void 0, cancelTimeout = "function" === typeof clearTimeout ? clearTimeout : void 0, noTimeout = -1, localPromise = "function" === typeof Promise ? Promise : void 0, scheduleMicrotask = "function" === typeof queueMicrotask ? queueMicrotask : "undefined" !== typeof localPromise ? function(callback) {
			return localPromise.resolve(null).then(callback).catch(handleErrorInNextTick);
		} : scheduleTimeout, previousHydratableOnEnteringScopedSingleton = null, NotLoaded = 0, Loaded = 1, Errored = 2, Settled = 3, Inserted = 4, preloadPropsMap = /* @__PURE__ */ new Map(), preconnectsSet = /* @__PURE__ */ new Set(), previousDispatcher = ReactDOMSharedInternals.d;
		ReactDOMSharedInternals.d = {
			f: function() {
				var previousWasRendering = previousDispatcher.f(), wasRendering = flushSyncWork$1();
				return previousWasRendering || wasRendering;
			},
			r: function(form) {
				var formInst = getInstanceFromNode(form);
				null !== formInst && 5 === formInst.tag && "form" === formInst.type ? requestFormReset$1(formInst) : previousDispatcher.r(form);
			},
			D: function(href) {
				previousDispatcher.D(href);
				preconnectAs("dns-prefetch", href, null);
			},
			C: function(href, crossOrigin) {
				previousDispatcher.C(href, crossOrigin);
				preconnectAs("preconnect", href, crossOrigin);
			},
			L: function(href, as, options) {
				previousDispatcher.L(href, as, options);
				var ownerDocument = globalDocument;
				if (ownerDocument && href && as) {
					var preloadSelector = "link[rel=\"preload\"][as=\"" + escapeSelectorAttributeValueInsideDoubleQuotes(as) + "\"]";
					"image" === as ? options && options.imageSrcSet ? (preloadSelector += "[imagesrcset=\"" + escapeSelectorAttributeValueInsideDoubleQuotes(options.imageSrcSet) + "\"]", "string" === typeof options.imageSizes && (preloadSelector += "[imagesizes=\"" + escapeSelectorAttributeValueInsideDoubleQuotes(options.imageSizes) + "\"]")) : preloadSelector += "[href=\"" + escapeSelectorAttributeValueInsideDoubleQuotes(href) + "\"]" : preloadSelector += "[href=\"" + escapeSelectorAttributeValueInsideDoubleQuotes(href) + "\"]";
					var key = preloadSelector;
					switch (as) {
						case "style":
							key = getStyleKey(href);
							break;
						case "script": key = getScriptKey(href);
					}
					preloadPropsMap.has(key) || (href = assign({
						rel: "preload",
						href: "image" === as && options && options.imageSrcSet ? void 0 : href,
						as
					}, options), preloadPropsMap.set(key, href), null !== ownerDocument.querySelector(preloadSelector) || "style" === as && ownerDocument.querySelector(getStylesheetSelectorFromKey(key)) || "script" === as && ownerDocument.querySelector(getScriptSelectorFromKey(key)) || (as = ownerDocument.createElement("link"), setInitialProperties(as, "link", href), markNodeAsHoistable(as), ownerDocument.head.appendChild(as)));
				}
			},
			m: function(href, options) {
				previousDispatcher.m(href, options);
				var ownerDocument = globalDocument;
				if (ownerDocument && href) {
					var as = options && "string" === typeof options.as ? options.as : "script", preloadSelector = "link[rel=\"modulepreload\"][as=\"" + escapeSelectorAttributeValueInsideDoubleQuotes(as) + "\"][href=\"" + escapeSelectorAttributeValueInsideDoubleQuotes(href) + "\"]", key = preloadSelector;
					switch (as) {
						case "audioworklet":
						case "paintworklet":
						case "serviceworker":
						case "sharedworker":
						case "worker":
						case "script": key = getScriptKey(href);
					}
					if (!preloadPropsMap.has(key) && (href = assign({
						rel: "modulepreload",
						href
					}, options), preloadPropsMap.set(key, href), null === ownerDocument.querySelector(preloadSelector))) {
						switch (as) {
							case "audioworklet":
							case "paintworklet":
							case "serviceworker":
							case "sharedworker":
							case "worker":
							case "script": if (ownerDocument.querySelector(getScriptSelectorFromKey(key))) return;
						}
						as = ownerDocument.createElement("link");
						setInitialProperties(as, "link", href);
						markNodeAsHoistable(as);
						ownerDocument.head.appendChild(as);
					}
				}
			},
			X: function(src, options) {
				previousDispatcher.X(src, options);
				var ownerDocument = globalDocument;
				if (ownerDocument && src) {
					var scripts = getResourcesFromRoot(ownerDocument).hoistableScripts, key = getScriptKey(src), resource = scripts.get(key);
					resource || (resource = ownerDocument.querySelector(getScriptSelectorFromKey(key)), resource || (src = assign({
						src,
						async: !0
					}, options), (options = preloadPropsMap.get(key)) && adoptPreloadPropsForScript(src, options), resource = ownerDocument.createElement("script"), markNodeAsHoistable(resource), setInitialProperties(resource, "link", src), ownerDocument.head.appendChild(resource)), resource = {
						type: "script",
						instance: resource,
						count: 1,
						state: null
					}, scripts.set(key, resource));
				}
			},
			S: function(href, precedence, options) {
				previousDispatcher.S(href, precedence, options);
				var ownerDocument = globalDocument;
				if (ownerDocument && href) {
					var styles = getResourcesFromRoot(ownerDocument).hoistableStyles, key = getStyleKey(href);
					precedence = precedence || "default";
					var resource = styles.get(key);
					if (!resource) {
						var state = {
							loading: NotLoaded,
							preload: null
						};
						if (resource = ownerDocument.querySelector(getStylesheetSelectorFromKey(key))) state.loading = Loaded | Inserted;
						else {
							href = assign({
								rel: "stylesheet",
								href,
								"data-precedence": precedence
							}, options);
							(options = preloadPropsMap.get(key)) && adoptPreloadPropsForStylesheet(href, options);
							var link = resource = ownerDocument.createElement("link");
							markNodeAsHoistable(link);
							setInitialProperties(link, "link", href);
							link._p = new Promise(function(resolve, reject) {
								link.onload = resolve;
								link.onerror = reject;
							});
							link.addEventListener("load", function() {
								state.loading |= Loaded;
							});
							link.addEventListener("error", function() {
								state.loading |= Errored;
							});
							state.loading |= Inserted;
							insertStylesheet(resource, precedence, ownerDocument);
						}
						resource = {
							type: "stylesheet",
							instance: resource,
							count: 1,
							state
						};
						styles.set(key, resource);
					}
				}
			},
			M: function(src, options) {
				previousDispatcher.M(src, options);
				var ownerDocument = globalDocument;
				if (ownerDocument && src) {
					var scripts = getResourcesFromRoot(ownerDocument).hoistableScripts, key = getScriptKey(src), resource = scripts.get(key);
					resource || (resource = ownerDocument.querySelector(getScriptSelectorFromKey(key)), resource || (src = assign({
						src,
						async: !0,
						type: "module"
					}, options), (options = preloadPropsMap.get(key)) && adoptPreloadPropsForScript(src, options), resource = ownerDocument.createElement("script"), markNodeAsHoistable(resource), setInitialProperties(resource, "link", src), ownerDocument.head.appendChild(resource)), resource = {
						type: "script",
						instance: resource,
						count: 1,
						state: null
					}, scripts.set(key, resource));
				}
			}
		};
		var globalDocument = "undefined" === typeof document ? null : document, tagCaches = null, SUSPENSEY_STYLESHEET_TIMEOUT = 6e4, SUSPENSEY_IMAGE_TIMEOUT = 800, SUSPENSEY_IMAGE_TIME_ESTIMATE = 500, estimatedBytesWithinLimit = 0, LAST_PRECEDENCE = null, precedencesByRoot = null, NotPendingTransition = NotPending, HostTransitionContext = {
			$$typeof: REACT_CONTEXT_TYPE,
			Provider: null,
			Consumer: null,
			_currentValue: NotPendingTransition,
			_currentValue2: NotPendingTransition,
			_threadCount: 0
		}, badgeFormat = "%c%s%c", badgeStyle = "background: #e6e6e6;background: light-dark(rgba(0,0,0,0.1), rgba(255,255,255,0.25));color: #000000;color: light-dark(#000000, #ffffff);border-radius: 2px", resetStyle = "", pad = " ", bind = Function.prototype.bind;
		var didWarnAboutNestedUpdates = !1;
		var overrideHookState = null, overrideHookStateDeletePath = null, overrideHookStateRenamePath = null, overrideProps = null, overridePropsDeletePath = null, overridePropsRenamePath = null, scheduleUpdate = null, scheduleRetry = null, setErrorHandler = null, setSuspenseHandler = null;
		overrideHookState = function(fiber, id, path, value) {
			id = findHook(fiber, id);
			null !== id && (path = copyWithSetImpl(id.memoizedState, path, 0, value), id.memoizedState = path, id.baseState = path, fiber.memoizedProps = assign({}, fiber.memoizedProps), path = enqueueConcurrentRenderForLane(fiber, 2), null !== path && scheduleUpdateOnFiber(path, fiber, 2));
		};
		overrideHookStateDeletePath = function(fiber, id, path) {
			id = findHook(fiber, id);
			null !== id && (path = copyWithDeleteImpl(id.memoizedState, path, 0), id.memoizedState = path, id.baseState = path, fiber.memoizedProps = assign({}, fiber.memoizedProps), path = enqueueConcurrentRenderForLane(fiber, 2), null !== path && scheduleUpdateOnFiber(path, fiber, 2));
		};
		overrideHookStateRenamePath = function(fiber, id, oldPath, newPath) {
			id = findHook(fiber, id);
			null !== id && (oldPath = copyWithRename(id.memoizedState, oldPath, newPath), id.memoizedState = oldPath, id.baseState = oldPath, fiber.memoizedProps = assign({}, fiber.memoizedProps), oldPath = enqueueConcurrentRenderForLane(fiber, 2), null !== oldPath && scheduleUpdateOnFiber(oldPath, fiber, 2));
		};
		overrideProps = function(fiber, path, value) {
			fiber.pendingProps = copyWithSetImpl(fiber.memoizedProps, path, 0, value);
			fiber.alternate && (fiber.alternate.pendingProps = fiber.pendingProps);
			path = enqueueConcurrentRenderForLane(fiber, 2);
			null !== path && scheduleUpdateOnFiber(path, fiber, 2);
		};
		overridePropsDeletePath = function(fiber, path) {
			fiber.pendingProps = copyWithDeleteImpl(fiber.memoizedProps, path, 0);
			fiber.alternate && (fiber.alternate.pendingProps = fiber.pendingProps);
			path = enqueueConcurrentRenderForLane(fiber, 2);
			null !== path && scheduleUpdateOnFiber(path, fiber, 2);
		};
		overridePropsRenamePath = function(fiber, oldPath, newPath) {
			fiber.pendingProps = copyWithRename(fiber.memoizedProps, oldPath, newPath);
			fiber.alternate && (fiber.alternate.pendingProps = fiber.pendingProps);
			oldPath = enqueueConcurrentRenderForLane(fiber, 2);
			null !== oldPath && scheduleUpdateOnFiber(oldPath, fiber, 2);
		};
		scheduleUpdate = function(fiber) {
			var root = enqueueConcurrentRenderForLane(fiber, 2);
			null !== root && scheduleUpdateOnFiber(root, fiber, 2);
		};
		scheduleRetry = function(fiber) {
			var lane = claimNextRetryLane(), root = enqueueConcurrentRenderForLane(fiber, lane);
			null !== root && scheduleUpdateOnFiber(root, fiber, lane);
		};
		setErrorHandler = function(newShouldErrorImpl) {
			shouldErrorImpl = newShouldErrorImpl;
		};
		setSuspenseHandler = function(newShouldSuspendImpl) {
			shouldSuspendImpl = newShouldSuspendImpl;
		};
		var _enabled = !0, return_targetInst = null, hasScheduledReplayAttempt = !1, queuedFocus = null, queuedDrag = null, queuedMouse = null, queuedPointers = /* @__PURE__ */ new Map(), queuedPointerCaptures = /* @__PURE__ */ new Map(), queuedExplicitHydrationTargets = [], discreteReplayableEvents = "mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset".split(" "), lastScheduledReplayQueue = null;
		ReactDOMHydrationRoot.prototype.render = ReactDOMRoot.prototype.render = function(children) {
			var root = this._internalRoot;
			if (null === root) throw Error("Cannot update an unmounted root.");
			var args = arguments;
			"function" === typeof args[1] ? console.error("does not support the second callback argument. To execute a side effect after rendering, declare it in a component body with useEffect().") : isValidContainer(args[1]) ? console.error("You passed a container to the second argument of root.render(...). You don't need to pass it again since you already passed it to create the root.") : "undefined" !== typeof args[1] && console.error("You passed a second argument to root.render(...) but it only accepts one argument.");
			args = children;
			var current = root.current;
			updateContainerImpl(current, requestUpdateLane(current), args, root, null, null);
		};
		ReactDOMHydrationRoot.prototype.unmount = ReactDOMRoot.prototype.unmount = function() {
			var args = arguments;
			"function" === typeof args[0] && console.error("does not support a callback argument. To execute a side effect after rendering, declare it in a component body with useEffect().");
			args = this._internalRoot;
			if (null !== args) {
				this._internalRoot = null;
				var container = args.containerInfo;
				(executionContext & (RenderContext | CommitContext)) !== NoContext && console.error("Attempted to synchronously unmount a root while React was already rendering. React cannot finish unmounting the root until the current render has completed, which may lead to a race condition.");
				updateContainerImpl(args.current, 2, null, args, null, null);
				flushSyncWork$1();
				container[internalContainerInstanceKey] = null;
			}
		};
		ReactDOMHydrationRoot.prototype.unstable_scheduleHydration = function(target) {
			if (target) {
				var updatePriority = resolveUpdatePriority();
				target = {
					blockedOn: null,
					target,
					priority: updatePriority
				};
				for (var i = 0; i < queuedExplicitHydrationTargets.length && 0 !== updatePriority && updatePriority < queuedExplicitHydrationTargets[i].priority; i++);
				queuedExplicitHydrationTargets.splice(i, 0, target);
				0 === i && attemptExplicitHydrationTarget(target);
			}
		};
		(function() {
			var isomorphicReactPackageVersion = React.version;
			if ("19.2.8" !== isomorphicReactPackageVersion) throw Error("Incompatible React versions: The \"react\" and \"react-dom\" packages must have the exact same version. Instead got:\n  - react:      " + (isomorphicReactPackageVersion + "\n  - react-dom:  19.2.8\nLearn more: https://react.dev/warnings/version-mismatch"));
		})();
		"function" === typeof Map && null != Map.prototype && "function" === typeof Map.prototype.forEach && "function" === typeof Set && null != Set.prototype && "function" === typeof Set.prototype.clear && "function" === typeof Set.prototype.forEach || console.error("React depends on Map and Set built-in types. Make sure that you load a polyfill in older browsers. https://react.dev/link/react-polyfills");
		ReactDOMSharedInternals.findDOMNode = function(componentOrElement) {
			var fiber = componentOrElement._reactInternals;
			if (void 0 === fiber) {
				if ("function" === typeof componentOrElement.render) throw Error("Unable to find node on an unmounted component.");
				componentOrElement = Object.keys(componentOrElement).join(",");
				throw Error("Argument appears to not be a ReactComponent. Keys: " + componentOrElement);
			}
			componentOrElement = findCurrentFiberUsingSlowPath(fiber);
			componentOrElement = null !== componentOrElement ? findCurrentHostFiberImpl(componentOrElement) : null;
			componentOrElement = null === componentOrElement ? null : componentOrElement.stateNode;
			return componentOrElement;
		};
		if (!(function() {
			var internals = {
				bundleType: 1,
				version: "19.2.8",
				rendererPackageName: "react-dom",
				currentDispatcherRef: ReactSharedInternals,
				reconcilerVersion: "19.2.8"
			};
			internals.overrideHookState = overrideHookState;
			internals.overrideHookStateDeletePath = overrideHookStateDeletePath;
			internals.overrideHookStateRenamePath = overrideHookStateRenamePath;
			internals.overrideProps = overrideProps;
			internals.overridePropsDeletePath = overridePropsDeletePath;
			internals.overridePropsRenamePath = overridePropsRenamePath;
			internals.scheduleUpdate = scheduleUpdate;
			internals.scheduleRetry = scheduleRetry;
			internals.setErrorHandler = setErrorHandler;
			internals.setSuspenseHandler = setSuspenseHandler;
			internals.scheduleRefresh = scheduleRefresh;
			internals.scheduleRoot = scheduleRoot;
			internals.setRefreshHandler = setRefreshHandler;
			internals.getCurrentFiber = getCurrentFiberForDevTools;
			return injectInternals(internals);
		})() && canUseDOM && window.top === window.self && (-1 < navigator.userAgent.indexOf("Chrome") && -1 === navigator.userAgent.indexOf("Edge") || -1 < navigator.userAgent.indexOf("Firefox"))) {
			var protocol = window.location.protocol;
			/^(https?|file):$/.test(protocol) && console.info("%cDownload the React DevTools for a better development experience: https://react.dev/link/react-devtools" + ("file:" === protocol ? "\nYou might need to use a local HTTP server (instead of file://): https://react.dev/link/react-devtools-faq" : ""), "font-weight:bold");
		}
		exports.createRoot = function(container, options) {
			if (!isValidContainer(container)) throw Error("Target container is not a DOM element.");
			warnIfReactDOMContainerInDEV(container);
			var isStrictMode = !1, identifierPrefix = "", onUncaughtError = defaultOnUncaughtError, onCaughtError = defaultOnCaughtError, onRecoverableError = defaultOnRecoverableError;
			null !== options && void 0 !== options && (options.hydrate ? console.warn("hydrate through createRoot is deprecated. Use ReactDOMClient.hydrateRoot(container, <App />) instead.") : "object" === typeof options && null !== options && options.$$typeof === REACT_ELEMENT_TYPE && console.error("You passed a JSX element to createRoot. You probably meant to call root.render instead. Example usage:\n\n  let root = createRoot(domContainer);\n  root.render(<App />);"), !0 === options.unstable_strictMode && (isStrictMode = !0), void 0 !== options.identifierPrefix && (identifierPrefix = options.identifierPrefix), void 0 !== options.onUncaughtError && (onUncaughtError = options.onUncaughtError), void 0 !== options.onCaughtError && (onCaughtError = options.onCaughtError), void 0 !== options.onRecoverableError && (onRecoverableError = options.onRecoverableError));
			options = createFiberRoot(container, 1, !1, null, null, isStrictMode, identifierPrefix, null, onUncaughtError, onCaughtError, onRecoverableError, defaultOnDefaultTransitionIndicator);
			container[internalContainerInstanceKey] = options.current;
			listenToAllSupportedEvents(container);
			return new ReactDOMRoot(options);
		};
		exports.hydrateRoot = function(container, initialChildren, options) {
			if (!isValidContainer(container)) throw Error("Target container is not a DOM element.");
			warnIfReactDOMContainerInDEV(container);
			void 0 === initialChildren && console.error("Must provide initial children as second argument to hydrateRoot. Example usage: hydrateRoot(domContainer, <App />)");
			var isStrictMode = !1, identifierPrefix = "", onUncaughtError = defaultOnUncaughtError, onCaughtError = defaultOnCaughtError, onRecoverableError = defaultOnRecoverableError, formState = null;
			null !== options && void 0 !== options && (!0 === options.unstable_strictMode && (isStrictMode = !0), void 0 !== options.identifierPrefix && (identifierPrefix = options.identifierPrefix), void 0 !== options.onUncaughtError && (onUncaughtError = options.onUncaughtError), void 0 !== options.onCaughtError && (onCaughtError = options.onCaughtError), void 0 !== options.onRecoverableError && (onRecoverableError = options.onRecoverableError), void 0 !== options.formState && (formState = options.formState));
			initialChildren = createFiberRoot(container, 1, !0, initialChildren, null != options ? options : null, isStrictMode, identifierPrefix, formState, onUncaughtError, onCaughtError, onRecoverableError, defaultOnDefaultTransitionIndicator);
			initialChildren.context = getContextForSubtree(null);
			options = initialChildren.current;
			isStrictMode = requestUpdateLane(options);
			isStrictMode = getBumpedLaneForHydrationByLane(isStrictMode);
			identifierPrefix = createUpdate(isStrictMode);
			identifierPrefix.callback = null;
			enqueueUpdate(options, identifierPrefix, isStrictMode);
			startUpdateTimerByLane(isStrictMode, "hydrateRoot()", null);
			options = isStrictMode;
			initialChildren.current.lanes = options;
			markRootUpdated$1(initialChildren, options);
			ensureRootIsScheduled(initialChildren);
			container[internalContainerInstanceKey] = initialChildren.current;
			listenToAllSupportedEvents(container);
			return new ReactDOMHydrationRoot(initialChildren);
		};
		exports.version = "19.2.8";
		"undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
	})();
}));
//#endregion
//#region node_modules/web-vitals/dist/web-vitals.attribution.js
var import_client = (/* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_react_dom_client_development();
})))();
var t = class {
	t;
	o = 0;
	i = [];
	l(t) {
		if (t.hadRecentInput) return;
		const n = this.i[0], e = this.i.at(-1);
		this.o && n && e && t.startTime - e.startTime < 1e3 && t.startTime - n.startTime < 5e3 ? (this.o += t.value, this.i.push(t)) : (this.o = t.value, this.i = [t]), this.t?.(t);
	}
};
var n = () => {
	const t = performance.getEntriesByType("navigation")[0];
	if (t && t.responseStart > 0 && t.responseStart < performance.now()) return t;
};
var e = (t) => {
	if ("loading" === document.readyState) return "loading";
	const e = n();
	if (e) {
		if (t < e.domInteractive) return "loading";
		if (0 === e.domContentLoadedEventStart || t < e.domContentLoadedEventStart) return "dom-interactive";
		if (0 === e.domComplete || t < e.domComplete) return "dom-content-loaded";
	}
	return "complete";
};
var o = (t) => {
	const n = t.nodeName;
	return 1 === t.nodeType ? n.toLowerCase() : n.toUpperCase().replace(/^#/, "");
};
var i = (t) => {
	let n = "";
	try {
		for (; 9 !== t?.nodeType;) {
			const e = t, i = e.id ? "#" + e.id : [o(e), ...Array.from(e.classList ?? []).sort()].join(".");
			if (n.length + i.length > 99) return n || i;
			if (n = n ? i + ">" + n : i, e.id) break;
			t = e.parentNode;
		}
	} catch {}
	return n;
};
var a = /* @__PURE__ */ new WeakMap();
function r(t, n) {
	let e = a.get(n);
	return e || (e = /* @__PURE__ */ new WeakMap(), a.set(n, e)), e.get(t) || e.set(t, new n()), e.get(t);
}
var s = -1;
var c$1 = () => s;
var f = (t) => {
	addEventListener("pageshow", (n) => {
		n.persisted && (s = n.timeStamp, t(n));
	}, !0);
};
var l = (t, n, e, o) => {
	let i, a;
	return (r) => {
		n.value >= 0 && (r || o) && (a = n.value - (i ?? 0), (a || void 0 === i) && (i = n.value, n.delta = a, n.rating = ((t, n) => t > n[1] ? "poor" : t > n[0] ? "needs-improvement" : "good")(n.value, e), t(n)));
	};
};
var u = (t) => {
	requestAnimationFrame(() => requestAnimationFrame(t));
};
var d = () => n()?.activationStart ?? 0;
var h = -1;
var g = /* @__PURE__ */ new Set();
var v = () => "hidden" !== document.visibilityState || document.prerendering ? 1 / 0 : 0;
var p = (t) => {
	if ("hidden" === document.visibilityState) {
		if ("visibilitychange" === t.type) for (const t of g) t();
		isFinite(h) || (h = "visibilitychange" === t.type ? t.timeStamp : 0, removeEventListener("prerenderingchange", p, !0));
	}
};
var m = (t = !1) => {
	if (t && (h = 1 / 0), h < 0) {
		const t = d();
		h = (document.prerendering ? void 0 : globalThis.performance.getEntriesByType("visibility-state").find((n) => "hidden" === n.name && n.startTime >= t)?.startTime) ?? v(), addEventListener("visibilitychange", p, !0), addEventListener("prerenderingchange", p, !0), f(() => {
			setTimeout(() => {
				h = v();
			});
		});
	}
	return {
		get firstHiddenTime() {
			return h;
		},
		onHidden(t) {
			g.add(t);
		}
	};
};
var y = (t, e = -1, o, i = 0, a, r, s) => {
	const f = n(), l = f?.navigationId || 0;
	let u = "navigate";
	o ? u = o : c$1() >= 0 ? u = "back-forward-cache" : f && (document.prerendering || d() > 0 ? u = "prerender" : document.wasDiscarded ? u = "restore" : f.type && (u = f.type.replace(/_/g, "-")));
	return {
		name: t,
		value: e,
		rating: "good",
		delta: 0,
		entries: [],
		id: `v6-${Date.now()}-${Math.floor(8999999999999 * Math.random()) + 0xe8d4a51000}`,
		navigationType: u,
		navigationId: i || l,
		navigationInteractionId: a,
		navigationURL: r || f?.name,
		navigationStartTime: s || 0
	};
};
var b = (t, n, e = {}) => {
	try {
		const o = t.filter((t) => PerformanceObserver.supportedEntryTypes.includes(t));
		if (o.length > 0) {
			const t = new PerformanceObserver((t) => {
				queueMicrotask(() => {
					const e = t.getEntries();
					o.length > 1 && e.sort((t, n) => t.startTime + t.duration - (n.startTime + n.duration)), n(e);
				});
			});
			for (const n of o) t.observe({
				type: n,
				buffered: !0,
				...e
			});
			return t;
		}
	} catch {}
};
var M = (t) => globalThis.PerformanceObserver?.supportedEntryTypes.includes("soft-navigation") && "function" == typeof globalThis.PerformanceSoftNavigation?.prototype?.getLargestInteractionContentfulPaint && t && t.reportSoftNavs;
var T = (t, n) => {
	if (t.set(n.navigationId, n), t.size > 2) {
		const n = t.keys().next().value;
		void 0 !== n && t.delete(n);
	}
};
var E = (t) => {
	let n = !1;
	return () => {
		n || (t(), n = !0);
	};
};
var D = class {
	u;
};
var w = (t) => {
	document.prerendering ? addEventListener("prerenderingchange", t, !0) : t();
};
var S = [1800, 3e3];
var k = (t, n = {}) => {
	const e = M(n);
	w(() => {
		const o = r(n, D), i = m();
		let a, s = y("FCP");
		const h = b(["paint"], (t) => {
			for (const n of t) "first-contentful-paint" === n.name && (h.disconnect(), n.startTime < i.firstHiddenTime && (s.value = Math.max(n.startTime - d(), 0), s.entries.push(n), s.navigationId = n.navigationId || s.navigationId, a(!0)));
		});
		if (h && (a = l(t, s, S, n.reportAllChanges), f((e) => {
			s = y("FCP", -1, "back-forward-cache", s.navigationId, s.navigationInteractionId, s.navigationURL, c$1()), a = l(t, s, S, n.reportAllChanges), u(() => {
				s.value = performance.now() - e.timeStamp, a(!0);
			});
		})), e) b(["soft-navigation"], (e) => {
			e.forEach((e) => {
				o.u && e.navigationId && T(o.u, e);
				s = y("FCP", Math.max((e.presentationTime || e.paintTime || 0) - e.startTime, 0), "soft-navigation", e.navigationId, e.interactionId, e.name, e.startTime), a = l(t, s, S, n.reportAllChanges), a(!0);
			});
		}, n);
	});
};
var L = [.1, .25];
var P = (t) => t.find((t) => 1 === t.node?.nodeType) || t[0];
var F = (n, o = {}) => {
	const a = r(o = Object.assign({}, o), t), s = /* @__PURE__ */ new WeakMap();
	a.t = (t) => {
		if (t?.sources?.length) {
			const n = P(t.sources), e = n?.node;
			if (e) {
				const t = o.generateTarget?.(e) ?? i(e);
				s.set(n, t);
			}
		}
	};
	((n, e = {}) => {
		const o = m();
		k(E(() => {
			let i, a = y("CLS", 0);
			const s = r(e, t), d = (t, o, r, c, f) => {
				a = y("CLS", 0, t, o, r, c, f), s.o = 0, i = l(n, a, L, e.reportAllChanges);
			}, h = (t = !1) => {
				s.o > a.value && (a.value = s.o, a.entries = s.i), i(t);
			}, g = (t) => {
				h(!0), d("soft-navigation", t.navigationId, t.interactionId, t.name, t.startTime);
			}, v = (t) => {
				for (const n of t) "soft-navigation" !== n.entryType ? s.l(n) : g(n);
				h();
			}, p = ["layout-shift"];
			M(e) && p.push("soft-navigation");
			const m = b(p, v);
			m && (i = l(n, a, L, e.reportAllChanges), o.onHidden(() => {
				v(m.takeRecords()), i(!0);
			}), f(() => {
				d("back-forward-cache", a.navigationId, a.navigationInteractionId, a.navigationURL, c$1()), u(i);
			}), setTimeout(i));
		}));
	})((t) => {
		n(((t) => {
			let n = {};
			if (t.entries.length) {
				const o = t.entries.reduce((t, n) => t.value > n.value ? t : n);
				if (o?.sources?.length) {
					const t = P(o.sources);
					t && (n = {
						largestShiftTarget: s.get(t),
						largestShiftTime: o.startTime,
						largestShiftValue: o.value,
						largestShiftSource: t,
						largestShiftEntry: o,
						loadState: e(o.startTime)
					});
				}
			}
			return Object.assign(t, { attribution: n });
		})(t));
	}, o);
};
var _ = (t, o = {}) => {
	const i = r(o = Object.assign({}, o), D);
	M(o) && (i.u = /* @__PURE__ */ new Map());
	k((o) => {
		t(((t) => {
			let o = {
				timeToFirstByte: 0,
				firstByteToFCP: t.value,
				loadState: e(c$1())
			};
			if ("soft-navigation" !== t.navigationType) {
				if (t.entries.length) {
					const i = n(), a = t.entries.at(-1);
					if (i) {
						const n = i.responseStart, r = i.activationStart || 0, s = Math.max(0, n - r);
						o = {
							timeToFirstByte: s,
							firstByteToFCP: t.value - s,
							loadState: e(t.entries[0].startTime),
							navigationEntry: i,
							fcpEntry: a
						};
					}
				}
			} else {
				const n = i.u?.get(t.navigationId);
				n && (o = {
					timeToFirstByte: 0,
					firstByteToFCP: t.value,
					loadState: "complete",
					navigationEntry: n
				});
			}
			return Object.assign(t, { attribution: o });
		})(o));
	}, o);
};
var B = 0;
var C = 1 / 0;
var I = 0;
var O = (t) => {
	for (const n of t) n.interactionId && (C = Math.min(C, n.interactionId), I = Math.max(I, n.interactionId), B = I ? (I - C) / 7 + 1 : 0);
};
var j;
var A = () => j ? B : performance.interactionCount ?? 0;
var q = () => {
	"interactionCount" in performance || j || (j = b(["event"], O, { durationThreshold: 0 }));
};
var N = 0;
var W = class {
	h = [];
	v = /* @__PURE__ */ new Map();
	p;
	m;
	M() {
		N = A(), this.h.length = 0, this.v.clear();
	}
	T(t) {
		const n = A() - N, e = Math.min(this.h.length - 1, Math.floor(n / 50));
		return !n || -1 !== e || "soft-navigation" !== t && "back-forward-cache" !== t ? this.h[e] : {
			D: 8,
			id: -1,
			entries: []
		};
	}
	l(t) {
		if (this.p?.(t), !t.interactionId && "first-input" !== t.entryType) return;
		const n = this.h.at(-1);
		let e = this.v.get(t.interactionId);
		if (e || this.h.length < 10 || t.duration > n.D) {
			if (e ? t.duration > e.D ? (e.entries = [t], e.D = t.duration) : t.duration === e.D && t.startTime === e.entries[0].startTime && e.entries.push(t) : (e = {
				id: t.interactionId,
				entries: [t],
				D: t.duration
			}, this.v.set(e.id, e), this.h.push(e)), this.h.sort((t, n) => n.D - t.D), this.h.length > 10) {
				const t = this.h.splice(10);
				for (const n of t) this.v.delete(n.id);
			}
			this.m?.(e);
		}
	}
};
var x = (t) => {
	const n = "requestIdleCallback" in globalThis ? 1e3 : 0, e = globalThis.requestIdleCallback || setTimeout, o = globalThis.cancelIdleCallback || clearTimeout;
	if ("hidden" === document.visibilityState) t();
	else {
		const i = E(t);
		let a = -1;
		const r = () => {
			o(a), i();
		};
		addEventListener("visibilitychange", r, {
			once: !0,
			capture: !0
		}), a = e(() => {
			removeEventListener("visibilitychange", r, { capture: !0 }), i();
		}, { timeout: n });
	}
};
var R = [200, 500];
var U = (t, n = {}) => {
	const o = r(n = Object.assign({}, n), W);
	let a = [], s = [], u = 0;
	const d = /* @__PURE__ */ new WeakMap(), h = /* @__PURE__ */ new WeakMap();
	let g = !1;
	const v = () => {
		g || (x(p), g = !0);
	}, p = () => {
		const t = new Set(o.h.map((t) => d.get(t.entries[0]))), n = s.length - 10;
		s = s.filter((e, o) => o >= n || t.has(e));
		const e = /* @__PURE__ */ new Set();
		for (const t of s) {
			const n = T(t.startTime, t.processingEnd);
			for (const t of n) e.add(t);
		}
		a = a.filter((t) => t.startTime > u || e.has(t)), g = !1;
	};
	o.p = (t) => {
		const e = t.startTime + t.duration;
		let o;
		u = Math.max(u, t.processingEnd);
		for (let i = s.length - 1; i >= 0; i--) {
			const a = s[i];
			if (Math.abs(e - a.renderTime) <= 8) {
				o = a, o.startTime = Math.min(t.startTime, o.startTime), o.processingStart = Math.min(t.processingStart, o.processingStart), o.processingEnd = Math.max(t.processingEnd, o.processingEnd), n.includeProcessedEventEntries && o.entries.push(t);
				break;
			}
		}
		o || (o = {
			startTime: t.startTime,
			processingStart: t.processingStart,
			processingEnd: t.processingEnd,
			renderTime: e,
			entries: n.includeProcessedEventEntries ? [t] : []
		}, s.push(o)), (t.interactionId || "first-input" === t.entryType) && d.set(t, o), v();
	}, o.m = (t) => {
		if (!h.get(t)) {
			const e = t.entries.find((t) => t.target)?.target;
			if (e) {
				const o = n.generateTarget?.(e) ?? i(e);
				h.set(t, o);
			} else {
				const n = t.entries.find((t) => t.targetSelector)?.targetSelector;
				n && h.set(t, n);
			}
		}
	};
	const T = (t, n) => {
		const e = [];
		for (const o of a) if (!(o.startTime + o.duration < t)) {
			if (o.startTime > n) break;
			e.push(o);
		}
		return e;
	}, E = (t) => {
		if (0 === t.entries.length) {
			const n = t.navigationStartTime || 0, o = {
				processedEventEntries: [],
				longAnimationFrameEntries: [],
				inputDelay: 0,
				processingDuration: 0,
				presentationDelay: t.value,
				loadState: e(n)
			};
			return Object.assign(t, { attribution: o });
		}
		const n = t.entries[0], i = d.get(n), a = i.processingStart, r = Math.max(n.startTime + n.duration, a), s = Math.min(i.processingEnd, r), c = i.entries.sort((t, n) => t.processingStart - n.processingStart), f = T(n.startTime, s), l = o.v.get(n.interactionId), u = {
			interactionTarget: h.get(l),
			interactionType: n.name.startsWith("key") ? "keyboard" : "pointer",
			interactionTime: n.startTime,
			nextPaintTime: r,
			processedEventEntries: c,
			longAnimationFrameEntries: f,
			inputDelay: a - n.startTime,
			processingDuration: s - a,
			presentationDelay: r - s,
			loadState: e(n.startTime),
			longestScript: void 0,
			totalScriptDuration: void 0,
			totalStyleAndLayoutDuration: void 0,
			totalPaintDuration: void 0,
			totalUnattributedDuration: void 0
		};
		return ((t) => {
			const n = t.interactionTime, e = t.nextPaintTime;
			if (!t.longAnimationFrameEntries?.length || !n || !e) return;
			const o = t.inputDelay, i = t.processingDuration;
			let a, r, s = 0, c = 0, f = 0, l = 0;
			for (const e of t.longAnimationFrameEntries) {
				c = c + e.startTime + e.duration - e.styleAndLayoutStart;
				for (const t of e.scripts) {
					const e = t.startTime + t.duration;
					if (e < n) continue;
					const f = e - Math.max(n, t.startTime), u = t.duration ? f / t.duration * t.forcedStyleAndLayoutDuration : 0;
					s += f - u, c += u, f > l && (r = t.startTime < n + o ? "input-delay" : t.startTime >= n + o + i ? "presentation-delay" : "processing-duration", a = t, l = f);
				}
			}
			const u = t.longAnimationFrameEntries.at(-1), d = u ? u.startTime + u.duration : 0;
			d >= n + o + i && (f = e - d), a && r && (t.longestScript = {
				entry: a,
				subpart: r,
				intersectingDuration: l
			}), t.totalScriptDuration = s, t.totalStyleAndLayoutDuration = c, t.totalPaintDuration = f, t.totalUnattributedDuration = e - n - s - c - f;
		})(u), Object.assign(t, { attribution: u });
	};
	b(["long-animation-frame"], (t) => {
		a = a.concat(t), v();
	}, n), ((t, n = {}) => {
		if (!globalThis.PerformanceEventTiming || !("interactionId" in PerformanceEventTiming.prototype)) return;
		const e = m();
		w(() => {
			q();
			let o, i = y("INP");
			const a = r(n, W), s = (e, r, s, c, f) => {
				a.M(), i = y("INP", -1, e, r, s, c, f), o = l(t, i, R, n.reportAllChanges);
			}, u = () => {
				const t = a.T(i.navigationType);
				t && t.D !== i.value && (i.value = t.D, i.entries = t.entries, o());
			}, d = (t) => {
				u(), o(!0), s("soft-navigation", t.navigationId, t.interactionId, t.name, t.startTime);
			}, h = (t, n = !1) => {
				x(() => {
					for (const n of t) "soft-navigation" !== n.entryType ? a.l(n) : d(n);
					u(), n && o(!0);
				});
			}, g = ["event", "first-input"];
			M(n) && g.push("soft-navigation");
			const v = b(g, h, {
				...n,
				durationThreshold: n.durationThreshold ?? 40
			});
			o = l(t, i, R, n.reportAllChanges), v && (e.onHidden(() => {
				h(v.takeRecords(), !0);
			}), f(() => {
				s("back-forward-cache", i.navigationId, i.navigationInteractionId, i.navigationURL, c$1());
			}));
		});
	})((n) => {
		t(E(n));
	}, n);
};
var $ = class {
	p;
	u;
	l(t) {
		this.p?.(t);
	}
};
var H = [2500, 4e3];
var V = 50;
var z = [];
b(["resource"], (t) => {
	for (const n of t) z.push(n), z.length > V && z.shift();
});
var G = (t, e = {}) => {
	null != (e = Object.assign({}, e)).resourceBufferSize && (V = e.resourceBufferSize);
	const o = r(e, $), a = /* @__PURE__ */ new WeakMap();
	M(e) && (o.u = /* @__PURE__ */ new Map()), o.p = (t) => {
		const n = t.element;
		if (n) {
			const o = e.generateTarget?.(n) ?? i(n);
			a.set(t, o);
		} else t.id && a.set(t, `#${t.id}`);
	};
	((t, n = {}) => {
		let e = !1;
		const o = M(n);
		w(() => {
			let i, a = m(), s = y("LCP");
			const h = r(n, $), g = (o, r, c, f, u) => {
				s = y("LCP", -1, o, r, c, f, u), i = l(t, s, H, n.reportAllChanges), e = !1, "soft-navigation" === o && (a = m(!0));
			}, v = (t) => {
				h.u && t.navigationId && T(h.u, t), e || i(!0), g("soft-navigation", t.navigationId, t.interactionId, t.name, t.startTime);
				const n = t.getLargestInteractionContentfulPaint?.();
				n && p([n]);
			}, p = (t) => {
				n.reportAllChanges || o || (t = t.slice(-1));
				for (const n of t) {
					if (!n) continue;
					if ("soft-navigation" === n.entryType) {
						v(n);
						continue;
					}
					let t = 0, e = [], o = n.startTime;
					if ("largest-contentful-paint" === n.entryType) t = Math.max(n.startTime - d(), 0), h.l(n), e = [n];
					else if ("interaction-contentful-paint" === n.entryType) {
						const i = n;
						if (!s.navigationId) continue;
						if ("interactionId" in i && i.interactionId != s.navigationInteractionId) continue;
						o = i.largestContentfulPaint?.renderTime || 0, t = Math.max(o - n.startTime, 0), i.largestContentfulPaint && (h.l(i.largestContentfulPaint), e = [i.largestContentfulPaint]);
					}
					o < a.firstHiddenTime && (s.value = t, s.entries = e, i());
				}
			}, M = ["largest-contentful-paint"];
			o && M.push("interaction-contentful-paint", "soft-navigation");
			const E = b(M, p);
			if (E) {
				i = l(t, s, H, n.reportAllChanges);
				const a = [
					"keydown",
					"click",
					"visibilitychange"
				], r = (t) => {
					if (t.isTrusted && !e) {
						const t = s.id;
						x(() => {
							if (!e) {
								if (!o) {
									E.disconnect();
									for (const t of a) removeEventListener(t, r, { capture: !0 });
								}
								t === s.id && (e = !0, i(!0));
							}
						});
					}
				};
				for (const t of a) addEventListener(t, r, { capture: !0 });
				f((o) => {
					g("back-forward-cache", s.navigationId, s.navigationInteractionId, s.navigationURL, c$1()), i = l(t, s, H, n.reportAllChanges), u(() => {
						s.value = performance.now() - o.timeStamp, e = !0, i(!0);
					});
				});
			}
		});
	})((e) => {
		t(((t) => {
			let e = {
				timeToFirstByte: 0,
				resourceLoadDelay: 0,
				resourceLoadDuration: 0,
				elementRenderDelay: t.value
			};
			if (t.entries.length) {
				const i = t.entries.at(-1), r = i.url && (z.findLast((t) => t.name === i.url) || performance.getEntriesByType("resource").findLast((t) => t.name === i.url));
				let s;
				e.target = a.get(i), e.lcpEntry = i, i.url && (e.url = i.url), r && (e.lcpResourceEntry = r);
				let c = 0, f = 0;
				if ("soft-navigation" !== t.navigationType ? (s = n(), c = s?.activationStart ?? 0, f = s?.responseStart ?? 0) : (c = t.navigationStartTime || 0, s = o.u?.get(t.navigationId)), s) {
					const n = Math.max(0, f - c), o = Math.max(n, r ? (r.requestStart || r.startTime) - c : 0), i = Math.min(t.value, Math.max(o, r ? r.responseEnd - c : 0));
					e = {
						...e,
						timeToFirstByte: n,
						resourceLoadDelay: o - n,
						resourceLoadDuration: i - o,
						elementRenderDelay: t.value - i,
						navigationEntry: s
					};
				}
			}
			return Object.assign(t, { attribution: e });
		})(e));
	}, e);
};
var J = [800, 1800];
var K = (t) => {
	document.prerendering ? w(() => K(t)) : "complete" !== document.readyState ? addEventListener("load", () => K(t), !0) : setTimeout(t);
};
var Q = (t, e = {}) => {
	((t, e = {}) => {
		const o = M(e);
		let i = y("TTFB"), a = l(t, i, J, e.reportAllChanges);
		K(() => {
			const r = n();
			if (r) {
				const n = r.responseStart;
				i.value = Math.max(n - d(), 0), i.entries = [r], a(!0), f(() => {
					i = y("TTFB", 0, "back-forward-cache", i.navigationId, i.navigationInteractionId, i.navigationURL, c$1()), a = l(t, i, J, e.reportAllChanges), a(!0);
				}), o && b(["soft-navigation"], (n) => {
					n.forEach((n) => {
						n.navigationId && (i = y("TTFB", 0, "soft-navigation", n.navigationId, n.interactionId, n.name, n.startTime), i.entries = [n], a = l(t, i, J, e.reportAllChanges), a(!0));
					});
				}, e);
			}
		});
	})((n) => {
		t(((t) => {
			const n = t.entries[0];
			let e = {
				waitingDuration: 0,
				cacheDuration: 0,
				dnsDuration: 0,
				connectionDuration: 0,
				requestDuration: 0,
				navigationEntry: n
			};
			if (t.entries.length && n instanceof PerformanceNavigationTiming) {
				const o = n.activationStart || 0, i = Math.max((n.workerStart || n.fetchStart || 0) - o, 0), a = Math.max(n.domainLookupStart - o, 0), r = Math.max(n.connectStart - o, 0), s = Math.max(n.connectEnd - o, 0);
				e = {
					waitingDuration: i,
					cacheDuration: a - i,
					dnsDuration: r - a,
					connectionDuration: s - r,
					requestDuration: t.value - s,
					navigationEntry: n
				};
			}
			return Object.assign(t, { attribution: e });
		})(n));
	}, e);
};
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/assertThisInitialized.js
function _assertThisInitialized(e) {
	if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
	return e;
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/getPrototypeOf.js
function _getPrototypeOf(t) {
	return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(t) {
		return t.__proto__ || Object.getPrototypeOf(t);
	}, _getPrototypeOf(t);
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/isNativeFunction.js
function _isNativeFunction(t) {
	try {
		return -1 !== Function.toString.call(t).indexOf("[native code]");
	} catch (n) {
		return "function" == typeof t;
	}
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/isNativeReflectConstruct.js
function _isNativeReflectConstruct() {
	try {
		var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {}));
	} catch (t) {}
	return (_isNativeReflectConstruct = function _isNativeReflectConstruct() {
		return !!t;
	})();
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/construct.js
function _construct(t, e, r) {
	if (_isNativeReflectConstruct()) return Reflect.construct.apply(null, arguments);
	var o = [null];
	o.push.apply(o, e);
	var p = new (t.bind.apply(t, o))();
	return r && _setPrototypeOf(p, r.prototype), p;
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/wrapNativeSuper.js
function _wrapNativeSuper(t) {
	var r = "function" == typeof Map ? /* @__PURE__ */ new Map() : void 0;
	return _wrapNativeSuper = function _wrapNativeSuper(t) {
		if (null === t || !_isNativeFunction(t)) return t;
		if ("function" != typeof t) throw new TypeError("Super expression must either be null or a function");
		if (void 0 !== r) {
			if (r.has(t)) return r.get(t);
			r.set(t, Wrapper);
		}
		function Wrapper() {
			return _construct(t, arguments, _getPrototypeOf(this).constructor);
		}
		return Wrapper.prototype = Object.create(t.prototype, { constructor: {
			value: Wrapper,
			enumerable: !1,
			writable: !0,
			configurable: !0
		} }), _setPrototypeOf(Wrapper, t);
	}, _wrapNativeSuper(t);
}
//#endregion
//#region node_modules/polished/dist/polished.esm.js
/**
* Parse errors.md and turn it into a simple hash of code: message
* @private
*/
var ERRORS = {
	"1": "Passed invalid arguments to hsl, please pass multiple numbers e.g. hsl(360, 0.75, 0.4) or an object e.g. rgb({ hue: 255, saturation: 0.4, lightness: 0.75 }).\n\n",
	"2": "Passed invalid arguments to hsla, please pass multiple numbers e.g. hsla(360, 0.75, 0.4, 0.7) or an object e.g. rgb({ hue: 255, saturation: 0.4, lightness: 0.75, alpha: 0.7 }).\n\n",
	"3": "Passed an incorrect argument to a color function, please pass a string representation of a color.\n\n",
	"4": "Couldn't generate valid rgb string from %s, it returned %s.\n\n",
	"5": "Couldn't parse the color string. Please provide the color as a string in hex, rgb, rgba, hsl or hsla notation.\n\n",
	"6": "Passed invalid arguments to rgb, please pass multiple numbers e.g. rgb(255, 205, 100) or an object e.g. rgb({ red: 255, green: 205, blue: 100 }).\n\n",
	"7": "Passed invalid arguments to rgba, please pass multiple numbers e.g. rgb(255, 205, 100, 0.75) or an object e.g. rgb({ red: 255, green: 205, blue: 100, alpha: 0.75 }).\n\n",
	"8": "Passed invalid argument to toColorString, please pass a RgbColor, RgbaColor, HslColor or HslaColor object.\n\n",
	"9": "Please provide a number of steps to the modularScale helper.\n\n",
	"10": "Please pass a number or one of the predefined scales to the modularScale helper as the ratio.\n\n",
	"11": "Invalid value passed as base to modularScale, expected number or em string but got \"%s\"\n\n",
	"12": "Expected a string ending in \"px\" or a number passed as the first argument to %s(), got \"%s\" instead.\n\n",
	"13": "Expected a string ending in \"px\" or a number passed as the second argument to %s(), got \"%s\" instead.\n\n",
	"14": "Passed invalid pixel value (\"%s\") to %s(), please pass a value like \"12px\" or 12.\n\n",
	"15": "Passed invalid base value (\"%s\") to %s(), please pass a value like \"12px\" or 12.\n\n",
	"16": "You must provide a template to this method.\n\n",
	"17": "You passed an unsupported selector state to this method.\n\n",
	"18": "minScreen and maxScreen must be provided as stringified numbers with the same units.\n\n",
	"19": "fromSize and toSize must be provided as stringified numbers with the same units.\n\n",
	"20": "expects either an array of objects or a single object with the properties prop, fromSize, and toSize.\n\n",
	"21": "expects the objects in the first argument array to have the properties `prop`, `fromSize`, and `toSize`.\n\n",
	"22": "expects the first argument object to have the properties `prop`, `fromSize`, and `toSize`.\n\n",
	"23": "fontFace expects a name of a font-family.\n\n",
	"24": "fontFace expects either the path to the font file(s) or a name of a local copy.\n\n",
	"25": "fontFace expects localFonts to be an array.\n\n",
	"26": "fontFace expects fileFormats to be an array.\n\n",
	"27": "radialGradient requries at least 2 color-stops to properly render.\n\n",
	"28": "Please supply a filename to retinaImage() as the first argument.\n\n",
	"29": "Passed invalid argument to triangle, please pass correct pointingDirection e.g. 'right'.\n\n",
	"30": "Passed an invalid value to `height` or `width`. Please provide a pixel based unit.\n\n",
	"31": "The animation shorthand only takes 8 arguments. See the specification for more information: http://mdn.io/animation\n\n",
	"32": "To pass multiple animations please supply them in arrays, e.g. animation(['rotate', '2s'], ['move', '1s'])\nTo pass a single animation please supply them in simple values, e.g. animation('rotate', '2s')\n\n",
	"33": "The animation shorthand arrays can only have 8 elements. See the specification for more information: http://mdn.io/animation\n\n",
	"34": "borderRadius expects a radius value as a string or number as the second argument.\n\n",
	"35": "borderRadius expects one of \"top\", \"bottom\", \"left\" or \"right\" as the first argument.\n\n",
	"36": "Property must be a string value.\n\n",
	"37": "Syntax Error at %s.\n\n",
	"38": "Formula contains a function that needs parentheses at %s.\n\n",
	"39": "Formula is missing closing parenthesis at %s.\n\n",
	"40": "Formula has too many closing parentheses at %s.\n\n",
	"41": "All values in a formula must have the same unit or be unitless.\n\n",
	"42": "Please provide a number of steps to the modularScale helper.\n\n",
	"43": "Please pass a number or one of the predefined scales to the modularScale helper as the ratio.\n\n",
	"44": "Invalid value passed as base to modularScale, expected number or em/rem string but got %s.\n\n",
	"45": "Passed invalid argument to hslToColorString, please pass a HslColor or HslaColor object.\n\n",
	"46": "Passed invalid argument to rgbToColorString, please pass a RgbColor or RgbaColor object.\n\n",
	"47": "minScreen and maxScreen must be provided as stringified numbers with the same units.\n\n",
	"48": "fromSize and toSize must be provided as stringified numbers with the same units.\n\n",
	"49": "Expects either an array of objects or a single object with the properties prop, fromSize, and toSize.\n\n",
	"50": "Expects the objects in the first argument array to have the properties prop, fromSize, and toSize.\n\n",
	"51": "Expects the first argument object to have the properties prop, fromSize, and toSize.\n\n",
	"52": "fontFace expects either the path to the font file(s) or a name of a local copy.\n\n",
	"53": "fontFace expects localFonts to be an array.\n\n",
	"54": "fontFace expects fileFormats to be an array.\n\n",
	"55": "fontFace expects a name of a font-family.\n\n",
	"56": "linearGradient requries at least 2 color-stops to properly render.\n\n",
	"57": "radialGradient requries at least 2 color-stops to properly render.\n\n",
	"58": "Please supply a filename to retinaImage() as the first argument.\n\n",
	"59": "Passed invalid argument to triangle, please pass correct pointingDirection e.g. 'right'.\n\n",
	"60": "Passed an invalid value to `height` or `width`. Please provide a pixel based unit.\n\n",
	"61": "Property must be a string value.\n\n",
	"62": "borderRadius expects a radius value as a string or number as the second argument.\n\n",
	"63": "borderRadius expects one of \"top\", \"bottom\", \"left\" or \"right\" as the first argument.\n\n",
	"64": "The animation shorthand only takes 8 arguments. See the specification for more information: http://mdn.io/animation.\n\n",
	"65": "To pass multiple animations please supply them in arrays, e.g. animation(['rotate', '2s'], ['move', '1s'])\\nTo pass a single animation please supply them in simple values, e.g. animation('rotate', '2s').\n\n",
	"66": "The animation shorthand arrays can only have 8 elements. See the specification for more information: http://mdn.io/animation.\n\n",
	"67": "You must provide a template to this method.\n\n",
	"68": "You passed an unsupported selector state to this method.\n\n",
	"69": "Expected a string ending in \"px\" or a number passed as the first argument to %s(), got %s instead.\n\n",
	"70": "Expected a string ending in \"px\" or a number passed as the second argument to %s(), got %s instead.\n\n",
	"71": "Passed invalid pixel value %s to %s(), please pass a value like \"12px\" or 12.\n\n",
	"72": "Passed invalid base value %s to %s(), please pass a value like \"12px\" or 12.\n\n",
	"73": "Please provide a valid CSS variable.\n\n",
	"74": "CSS variable not found and no default was provided.\n\n",
	"75": "important requires a valid style object, got a %s instead.\n\n",
	"76": "fromSize and toSize must be provided as stringified numbers with the same units as minScreen and maxScreen.\n\n",
	"77": "remToPx expects a value in \"rem\" but you provided it in \"%s\".\n\n",
	"78": "base must be set in \"px\" or \"%\" but you set it in \"%s\".\n"
};
/**
* super basic version of sprintf
* @private
*/
function format() {
	for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
	var a = args[0];
	var b = [];
	var c;
	for (c = 1; c < args.length; c += 1) b.push(args[c]);
	b.forEach(function(d) {
		a = a.replace(/%[a-z]/, d);
	});
	return a;
}
/**
* Create an error file out of errors.md for development and a simple web link to the full errors
* in production mode.
* @private
*/
var PolishedError = /*#__PURE__*/ function(_Error) {
	_inheritsLoose(PolishedError, _Error);
	function PolishedError(code) {
		var _this;
		for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) args[_key2 - 1] = arguments[_key2];
		_this = _Error.call(this, format.apply(void 0, [ERRORS[code]].concat(args))) || this;
		return _assertThisInitialized(_this);
	}
	return PolishedError;
}(/*#__PURE__*/ _wrapNativeSuper(Error));
function colorToInt(color) {
	return Math.round(color * 255);
}
function convertToInt(red, green, blue) {
	return colorToInt(red) + "," + colorToInt(green) + "," + colorToInt(blue);
}
function hslToRgb(hue, saturation, lightness, convert) {
	if (convert === void 0) convert = convertToInt;
	if (saturation === 0) return convert(lightness, lightness, lightness);
	var huePrime = (hue % 360 + 360) % 360 / 60;
	var chroma = (1 - Math.abs(2 * lightness - 1)) * saturation;
	var secondComponent = chroma * (1 - Math.abs(huePrime % 2 - 1));
	var red = 0;
	var green = 0;
	var blue = 0;
	if (huePrime >= 0 && huePrime < 1) {
		red = chroma;
		green = secondComponent;
	} else if (huePrime >= 1 && huePrime < 2) {
		red = secondComponent;
		green = chroma;
	} else if (huePrime >= 2 && huePrime < 3) {
		green = chroma;
		blue = secondComponent;
	} else if (huePrime >= 3 && huePrime < 4) {
		green = secondComponent;
		blue = chroma;
	} else if (huePrime >= 4 && huePrime < 5) {
		red = secondComponent;
		blue = chroma;
	} else if (huePrime >= 5 && huePrime < 6) {
		red = chroma;
		blue = secondComponent;
	}
	var lightnessModification = lightness - chroma / 2;
	var finalRed = red + lightnessModification;
	var finalGreen = green + lightnessModification;
	var finalBlue = blue + lightnessModification;
	return convert(finalRed, finalGreen, finalBlue);
}
var namedColorMap = {
	aliceblue: "f0f8ff",
	antiquewhite: "faebd7",
	aqua: "00ffff",
	aquamarine: "7fffd4",
	azure: "f0ffff",
	beige: "f5f5dc",
	bisque: "ffe4c4",
	black: "000",
	blanchedalmond: "ffebcd",
	blue: "0000ff",
	blueviolet: "8a2be2",
	brown: "a52a2a",
	burlywood: "deb887",
	cadetblue: "5f9ea0",
	chartreuse: "7fff00",
	chocolate: "d2691e",
	coral: "ff7f50",
	cornflowerblue: "6495ed",
	cornsilk: "fff8dc",
	crimson: "dc143c",
	cyan: "00ffff",
	darkblue: "00008b",
	darkcyan: "008b8b",
	darkgoldenrod: "b8860b",
	darkgray: "a9a9a9",
	darkgreen: "006400",
	darkgrey: "a9a9a9",
	darkkhaki: "bdb76b",
	darkmagenta: "8b008b",
	darkolivegreen: "556b2f",
	darkorange: "ff8c00",
	darkorchid: "9932cc",
	darkred: "8b0000",
	darksalmon: "e9967a",
	darkseagreen: "8fbc8f",
	darkslateblue: "483d8b",
	darkslategray: "2f4f4f",
	darkslategrey: "2f4f4f",
	darkturquoise: "00ced1",
	darkviolet: "9400d3",
	deeppink: "ff1493",
	deepskyblue: "00bfff",
	dimgray: "696969",
	dimgrey: "696969",
	dodgerblue: "1e90ff",
	firebrick: "b22222",
	floralwhite: "fffaf0",
	forestgreen: "228b22",
	fuchsia: "ff00ff",
	gainsboro: "dcdcdc",
	ghostwhite: "f8f8ff",
	gold: "ffd700",
	goldenrod: "daa520",
	gray: "808080",
	green: "008000",
	greenyellow: "adff2f",
	grey: "808080",
	honeydew: "f0fff0",
	hotpink: "ff69b4",
	indianred: "cd5c5c",
	indigo: "4b0082",
	ivory: "fffff0",
	khaki: "f0e68c",
	lavender: "e6e6fa",
	lavenderblush: "fff0f5",
	lawngreen: "7cfc00",
	lemonchiffon: "fffacd",
	lightblue: "add8e6",
	lightcoral: "f08080",
	lightcyan: "e0ffff",
	lightgoldenrodyellow: "fafad2",
	lightgray: "d3d3d3",
	lightgreen: "90ee90",
	lightgrey: "d3d3d3",
	lightpink: "ffb6c1",
	lightsalmon: "ffa07a",
	lightseagreen: "20b2aa",
	lightskyblue: "87cefa",
	lightslategray: "789",
	lightslategrey: "789",
	lightsteelblue: "b0c4de",
	lightyellow: "ffffe0",
	lime: "0f0",
	limegreen: "32cd32",
	linen: "faf0e6",
	magenta: "f0f",
	maroon: "800000",
	mediumaquamarine: "66cdaa",
	mediumblue: "0000cd",
	mediumorchid: "ba55d3",
	mediumpurple: "9370db",
	mediumseagreen: "3cb371",
	mediumslateblue: "7b68ee",
	mediumspringgreen: "00fa9a",
	mediumturquoise: "48d1cc",
	mediumvioletred: "c71585",
	midnightblue: "191970",
	mintcream: "f5fffa",
	mistyrose: "ffe4e1",
	moccasin: "ffe4b5",
	navajowhite: "ffdead",
	navy: "000080",
	oldlace: "fdf5e6",
	olive: "808000",
	olivedrab: "6b8e23",
	orange: "ffa500",
	orangered: "ff4500",
	orchid: "da70d6",
	palegoldenrod: "eee8aa",
	palegreen: "98fb98",
	paleturquoise: "afeeee",
	palevioletred: "db7093",
	papayawhip: "ffefd5",
	peachpuff: "ffdab9",
	peru: "cd853f",
	pink: "ffc0cb",
	plum: "dda0dd",
	powderblue: "b0e0e6",
	purple: "800080",
	rebeccapurple: "639",
	red: "f00",
	rosybrown: "bc8f8f",
	royalblue: "4169e1",
	saddlebrown: "8b4513",
	salmon: "fa8072",
	sandybrown: "f4a460",
	seagreen: "2e8b57",
	seashell: "fff5ee",
	sienna: "a0522d",
	silver: "c0c0c0",
	skyblue: "87ceeb",
	slateblue: "6a5acd",
	slategray: "708090",
	slategrey: "708090",
	snow: "fffafa",
	springgreen: "00ff7f",
	steelblue: "4682b4",
	tan: "d2b48c",
	teal: "008080",
	thistle: "d8bfd8",
	tomato: "ff6347",
	turquoise: "40e0d0",
	violet: "ee82ee",
	wheat: "f5deb3",
	white: "fff",
	whitesmoke: "f5f5f5",
	yellow: "ff0",
	yellowgreen: "9acd32"
};
/**
* Checks if a string is a CSS named color and returns its equivalent hex value, otherwise returns the original color.
* @private
*/
function nameToHex(color) {
	if (typeof color !== "string") return color;
	var normalizedColorName = color.toLowerCase();
	return namedColorMap[normalizedColorName] ? "#" + namedColorMap[normalizedColorName] : color;
}
var hexRegex = /^#[a-fA-F0-9]{6}$/;
var hexRgbaRegex = /^#[a-fA-F0-9]{8}$/;
var reducedHexRegex = /^#[a-fA-F0-9]{3}$/;
var reducedRgbaHexRegex = /^#[a-fA-F0-9]{4}$/;
var rgbRegex = /^rgb\(\s*(\d{1,3})\s*(?:,)?\s*(\d{1,3})\s*(?:,)?\s*(\d{1,3})\s*\)$/i;
var rgbaRegex = /^rgb(?:a)?\(\s*(\d{1,3})\s*(?:,)?\s*(\d{1,3})\s*(?:,)?\s*(\d{1,3})\s*(?:,|\/)\s*([-+]?\d*[.]?\d+[%]?)\s*\)$/i;
var hslRegex = /^hsl\(\s*(\d{0,3}[.]?[0-9]+(?:deg)?)\s*(?:,)?\s*(\d{1,3}[.]?[0-9]?)%\s*(?:,)?\s*(\d{1,3}[.]?[0-9]?)%\s*\)$/i;
var hslaRegex = /^hsl(?:a)?\(\s*(\d{0,3}[.]?[0-9]+(?:deg)?)\s*(?:,)?\s*(\d{1,3}[.]?[0-9]?)%\s*(?:,)?\s*(\d{1,3}[.]?[0-9]?)%\s*(?:,|\/)\s*([-+]?\d*[.]?\d+[%]?)\s*\)$/i;
/**
* Returns an RgbColor or RgbaColor object. This utility function is only useful
* if want to extract a color component. With the color util `toColorString` you
* can convert a RgbColor or RgbaColor object back to a string.
*
* @example
* // Assigns `{ red: 255, green: 0, blue: 0 }` to color1
* const color1 = parseToRgb('rgb(255, 0, 0)');
* // Assigns `{ red: 92, green: 102, blue: 112, alpha: 0.75 }` to color2
* const color2 = parseToRgb('hsla(210, 10%, 40%, 0.75)');
*/
function parseToRgb(color) {
	if (typeof color !== "string") throw new PolishedError(3);
	var normalizedColor = nameToHex(color);
	if (normalizedColor.match(hexRegex)) return {
		red: parseInt("" + normalizedColor[1] + normalizedColor[2], 16),
		green: parseInt("" + normalizedColor[3] + normalizedColor[4], 16),
		blue: parseInt("" + normalizedColor[5] + normalizedColor[6], 16)
	};
	if (normalizedColor.match(hexRgbaRegex)) {
		var alpha = parseFloat((parseInt("" + normalizedColor[7] + normalizedColor[8], 16) / 255).toFixed(2));
		return {
			red: parseInt("" + normalizedColor[1] + normalizedColor[2], 16),
			green: parseInt("" + normalizedColor[3] + normalizedColor[4], 16),
			blue: parseInt("" + normalizedColor[5] + normalizedColor[6], 16),
			alpha
		};
	}
	if (normalizedColor.match(reducedHexRegex)) return {
		red: parseInt("" + normalizedColor[1] + normalizedColor[1], 16),
		green: parseInt("" + normalizedColor[2] + normalizedColor[2], 16),
		blue: parseInt("" + normalizedColor[3] + normalizedColor[3], 16)
	};
	if (normalizedColor.match(reducedRgbaHexRegex)) {
		var _alpha = parseFloat((parseInt("" + normalizedColor[4] + normalizedColor[4], 16) / 255).toFixed(2));
		return {
			red: parseInt("" + normalizedColor[1] + normalizedColor[1], 16),
			green: parseInt("" + normalizedColor[2] + normalizedColor[2], 16),
			blue: parseInt("" + normalizedColor[3] + normalizedColor[3], 16),
			alpha: _alpha
		};
	}
	var rgbMatched = rgbRegex.exec(normalizedColor);
	if (rgbMatched) return {
		red: parseInt("" + rgbMatched[1], 10),
		green: parseInt("" + rgbMatched[2], 10),
		blue: parseInt("" + rgbMatched[3], 10)
	};
	var rgbaMatched = rgbaRegex.exec(normalizedColor.substring(0, 50));
	if (rgbaMatched) return {
		red: parseInt("" + rgbaMatched[1], 10),
		green: parseInt("" + rgbaMatched[2], 10),
		blue: parseInt("" + rgbaMatched[3], 10),
		alpha: parseFloat("" + rgbaMatched[4]) > 1 ? parseFloat("" + rgbaMatched[4]) / 100 : parseFloat("" + rgbaMatched[4])
	};
	var hslMatched = hslRegex.exec(normalizedColor);
	if (hslMatched) {
		var rgbColorString = "rgb(" + hslToRgb(parseInt("" + hslMatched[1], 10), parseInt("" + hslMatched[2], 10) / 100, parseInt("" + hslMatched[3], 10) / 100) + ")";
		var hslRgbMatched = rgbRegex.exec(rgbColorString);
		if (!hslRgbMatched) throw new PolishedError(4, normalizedColor, rgbColorString);
		return {
			red: parseInt("" + hslRgbMatched[1], 10),
			green: parseInt("" + hslRgbMatched[2], 10),
			blue: parseInt("" + hslRgbMatched[3], 10)
		};
	}
	var hslaMatched = hslaRegex.exec(normalizedColor.substring(0, 50));
	if (hslaMatched) {
		var _rgbColorString = "rgb(" + hslToRgb(parseInt("" + hslaMatched[1], 10), parseInt("" + hslaMatched[2], 10) / 100, parseInt("" + hslaMatched[3], 10) / 100) + ")";
		var _hslRgbMatched = rgbRegex.exec(_rgbColorString);
		if (!_hslRgbMatched) throw new PolishedError(4, normalizedColor, _rgbColorString);
		return {
			red: parseInt("" + _hslRgbMatched[1], 10),
			green: parseInt("" + _hslRgbMatched[2], 10),
			blue: parseInt("" + _hslRgbMatched[3], 10),
			alpha: parseFloat("" + hslaMatched[4]) > 1 ? parseFloat("" + hslaMatched[4]) / 100 : parseFloat("" + hslaMatched[4])
		};
	}
	throw new PolishedError(5);
}
function rgbToHsl(color) {
	var red = color.red / 255;
	var green = color.green / 255;
	var blue = color.blue / 255;
	var max = Math.max(red, green, blue);
	var min = Math.min(red, green, blue);
	var lightness = (max + min) / 2;
	if (max === min) {
		if (color.alpha !== void 0) return {
			hue: 0,
			saturation: 0,
			lightness,
			alpha: color.alpha
		};
		else return {
			hue: 0,
			saturation: 0,
			lightness
		};
	}
	var hue;
	var delta = max - min;
	var saturation = lightness > .5 ? delta / (2 - max - min) : delta / (max + min);
	switch (max) {
		case red:
			hue = (green - blue) / delta + (green < blue ? 6 : 0);
			break;
		case green:
			hue = (blue - red) / delta + 2;
			break;
		default: hue = (red - green) / delta + 4;
	}
	hue *= 60;
	if (color.alpha !== void 0) return {
		hue,
		saturation,
		lightness,
		alpha: color.alpha
	};
	return {
		hue,
		saturation,
		lightness
	};
}
/**
* Returns an HslColor or HslaColor object. This utility function is only useful
* if want to extract a color component. With the color util `toColorString` you
* can convert a HslColor or HslaColor object back to a string.
*
* @example
* // Assigns `{ hue: 0, saturation: 1, lightness: 0.5 }` to color1
* const color1 = parseToHsl('rgb(255, 0, 0)');
* // Assigns `{ hue: 128, saturation: 1, lightness: 0.5, alpha: 0.75 }` to color2
* const color2 = parseToHsl('hsla(128, 100%, 50%, 0.75)');
*/
function parseToHsl(color) {
	return rgbToHsl(parseToRgb(color));
}
var reduceHexValue$1 = function reduceHexValue(value) {
	if (value.length === 7 && value[1] === value[2] && value[3] === value[4] && value[5] === value[6]) return "#" + value[1] + value[3] + value[5];
	return value;
};
function numberToHex(value) {
	var hex = value.toString(16);
	return hex.length === 1 ? "0" + hex : hex;
}
function colorToHex(color) {
	return numberToHex(Math.round(color * 255));
}
function convertToHex(red, green, blue) {
	return reduceHexValue$1("#" + colorToHex(red) + colorToHex(green) + colorToHex(blue));
}
function hslToHex(hue, saturation, lightness) {
	return hslToRgb(hue, saturation, lightness, convertToHex);
}
/**
* Returns a string value for the color. The returned result is the smallest possible hex notation.
*
* @example
* // Styles as object usage
* const styles = {
*   background: hsl(359, 0.75, 0.4),
*   background: hsl({ hue: 360, saturation: 0.75, lightness: 0.4 }),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${hsl(359, 0.75, 0.4)};
*   background: ${hsl({ hue: 360, saturation: 0.75, lightness: 0.4 })};
* `
*
* // CSS in JS Output
*
* element {
*   background: "#b3191c";
*   background: "#b3191c";
* }
*/
function hsl(value, saturation, lightness) {
	if (typeof value === "number" && typeof saturation === "number" && typeof lightness === "number") return hslToHex(value, saturation, lightness);
	else if (typeof value === "object" && saturation === void 0 && lightness === void 0) return hslToHex(value.hue, value.saturation, value.lightness);
	throw new PolishedError(1);
}
/**
* Returns a string value for the color. The returned result is the smallest possible rgba or hex notation.
*
* @example
* // Styles as object usage
* const styles = {
*   background: hsla(359, 0.75, 0.4, 0.7),
*   background: hsla({ hue: 360, saturation: 0.75, lightness: 0.4, alpha: 0,7 }),
*   background: hsla(359, 0.75, 0.4, 1),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${hsla(359, 0.75, 0.4, 0.7)};
*   background: ${hsla({ hue: 360, saturation: 0.75, lightness: 0.4, alpha: 0,7 })};
*   background: ${hsla(359, 0.75, 0.4, 1)};
* `
*
* // CSS in JS Output
*
* element {
*   background: "rgba(179,25,28,0.7)";
*   background: "rgba(179,25,28,0.7)";
*   background: "#b3191c";
* }
*/
function hsla(value, saturation, lightness, alpha) {
	if (typeof value === "number" && typeof saturation === "number" && typeof lightness === "number" && typeof alpha === "number") return alpha >= 1 ? hslToHex(value, saturation, lightness) : "rgba(" + hslToRgb(value, saturation, lightness) + "," + alpha + ")";
	else if (typeof value === "object" && saturation === void 0 && lightness === void 0 && alpha === void 0) return value.alpha >= 1 ? hslToHex(value.hue, value.saturation, value.lightness) : "rgba(" + hslToRgb(value.hue, value.saturation, value.lightness) + "," + value.alpha + ")";
	throw new PolishedError(2);
}
/**
* Returns a string value for the color. The returned result is the smallest possible hex notation.
*
* @example
* // Styles as object usage
* const styles = {
*   background: rgb(255, 205, 100),
*   background: rgb({ red: 255, green: 205, blue: 100 }),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${rgb(255, 205, 100)};
*   background: ${rgb({ red: 255, green: 205, blue: 100 })};
* `
*
* // CSS in JS Output
*
* element {
*   background: "#ffcd64";
*   background: "#ffcd64";
* }
*/
function rgb(value, green, blue) {
	if (typeof value === "number" && typeof green === "number" && typeof blue === "number") return reduceHexValue$1("#" + numberToHex(value) + numberToHex(green) + numberToHex(blue));
	else if (typeof value === "object" && green === void 0 && blue === void 0) return reduceHexValue$1("#" + numberToHex(value.red) + numberToHex(value.green) + numberToHex(value.blue));
	throw new PolishedError(6);
}
/**
* Returns a string value for the color. The returned result is the smallest possible rgba or hex notation.
*
* Can also be used to fade a color by passing a hex value or named CSS color along with an alpha value.
*
* @example
* // Styles as object usage
* const styles = {
*   background: rgba(255, 205, 100, 0.7),
*   background: rgba({ red: 255, green: 205, blue: 100, alpha: 0.7 }),
*   background: rgba(255, 205, 100, 1),
*   background: rgba('#ffffff', 0.4),
*   background: rgba('black', 0.7),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${rgba(255, 205, 100, 0.7)};
*   background: ${rgba({ red: 255, green: 205, blue: 100, alpha: 0.7 })};
*   background: ${rgba(255, 205, 100, 1)};
*   background: ${rgba('#ffffff', 0.4)};
*   background: ${rgba('black', 0.7)};
* `
*
* // CSS in JS Output
*
* element {
*   background: "rgba(255,205,100,0.7)";
*   background: "rgba(255,205,100,0.7)";
*   background: "#ffcd64";
*   background: "rgba(255,255,255,0.4)";
*   background: "rgba(0,0,0,0.7)";
* }
*/
function rgba(firstValue, secondValue, thirdValue, fourthValue) {
	if (typeof firstValue === "string" && typeof secondValue === "number") {
		var rgbValue = parseToRgb(firstValue);
		return "rgba(" + rgbValue.red + "," + rgbValue.green + "," + rgbValue.blue + "," + secondValue + ")";
	} else if (typeof firstValue === "number" && typeof secondValue === "number" && typeof thirdValue === "number" && typeof fourthValue === "number") return fourthValue >= 1 ? rgb(firstValue, secondValue, thirdValue) : "rgba(" + firstValue + "," + secondValue + "," + thirdValue + "," + fourthValue + ")";
	else if (typeof firstValue === "object" && secondValue === void 0 && thirdValue === void 0 && fourthValue === void 0) return firstValue.alpha >= 1 ? rgb(firstValue.red, firstValue.green, firstValue.blue) : "rgba(" + firstValue.red + "," + firstValue.green + "," + firstValue.blue + "," + firstValue.alpha + ")";
	throw new PolishedError(7);
}
var isRgb = function isRgb(color) {
	return typeof color.red === "number" && typeof color.green === "number" && typeof color.blue === "number" && (typeof color.alpha !== "number" || typeof color.alpha === "undefined");
};
var isRgba = function isRgba(color) {
	return typeof color.red === "number" && typeof color.green === "number" && typeof color.blue === "number" && typeof color.alpha === "number";
};
var isHsl = function isHsl(color) {
	return typeof color.hue === "number" && typeof color.saturation === "number" && typeof color.lightness === "number" && (typeof color.alpha !== "number" || typeof color.alpha === "undefined");
};
var isHsla = function isHsla(color) {
	return typeof color.hue === "number" && typeof color.saturation === "number" && typeof color.lightness === "number" && typeof color.alpha === "number";
};
/**
* Converts a RgbColor, RgbaColor, HslColor or HslaColor object to a color string.
* This util is useful in case you only know on runtime which color object is
* used. Otherwise we recommend to rely on `rgb`, `rgba`, `hsl` or `hsla`.
*
* @example
* // Styles as object usage
* const styles = {
*   background: toColorString({ red: 255, green: 205, blue: 100 }),
*   background: toColorString({ red: 255, green: 205, blue: 100, alpha: 0.72 }),
*   background: toColorString({ hue: 240, saturation: 1, lightness: 0.5 }),
*   background: toColorString({ hue: 360, saturation: 0.75, lightness: 0.4, alpha: 0.72 }),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${toColorString({ red: 255, green: 205, blue: 100 })};
*   background: ${toColorString({ red: 255, green: 205, blue: 100, alpha: 0.72 })};
*   background: ${toColorString({ hue: 240, saturation: 1, lightness: 0.5 })};
*   background: ${toColorString({ hue: 360, saturation: 0.75, lightness: 0.4, alpha: 0.72 })};
* `
*
* // CSS in JS Output
* element {
*   background: "#ffcd64";
*   background: "rgba(255,205,100,0.72)";
*   background: "#00f";
*   background: "rgba(179,25,25,0.72)";
* }
*/
function toColorString(color) {
	if (typeof color !== "object") throw new PolishedError(8);
	if (isRgba(color)) return rgba(color);
	if (isRgb(color)) return rgb(color);
	if (isHsla(color)) return hsla(color);
	if (isHsl(color)) return hsl(color);
	throw new PolishedError(8);
}
function curried(f, length, acc) {
	return function fn() {
		var combined = acc.concat(Array.prototype.slice.call(arguments));
		return combined.length >= length ? f.apply(this, combined) : curried(f, length, combined);
	};
}
function curry(f) {
	return curried(f, f.length, []);
}
/**
* Changes the hue of the color. Hue is a number between 0 to 360. The first
* argument for adjustHue is the amount of degrees the color is rotated around
* the color wheel, always producing a positive hue value.
*
* @example
* // Styles as object usage
* const styles = {
*   background: adjustHue(180, '#448'),
*   background: adjustHue('180', 'rgba(101,100,205,0.7)'),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${adjustHue(180, '#448')};
*   background: ${adjustHue('180', 'rgba(101,100,205,0.7)')};
* `
*
* // CSS in JS Output
* element {
*   background: "#888844";
*   background: "rgba(136,136,68,0.7)";
* }
*/
function adjustHue(degree, color) {
	if (color === "transparent") return color;
	var hslColor = parseToHsl(color);
	return toColorString(_extends({}, hslColor, { hue: hslColor.hue + parseFloat(degree) }));
}
curry(adjustHue);
function guard(lowerBoundary, upperBoundary, value) {
	return Math.max(lowerBoundary, Math.min(upperBoundary, value));
}
/**
* Returns a string value for the darkened color.
*
* @example
* // Styles as object usage
* const styles = {
*   background: darken(0.2, '#FFCD64'),
*   background: darken('0.2', 'rgba(255,205,100,0.7)'),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${darken(0.2, '#FFCD64')};
*   background: ${darken('0.2', 'rgba(255,205,100,0.7)')};
* `
*
* // CSS in JS Output
*
* element {
*   background: "#ffbd31";
*   background: "rgba(255,189,49,0.7)";
* }
*/
function darken(amount, color) {
	if (color === "transparent") return color;
	var hslColor = parseToHsl(color);
	return toColorString(_extends({}, hslColor, { lightness: guard(0, 1, hslColor.lightness - parseFloat(amount)) }));
}
curry(darken);
/**
* Decreases the intensity of a color. Its range is between 0 to 1. The first
* argument of the desaturate function is the amount by how much the color
* intensity should be decreased.
*
* @example
* // Styles as object usage
* const styles = {
*   background: desaturate(0.2, '#CCCD64'),
*   background: desaturate('0.2', 'rgba(204,205,100,0.7)'),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${desaturate(0.2, '#CCCD64')};
*   background: ${desaturate('0.2', 'rgba(204,205,100,0.7)')};
* `
*
* // CSS in JS Output
* element {
*   background: "#b8b979";
*   background: "rgba(184,185,121,0.7)";
* }
*/
function desaturate(amount, color) {
	if (color === "transparent") return color;
	var hslColor = parseToHsl(color);
	return toColorString(_extends({}, hslColor, { saturation: guard(0, 1, hslColor.saturation - parseFloat(amount)) }));
}
curry(desaturate);
/**
* Returns a number (float) representing the luminance of a color.
*
* @example
* // Styles as object usage
* const styles = {
*   background: getLuminance('#CCCD64') >= getLuminance('#0000ff') ? '#CCCD64' : '#0000ff',
*   background: getLuminance('rgba(58, 133, 255, 1)') >= getLuminance('rgba(255, 57, 149, 1)') ?
*                             'rgba(58, 133, 255, 1)' :
*                             'rgba(255, 57, 149, 1)',
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${getLuminance('#CCCD64') >= getLuminance('#0000ff') ? '#CCCD64' : '#0000ff'};
*   background: ${getLuminance('rgba(58, 133, 255, 1)') >= getLuminance('rgba(255, 57, 149, 1)') ?
*                             'rgba(58, 133, 255, 1)' :
*                             'rgba(255, 57, 149, 1)'};
*
* // CSS in JS Output
*
* div {
*   background: "#CCCD64";
*   background: "rgba(58, 133, 255, 1)";
* }
*/
function getLuminance(color) {
	if (color === "transparent") return 0;
	var rgbColor = parseToRgb(color);
	var _Object$keys$map = Object.keys(rgbColor).map(function(key) {
		var channel = rgbColor[key] / 255;
		return channel <= .03928 ? channel / 12.92 : Math.pow((channel + .055) / 1.055, 2.4);
	}), r = _Object$keys$map[0], g = _Object$keys$map[1], b = _Object$keys$map[2];
	return parseFloat((.2126 * r + .7152 * g + .0722 * b).toFixed(3));
}
/**
* Returns a string value for the lightened color.
*
* @example
* // Styles as object usage
* const styles = {
*   background: lighten(0.2, '#CCCD64'),
*   background: lighten('0.2', 'rgba(204,205,100,0.7)'),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${lighten(0.2, '#FFCD64')};
*   background: ${lighten('0.2', 'rgba(204,205,100,0.7)')};
* `
*
* // CSS in JS Output
*
* element {
*   background: "#e5e6b1";
*   background: "rgba(229,230,177,0.7)";
* }
*/
function lighten(amount, color) {
	if (color === "transparent") return color;
	var hslColor = parseToHsl(color);
	return toColorString(_extends({}, hslColor, { lightness: guard(0, 1, hslColor.lightness + parseFloat(amount)) }));
}
curry(lighten);
/**
* Mixes the two provided colors together by calculating the average of each of the RGB components weighted to the first color by the provided weight.
*
* @example
* // Styles as object usage
* const styles = {
*   background: mix(0.5, '#f00', '#00f')
*   background: mix(0.25, '#f00', '#00f')
*   background: mix('0.5', 'rgba(255, 0, 0, 0.5)', '#00f')
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${mix(0.5, '#f00', '#00f')};
*   background: ${mix(0.25, '#f00', '#00f')};
*   background: ${mix('0.5', 'rgba(255, 0, 0, 0.5)', '#00f')};
* `
*
* // CSS in JS Output
*
* element {
*   background: "#7f007f";
*   background: "#3f00bf";
*   background: "rgba(63, 0, 191, 0.75)";
* }
*/
function mix(weight, color, otherColor) {
	if (color === "transparent") return otherColor;
	if (otherColor === "transparent") return color;
	if (weight === 0) return otherColor;
	var parsedColor1 = parseToRgb(color);
	var color1 = _extends({}, parsedColor1, { alpha: typeof parsedColor1.alpha === "number" ? parsedColor1.alpha : 1 });
	var parsedColor2 = parseToRgb(otherColor);
	var color2 = _extends({}, parsedColor2, { alpha: typeof parsedColor2.alpha === "number" ? parsedColor2.alpha : 1 });
	var alphaDelta = color1.alpha - color2.alpha;
	var x = parseFloat(weight) * 2 - 1;
	var weight1 = ((x * alphaDelta === -1 ? x : x + alphaDelta) / (1 + x * alphaDelta) + 1) / 2;
	var weight2 = 1 - weight1;
	return rgba({
		red: Math.floor(color1.red * weight1 + color2.red * weight2),
		green: Math.floor(color1.green * weight1 + color2.green * weight2),
		blue: Math.floor(color1.blue * weight1 + color2.blue * weight2),
		alpha: color1.alpha * parseFloat(weight) + color2.alpha * (1 - parseFloat(weight))
	});
}
var mix$1 = curry(mix);
/**
* Increases the opacity of a color. Its range for the amount is between 0 to 1.
*
*
* @example
* // Styles as object usage
* const styles = {
*   background: opacify(0.1, 'rgba(255, 255, 255, 0.9)');
*   background: opacify(0.2, 'hsla(0, 0%, 100%, 0.5)'),
*   background: opacify('0.5', 'rgba(255, 0, 0, 0.2)'),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${opacify(0.1, 'rgba(255, 255, 255, 0.9)')};
*   background: ${opacify(0.2, 'hsla(0, 0%, 100%, 0.5)')},
*   background: ${opacify('0.5', 'rgba(255, 0, 0, 0.2)')},
* `
*
* // CSS in JS Output
*
* element {
*   background: "#fff";
*   background: "rgba(255,255,255,0.7)";
*   background: "rgba(255,0,0,0.7)";
* }
*/
function opacify(amount, color) {
	if (color === "transparent") return color;
	var parsedColor = parseToRgb(color);
	var alpha = typeof parsedColor.alpha === "number" ? parsedColor.alpha : 1;
	return rgba(_extends({}, parsedColor, { alpha: guard(0, 1, (alpha * 100 + parseFloat(amount) * 100) / 100) }));
}
curry(opacify);
/**
* Increases the intensity of a color. Its range is between 0 to 1. The first
* argument of the saturate function is the amount by how much the color
* intensity should be increased.
*
* @example
* // Styles as object usage
* const styles = {
*   background: saturate(0.2, '#CCCD64'),
*   background: saturate('0.2', 'rgba(204,205,100,0.7)'),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${saturate(0.2, '#FFCD64')};
*   background: ${saturate('0.2', 'rgba(204,205,100,0.7)')};
* `
*
* // CSS in JS Output
*
* element {
*   background: "#e0e250";
*   background: "rgba(224,226,80,0.7)";
* }
*/
function saturate(amount, color) {
	if (color === "transparent") return color;
	var hslColor = parseToHsl(color);
	return toColorString(_extends({}, hslColor, { saturation: guard(0, 1, hslColor.saturation + parseFloat(amount)) }));
}
curry(saturate);
/**
* Sets the hue of a color to the provided value. The hue range can be
* from 0 and 359.
*
* @example
* // Styles as object usage
* const styles = {
*   background: setHue(42, '#CCCD64'),
*   background: setHue('244', 'rgba(204,205,100,0.7)'),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${setHue(42, '#CCCD64')};
*   background: ${setHue('244', 'rgba(204,205,100,0.7)')};
* `
*
* // CSS in JS Output
* element {
*   background: "#cdae64";
*   background: "rgba(107,100,205,0.7)";
* }
*/
function setHue(hue, color) {
	if (color === "transparent") return color;
	return toColorString(_extends({}, parseToHsl(color), { hue: parseFloat(hue) }));
}
curry(setHue);
/**
* Sets the lightness of a color to the provided value. The lightness range can be
* from 0 and 1.
*
* @example
* // Styles as object usage
* const styles = {
*   background: setLightness(0.2, '#CCCD64'),
*   background: setLightness('0.75', 'rgba(204,205,100,0.7)'),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${setLightness(0.2, '#CCCD64')};
*   background: ${setLightness('0.75', 'rgba(204,205,100,0.7)')};
* `
*
* // CSS in JS Output
* element {
*   background: "#4d4d19";
*   background: "rgba(223,224,159,0.7)";
* }
*/
function setLightness(lightness, color) {
	if (color === "transparent") return color;
	return toColorString(_extends({}, parseToHsl(color), { lightness: parseFloat(lightness) }));
}
curry(setLightness);
/**
* Sets the saturation of a color to the provided value. The saturation range can be
* from 0 and 1.
*
* @example
* // Styles as object usage
* const styles = {
*   background: setSaturation(0.2, '#CCCD64'),
*   background: setSaturation('0.75', 'rgba(204,205,100,0.7)'),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${setSaturation(0.2, '#CCCD64')};
*   background: ${setSaturation('0.75', 'rgba(204,205,100,0.7)')};
* `
*
* // CSS in JS Output
* element {
*   background: "#adad84";
*   background: "rgba(228,229,76,0.7)";
* }
*/
function setSaturation(saturation, color) {
	if (color === "transparent") return color;
	return toColorString(_extends({}, parseToHsl(color), { saturation: parseFloat(saturation) }));
}
curry(setSaturation);
/**
* Shades a color by mixing it with black. `shade` can produce
* hue shifts, where as `darken` manipulates the luminance channel and therefore
* doesn't produce hue shifts.
*
* @example
* // Styles as object usage
* const styles = {
*   background: shade(0.25, '#00f')
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${shade(0.25, '#00f')};
* `
*
* // CSS in JS Output
*
* element {
*   background: "#00003f";
* }
*/
function shade(percentage, color) {
	if (color === "transparent") return color;
	return mix$1(parseFloat(percentage), "rgb(0, 0, 0)", color);
}
curry(shade);
/**
* Tints a color by mixing it with white. `tint` can produce
* hue shifts, where as `lighten` manipulates the luminance channel and therefore
* doesn't produce hue shifts.
*
* @example
* // Styles as object usage
* const styles = {
*   background: tint(0.25, '#00f')
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${tint(0.25, '#00f')};
* `
*
* // CSS in JS Output
*
* element {
*   background: "#bfbfff";
* }
*/
function tint(percentage, color) {
	if (color === "transparent") return color;
	return mix$1(parseFloat(percentage), "rgb(255, 255, 255)", color);
}
curry(tint);
/**
* Decreases the opacity of a color. Its range for the amount is between 0 to 1.
*
*
* @example
* // Styles as object usage
* const styles = {
*   background: transparentize(0.1, '#fff'),
*   background: transparentize(0.2, 'hsl(0, 0%, 100%)'),
*   background: transparentize('0.5', 'rgba(255, 0, 0, 0.8)'),
* }
*
* // styled-components usage
* const div = styled.div`
*   background: ${transparentize(0.1, '#fff')};
*   background: ${transparentize(0.2, 'hsl(0, 0%, 100%)')};
*   background: ${transparentize('0.5', 'rgba(255, 0, 0, 0.8)')};
* `
*
* // CSS in JS Output
*
* element {
*   background: "rgba(255,255,255,0.9)";
*   background: "rgba(255,255,255,0.8)";
*   background: "rgba(255,0,0,0.3)";
* }
*/
function transparentize(amount, color) {
	if (color === "transparent") return color;
	var parsedColor = parseToRgb(color);
	var alpha = typeof parsedColor.alpha === "number" ? parsedColor.alpha : 1;
	return rgba(_extends({}, parsedColor, { alpha: guard(0, 1, +(alpha * 100 - parseFloat(amount) * 100).toFixed(2) / 100) }));
}
curry(transparentize);
//#endregion
//#region node_modules/sanity/lib/index.js
/**
* This is used to draw the bar that wraps the diff components in the changes panel
*
* @internal
*/
var ChangeFieldWrapper = (props) => {
	let $ = (0, import_compiler_runtime.c)(20), { path, hasRevertHover } = props, { onSetFocus } = (0, import_react.useContext)(ReviewChangesContext), zIndex = useZIndex(), [hasHover, setHover] = (0, import_react.useState)(!1), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = () => {
		setHover(!0);
	}, $[0] = t0) : t0 = $[0];
	let onMouseEnter = t0, t1;
	$[1] === Symbol.for("react.memo_cache_sentinel") ? (t1 = () => {
		setHover(!1);
	}, $[1] = t1) : t1 = $[1];
	let onMouseLeave = t1, [element, setElement] = (0, import_react.useState)(null), t2;
	$[2] !== element || $[3] !== path ? (t2 = element ? `change-${toString(path)}` : null, $[2] = element, $[3] = path, $[4] = t2) : t2 = $[4];
	let reporterId = t2, t3;
	$[5] !== element || $[6] !== hasHover || $[7] !== hasRevertHover || $[8] !== path || $[9] !== zIndex.popover ? (t3 = () => ({
		element,
		path,
		isChanged: !0,
		hasFocus: !1,
		hasHover,
		zIndex: Array.isArray(zIndex.popover) ? zIndex.popover[0] : zIndex.popover,
		hasRevertHover
	}), $[5] = element, $[6] = hasHover, $[7] = hasRevertHover, $[8] = path, $[9] = zIndex.popover, $[10] = t3) : t3 = $[10], useChangeIndicatorsReporter(reporterId, t3, import_react_fast_compare.default);
	let t4;
	$[11] !== onSetFocus || $[12] !== path ? (t4 = (event) => {
		setFocusWithStopPropagation(event, onSetFocus, path);
	}, $[11] = onSetFocus, $[12] = path, $[13] = t4) : t4 = $[13];
	let handleClick = t4, t5;
	$[14] === path ? t5 = $[15] : (t5 = !1, $[14] = path, $[15] = t5);
	let t6;
	return $[16] !== handleClick || $[17] !== props.children || $[18] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		ref: setElement,
		onClick: handleClick,
		onMouseLeave,
		onMouseEnter,
		children: [t5, props.children]
	}), $[16] = handleClick, $[17] = props.children, $[18] = t5, $[19] = t6) : t6 = $[19], t6;
};
function setFocusWithStopPropagation(event, onSetFocus, path) {
	event.stopPropagation(), onSetFocus(path);
}
/**
* @internal
*/
function useReviewChanges() {
	let $ = (0, import_compiler_runtime.c)(2), context = (0, import_react.useContext)(ReviewChangesContext), t0;
	return $[0] === context ? t0 = $[1] : (t0 = {
		...context,
		changesOpen: context.isReviewChangesOpen
	}, $[0] = context, $[1] = t0), t0;
}
function findMostSpecificTarget(targetType, id, values) {
	let pathString = id.slice(id.indexOf("-") + 1) || "[]", path = fromString(pathString), exactId = `${targetType}-${toString(path)}`;
	if (values.has(exactId)) return values.get(exactId);
	if (path.length > 0) for (let i = path.length - 1; i >= 0; i--) {
		let lessSpecificPath = path.slice(0, i), lessSpecificExactId = `${targetType}-${toString(lessSpecificPath)}`;
		if (values.has(lessSpecificExactId)) return values.get(lessSpecificExactId);
	}
	let closestDescendant;
	for (let [targetId, target] of values) !("path" in target) || !targetId.startsWith(targetType) || target.isChanged && target.path.length > path.length && numEqualSegments(path, target.path) === path.length && (!closestDescendant || target.path.length < closestDescendant.path.length) && (closestDescendant = target);
	if (closestDescendant) return closestDescendant;
	let mostSpecific;
	for (let [targetId, target] of values) {
		if (!("path" in target) || !targetId.startsWith(targetType)) continue;
		let numEqual = numEqualSegments(path, target.path), lastPathSegment = target.path[target.path.length - 1], pathOnlyDiffersByKey = numEqual === target.path.length - 1 && isKeyedObject(lastPathSegment);
		if (numEqual !== 0 && !(numEqual !== target.path.length && !pathOnlyDiffersByKey) && (mostSpecific = target, numEqual === path.length)) break;
	}
	return mostSpecific;
}
function hasOverflowScroll(el) {
	let overflow = getComputedStyle(el).overflow;
	return overflow.includes("auto") || overflow.includes("scroll");
}
function isScrollable(el) {
	return (el.scrollHeight !== el.offsetHeight || el.scrollWidth !== el.offsetWidth) && hasOverflowScroll(el);
}
var getOffsetsTo = (source, target) => {
	let bounds = {
		top: 0,
		left: 0,
		height: target.offsetHeight,
		width: target.offsetWidth
	}, rect = {
		top: 0,
		left: 0,
		height: source.offsetHeight,
		width: source.offsetWidth
	}, foundScrollContainer = !1, el = source;
	for (; el && el !== target && target.contains(el);) foundScrollContainer && (bounds.top += el.offsetTop, bounds.left += el.offsetLeft), hasOverflowScroll(el) && (bounds.top = el.offsetTop, bounds.height = el.offsetHeight, bounds.left = el.offsetLeft, bounds.width = el.offsetWidth, foundScrollContainer = !0), rect.top += el.offsetTop - el.scrollTop, rect.left += el.offsetLeft - el.scrollLeft, el = el.offsetParent;
	return {
		rect,
		bounds
	};
};
function isChangeBar(v) {
	return v[0] !== "changePanel";
}
function scrollIntoView(field) {
	let element = field.element;
	if (!element) return;
	let parentElementWithScroll = element;
	for (; !isScrollable(parentElementWithScroll);) if (parentElementWithScroll = parentElementWithScroll.parentElement, !parentElementWithScroll) return;
	parentElementWithScroll.scroll({
		top: parentElementWithScroll.scrollTop + field.rect.top - field.bounds.top + -15,
		left: 0,
		behavior: "smooth"
	});
}
function ClampedRect(props) {
	let $ = (0, import_compiler_runtime.c)(9), bounds, rest;
	$[0] === props ? (bounds = $[1], rest = $[2]) : ({bounds, ...rest} = props, $[0] = props, $[1] = bounds, $[2] = rest);
	let x = Math.max(bounds.left, props.left), y = Math.max(props.top, bounds.top), height = Math.max(0, props.height - (y - props.top)), width = Math.max(0, props.width - (x - props.left)), t0;
	return $[3] !== height || $[4] !== rest || $[5] !== width || $[6] !== x || $[7] !== y ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", {
		...rest,
		x,
		y,
		height,
		width
	}), $[3] = height, $[4] = rest, $[5] = width, $[6] = x, $[7] = y, $[8] = t0) : t0 = $[8], t0;
}
var connectorPath = "w22oku1";
var interactivePath = "w22oku2";
var rightBarWrapper = "w22oku3";
function ConnectorPath(props) {
	let $ = (0, import_compiler_runtime.c)(8), className, restProps;
	$[0] === props ? (className = $[1], restProps = $[2]) : ({className, ...restProps} = props, $[0] = props, $[1] = className, $[2] = restProps);
	let t0;
	$[3] === className ? t0 = $[4] : (t0 = clsx(connectorPath, className), $[3] = className, $[4] = t0);
	let t1;
	return $[5] !== restProps || $[6] !== t0 ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
		...restProps,
		className: t0
	}), $[5] = restProps, $[6] = t0, $[7] = t1) : t1 = $[7], t1;
}
function InteractivePath(props) {
	let $ = (0, import_compiler_runtime.c)(8), className, restProps;
	$[0] === props ? (className = $[1], restProps = $[2]) : ({className, ...restProps} = props, $[0] = props, $[1] = className, $[2] = restProps);
	let t0;
	$[3] === className ? t0 = $[4] : (t0 = clsx(interactivePath, className), $[3] = className, $[4] = t0);
	let t1;
	return $[5] !== restProps || $[6] !== t0 ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
		...restProps,
		className: t0
	}), $[5] = restProps, $[6] = t0, $[7] = t1) : t1 = $[7], t1;
}
function RightBarWrapper(props) {
	let $ = (0, import_compiler_runtime.c)(8), className, restProps;
	$[0] === props ? (className = $[1], restProps = $[2]) : ({className, ...restProps} = props, $[0] = props, $[1] = className, $[2] = restProps);
	let t0;
	$[3] === className ? t0 = $[4] : (t0 = clsx(rightBarWrapper, className), $[3] = className, $[4] = t0);
	let t1;
	return $[5] !== restProps || $[6] !== t0 ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ClampedRect, {
		...restProps,
		className: t0
	}), $[5] = restProps, $[6] = t0, $[7] = t1) : t1 = $[7], t1;
}
function arrowPath(x, y, dir) {
	return [
		`M ${x - 4} ${y - 4 * dir} `,
		`L ${x} ${y}`,
		`L ${x + 4} ${y - 4 * dir}`
	].join("");
}
function moveTo(x, y) {
	return `M${x} ${y}`;
}
function lineTo(x, y) {
	return `L${x} ${y}`;
}
function join(strings, delim = "") {
	return strings.join(delim);
}
function quadCurve(x1, y1, x, y) {
	return `Q${x1} ${y1} ${x} ${y}`;
}
function generateConnectorPath(line) {
	let { from, to } = line, { left: fromX, top: fromY } = from, { left: toX, top: toY } = to, cmds = [], r1 = Math.min(4, Math.abs(fromY - toY) / 2);
	return from.isAbove ? cmds.push(moveTo(fromX + 8, fromY - 12 + 2), lineTo(fromX + 8, fromY - 4), quadCurve(fromX + 8, fromY, fromX + 8 + 4, fromY)) : from.isBelow ? cmds.push(moveTo(fromX + 8, fromY + 12 - 2), lineTo(fromX + 8, fromY + 4), quadCurve(fromX + 8, fromY, fromX + 8 + 4, fromY)) : cmds.push(moveTo(fromX, fromY)), to.isAbove ? fromY < to.bounds.top ? cmds.push(lineTo(to.bounds.left - 8 - r1, fromY), quadCurve(to.bounds.left - 8, fromY, to.bounds.left - 8, fromY + r1), lineTo(to.bounds.left - 8, toY - r1), quadCurve(to.bounds.left - 8, toY, to.bounds.left - 8 + r1, toY), lineTo(to.bounds.left + 8 - 4, toY), quadCurve(to.bounds.left + 8, toY, to.bounds.left + 8, toY - 4), lineTo(to.bounds.left + 8, toY - 12 + 2)) : cmds.push(lineTo(to.bounds.left + 8 - 4, fromY), quadCurve(to.bounds.left + 8, fromY, to.bounds.left + 8, fromY - 4), lineTo(to.bounds.left + 8, toY - 12 + 2)) : to.isBelow ? fromY > to.bounds.top + to.bounds.height ? cmds.push(lineTo(to.bounds.left - 8 - r1, fromY), quadCurve(to.bounds.left - 8, fromY, to.bounds.left - 8, fromY - r1), lineTo(to.bounds.left - 8, toY + r1), quadCurve(to.bounds.left - 8, toY, to.bounds.left - 8 + r1, toY), lineTo(to.bounds.left + 8 - 4, toY), quadCurve(to.bounds.left + 8, toY, to.bounds.left + 8, toY + 4), lineTo(to.bounds.left + 8, toY + 12 - 2)) : cmds.push(lineTo(to.bounds.left + 8 - 4, fromY), quadCurve(to.bounds.left + 8, fromY, to.bounds.left + 8, fromY + 4), lineTo(to.bounds.left + 8, toY + 12 - 2)) : fromY < toY ? cmds.push(lineTo(to.bounds.left + 8 - r1, fromY), quadCurve(to.bounds.left + 8, fromY, to.bounds.left + 8, fromY + r1), lineTo(to.bounds.left + 8, toY - r1), quadCurve(to.bounds.left + 8, toY, to.bounds.left + 8 + r1, toY), lineTo(toX, toY)) : cmds.push(lineTo(to.bounds.left + 8 - r1, fromY), quadCurve(to.bounds.left + 8, fromY, to.bounds.left + 8, fromY - r1), lineTo(to.bounds.left + 8, toY + r1), quadCurve(to.bounds.left + 8, toY, to.bounds.left + 8 + r1, toY), lineTo(toX, toY)), join(cmds);
}
function getConnectorLinePoint(rect, bounds) {
	let centerY = rect.top + rect.height / 2, isAbove = rect.top + rect.height < bounds.top + 2, isBelow = rect.top > bounds.top + bounds.height - 2;
	return {
		bounds,
		left: rect.left,
		top: centerY,
		centerY,
		startY: rect.top + 8,
		endY: rect.top + rect.height - 8,
		isAbove,
		isBelow,
		outOfBounds: isAbove || isBelow
	};
}
function mapConnectorToLine(connector) {
	let fromBounds = {
		top: connector.from.bounds.top + 12,
		bottom: connector.from.bounds.top + connector.from.bounds.height - 12,
		left: connector.from.bounds.left,
		right: connector.from.bounds.left + connector.from.bounds.width,
		width: connector.from.bounds.width,
		height: connector.from.bounds.height - 24
	}, from = getConnectorLinePoint(connector.from.rect, fromBounds);
	from.left = connector.from.rect.left + connector.from.rect.width + 1;
	let toBounds = {
		top: connector.to.bounds.top + 12,
		bottom: connector.to.bounds.top + connector.to.bounds.height - 12,
		left: connector.to.bounds.left,
		right: connector.to.bounds.left + connector.to.bounds.width,
		width: connector.to.bounds.width,
		height: connector.to.bounds.height - 24
	}, to = getConnectorLinePoint(connector.to.rect, toBounds), maxStartY = Math.max(to.startY, from.startY);
	return from.top = Math.min(maxStartY, from.endY), from.top < toBounds.top ? from.top = Math.min(toBounds.top, from.endY) : from.top > toBounds.bottom && (from.top = Math.max(toBounds.bottom, from.startY)), to.top = Math.min(maxStartY, to.endY), to.top < fromBounds.top ? to.top = Math.min(fromBounds.top, to.endY) : to.top > fromBounds.bottom && (to.top = Math.max(fromBounds.bottom, to.startY)), from.top = Math.min(Math.max(from.top, fromBounds.top), fromBounds.bottom), to.top = Math.min(Math.max(to.top, toBounds.top), toBounds.bottom), {
		from,
		to
	};
}
var Connector = (0, import_react.memo)(function Connector(t0) {
	let $ = (0, import_compiler_runtime.c)(41), { from, to } = t0, line, t1, t2;
	if ($[0] !== from || $[1] !== to) {
		t2 = Symbol.for("react.early_return_sentinel");
		bb0: {
			if (line = mapConnectorToLine({
				from,
				to
			}), line.from.outOfBounds && line.to.outOfBounds) {
				t2 = null;
				break bb0;
			}
			t1 = generateConnectorPath(line);
		}
		$[0] = from, $[1] = to, $[2] = line, $[3] = t1, $[4] = t2;
	} else line = $[2], t1 = $[3], t2 = $[4];
	if (t2 !== Symbol.for("react.early_return_sentinel")) return t2;
	let linePathDescription = t1, t3, t4;
	$[5] === linePathDescription ? (t3 = $[6], t4 = $[7]) : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InteractivePath, {
		d: linePathDescription,
		strokeWidth: 16
	}), t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConnectorPath, {
		d: linePathDescription,
		strokeWidth: 1
	}), $[5] = linePathDescription, $[6] = t3, $[7] = t4);
	let t5 = to.rect.left - .5, t6;
	$[8] !== t5 || $[9] !== to.bounds || $[10] !== to.rect.height || $[11] !== to.rect.top ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RightBarWrapper, {
		top: to.rect.top,
		left: t5,
		height: to.rect.height,
		width: 1,
		bounds: to.bounds
	}), $[8] = t5, $[9] = to.bounds, $[10] = to.rect.height, $[11] = to.rect.top, $[12] = t6) : t6 = $[12];
	let t7;
	$[13] !== line.from.bounds || $[14] !== line.from.isAbove || $[15] !== line.from.left ? (t7 = line.from.isAbove && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConnectorPath, {
		d: arrowPath(line.from.left + 8, line.from.bounds.top - 12 + 2, -1),
		strokeWidth: 1
	}), $[13] = line.from.bounds, $[14] = line.from.isAbove, $[15] = line.from.left, $[16] = t7) : t7 = $[16];
	let t8;
	$[17] !== line.from.bounds || $[18] !== line.from.isBelow || $[19] !== line.from.left ? (t8 = line.from.isBelow && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConnectorPath, {
		d: arrowPath(line.from.left + 8, line.from.bounds.top + line.from.bounds.height + 12 - 2, 1),
		strokeWidth: 1
	}), $[17] = line.from.bounds, $[18] = line.from.isBelow, $[19] = line.from.left, $[20] = t8) : t8 = $[20];
	let t9;
	$[21] !== line.to.bounds || $[22] !== line.to.isAbove ? (t9 = line.to.isAbove && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConnectorPath, {
		d: arrowPath(line.to.bounds.left + 8, line.to.bounds.top - 12 + 2, -1),
		strokeWidth: 1
	}), $[21] = line.to.bounds, $[22] = line.to.isAbove, $[23] = t9) : t9 = $[23];
	let t10;
	$[24] !== line.to.bounds || $[25] !== line.to.isBelow ? (t10 = line.to.isBelow && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConnectorPath, {
		d: arrowPath(line.to.bounds.left + 8, line.to.bounds.top + line.to.bounds.height + 12 - 2, 1),
		strokeWidth: 1
	}), $[24] = line.to.bounds, $[25] = line.to.isBelow, $[26] = t10) : t10 = $[26];
	let t11;
	$[27] !== line.from.bounds || $[28] !== line.from.outOfBounds || $[29] !== line.to.bounds || $[30] !== line.to.outOfBounds ? (t11 = !1, $[27] = line.from.bounds, $[28] = line.from.outOfBounds, $[29] = line.to.bounds, $[30] = line.to.outOfBounds, $[31] = t11) : t11 = $[31];
	let t12;
	return $[32] !== t10 || $[33] !== t11 || $[34] !== t3 || $[35] !== t4 || $[36] !== t6 || $[37] !== t7 || $[38] !== t8 || $[39] !== t9 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		t3,
		t4,
		t6,
		t7,
		t8,
		t9,
		t10,
		t11
	] }), $[32] = t10, $[33] = t11, $[34] = t3, $[35] = t4, $[36] = t6, $[37] = t7, $[38] = t8, $[39] = t9, $[40] = t12) : t12 = $[40], t12;
});
var svgWrapper = "_1xoz7px0";
function SvgWrapper(props) {
	let $ = (0, import_compiler_runtime.c)(8), className, restProps;
	$[0] === props ? (className = $[1], restProps = $[2]) : ({className, ...restProps} = props, $[0] = props, $[1] = className, $[2] = restProps);
	let t0;
	$[3] === className ? t0 = $[4] : (t0 = clsx(svgWrapper, className), $[3] = className, $[4] = t0);
	let t1;
	return $[5] !== restProps || $[6] !== t0 ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", {
		...restProps,
		className: t0
	}), $[5] = restProps, $[6] = t0, $[7] = t1) : t1 = $[7], t1;
}
var EMPTY_CONNECTORS = [];
function rectsAreEqual(current, next) {
	return current.top === next.top && current.left === next.left && current.width === next.width && current.height === next.height;
}
function connectorsAreEqual(current, next) {
	return current.length === next.length && current.every((currentPair, index) => {
		let nextPair = next[index];
		return currentPair.field.id === nextPair.field.id && currentPair.field.element === nextPair.field.element && currentPair.change.element === nextPair.change.element && currentPair.field.zIndex === nextPair.field.zIndex && currentPair.change.zIndex === nextPair.change.zIndex && rectsAreEqual(currentPair.field.rect, nextPair.field.rect) && rectsAreEqual(currentPair.field.bounds, nextPair.field.bounds) && rectsAreEqual(currentPair.change.rect, nextPair.change.rect) && rectsAreEqual(currentPair.change.bounds, nextPair.change.bounds);
	});
}
function getConnectors(allReportedValues, hovered, byId, rootElement) {
	let changeBarsWithHover = [], changeBarsWithFocus = [];
	for (let value of allReportedValues) {
		if (!isChangeBar(value) || !value[1].isChanged) continue;
		let [id, reportedChangeBar] = value;
		if (id === hovered) {
			changeBarsWithHover.push(value);
			continue;
		}
		if (reportedChangeBar.hasHover) {
			changeBarsWithHover.push(value);
			continue;
		}
		if (reportedChangeBar.hasFocus) {
			changeBarsWithFocus.push(value);
			continue;
		}
	}
	return (changeBarsWithHover.length > 0 ? changeBarsWithHover : changeBarsWithFocus).map(([id]) => {
		let field = findMostSpecificTarget("field", id, byId), change = findMostSpecificTarget("change", id, byId);
		return !field || !change ? null : {
			field: {
				id,
				...field
			},
			change: {
				id,
				...change
			}
		};
	}).filter((value) => !!value?.field.element && !!value?.change.element).map(({ field, change }) => ({
		hasHover: field.hasHover || change.hasHover,
		hasFocus: field.hasFocus,
		hasRevertHover: change.hasRevertHover,
		field: {
			...field,
			...getOffsetsTo(field.element, rootElement)
		},
		change: {
			...change,
			...getOffsetsTo(change.element, rootElement)
		}
	}));
}
function ConnectorsOverlay(props) {
	let $ = (0, import_compiler_runtime.c)(41), { rootElement, onSetFocus } = props, [hovered, setHovered] = (0, import_react.useState)(null), allReportedValues = useChangeIndicatorsReportedValues(), { isReviewChangesOpen } = useReviewChanges(), t0;
	$[0] === allReportedValues ? t0 = $[1] : (t0 = new Map(allReportedValues), $[0] = allReportedValues, $[1] = t0);
	let byId = t0, [connectors, setConnectors] = (0, import_react.useState)(EMPTY_CONNECTORS), t1;
	$[2] !== allReportedValues || $[3] !== byId || $[4] !== hovered || $[5] !== isReviewChangesOpen || $[6] !== rootElement ? (t1 = () => {
		let next = isReviewChangesOpen ? getConnectors(allReportedValues, hovered, byId, rootElement) : EMPTY_CONNECTORS, nextConnectors = next.length === 0 ? EMPTY_CONNECTORS : next;
		setConnectors((current) => connectorsAreEqual(current, nextConnectors) ? current : nextConnectors);
	}, $[2] = allReportedValues, $[3] = byId, $[4] = hovered, $[5] = isReviewChangesOpen, $[6] = rootElement, $[7] = t1) : t1 = $[7];
	let updateConnectors = useEffectEvent(t1), frameRef = (0, import_react.useRef)(null), t2;
	$[8] === updateConnectors ? t2 = $[9] : (t2 = () => {
		frameRef.current === null && (frameRef.current = requestAnimationFrame(() => {
			frameRef.current = null, updateConnectors();
		}));
	}, $[8] = updateConnectors, $[9] = t2);
	let scheduleUpdate = useEffectEvent(t2), t3;
	$[10] === scheduleUpdate ? t3 = $[11] : (t3 = () => {
		scheduleUpdate();
	}, $[10] = scheduleUpdate, $[11] = t3);
	let t4;
	$[12] !== allReportedValues || $[13] !== hovered || $[14] !== isReviewChangesOpen || $[15] !== rootElement ? (t4 = [
		allReportedValues,
		hovered,
		isReviewChangesOpen,
		rootElement
	], $[12] = allReportedValues, $[13] = hovered, $[14] = isReviewChangesOpen, $[15] = rootElement, $[16] = t4) : t4 = $[16], (0, import_react.useEffect)(t3, t4);
	let t5, t6;
	$[17] === Symbol.for("react.memo_cache_sentinel") ? (t5 = () => () => {
		frameRef.current !== null && (cancelAnimationFrame(frameRef.current), frameRef.current = null);
	}, t6 = [], $[17] = t5, $[18] = t6) : (t5 = $[17], t6 = $[18]), (0, import_react.useEffect)(t5, t6);
	let t7;
	$[19] !== rootElement || $[20] !== scheduleUpdate ? (t7 = () => {
		let observer = new ResizeObserver(() => scheduleUpdate());
		return observer.observe(rootElement), () => observer.disconnect();
	}, $[19] = rootElement, $[20] = scheduleUpdate, $[21] = t7) : t7 = $[21];
	let t8;
	$[22] === rootElement ? t8 = $[23] : (t8 = [rootElement], $[22] = rootElement, $[23] = t8), (0, import_react.useEffect)(t7, t8);
	let scrollContext = (0, import_react.use)(ScrollContext), t9;
	$[24] !== scheduleUpdate || $[25] !== scrollContext ? (t9 = () => scrollContext?.subscribe(() => scheduleUpdate()), $[24] = scheduleUpdate, $[25] = scrollContext, $[26] = t9) : t9 = $[26];
	let t10;
	$[27] === scrollContext ? t10 = $[28] : (t10 = [scrollContext], $[27] = scrollContext, $[28] = t10), (0, import_react.useEffect)(t9, t10);
	let mostSpecific;
	if ($[29] !== connectors || $[30] !== mostSpecific) {
		for (let connector of connectors) (!mostSpecific || connector.field.id.length > mostSpecific.field.id.length) && (mostSpecific = connector);
		$[29] = connectors, $[30] = mostSpecific, $[31] = mostSpecific;
	} else mostSpecific = $[31];
	let visibleConnector = mostSpecific, t11 = visibleConnector && visibleConnector.field.zIndex, t12;
	$[32] === t11 ? t12 = $[33] : (t12 = { zIndex: t11 }, $[32] = t11, $[33] = t12);
	let t13;
	$[34] !== isReviewChangesOpen || $[35] !== onSetFocus || $[36] !== visibleConnector ? (t13 = isReviewChangesOpen && visibleConnector?.change && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConnectorGroup, {
		field: visibleConnector.field,
		change: visibleConnector.change,
		onSetFocus,
		setHovered
	}, visibleConnector.field.id), $[34] = isReviewChangesOpen, $[35] = onSetFocus, $[36] = visibleConnector, $[37] = t13) : t13 = $[37];
	let t14;
	return $[38] !== t12 || $[39] !== t13 ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SvgWrapper, {
		"data-testid": "change-connectors-overlay",
		style: t12,
		children: t13
	}), $[38] = t12, $[39] = t13, $[40] = t14) : t14 = $[40], t14;
}
function ConnectorGroup(props) {
	let $ = (0, import_compiler_runtime.c)(35), { change, field, onSetFocus, setHovered } = props, t0;
	$[0] !== change || $[1] !== field || $[2] !== onSetFocus ? (t0 = () => {
		scrollIntoView(field), scrollIntoView(change), onSetFocus(field.path);
	}, $[0] = change, $[1] = field, $[2] = onSetFocus, $[3] = t0) : t0 = $[3];
	let onConnectorClick = t0, t1;
	$[4] !== field.id || $[5] !== setHovered ? (t1 = () => setHovered(field.id), $[4] = field.id, $[5] = setHovered, $[6] = t1) : t1 = $[6];
	let handleMouseEnter = t1, t2;
	$[7] === setHovered ? t2 = $[8] : (t2 = () => setHovered(null), $[7] = setHovered, $[8] = t2);
	let handleMouseLeave = t2, t3 = field.rect.left + 3, t4;
	$[9] !== field.rect || $[10] !== t3 ? (t4 = {
		...field.rect,
		left: t3
	}, $[9] = field.rect, $[10] = t3, $[11] = t4) : t4 = $[11];
	let t5;
	$[12] !== field.bounds || $[13] !== t4 ? (t5 = {
		rect: t4,
		bounds: field.bounds
	}, $[12] = field.bounds, $[13] = t4, $[14] = t5) : t5 = $[14];
	let from = t5, t6 = change.rect.left + 1, t7;
	$[15] !== change.rect || $[16] !== t6 ? (t7 = {
		...change.rect,
		left: t6
	}, $[15] = change.rect, $[16] = t6, $[17] = t7) : t7 = $[17];
	let t8;
	$[18] !== change.bounds || $[19] !== t7 ? (t8 = {
		rect: t7,
		bounds: change.bounds
	}, $[18] = change.bounds, $[19] = t7, $[20] = t8) : t8 = $[20];
	let to = t8, t9;
	$[21] !== from || $[22] !== to ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Connector, {
		from,
		to
	}), $[21] = from, $[22] = to, $[23] = t9) : t9 = $[23];
	let t10;
	$[24] !== handleMouseEnter || $[25] !== handleMouseLeave || $[26] !== onConnectorClick || $[27] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("g", {
		onClick: onConnectorClick,
		onMouseEnter: handleMouseEnter,
		onMouseLeave: handleMouseLeave,
		children: t9
	}), $[24] = handleMouseEnter, $[25] = handleMouseLeave, $[26] = onConnectorClick, $[27] = t9, $[28] = t10) : t10 = $[28];
	let t11;
	$[29] !== change || $[30] !== field ? (t11 = !1, $[29] = change, $[30] = field, $[31] = t11) : t11 = $[31];
	let t12;
	return $[32] !== t10 || $[33] !== t11 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [t10, t11] }), $[32] = t10, $[33] = t11, $[34] = t12) : t12 = $[34], t12;
}
/** @internal */
function ChangeConnectorRoot(t0) {
	let $ = (0, import_compiler_runtime.c)(22), children, className, isReviewChangesOpen, onOpenReviewChanges, onSetFocus, restProps;
	$[0] === t0 ? (children = $[1], className = $[2], isReviewChangesOpen = $[3], onOpenReviewChanges = $[4], onSetFocus = $[5], restProps = $[6]) : ({children, className, isReviewChangesOpen, onOpenReviewChanges, onSetFocus, ...restProps} = t0, $[0] = t0, $[1] = children, $[2] = className, $[3] = isReviewChangesOpen, $[4] = onOpenReviewChanges, $[5] = onSetFocus, $[6] = restProps);
	let [rootElement, setRootElement] = (0, import_react.useState)(), t1;
	$[7] !== isReviewChangesOpen || $[8] !== onOpenReviewChanges || $[9] !== onSetFocus ? (t1 = {
		isReviewChangesOpen,
		onOpenReviewChanges,
		onSetFocus
	}, $[7] = isReviewChangesOpen, $[8] = onOpenReviewChanges, $[9] = onSetFocus, $[10] = t1) : t1 = $[10];
	let contextValue = t1, t2;
	$[11] !== onSetFocus || $[12] !== rootElement ? (t2 = rootElement && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConnectorsOverlay, {
		rootElement,
		onSetFocus
	}), $[11] = onSetFocus, $[12] = rootElement, $[13] = t2) : t2 = $[13];
	let t3;
	$[14] !== children || $[15] !== className || $[16] !== restProps || $[17] !== t2 ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChangeIndicatorsTracker, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ScrollContainer, {
		...restProps,
		ref: setRootElement,
		className,
		children: [children, t2]
	}) }), $[14] = children, $[15] = className, $[16] = restProps, $[17] = t2, $[18] = t3) : t3 = $[18];
	let t4;
	return $[19] !== contextValue || $[20] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReviewChangesContext.Provider, {
		value: contextValue,
		children: t3
	}), $[19] = contextValue, $[20] = t3, $[21] = t4) : t4 = $[21], t4;
}
/**
* @beta
* @hidden
*/
var CommentsIntentProvider = (0, import_react.memo)(function CommentsIntentProvider(props) {
	let $ = (0, import_compiler_runtime.c)(3), { children, getIntent } = props, t0;
	return $[0] !== children || $[1] !== getIntent ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommentsIntentContext.Provider, {
		value: getIntent,
		children
	}), $[0] = children, $[1] = getIntent, $[2] = t0) : t0 = $[2], t0;
});
/** @internal */
function BetaBadge(props) {
	let $ = (0, import_compiler_runtime.c)(8), rest, t0, t1;
	$[0] === props ? (rest = $[1], t0 = $[2], t1 = $[3]) : ({fontSize: t0, children: t1, ...rest} = props, $[0] = props, $[1] = rest, $[2] = t0, $[3] = t1);
	let fontSize = t0 === void 0 ? 1 : t0, children = t1 === void 0 ? "Beta" : t1, t2;
	return $[4] !== children || $[5] !== fontSize || $[6] !== rest ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Badge, {
		...rest,
		fontSize,
		radius: 2,
		tone: "primary",
		children
	}), $[4] = children, $[5] = fontSize, $[6] = rest, $[7] = t2) : t2 = $[7], t2;
}
/**
* `CapabilityGate` only renders its children if the current Studio rendering context does not
* provide the specified capability.
*
* This allows consumers of the component to conveniently mark a portion of the React tree as
* providing a capability that may be overriden by the Studio rendering context. If the rendering
* context provides this capability, the local implementation will not be rendered.
*
* @internal
*/
var CapabilityGate = (t0) => {
	let $ = (0, import_compiler_runtime.c)(1), { children, capability, condition: t1 } = t0, condition = t1 === void 0 ? "unavailable" : t1, renderingContextStore = useRenderingContextStore(), t2;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t2 = {}, $[0] = t2) : t2 = $[0];
	let renderingContextHasCapability = useSyncObservable(renderingContextStore.capabilities, t2)[capability] === !0;
	return condition === "available" && !renderingContextHasCapability || condition === "unavailable" && renderingContextHasCapability ? null : children;
};
/**
* Renders its children after the given delay has elapsed.
*
* @internal
*/
function Delay(t0) {
	let $ = (0, import_compiler_runtime.c)(6), { children, ms: t1 } = t0, ms = t1 === void 0 ? 0 : t1, [ready, setReady] = (0, import_react.useState)(ms <= 0), t2, t3;
	if ($[0] === ms ? (t2 = $[1], t3 = $[2]) : (t2 = () => {
		if (ms <= 0) return;
		let timeoutId = setTimeout(() => setReady(!0), ms);
		return () => {
			clearTimeout(timeoutId);
		};
	}, t3 = [ms], $[0] = ms, $[1] = t2, $[2] = t3), (0, import_react.useEffect)(t2, t3), !ready || !children) {
		let t4;
		return $[3] === Symbol.for("react.memo_cache_sentinel") ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, {}), $[3] = t4) : t4 = $[3], t4;
	}
	let t4;
	return $[4] === children ? t4 = $[5] : (t4 = typeof children == "function" ? children() : children, $[4] = children, $[5] = t4), t4;
}
var strings = {
	"retry.title": "Retry",
	"retrying.title": "Retrying…",
	"copy-error-details.description": "These technical details may be useful for developers.",
	"copy-error-details.title": "Copy error details",
	"copy-error-details.toast.get-failed": "Failed to get error details",
	"copy-error-details.toast.copy-failed": "Failed to copy error details"
};
var TOAST_ID = "copyErrorDetails";
/**
* @internal
*/
function useCopyErrorDetails(error, eventId) {
	let $ = (0, import_compiler_runtime.c)(4), toast = useToast(), t0;
	return $[0] !== error || $[1] !== eventId || $[2] !== toast ? (t0 = () => {
		of({
			error,
			eventId
		}).pipe(serializeError(), catchError((serializeErrorError) => (console.error(serializeErrorError), toast.push({
			status: "error",
			title: strings["copy-error-details.toast.get-failed"],
			id: TOAST_ID
		}), EMPTY)), tap(_temp$58), catchError((copyErrorError) => (console.error(copyErrorError), toast.push({
			status: "error",
			title: strings["copy-error-details.toast.copy-failed"],
			id: TOAST_ID
		}), EMPTY))).subscribe();
	}, $[0] = error, $[1] = eventId, $[2] = toast, $[3] = t0) : t0 = $[3], t0;
}
/**
* @internal
*/
function _temp$58(errorDetailsString) {
	return navigator.clipboard.writeText(errorDetailsString);
}
function serializeError() {
	return map(({ error, eventId }) => {
		let sanitizedErrorInfo = sanitizeErrorInfo(isRecord$4(error) ? pick(error, Object.getOwnPropertyNames(error)) : void 0);
		return JSON.stringify({
			error: sanitizedErrorInfo,
			eventId
		}, null, 2);
	});
}
var SENSITIVE_HEADERS = ["authorization"];
function sanitizeErrorInfo(obj) {
	if (!isRecord$4(obj)) return obj;
	let sanitized = {};
	for (let [key, value] of Object.entries(obj)) if (key.toLowerCase() === "headers" && isRecord$4(value)) {
		let sanitizedHeaders = {};
		for (let [headerName, headerValue] of Object.entries(value)) SENSITIVE_HEADERS.some((sensitiveHeader) => headerName.toLowerCase().includes(sensitiveHeader)) ? sanitizedHeaders[headerName] = "[hidden]" : sanitizedHeaders[headerName] = headerValue;
		sanitized[key] = sanitizedHeaders;
	} else isRecord$4(value) || Array.isArray(value) ? sanitized[key] = sanitizeErrorInfo(value) : sanitized[key] = value;
	return sanitized;
}
/**
* @internal
*/
var ErrorActions = (t0) => {
	let $ = (0, import_compiler_runtime.c)(10), { error, eventId, onRetry, isRetrying, size } = t0, copyErrorDetails = useCopyErrorDetails(error, eventId), t1;
	$[0] !== isRetrying || $[1] !== onRetry || $[2] !== size ? (t1 = onRetry && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		disabled: isRetrying,
		loading: isRetrying,
		onClick: onRetry,
		text: strings["retry.title"],
		tone: "primary",
		icon: SyncIcon,
		size
	}), $[0] = isRetrying, $[1] = onRetry, $[2] = size, $[3] = t1) : t1 = $[3];
	let t2;
	$[4] !== copyErrorDetails || $[5] !== size ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip$1, {
		content: strings["copy-error-details.description"],
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			onClick: copyErrorDetails,
			text: strings["copy-error-details.title"],
			tone: "default",
			mode: "ghost",
			icon: CopyIcon,
			size
		})
	}), $[4] = copyErrorDetails, $[5] = size, $[6] = t2) : t2 = $[6];
	let t3;
	return $[7] !== t1 || $[8] !== t2 ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Inline, {
		gap: 3,
		children: [t1, t2]
	}), $[7] = t1, $[8] = t2, $[9] = t3) : t3 = $[9], t3;
};
/**
*
* @hidden
* @beta
*/
function IntentButton(props) {
	let $ = (0, import_compiler_runtime.c)(2), t0;
	return $[0] === props ? t0 = $[1] : (t0 = props.disabled ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		...props,
		as: "a",
		role: "link",
		"aria-disabled": "true"
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		...props,
		as: IntentLink
	}), $[0] = props, $[1] = t0), t0;
}
var DEFAULT_MEDIA_DIMENSION = {
	...PREVIEW_SIZES.default.media,
	aspect: 1,
	fit: "crop",
	dpr: getDevicePixelRatio()
};
var Root$4 = gt(Box).withConfig({
	displayName: "Root",
	componentId: "sc-up8102-0"
})`height:100%;a{color:currentColor;text-decoration:none;}svg[data-sanity-icon]{margin:0;}`;
var HeaderFlex = gt(Flex).attrs({ align: "center" }).withConfig({
	displayName: "HeaderFlex",
	componentId: "sc-up8102-1"
})`height:${rem(PREVIEW_SIZES.default.media.height)};`;
var TitleSkeleton = gt(TextSkeleton).attrs({
	animated: !0,
	radius: 1
}).withConfig({
	displayName: "TitleSkeleton",
	componentId: "sc-up8102-2"
})`max-width:${rem(160)};width:80%;`;
var SubtitleSkeleton = gt(TextSkeleton).attrs({
	animated: !0,
	radius: 1,
	size: 1
}).withConfig({
	displayName: "SubtitleSkeleton",
	componentId: "sc-up8102-3"
})`max-width:${rem(120)};width:60%;`;
/**
* @hidden
* @beta */
function TemplatePreview(props) {
	let $ = (0, import_compiler_runtime.c)(25), { description, isPlaceholder, media, mediaDimensions: t0, subtitle: Subtitle, title: t1 } = props, mediaDimensions = t0 === void 0 ? DEFAULT_MEDIA_DIMENSION : t0, Title = t1 === void 0 ? "Untitled" : t1;
	if (isPlaceholder) {
		let t2;
		return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Root$4, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HeaderFlex, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			flex: 1,
			gap: 2,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(TitleSkeleton, {}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SubtitleSkeleton, {})]
		}) }) }), $[0] = t2) : t2 = $[0], t2;
	}
	let t2;
	$[1] === Title ? t2 = $[2] : (t2 = (0, import_react_is.isValidElementType)(Title) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		textOverflow: "ellipsis",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Title, { layout: "default" })
	}), $[1] = Title, $[2] = t2);
	let t3;
	$[3] === Title ? t3 = $[4] : (t3 = (0, import_react.isValidElement)(Title) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		textOverflow: "ellipsis",
		children: Title
	}), $[3] = Title, $[4] = t3);
	let t4;
	$[5] === Subtitle ? t4 = $[6] : (t4 = (0, import_react_is.isValidElementType)(Subtitle) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		muted: !0,
		size: 1,
		textOverflow: "ellipsis",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Subtitle, { layout: "default" })
	}), $[5] = Subtitle, $[6] = t4);
	let t5;
	$[7] === Subtitle ? t5 = $[8] : (t5 = (0, import_react.isValidElement)(Subtitle) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		muted: !0,
		size: 1,
		textOverflow: "ellipsis",
		children: Subtitle
	}), $[7] = Subtitle, $[8] = t5);
	let t6;
	$[9] !== t2 || $[10] !== t3 || $[11] !== t4 || $[12] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		flex: 1,
		gap: 2,
		children: [
			t2,
			t3,
			t4,
			t5
		]
	}), $[9] = t2, $[10] = t3, $[11] = t4, $[12] = t5, $[13] = t6) : t6 = $[13];
	let t7;
	$[14] !== media || $[15] !== mediaDimensions ? (t7 = media && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		align: "flex-start",
		paddingLeft: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Media$1, {
			dimensions: mediaDimensions,
			layout: "default",
			media
		})
	}), $[14] = media, $[15] = mediaDimensions, $[16] = t7) : t7 = $[16];
	let t8;
	$[17] !== t6 || $[18] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(HeaderFlex, { children: [t6, t7] }), $[17] = t6, $[18] = t7, $[19] = t8) : t8 = $[19];
	let t9;
	$[20] === description ? t9 = $[21] : (t9 = description && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		marginTop: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			muted: !0,
			size: 1,
			style: { whiteSpace: "break-spaces" },
			children: description
		})
	}), $[20] = description, $[21] = t9);
	let t10;
	return $[22] !== t8 || $[23] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Root$4, { children: [t8, t9] }), $[22] = t8, $[23] = t9, $[24] = t10) : t10 = $[24], t10;
}
/** @internal */
function useOnScroll(callback) {
	let $ = (0, import_compiler_runtime.c)(4), parentContext = (0, import_react.useContext)(ScrollContext), t0, t1;
	$[0] !== callback || $[1] !== parentContext ? (t0 = () => parentContext?.subscribe(callback), t1 = [callback, parentContext], $[0] = callback, $[1] = parentContext, $[2] = t0, $[3] = t1) : (t0 = $[2], t1 = $[3]), (0, import_react.useEffect)(t0, t1);
}
/**
* @internal
* @deprecated -- Refactor the component so it can call `useToast` instead
*/
function ImperativeToast(t0) {
	let $ = (0, import_compiler_runtime.c)(2), { ref } = t0, { push } = useToast(), t1;
	return $[0] === push ? t1 = $[1] : (t1 = () => ({ push }), $[0] = push, $[1] = t1), (0, import_react.useImperativeHandle)(ref, t1), null;
}
ImperativeToast.displayName = "ImperativeToast";
var EMPTY$1 = [];
/**
* @internal
* @deprecated - Will be removed in 4.0.0, use the `useReferringDocuments(<documentId>)` hook instead
*/
function WithReferringDocuments(t0) {
	let $ = (0, import_compiler_runtime.c)(3), { children, id } = t0, t1 = useReferringDocuments(id, EMPTY$1), t2;
	return $[0] !== children || $[1] !== t1 ? (t2 = children(t1), $[0] = children, $[1] = t1, $[2] = t2) : t2 = $[2], t2;
}
/**
* TODO: Rename to `ZOffsetsProvider`
*
* @internal
*/
function ZIndexProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(2), { children } = t0, t1;
	return $[0] === children ? t1 = $[1] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ZIndexContext.Provider, {
		value: zIndexContextDefaults,
		children
	}), $[0] = children, $[1] = t1), t1;
}
/**
* @hidden
* @beta */
function defineConfig(config) {
	return config;
}
/**
* @deprecated Use `defineConfig` instead
*
* @hidden
* @beta
*/
function createConfig(config) {
	return defineConfig(config);
}
var SANITY_DEFINED_ACTIONS = {
	delete: "delete",
	discardChanges: "discardChanges",
	discardVersion: "discardVersion",
	duplicate: "duplicate",
	restore: "restore",
	publish: "publish",
	unpublish: "unpublish",
	unpublishVersion: "unpublishVersion",
	linkToCanvas: "linkToCanvas",
	editInCanvas: "editInCanvas",
	unlinkFromCanvas: "unlinkFromCanvas",
	schedule: "schedule"
};
var isSanityDefinedAction = (action) => action.action ? action.action in SANITY_DEFINED_ACTIONS : !1;
/**
* Creates a mock `AuthStore` (for testing) that emits an `AuthState` derived
* from the `client` and `currentUser` given.
*
* @internal
*/
function createMockAuthStore({ client, currentUser = null }) {
	return { state: of({
		authenticated: !0,
		client,
		currentUser
	}) };
}
/**
* Fully resolves a configuration including subscribing to all sources and
* workspaces from a config. Returns an `Observable` that waits till all sources
* emit once before emitting an array of fully resolved sources and workspaces.
*
* @internal
*/
function resolveConfig(config) {
	let { workspaces } = prepareConfig(config);
	return combineLatest(workspaces.flatMap((workspaceSummary) => combineLatest(workspaceSummary.__internal.sources.map(({ source }) => source)).pipe(map((sources) => {
		let { releases: _releases, ...workspaceMetadata } = workspaceSummary;
		return {
			...workspaceMetadata,
			...sources[0],
			unstable_sources: sources,
			type: "workspace"
		};
	}))));
}
/**
* PRIMARILY FOR TESTING PURPOSES.
*
* This will create a fully resolved workspace from a config and optionally
* allows a `client` and `currentUser` override. This exists primarily for
* testing purposes. If you need to use a workspace, we recommend using the
* `useWorkspace` hook to grab the fully resolved workspace from the
* `StudioProvider`
*
* @internal
*/
async function createWorkspaceFromConfig(options) {
	let client = "getClient" in options ? options.getClient({ apiVersion: "2023-11-13" }) : void 0, [workspace] = await firstValueFrom(resolveConfig({
		...options,
		...client && "currentUser" in options && { auth: createMockAuthStore({
			...options,
			client
		}) }
	}));
	return workspace;
}
/**
* PRIMARILY FOR TESTING PURPOSES.
*
* This will create a fully resolved source from a config and optionally
* allows a `client` and `currentUser` override. This exists primarily for
* testing purposes. If you need to use a source, we recommend using the
* `useSource` hook to grab the fully resolved source from the `StudioProvider`
*
* @internal
*/
async function createSourceFromConfig(options) {
	return (await createWorkspaceFromConfig(options)).unstable_sources[0];
}
/**
* Reduce a {@link Source} down to a {@link ConfigContext}, memoizing using `useMemo`
*
* @param source - Source to convert
* @returns A config context containing only the defined properties of that interface
* @internal
*/
function useConfigContextFromSource(source) {
	let $ = (0, import_compiler_runtime.c)(7), { projectId, dataset, schema, currentUser, getClient, i18n } = source, t0;
	return $[0] !== currentUser || $[1] !== dataset || $[2] !== getClient || $[3] !== i18n || $[4] !== projectId || $[5] !== schema ? (t0 = {
		projectId,
		dataset,
		schema,
		currentUser,
		getClient,
		i18n
	}, $[0] = currentUser, $[1] = dataset, $[2] = getClient, $[3] = i18n, $[4] = projectId, $[5] = schema, $[6] = t0) : t0 = $[6], t0;
}
/**
* Reduce a {@link Source} down to a {@link ConfigContext}, without memoization - use for non-react contexts
*
* @param source - Source to convert
* @returns A config context containing only the defined properties of that interface
* @internal
*/
function getConfigContextFromSource(source) {
	let { projectId, dataset, schema, currentUser, getClient, i18n } = source;
	return {
		projectId,
		dataset,
		schema,
		currentUser,
		getClient,
		i18n
	};
}
/**
* Gets the global scope instance in a given environment.
*
* The strategy is to return the most modern, and if not, the most common:
* - The `globalThis` variable is the modern approach to accessing the global scope
* - The `window` variable is the global scope in a web browser
* - The `self` variable is the global scope in workers and others
* - The `global` variable is the global scope in Node.js
*/
function getGlobalScope() {
	if (typeof globalThis < "u") return globalThis;
	if (typeof window < "u") return window;
	if (typeof self < "u") return self;
	if (typeof global < "u") return global;
	throw Error("@sanity/ui: could not locate global scope");
}
/** @internal */
var globalScope = getGlobalScope();
var STUDIO_DSN = "https://8914c8dde7e1ebce191f15af8bf6b7b9@sentry.sanity.io/4507342122123264";
var IS_EMBEDDED_STUDIO = !("__sanityErrorChannel" in globalScope);
var DEBUG_ERROR_REPORTING = supportsLocalStorage && !!localStorage.getItem("SANITY_DEBUG_ERROR_REPORTING");
var clientOptions = {
	dsn: STUDIO_DSN,
	tunnel: "https://api.sanity.io/vX/intake/tracing",
	release: SANITY_VERSION,
	environment: "development",
	debug: DEBUG_ERROR_REPORTING,
	enabled: typeof window < "u" && DEBUG_ERROR_REPORTING
};
var integrations = [
	inboundFiltersIntegration(),
	functionToStringIntegration(),
	browserApiErrorsIntegration({ eventTarget: !1 }),
	breadcrumbsIntegration({ console: !1 }),
	globalHandlersIntegration({
		onerror: !0,
		onunhandledrejection: !0
	}),
	linkedErrorsIntegration(),
	dedupeIntegration(),
	sanityDedupeIntegration(),
	httpContextIntegration()
];
/**
* Get an instance of the Sentry error reporter
*
* @internal
*/
function getSentryErrorReporter() {
	let client, scope, preInitErrors = [];
	function _initialize() {
		if (client || !DEBUG_ERROR_REPORTING && !hasSanityPackageInImportMap() || !DEBUG_ERROR_REPORTING && IS_EMBEDDED_STUDIO) return;
		let isSentryInitialized = isInitialized(), hasThirdPartySentry = isSentryInitialized && getClient()?.getOptions().dsn === "https://8914c8dde7e1ebce191f15af8bf6b7b9@sentry.sanity.io/4507342122123264";
		if (isSentryInitialized && !hasThirdPartySentry) {
			client = getClient(), scope = getCurrentScope();
			return;
		}
		if (hasThirdPartySentry) {
			client = new BrowserClient({
				...clientOptions,
				stackParser: defaultStackParser,
				integrations,
				beforeSend,
				transport: makeFetchTransport
			}), scope = new Scope(), scope.setClient(client), client.init();
			return;
		}
		init({
			...clientOptions,
			defaultIntegrations: !1,
			integrations,
			beforeSend
		}), client = getClient(), scope = getCurrentScope();
	}
	function initialize() {
		_initialize(), client && preInitErrors.length > 0 && (preInitErrors.forEach(({ error, options }) => reportError(error, options)), preInitErrors.length = 0);
	}
	function reportError(error, options = {}) {
		if (!client) return preInitErrors.push({
			error,
			options
		}), null;
		let { reactErrorInfo = {}, errorBoundary } = options, { componentStack } = reactErrorInfo, contexts = {};
		componentStack && (contexts.react = { componentStack });
		let tags = { handled: "no" };
		errorBoundary && (tags.errorBoundary = errorBoundary);
		let eventId = null;
		return withScope(() => {
			if (componentStack && isError(error)) {
				let errorBoundaryError = Error(error.message);
				errorBoundaryError.name = `${errorBoundary || "ErrorBoundary"} ${error.name}`, errorBoundaryError.stack = componentStack, setCause(error, errorBoundaryError);
			}
			eventId = captureException(error, {
				mechanism: { handled: !1 },
				captureContext: {
					contexts,
					tags
				}
			});
		}), eventId ? { eventId } : null;
	}
	return {
		initialize,
		reportError
	};
}
var objectToString = Object.prototype.toString;
/**
* Checks whether given value's type is one of a few Error or Error-like
*
* @param thing - A value to be checked
* @returns A boolean representing the result
* @internal
*/
function isError(thing) {
	switch (objectToString.call(thing)) {
		case "[object Error]":
		case "[object Exception]":
		case "[object DOMException]": return !0;
		default: return isInstanceOf(thing, Error);
	}
}
/**
* Checks whether given value's type is an instance of provided constructor.
*
* @param thing - A value to be checked.
* @param base - A constructor to be used in a check.
* @returns A boolean representing the result.
* @internal
*/
function isInstanceOf(thing, base) {
	try {
		return thing instanceof base;
	} catch {
		return !1;
	}
}
/**
* Set the `cause` property on an error object
*
* @param error - The error to set the cause on
* @param cause - The cause of the error
* @internal
*/
function setCause(error, cause) {
	let seenErrors = /* @__PURE__ */ new WeakMap();
	function recurse(err, subCause) {
		if (!seenErrors.has(err)) {
			if (isError(err.cause)) {
				seenErrors.set(err, !0), recurse(err.cause, subCause);
				return;
			}
			err.cause = subCause;
		}
	}
	recurse(error, cause);
}
/**
* Sentry treats errors that are caught in an error boundary as "handled", which we don't want.
* It gives a false sense of security, as the error is only caught to show a more helpful error
* than a blank page. This function sets the `handled` prop on the error's mechanism to `false`.
* Note: This _mutates_ the event, in order to avoid having to deep-clone.
*
* @param event - The event to mark as unhandled
* @internal
*/
function setAsUnhandled(event) {
	for (let exception of event.exception?.values || []) exception.mechanism && (exception.mechanism.handled = !1);
}
/**
* "Before send" event handler, which sets the error as unhandled.
* @see setAsUnhandled for a clearer rationale.
*
* @param event - The event to be sent
* @returns The event to be sent
* @internal
*/
function beforeSend(event) {
	return setAsUnhandled(event), scrubPii(event);
}
/**
* Strips personally identifiable information from a Sentry event so that
* error reports never contain data that could identify a user.
*
* @param event - The event to scrub
* @returns The scrubbed event
* @internal
*/
function scrubPii(event) {
	return delete event.user, delete event.request, event.user = { ip_address: "0.0.0.0" }, event;
}
/**
* We'll want a more aggressive dedupe strategy than the default one, as the default is very
* fine grained, needing the same exact stack and message to be considered a duplicate.
* We want to be more conservative.
*
* @internal
*/
function sanityDedupeIntegration() {
	let previousEvents = [];
	return {
		name: "SanityDedupe",
		processEvent(currentEvent) {
			if (currentEvent.type) return currentEvent;
			try {
				if (shouldDropEvent(currentEvent, previousEvents)) return DEBUG_ERROR_REPORTING && console.warn("[sanity/sentry] Dropping error from being reported because it is a duplicate"), null;
			} catch {}
			return previousEvents.length > 10 && previousEvents.shift(), previousEvents.push(currentEvent), currentEvent;
		}
	};
}
/**
* Determines whether or not the given event should be dropped or not, based on a window of
* previously reported events.
*
* @param currentEvent - The event to check
* @param previousEvents - An array of previously reported events
* @returns True if event should be dropped, false otherwise
* @internal
*/
function shouldDropEvent(currentEvent, previousEvents) {
	for (let previousEvent of previousEvents) {
		let currentMessage = getMessageFromEvent(currentEvent), previousMessage = getMessageFromEvent(previousEvent);
		if (currentMessage && previousMessage && currentMessage !== previousMessage) continue;
		let currentTimestamp = Math.floor(currentEvent.timestamp || 0), previousTimestamp = Math.floor(previousEvent.timestamp || 0);
		if (Math.abs(currentTimestamp - previousTimestamp) < 300) return !0;
	}
	return !1;
}
/**
* Extract the `message` string from a Sentry event. Sometimes this is not available on the `event`
* itself, but buried inside of the `event.exception` property.
*
* @param event - The Sentry event to extract the message from
* @returns A string representing the message, or `undefined` if not found
* @internal
*/
function getMessageFromEvent(event) {
	if (event.message) return event.message;
	if (event.exception) {
		for (let exception of event.exception.values || []) if (exception.value) return exception.value;
	}
}
/** @internal */
function useProject() {
	let $ = (0, import_compiler_runtime.c)(6), projectStore = useProjectStore(), errorHandler = useStudioErrorHandler(), [value, setValue] = (0, import_react.useState)(null), t0, t1;
	$[0] !== errorHandler || $[1] !== projectStore ? (t0 = () => {
		let sub = from(errorHandler.attempt(() => projectStore.get(), { retryable: !0 })).subscribe(setValue);
		return () => sub.unsubscribe();
	}, t1 = [errorHandler, projectStore], $[0] = errorHandler, $[1] = projectStore, $[2] = t0, $[3] = t1) : (t0 = $[2], t1 = $[3]), (0, import_react.useEffect)(t0, t1);
	let t2;
	return $[4] === value ? t2 = $[5] : (t2 = { value }, $[4] = value, $[5] = t2), t2;
}
var INITIAL_STATE$3 = {
	value: null,
	loading: !0
};
/**
* @beta
* Returns the organization data for the current project.
* */
function useProjectOrganizationData() {
	let $ = (0, import_compiler_runtime.c)(2), projectStore = useProjectStore(), t0;
	return $[0] === projectStore ? t0 = $[1] : (t0 = projectStore.getOrganizationData().pipe(map(_temp$57), startWith({
		value: null,
		loading: !0
	})), $[0] = projectStore, $[1] = t0), useObservable(t0, INITIAL_STATE$3);
}
function _temp$57(res) {
	return {
		value: res,
		loading: !1
	};
}
var sessionId$1 = v4();
function collectLeafPluginNames(plugins) {
	let names = [];
	for (let plugin of plugins) plugin.plugins && plugin.plugins.length > 0 ? names.push(...collectLeafPluginNames(plugin.plugins)) : names.push(plugin.name);
	return names;
}
function resolvePluginNames(workspace) {
	return collectLeafPluginNames(workspace.__internal?.options?.plugins ?? []).filter(Boolean);
}
/**
* Gathers the base (static) and dynamic (navigation-dependent) tags for feedback.
*
* @internal
*/
function useStudioFeedbackTags() {
	let $ = (0, import_compiler_runtime.c)(31), workspace = useWorkspace(), currentUser = useCurrentUser(), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = { apiVersion: "2023-12-18" }, $[0] = t0) : t0 = $[0];
	let client = useClient(t0), t1;
	$[1] === client ? t1 = $[2] : (t1 = client.config().projectId ?? "", $[1] = client, $[2] = t1);
	let projectId = t1, { value: orgId } = useProjectOrganizationId(), { value: orgData } = useProjectOrganizationData(), orgName = orgData?.name ?? "", { value: projectData } = useProject(), { projectSubscriptions } = useProjectSubscriptions(), activeTool = useRouterState(_temp$56), t2;
	$[3] === workspace ? t2 = $[4] : (t2 = resolvePluginNames(workspace), $[3] = workspace, $[4] = t2);
	let pluginNames = t2, userId = currentUser?.id ?? "unknown";
	projectData?.displayName, projectSubscriptions?.plan?.name;
	let t3 = projectData?.displayName ?? "", t4 = orgId ?? "", t5 = projectSubscriptions?.plan?.name ?? "", t6;
	$[5] === pluginNames ? t6 = $[6] : (t6 = pluginNames.join(","), $[5] = pluginNames, $[6] = t6);
	let t7;
	$[7] !== orgName || $[8] !== pluginNames.length || $[9] !== projectId || $[10] !== t3 || $[11] !== t4 || $[12] !== t5 || $[13] !== t6 || $[14] !== userId ? (t7 = {
		userAgent: typeof navigator < "u" ? navigator.userAgent : "",
		screenDensity: typeof window < "u" ? String(window.devicePixelRatio) : "",
		screenHeight: typeof screen < "u" ? String(screen.height) : "",
		screenWidth: typeof screen < "u" ? String(screen.width) : "",
		innerHeight: typeof window < "u" ? String(window.innerHeight) : "",
		innerWidth: typeof window < "u" ? String(window.innerWidth) : "",
		studioVersion: SANITY_VERSION,
		reactVersion: import_react.version ?? "",
		environment: typeof location < "u" && location.hostname === "localhost" ? "development" : "production",
		projectId,
		projectName: t3,
		orgId: t4,
		orgName,
		planTier: t5,
		sessionId: sessionId$1,
		userId,
		plugins: t6,
		pluginsCount: pluginNames.length
	}, $[7] = orgName, $[8] = pluginNames.length, $[9] = projectId, $[10] = t3, $[11] = t4, $[12] = t5, $[13] = t6, $[14] = userId, $[15] = t7) : t7 = $[15];
	let baseTags = t7, t8;
	$[16] !== activeTool || $[17] !== workspace.dataset || $[18] !== workspace.name || $[19] !== workspace.projectId ? (t8 = {
		activeTool,
		activeWorkspace: workspace.name,
		activeProjectId: workspace.projectId,
		activeDataset: workspace.dataset,
		url: typeof window < "u" ? window.location.href : ""
	}, $[16] = activeTool, $[17] = workspace.dataset, $[18] = workspace.name, $[19] = workspace.projectId, $[20] = t8) : t8 = $[20];
	let dynamicTags = t8, t9;
	$[21] !== baseTags || $[22] !== dynamicTags ? (t9 = {
		...baseTags,
		...dynamicTags
	}, $[21] = baseTags, $[22] = dynamicTags, $[23] = t9) : t9 = $[23];
	let allTags = t9, t10 = currentUser?.name ?? void 0, t11 = currentUser?.email ?? void 0, t12;
	return $[24] !== allTags || $[25] !== baseTags || $[26] !== dynamicTags || $[27] !== t10 || $[28] !== t11 || $[29] !== userId ? (t12 = {
		baseTags,
		dynamicTags,
		allTags,
		userName: t10,
		userEmail: t11,
		userId
	}, $[24] = allTags, $[25] = baseTags, $[26] = dynamicTags, $[27] = t10, $[28] = t11, $[29] = userId, $[30] = t12) : t12 = $[30], t12;
}
function _temp$56(routerState) {
	return typeof routerState.tool == "string" ? routerState.tool : "";
}
var cache$1 = /* @__PURE__ */ new Map();
/**
* Returns an observable that resolves the user's telemetry consent status.
* The result is cached per project ID for the lifetime of the session —
* subsequent calls for the same project return the same shared observable
* without making additional API requests.
*
* @internal
*/
function getTelemetryConsent$(client) {
	let projectId = client.config().projectId ?? "__default", cached$ = cache$1.get(projectId);
	return cached$ || (cached$ = client.observable.request({
		uri: "/intake/telemetry-status",
		tag: "telemetry-consent"
	}).pipe(map((res) => res?.status === "granted" ? "granted" : "denied"), shareReplay(1)), cache$1.set(projectId, cached$)), cached$;
}
/**
* Returns the current user's telemetry consent status.
* The underlying API call is cached — multiple components using this hook
* share a single request.
*
* @internal
*/
function useTelemetryConsent() {
	let $ = (0, import_compiler_runtime.c)(3), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = { apiVersion: "2023-12-18" }, $[0] = t0) : t0 = $[0];
	let client = useClient(t0), t1;
	return $[1] === client ? t1 = $[2] : (t1 = getTelemetryConsent$(client).pipe(catchError(_temp$55)), $[1] = client, $[2] = t1), useObservable(t1, "loading");
}
function _temp$55() {
	return of("denied");
}
/** @internal */
function StudioFeedbackProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(10), { children } = t0, { allTags, userName, userEmail } = useStudioFeedbackTags(), telemetryConsent = useTelemetryConsent(), t1;
	$[0] === allTags ? t1 = $[1] : (t1 = { ...allTags }, $[0] = allTags, $[1] = t1);
	let t2;
	$[2] !== t1 || $[3] !== telemetryConsent || $[4] !== userEmail || $[5] !== userName ? (t2 = {
		telemetryConsent,
		userName,
		userEmail,
		tags: t1
	}, $[2] = t1, $[3] = telemetryConsent, $[4] = userEmail, $[5] = userName, $[6] = t2) : t2 = $[6];
	let value = t2, t3;
	return $[7] !== children || $[8] !== value ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FeedbackContext.Provider, {
		value,
		children
	}), $[7] = children, $[8] = value, $[9] = t3) : t3 = $[9], t3;
}
/** @internal */
var FEEDBACK_TUNNEL_URL = "https://api.sanity.io/vX/intake/feedback";
var clientsByDsn = /* @__PURE__ */ new Map();
/**
* Get or create a dedicated Sentry client and scope for sending feedback.
* Each DSN gets its own client with the feedback tunnel.
* @param dsn - Sentry DSN for the target project.
* Format: `https://[key]@[host]/[project-id]`
* @returns The Sentry scope bound to a feedback-specific client.
*/
function getFeedbackClient(dsn) {
	let cached = clientsByDsn.get(dsn);
	if (cached) return cached;
	let client = new BrowserClient({
		dsn,
		tunnel: FEEDBACK_TUNNEL_URL,
		release: SANITY_VERSION,
		environment: "development",
		stackParser: defaultStackParser,
		integrations: [],
		transport: makeFetchTransport
	}), scope = new Scope();
	return scope.setClient(client), client.init(), clientsByDsn.set(dsn, scope), scope;
}
/**
* Send user feedback to Sentry.
* Waits for the transport to flush so network-level failures
* (ad blockers, timeouts) propagate to the caller.
*
* @internal
*/
async function sendFeedbackToSentry(payload) {
	let scope = getFeedbackClient(payload.dsn), { message, name, email, source, feedbackVersion, telemetryConsent, tags, attachments } = payload, hasTelemetryConsent = telemetryConsent === "granted", hasContactConsent = String(tags.contactConsent) === "true", shareName = hasTelemetryConsent || hasContactConsent || !!name, shareEmail = hasTelemetryConsent || hasContactConsent || !!email, { userId: _userId, ...safeTags } = tags, eventTags = hasTelemetryConsent ? tags : safeTags, hasAttachments = !!attachments?.length, feedbackEvent = {
		contexts: { feedback: {
			contactEmail: shareEmail ? email : void 0,
			name: shareName ? name : void 0,
			message,
			url: tags.url,
			source
		} },
		type: "feedback",
		level: "info",
		tags: {
			...eventTags,
			feedbackVersion,
			...shareName ? { contactName: name ?? "" } : {},
			...shareEmail ? { contactEmail: email ?? "" } : {},
			telemetryConsent,
			hasAttachments,
			type: "feedback",
			source
		}
	}, hint = hasAttachments ? { attachments } : {}, eventId = scope.captureEvent(feedbackEvent, hint), client = scope.getClient();
	return client && await client.flush(1e4), eventId;
}
var FeedbackDialogOpened = defineEvent({
	name: "Feedback Dialog Opened",
	version: 1,
	description: "User opened the in-studio feedback dialog"
});
var FeedbackDialogDismissed = defineEvent({
	name: "Feedback Dialog Dismissed",
	version: 1,
	description: "User closed the in-studio feedback dialog without sending feedback."
});
/** @internal */
function useFeedbackTelemetry() {
	let $ = (0, import_compiler_runtime.c)(7), telemetry = useTelemetry(), t0;
	$[0] === telemetry ? t0 = $[1] : (t0 = () => {
		telemetry.log(FeedbackDialogOpened);
	}, $[0] = telemetry, $[1] = t0);
	let feedbackDialogOpened = t0, t1;
	$[2] === telemetry ? t1 = $[3] : (t1 = () => {
		telemetry.log(FeedbackDialogDismissed);
	}, $[2] = telemetry, $[3] = t1);
	let feedbackDialogDismissed = t1, t2;
	return $[4] !== feedbackDialogDismissed || $[5] !== feedbackDialogOpened ? (t2 = {
		feedbackDialogOpened,
		feedbackDialogDismissed
	}, $[4] = feedbackDialogDismissed, $[5] = feedbackDialogOpened, $[6] = t2) : t2 = $[6], t2;
}
/**
* Baked-in English strings for the feedback UI.
*
* When the studio's I18nextProvider is present these keys resolve through the
* full i18n pipeline (custom locales, overrides, etc.). When the provider is
* absent — for example the dialog is rendered outside of the studio then these values
* are used as-is, so the UI never shows raw translation keys.
*/
var FEEDBACK_STRINGS = {
	"feedback.dialog.title": "Share feedback with Sanity",
	"feedback.sentiment.label": "How easy or difficult is Sanity to use?",
	"feedback.sentiment.happy": "Easy",
	"feedback.sentiment.neutral": "Not sure",
	"feedback.sentiment.unhappy": "Difficult",
	"feedback.message.label": "What is working? What could be better?",
	"feedback.message.placeholder": "Describe your issue or request...",
	"feedback.attachment.label": "Attach an image",
	"feedback.attachment.browse": "Browse",
	"feedback.attachment.drop-zone": "Drag or paste file here",
	"feedback.attachment.remove": "Remove",
	"feedback.attachment.error.size": "Image must be under 20 MB",
	"feedback.consent.label": "Can we follow up with you about this feedback?",
	"feedback.consent.disclaimer": "We'd love to learn more. Selecting yes shares your name and email with the Sanity team.",
	"feedback.consent.yes": "Yes",
	"feedback.consent.no": "No",
	"feedback.cancel": "Cancel",
	"feedback.submit": "Send feedback"
};
/**
* Translation hook for feedback components that gracefully degrades
* when no I18nextProvider is available.
*
* @internal
*/
function useFeedbackTranslation() {
	let $ = (0, import_compiler_runtime.c)(4), i18n = (0, import_react.useContext)(I18nContext)?.i18n, t0;
	$[0] === i18n ? t0 = $[1] : (t0 = (key) => {
		if (i18n?.isInitialized) {
			let result = i18n.t(key, { ns: "feedback" });
			if (typeof result == "string" && result !== key) return result;
		}
		return FEEDBACK_STRINGS[key] ?? key;
	}, $[0] = i18n, $[1] = t0);
	let t = t0, t1;
	return $[2] === t ? t1 = $[3] : (t1 = { t }, $[2] = t, $[3] = t1), t1;
}
var FileTargetCard = fileTarget(Card);
/** @internal */
function ImageAttachment(props) {
	let $ = (0, import_compiler_runtime.c)(52), { imageFile, showAttachment, dragOver, error, onFiles, onFilesOver, onFilesOut, onRemove, onExpand } = props, { t } = useFeedbackTranslation();
	if (!showAttachment && !imageFile) {
		let t0;
		$[0] === t ? t0 = $[1] : (t0 = t("feedback.attachment.label"), $[0] = t, $[1] = t0);
		let t1;
		$[2] === Symbol.for("react.memo_cache_sentinel") ? (t1 = { cursor: "pointer" }, $[2] = t1) : t1 = $[2];
		let t2;
		return $[3] !== onExpand || $[4] !== t0 ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			mode: "bleed",
			tone: "primary",
			icon: UploadIcon,
			text: t0,
			onClick: onExpand,
			style: t1
		}) }), $[3] = onExpand, $[4] = t0, $[5] = t2) : t2 = $[5], t2;
	}
	if (imageFile) {
		let t0;
		$[6] === t ? t0 = $[7] : (t0 = t("feedback.attachment.label"), $[6] = t, $[7] = t0);
		let t1;
		$[8] === t0 ? t1 = $[9] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			weight: "medium",
			children: t0
		}), $[8] = t0, $[9] = t1);
		let t2;
		$[10] === imageFile.name ? t2 = $[11] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			muted: !0,
			children: imageFile.name
		}), $[10] = imageFile.name, $[11] = t2);
		let t3;
		$[12] === t ? t3 = $[13] : (t3 = t("feedback.attachment.remove"), $[12] = t, $[13] = t3);
		let t4;
		$[14] !== onRemove || $[15] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			mode: "bleed",
			tone: "critical",
			text: t3,
			onClick: onRemove
		}), $[14] = onRemove, $[15] = t3, $[16] = t4) : t4 = $[16];
		let t5;
		$[17] !== t2 || $[18] !== t4 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
			padding: 3,
			radius: 2,
			border: !0,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
				align: "center",
				justify: "space-between",
				children: [t2, t4]
			})
		}), $[17] = t2, $[18] = t4, $[19] = t5) : t5 = $[19];
		let t6;
		return $[20] !== t1 || $[21] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 3,
			children: [t1, t5]
		}), $[20] = t1, $[21] = t5, $[22] = t6) : t6 = $[22], t6;
	}
	let t0;
	$[23] === t ? t0 = $[24] : (t0 = t("feedback.attachment.label"), $[23] = t, $[24] = t0);
	let t1;
	$[25] === t0 ? t1 = $[26] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		weight: "medium",
		children: t0
	}), $[25] = t0, $[26] = t1);
	let t2 = dragOver ? "primary" : "default", t3;
	$[27] === Symbol.for("react.memo_cache_sentinel") ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		muted: !0,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BinaryDocumentIcon, {})
	}), $[27] = t3) : t3 = $[27];
	let t4;
	$[28] === t ? t4 = $[29] : (t4 = t("feedback.attachment.drop-zone"), $[28] = t, $[29] = t4);
	let t5;
	$[30] === t4 ? t5 = $[31] : (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		gap: 2,
		children: [t3, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			muted: !0,
			children: t4
		})]
	}), $[30] = t4, $[31] = t5);
	let t6;
	$[32] === t ? t6 = $[33] : (t6 = t("feedback.attachment.browse"), $[32] = t, $[33] = t6);
	let t7;
	$[34] !== onFiles || $[35] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FileInputButton, {
		mode: "ghost",
		text: t6,
		accept: "image/*",
		onSelect: onFiles
	}), $[34] = onFiles, $[35] = t6, $[36] = t7) : t7 = $[36];
	let t8;
	$[37] !== t5 || $[38] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		justify: "space-between",
		children: [t5, t7]
	}), $[37] = t5, $[38] = t7, $[39] = t8) : t8 = $[39];
	let t9;
	$[40] !== onFiles || $[41] !== onFilesOut || $[42] !== onFilesOver || $[43] !== t2 || $[44] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FileTargetCard, {
		padding: 3,
		radius: 2,
		border: !0,
		tone: t2,
		onFiles,
		onFilesOver,
		onFilesOut,
		children: t8
	}), $[40] = onFiles, $[41] = onFilesOut, $[42] = onFilesOver, $[43] = t2, $[44] = t8, $[45] = t9) : t9 = $[45];
	let t10;
	$[46] === error ? t10 = $[47] : (t10 = error && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		style: { color: "var(--card-badge-critical-fg-color)" },
		children: error
	}), $[46] = error, $[47] = t10);
	let t11;
	return $[48] !== t1 || $[49] !== t10 || $[50] !== t9 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 3,
		children: [
			t1,
			t9,
			t10
		]
	}), $[48] = t1, $[49] = t10, $[50] = t9, $[51] = t11) : t11 = $[51], t11;
}
var SENTIMENTS = [
	{
		value: "happy",
		icon: FaceHappyIcon,
		labelKey: "feedback.sentiment.happy"
	},
	{
		value: "neutral",
		icon: FaceIndifferentIcon,
		labelKey: "feedback.sentiment.neutral"
	},
	{
		value: "unhappy",
		icon: FaceSadIcon,
		labelKey: "feedback.sentiment.unhappy"
	}
];
var MAX_FILE_SIZE = 20971520;
/** @internal */
function FeedbackDialog(props) {
	let $ = (0, import_compiler_runtime.c)(101), { onClose, dsn, feedbackVersion, source, extraTags, title: dialogTitle, sentimentLabel, userName: userNameProp, userEmail: userEmailProp, onSuccess, onError } = props, dialogId = (0, import_react.useId)(), messageId = (0, import_react.useId)(), contactConsentId = (0, import_react.useId)(), { t } = useFeedbackTranslation(), { telemetryConsent, userName: contextUserName, userEmail: contextUserEmail, tags } = (0, import_react.useContext)(FeedbackContext), resolvedName = userNameProp ?? contextUserName, resolvedEmail = userEmailProp ?? contextUserEmail, [sentiment, setSentiment] = (0, import_react.useState)(null), [message, setMessage] = (0, import_react.useState)(""), [contactConsent, setContactConsent] = (0, import_react.useState)(!1), [imageFile, setImageFile] = (0, import_react.useState)(null), [attachmentError, setAttachmentError] = (0, import_react.useState)(null), [showAttachment, setShowAttachment] = (0, import_react.useState)(!1), [submitting, setSubmitting] = (0, import_react.useState)(!1), [dragOver, setDragOver] = (0, import_react.useState)(!1), { feedbackDialogDismissed } = useFeedbackTelemetry(), t0;
	$[0] !== feedbackDialogDismissed || $[1] !== onClose ? (t0 = () => {
		feedbackDialogDismissed(), onClose();
	}, $[0] = feedbackDialogDismissed, $[1] = onClose, $[2] = t0) : t0 = $[2];
	let handleDismiss = t0, t1;
	$[3] === Symbol.for("react.memo_cache_sentinel") ? (t1 = (event) => {
		setMessage(event.currentTarget.value);
	}, $[3] = t1) : t1 = $[3];
	let handleMessageChange = t1, t2;
	$[4] === t ? t2 = $[5] : (t2 = (files) => {
		let img = files.find(_temp$54);
		if (img) {
			if (img.size > MAX_FILE_SIZE) {
				setAttachmentError(t("feedback.attachment.error.size"));
				return;
			}
			setAttachmentError(null), setImageFile(img);
		}
	}, $[4] = t, $[5] = t2);
	let handleFiles = t2, t3;
	$[6] === Symbol.for("react.memo_cache_sentinel") ? (t3 = () => setDragOver(!0), $[6] = t3) : t3 = $[6];
	let handleFilesOver = t3, t4;
	$[7] === Symbol.for("react.memo_cache_sentinel") ? (t4 = () => setDragOver(!1), $[7] = t4) : t4 = $[7];
	let handleFilesOut = t4, t5;
	$[8] === t ? t5 = $[9] : (t5 = (event_0) => {
		let items = event_0.clipboardData?.items;
		if (items) {
			for (let item of Array.from(items)) if (item.type.startsWith("image/")) {
				let file = item.getAsFile();
				if (file) {
					file.size > MAX_FILE_SIZE ? setAttachmentError(t("feedback.attachment.error.size")) : (setAttachmentError(null), setImageFile(file));
					break;
				}
			}
		}
	}, $[8] = t, $[9] = t5);
	let handlePaste = t5, t6;
	$[10] !== contactConsent || $[11] !== dsn || $[12] !== extraTags || $[13] !== feedbackVersion || $[14] !== imageFile || $[15] !== message || $[16] !== onClose || $[17] !== onError || $[18] !== onSuccess || $[19] !== resolvedEmail || $[20] !== resolvedName || $[21] !== sentiment || $[22] !== source || $[23] !== tags || $[24] !== telemetryConsent ? (t6 = async () => {
		let finalMessage = message ? message.trim() : `The user submitted a ${sentiment} sentiment rating without additional comments.`;
		if (sentiment) {
			setSubmitting(!0);
			try {
				let attachments = [];
				if (imageFile) {
					let buffer = await imageFile.arrayBuffer();
					attachments.push({
						filename: imageFile.name,
						data: new Uint8Array(buffer)
					});
				}
				await sendFeedbackToSentry({
					dsn,
					feedbackVersion,
					telemetryConsent,
					name: resolvedName,
					email: resolvedEmail,
					message: finalMessage,
					source,
					tags: {
						...tags,
						...extraTags,
						sentiment,
						contactConsent: String(contactConsent)
					},
					attachments
				}), setSubmitting(!1), onSuccess && onSuccess(), onClose();
			} catch (t7) {
				let err = t7;
				setSubmitting(!1), onError && onError(err instanceof Error ? err : Error(String(err)));
			}
		}
	}, $[10] = contactConsent, $[11] = dsn, $[12] = extraTags, $[13] = feedbackVersion, $[14] = imageFile, $[15] = message, $[16] = onClose, $[17] = onError, $[18] = onSuccess, $[19] = resolvedEmail, $[20] = resolvedName, $[21] = sentiment, $[22] = source, $[23] = tags, $[24] = telemetryConsent, $[25] = t6) : t6 = $[25];
	let handleSubmit = t6, t7;
	$[26] !== dialogTitle || $[27] !== t ? (t7 = dialogTitle ?? t("feedback.dialog.title"), $[26] = dialogTitle, $[27] = t, $[28] = t7) : t7 = $[28];
	let t8;
	$[29] !== sentimentLabel || $[30] !== t ? (t8 = sentimentLabel ?? t("feedback.sentiment.label"), $[29] = sentimentLabel, $[30] = t, $[31] = t8) : t8 = $[31];
	let t9;
	$[32] === t8 ? t9 = $[33] : (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		weight: "medium",
		children: t8
	}), $[32] = t8, $[33] = t9);
	let t10;
	$[34] !== sentiment || $[35] !== t ? (t10 = SENTIMENTS.map((option) => {
		let Icon = option.icon, isSelected = sentiment === option.value;
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			mode: isSelected ? "default" : "bleed",
			tone: isSelected ? "primary" : "default",
			onClick: () => setSentiment(option.value),
			icon: Icon,
			text: t(option.labelKey),
			style: { cursor: "pointer" }
		}, option.value);
	}), $[34] = sentiment, $[35] = t, $[36] = t10) : t10 = $[36];
	let t11;
	$[37] === t10 ? t11 = $[38] : (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		gap: 2,
		children: t10
	}), $[37] = t10, $[38] = t11);
	let t12;
	$[39] !== t11 || $[40] !== t9 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 2,
		children: [t9, t11]
	}), $[39] = t11, $[40] = t9, $[41] = t12) : t12 = $[41];
	let t13;
	$[42] === t ? t13 = $[43] : (t13 = t("feedback.message.label"), $[42] = t, $[43] = t13);
	let t14;
	$[44] !== messageId || $[45] !== t13 ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "label",
		htmlFor: messageId,
		size: 1,
		weight: "medium",
		children: t13
	}), $[44] = messageId, $[45] = t13, $[46] = t14) : t14 = $[46];
	let t15;
	$[47] === t ? t15 = $[48] : (t15 = t("feedback.message.placeholder"), $[47] = t, $[48] = t15);
	let t16;
	$[49] !== message || $[50] !== messageId || $[51] !== t15 ? (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextArea, {
		id: messageId,
		fontSize: 1,
		rows: 4,
		value: message,
		onChange: handleMessageChange,
		placeholder: t15
	}), $[49] = message, $[50] = messageId, $[51] = t15, $[52] = t16) : t16 = $[52];
	let t17, t18;
	$[53] === Symbol.for("react.memo_cache_sentinel") ? (t17 = () => {
		setImageFile(null), setAttachmentError(null);
	}, t18 = () => setShowAttachment(!0), $[53] = t17, $[54] = t18) : (t17 = $[53], t18 = $[54]);
	let t19;
	$[55] !== attachmentError || $[56] !== dragOver || $[57] !== handleFiles || $[58] !== imageFile || $[59] !== showAttachment ? (t19 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ImageAttachment, {
		imageFile,
		showAttachment,
		dragOver,
		error: attachmentError,
		onFiles: handleFiles,
		onFilesOver: handleFilesOver,
		onFilesOut: handleFilesOut,
		onRemove: t17,
		onExpand: t18
	}), $[55] = attachmentError, $[56] = dragOver, $[57] = handleFiles, $[58] = imageFile, $[59] = showAttachment, $[60] = t19) : t19 = $[60];
	let t20;
	$[61] !== t14 || $[62] !== t16 || $[63] !== t19 ? (t20 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 3,
		children: [
			t14,
			t16,
			t19
		]
	}), $[61] = t14, $[62] = t16, $[63] = t19, $[64] = t20) : t20 = $[64];
	let t21;
	$[65] !== contactConsent || $[66] !== contactConsentId || $[67] !== imageFile || $[68] !== message || $[69] !== resolvedEmail || $[70] !== resolvedName || $[71] !== t ? (t21 = (message.trim() || imageFile) && (resolvedName || resolvedEmail) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 4,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 3,
			paddingRight: 3,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				as: "label",
				htmlFor: contactConsentId,
				size: 1,
				weight: "medium",
				children: t("feedback.consent.label")
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				muted: !0,
				children: t("feedback.consent.disclaimer")
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			align: "center",
			gap: 2,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Switch, {
				id: contactConsentId,
				checked: contactConsent,
				onChange: () => setContactConsent(_temp2$21)
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				muted: !0,
				children: t(contactConsent ? "feedback.consent.yes" : "feedback.consent.no")
			})]
		})]
	}), $[65] = contactConsent, $[66] = contactConsentId, $[67] = imageFile, $[68] = message, $[69] = resolvedEmail, $[70] = resolvedName, $[71] = t, $[72] = t21) : t21 = $[72];
	let t22;
	$[73] !== t12 || $[74] !== t20 || $[75] !== t21 ? (t22 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 5,
		children: [
			t12,
			t20,
			t21
		]
	}), $[73] = t12, $[74] = t20, $[75] = t21, $[76] = t22) : t22 = $[76];
	let t23;
	$[77] !== handlePaste || $[78] !== t22 ? (t23 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		paddingX: 4,
		paddingY: 5,
		borderTop: !0,
		onPaste: handlePaste,
		children: t22
	}), $[77] = handlePaste, $[78] = t22, $[79] = t23) : t23 = $[79];
	let t24;
	$[80] === t ? t24 = $[81] : (t24 = t("feedback.cancel"), $[80] = t, $[81] = t24);
	let t25;
	$[82] !== handleDismiss || $[83] !== t24 ? (t25 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		mode: "ghost",
		text: t24,
		onClick: handleDismiss
	}), $[82] = handleDismiss, $[83] = t24, $[84] = t25) : t25 = $[84];
	let t26;
	$[85] === t ? t26 = $[86] : (t26 = t("feedback.submit"), $[85] = t, $[86] = t26);
	let t27 = !sentiment || submitting, t28;
	$[87] !== handleSubmit || $[88] !== submitting || $[89] !== t26 || $[90] !== t27 ? (t28 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		tone: "primary",
		text: t26,
		onClick: handleSubmit,
		disabled: t27,
		loading: submitting
	}), $[87] = handleSubmit, $[88] = submitting, $[89] = t26, $[90] = t27, $[91] = t28) : t28 = $[91];
	let t29;
	$[92] !== t25 || $[93] !== t28 ? (t29 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		padding: 3,
		borderTop: !0,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			gap: 2,
			justify: "flex-end",
			children: [t25, t28]
		})
	}), $[92] = t25, $[93] = t28, $[94] = t29) : t29 = $[94];
	let t30;
	return $[95] !== dialogId || $[96] !== handleDismiss || $[97] !== t23 || $[98] !== t29 || $[99] !== t7 ? (t30 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Dialog$1, {
		id: dialogId,
		header: t7,
		onClose: handleDismiss,
		onClickOutside: handleDismiss,
		width: 1,
		padding: !1,
		children: [t23, t29]
	}), $[95] = dialogId, $[96] = handleDismiss, $[97] = t23, $[98] = t29, $[99] = t7, $[100] = t30) : t30 = $[100], t30;
}
function _temp2$21(prev) {
	return !prev;
}
function _temp$54(f) {
	return f.type.startsWith("image/");
}
function StudioFeedbackDialogInner(props) {
	let $ = (0, import_compiler_runtime.c)(10), toast = useToast(), { t } = useTranslation$1(feedbackLocaleNamespace), t0;
	$[0] !== t || $[1] !== toast ? (t0 = () => {
		toast.push({
			status: "success",
			title: t("feedback.success"),
			closable: !0
		});
	}, $[0] = t, $[1] = toast, $[2] = t0) : t0 = $[2];
	let handleSuccess = t0, t1;
	$[3] !== t || $[4] !== toast ? (t1 = (err) => {
		toast.push({
			status: "warning",
			title: t("feedback.error"),
			description: err.message,
			closable: !0
		});
	}, $[3] = t, $[4] = toast, $[5] = t1) : t1 = $[5];
	let handleError = t1, t2;
	return $[6] !== handleError || $[7] !== handleSuccess || $[8] !== props ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FeedbackDialog, {
		...props,
		onSuccess: handleSuccess,
		onError: handleError
	}), $[6] = handleError, $[7] = handleSuccess, $[8] = props, $[9] = t2) : t2 = $[9], t2;
}
/**
* Studio-aware wrapper around {@link FeedbackDialog}.
*
* Wraps the dialog with a {@link StudioFeedbackProvider} so that
* telemetry consent, user info, and studio tags are supplied
* automatically from studio context hooks.
*
* Shows toast notifications on success/error via the studio's ToastProvider.
*
* @internal
*/
function StudioFeedbackDialog(props) {
	let $ = (0, import_compiler_runtime.c)(2), t0;
	return $[0] === props ? t0 = $[1] : (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioFeedbackProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioFeedbackDialogInner, { ...props }) }), $[0] = props, $[1] = t0), t0;
}
var CopyToDraftsDialog = (0, import_react.memo)(function CopyToDraftsDialog(props) {
	let $ = (0, import_compiler_runtime.c)(26), { onClose, onConfirm } = props, { t: tReleases } = useTranslation$1(releasesLocaleNamespace), [isProcessing, startTransition] = (0, import_react.useTransition)(), t0;
	$[0] !== onClose || $[1] !== onConfirm ? (t0 = () => startTransition(async () => {
		await onConfirm({ shouldConfirmDraftDiscard: !1 }), onClose();
	}), $[0] = onClose, $[1] = onConfirm, $[2] = t0) : t0 = $[2];
	let handleConfirm = t0, t1;
	$[3] === tReleases ? t1 = $[4] : (t1 = tReleases("copy-to-draft-dialog.title"), $[3] = tReleases, $[4] = t1);
	let t2;
	$[5] === tReleases ? t2 = $[6] : (t2 = tReleases("copy-to-draft-dialog.confirm-button"), $[5] = tReleases, $[6] = t2);
	let t3;
	$[7] !== handleConfirm || $[8] !== isProcessing || $[9] !== t2 ? (t3 = {
		text: t2,
		onClick: handleConfirm,
		loading: isProcessing,
		disabled: isProcessing
	}, $[7] = handleConfirm, $[8] = isProcessing, $[9] = t2, $[10] = t3) : t3 = $[10];
	let t4;
	$[11] !== isProcessing || $[12] !== onClose ? (t4 = {
		onClick: onClose,
		disabled: isProcessing
	}, $[11] = isProcessing, $[12] = onClose, $[13] = t4) : t4 = $[13];
	let t5;
	$[14] !== t3 || $[15] !== t4 ? (t5 = {
		confirmButton: t3,
		cancelButton: t4
	}, $[14] = t3, $[15] = t4, $[16] = t5) : t5 = $[16];
	let t6;
	$[17] === tReleases ? t6 = $[18] : (t6 = tReleases("copy-to-draft-dialog.description"), $[17] = tReleases, $[18] = t6);
	let t7;
	$[19] === t6 ? t7 = $[20] : (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		children: t6
	}), $[19] = t6, $[20] = t7);
	let t8;
	return $[21] !== onClose || $[22] !== t1 || $[23] !== t5 || $[24] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog$1, {
		id: "copy-to-drafts-dialog",
		header: t1,
		onClose,
		footer: t5,
		children: t7
	}), $[21] = onClose, $[22] = t1, $[23] = t5, $[24] = t7, $[25] = t8) : t8 = $[25], t8;
});
function CopyToNewReleaseDialog(props) {
	let $ = (0, import_compiler_runtime.c)(79), { onClose, versionId, documentType, release: sourceRelease, title, onCreateVersion } = props, { t } = useTranslation$1(), toast = useToast(), createReleaseMetadata = useCreateReleaseMetadata(), { releasePromise } = useGuardWithReleaseLimitUpsell(), schema = useSchema(), t0;
	$[0] !== documentType || $[1] !== schema ? (t0 = schema.get(documentType), $[0] = documentType, $[1] = schema, $[2] = t0) : t0 = $[2];
	let schemaType = t0, [newReleaseId] = (0, import_react.useState)(createReleaseId), t1;
	$[3] === newReleaseId ? t1 = $[4] : (t1 = () => ({
		_id: newReleaseId,
		metadata: {
			title: "",
			description: "",
			releaseType: DEFAULT_RELEASE_TYPE
		}
	}), $[3] = newReleaseId, $[4] = t1);
	let [release, setRelease] = (0, import_react.useState)(t1), [isSubmitting, setIsSubmitting] = (0, import_react.useState)(!1), [, setRerenderDialog] = (0, import_react.useState)(0), telemetry = useTelemetry(), { createRelease } = useReleaseOperations(), { clearReleaseDataFromStorage } = useReleaseFormStorage(), t2;
	$[5] !== t || $[6] !== title ? (t2 = title || t("release.placeholder-untitled-release"), $[5] = t, $[6] = title, $[7] = t2) : t2 = $[7];
	let displayTitle = t2, t3;
	$[8] === release ? t3 = $[9] : (t3 = getIsScheduledDateInPast(release), $[8] = release, $[9] = t3);
	let isScheduledDateInPast = t3, t4;
	$[10] === release ? t4 = $[11] : (t4 = getIsReleaseInvalid(release), $[10] = release, $[11] = t4);
	let invalid = t4, t5;
	$[12] === Symbol.for("react.memo_cache_sentinel") ? (t5 = (releaseMetadata) => {
		setRelease(releaseMetadata);
	}, $[12] = t5) : t5 = $[12];
	let handleOnChange = t5, t6;
	$[13] !== newReleaseId || $[14] !== onCreateVersion ? (t6 = () => {
		onCreateVersion(newReleaseId);
	}, $[13] = newReleaseId, $[14] = onCreateVersion, $[15] = t6) : t6 = $[15];
	let handleAddVersion = t6, t7;
	$[16] !== clearReleaseDataFromStorage || $[17] !== createRelease || $[18] !== createReleaseMetadata || $[19] !== handleAddVersion || $[20] !== onClose || $[21] !== release || $[22] !== releasePromise || $[23] !== setRerenderDialog || $[24] !== t || $[25] !== telemetry || $[26] !== toast ? (t7 = async () => {
		if (getIsScheduledDateInPast(release)) {
			setRerenderDialog(_temp$53);
			return;
		}
		if (setIsSubmitting(!0), !await releasePromise) {
			setIsSubmitting(!1);
			return;
		}
		try {
			let releaseValue = createReleaseMetadata(release);
			await createRelease(releaseValue), handleAddVersion(), telemetry.log(CreatedRelease, { origin: "document-panel" });
		} catch (t8) {
			let err = t8;
			isReleaseLimitError(err) ? onClose() : (console.error(err), toast.push({
				closable: !0,
				status: "error",
				title: t("release.toast.create-release-error.title"),
				description: err.message
			}));
		}
		setIsSubmitting(!1), clearReleaseDataFromStorage();
	}, $[16] = clearReleaseDataFromStorage, $[17] = createRelease, $[18] = createReleaseMetadata, $[19] = handleAddVersion, $[20] = onClose, $[21] = release, $[22] = releasePromise, $[23] = setRerenderDialog, $[24] = t, $[25] = telemetry, $[26] = toast, $[27] = t7) : t7 = $[27];
	let handleCreateRelease = t7, t8;
	$[28] !== clearReleaseDataFromStorage || $[29] !== onClose ? (t8 = () => {
		clearReleaseDataFromStorage(), onClose();
	}, $[28] = clearReleaseDataFromStorage, $[29] = onClose, $[30] = t8) : t8 = $[30];
	let handleOnClose = t8, t9;
	$[31] === t ? t9 = $[32] : (t9 = t("release.dialog.copy-to-release.title"), $[31] = t, $[32] = t9);
	let t10;
	$[33] === Symbol.for("react.memo_cache_sentinel") ? (t10 = { borderBottom: "1px solid var(--card-border-color)" }, $[33] = t10) : t10 = $[33];
	let t11;
	$[34] !== schemaType || $[35] !== versionId ? (t11 = schemaType ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Preview, {
		value: { _id: versionId },
		schemaType
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingBlock, {}), $[34] = schemaType, $[35] = versionId, $[36] = t11) : t11 = $[36];
	let t12;
	$[37] === Symbol.for("react.memo_cache_sentinel") ? (t12 = {
		borderRadius: 999,
		border: "1px solid var(--card-border-color)",
		whiteSpace: "nowrap",
		overflow: "hidden",
		textOverflow: "ellipsis"
	}, $[37] = t12) : t12 = $[37];
	let t13;
	$[38] === sourceRelease ? t13 = $[39] : (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseAvatar, {
		padding: 1,
		release: sourceRelease
	}), $[38] = sourceRelease, $[39] = t13);
	let t14;
	$[40] === displayTitle ? t14 = $[41] : (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		title: displayTitle,
		children: displayTitle
	}), $[40] = displayTitle, $[41] = t14);
	let t15;
	$[42] !== t13 || $[43] !== t14 ? (t15 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		gap: 2,
		padding: 1,
		paddingRight: 2,
		style: t12,
		children: [t13, t14]
	}), $[42] = t13, $[43] = t14, $[44] = t15) : t15 = $[44];
	let t16;
	$[45] !== t11 || $[46] !== t15 ? (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingX: 2,
		marginBottom: 2,
		style: t10,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			align: "center",
			padding: 4,
			paddingTop: 1,
			justify: "space-between",
			children: [t11, t15]
		})
	}), $[45] = t11, $[46] = t15, $[47] = t16) : t16 = $[47];
	let t17;
	$[48] !== isScheduledDateInPast || $[49] !== t ? (t17 = isScheduledDateInPast && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		padding: 3,
		marginBottom: 3,
		radius: 2,
		shadow: 1,
		tone: "critical",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			children: t("release.schedule-dialog.publish-date-in-past-warning")
		})
	}), $[48] = isScheduledDateInPast, $[49] = t, $[50] = t17) : t17 = $[50];
	let t18;
	$[51] === release ? t18 = $[52] : (t18 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseForm, {
		onChange: handleOnChange,
		value: release
	}), $[51] = release, $[52] = t18);
	let t19;
	$[53] === t ? t19 = $[54] : (t19 = t("common.dialog.cancel-button.text"), $[53] = t, $[54] = t19);
	let t20;
	$[55] !== isSubmitting || $[56] !== onClose || $[57] !== t19 ? (t20 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		disabled: isSubmitting,
		text: t19,
		"data-testid": "cancel-button",
		onClick: onClose,
		mode: "bleed"
	}), $[55] = isSubmitting, $[56] = onClose, $[57] = t19, $[58] = t20) : t20 = $[58];
	let t21 = isSubmitting || isScheduledDateInPast || invalid, t22;
	$[59] === t ? t22 = $[60] : (t22 = t("release.action.add-to-new-release"), $[59] = t, $[60] = t22);
	let t23;
	$[61] !== handleCreateRelease || $[62] !== isSubmitting || $[63] !== t21 || $[64] !== t22 ? (t23 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		disabled: t21,
		type: "submit",
		onClick: handleCreateRelease,
		text: t22,
		loading: isSubmitting,
		tone: "primary",
		"data-testid": "confirm-button"
	}), $[61] = handleCreateRelease, $[62] = isSubmitting, $[63] = t21, $[64] = t22, $[65] = t23) : t23 = $[65];
	let t24;
	$[66] !== t20 || $[67] !== t23 ? (t24 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		gap: 3,
		justify: "flex-end",
		paddingTop: 3,
		align: "center",
		children: [t20, t23]
	}), $[66] = t20, $[67] = t23, $[68] = t24) : t24 = $[68];
	let t25;
	$[69] !== t17 || $[70] !== t18 || $[71] !== t24 ? (t25 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Box, {
		paddingX: 5,
		paddingY: 3,
		children: [
			t17,
			t18,
			t24
		]
	}), $[69] = t17, $[70] = t18, $[71] = t24, $[72] = t25) : t25 = $[72];
	let t26;
	return $[73] !== handleOnClose || $[74] !== onClose || $[75] !== t16 || $[76] !== t25 || $[77] !== t9 ? (t26 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Dialog$1, {
		id: "create-release-dialog",
		header: t9,
		onClickOutside: onClose,
		onClose: handleOnClose,
		padding: !1,
		width: 1,
		children: [t16, t25]
	}), $[73] = handleOnClose, $[74] = onClose, $[75] = t16, $[76] = t25, $[77] = t9, $[78] = t26) : t26 = $[78], t26;
}
function _temp$53(cur) {
	return cur + 1;
}
/**
* Renders the dialog (if any) opened from the version context menu. Use
* together with `useVersionContextMenu` and `VersionContextMenuPopover`.
*
* @internal
*/
var VersionContextMenuDialogs = (0, import_react.memo)(function VersionContextMenuDialogs(props) {
	let $ = (0, import_compiler_runtime.c)(25), { dialogState, onClose, documentType, versionId, title, sourceReleasePerspective, onCreateVersion, onCopyToDrafts, isGoingToUnpublish: t0, isDiscardable: t1, scheduledDraftDialogs } = props, isGoingToUnpublish = t0 !== void 0 && t0, isDiscardable = t1 === void 0 || t1, t2;
	$[0] !== dialogState || $[1] !== documentType || $[2] !== isDiscardable || $[3] !== isGoingToUnpublish || $[4] !== onClose || $[5] !== title || $[6] !== versionId ? (t2 = dialogState === "discard-version" && isDiscardable && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DiscardVersionDialog, {
		onClose,
		versionId,
		fromPerspective: title,
		documentType,
		isGoingToUnpublish
	}), $[0] = dialogState, $[1] = documentType, $[2] = isDiscardable, $[3] = isGoingToUnpublish, $[4] = onClose, $[5] = title, $[6] = versionId, $[7] = t2) : t2 = $[7];
	let t3;
	$[8] !== dialogState || $[9] !== documentType || $[10] !== onClose || $[11] !== onCreateVersion || $[12] !== sourceReleasePerspective || $[13] !== title || $[14] !== versionId ? (t3 = dialogState === "create-release" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CopyToNewReleaseDialog, {
		onClose,
		onCreateVersion,
		versionId,
		documentType,
		release: sourceReleasePerspective,
		title
	}), $[8] = dialogState, $[9] = documentType, $[10] = onClose, $[11] = onCreateVersion, $[12] = sourceReleasePerspective, $[13] = title, $[14] = versionId, $[15] = t3) : t3 = $[15];
	let t4;
	$[16] !== dialogState || $[17] !== onClose || $[18] !== onCopyToDrafts ? (t4 = dialogState === "copy-to-drafts" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CopyToDraftsDialog, {
		onClose,
		onConfirm: onCopyToDrafts
	}), $[16] = dialogState, $[17] = onClose, $[18] = onCopyToDrafts, $[19] = t4) : t4 = $[19];
	let t5;
	return $[20] !== scheduledDraftDialogs || $[21] !== t2 || $[22] !== t3 || $[23] !== t4 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		t2,
		t3,
		t4,
		scheduledDraftDialogs
	] }), $[20] = scheduledDraftDialogs, $[21] = t2, $[22] = t3, $[23] = t4, $[24] = t5) : t5 = $[24], t5;
});
var useHasCopyToDraftOption = (documentType, fromRelease) => {
	let $ = (0, import_compiler_runtime.c)(3), { document } = useWorkspace(), schema = useSchema(), t0;
	$[0] !== documentType || $[1] !== schema ? (t0 = schema.get(documentType), $[0] = documentType, $[1] = schema, $[2] = t0) : t0 = $[2];
	let isLiveEdit = t0?.liveEdit;
	return document?.drafts?.enabled && fromRelease !== "draft" && fromRelease !== "published" && !isLiveEdit;
};
var CopyToDraftsMenuItem = (0, import_react.memo)(function CopyToDraftsMenuItem(props) {
	let $ = (0, import_compiler_runtime.c)(7), { documentType, fromRelease, onClick } = props, { t } = useTranslation$1();
	if (!useHasCopyToDraftOption(documentType, fromRelease)) return null;
	let t0;
	$[0] === onClick ? t0 = $[1] : (t0 = () => void onClick({ shouldConfirmDraftDiscard: !0 }), $[0] = onClick, $[1] = t0);
	let t1;
	$[2] === t ? t1 = $[3] : (t1 = () => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		gap: 3,
		align: "center",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			flex: "none",
			paddingX: 2,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseAvatar, {
				padding: 0,
				release: LATEST
			})
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			weight: "medium",
			children: t("release.chip.draft")
		})]
	}), $[2] = t, $[3] = t1);
	let t2;
	return $[4] !== t0 || $[5] !== t1 ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
		as: "a",
		onClick: t0,
		"data-testid": "copy-to-drafts-menu-item",
		renderMenuItem: t1
	}), $[4] = t0, $[5] = t1, $[6] = t2) : t2 = $[6], t2;
});
var VersionContextMenuItem = (0, import_react.memo)(function VersionContextMenuItem(props) {
	let $ = (0, import_compiler_runtime.c)(33), { release } = props, { t } = useTranslation$1(), formatPublishDate = useFormatRelativeLocalePublishDate(), t0;
	$[0] === release ? t0 = $[1] : (t0 = isReleaseScheduledOrScheduling(release), $[0] = release, $[1] = t0);
	let isScheduled = t0, t1;
	$[2] === release ? t1 = $[3] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseAvatar, {
		padding: 2,
		release
	}), $[2] = release, $[3] = t1);
	let t2 = release.metadata?.title, t3;
	$[4] === t ? t3 = $[5] : (t3 = t("release.placeholder-untitled-release"), $[4] = t, $[5] = t3);
	let t4;
	$[6] === Symbol.for("react.memo_cache_sentinel") ? (t4 = {
		size: 1,
		weight: "medium"
	}, $[6] = t4) : t4 = $[6];
	let t5;
	$[7] !== t2 || $[8] !== t3 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseTitle, {
		title: t2,
		fallback: t3,
		textProps: t4
	}), $[7] = t2, $[8] = t3, $[9] = t5) : t5 = $[9];
	let t6;
	$[10] !== release.metadata.releaseType || $[11] !== t ? (t6 = release.metadata.releaseType === "asap" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: t("release.type.asap") }), $[10] = release.metadata.releaseType, $[11] = t, $[12] = t6) : t6 = $[12];
	let t7;
	$[13] !== formatPublishDate || $[14] !== release || $[15] !== t ? (t7 = release.metadata.releaseType === "scheduled" && (release.metadata.intendedPublishAt ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: formatPublishDate(release) }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: t("release.chip.tooltip.unknown-date") })), $[13] = formatPublishDate, $[14] = release, $[15] = t, $[16] = t7) : t7 = $[16];
	let t8;
	$[17] !== release.metadata.releaseType || $[18] !== t ? (t8 = release.metadata.releaseType === "undecided" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: t("release.type.undecided") }), $[17] = release.metadata.releaseType, $[18] = t, $[19] = t8) : t8 = $[19];
	let t9;
	$[20] !== t6 || $[21] !== t7 || $[22] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
		muted: !0,
		size: 1,
		children: [
			t6,
			t7,
			t8
		]
	}), $[20] = t6, $[21] = t7, $[22] = t8, $[23] = t9) : t9 = $[23];
	let t10;
	$[24] !== t5 || $[25] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		flex: 1,
		gap: 2,
		children: [t5, t9]
	}), $[24] = t5, $[25] = t9, $[26] = t10) : t10 = $[26];
	let t11;
	$[27] === isScheduled ? t11 = $[28] : (t11 = isScheduled && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LockIcon, {}), $[27] = isScheduled, $[28] = t11);
	let t12;
	return $[29] !== t1 || $[30] !== t10 || $[31] !== t11 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		gap: 3,
		justify: "center",
		align: "center",
		children: [
			t1,
			t10,
			t11
		]
	}), $[29] = t1, $[30] = t10, $[31] = t11, $[32] = t12) : t12 = $[32], t12;
});
var ReleasesList = gt(Stack).withConfig({
	displayName: "ReleasesList",
	componentId: "sc-ynga99-0"
})`max-width:300px;max-height:200px;overflow-y:auto;`;
var CopyToReleaseMenuGroup = (0, import_react.memo)(function CopyToReleaseMenuGroup(props) {
	let $ = (0, import_compiler_runtime.c)(29), { releases, bundleId, onCreateRelease, onCopyToDrafts, onCreateVersion, disabled, isReleasesEnabled, hasCreatePermission, hasCopyToDraftOption, documentType } = props, { t } = useTranslation$1(), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
		placement: "right-start",
		fallbackPlacements: ["left-start"]
	}, $[0] = t0) : t0 = $[0];
	let t1;
	$[1] === t ? t1 = $[2] : (t1 = t("release.action.copy-to"), $[1] = t, $[2] = t1);
	let t2 = hasCreatePermission === !0, t3;
	$[3] === t ? t3 = $[4] : (t3 = t("release.action.permission.error"), $[3] = t, $[4] = t3);
	let t4;
	$[5] !== t2 || $[6] !== t3 ? (t4 = {
		disabled: t2,
		content: t3
	}, $[5] = t2, $[6] = t3, $[7] = t4) : t4 = $[7];
	let t5;
	$[8] !== bundleId || $[9] !== documentType || $[10] !== hasCopyToDraftOption || $[11] !== onCopyToDrafts || $[12] !== onCreateVersion || $[13] !== releases ? (t5 = (hasCopyToDraftOption || releases.length > 0) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ReleasesList, {
		gap: 1,
		children: [hasCopyToDraftOption && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CopyToDraftsMenuItem, {
			documentType,
			fromRelease: bundleId,
			onClick: onCopyToDrafts
		}), releases.map((targetRelease) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
			as: "a",
			onClick: () => onCreateVersion(targetRelease._id),
			renderMenuItem: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VersionContextMenuItem, { release: targetRelease })
		}, targetRelease._id))]
	}, bundleId), $[8] = bundleId, $[9] = documentType, $[10] = hasCopyToDraftOption, $[11] = onCopyToDrafts, $[12] = onCreateVersion, $[13] = releases, $[14] = t5) : t5 = $[14];
	let t6;
	$[15] !== hasCopyToDraftOption || $[16] !== isReleasesEnabled || $[17] !== releases ? (t6 = isReleasesEnabled && (hasCopyToDraftOption || releases.length > 0) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuDivider, {}), $[15] = hasCopyToDraftOption, $[16] = isReleasesEnabled, $[17] = releases, $[18] = t6) : t6 = $[18];
	let t7;
	$[19] !== isReleasesEnabled || $[20] !== onCreateRelease ? (t7 = isReleasesEnabled && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CreateReleaseMenuItem, { onCreateRelease }), $[19] = isReleasesEnabled, $[20] = onCreateRelease, $[21] = t7) : t7 = $[21];
	let t8;
	return $[22] !== disabled || $[23] !== t1 || $[24] !== t4 || $[25] !== t5 || $[26] !== t6 || $[27] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(MenuGroup$1, {
		icon: CopyIcon,
		popover: t0,
		text: t1,
		disabled,
		tooltipProps: t4,
		"data-testid": "copy-version-to-release-button-group",
		children: [
			t5,
			t6,
			t7
		]
	}), $[22] = disabled, $[23] = t1, $[24] = t4, $[25] = t5, $[26] = t6, $[27] = t7, $[28] = t8) : t8 = $[28], t8;
});
var CanonicalReleaseContextMenu = (0, import_react.memo)(function CanonicalReleaseContextMenu(props) {
	let $ = (0, import_compiler_runtime.c)(40), { releases, releasesLoading, bundleId, onDiscard, onCreateRelease, onCopyToDrafts, onCreateVersion, disabled, locked, release, isGoingToUnpublish: t0, hasCreatePermission, hasDiscardPermission, isPublished, isDiscardable: t1, documentType } = props, isGoingToUnpublish = t0 !== void 0 && t0, isDiscardable = t1 === void 0 || t1, { t } = useTranslation$1(), hasCopyToDraftOption = useHasCopyToDraftOption(documentType, bundleId), isCopyToReleaseDisabled = disabled || !hasCreatePermission || isGoingToUnpublish, t2;
	$[0] === releases ? t2 = $[1] : (t2 = releases.filter(_temp$52), $[0] = releases, $[1] = t2);
	let copyToReleaseOptions = t2, isReleasesEnabled = !!useWorkspace().releases?.enabled, showCopyToReleaseMenuItem = isReleasesEnabled || hasCopyToDraftOption, t3;
	$[2] !== bundleId || $[3] !== disabled || $[4] !== release || $[5] !== t ? (t3 = release && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IntentLink, {
		intent: "release",
		params: { id: bundleId },
		rel: "noopener noreferrer",
		style: { textDecoration: "none" },
		disabled,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
			icon: CalendarIcon,
			text: t("release.action.view-release")
		})
	}), $[2] = bundleId, $[3] = disabled, $[4] = release, $[5] = t, $[6] = t3) : t3 = $[6];
	let t4;
	$[7] === releasesLoading ? t4 = $[8] : (t4 = releasesLoading && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Spinner, {}), $[7] = releasesLoading, $[8] = t4);
	let t5;
	$[9] !== bundleId || $[10] !== copyToReleaseOptions || $[11] !== documentType || $[12] !== hasCopyToDraftOption || $[13] !== hasCreatePermission || $[14] !== isCopyToReleaseDisabled || $[15] !== isReleasesEnabled || $[16] !== onCopyToDrafts || $[17] !== onCreateRelease || $[18] !== onCreateVersion || $[19] !== showCopyToReleaseMenuItem ? (t5 = showCopyToReleaseMenuItem && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CopyToReleaseMenuGroup, {
		releases: copyToReleaseOptions,
		hasCopyToDraftOption,
		isReleasesEnabled,
		bundleId,
		onCreateRelease,
		onCopyToDrafts,
		onCreateVersion,
		disabled: isCopyToReleaseDisabled,
		hasCreatePermission,
		documentType
	}), $[9] = bundleId, $[10] = copyToReleaseOptions, $[11] = documentType, $[12] = hasCopyToDraftOption, $[13] = hasCreatePermission, $[14] = isCopyToReleaseDisabled, $[15] = isReleasesEnabled, $[16] = onCopyToDrafts, $[17] = onCreateRelease, $[18] = onCreateVersion, $[19] = showCopyToReleaseMenuItem, $[20] = t5) : t5 = $[20];
	let t6;
	$[21] !== isDiscardable || $[22] !== isPublished || $[23] !== release || $[24] !== showCopyToReleaseMenuItem ? (t6 = !isPublished && isDiscardable && (showCopyToReleaseMenuItem || release) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuDivider, {}), $[21] = isDiscardable, $[22] = isPublished, $[23] = release, $[24] = showCopyToReleaseMenuItem, $[25] = t6) : t6 = $[25];
	let t7;
	$[26] !== disabled || $[27] !== hasDiscardPermission || $[28] !== isDiscardable || $[29] !== isPublished || $[30] !== locked || $[31] !== onDiscard || $[32] !== t ? (t7 = !isPublished && isDiscardable && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
		icon: TrashIcon,
		onClick: onDiscard,
		text: t("release.action.discard-version"),
		tone: "critical",
		disabled: disabled || locked || !hasDiscardPermission,
		tooltipProps: {
			disabled: hasDiscardPermission,
			content: t("release.action.permission.error")
		}
	}), $[26] = disabled, $[27] = hasDiscardPermission, $[28] = isDiscardable, $[29] = isPublished, $[30] = locked, $[31] = onDiscard, $[32] = t, $[33] = t7) : t7 = $[33];
	let t8;
	return $[34] !== t3 || $[35] !== t4 || $[36] !== t5 || $[37] !== t6 || $[38] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Menu, { children: [
		t3,
		t4,
		t5,
		t6,
		t7
	] }), $[34] = t3, $[35] = t4, $[36] = t5, $[37] = t6, $[38] = t7, $[39] = t8) : t8 = $[39], t8;
});
function _temp$52(r) {
	return !isReleaseScheduledOrScheduling(r);
}
var ScheduledDraftContextMenu = (0, import_react.memo)(function ScheduledDraftContextMenu(props) {
	let $ = (0, import_compiler_runtime.c)(34), { releases, bundleId, onCreateRelease, onCopyToDrafts, onCreateVersion, disabled, isGoingToUnpublish: t0, hasCreatePermission, scheduledDraftMenuActions, documentType, release } = props, isGoingToUnpublish = t0 !== void 0 && t0, { t } = useTranslation$1(), hasCopyToDraftOption = useHasCopyToDraftOption(documentType, bundleId), isCopyToReleaseDisabled = disabled || !hasCreatePermission || isGoingToUnpublish, t1;
	$[0] === releases ? t1 = $[1] : (t1 = releases.filter(_temp$51), $[0] = releases, $[1] = t1);
	let copyToReleaseOptions = t1, isReleasesEnabled = !!useWorkspace().releases?.enabled, showCopyToReleaseMenuItem = isReleasesEnabled && copyToReleaseOptions.length > 0, { actions } = scheduledDraftMenuActions, t2;
	$[2] === actions.publishNow ? t2 = $[3] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, { ...actions.publishNow }), $[2] = actions.publishNow, $[3] = t2);
	let t3;
	$[4] !== actions.editSchedule || $[5] !== release ? (t3 = !isPausedCardinalityOneRelease(release) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, { ...actions.editSchedule }), $[4] = actions.editSchedule, $[5] = release, $[6] = t3) : t3 = $[6];
	let t4;
	$[7] === Symbol.for("react.memo_cache_sentinel") ? (t4 = { view: "drafts" }, $[7] = t4) : t4 = $[7];
	let t5;
	$[8] === Symbol.for("react.memo_cache_sentinel") ? (t5 = { textDecoration: "none" }, $[8] = t5) : t5 = $[8];
	let t6;
	$[9] === t ? t6 = $[10] : (t6 = t("release.action.view-scheduled-drafts"), $[9] = t, $[10] = t6);
	let t7;
	$[11] === t6 ? t7 = $[12] : (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IntentLink, {
		intent: RELEASES_SCHEDULED_DRAFTS_INTENT,
		params: t4,
		rel: "noopener noreferrer",
		style: t5,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
			icon: CalendarIcon,
			text: t6
		})
	}), $[11] = t6, $[12] = t7);
	let t8;
	$[13] === Symbol.for("react.memo_cache_sentinel") ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuDivider, {}), $[13] = t8) : t8 = $[13];
	let t9;
	$[14] !== bundleId || $[15] !== copyToReleaseOptions || $[16] !== documentType || $[17] !== hasCopyToDraftOption || $[18] !== hasCreatePermission || $[19] !== isCopyToReleaseDisabled || $[20] !== isReleasesEnabled || $[21] !== onCopyToDrafts || $[22] !== onCreateRelease || $[23] !== onCreateVersion || $[24] !== showCopyToReleaseMenuItem ? (t9 = (showCopyToReleaseMenuItem || hasCopyToDraftOption) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CopyToReleaseMenuGroup, {
		releases: copyToReleaseOptions,
		bundleId,
		hasCopyToDraftOption,
		isReleasesEnabled,
		onCreateRelease,
		onCopyToDrafts,
		onCreateVersion,
		disabled: isCopyToReleaseDisabled,
		hasCreatePermission,
		documentType
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuDivider, {})] }), $[14] = bundleId, $[15] = copyToReleaseOptions, $[16] = documentType, $[17] = hasCopyToDraftOption, $[18] = hasCreatePermission, $[19] = isCopyToReleaseDisabled, $[20] = isReleasesEnabled, $[21] = onCopyToDrafts, $[22] = onCreateRelease, $[23] = onCreateVersion, $[24] = showCopyToReleaseMenuItem, $[25] = t9) : t9 = $[25];
	let t10;
	$[26] === actions.deleteSchedule ? t10 = $[27] : (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, { ...actions.deleteSchedule }), $[26] = actions.deleteSchedule, $[27] = t10);
	let t11;
	return $[28] !== t10 || $[29] !== t2 || $[30] !== t3 || $[31] !== t7 || $[32] !== t9 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Menu, { children: [
		t2,
		t3,
		t7,
		t8,
		t9,
		t10
	] }), $[28] = t10, $[29] = t2, $[30] = t3, $[31] = t7, $[32] = t9, $[33] = t11) : t11 = $[33], t11;
});
function _temp$51(r) {
	return !isReleaseScheduledOrScheduling(r);
}
var VersionContextMenu = (0, import_react.memo)(function VersionContextMenu(props) {
	let $ = (0, import_compiler_runtime.c)(42), { documentGroupId, releases, releasesLoading, fromRelease, onDiscard, versionId, onCreateRelease, onCopyToDrafts, onCreateVersion, disabled, locked, type, isGoingToUnpublish: t0, release, isScheduledDraft, scheduledDraftMenuActions, isDiscardable: t1 } = props, isGoingToUnpublish = t0 !== void 0 && t0, isDiscardable = t1 === void 0 || t1, t2;
	$[0] === versionId ? t2 = $[1] : (t2 = isPublishedId(versionId), $[0] = versionId, $[1] = t2);
	let isPublished = t2, t3;
	$[2] === versionId ? t3 = $[3] : (t3 = getVersionFromId(versionId), $[2] = versionId, $[3] = t3);
	let versionName = t3, { checkWithPermissionGuard } = useReleasePermissions(), { createRelease } = useReleaseOperations(), [hasCreatePermission, setHasCreatePermission] = (0, import_react.useState)(null), t4 = fromRelease === "draft" ? "discardDraft" : "discardVersion", t5;
	$[4] !== documentGroupId || $[5] !== t4 || $[6] !== type || $[7] !== versionName ? (t5 = {
		id: documentGroupId,
		type,
		version: versionName,
		permission: t4
	}, $[4] = documentGroupId, $[5] = t4, $[6] = type, $[7] = versionName, $[8] = t5) : t5 = $[8];
	let [permissions, isPermissionsLoading] = useDocumentPairPermissions(t5), hasDiscardPermission = !isPermissionsLoading && permissions?.granted, isMounted = (0, import_react.useRef)(!1), t6, t7;
	if ($[9] !== checkWithPermissionGuard || $[10] !== createRelease ? (t6 = () => (isMounted.current = !0, checkWithPermissionGuard(createRelease, getReleaseDefaults()).then((hasPermission) => {
		isMounted.current && setHasCreatePermission(hasPermission);
	}), () => {
		isMounted.current = !1;
	}), t7 = [checkWithPermissionGuard, createRelease], $[9] = checkWithPermissionGuard, $[10] = createRelease, $[11] = t6, $[12] = t7) : (t6 = $[11], t7 = $[12]), (0, import_react.useEffect)(t6, t7), isScheduledDraft && versionName && release && scheduledDraftMenuActions) {
		let t8;
		return $[13] !== disabled || $[14] !== fromRelease || $[15] !== hasCreatePermission || $[16] !== isGoingToUnpublish || $[17] !== onCopyToDrafts || $[18] !== onCreateRelease || $[19] !== onCreateVersion || $[20] !== release || $[21] !== releases || $[22] !== scheduledDraftMenuActions || $[23] !== type ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ScheduledDraftContextMenu, {
			releases,
			bundleId: fromRelease,
			release,
			onCreateRelease,
			onCopyToDrafts,
			onCreateVersion,
			disabled,
			isGoingToUnpublish,
			hasCreatePermission,
			scheduledDraftMenuActions,
			documentType: type
		}), $[13] = disabled, $[14] = fromRelease, $[15] = hasCreatePermission, $[16] = isGoingToUnpublish, $[17] = onCopyToDrafts, $[18] = onCreateRelease, $[19] = onCreateVersion, $[20] = release, $[21] = releases, $[22] = scheduledDraftMenuActions, $[23] = type, $[24] = t8) : t8 = $[24], t8;
	}
	let t8 = hasDiscardPermission || !1, t9;
	return $[25] !== disabled || $[26] !== fromRelease || $[27] !== hasCreatePermission || $[28] !== isDiscardable || $[29] !== isGoingToUnpublish || $[30] !== isPublished || $[31] !== locked || $[32] !== onCopyToDrafts || $[33] !== onCreateRelease || $[34] !== onCreateVersion || $[35] !== onDiscard || $[36] !== release || $[37] !== releases || $[38] !== releasesLoading || $[39] !== t8 || $[40] !== type ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CanonicalReleaseContextMenu, {
		releases,
		releasesLoading,
		bundleId: fromRelease,
		release,
		onDiscard,
		onCreateRelease,
		onCopyToDrafts,
		onCreateVersion,
		disabled,
		locked,
		isGoingToUnpublish,
		hasCreatePermission,
		hasDiscardPermission: t8,
		isPublished,
		isDiscardable,
		documentType: type
	}), $[25] = disabled, $[26] = fromRelease, $[27] = hasCreatePermission, $[28] = isDiscardable, $[29] = isGoingToUnpublish, $[30] = isPublished, $[31] = locked, $[32] = onCopyToDrafts, $[33] = onCreateRelease, $[34] = onCreateVersion, $[35] = onDiscard, $[36] = release, $[37] = releases, $[38] = releasesLoading, $[39] = t8, $[40] = type, $[41] = t9) : t9 = $[41], t9;
});
var VersionContextMenuPopover = (0, import_react.memo)(function VersionContextMenuPopover(props) {
	let $ = (0, import_compiler_runtime.c)(29), { contextMenu, popoverRef, referenceElement, documentGroupId, versionId, documentType, bundleId, releases, releasesLoading, onDiscard, onCreateRelease, onCopyToDrafts, onCreateVersion, disabled: t0, locked: t1, isGoingToUnpublish: t2, release, isScheduledDraft: t3, scheduledDraftMenuActions, isDiscardable: t4, portal: t5 } = props, disabled = t0 !== void 0 && t0, locked = t1 !== void 0 && t1, isGoingToUnpublish = t2 !== void 0 && t2, isScheduledDraft = t3 !== void 0 && t3, isDiscardable = t4 === void 0 || t4, portal = t5 === void 0 || t5, t6;
	$[0] !== bundleId || $[1] !== disabled || $[2] !== documentGroupId || $[3] !== documentType || $[4] !== isDiscardable || $[5] !== isGoingToUnpublish || $[6] !== isScheduledDraft || $[7] !== locked || $[8] !== onCopyToDrafts || $[9] !== onCreateRelease || $[10] !== onCreateVersion || $[11] !== onDiscard || $[12] !== release || $[13] !== releases || $[14] !== releasesLoading || $[15] !== scheduledDraftMenuActions || $[16] !== versionId ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VersionContextMenu, {
		documentGroupId,
		releases,
		releasesLoading,
		fromRelease: bundleId,
		versionId,
		onDiscard,
		onCreateRelease,
		onCopyToDrafts,
		disabled,
		onCreateVersion,
		locked,
		type: documentType,
		isGoingToUnpublish,
		release,
		isScheduledDraft,
		scheduledDraftMenuActions,
		isDiscardable
	}), $[0] = bundleId, $[1] = disabled, $[2] = documentGroupId, $[3] = documentType, $[4] = isDiscardable, $[5] = isGoingToUnpublish, $[6] = isScheduledDraft, $[7] = locked, $[8] = onCopyToDrafts, $[9] = onCreateRelease, $[10] = onCreateVersion, $[11] = onDiscard, $[12] = release, $[13] = releases, $[14] = releasesLoading, $[15] = scheduledDraftMenuActions, $[16] = versionId, $[17] = t6) : t6 = $[17];
	let t7;
	$[18] === Symbol.for("react.memo_cache_sentinel") ? (t7 = [], $[18] = t7) : t7 = $[18];
	let t8;
	$[19] !== contextMenu.open || $[20] !== contextMenu.translate ? (t8 = contextMenu.open ? { transform: `translate(${contextMenu.translate.x}px, ${contextMenu.translate.y}px)` } : void 0, $[19] = contextMenu.open, $[20] = contextMenu.translate, $[21] = t8) : t8 = $[21];
	let t9;
	return $[22] !== contextMenu.open || $[23] !== popoverRef || $[24] !== portal || $[25] !== referenceElement || $[26] !== t6 || $[27] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Popover$1, {
		animate: !1,
		content: t6,
		fallbackPlacements: t7,
		open: contextMenu.open,
		portal,
		placement: "bottom-start",
		ref: popoverRef,
		referenceElement,
		zOffset: 10,
		style: t8
	}), $[22] = contextMenu.open, $[23] = popoverRef, $[24] = portal, $[25] = referenceElement, $[26] = t6, $[27] = t8, $[28] = t9) : t9 = $[28], t9;
});
function getSupportedBundleId(selectedPerspective) {
	let bundleId = getBundleIdFromPerspective(selectedPerspective);
	if (bundleId !== "published") return bundleId;
}
/**
* Creates a variant-scoped version document via the variants document create action.
*
* @internal
*/
async function createVariantScopedDocument({ client, variant, selectedPerspective, documentGroupId, signal, ...options }) {
	let bundleId = getSupportedBundleId(selectedPerspective), action = {
		actionType: "sanity.action.document.variant.create",
		variantId: getVariantId(variant._id),
		...bundleId ? { bundleId } : {},
		publishedId: documentGroupId
	};
	if ("baseId" in options) return variantsClient(client).action({
		baseId: options.baseId,
		...typeof options.ifBaseRevisionId == "string" ? { ifBaseRevisionId: options.ifBaseRevisionId } : {},
		...action
	}, {
		tag: "variants.document.create",
		signal
	});
	if ("document" in options) return variantsClient(client).action({
		document: options.document,
		...action
	}, {
		tag: "variants.document.create",
		signal
	});
	throw Error("`createVariantScopedDocument`: `baseId` or `document` must be provided.");
}
/**
* @internal
*/
function useVariantDocumentOperations() {
	let $ = (0, import_compiler_runtime.c)(4), client = useClient(VARIANTS_STUDIO_CLIENT_OPTIONS), t0;
	$[0] === client ? t0 = $[1] : (t0 = (options) => createVariantScopedDocument({
		client,
		...options
	}), $[0] = client, $[1] = t0);
	let createVariantDocument = t0, t1;
	return $[2] === createVariantDocument ? t1 = $[3] : (t1 = { createVariantDocument }, $[2] = createVariantDocument, $[3] = t1), t1;
}
/**
* @internal
*/
function isVariantId(maybeVariantId) {
	return typeof maybeVariantId == "string" && maybeVariantId.match(/^_\.variants\.['a-zA-Z0-9._-]+$/) !== null;
}
function useCopyToDrafts(options) {
	let $ = (0, import_compiler_runtime.c)(16), { onNavigate, onConfirmationRequest, documentVersionInfoStub, documentGroupId } = options, client = useClient(DEFAULT_STUDIO_CLIENT_OPTIONS), toast = useToast(), { t } = useTranslation$1(), t0;
	$[0] === documentGroupId ? t0 = $[1] : (t0 = { documentId: documentGroupId }, $[0] = documentGroupId, $[1] = t0);
	let { versions } = useDocumentVersions(t0), variantRef = documentVersionInfoStub?._system.variant?._ref, t1;
	$[2] !== variantRef || $[3] !== versions ? (t1 = getTargetDocument({
		bundle: "drafts",
		variant: variantRef,
		documentVersions: versions
	}), $[2] = variantRef, $[3] = versions, $[4] = t1) : t1 = $[4];
	let hasDraftVersion = !!t1, t2;
	$[5] !== client || $[6] !== documentGroupId || $[7] !== documentVersionInfoStub || $[8] !== hasDraftVersion || $[9] !== onConfirmationRequest || $[10] !== onNavigate || $[11] !== t || $[12] !== toast ? (t2 = async (t3) => {
		let { shouldConfirmDraftDiscard } = t3;
		if (shouldConfirmDraftDiscard && hasDraftVersion) {
			onConfirmationRequest();
			return;
		}
		let run = async () => {
			if (!documentVersionInfoStub) throw Error("Document version info stub is required, not found for document group id: " + documentGroupId);
			if (documentVersionInfoStub._system.bundleId === "drafts") throw Error("Cannot copy a draft onto itself");
			let actions = [], variantRef_0 = documentVersionInfoStub._system.variant?._ref;
			if (variantRef_0) {
				let variantId = getVariantId(variantRef_0);
				hasDraftVersion && actions.push({
					actionType: "sanity.action.document.variant.delete",
					bundleId: "drafts",
					publishedId: documentGroupId,
					variantId
				}), actions.push({
					actionType: "sanity.action.document.variant.create",
					bundleId: "drafts",
					publishedId: documentGroupId,
					variantId,
					baseId: documentVersionInfoStub._id,
					ifBaseRevisionId: documentVersionInfoStub._rev
				});
			} else hasDraftVersion && actions.push({
				actionType: "sanity.action.document.discard",
				draftId: getDraftId(documentGroupId)
			}), actions.push({
				actionType: "sanity.action.document.version.create",
				versionId: getDraftId(documentGroupId),
				baseId: documentVersionInfoStub._id,
				ifBaseRevisionId: documentVersionInfoStub._rev,
				publishedId: documentGroupId
			});
			await client.action(actions, { tag: "document.copy-to-drafts" }), onNavigate();
		};
		try {
			await run();
		} catch (t4) {
			let err = t4;
			toast.push({
				closable: !0,
				status: "error",
				title: t("release.action.create-version.failure"),
				description: err.message
			});
		}
	}, $[5] = client, $[6] = documentGroupId, $[7] = documentVersionInfoStub, $[8] = hasDraftVersion, $[9] = onConfirmationRequest, $[10] = onNavigate, $[11] = t, $[12] = toast, $[13] = t2) : t2 = $[13];
	let handleCopyToDrafts = t2, t3;
	return $[14] === handleCopyToDrafts ? t3 = $[15] : (t3 = { handleCopyToDrafts }, $[14] = handleCopyToDrafts, $[15] = t3), t3;
}
var CONTEXT_MENU_CLOSED = { open: !1 };
/**
* Manages the state and actions backing a version context menu: menu
* open/position state, dialog state, version creation and scheduled draft
* actions.
*
* Render the menu and its dialogs with `VersionContextMenuPopover` and
* `VersionContextMenuDialogs`.
*
* @internal
*/
function useVersionContextMenu(options) {
	let $ = (0, import_compiler_runtime.c)(64), { documentGroupId, versionId, documentType, disabled: t0, documentVersionInfoStub: _documentVersionInfoStub, onCopyToDraftsComplete } = options, disabled = t0 !== void 0 && t0, { map: releasesMap } = useAllReleases(), t1;
	$[0] === documentGroupId ? t1 = $[1] : (t1 = { documentId: documentGroupId }, $[0] = documentGroupId, $[1] = t1);
	let { versions } = useDocumentVersions(t1), t2;
	bb0: {
		if (_documentVersionInfoStub) {
			t2 = _documentVersionInfoStub;
			break bb0;
		}
		let t3;
		if ($[2] !== versionId || $[3] !== versions) {
			let t4;
			$[5] === versionId ? t4 = $[6] : (t4 = (version) => version._id === versionId, $[5] = versionId, $[6] = t4), t3 = versions.find(t4), $[2] = versionId, $[3] = versions, $[4] = t3;
		} else t3 = $[4];
		t2 = t3;
	}
	let documentVersionInfoStub = t2, releaseRef = documentVersionInfoStub?._system.release?._ref, release = releaseRef ? releasesMap.get(releaseRef) : void 0;
	!isDocumentGroupId(documentGroupId) && console.warn(`useVersionContextMenu: expected a document group id, got "${documentGroupId}". Pass the group (published) id as \`documentGroupId\` and the full document id as \`versionId\`.`);
	let { createVariantDocument } = useVariantDocumentOperations(), stubVariantRef = documentVersionInfoStub?._system.variant?._ref, t3;
	$[7] === stubVariantRef ? t3 = $[8] : (t3 = isVariantId(stubVariantRef) ? stubVariantRef : void 0, $[7] = stubVariantRef, $[8] = t3);
	let variantRef = t3, t4;
	$[9] === Symbol.for("react.memo_cache_sentinel") ? (t4 = { open: !1 }, $[9] = t4) : t4 = $[9];
	let [contextMenu, setContextMenu] = (0, import_react.useState)(t4), popoverRef = (0, import_react.useRef)(null), [referenceElement, setReferenceElement] = (0, import_react.useState)(null), [dialogState, setDialogState] = (0, import_react.useState)("idle"), { createVersion } = useVersionOperations(), toast = useToast(), { t } = useTranslation$1(), { onSetScheduledDraftPerspective } = useSingleDocRelease(), setPerspective = useSetPerspective(), setVariant = useSetVariant(), t5;
	$[10] === Symbol.for("react.memo_cache_sentinel") ? (t5 = () => setContextMenu(CONTEXT_MENU_CLOSED), $[10] = t5) : t5 = $[10];
	let closeContextMenu = t5, t6;
	$[11] === Symbol.for("react.memo_cache_sentinel") ? (t6 = (event) => {
		event.preventDefault();
		let elementRect = event.currentTarget?.getBoundingClientRect();
		setContextMenu({
			open: !0,
			translate: {
				x: event.clientX - elementRect.left,
				y: elementRect.top - event.clientY
			}
		});
	}, $[11] = t6) : t6 = $[11];
	let handleContextMenu = t6, t7;
	$[12] === Symbol.for("react.memo_cache_sentinel") ? (t7 = () => [popoverRef.current], $[12] = t7) : t7 = $[12], useClickOutsideEvent(closeContextMenu, t7);
	let t8;
	$[13] === closeContextMenu ? t8 = $[14] : (t8 = (event_0) => {
		event_0.key === "Escape" && closeContextMenu();
	}, $[13] = closeContextMenu, $[14] = t8), useGlobalKeyDown(t8);
	let t9;
	$[15] === Symbol.for("react.memo_cache_sentinel") ? (t9 = () => {
		setDialogState("idle");
	}, $[15] = t9) : t9 = $[15];
	let closeDialog = t9, t10;
	$[16] === Symbol.for("react.memo_cache_sentinel") ? (t10 = () => {
		setDialogState("discard-version");
	}, $[16] = t10) : t10 = $[16];
	let openDiscardDialog = t10, t11;
	$[17] === Symbol.for("react.memo_cache_sentinel") ? (t11 = () => {
		setDialogState("create-release");
	}, $[17] = t11) : t11 = $[17];
	let openCreateReleaseDialog = t11, t12;
	$[18] !== onCopyToDraftsComplete || $[19] !== setPerspective || $[20] !== setVariant || $[21] !== variantRef ? (t12 = () => {
		variantRef ? setVariant({
			variantId: variantRef,
			perspective: "drafts"
		}) : setPerspective("drafts"), onCopyToDraftsComplete?.();
	}, $[18] = onCopyToDraftsComplete, $[19] = setPerspective, $[20] = setVariant, $[21] = variantRef, $[22] = t12) : t12 = $[22];
	let handleCopyToDraftsNavigate = t12, t13;
	$[23] === Symbol.for("react.memo_cache_sentinel") ? (t13 = () => setDialogState("copy-to-drafts"), $[23] = t13) : t13 = $[23];
	let t14;
	$[24] !== documentGroupId || $[25] !== documentVersionInfoStub || $[26] !== handleCopyToDraftsNavigate ? (t14 = {
		documentGroupId,
		documentVersionInfoStub,
		onNavigate: handleCopyToDraftsNavigate,
		onConfirmationRequest: t13
	}, $[24] = documentGroupId, $[25] = documentVersionInfoStub, $[26] = handleCopyToDraftsNavigate, $[27] = t14) : t14 = $[27];
	let { handleCopyToDrafts } = useCopyToDrafts(t14), t15;
	$[28] !== closeContextMenu || $[29] !== createVariantDocument || $[30] !== createVersion || $[31] !== documentGroupId || $[32] !== documentVersionInfoStub || $[33] !== setPerspective || $[34] !== setVariant || $[35] !== t || $[36] !== toast || $[37] !== variantRef ? (t15 = async (targetRelease) => {
		let perspective = isReleaseDocumentId(targetRelease) ? getReleaseIdFromReleaseDocumentId(targetRelease) : targetRelease, runCreateVersion = async () => {
			if (!documentVersionInfoStub?._id) throw Error("Document version info stub is required");
			variantRef ? await createVariantDocument({
				baseId: documentVersionInfoStub._id,
				documentGroupId,
				variant: { _id: variantRef },
				selectedPerspective: perspective
			}) : await createVersion(getReleaseIdFromReleaseDocumentId(targetRelease), documentVersionInfoStub._id, { navigate: !1 });
		};
		try {
			await runCreateVersion(), variantRef ? setVariant({
				variantId: variantRef,
				perspective
			}) : setPerspective(perspective);
		} catch (t16) {
			let err = t16;
			toast.push({
				closable: !0,
				status: "error",
				title: t("release.action.create-version.failure"),
				description: err.message
			});
		}
		closeContextMenu();
	}, $[28] = closeContextMenu, $[29] = createVariantDocument, $[30] = createVersion, $[31] = documentGroupId, $[32] = documentVersionInfoStub, $[33] = setPerspective, $[34] = setVariant, $[35] = t, $[36] = toast, $[37] = variantRef, $[38] = t15) : t15 = $[38];
	let handleAddVersion = t15, isScheduledDraft = !!(release && isCardinalityOneRelease(release)), t16;
	$[39] !== onSetScheduledDraftPerspective || $[40] !== release ? (t16 = () => {
		release && onSetScheduledDraftPerspective(getReleaseIdFromReleaseDocumentId(release._id));
	}, $[39] = onSetScheduledDraftPerspective, $[40] = release, $[41] = t16) : t16 = $[41];
	let handleEditScheduleComplete = t16, onDeleteComplete = useClearScheduledDraftPerspectiveOnDelete(release), t17;
	$[42] !== disabled || $[43] !== documentGroupId || $[44] !== documentType || $[45] !== handleEditScheduleComplete || $[46] !== onDeleteComplete || $[47] !== release ? (t17 = {
		release,
		documentType,
		documentId: documentGroupId,
		disabled,
		onActionComplete: handleEditScheduleComplete,
		onDeleteComplete
	}, $[42] = disabled, $[43] = documentGroupId, $[44] = documentType, $[45] = handleEditScheduleComplete, $[46] = onDeleteComplete, $[47] = release, $[48] = t17) : t17 = $[48];
	let scheduledDraftMenuActions = useScheduledDraftMenuActions(t17), sourceReleasePerspective = release ?? (documentVersionInfoStub ? documentVersionInfoStub._system.bundleId ?? "published" : "drafts"), t18;
	return $[49] !== closeContextMenu || $[50] !== closeDialog || $[51] !== contextMenu || $[52] !== dialogState || $[53] !== handleAddVersion || $[54] !== handleContextMenu || $[55] !== handleCopyToDrafts || $[56] !== isScheduledDraft || $[57] !== openCreateReleaseDialog || $[58] !== openDiscardDialog || $[59] !== referenceElement || $[60] !== scheduledDraftMenuActions || $[61] !== setReferenceElement || $[62] !== sourceReleasePerspective ? (t18 = {
		contextMenu,
		handleContextMenu,
		closeContextMenu,
		popoverRef,
		referenceElement,
		setReferenceElement,
		dialogState,
		closeDialog,
		openDiscardDialog,
		openCreateReleaseDialog,
		handleCopyToDrafts,
		handleAddVersion,
		isScheduledDraft,
		scheduledDraftMenuActions,
		sourceReleasePerspective
	}, $[49] = closeContextMenu, $[50] = closeDialog, $[51] = contextMenu, $[52] = dialogState, $[53] = handleAddVersion, $[54] = handleContextMenu, $[55] = handleCopyToDrafts, $[56] = isScheduledDraft, $[57] = openCreateReleaseDialog, $[58] = openDiscardDialog, $[59] = referenceElement, $[60] = scheduledDraftMenuActions, $[61] = setReferenceElement, $[62] = sourceReleasePerspective, $[63] = t18) : t18 = $[63], t18;
}
var deletionMachine = setup({
	types: {},
	actors: {
		referringDocuments: fromObservable(() => {
			throw Error("`referringDocuments` actor must be provided.");
		}),
		deleteVariants: fromPromise(() => Promise.reject(/* @__PURE__ */ Error("`deleteVariants` actor must be provided.")))
	},
	actions: { requestDeletionConfirmation: () => {} },
	guards: {
		hasSelection: ({ context }) => context.ids.length !== 0,
		selectionExcludesPublished: ({ context }) => !context.ids.some(isPublishedId),
		canRequestDeletion: and(["hasSelection"]),
		canConfirmDeletion: and(["hasSelection", or(["selectionExcludesPublished", stateIn("#incomingReferencesChecked")])]),
		shouldWarnIncomingReferences: ({ context }) => context.ids.some(isPublishedId) && ((context.internalReferences?.references.length ?? 0) > 0 || (context.crossDatasetReferences?.references.length ?? 0) > 0)
	}
}).createMachine({
	id: "deletion",
	context: {
		ids: [],
		internalReferences: void 0,
		crossDatasetReferences: void 0,
		projectIds: [],
		datasetNames: [],
		hasUnknownDatasetNames: !1,
		error: void 0
	},
	on: { "selection.changed": { actions: assign({ ids: ({ event }) => [...event.selectedIds] }) } },
	initial: "idle",
	states: {
		idle: { on: { "delete.request": {
			guard: "canRequestDeletion",
			target: "active"
		} } },
		active: {
			type: "parallel",
			entry: sendParent({ type: "deletion.activated" }),
			exit: sendParent({ type: "deletion.deactivated" }),
			on: { "delete.cancel": { target: "#deletion.idle" } },
			states: {
				deletion: {
					initial: "preparing",
					states: {
						preparing: {
							type: "parallel",
							states: {
								checkingIncomingReferences: {
									initial: "checking",
									states: {
										checking: { invoke: {
											src: "referringDocuments",
											onSnapshot: {
												guard: ({ event }) => event.snapshot.context?.isLoading === !1,
												target: "checked",
												actions: assign({
													internalReferences: ({ event }) => event.snapshot.context?.internalReferences,
													crossDatasetReferences: ({ event }) => event.snapshot.context?.crossDatasetReferences,
													projectIds: ({ event }) => event.snapshot.context?.projectIds ?? [],
													datasetNames: ({ event }) => event.snapshot.context?.datasetNames ?? [],
													hasUnknownDatasetNames: ({ event }) => event.snapshot.context?.hasUnknownDatasetNames ?? !1
												})
											},
											onError: {
												target: "#deletion.active.deletion.error",
												actions: assign({ error: ({ event }) => event.error })
											}
										} },
										checked: { id: "incomingReferencesChecked" }
									}
								},
								awaitingDeletionConfirmation: {
									tags: "awaitingDeletionConfirmation",
									entry: "requestDeletionConfirmation",
									on: { "delete.confirm": {
										guard: "canConfirmDeletion",
										target: "#deletion.active.deletion.deleting"
									} }
								}
							}
						},
						deleting: { invoke: {
							src: "deleteVariants",
							input: ({ context }) => ({ ids: context.ids }),
							onDone: { target: "#deletion.idle" },
							onError: {
								target: "#deletion.active.deletion.error",
								actions: assign({ error: ({ event }) => event.error })
							}
						} },
						error: { on: { "delete.confirm": {
							guard: "hasSelection",
							target: "#deletion.active.deletion.deleting"
						} } }
					}
				},
				incomingReferenceWarning: {
					initial: "inactive",
					states: {
						inactive: { always: {
							guard: "shouldWarnIncomingReferences",
							target: "warnIncomingReferences"
						} },
						warnIncomingReferences: {
							tags: "warnIncomingReferences",
							always: {
								guard: not("shouldWarnIncomingReferences"),
								target: "inactive"
							}
						}
					}
				}
			}
		}
	}
});
/**
* @internal
*/
function computeSets({ meta, current, t, variantsEnabled }) {
	if (!meta) return current;
	let { releases } = meta.releases, { variants } = meta.variants, userBundleIds = new Set(meta.agentBundles.bundles.map(({ id }) => id)), proposedChanges = meta.versionState.versions.find((version) => {
		let bundleId = getVersionFromId(version._id);
		return typeof bundleId == "string" && userBundleIds.has(bundleId);
	});
	if (!variantsEnabled) {
		meta.versionState.versions.some((version) => version._system.variant !== void 0) && console.warn("Content Variants is switched off, but there are variants of this document.\nSwitch Content Variants on in your Studio configuration to ensure variants are displayed correctly (set `beta.variants.enabled` to `true`).");
		let variants = meta.versionState.versions.filter((version) => !isAgentBundleName(getVersionFromId(version._id))).map((version) => ({
			id: version._id,
			name: getVersionName({
				document: version,
				releases,
				t
			}),
			document: version
		}));
		return proposedChanges && variants.unshift({
			id: proposedChanges._id,
			name: t("version.agent-bundle.proposed-changes"),
			document: proposedChanges
		}), [{
			key: "studio:all",
			name: t("document-group-inventory.title", {
				count: variants.length,
				subject: t("document-group.subject.version", { count: variants.length })
			}),
			variants
		}];
	}
	let sets = [...meta.versionState.versions.filter((version) => !isAgentBundleName(getVersionFromId(version._id))).reduce((currentVariantsByRelease, version) => {
		let bundle;
		switch (readVersionType(version)) {
			case "release":
				bundle = version._system.release?._ref;
				break;
			case "published":
				bundle = "published";
				break;
			case "draft": bundle = "drafts";
		}
		return bundle === void 0 ? currentVariantsByRelease : (currentVariantsByRelease.has(bundle) || currentVariantsByRelease.set(bundle, []), currentVariantsByRelease.get(bundle)?.push(version), currentVariantsByRelease);
	}, /* @__PURE__ */ new Map()).entries()].map(([releaseId, releaseVariants]) => ({
		key: releaseId,
		name: getReleaseName({
			releaseId,
			releases,
			t
		}),
		variants: releaseVariants.map((variant) => ({
			id: variant._id,
			name: getVariantName({
				document: variant,
				variants,
				t
			}),
			releaseDocument: releases.get(releaseId),
			document: variant
		}))
	}));
	return proposedChanges ? sets.toSpliced(0, 0, {
		key: "studio:content-agent",
		name: t("content-agent"),
		variants: [{
			id: proposedChanges._id,
			name: t("version.agent-bundle.proposed-changes"),
			document: proposedChanges
		}]
	}) : sets;
}
function getVariantName({ document, variants, t }) {
	let releaseDocumentId = document._system.variant?._ref;
	return (releaseDocumentId ? variants.get(releaseDocumentId) : void 0)?.metadata?.title ?? t("document-group.base-variant");
}
function getVersionName({ document, releases, t }) {
	let state = readVersionType(document);
	return state === "draft" ? t("release.chip.draft") : state === "published" ? t("release.chip.published") : releases.get(document._system.release?._ref ?? "")?.metadata.title ?? document._system.release?._ref ?? document._id;
}
function getReleaseName({ releaseId, releases, t }) {
	return releaseId === "published" ? t("release.chip.published") : releaseId === "drafts" ? t("release.chip.draft") : releases.get(releaseId)?.metadata.title ?? releaseId;
}
var documentGroupInventoryMachine = setup({
	types: {},
	actors: { meta: fromObservable(() => EMPTY) },
	actions: { onFeedbackBegin: () => {} },
	guards: { isCreatingVariant: stateIn("creatingVariant") }
}).createMachine({
	id: "documentGroupInventory",
	context: ({ input, spawn }) => ({
		selectionRef: spawn(input.selectionMachine, {
			systemId: "selection",
			input: void 0
		}),
		deletionRef: spawn(input.deletionMachine, {
			systemId: "deletion",
			input: void 0
		}),
		variantCreationRef: spawn(input.variantCreationMachine, {
			systemId: "variantCreation",
			input: void 0
		}),
		sets: [],
		releases: /* @__PURE__ */ new Map(),
		t: input.t,
		variantsEnabled: input.variantsEnabled,
		metaState: "pending"
	}),
	invoke: {
		src: "meta",
		onSnapshot: { actions: [assign({
			sets: ({ context, event }) => computeSets({
				meta: event.snapshot.context,
				current: context.sets,
				t: context.t,
				variantsEnabled: context.variantsEnabled
			}),
			releases: ({ event }) => event.snapshot.context?.releases.releases ?? /* @__PURE__ */ new Map(),
			metaState: ({ context, event }) => context.metaState === "ready" || metaIsSettled(event.snapshot.context) ? "ready" : "pending"
		}), sendTo(({ context }) => context.selectionRef, ({ context, event }) => {
			let meta = event.snapshot.context;
			return metaHasError(meta) ? { type: "meta.error" } : {
				type: "variants.changed",
				variants: context.sets.flatMap((set) => set.variants),
				loaded: metaIsSettled(meta)
			};
		})] },
		onError: { actions: sendTo(({ context }) => context.selectionRef, ({ event }) => ({
			type: "meta.error",
			error: event.error
		})) }
	},
	on: {
		"selection.changed": { actions: forwardTo(({ context }) => context.deletionRef) },
		"deletion.activated": { actions: sendTo(({ context }) => context.selectionRef, { type: "selection.lock" }) },
		"deletion.deactivated": { actions: sendTo(({ context }) => context.selectionRef, { type: "selection.unlock" }) },
		"variantCreation.activated": ".creatingVariant",
		"variantCreation.deactivated": {
			guard: "isCreatingVariant",
			target: ".idle"
		},
		"feedback.begin": ".feedback",
		"feedback.end": ".idle"
	},
	initial: "idle",
	states: {
		idle: {},
		creatingVariant: {},
		feedback: { entry: "onFeedbackBegin" }
	}
});
function metaHasError(meta) {
	return meta ? !!meta.versionState.error || meta.releases.state === "error" : !1;
}
function metaIsSettled(meta) {
	return meta ? !meta.versionState.loading && meta.releases.state !== "initialising" && meta.releases.state !== "loading" && meta.variants.state !== "initialising" && meta.variants.state !== "loading" && !meta.agentBundles.loading : !1;
}
var selectionMachine = setup({
	types: {},
	actors: { filterString: fromObservable(() => EMPTY) },
	guards: { hasSelection: ({ context }) => context.selectedIds.size !== 0 },
	actions: {
		notifySelectionChanged: sendParent(({ context }) => ({
			type: "selection.changed",
			selectedIds: context.selectedIds
		})),
		applyVariants: assign(({ context, event }) => event.type === "variants.changed" ? {
			variants: event.variants,
			selectedIds: pruneSelectedIds(context.selectedIds, event.variants)
		} : {}),
		filterStringChanged: assign({ filterMatchingVariantIds: ({ context }) => {
			if (context.filterString === void 0) return /* @__PURE__ */ new Set();
			let needle = context.filterString.toLowerCase();
			return new Set(context.variants.filter((variant) => variant.name.toLowerCase().includes(needle)).map(({ id }) => id));
		} })
	}
}).createMachine({
	id: "selection",
	context: {
		selectedIds: /* @__PURE__ */ new Set(),
		variants: [],
		filterString: "",
		filterMatchingVariantIds: /* @__PURE__ */ new Set()
	},
	invoke: {
		src: "filterString",
		onSnapshot: { actions: [assign({ filterString: ({ event }) => event.snapshot.context }), "filterStringChanged"] }
	},
	on: {
		"variants.changed": [{
			guard: and([stateIn("loading"), ({ event }) => event.loaded]),
			target: ".ready",
			actions: ["applyVariants", "notifySelectionChanged"]
		}, { actions: ["applyVariants", "notifySelectionChanged"] }],
		"meta.error": {
			target: ".error",
			actions: assign({ variants: [] })
		}
	},
	initial: "loading",
	states: {
		loading: {},
		ready: { on: {
			"selection.toggle": { actions: [assign({ selectedIds: ({ context, event }) => {
				let next = new Set(context.selectedIds);
				return next.delete(event.variantId) || next.add(event.variantId), next;
			} }), "notifySelectionChanged"] },
			"selection.add": { actions: [assign({ selectedIds: ({ context, event }) => {
				let next = new Set(context.selectedIds);
				return next.add(event.variantId), next;
			} }), "notifySelectionChanged"] },
			"selection.remove": { actions: [assign({ selectedIds: ({ context, event }) => {
				let next = new Set(context.selectedIds);
				return next.delete(event.variantId), next;
			} }), "notifySelectionChanged"] },
			"selection.clear": { actions: [assign({ selectedIds: () => /* @__PURE__ */ new Set() }), "notifySelectionChanged"] },
			"filterString.set": { actions: [assign({ filterString: ({ event }) => event.value }), "filterStringChanged"] },
			"selection.lock": "readonly"
		} },
		readonly: { on: { "selection.unlock": "ready" } },
		error: {}
	}
});
function pruneSelectedIds(selectedIds, variants) {
	let availableIds = new Set(variants.map((variant) => variant.id));
	return new Set([...selectedIds].filter((id) => availableIds.has(id)));
}
var variantCreationMachine = setup({
	types: {},
	actors: {
		variants: fromObservable(() => EMPTY),
		releases: fromObservable(() => EMPTY),
		createVariant: fromPromise(() => Promise.reject(/* @__PURE__ */ Error("`createVariant` actor must be provided.")))
	},
	guards: {
		hasSelectedVariant: ({ context }) => context.selectedVariantId !== void 0,
		hasSelectedBundle: ({ context }) => context.selectedBundle !== void 0,
		canConfirmCreation: and(["hasSelectedVariant", "hasSelectedBundle"])
	}
}).createMachine({
	id: "variantCreation",
	context: {
		variants: void 0,
		releases: void 0,
		selectedVariantId: void 0,
		selectedBundle: void 0,
		error: void 0
	},
	invoke: [{
		src: "variants",
		onSnapshot: { actions: assign({ variants: ({ event }) => event.snapshot.context }) }
	}, {
		src: "releases",
		onSnapshot: { actions: assign({ releases: ({ event }) => event.snapshot.context }) }
	}],
	initial: "idle",
	states: {
		idle: { on: { "createVariant.request": "active" } },
		active: {
			entry: sendParent({ type: "variantCreation.activated" }),
			exit: [sendParent({ type: "variantCreation.deactivated" }), assign({
				selectedVariantId: void 0,
				selectedBundle: void 0,
				error: void 0
			})],
			on: {
				"createVariant.cancel": "#variantCreation.idle",
				"createVariant.selectVariant": {
					guard: not(stateIn("#variantCreation.active.creating")),
					actions: assign({ selectedVariantId: ({ event }) => event.variantId })
				},
				"createVariant.selectBundle": {
					guard: not(stateIn("#variantCreation.active.creating")),
					actions: assign({ selectedBundle: ({ event }) => event.bundle })
				}
			},
			initial: "configuring",
			states: {
				configuring: { on: { "createVariant.confirm": {
					guard: "canConfirmCreation",
					target: "creating"
				} } },
				creating: { invoke: {
					src: "createVariant",
					input: ({ context }) => {
						let { selectedVariantId, selectedBundle } = context, variantDefinition = context.variants?.variants.get(selectedVariantId ?? ""), bundle = selectedBundle?.type === "drafts" ? "drafts" : context.releases?.releases.get(selectedBundle?.releaseId ?? "");
						if (variantDefinition === void 0 || bundle === void 0) throw Error("Cannot create a variant before a variant and bundle are selected.");
						return {
							variantDefinition,
							bundle
						};
					},
					onDone: { target: "#variantCreation.idle" },
					onError: {
						target: "error",
						actions: assign({ error: ({ event }) => event.error })
					}
				} },
				error: { on: { "createVariant.confirm": {
					guard: "canConfirmCreation",
					target: "creating"
				} } }
			}
		}
	}
});
var Body = /* @__PURE__ */ gt.div.withConfig({
	displayName: "Body",
	componentId: "sc-3lpmte-0"
})(({ theme }) => {
	let { space } = getTheme_v2(theme);
	return ft`overflow-x:clip;overflow-y:auto;overscroll-behavior:contain;padding:calc(${space[5]}px * 0.5) calc(${space[4]}px);@property --start-mask-color{syntax:'<color>';inherits:false;initial-value:#000;}@property --end-mask-color{syntax:'<color>';inherits:false;initial-value:#000;}@keyframes fade-mask{0%{--start-mask-color:#000;--end-mask-color:transparent;}2%,98%{--start-mask-color:transparent;--end-mask-color:transparent;}100%{--start-mask-color:transparent;--end-mask-color:#000;}}--mask-size:3rem;mask-image:linear-gradient( to var(--direction,bottom),var(--start-mask-color),#000 var(--mask-size),#000 calc(100% - var(--mask-size)),var(--end-mask-color) );mask-position:0% 0%;mask-repeat:no-repeat;mask-size:100% 100%;animation:fade-mask;animation-timeline:scroll(self y);`;
});
var ChevronWrapper = gt(Box).withConfig({
	displayName: "ChevronWrapper",
	componentId: "sc-s371q7-0"
})`margin-inline-start:auto;`;
var CrossDatasetReferencesDetails = gt.details.withConfig({
	displayName: "CrossDatasetReferencesDetails",
	componentId: "sc-s371q7-1"
})`flex:none;&[open] ${ChevronWrapper}{transform:rotate(180deg);}`;
var CrossDatasetReferencesSummary = gt.summary.withConfig({
	displayName: "CrossDatasetReferencesSummary",
	componentId: "sc-s371q7-2"
})`list-style:none;&::-webkit-details-marker{display:none;}`;
var Table = gt.table.withConfig({
	displayName: "Table",
	componentId: "sc-s371q7-3"
})`inline-size:100%;text-align:start;padding-block:0;padding-inline:${({ theme }) => rem(theme.sanity.space[2])};border-collapse:collapse;th{padding:${({ theme }) => rem(theme.sanity.space[1])};}td{padding-block:0;padding-inline:${({ theme }) => rem(theme.sanity.space[1])};}tr > *:last-child{text-align:end;}`;
var DocumentIdFlex = gt(Flex).withConfig({
	displayName: "DocumentIdFlex",
	componentId: "sc-s371q7-4"
})`min-block-size:33px;`;
var OtherReferenceCount = (props) => {
	let $ = (0, import_compiler_runtime.c)(13), { t } = useTranslation$1(studioLocaleNamespace), difference = props.totalCount - props.references.length;
	if (!difference) return null;
	let t0;
	$[0] !== difference || $[1] !== t ? (t0 = t("document-group.delete.other-reference-count.title", { count: difference }), $[0] = difference, $[1] = t, $[2] = t0) : t0 = $[2];
	let t1;
	$[3] === t0 ? t1 = $[4] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		muted: !0,
		children: t0
	}), $[3] = t0, $[4] = t1);
	let t2;
	$[5] === t ? t2 = $[6] : (t2 = t("document-group.delete.other-reference-count.tooltip"), $[5] = t, $[6] = t2);
	let t3;
	$[7] === Symbol.for("react.memo_cache_sentinel") ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		muted: !0,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InfoOutlineIcon, {})
	}), $[7] = t3) : t3 = $[7];
	let t4;
	$[8] === t2 ? t4 = $[9] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip$1, {
		portal: !0,
		placement: "top",
		content: t2,
		children: t3
	}), $[8] = t2, $[9] = t4);
	let t5;
	return $[10] !== t1 || $[11] !== t4 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		padding: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Inline, {
			gap: 2,
			children: [t1, t4]
		})
	}), $[10] = t1, $[11] = t4, $[12] = t5) : t5 = $[12], t5;
};
var EMPTY_INTERNAL_REFERENCES = {
	totalCount: 0,
	references: []
};
var EMPTY_CROSS_DATASET_REFERENCES = {
	totalCount: 0,
	references: []
};
var ConfirmDeleteDialog = (t0) => {
	let $ = (0, import_compiler_runtime.c)(75), { documentId, documentType, deletionRef, selectionRef, portalElementName, components } = t0, { t } = useTranslation$1(studioLocaleNamespace), { DocTitle, ReferencePreviewLink, VersionsPreviewList } = components, variantIds = useSelector(selectionRef, _temp$50), internalReferences = useSelector(deletionRef, _temp2$20) ?? EMPTY_INTERNAL_REFERENCES, crossDatasetReferences = useSelector(deletionRef, _temp3$12) ?? EMPTY_CROSS_DATASET_REFERENCES, datasetNames = useSelector(deletionRef, _temp4$3), hasUnknownDatasetNames = useSelector(deletionRef, _temp5$2), warnIncomingReferences = useSelector(deletionRef, _temp6$2), isDeletionError = useSelector(deletionRef, _temp7$1), deletionError = useSelector(deletionRef, _temp8$1), error = isDeletionError ? deletionError : void 0, canConfirmDeletion = useSelector(deletionRef, _temp9$1), subjectCount = variantIds.size, T0, documentTitle, t1, t2, t3, t4, totalCount;
	if ($[0] !== DocTitle || $[1] !== crossDatasetReferences.totalCount || $[2] !== documentId || $[3] !== documentType || $[4] !== internalReferences.totalCount || $[5] !== portalElementName || $[6] !== subjectCount || $[7] !== t) {
		let subject = t("document-group.subject.version", { count: subjectCount });
		totalCount = internalReferences.totalCount + crossDatasetReferences.totalCount;
		let t5;
		$[15] !== documentId || $[16] !== documentType ? (t5 = {
			_id: documentId,
			_type: documentType
		}, $[15] = documentId, $[16] = documentType, $[17] = t5) : t5 = $[17];
		let t6 = t5, t7;
		$[18] !== DocTitle || $[19] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DocTitle, { document: t6 }), $[18] = DocTitle, $[19] = t6, $[20] = t7) : t7 = $[20], documentTitle = t7, T0 = Dialog$1, t1 = "confirmVariantDeletion", t2 = 1, t3 = portalElementName, t4 = t("document-group.delete.title", {
			count: subjectCount,
			subject
		}), $[0] = DocTitle, $[1] = crossDatasetReferences.totalCount, $[2] = documentId, $[3] = documentType, $[4] = internalReferences.totalCount, $[5] = portalElementName, $[6] = subjectCount, $[7] = t, $[8] = T0, $[9] = documentTitle, $[10] = t1, $[11] = t2, $[12] = t3, $[13] = t4, $[14] = totalCount;
	} else T0 = $[8], documentTitle = $[9], t1 = $[10], t2 = $[11], t3 = $[12], t4 = $[13], totalCount = $[14];
	let t5;
	$[21] === deletionRef ? t5 = $[22] : (t5 = () => deletionRef.send({ type: "delete.cancel" }), $[21] = deletionRef, $[22] = t5);
	let t6;
	$[23] === t ? t6 = $[24] : (t6 = t("document-group.delete.cancel-button.text"), $[23] = t, $[24] = t6);
	let t7;
	$[25] !== t5 || $[26] !== t6 ? (t7 = {
		onClick: t5,
		text: t6
	}, $[25] = t5, $[26] = t6, $[27] = t7) : t7 = $[27];
	let t8;
	$[28] !== subjectCount || $[29] !== t ? (t8 = t("document-group.delete.confirm-button.text", { count: subjectCount }), $[28] = subjectCount, $[29] = t, $[30] = t8) : t8 = $[30];
	let t9;
	$[31] === deletionRef ? t9 = $[32] : (t9 = () => deletionRef.send({ type: "delete.confirm" }), $[31] = deletionRef, $[32] = t9);
	let t10 = !canConfirmDeletion, t11;
	$[33] !== t10 || $[34] !== t8 || $[35] !== t9 ? (t11 = {
		text: t8,
		onClick: t9,
		disabled: t10
	}, $[33] = t10, $[34] = t8, $[35] = t9, $[36] = t11) : t11 = $[36];
	let t12;
	$[37] !== t11 || $[38] !== t7 ? (t12 = {
		cancelButton: t7,
		confirmButton: t11
	}, $[37] = t11, $[38] = t7, $[39] = t12) : t12 = $[39];
	let t13;
	$[40] === deletionRef ? t13 = $[41] : (t13 = () => deletionRef.send({ type: "delete.cancel" }), $[40] = deletionRef, $[41] = t13);
	let t14;
	$[42] !== error || $[43] !== t ? (t14 = error ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		tone: "critical",
		padding: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			children: t("document-group.delete.error.message")
		})
	}) : null, $[42] = error, $[43] = t, $[44] = t14) : t14 = $[44];
	let t15;
	$[45] === variantIds ? t15 = $[46] : (t15 = [...variantIds], $[45] = variantIds, $[46] = t15);
	let t16;
	$[47] !== VersionsPreviewList || $[48] !== documentType || $[49] !== t15 ? (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VersionsPreviewList, {
		documentType,
		documentVersions: t15
	}), $[47] = VersionsPreviewList, $[48] = documentType, $[49] = t15, $[50] = t16) : t16 = $[50];
	let t17;
	$[51] !== ReferencePreviewLink || $[52] !== crossDatasetReferences || $[53] !== datasetNames || $[54] !== documentId || $[55] !== documentTitle || $[56] !== hasUnknownDatasetNames || $[57] !== internalReferences || $[58] !== t || $[59] !== totalCount || $[60] !== warnIncomingReferences ? (t17 = warnIncomingReferences && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		padding: 3,
		radius: 2,
		tone: "caution",
		flex: "none",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			"aria-hidden": "true",
			size: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WarningOutlineIcon, {})
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			flex: 1,
			marginLeft: 3,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Translate, {
					i18nKey: "document-group.delete.referring-document-count.text",
					components: { DocumentTitle: () => documentTitle },
					t,
					values: { count: totalCount }
				})
			})
		})] })
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(References, {
		documentId,
		documentTitle,
		internalReferences,
		crossDatasetReferences,
		datasetNames,
		hasUnknownDatasetNames,
		ReferencePreviewLink
	})] }), $[51] = ReferencePreviewLink, $[52] = crossDatasetReferences, $[53] = datasetNames, $[54] = documentId, $[55] = documentTitle, $[56] = hasUnknownDatasetNames, $[57] = internalReferences, $[58] = t, $[59] = totalCount, $[60] = warnIncomingReferences, $[61] = t17) : t17 = $[61];
	let t18;
	$[62] !== t14 || $[63] !== t16 || $[64] !== t17 ? (t18 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 4,
		children: [
			t14,
			t16,
			t17
		]
	}), $[62] = t14, $[63] = t16, $[64] = t17, $[65] = t18) : t18 = $[65];
	let t19;
	return $[66] !== T0 || $[67] !== t1 || $[68] !== t12 || $[69] !== t13 || $[70] !== t18 || $[71] !== t2 || $[72] !== t3 || $[73] !== t4 ? (t19 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(T0, {
		id: t1,
		width: t2,
		portal: t3,
		header: t4,
		footer: t12,
		onClose: t13,
		children: t18
	}), $[66] = T0, $[67] = t1, $[68] = t12, $[69] = t13, $[70] = t18, $[71] = t2, $[72] = t3, $[73] = t4, $[74] = t19) : t19 = $[74], t19;
};
var References = (t0) => {
	let $ = (0, import_compiler_runtime.c)(37), { documentId, documentTitle, internalReferences, crossDatasetReferences, datasetNames, hasUnknownDatasetNames, onReferenceLinkClick, ReferencePreviewLink } = t0, schema = useSchema(), toast = useToast(), { t } = useTranslation$1(studioLocaleNamespace), t1;
	$[0] !== ReferencePreviewLink || $[1] !== onReferenceLinkClick || $[2] !== schema || $[3] !== t ? (t1 = (item) => {
		let type = schema.get(item._type);
		return type ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReferencePreviewLink, {
			type,
			value: item,
			onClick: onReferenceLinkClick
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			padding: 2,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SanityDefaultPreview, {
				icon: UnknownIcon,
				title: t("document-group.delete.preview-item.preview-unavailable.title"),
				subtitle: t("document-group.delete.preview-item.preview-unavailable.subtitle", { documentId: item._id }),
				layout: "default"
			})
		});
	}, $[0] = ReferencePreviewLink, $[1] = onReferenceLinkClick, $[2] = schema, $[3] = t, $[4] = t1) : t1 = $[4];
	let renderPreviewItem = t1;
	if (internalReferences.totalCount === 0 && crossDatasetReferences.totalCount === 0) return null;
	let t2;
	$[5] === hasUnknownDatasetNames ? t2 = $[6] : (t2 = hasUnknownDatasetNames ? ["unavailable"] : [], $[5] = hasUnknownDatasetNames, $[6] = t2);
	let t3;
	$[7] !== datasetNames || $[8] !== t2 ? (t3 = [...datasetNames, ...t2], $[7] = datasetNames, $[8] = t2, $[9] = t3) : t3 = $[9];
	let normalizedDatasetNames = t3, t4 = normalizedDatasetNames.length, t5 = normalizedDatasetNames.join(", "), t6 = hasUnknownDatasetNames && normalizedDatasetNames.length ? "unavailable" : "", t7;
	$[10] !== normalizedDatasetNames.length || $[11] !== t || $[12] !== t5 || $[13] !== t6 ? (t7 = t("document-group.delete.cdr-summary.subtitle", {
		count: t4,
		datasets: t5,
		context: t6
	}), $[10] = normalizedDatasetNames.length, $[11] = t, $[12] = t5, $[13] = t6, $[14] = t7) : t7 = $[14];
	let datasetSubtitle = t7, t8;
	$[15] === documentTitle ? t8 = $[16] : (t8 = { DocumentTitle: () => documentTitle }, $[15] = documentTitle, $[16] = t8);
	let t9;
	$[17] !== t || $[18] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		flex: "none",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Translate, {
				i18nKey: "document-group.delete.referring-documents-descriptor.text",
				t,
				components: t8
			})
		})
	}), $[17] = t, $[18] = t8, $[19] = t9) : t9 = $[19];
	let t10;
	$[20] !== internalReferences || $[21] !== renderPreviewItem ? (t10 = internalReferences.totalCount > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		as: "ul",
		gap: 2,
		"data-testid": "internal-references",
		children: [internalReferences.references.map((item_0) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			as: "li",
			children: renderPreviewItem(item_0)
		}, item_0._id)), internalReferences.totalCount > internalReferences.references.length && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			as: "li",
			padding: 3,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OtherReferenceCount, { ...internalReferences })
		})]
	}), $[20] = internalReferences, $[21] = renderPreviewItem, $[22] = t10) : t10 = $[22];
	let t11;
	$[23] !== crossDatasetReferences || $[24] !== datasetSubtitle || $[25] !== documentId || $[26] !== internalReferences.totalCount || $[27] !== normalizedDatasetNames.length || $[28] !== t || $[29] !== toast ? (t11 = crossDatasetReferences.totalCount > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(CrossDatasetReferencesDetails, {
		"data-testid": "cross-dataset-references",
		style: { borderTop: internalReferences.totalCount > 0 ? "1px solid var(--card-shadow-outline-color)" : void 0 },
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CrossDatasetReferencesSummary, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
			as: "a",
			marginTop: internalReferences.totalCount > 0 ? 2 : 0,
			radius: 2,
			shadow: 1,
			paddingY: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
				align: "center",
				gap: 3,
				paddingX: 3,
				paddingY: 1,
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
						size: 1,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DocumentsIcon, {})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
						gap: 2,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
							textOverflow: "ellipsis",
							size: 1,
							children: t("document-group.delete.cdr-summary.title", {
								count: normalizedDatasetNames.length,
								documentCount: t("document-group.delete.cdr-summary.document-count", { count: crossDatasetReferences.totalCount })
							})
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
							title: datasetSubtitle,
							textOverflow: "ellipsis",
							size: 1,
							muted: !0,
							children: datasetSubtitle
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronWrapper, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
						muted: !0,
						size: 1,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDownIcon, {})
					}) })
				]
			})
		}) }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Box, {
			overflow: "auto",
			paddingTop: 2,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Table, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("thead", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("tr", { children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
					muted: !0,
					size: 1,
					style: { minWidth: "5rem" },
					weight: "medium",
					children: t("document-group.delete.cdr-table.project-id.label")
				}) }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
					muted: !0,
					size: 1,
					weight: "medium",
					children: t("document-group.delete.cdr-table.dataset.label")
				}) }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
					muted: !0,
					size: 1,
					weight: "medium",
					children: t("document-group.delete.cdr-table.document-id.label")
				}) })
			] }) }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tbody", { children: crossDatasetReferences.references.filter(_temp0$1).map((t12, index) => {
				let { projectId, datasetName, documentId: referenceId } = t12;
				return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("tr", { children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
						size: 1,
						children: projectId
					}) }),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
						size: 1,
						children: datasetName || "unavailable"
					}) }),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DocumentIdFlex, {
						align: "center",
						gap: 2,
						justify: "flex-end",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
							textOverflow: "ellipsis",
							size: 1,
							children: referenceId || "unavailable"
						}), referenceId && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
							mode: "bleed",
							icon: CopyIcon,
							tooltipProps: { content: t("document-group.delete.cdr-table.copy-id-button.tooltip") },
							onClick: () => {
								navigator.clipboard.writeText(referenceId).catch(() => {
									toast.push({
										status: "error",
										title: t("document-group.delete.cdr-table.id-copied-toast.title-failed")
									});
								});
							}
						})]
					}) })
				] }, `${documentId}-${index}`);
			}) })] }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OtherReferenceCount, { ...crossDatasetReferences })]
		})]
	}), $[23] = crossDatasetReferences, $[24] = datasetSubtitle, $[25] = documentId, $[26] = internalReferences.totalCount, $[27] = normalizedDatasetNames.length, $[28] = t, $[29] = toast, $[30] = t11) : t11 = $[30];
	let t12;
	$[31] !== t10 || $[32] !== t11 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		radius: 2,
		shadow: 1,
		flex: "auto",
		padding: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			direction: "column",
			children: [t10, t11]
		})
	}), $[31] = t10, $[32] = t11, $[33] = t12) : t12 = $[33];
	let t13;
	return $[34] !== t12 || $[35] !== t9 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [t9, t12] }), $[34] = t12, $[35] = t9, $[36] = t13) : t13 = $[36], t13;
};
function _temp$50(t0) {
	let { context } = t0;
	return context.selectedIds;
}
function _temp2$20(t0) {
	let { context: context_0 } = t0;
	return context_0.internalReferences;
}
function _temp3$12(t0) {
	let { context: context_1 } = t0;
	return context_1.crossDatasetReferences;
}
function _temp4$3(t0) {
	let { context: context_2 } = t0;
	return context_2.datasetNames;
}
function _temp5$2(t0) {
	let { context: context_3 } = t0;
	return context_3.hasUnknownDatasetNames;
}
function _temp6$2(snapshot) {
	return snapshot.hasTag("warnIncomingReferences");
}
function _temp7$1(snapshot_0) {
	return snapshot_0.matches({ active: { deletion: "error" } });
}
function _temp8$1(t0) {
	let { context: context_4 } = t0;
	return context_4.error;
}
function _temp9$1(snapshot_1) {
	return snapshot_1.can({ type: "delete.confirm" });
}
function _temp0$1(reference) {
	return "projectId" in reference;
}
var Container$1 = /* @__PURE__ */ gt.div.withConfig({
	displayName: "Container",
	componentId: "sc-l8bk6-0"
})(({ theme }) => {
	let { container, space } = getTheme_v2(theme);
	return ft`display:grid;grid-template-rows:min-content 1fr min-content;inline-size:min(calc(${container[0]}px * 1.5),calc(100vw - (${space[3]}px * 2)));max-block-size:75vh;block-size:var(--intrinsic-block-size,auto);`;
});
var Header = /* @__PURE__ */ gt.div.withConfig({
	displayName: "Header",
	componentId: "sc-gmh3z-0"
})(({ theme }) => {
	let { space } = getTheme_v2(theme);
	return ft`min-inline-size:0;padding-inline:${space[4]}px;padding-block-start:${space[4]}px;padding-block-end:calc(${space[5]}px * 0.5);`;
});
var TextButton = /* @__PURE__ */ gt.button.withConfig({
	displayName: "TextButton",
	componentId: "sc-xank9c-0"
})(({ theme }) => {
	let { color } = getTheme_v2(theme);
	return ft`all:unset;display:inline-block;max-inline-size:100%;appearance:none;border:0;margin:0;padding:0;outline:none;color:${color.button.ghost.neutral.enabled.fg};*{color:inherit;}svg[data-sanity-icon]{color:currentColor;}`;
});
var SelectBundle = (t0) => {
	let $ = (0, import_compiler_runtime.c)(52), { variantCreationRef, selectionRef } = t0, { t } = useTranslation$1(studioLocaleNamespace), setVariant = useSetVariant(), selectedVariantDefinition = useSelector(variantCreationRef, _temp$49), bundles = useSelector(variantCreationRef, _temp2$19), isVariantCreationPending = useSelector(variantCreationRef, _temp3$11), canSelectBundle = useSelector(variantCreationRef, _temp4$2), selectedBundle = useSelector(variantCreationRef, _temp5$1), existingVariants = useSelector(selectionRef, _temp6$1), t1;
	$[0] !== selectedVariantDefinition || $[1] !== t ? (t1 = selectedVariantDefinition === void 0 ? t("document-group.create-variant") : t("document-group.create-variant.for-target", { variantDefinitionName: getVariantTitle(selectedVariantDefinition) }), $[0] = selectedVariantDefinition, $[1] = t, $[2] = t1) : t1 = $[2];
	let headerTitle = t1, T0, T1, t2, t3, t4, t5, t6;
	if ($[3] !== bundles || $[4] !== canSelectBundle || $[5] !== existingVariants || $[6] !== headerTitle || $[7] !== isVariantCreationPending || $[8] !== selectedBundle || $[9] !== selectedVariantDefinition?._id || $[10] !== setVariant || $[11] !== t || $[12] !== variantCreationRef) {
		let t7;
		$[20] === selectedVariantDefinition?._id ? t7 = $[21] : (t7 = (bundleKeys, variant) => {
			let variantId = variant.document?._system.variant?._ref;
			return variantId !== void 0 && variantId === selectedVariantDefinition?._id && bundleKeys.add(variant.document?._system.bundleId ?? variant.document?._system.release?._ref ?? "published"), bundleKeys;
		}, $[20] = selectedVariantDefinition?._id, $[21] = t7);
		let existingBundles = existingVariants.reduce(t7, /* @__PURE__ */ new Set()), t8;
		$[22] === variantCreationRef ? t8 = $[23] : (t8 = () => variantCreationRef.send({
			type: "createVariant.selectVariant",
			variantId: void 0
		}), $[22] = variantCreationRef, $[23] = t8);
		let t9;
		$[24] === Symbol.for("react.memo_cache_sentinel") ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronLeftIcon, {}), $[24] = t9) : t9 = $[24];
		let t10;
		$[25] === headerTitle ? t10 = $[26] : (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			weight: "medium",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
				gap: 2,
				align: "center",
				children: [t9, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TruncatedText, { children: headerTitle })]
			})
		}), $[25] = headerTitle, $[26] = t10), $[27] !== headerTitle || $[28] !== t10 || $[29] !== t8 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Header, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextButton, {
			title: headerTitle,
			onClick: t8,
			children: t10
		}) }), $[27] = headerTitle, $[28] = t10, $[29] = t8, $[30] = t6) : t6 = $[30], T1 = Body, T0 = Stack, t2 = 4, t3 = !existingBundles.has("drafts") && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 3,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
				as: "h3",
				children: t("document-group.create-variant.target-drafts")
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
				gap: 1,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					mode: "bleed",
					justify: "flex-start",
					paddingX: 3,
					paddingY: 3,
					text: t("release.chip.global.drafts"),
					icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseAvatarIcon, { release: "drafts" }),
					iconRight: isVariantCreationPending && selectedBundle?.type === "drafts" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Delay, {
						ms: 500,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Spinner, {})
					}),
					disabled: !canSelectBundle,
					onClick: () => {
						variantCreationRef.send({
							type: "createVariant.selectBundle",
							bundle: { type: "drafts" }
						}), variantCreationRef.send({ type: "createVariant.confirm" });
					}
				})
			})]
		});
		let t11;
		$[31] === t ? t11 = $[32] : (t11 = t("document-group.create-variant.target-releases"), $[31] = t, $[32] = t11);
		let t12;
		$[33] === t11 ? t12 = $[34] : (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
			as: "h3",
			children: t11
		}), $[33] = t11, $[34] = t12);
		let t15 = [...bundles.entries()].filter((t13) => {
			let [, bundle] = t13;
			return bundle.state !== "published" && isNotArchivedRelease(bundle) && !isReleaseScheduledOrScheduling(bundle) && !existingBundles.has(getReleaseIdFromReleaseDocumentId(bundle._id));
		}).map((t14) => {
			let [id, bundle_0] = t14;
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				mode: "bleed",
				justify: "flex-start",
				paddingX: 3,
				paddingY: 3,
				text: bundle_0.metadata.title ?? bundle_0._id,
				icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseAvatarIcon, { release: bundle_0 }),
				iconRight: isVariantCreationPending && selectedBundle?.type === "release" && selectedBundle.releaseId === id && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Delay, {
					ms: 500,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Spinner, {})
				}),
				disabled: !canSelectBundle,
				onClick: () => {
					variantCreationRef.send({
						type: "createVariant.selectBundle",
						bundle: {
							type: "release",
							releaseId: id
						}
					}), variantCreationRef.send({ type: "createVariant.confirm" });
				}
			}, id);
		}), t16;
		$[35] === t15 ? t16 = $[36] : (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
			gap: 1,
			children: t15
		}), $[35] = t15, $[36] = t16), $[37] !== t12 || $[38] !== t16 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 3,
			children: [t12, t16]
		}), $[37] = t12, $[38] = t16, $[39] = t4) : t4 = $[39], t5 = existingBundles.size !== 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 3,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
				as: "h3",
				children: t("document-group.create-variant.view-existing-variants")
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
				gap: 1,
				children: [...existingBundles.values()].map((bundleKey) => {
					let bundle_1 = bundles.get(getReleaseDocumentIdFromReleaseId(bundleKey));
					return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
						mode: "bleed",
						justify: "flex-start",
						paddingX: 3,
						paddingY: 3,
						text: (bundleKey === "drafts" ? t("release.chip.draft") : bundleKey === "published" ? t("release.chip.published") : bundle_1?.metadata.title ?? bundle_1?._id) ?? bundleKey,
						icon: bundle_1 !== void 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseAvatarIcon, { release: bundle_1 }),
						disabled: !canSelectBundle,
						onClick: () => setVariant({
							perspective: bundleKey,
							variantId: selectedVariantDefinition?._id
						})
					}, bundleKey);
				})
			})]
		}), $[3] = bundles, $[4] = canSelectBundle, $[5] = existingVariants, $[6] = headerTitle, $[7] = isVariantCreationPending, $[8] = selectedBundle, $[9] = selectedVariantDefinition?._id, $[10] = setVariant, $[11] = t, $[12] = variantCreationRef, $[13] = T0, $[14] = T1, $[15] = t2, $[16] = t3, $[17] = t4, $[18] = t5, $[19] = t6;
	} else T0 = $[13], T1 = $[14], t2 = $[15], t3 = $[16], t4 = $[17], t5 = $[18], t6 = $[19];
	let t7;
	$[40] !== T0 || $[41] !== t2 || $[42] !== t3 || $[43] !== t4 || $[44] !== t5 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(T0, {
		gap: t2,
		children: [
			t3,
			t4,
			t5
		]
	}), $[40] = T0, $[41] = t2, $[42] = t3, $[43] = t4, $[44] = t5, $[45] = t7) : t7 = $[45];
	let t8;
	$[46] !== T1 || $[47] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(T1, { children: t7 }), $[46] = T1, $[47] = t7, $[48] = t8) : t8 = $[48];
	let t9;
	return $[49] !== t6 || $[50] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [t6, t8] }), $[49] = t6, $[50] = t8, $[51] = t9) : t9 = $[51], t9;
};
var TruncatedText = gt.span.withConfig({
	displayName: "TruncatedText",
	componentId: "sc-ttftn9-0"
})`overflow:hidden;min-inline-size:0;text-overflow:ellipsis;white-space:nowrap;`;
function _temp$49(t0) {
	let { context } = t0;
	return context.variants?.variants.get(context.selectedVariantId ?? "");
}
function _temp2$19(t0) {
	let { context: context_0 } = t0;
	return context_0.releases?.releases ?? /* @__PURE__ */ new Map();
}
function _temp3$11(snapshot) {
	return snapshot.matches({ active: "creating" });
}
function _temp4$2(snapshot_0) {
	return snapshot_0.can({
		type: "createVariant.selectBundle",
		bundle: void 0
	});
}
function _temp5$1(t0) {
	let { context: context_1 } = t0;
	return context_1.selectedBundle;
}
function _temp6$1(t0) {
	let { context: context_2 } = t0;
	return context_2.variants;
}
var SelectVariantDefinition = (t0) => {
	let $ = (0, import_compiler_runtime.c)(19), { variantCreationRef } = t0, { t } = useTranslation$1(studioLocaleNamespace), variantDefinitions = useSelector(variantCreationRef, _temp$48), t1;
	$[0] === t ? t1 = $[1] : (t1 = t("document-group.create-variant"), $[0] = t, $[1] = t1);
	let t2;
	$[2] === variantCreationRef ? t2 = $[3] : (t2 = () => variantCreationRef.send({ type: "createVariant.cancel" }), $[2] = variantCreationRef, $[3] = t2);
	let t3;
	$[4] === Symbol.for("react.memo_cache_sentinel") ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronLeftIcon, {}), $[4] = t3) : t3 = $[4];
	let t4;
	$[5] === t ? t4 = $[6] : (t4 = t("document-group.create-variant"), $[5] = t, $[6] = t4);
	let t5;
	$[7] === t4 ? t5 = $[8] : (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		weight: "medium",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			gap: 2,
			align: "center",
			children: [t3, t4]
		})
	}), $[7] = t4, $[8] = t5);
	let t6;
	$[9] !== t1 || $[10] !== t2 || $[11] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Header, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextButton, {
		title: t1,
		onClick: t2,
		children: t5
	}) }), $[9] = t1, $[10] = t2, $[11] = t5, $[12] = t6) : t6 = $[12];
	let t7;
	$[13] !== variantCreationRef || $[14] !== variantDefinitions ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Body, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		gap: 1,
		children: [...variantDefinitions.entries()].map((t8) => {
			let [id, variantDefinition] = t8;
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
				mode: "bleed",
				justify: "flex-start",
				paddingX: 3,
				paddingY: 3,
				icon: RhombusIcon,
				text: getVariantTitle(variantDefinition),
				onClick: () => variantCreationRef.send({
					type: "createVariant.selectVariant",
					variantId: id
				})
			}, id);
		})
	}) }), $[13] = variantCreationRef, $[14] = variantDefinitions, $[15] = t7) : t7 = $[15];
	let t8;
	return $[16] !== t6 || $[17] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [t6, t7] }), $[16] = t6, $[17] = t7, $[18] = t8) : t8 = $[18], t8;
};
function _temp$48(t0) {
	let { context } = t0;
	return context.variants?.variants ?? /* @__PURE__ */ new Map();
}
var CreateVariant = (t0) => {
	let $ = (0, import_compiler_runtime.c)(11), { variantCreationRef, selectionRef } = t0, hasSelectedVariantId = useSelector(variantCreationRef, _temp$47), hasSelectedBundle = useSelector(variantCreationRef, _temp2$18), t1;
	$[0] !== hasSelectedBundle || $[1] !== hasSelectedVariantId || $[2] !== variantCreationRef ? (t1 = !hasSelectedVariantId && !hasSelectedBundle && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectVariantDefinition, { variantCreationRef }), $[0] = hasSelectedBundle, $[1] = hasSelectedVariantId, $[2] = variantCreationRef, $[3] = t1) : t1 = $[3];
	let t2;
	$[4] !== hasSelectedVariantId || $[5] !== selectionRef || $[6] !== variantCreationRef ? (t2 = hasSelectedVariantId && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectBundle, {
		variantCreationRef,
		selectionRef
	}), $[4] = hasSelectedVariantId, $[5] = selectionRef, $[6] = variantCreationRef, $[7] = t2) : t2 = $[7];
	let t3;
	return $[8] !== t1 || $[9] !== t2 ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [t1, t2] }), $[8] = t1, $[9] = t2, $[10] = t3) : t3 = $[10], t3;
};
function _temp$47(t0) {
	let { context } = t0;
	return context.selectedVariantId !== void 0;
}
function _temp2$18(t0) {
	let { context: context_0 } = t0;
	return context_0.selectedBundle !== void 0;
}
var Footer = /* @__PURE__ */ gt.div.withConfig({
	displayName: "Footer",
	componentId: "sc-z7b92b-0"
})(({ theme }) => {
	let { space } = getTheme_v2(theme);
	return ft`display:flex;padding-inline:${space[4]}px;padding-block-start:calc(${space[5]}px * 0.5);padding-block-end:${space[4]}px;gap:${space[3]}px;justify-content:end;`;
});
/**
* Finds the releases a variant hasn't been added to yet.
*/
function useVariantPendingReleases(t0) {
	let $ = (0, import_compiler_runtime.c)(8), { documentId, variantRef } = t0, t1;
	$[0] === documentId ? t1 = $[1] : (t1 = { documentId }, $[0] = documentId, $[1] = t1);
	let { versions } = useDocumentVersions(t1), { data: releases } = useActiveReleases(), t2;
	if ($[2] !== releases || $[3] !== variantRef || $[4] !== versions) {
		let t3;
		$[6] === variantRef ? t3 = $[7] : (t3 = (version) => variantRef ? version._system.variant?._ref === variantRef : !version._system.variant?._ref, $[6] = variantRef, $[7] = t3);
		let releasesOfThisVariant = versions.filter(t3).map(_temp$46);
		t2 = releases.filter((release) => !releasesOfThisVariant.includes(release._id)), $[2] = releases, $[3] = variantRef, $[4] = versions, $[5] = t2;
	} else t2 = $[5];
	return t2;
}
function _temp$46(version_0) {
	return version_0._system.release?._ref;
}
var StatusBadge = /* @__PURE__ */ gt(Badge).withConfig({
	displayName: "StatusBadge",
	componentId: "sc-4vs0bi-0"
})(({ theme }) => {
	let { space } = getTheme_v2(theme);
	return `
  span {
  display: flex;
  align-items: center;
  gap: ${space[2]}px;
  word-break: keep-all;
  white-space: noWrap;
  }
`;
});
var VariantCheckbox = gt(Checkbox).withConfig({
	displayName: "VariantCheckbox",
	componentId: "sc-f81hgi-0"
})`input::before{display:block;position:absolute;content:'';inset:-1rem;}`;
var VariantSet = gt(Card).attrs({
	border: !0,
	radius: 3
}).withConfig({
	displayName: "VariantSet",
	componentId: "sc-1fo9di-0"
})`overflow:hidden;`;
var VariantSetEntry = /* @__PURE__ */ gt.div.withConfig({
	displayName: "VariantSetEntry",
	componentId: "sc-9r6gzc-0"
})(({ theme }) => {
	let { color, space } = getTheme_v2(theme);
	return ft`position:relative;padding:${space[3]}px;word-break:break-all;justify-content:space-between;* + &{border-block-start:1px solid ${color.border};}&,.atom{display:flex;gap:${space[3]}px;align-items:center;}.inert{pointer-events:none;}.primary-action{appearance:none;position:absolute;margin:0;padding:0;border:0;background:none;inset:0;opacity:0;}&:hover,&:focus-within{background-color:var(--card-muted-bg-color);}&:has(:checked){background-color:${color.focusRing};}`;
});
var VariantSetHeader = /* @__PURE__ */ gt(VariantSetEntry).withConfig({
	displayName: "VariantSetHeader",
	componentId: "sc-azkc61-0"
})(({ theme }) => {
	let { radius } = getTheme_v2(theme);
	return ft`background-color:var(--card-muted-bg-color);border-radius:${radius[3]}px ${radius[3]}px 0 0;`;
});
var DocumentGroupInventory = (t0) => {
	let $ = (0, import_compiler_runtime.c)(99), { documentType, documentId, portalElementName, perspectiveList, referringDocuments$, requestClose, components } = t0, { beta } = useWorkspace(), variantsEnabled = beta?.variants?.enabled, { t } = useTranslation$1(studioLocaleNamespace), { t: feedbackT } = useTranslation$1(feedbackLocaleNamespace), client = useClient(DEFAULT_STUDIO_CLIENT_OPTIONS), t1 = useSchema(), t2;
	$[0] !== documentType || $[1] !== t1 ? (t2 = t1.get(documentType), $[0] = documentType, $[1] = t1, $[2] = t2) : t2 = $[2];
	let schema = t2, t3;
	$[3] === documentId ? t3 = $[4] : (t3 = { documentId }, $[3] = documentId, $[4] = t3);
	let versionState = useDocumentVersionsObservable(t3), { state$: releases } = useReleasesStore(), { state$: agentBundles } = useAgentBundlesStore(), { state$: variants } = useVariantsStore(), [containerElement, setContainerElement] = (0, import_react.useState)(null), t4;
	$[5] === Symbol.for("react.memo_cache_sentinel") ? (t4 = new Subject(), $[5] = t4) : t4 = $[5];
	let filterStringEvent = t4, [menuPortalElement, setMenuPortalElement] = (0, import_react.useState)(null), { feedbackDialogOpened } = useFeedbackTelemetry(), setVariant = useSetVariant(), { createVariantDocument } = useVariantDocumentOperations(), documentStore = useDocumentStore(), t5;
	$[6] === Symbol.for("react.memo_cache_sentinel") ? (t5 = filterStringEvent.pipe(map(_temp$45), debounceTime(150), map(_temp2$17), map(_temp3$10), startWith(void 0)), $[6] = t5) : t5 = $[6];
	let filterString = t5, t6;
	if ($[7] !== agentBundles || $[8] !== feedbackDialogOpened || $[9] !== releases || $[10] !== variants || $[11] !== versionState) {
		let t7;
		$[13] !== agentBundles || $[14] !== releases || $[15] !== variants || $[16] !== versionState ? (t7 = () => combineLatest({
			versionState,
			releases,
			variants,
			agentBundles
		}), $[13] = agentBundles, $[14] = releases, $[15] = variants, $[16] = versionState, $[17] = t7) : t7 = $[17], t6 = documentGroupInventoryMachine.provide({
			actors: { meta: fromObservable(t7) },
			actions: { onFeedbackBegin: feedbackDialogOpened }
		}), $[7] = agentBundles, $[8] = feedbackDialogOpened, $[9] = releases, $[10] = variants, $[11] = versionState, $[12] = t6;
	} else t6 = $[12];
	let inventoryMachine = t6, t7;
	$[18] === Symbol.for("react.memo_cache_sentinel") ? (t7 = selectionMachine.provide({ actors: { filterString: fromObservable(() => filterString) } }), $[18] = t7) : t7 = $[18];
	let t8 = t7, t9;
	if ($[19] !== client || $[20] !== referringDocuments$) {
		let t10;
		$[22] === referringDocuments$ ? t10 = $[23] : (t10 = () => referringDocuments$, $[22] = referringDocuments$, $[23] = t10);
		let t11;
		$[24] === client ? t11 = $[25] : (t11 = (t12) => {
			let { input, signal } = t12;
			return input.ids.reduce(_temp4$1, client.transaction()).commit({
				tag: "document.delete",
				skipCrossDatasetReferenceValidation: !0,
				signal
			});
		}, $[24] = client, $[25] = t11), t9 = deletionMachine.provide({ actors: {
			referringDocuments: fromObservable(t10),
			deleteVariants: fromPromise(t11)
		} }), $[19] = client, $[20] = referringDocuments$, $[21] = t9;
	} else t9 = $[21];
	let t10 = t9, t11;
	if ($[26] !== createVariantDocument || $[27] !== documentId || $[28] !== documentStore.pair || $[29] !== documentType || $[30] !== releases || $[31] !== setVariant || $[32] !== variants) {
		let t12;
		$[34] === variants ? t12 = $[35] : (t12 = () => variants, $[34] = variants, $[35] = t12);
		let t13;
		$[36] === releases ? t13 = $[37] : (t13 = () => releases, $[36] = releases, $[37] = t13);
		let t14;
		$[38] !== createVariantDocument || $[39] !== documentId || $[40] !== documentStore.pair || $[41] !== documentType || $[42] !== setVariant ? (t14 = async (t15) => {
			let { input: input_0, signal: signal_0 } = t15, bundleId = typeof input_0.bundle == "string" ? void 0 : getReleaseIdFromReleaseDocumentId(input_0.bundle._id), editStateSlot = typeof input_0.bundle == "string" ? input_0.bundle === "drafts" ? "draft" : "published" : "version", baseVariant = (await firstValueFrom(documentStore.pair.editState(getPublishedId(documentId), documentType, bundleId).pipe(filter(_temp5), timeout({ first: 3e4 }))))[editStateSlot];
			baseVariant === null && await createVariantDocument({
				documentGroupId: getPublishedId(documentId),
				document: { _type: documentType },
				variant: input_0.variantDefinition,
				selectedPerspective: input_0.bundle,
				signal: signal_0
			}), baseVariant !== null && await createVariantDocument({
				documentGroupId: getPublishedId(documentId),
				baseId: baseVariant._id,
				variant: input_0.variantDefinition,
				selectedPerspective: input_0.bundle,
				signal: signal_0
			}), setVariant({
				variantId: input_0.variantDefinition._id,
				perspective: typeof input_0.bundle == "string" ? input_0.bundle : getReleaseIdFromReleaseDocumentId(input_0.bundle._id)
			});
		}, $[38] = createVariantDocument, $[39] = documentId, $[40] = documentStore.pair, $[41] = documentType, $[42] = setVariant, $[43] = t14) : t14 = $[43], t11 = variantCreationMachine.provide({ actors: {
			variants: fromObservable(t12),
			releases: fromObservable(t13),
			createVariant: fromPromise(t14)
		} }), $[26] = createVariantDocument, $[27] = documentId, $[28] = documentStore.pair, $[29] = documentType, $[30] = releases, $[31] = setVariant, $[32] = variants, $[33] = t11;
	} else t11 = $[33];
	let t12 = t11, t13;
	$[44] !== t || $[45] !== t10 || $[46] !== t12 || $[47] !== variantsEnabled ? (t13 = { input: {
		t,
		variantsEnabled,
		selectionMachine: t8,
		deletionMachine: t10,
		variantCreationMachine: t12
	} }, $[44] = t, $[45] = t10, $[46] = t12, $[47] = variantsEnabled, $[48] = t13) : t13 = $[48];
	let inventoryRef = useActorRef(inventoryMachine, t13), selectionRef = useSelector(inventoryRef, _temp6), deletionRef = useSelector(inventoryRef, _temp7), variantCreationRef = useSelector(inventoryRef, _temp8), metaState = useSelector(inventoryRef, _temp9), selectionCount = useSelector(selectionRef, _temp0), isReadOnly = useSelector(selectionRef, _temp1), isDeletionActive = useSelector(deletionRef, _temp10), isFeedbackActive = useSelector(inventoryRef, _temp11), isVariantCreationActive = useSelector(inventoryRef, _temp12), isVariantCreationPending = useSelector(variantCreationRef, _temp13), canRequestDeletion = useSelector(deletionRef, _temp14), [isActive, setIsActive] = (0, import_react.useState)(!1), t14, t15;
	$[49] === metaState ? (t14 = $[50], t15 = $[51]) : (t14 = () => {
		let frame = requestAnimationFrame(() => setIsActive(metaState === "ready"));
		return () => cancelAnimationFrame(frame);
	}, t15 = [metaState], $[49] = metaState, $[50] = t14, $[51] = t15), (0, import_react.useLayoutEffect)(t14, t15);
	let t16;
	$[52] !== containerElement || $[53] !== isActive ? (t16 = {
		element: containerElement,
		isActive
	}, $[52] = containerElement, $[53] = isActive, $[54] = t16) : t16 = $[54], usePreserveIntrinsicBlockSize(t16);
	let t17;
	$[55] !== isVariantCreationActive || $[56] !== isVariantCreationPending || $[57] !== selectionRef || $[58] !== variantCreationRef ? (t17 = (isVariantCreationActive || isVariantCreationPending) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CreateVariant, {
		variantCreationRef,
		selectionRef
	}), $[55] = isVariantCreationActive, $[56] = isVariantCreationPending, $[57] = selectionRef, $[58] = variantCreationRef, $[59] = t17) : t17 = $[59];
	let t18;
	$[60] !== canRequestDeletion || $[61] !== deletionRef || $[62] !== documentId || $[63] !== documentType || $[64] !== feedbackT || $[65] !== inventoryRef || $[66] !== isReadOnly || $[67] !== isVariantCreationActive || $[68] !== menuPortalElement || $[69] !== perspectiveList || $[70] !== requestClose || $[71] !== schema || $[72] !== selectionCount || $[73] !== selectionRef || $[74] !== setVariant || $[75] !== t || $[76] !== variantCreationRef || $[77] !== variantsEnabled ? (t18 = !isVariantCreationActive && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Header, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 4,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
				gap: 4,
				align: "center",
				justify: "flex-end",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextButton, {
					onClick: () => inventoryRef.send({ type: "feedback.begin" }),
					title: feedbackT("feedback.menu-item"),
					"aria-label": feedbackT("feedback.menu-item"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
						size: 1,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FeedbackIcon, {})
					})
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextButton, {
					onClick: requestClose,
					title: t("document-group-inventory.action.cancel"),
					"aria-label": t("document-group-inventory.action.cancel"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
						size: 1,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CloseIcon, {})
					})
				})]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("search", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextInput, {
				name: t("document-group-inventory.filter-string.label", { subject: t("document-group.subject.version_other") }),
				placeholder: t("document-group-inventory.filter-string.label", { subject: t("document-group.subject.version_other") }),
				icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SearchIcon, {}),
				readOnly: isReadOnly,
				onChange: (event_0) => filterStringEvent.next(event_0)
			}) })]
		}) }),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Body, { children: schema && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Select$1, {
			machine: selectionRef,
			inventoryRef,
			documentId,
			documentType,
			menuPortalElement,
			perspectiveList,
			onPrimaryAction: setVariant
		}) }),
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Footer, { children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
				text: t("document-group-inventory.action.cancel"),
				size: "large",
				mode: "bleed",
				onClick: requestClose
			}),
			variantsEnabled && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
				text: t("document-group.create-variant"),
				tone: "suggest",
				size: "large",
				icon: CreateVariantIcon,
				onClick: () => variantCreationRef.send({ type: "createVariant.request" })
			}),
			canRequestDeletion && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
				text: t("document-group.delete.confirm-button.text", { count: selectionCount }),
				onClick: () => deletionRef.send({ type: "delete.request" }),
				tone: "critical",
				size: "large",
				icon: TrashIcon
			})
		] })
	] }), $[60] = canRequestDeletion, $[61] = deletionRef, $[62] = documentId, $[63] = documentType, $[64] = feedbackT, $[65] = inventoryRef, $[66] = isReadOnly, $[67] = isVariantCreationActive, $[68] = menuPortalElement, $[69] = perspectiveList, $[70] = requestClose, $[71] = schema, $[72] = selectionCount, $[73] = selectionRef, $[74] = setVariant, $[75] = t, $[76] = variantCreationRef, $[77] = variantsEnabled, $[78] = t18) : t18 = $[78];
	let t19;
	$[79] !== t17 || $[80] !== t18 ? (t19 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Container$1, {
		ref: setContainerElement,
		"data-testid": "document-group-inventory",
		children: [t17, t18]
	}), $[79] = t17, $[80] = t18, $[81] = t19) : t19 = $[81];
	let t20;
	$[82] === Symbol.for("react.memo_cache_sentinel") ? (t20 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { ref: setMenuPortalElement }), $[82] = t20) : t20 = $[82];
	let t21;
	$[83] !== components || $[84] !== deletionRef || $[85] !== documentId || $[86] !== documentType || $[87] !== isDeletionActive || $[88] !== portalElementName || $[89] !== selectionRef ? (t21 = isDeletionActive && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfirmDeleteDialog, {
		documentId,
		documentType,
		deletionRef,
		selectionRef,
		portalElementName,
		components
	}), $[83] = components, $[84] = deletionRef, $[85] = documentId, $[86] = documentType, $[87] = isDeletionActive, $[88] = portalElementName, $[89] = selectionRef, $[90] = t21) : t21 = $[90];
	let t22;
	$[91] !== inventoryRef || $[92] !== isFeedbackActive || $[93] !== t ? (t22 = isFeedbackActive && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioFeedbackDialog, {
		dsn: "https://8914c8dde7e1ebce191f15af8bf6b7b9@sentry.sanity.io/4507342122123264",
		feedbackVersion: "1",
		source: "document-group-inventory",
		onClose: () => inventoryRef.send({ type: "feedback.end" }),
		sentimentLabel: t("document-group-inventory.feedback.sentiment-label")
	}), $[91] = inventoryRef, $[92] = isFeedbackActive, $[93] = t, $[94] = t22) : t22 = $[94];
	let t23;
	return $[95] !== t19 || $[96] !== t21 || $[97] !== t22 ? (t23 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		t19,
		t20,
		t21,
		t22
	] }), $[95] = t19, $[96] = t21, $[97] = t22, $[98] = t23) : t23 = $[98], t23;
};
var Select$1 = (t0) => {
	let $ = (0, import_compiler_runtime.c)(23), { machine, inventoryRef, documentId, documentType, onPrimaryAction, menuPortalElement, perspectiveList } = t0;
	useTranslation$1(studioLocaleNamespace);
	let sets = useSelector(inventoryRef, _temp15), hasFilterString = useSelector(machine, _temp16), filterMatchingVariantIds = useSelector(machine, _temp17), t1;
	if ($[0] !== documentId || $[1] !== documentType || $[2] !== filterMatchingVariantIds || $[3] !== hasFilterString || $[4] !== inventoryRef || $[5] !== machine || $[6] !== menuPortalElement || $[7] !== onPrimaryAction || $[8] !== perspectiveList || $[9] !== sets) {
		let t2;
		$[11] !== documentId || $[12] !== documentType || $[13] !== filterMatchingVariantIds || $[14] !== hasFilterString || $[15] !== inventoryRef || $[16] !== machine || $[17] !== menuPortalElement || $[18] !== onPrimaryAction || $[19] !== perspectiveList ? (t2 = (set) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(VariantSet, {
			"data-variant-set": set.name,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(VariantSetHeader, {
				as: "header",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
					size: 1,
					weight: "medium",
					children: set.name
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextButton, {
					onClick: () => {
						set.variants.forEach((variant) => machine.send({
							type: "selection.add",
							variantId: variant.id
						}));
					},
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
						size: 1,
						children: `Select all ${set.variants.length}`
					})
				})]
			}), set.variants.filter((t3) => {
				let { id } = t3;
				return !hasFilterString || filterMatchingVariantIds.has(id);
			}).map((variant_0) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Variant, {
				variant: variant_0,
				machine,
				inventoryRef,
				documentType,
				onPrimaryAction,
				isActive: documentId === variant_0.id,
				isSelectable: !0,
				menuPortalElement,
				perspectiveList
			}, variant_0.id))]
		}, set.key), $[11] = documentId, $[12] = documentType, $[13] = filterMatchingVariantIds, $[14] = hasFilterString, $[15] = inventoryRef, $[16] = machine, $[17] = menuPortalElement, $[18] = onPrimaryAction, $[19] = perspectiveList, $[20] = t2) : t2 = $[20], t1 = sets.map(t2), $[0] = documentId, $[1] = documentType, $[2] = filterMatchingVariantIds, $[3] = hasFilterString, $[4] = inventoryRef, $[5] = machine, $[6] = menuPortalElement, $[7] = onPrimaryAction, $[8] = perspectiveList, $[9] = sets, $[10] = t1;
	} else t1 = $[10];
	let t2;
	return $[21] === t1 ? t2 = $[22] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		gap: 5,
		children: t1
	}), $[21] = t1, $[22] = t2), t2;
};
var Variant = (t0) => {
	let $ = (0, import_compiler_runtime.c)(86), { variant, machine, inventoryRef, documentType, onPrimaryAction, isActive: isSelected, isSelectable, menuPortalElement, perspectiveList } = t0, { t } = useTranslation$1(studioLocaleNamespace), releasesToolAvailable = useReleasesToolAvailable(), { loading: releasesLoading } = useActiveReleases(), { document } = variant, versionId = document._id, documentGroupId = document._system.group._ref, releaseRef = document._system.release?._ref, isPublishedVersion = !document._system.bundleId, isDraftVersion = document._system.bundleId === "drafts";
	isVersionId(versionId);
	let bundleId = isPublishedVersion ? "published" : isDraftVersion ? "draft" : document._system.bundleId ?? "", agentBundleName = isAgentBundleName(document._system.bundleId) ? document._system.bundleId : void 0, isReadOnly = useSelector(machine, _temp18), selectedIds = useSelector(machine, _temp19), releases = useSelector(inventoryRef, _temp20), { clearScheduledDraftPerspective } = perspectiveList, t1;
	$[0] !== releaseRef || $[1] !== releases ? (t1 = releaseRef ? releases.get(releaseRef) : void 0, $[0] = releaseRef, $[1] = releases, $[2] = t1) : t1 = $[2];
	let release = t1, t2 = document._system.variant?._ref, t3;
	$[3] !== documentGroupId || $[4] !== t2 ? (t3 = {
		documentId: documentGroupId,
		variantRef: t2
	}, $[3] = documentGroupId, $[4] = t2, $[5] = t3) : t3 = $[5];
	let pendingReleases = useVariantPendingReleases(t3), t4;
	$[6] !== clearScheduledDraftPerspective || $[7] !== document || $[8] !== documentGroupId || $[9] !== documentType || $[10] !== isReadOnly ? (t4 = {
		documentGroupId,
		documentVersionInfoStub: document,
		documentType,
		disabled: isReadOnly,
		onCopyToDraftsComplete: clearScheduledDraftPerspective
	}, $[6] = clearScheduledDraftPerspective, $[7] = document, $[8] = documentGroupId, $[9] = documentType, $[10] = isReadOnly, $[11] = t4) : t4 = $[11];
	let { contextMenu, handleContextMenu, popoverRef, referenceElement, setReferenceElement, dialogState, closeDialog, openDiscardDialog, openCreateReleaseDialog, handleCopyToDrafts, handleAddVersion, isScheduledDraft, scheduledDraftMenuActions, sourceReleasePerspective } = useVersionContextMenu(t4), contextMenuHandler = isReadOnly || !releasesToolAvailable ? void 0 : handleContextMenu, t5 = isSelected || void 0, t6;
	$[12] !== agentBundleName || $[13] !== document || $[14] !== onPrimaryAction || $[15] !== releaseRef ? (t6 = () => {
		let bundle;
		bb48: switch (readVersionType(document)) {
			case "release":
				bundle = getReleaseIdFromReleaseDocumentId(releaseRef ?? "");
				break bb48;
			case "published":
				bundle = "published";
				break bb48;
			case "draft": bundle = "drafts";
		}
		let variantId = isVariantId(document._system.variant?._ref) ? document._system.variant._ref : void 0;
		onPrimaryAction({
			variantId,
			perspective: agentBundleName ?? bundle
		});
	}, $[12] = agentBundleName, $[13] = document, $[14] = onPrimaryAction, $[15] = releaseRef, $[16] = t6) : t6 = $[16];
	let t7;
	$[17] !== contextMenuHandler || $[18] !== setReferenceElement || $[19] !== t6 || $[20] !== variant.name ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
		type: "button",
		className: "primary-action",
		ref: setReferenceElement,
		onClick: t6,
		onContextMenu: contextMenuHandler,
		children: variant.name
	}), $[17] = contextMenuHandler, $[18] = setReferenceElement, $[19] = t6, $[20] = variant.name, $[21] = t7) : t7 = $[21];
	let t8;
	$[22] !== isReadOnly || $[23] !== isSelectable || $[24] !== machine || $[25] !== selectedIds || $[26] !== variant.id ? (t8 = isSelectable && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VariantCheckbox, {
		checked: selectedIds.has(variant.id),
		readOnly: isReadOnly,
		onChange: () => {
			machine.send({
				type: "selection.toggle",
				variantId: variant.id
			});
		}
	}), $[22] = isReadOnly, $[23] = isSelectable, $[24] = machine, $[25] = selectedIds, $[26] = variant.id, $[27] = t8) : t8 = $[27];
	let t9;
	$[28] === variant.name ? t9 = $[29] : (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		weight: "medium",
		className: "inert",
		children: variant.name
	}), $[28] = variant.name, $[29] = t9);
	let t10;
	$[30] !== t7 || $[31] !== t8 || $[32] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: "atom",
		children: [
			t7,
			t8,
			t9
		]
	}), $[30] = t7, $[31] = t8, $[32] = t9, $[33] = t10) : t10 = $[33];
	let t11;
	$[34] !== isSelected || $[35] !== t ? (t11 = isSelected && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(StatusBadge, {
		radius: 2,
		tone: "primary",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(EyeOpenIcon, {}),
			" ",
			t("document-group-inventory.viewing-item-label")
		]
	}), $[34] = isSelected, $[35] = t, $[36] = t11) : t11 = $[36];
	let t12;
	$[37] !== agentBundleName || $[38] !== isDraftVersion || $[39] !== isPublishedVersion || $[40] !== release ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		children: agentBundleName ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseAvatarIcon, { tone: "suggest" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseAvatarIcon, { release: (isDraftVersion ? "drafts" : isPublishedVersion ? "published" : release) ?? "" })
	}), $[37] = agentBundleName, $[38] = isDraftVersion, $[39] = isPublishedVersion, $[40] = release, $[41] = t12) : t12 = $[41];
	let t13;
	$[42] !== t11 || $[43] !== t12 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: "atom inert",
		children: [t11, t12]
	}), $[42] = t11, $[43] = t12, $[44] = t13) : t13 = $[44];
	let t14;
	$[45] !== t10 || $[46] !== t13 || $[47] !== t5 || $[48] !== variant.name ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(VariantSetEntry, {
		"data-variant-name": variant.name,
		"data-selected": t5,
		children: [t10, t13]
	}), $[45] = t10, $[46] = t13, $[47] = t5, $[48] = variant.name, $[49] = t14) : t14 = $[49];
	let t15 = !!menuPortalElement, t16;
	$[50] !== bundleId || $[51] !== contextMenu || $[52] !== documentGroupId || $[53] !== documentType || $[54] !== handleAddVersion || $[55] !== handleCopyToDrafts || $[56] !== isReadOnly || $[57] !== isScheduledDraft || $[58] !== openCreateReleaseDialog || $[59] !== openDiscardDialog || $[60] !== pendingReleases || $[61] !== popoverRef || $[62] !== referenceElement || $[63] !== release || $[64] !== releasesLoading || $[65] !== scheduledDraftMenuActions || $[66] !== t15 || $[67] !== versionId ? (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VersionContextMenuPopover, {
		contextMenu,
		popoverRef,
		referenceElement,
		documentGroupId,
		documentType,
		bundleId,
		releases: pendingReleases,
		releasesLoading,
		versionId,
		onDiscard: openDiscardDialog,
		onCreateRelease: openCreateReleaseDialog,
		onCopyToDrafts: handleCopyToDrafts,
		onCreateVersion: handleAddVersion,
		disabled: isReadOnly,
		release,
		isScheduledDraft,
		scheduledDraftMenuActions,
		portal: t15,
		isDiscardable: !1
	}), $[50] = bundleId, $[51] = contextMenu, $[52] = documentGroupId, $[53] = documentType, $[54] = handleAddVersion, $[55] = handleCopyToDrafts, $[56] = isReadOnly, $[57] = isScheduledDraft, $[58] = openCreateReleaseDialog, $[59] = openDiscardDialog, $[60] = pendingReleases, $[61] = popoverRef, $[62] = referenceElement, $[63] = release, $[64] = releasesLoading, $[65] = scheduledDraftMenuActions, $[66] = t15, $[67] = versionId, $[68] = t16) : t16 = $[68];
	let t17;
	$[69] !== menuPortalElement || $[70] !== t16 ? (t17 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PortalProvider, {
		element: menuPortalElement,
		children: t16
	}), $[69] = menuPortalElement, $[70] = t16, $[71] = t17) : t17 = $[71];
	let t18 = isScheduledDraft && scheduledDraftMenuActions.dialogs, t19;
	$[72] !== closeDialog || $[73] !== dialogState || $[74] !== documentType || $[75] !== handleAddVersion || $[76] !== handleCopyToDrafts || $[77] !== sourceReleasePerspective || $[78] !== t18 || $[79] !== variant.name || $[80] !== versionId ? (t19 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VersionContextMenuDialogs, {
		dialogState,
		onClose: closeDialog,
		versionId,
		documentType,
		title: variant.name,
		sourceReleasePerspective,
		onCreateVersion: handleAddVersion,
		onCopyToDrafts: handleCopyToDrafts,
		scheduledDraftDialogs: t18
	}), $[72] = closeDialog, $[73] = dialogState, $[74] = documentType, $[75] = handleAddVersion, $[76] = handleCopyToDrafts, $[77] = sourceReleasePerspective, $[78] = t18, $[79] = variant.name, $[80] = versionId, $[81] = t19) : t19 = $[81];
	let t20;
	return $[82] !== t14 || $[83] !== t17 || $[84] !== t19 ? (t20 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		t14,
		t17,
		t19
	] }), $[82] = t14, $[83] = t17, $[84] = t19, $[85] = t20) : t20 = $[85], t20;
};
/**
* Preserve the intrinsic block size of an element by maintaining an `--intrinsic-block-size`
* custom property. This custom property must be used by styles to control the element's size.
*/
function usePreserveIntrinsicBlockSize(t0) {
	let $ = (0, import_compiler_runtime.c)(10), { isActive, element } = t0, t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = new Subject(), $[0] = t1) : t1 = $[0];
	let size = t1, currentSize = useSyncObservable(size), t2, t3;
	$[1] !== element || $[2] !== isActive ? (t2 = () => {
		let resizeObserver = new ResizeObserver((t4) => {
			let [entry] = t4;
			isActive || size.next(entry.contentRect);
		});
		return element && resizeObserver.observe(element), () => resizeObserver.disconnect();
	}, t3 = [
		isActive,
		element,
		size
	], $[1] = element, $[2] = isActive, $[3] = t2, $[4] = t3) : (t2 = $[3], t3 = $[4]), (0, import_react.useEffect)(t2, t3);
	let t4, t5;
	$[5] !== currentSize || $[6] !== element || $[7] !== isActive ? (t4 = () => {
		if (!element || !currentSize) return _temp21;
		let cleanUp = () => element.style.removeProperty("--intrinsic-block-size");
		return isActive ? (element?.style.setProperty("--intrinsic-block-size", `${currentSize.height}px`), cleanUp) : (cleanUp(), _temp22);
	}, t5 = [
		element,
		currentSize,
		isActive
	], $[5] = currentSize, $[6] = element, $[7] = isActive, $[8] = t4, $[9] = t5) : (t4 = $[8], t5 = $[9]), (0, import_react.useEffect)(t4, t5);
}
function _temp22() {}
function _temp21() {}
function _temp$45(event) {
	return event.target.value;
}
function _temp2$17(value) {
	return value.trim();
}
function _temp3$10(value_0) {
	return value_0.length > 1 ? value_0 : void 0;
}
function _temp4$1(pendingTransaction, id) {
	return pendingTransaction.delete(id);
}
function _temp5(t0) {
	let { ready } = t0;
	return ready;
}
function _temp6(t0) {
	let { context } = t0;
	return context.selectionRef;
}
function _temp7(t0) {
	let { context: context_0 } = t0;
	return context_0.deletionRef;
}
function _temp8(t0) {
	let { context: context_1 } = t0;
	return context_1.variantCreationRef;
}
function _temp9(t0) {
	let { context: context_2 } = t0;
	return context_2.metaState;
}
function _temp0(t0) {
	let { context: context_3 } = t0;
	return context_3.selectedIds.size;
}
function _temp1(snapshot) {
	return snapshot.matches("readonly");
}
function _temp10(snapshot_0) {
	return snapshot_0.matches("active");
}
function _temp11(snapshot_1) {
	return snapshot_1.matches("feedback");
}
function _temp12(snapshot_2) {
	return snapshot_2.matches("creatingVariant");
}
function _temp13(snapshot_3) {
	return snapshot_3.matches({ active: "creating" });
}
function _temp14(machine) {
	return machine.can({ type: "delete.request" });
}
function _temp15(t0) {
	let { context } = t0;
	return context.sets;
}
function _temp16(t0) {
	let { context: context_0 } = t0;
	return context_0.filterString !== void 0;
}
function _temp17(t0) {
	let { context: context_1 } = t0;
	return context_1.filterMatchingVariantIds;
}
function _temp18(snapshot) {
	return snapshot.matches("readonly");
}
function _temp19(t0) {
	let { context } = t0;
	return context.selectedIds;
}
function _temp20(t0) {
	let { context: context_0 } = t0;
	return context_0.releases;
}
var DocumentGroupInventoryAction = (t0) => {
	let $ = (0, import_compiler_runtime.c)(26), { children, documentId, portalElementName, isDocumentGroupInventoryActive, setIsDocumentGroupInventoryActive } = t0, { t } = useTranslation$1(), displayedRelease = useVersionRelease(documentId), buttonElement = (0, import_react.useRef)(null), popoverElement = (0, import_react.useRef)(null), t1;
	$[0] === documentId ? t1 = $[1] : (t1 = { documentId }, $[0] = documentId, $[1] = t1);
	let versionState = useDocumentVersionsObservable(t1), t2;
	$[2] === versionState ? t2 = $[3] : (t2 = versionState.pipe(map(_temp$44)), $[2] = versionState, $[3] = t2);
	let isAvailable = useObservable(t2), t3;
	$[4] === setIsDocumentGroupInventoryActive ? t3 = $[5] : (t3 = (event) => {
		let dialogs = document.querySelectorAll("[data-ui=\"DialogCard\"]");
		for (let dialog of Array.from(dialogs)) if (event.target && dialog.contains(event.target)) return;
		setIsDocumentGroupInventoryActive(!1);
	}, $[4] = setIsDocumentGroupInventoryActive, $[5] = t3);
	let t4;
	if ($[6] === Symbol.for("react.memo_cache_sentinel") ? (t4 = () => [buttonElement.current, popoverElement.current], $[6] = t4) : t4 = $[6], useClickOutsideEvent(t3, t4), !isAvailable) return null;
	let t5 = displayedRelease?.release, t6;
	$[7] !== t || $[8] !== t5 ? (t6 = variantLabel({
		perspective: t5,
		t
	}), $[7] = t, $[8] = t5, $[9] = t6) : t6 = $[9];
	let t7;
	$[10] !== isDocumentGroupInventoryActive || $[11] !== setIsDocumentGroupInventoryActive ? (t7 = () => setIsDocumentGroupInventoryActive(!isDocumentGroupInventoryActive), $[10] = isDocumentGroupInventoryActive, $[11] = setIsDocumentGroupInventoryActive, $[12] = t7) : t7 = $[12];
	let t8;
	$[13] === displayedRelease.release ? t8 = $[14] : (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VariantIcon, { perspective: displayedRelease.release }), $[13] = displayedRelease.release, $[14] = t8);
	let t9 = isDocumentGroupInventoryActive ? ChevronDownIcon : ChevronUpIcon, t10;
	$[15] === Symbol.for("react.memo_cache_sentinel") ? (t10 = {}, $[15] = t10) : t10 = $[15];
	let t11;
	$[16] !== t6 || $[17] !== t7 || $[18] !== t8 || $[19] !== t9 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
		ref: buttonElement,
		"data-testid": "action-document-group-inventory",
		text: t6,
		tone: "neutral",
		onClick: t7,
		icon: t8,
		iconRight: t9,
		tooltipProps: t10,
		mode: "ghost"
	}), $[16] = t6, $[17] = t7, $[18] = t8, $[19] = t9, $[20] = t11) : t11 = $[20];
	let t12;
	return $[21] !== children || $[22] !== isDocumentGroupInventoryActive || $[23] !== portalElementName || $[24] !== t11 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayerProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Popover$1, {
		ref: popoverElement,
		content: children,
		placement: "top-end",
		padding: 0,
		open: isDocumentGroupInventoryActive,
		portal: portalElementName,
		children: t11
	}) }), $[21] = children, $[22] = isDocumentGroupInventoryActive, $[23] = portalElementName, $[24] = t11, $[25] = t12) : t12 = $[25], t12;
};
function variantLabel({ perspective, t }) {
	return perspective === void 0 ? "" : isAgentBundleName(perspective) ? t("version.agent-bundle.proposed-changes") : isDraftPerspective(perspective) ? "Draft" : isPublishedPerspective(perspective) ? "Published" : typeof perspective == "string" ? perspective : perspective.metadata.title ?? perspective._id;
}
var VariantIcon = (t0) => {
	let $ = (0, import_compiler_runtime.c)(3), { perspective } = t0;
	if (perspective === void 0) {
		let t1;
		return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseAvatarIcon, { tone: "neutral" }), $[0] = t1) : t1 = $[0], t1;
	}
	let t1;
	return $[1] === perspective ? t1 = $[2] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseAvatarIcon, { release: perspective }), $[1] = perspective, $[2] = t1), t1;
};
var Button$1 = gt(Button$1$1).withConfig({
	displayName: "Button",
	componentId: "sc-wa2301-0"
})`max-inline-size:40ch;overflow:hidden;text-overflow:ellipsis;`;
function _temp$44(t0) {
	let { loading, versions } = t0;
	return !loading && versions.length !== 0;
}
/**
* Context-based hook for feedback submission that works in any environment.
*
* When rendered inside a {@link StudioFeedbackProvider} (or any custom provider),
* feedback is enriched with consent status, user info, and tags from that provider.
*
* When no provider is present, falls back to safe defaults:
* anonymous feedback with consent treated as denied.
*
* @internal
*/
function useFeedback() {
	let $ = (0, import_compiler_runtime.c)(7), { telemetryConsent, userName, userEmail, tags } = (0, import_react.useContext)(FeedbackContext), t0;
	$[0] !== tags || $[1] !== telemetryConsent || $[2] !== userEmail || $[3] !== userName ? (t0 = (opts) => {
		let { dsn, feedbackVersion, source, message, userName: userNameOverride, userEmail: userEmailOverride, extraTags, attachments } = opts;
		return sendFeedbackToSentry({
			dsn,
			feedbackVersion,
			telemetryConsent,
			name: userNameOverride ?? userName,
			email: userEmailOverride ?? userEmail,
			message,
			source,
			tags: {
				...tags,
				...extraTags
			},
			attachments
		});
	}, $[0] = tags, $[1] = telemetryConsent, $[2] = userEmail, $[3] = userName, $[4] = t0) : t0 = $[4];
	let sendFeedback = t0, t1;
	return $[5] === sendFeedback ? t1 = $[6] : (t1 = { sendFeedback }, $[5] = sendFeedback, $[6] = t1), t1;
}
/**
* Hook that encapsulates tag collection and feedback submission.
*
* Consumers only need to provide the fields they control (message, dsn, source, etc.).
*
* Base tags (userAgent, studioVersion, plugins, …) and
* dynamic tags (activeTool, url, …) are gathered automatically.
*
* @internal
*/
function useInStudioFeedback() {
	let $ = (0, import_compiler_runtime.c)(8), { allTags, userName, userEmail } = useStudioFeedbackTags(), telemetryConsent = useTelemetryConsent(), t0;
	$[0] !== allTags || $[1] !== telemetryConsent || $[2] !== userEmail || $[3] !== userName ? (t0 = (opts) => {
		let { dsn, feedbackVersion, source, message, extraTags, attachments } = opts;
		return sendFeedbackToSentry({
			dsn,
			feedbackVersion,
			telemetryConsent,
			name: userName,
			email: userEmail,
			message,
			source,
			tags: {
				...allTags,
				...extraTags
			},
			attachments
		});
	}, $[0] = allTags, $[1] = telemetryConsent, $[2] = userEmail, $[3] = userName, $[4] = t0) : t0 = $[4];
	let sendFeedback = t0, t1;
	return $[5] !== sendFeedback || $[6] !== telemetryConsent ? (t1 = {
		sendFeedback,
		telemetryConsent
	}, $[5] = sendFeedback, $[6] = telemetryConsent, $[7] = t1) : t1 = $[7], t1;
}
function omitRev(document) {
	let { _rev, ...doc } = document;
	return doc;
}
function annotationForTransactionIndex(transactions, idx, event) {
	let tx = transactions[idx];
	return tx ? {
		timestamp: tx.timestamp,
		author: tx.author,
		event
	} : null;
}
function extractAnnotationForFromInput(transactions, meta) {
	return meta ? annotationForTransactionIndex(transactions, meta.transactionIndex + 1, meta.event) : transactions.length > 0 ? annotationForTransactionIndex(transactions, 0) : null;
}
function extractAnnotationForToInput(transactions, meta) {
	return meta ? annotationForTransactionIndex(transactions, meta.transactionIndex, meta.event) : null;
}
function diffValue({ transactions, fromValue, fromRaw, toValue, toRaw }) {
	return diffInput(wrapValue(fromValue, fromRaw, {
		fromValue(value) {
			return extractAnnotationForFromInput(transactions, value.endMeta);
		},
		fromMeta(meta) {
			return extractAnnotationForFromInput(transactions, meta);
		}
	}), wrapValue(toValue, toRaw, {
		fromValue(value) {
			return extractAnnotationForToInput(transactions, value.startMeta);
		},
		fromMeta(meta) {
			return extractAnnotationForToInput(transactions, meta);
		}
	}));
}
function calculateDiff({ initialDoc, documentId, transactions, events = [] }) {
	let initialValue = incrementalPatcher.wrap(omitRev(initialDoc), null), document = incrementalPatcher.wrap(omitRev(initialDoc), null), finalDocument = omitRev(initialDoc);
	return transactions.forEach((transaction, index) => {
		let meta = {
			transactionIndex: index,
			event: events.find((event) => !isEditDocumentVersionEvent(event) && "revisionId" in event && event.revisionId === transaction.id)
		}, effect = transaction.effects[documentId];
		effect && (document = incrementalPatcher.applyPatch(document, effect.apply, meta), finalDocument = applyPatch(finalDocument, effect.apply));
	}), diffValue({
		transactions,
		fromValue: initialValue,
		fromRaw: initialDoc,
		toValue: document,
		toRaw: finalDocument
	});
}
var buildDocumentForDiffInput = (document) => {
	if (!document) return {};
	let { _id, _rev, _createdAt, _updatedAt, _type, ...rest } = JSON.parse(JSON.stringify(document));
	return rest;
};
function removeDuplicatedTransactions(transactions) {
	let seen = /* @__PURE__ */ new Set();
	return transactions.filter((tx) => seen.has(tx.id) ? !1 : (seen.add(tx.id), !0));
}
var MissingSinceDocumentError = class extends Error {
	revisionId;
	constructor(revisionId) {
		super(`Missing since document for revision ${revisionId}`), this.name = "MissingSinceDocumentError", this.revisionId = revisionId;
	}
};
function getDocumentChanges({ eventsObservable$, documentId, client, to$, since$, remoteTransactions$ }) {
	let lastResolvedSince = null, lastResolvedTo = null, lastTransactions = [];
	return combineLatest(to$, since$, eventsObservable$).pipe(switchMap(([toObs, since, { events }]) => {
		let to = toObs?.document, sinceDoc;
		if (since?.document) sinceDoc = since?.document;
		else {
			let selectedToEvent = events.find((event) => event.id === to?._rev);
			selectedToEvent && isCreateDocumentVersionEvent(selectedToEvent) && to && (sinceDoc = {
				_type: to._type,
				_id: to._id,
				_rev: to._rev
			});
		}
		return sinceDoc ? remoteTransactions$.pipe(switchMap((remoteTx) => {
			let viewingLatest = !to?._rev, getTransactions = () => sinceDoc._rev === "history-cleared" ? of([]) : viewingLatest && lastResolvedSince === sinceDoc._rev ? of(removeDuplicatedTransactions(lastTransactions.concat(remoteTx))) : lastResolvedSince && lastResolvedSince === sinceDoc._rev && lastResolvedTo && lastResolvedTo === to?._rev ? of(lastTransactions) : from(getDocumentTransactions({
				documentId,
				client,
				toTransaction: to?._rev,
				fromTransaction: sinceDoc._rev
			}));
			return getTransactions().pipe(tap((transactions) => {
				lastResolvedSince = sinceDoc._rev, lastTransactions = transactions, to?._rev && (lastResolvedTo = to._rev);
			}), map((transactions) => ({
				loading: !1,
				diff: calculateDiff({
					documentId,
					initialDoc: sinceDoc,
					transactions,
					events
				}),
				error: null
			})));
		}), catchError((error) => (console.error(error), of({
			loading: !1,
			diff: null,
			error
		}))), startWith({
			loading: !0,
			error: null,
			diff: sinceDoc && to ? diffInput(wrap$1(buildDocumentForDiffInput(sinceDoc), null), wrap$1(buildDocumentForDiffInput(to), null)) : null
		}), shareReplay(1)) : of({
			loading: !1,
			diff: null,
			error: since && !since.loading && since.revisionId ? new MissingSinceDocumentError(since?.revisionId) : null
		});
	}));
}
/**
* @internal
* */
function ChangesError(t0) {
	let $ = (0, import_compiler_runtime.c)(16), { error } = t0, { t } = useTranslation$1(), revisionNotFoundError = error instanceof MissingSinceDocumentError, t1;
	$[0] === t ? t1 = $[1] : (t1 = t("changes.error-title"), $[0] = t, $[1] = t1);
	let t2;
	$[2] === t1 ? t2 = $[3] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		weight: "medium",
		as: "h3",
		children: t1
	}), $[2] = t1, $[3] = t2);
	let t3;
	$[4] === t ? t3 = $[5] : (t3 = t("changes.error-description"), $[4] = t, $[5] = t3);
	let t4;
	$[6] === t3 ? t4 = $[7] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "p",
		size: 1,
		muted: !0,
		children: t3
	}), $[6] = t3, $[7] = t4);
	let t5;
	$[8] !== error || $[9] !== revisionNotFoundError || $[10] !== t ? (t5 = revisionNotFoundError && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingTop: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			as: "p",
			size: 1,
			muted: !0,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Translate, {
				i18nKey: "changes.missing-since-document-error",
				t,
				values: { revisionId: error.revisionId },
				components: { Break: _temp$43 }
			})
		})
	}), $[8] = error, $[9] = revisionNotFoundError, $[10] = t, $[11] = t5) : t5 = $[11];
	let t6;
	return $[12] !== t2 || $[13] !== t4 || $[14] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		tone: "caution",
		padding: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 3,
			children: [
				t2,
				t4,
				t5
			]
		})
	}), $[12] = t2, $[13] = t4, $[14] = t5, $[15] = t6) : t6 = $[15], t6;
}
function _temp$43() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("br", {});
}
/**
* @internal
*/
var FormContainer = /* @__PURE__ */ gt.div.withConfig({
	displayName: "FormContainer",
	componentId: "sc-rxsyrf-0"
})((props) => {
	let { space, container } = getTheme_v2(props.theme);
	return ft`--formGutterSize:calc(${space[4]}px * var(--formGutterEnabled));--formGutterGap:calc(${space[3]}px * var(--formGutterEnabled));box-sizing:border-box;margin-inline:auto;padding-inline:${space[4]}px;padding-block-start:${space[5]}px;padding-block-end:${space[9]}px;max-width:calc( ${container[1]}px + (var(--formGutterSize,0px) * 2) + (var(--formGutterGap,0px) * 2) );`;
});
/**
*
* @hidden
* @beta
*/
function TelephoneInput(props) {
	let $ = (0, import_compiler_runtime.c)(5), { schemaType, validationError, value, elementProps } = props, t0 = value || "", t1;
	return $[0] !== elementProps || $[1] !== schemaType.placeholder || $[2] !== t0 || $[3] !== validationError ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextInput, {
		type: "tel",
		inputMode: "tel",
		customValidity: validationError,
		value: t0,
		placeholder: schemaType.placeholder,
		...elementProps
	}), $[0] = elementProps, $[1] = schemaType.placeholder, $[2] = t0, $[3] = validationError, $[4] = t1) : t1 = $[4], t1;
}
/**
* Convenience component for rendering an "array of objects"-item
* @internal
*/
function ArrayOfObjectsInputMember(props) {
	let $ = (0, import_compiler_runtime.c)(11), { member, renderAnnotation, renderBlock, renderField, renderInlineBlock, renderInput, renderItem, renderPreview } = props;
	if (member.kind === "item") {
		let t0;
		return $[0] !== member || $[1] !== renderAnnotation || $[2] !== renderBlock || $[3] !== renderField || $[4] !== renderInlineBlock || $[5] !== renderInput || $[6] !== renderItem || $[7] !== renderPreview ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArrayOfObjectsItem, {
			member,
			renderAnnotation,
			renderBlock,
			renderField,
			renderInlineBlock,
			renderInput,
			renderItem,
			renderPreview
		}, member.key), $[0] = member, $[1] = renderAnnotation, $[2] = renderBlock, $[3] = renderField, $[4] = renderInlineBlock, $[5] = renderInput, $[6] = renderItem, $[7] = renderPreview, $[8] = t0) : t0 = $[8], t0;
	}
	if (member.kind === "error") {
		let t0;
		return $[9] === member ? t0 = $[10] : (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MemberItemError, { member }, member.key), $[9] = member, $[10] = t0), t0;
	}
	return console.warn(/* @__PURE__ */ Error(`Unhandled member kind ${member.kind}`)), null;
}
/**
* Convenience component for wrapping an array of objects
* @internal
*/
function ArrayOfObjectsInputMembers(props) {
	let $ = (0, import_compiler_runtime.c)(10), members, rest;
	$[0] === props ? (members = $[1], rest = $[2]) : ({members, ...rest} = props, $[0] = props, $[1] = members, $[2] = rest);
	let t0;
	if ($[3] !== members || $[4] !== rest) {
		let t1;
		$[6] === rest ? t1 = $[7] : (t1 = (member) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArrayOfObjectsInputMember, {
			member,
			...rest
		}, member.key), $[6] = rest, $[7] = t1), t0 = members.map(t1), $[3] = members, $[4] = rest, $[5] = t0;
	} else t0 = $[5];
	let t1;
	return $[8] === t0 ? t1 = $[9] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: t0 }), $[8] = t0, $[9] = t1), t1;
}
/**
* Overlay input-reported parse errors onto a validation array.
*
* While an input is unparseable, the value committed to the document is still
* `undefined`, so the validator output for that path is misleading "required"
* noise. For every path present in `parseErrors`, we drop the existing error
* markers at that path and emit a single error marker carrying the parse
* message. Markers at other paths and non-error markers at the same path
* (warning, info) are preserved.
*
* The `parseErrors` map is keyed by `@sanity/util/paths` `toString(path)`.
*
* @internal
*/
function mergeParseErrors(validation, parseErrors) {
	let keys = Object.keys(parseErrors);
	if (keys.length === 0) return validation;
	let replaced = /* @__PURE__ */ new Set(), merged = [];
	for (let marker of validation) {
		if (marker.level !== "error") {
			merged.push(marker);
			continue;
		}
		let key = toString(marker.path);
		if (!(key in parseErrors)) {
			merged.push(marker);
			continue;
		}
		replaced.has(key) || (replaced.add(key), merged.push({
			level: "error",
			message: parseErrors[key].message,
			path: fromString(key)
		}));
	}
	for (let key of [...keys].sort()) replaced.has(key) || merged.push({
		level: "error",
		message: parseErrors[key].message,
		path: fromString(key)
	});
	return merged;
}
/** @internal */
var defaultRenderAnnotation = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(defaultResolveAnnotationComponent(props.schemaType), { ...props });
var defaultRenderBlock = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(defaultResolveBlockComponent(props.schemaType), { ...props });
var defaultRenderInlineBlock = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(defaultResolveInlineBlockComponent(props.schemaType), { ...props });
var defaultRenderField = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(defaultResolveFieldComponent(props.schemaType), { ...props });
var defaultRenderInput = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(defaultResolveInputComponent(props.schemaType), { ...props });
var defaultRenderItem = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(defaultResolveItemComponent(props.schemaType), { ...props });
var defaultRenderPreview = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Preview, { ...props });
/** @internal */
function useDocumentOperationEvent(publishedDocId, docTypeName) {
	let $ = (0, import_compiler_runtime.c)(4), documentStore = useDocumentStore(), t0;
	return $[0] !== docTypeName || $[1] !== documentStore.pair || $[2] !== publishedDocId ? (t0 = documentStore.pair.operationEvents(publishedDocId, docTypeName), $[0] = docTypeName, $[1] = documentStore.pair, $[2] = publishedDocId, $[3] = t0) : t0 = $[3], useSyncObservable(t0);
}
var PERIODS = [
	"days",
	"hours",
	"minutes",
	"seconds",
	"milliseconds"
];
/**
* Formats a duration (in milliseconds) to a more user friendly string eg `1h 30m` or `1t 29m 15s`.
* Can be configured to output full units, eg `1 hour 30 minutes` or `1 hour 3 minutes 15 seconds`.
* Uses the current locale, which also applies to the division of units.
*
* @example English (en-US) locale formatting
* ```ts
* useFormattedDuration(5589000)
* // {"formatted": "1 hour, 33 minutes, and 9 seconds", "iso8601": "PT1H33M9S"}
* ```
*
* @example Norwegian (no-NB) locale formatting
* ```ts
* useFormattedDuration(5589000)
* // {"formatted": "1 time, 33 minutter og 9 sekunder", "iso8601": "PT1H33M9S"}
* ```
*
* @param options - Optional options for the number formatter
* @returns An object with `formatted` and `iso8601` properties
* @public
*/
function useFormattedDuration(durationMs, options) {
	let $ = (0, import_compiler_runtime.c)(36), t0;
	$[0] === options ? t0 = $[1] : (t0 = options || {}, $[0] = options, $[1] = t0);
	let { style: t1, resolution: t2 } = t0, style = t1 === void 0 ? "short" : t1, resolution = t2 === void 0 ? "seconds" : t2, unitDisplay = style, locale = useCurrentLocale().id, t3;
	$[2] === style ? t3 = $[3] : (t3 = {
		type: "unit",
		style
	}, $[2] = style, $[3] = t3);
	let listFormat = useListFormat(t3), isNegative = durationMs < 0, formatted, t4;
	if ($[4] !== durationMs || $[5] !== isNegative || $[6] !== listFormat || $[7] !== locale || $[8] !== resolution || $[9] !== unitDisplay) {
		let duration = parseMilliseconds(Math.abs(durationMs)), t5;
		$[12] !== locale || $[13] !== unitDisplay ? (t5 = intlCache.numberFormat(locale, {
			style: "unit",
			unit: "day",
			unitDisplay
		}), $[12] = locale, $[13] = unitDisplay, $[14] = t5) : t5 = $[14];
		let t6;
		$[15] !== locale || $[16] !== unitDisplay ? (t6 = intlCache.numberFormat(locale, {
			style: "unit",
			unit: "hour",
			unitDisplay
		}), $[15] = locale, $[16] = unitDisplay, $[17] = t6) : t6 = $[17];
		let t7;
		$[18] !== locale || $[19] !== unitDisplay ? (t7 = intlCache.numberFormat(locale, {
			style: "unit",
			unit: "minute",
			unitDisplay
		}), $[18] = locale, $[19] = unitDisplay, $[20] = t7) : t7 = $[20];
		let t8;
		$[21] !== locale || $[22] !== unitDisplay ? (t8 = intlCache.numberFormat(locale, {
			style: "unit",
			unit: "second",
			unitDisplay
		}), $[21] = locale, $[22] = unitDisplay, $[23] = t8) : t8 = $[23];
		let t9;
		$[24] !== locale || $[25] !== unitDisplay ? (t9 = intlCache.numberFormat(locale, {
			style: "unit",
			unit: "millisecond",
			unitDisplay
		}), $[24] = locale, $[25] = unitDisplay, $[26] = t9) : t9 = $[26];
		let t10;
		$[27] !== t5 || $[28] !== t6 || $[29] !== t7 || $[30] !== t8 || $[31] !== t9 ? (t10 = {
			days: t5,
			hours: t6,
			minutes: t7,
			seconds: t8,
			milliseconds: t9
		}, $[27] = t5, $[28] = t6, $[29] = t7, $[30] = t8, $[31] = t9, $[32] = t10) : t10 = $[32];
		let formatters = t10, parts = [];
		for (let period of PERIODS) {
			let value = duration[period];
			if (!value || resolution === "seconds" && period === "milliseconds") continue;
			let prefix = isNegative && parts.length === 0 ? "-" : "";
			parts.push(`${prefix}${formatters[period].format(value)}`);
		}
		formatted = parts.length === 0 ? formatters[resolution].format(0) : listFormat.format(parts), t4 = durationToISO8601(duration, isNegative), $[4] = durationMs, $[5] = isNegative, $[6] = listFormat, $[7] = locale, $[8] = resolution, $[9] = unitDisplay, $[10] = formatted, $[11] = t4;
	} else formatted = $[10], t4 = $[11];
	let iso8601 = t4, t5;
	return $[33] !== formatted || $[34] !== iso8601 ? (t5 = {
		formatted,
		iso8601
	}, $[33] = formatted, $[34] = iso8601, $[35] = t5) : t5 = $[35], t5;
}
/**
* Parse milliseconds to durations. We stop at days because months are not a fixed unit,
* nor are years (365.25 strictly speaking, but people may expact calendar days). If we
* wanted to include those, we'd want to use `intervalToDuration` from date-fns, and base
* it on two specific dates.
*
* @param milliseconds - Milliseconds to parse
* @returns Object of duration parts
* @internal
*/
function parseMilliseconds(milliseconds) {
	return {
		days: Math.trunc(milliseconds / 864e5),
		hours: Math.trunc(milliseconds / 36e5) % 24,
		minutes: Math.trunc(milliseconds / 6e4) % 60,
		seconds: Math.trunc(milliseconds / 1e3) % 60,
		milliseconds: Math.trunc(milliseconds) % 1e3
	};
}
/**
* Format the given duration to an {@link https://en.wikipedia.org/wiki/ISO_8601#Durations | ISO-8601 duration}
* string, eg `P1DT2H3M4.005S` meaning "1 day, 2 hours, 3 minutes, 4 seconds and 5 milliseconds".
*
* If days are zero, it will skip straight to the time part, eg `PT2H3M4.005S`.
* If milliseconds are zero, it will skip the milliseconds part, eg `PT2H3M4S`.
*
* @param dur - The duration to format
* @returns The formatted duration
* @internal
*/
function durationToISO8601(dur, isNegative) {
	let date = dur.days ? `${dur.days}D` : "", time = "";
	if (dur.hours && (time += `${dur.hours}H`), dur.minutes && (time += `${dur.minutes}M`), dur.milliseconds ? time += `${((dur.seconds * 1e3 + dur.milliseconds) / 1e3).toFixed(3)}S` : dur.seconds && (time += `${dur.seconds}S`), !date && !time) return "PT0S";
	let duration = `P${(time ? [date, time] : [date]).join("T")}`;
	return isNegative ? `-${duration}` : duration;
}
var isCopyHotKey = isHotkey("mod+c");
var isPasteHotKey = isHotkey("mod+v");
/** @internal */
function useGlobalCopyPasteElementHandler(t0) {
	let $ = (0, import_compiler_runtime.c)(11), { value, element, focusPath } = t0, { onCopy, onPaste } = useCopyPaste(), t1;
	$[0] !== focusPath || $[1] !== onCopy || $[2] !== onPaste || $[3] !== value ? (t1 = (event) => {
		let targetElement = event.target;
		if (isCopyHotKey(event)) {
			if (isNativeEditableElement(targetElement) || hasSelection() || isEmptyFocusPath(focusPath)) return;
			event.preventDefault(), event.stopPropagation(), onCopy(focusPath, value, { context: { source: "keyboardShortcut" } });
		}
		if (isPasteHotKey(event)) {
			if (isNativeEditableElement(targetElement) || isEmptyFocusPath(focusPath) || isFileTargetElement(targetElement)) return;
			event.stopPropagation(), event.preventDefault(), onPaste(focusPath, value, { context: { source: "keyboardShortcut" } });
		}
	}, $[0] = focusPath, $[1] = onCopy, $[2] = onPaste, $[3] = value, $[4] = t1) : t1 = $[4];
	let handleKeydown = useEffectEvent(t1), t2;
	$[5] !== element || $[6] !== handleKeydown ? (t2 = () => (element?.addEventListener("keydown", handleKeydown), () => {
		element?.removeEventListener("keydown", handleKeydown);
	}), $[5] = element, $[6] = handleKeydown, $[7] = t2) : t2 = $[7];
	let t3;
	$[8] !== element || $[9] !== onPaste ? (t3 = [element, onPaste], $[8] = element, $[9] = onPaste, $[10] = t3) : t3 = $[10], (0, import_react.useEffect)(t2, t3);
}
/**
* IMPORTANT!
*
* Loosely based on the `useManageFavorite.ts` from `@sanity/sdk`.
* This version has been modified to use Studio's Comlink Store.
*
* TODO: Adopt `@sanity/sdk`.
*
* ---
*
* This hook provides functionality to add and remove documents from favorites,
* and tracks the current favorite status of the document.
*
* @internal
*/
function useManageFavorite(t0) {
	let $ = (0, import_compiler_runtime.c)(18), { documentId, documentType, projectId, dataset, resourceId: paramResourceId, resourceType, schemaName } = t0, { node } = useComlinkStore();
	if (resourceType === "studio" && (!projectId || !dataset)) throw Error("projectId and dataset are required for studio resources");
	let resourceId = resourceType === "studio" && !paramResourceId ? `${projectId}.${dataset}` : paramResourceId;
	if (!resourceId) throw Error("resourceId is required for media-library and canvas resources");
	let t1;
	$[0] !== documentId || $[1] !== documentType || $[2] !== resourceId || $[3] !== resourceType || $[4] !== schemaName ? (t1 = {
		documentId,
		documentType,
		resourceId,
		resourceType,
		schemaName
	}, $[0] = documentId, $[1] = documentType, $[2] = resourceId, $[3] = resourceType, $[4] = schemaName, $[5] = t1) : t1 = $[5];
	let context = t1, t2;
	$[6] !== context || $[7] !== node ? (t2 = optimisticState({
		node,
		context
	}), $[6] = context, $[7] = node, $[8] = t2) : t2 = $[8];
	let stateController = t2, state = useSyncObservable(stateController.state), t3;
	$[9] === stateController ? t3 = $[10] : (t3 = () => stateController.setState(!0), $[9] = stateController, $[10] = t3);
	let t4 = t3, t5;
	$[11] === stateController ? t5 = $[12] : (t5 = () => stateController.setState(!1), $[11] = stateController, $[12] = t5);
	let t6 = t5, t7 = state?.value, t8 = state?.isReady ?? !1, t9;
	return $[13] !== t4 || $[14] !== t6 || $[15] !== t7 || $[16] !== t8 ? (t9 = {
		favorite: t4,
		unfavorite: t6,
		isFavorited: t7,
		isReady: t8
	}, $[13] = t4, $[14] = t6, $[15] = t7, $[16] = t8, $[17] = t9) : t9 = $[17], t9;
}
var INITIAL_STATE$2 = {
	value: void 0,
	isReady: !1
};
/**
* Control the local favorite state optimistically, writing it to the remote after a debounce period
* if it has changed.
*/
function optimisticState({ node, context }) {
	let dispatchSubject = new Subject(), setState = (value) => dispatchSubject.next(value), previousState;
	return {
		state: iif(() => node === void 0, of(INITIAL_STATE$2), merge$1(dispatchSubject.pipe(distinctUntilChanged(), filter((value) => typeof value == "boolean"), connect((shared) => merge$1(shared.pipe(map((value) => ({
			type: "SET",
			payload: value
		}))), shared.pipe(debounceTime(750), filter((value) => previousState !== void 0 && value !== previousState), writeRemoteState({
			node,
			context
		}), tap(({ success, isFavorited }) => {
			success && (previousState = isFavorited);
		}), switchMap(() => EMPTY))))), readRemoteState({
			node,
			context
		}).pipe(map((response) => ({
			type: "END_READ",
			payload: response
		})), tap(({ payload }) => {
			previousState = payload.isFavorited;
		}), startWith({ type: "BEGIN_READ" }))).pipe(reducer())),
		setState
	};
}
function reducer() {
	return scan((previousState, action) => {
		switch (action.type) {
			case "BEGIN_READ": return {
				...previousState,
				isReady: !1
			};
			case "END_READ": return {
				...previousState,
				value: action.payload.isFavorited,
				isReady: !0
			};
			case "SET": return {
				...previousState,
				value: action.payload
			};
			default: return previousState;
		}
	}, INITIAL_STATE$2);
}
function readRemoteState({ node, context }) {
	if (context.resourceId === void 0 || node === void 0) return EMPTY;
	let payload = { document: {
		id: context.documentId,
		type: context.documentType,
		resource: {
			id: context.resourceId,
			type: context.resourceType,
			schemaName: context.schemaName
		}
	} };
	return from(node.fetch("dashboard/v1/events/favorite/query", payload)).pipe(timeout({
		first: 3e3,
		with: () => throwError(() => /* @__PURE__ */ Error("Favorites service connection timeout"))
	}), map((response) => ({ isFavorited: response.isFavorited })), catchError((err) => (console.error("Favorites service connection error", err), of({ isFavorited: !1 }))));
}
function writeRemoteState({ node, context }) {
	return pipe(mergeMap((value) => {
		if (context.resourceId === void 0 || node === void 0) return EMPTY;
		let payload = {
			eventType: value ? "added" : "removed",
			document: {
				id: context.documentId,
				type: context.documentType,
				resource: {
					id: context.resourceId,
					type: context.resourceType,
					...context.schemaName ? { schemaName: context.schemaName } : {}
				}
			}
		};
		return from(node.fetch("dashboard/v1/events/favorite/mutate", payload)).pipe(catchError((error) => (console.error("Favorites service write error", error), NEVER)), map((response) => ({
			success: typeof response == "object" && !!response && "success" in response && !!response.success,
			isFavorited: value
		})));
	}, 1));
}
/**
* Returns an instance of `Intl.NumberFormat` that uses the currently selected locale,
* and enables locale/language-sensitive number formatting.
*
* @param options - Optional options for the number formatter
* @returns Instance of `Intl.NumberFormat`
* @public
*/
function useNumberFormat(t0) {
	let $ = (0, import_compiler_runtime.c)(5), t1;
	$[0] === t0 ? t1 = $[1] : (t1 = t0 === void 0 ? {} : t0, $[0] = t0, $[1] = t1);
	let options = t1, currentLocale = useCurrentLocale().id, t2;
	return $[2] !== currentLocale || $[3] !== options ? (t2 = intlCache.numberFormat(currentLocale, options), $[2] = currentLocale, $[3] = options, $[4] = t2) : t2 = $[4], t2;
}
var SYNCING = { isSyncing: !0 };
var NOT_SYNCING = { isSyncing: !1 };
/** @internal */
function useSyncState(publishedDocId, documentType, version) {
	let $ = (0, import_compiler_runtime.c)(5), documentStore = useDocumentStore(), t0;
	return $[0] !== documentStore.pair || $[1] !== documentType || $[2] !== publishedDocId || $[3] !== version ? (t0 = documentStore.pair.consistencyStatus(publishedDocId, documentType, version).pipe(map(_temp$42)), $[0] = documentStore.pair, $[1] = documentType, $[2] = publishedDocId, $[3] = version, $[4] = t0) : t0 = $[4], useSyncObservable(t0, NOT_SYNCING);
}
function _temp$42(isConsistent) {
	return isConsistent ? NOT_SYNCING : SYNCING;
}
/**
* @deprecated - Use {@link useRelativeTime} instead
* @internal
*/
function useTimeAgo(time, t0) {
	let $ = (0, import_compiler_runtime.c)(5), t1;
	$[0] === t0 ? t1 = $[1] : (t1 = t0 === void 0 ? {} : t0, $[0] = t0, $[1] = t1);
	let options = t1, t2;
	return $[2] !== options.agoSuffix || $[3] !== options.minimal ? (t2 = {
		minimal: options.minimal,
		useTemporalPhrase: options.agoSuffix
	}, $[2] = options.agoSuffix, $[3] = options.minimal, $[4] = t2) : t2 = $[4], useRelativeTime(time, t2);
}
/**
* @internal
* @hidden
*/
function LocaleProvider(props) {
	let $ = (0, import_compiler_runtime.c)(6), { projectId, name: sourceId, i18n: t0, __internal: t1 } = useSource(), { locales } = t0, { i18next } = t1, t2;
	return $[0] !== i18next || $[1] !== locales || $[2] !== projectId || $[3] !== props || $[4] !== sourceId ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LocaleProviderBase, {
		...props,
		projectId,
		sourceId,
		locales,
		i18next
	}), $[0] = i18next, $[1] = locales, $[2] = projectId, $[3] = props, $[4] = sourceId, $[5] = t2) : t2 = $[5], t2;
}
/**
* @internal
* @hidden
*/
function LocaleProviderBase(t0) {
	let $ = (0, import_compiler_runtime.c)(24), { projectId, sourceId, locales, i18next, children } = t0, t1;
	$[0] === i18next ? t1 = $[1] : (t1 = (callback) => (i18next.on("languageChanged", callback), () => i18next.off("languageChanged", callback)), $[0] = i18next, $[1] = t1);
	let subscribe = t1, t2;
	$[2] !== i18next.language || $[3] !== locales ? (t2 = () => locales.find((candidate) => i18next.language === candidate.id) || defaultLocale, $[2] = i18next.language, $[3] = locales, $[4] = t2) : t2 = $[4];
	let currentLocale = (0, import_react.useSyncExternalStore)(subscribe, t2), t3;
	$[5] === i18next ? t3 = $[6] : (t3 = { i18next }, $[5] = i18next, $[6] = t3);
	let t4;
	$[7] !== i18next || $[8] !== projectId || $[9] !== sourceId ? (t4 = async (newLocale) => {
		storePreferredLocale(projectId, sourceId, newLocale), await i18next.changeLanguage(newLocale);
	}, $[7] = i18next, $[8] = projectId, $[9] = sourceId, $[10] = t4) : t4 = $[10];
	let t5;
	$[11] !== currentLocale || $[12] !== locales || $[13] !== t3 || $[14] !== t4 ? (t5 = {
		locales,
		currentLocale,
		__internal: t3,
		changeLocale: t4
	}, $[11] = currentLocale, $[12] = locales, $[13] = t3, $[14] = t4, $[15] = t5) : t5 = $[15];
	let context = t5, t6;
	$[16] === Symbol.for("react.memo_cache_sentinel") ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingBlock, {}), $[16] = t6) : t6 = $[16];
	let t7;
	$[17] !== children || $[18] !== context || $[19] !== currentLocale.id ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LocaleContext.Provider, {
		value: context,
		children
	}, currentLocale.id), $[17] = children, $[18] = context, $[19] = currentLocale.id, $[20] = t7) : t7 = $[20];
	let t8;
	return $[21] !== i18next || $[22] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Suspense, {
		fallback: t6,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(I18nextProvider, {
			i18n: i18next,
			children: t7
		})
	}), $[21] = i18next, $[22] = t7, $[23] = t8) : t8 = $[23], t8;
}
function DocumentLimitUpsellProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(9), { children } = t0, t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = {
		dataUri: "/journey/document-limit",
		feature: "document-limits"
	}, $[0] = t1) : t1 = $[0];
	let contextValue = useUpsellContext(t1), t2;
	$[1] === contextValue ? t2 = $[2] : (t2 = getDialogPropsFromContext(contextValue), $[1] = contextValue, $[2] = t2);
	let t3;
	$[3] === t2 ? t3 = $[4] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UpsellDialog, { ...t2 }), $[3] = t2, $[4] = t3);
	let t4;
	return $[5] !== children || $[6] !== contextValue || $[7] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DocumentLimitUpsellContext.Provider, {
		value: contextValue,
		children: [children, t3]
	}), $[5] = children, $[6] = contextValue, $[7] = t3, $[8] = t4) : t4 = $[8], t4;
}
/**
* @internal
*/
var useDocumentLimitsUpsellContext = () => {
	let context = (0, import_react.useContext)(DocumentLimitUpsellContext);
	if (!context) throw Error("useDocumentLimitsUpsellContext must be used within a DocumentLimitUpsellProvider");
	return context;
};
var isDocumentLimitError = (error) => error instanceof ClientError && error.response.body.error.type === "documentLimitExceededError";
/**
* Check whether the provided schema type can be written to the provided perspective. This depends
* on factors such as whether the schema type supports live-editing, whether the perspective
* represents an inactive release, and whether the perspective represents a special group (e.g.
* published documents).
*
* @internal
*/
function isPerspectiveWriteable({ selectedPerspective, isDraftModelEnabled, schemaType }) {
	return schemaType === void 0 ? {
		result: !1,
		reason: "INSUFFICIENT_DATA"
	} : isReleaseDocument(selectedPerspective) && selectedPerspective.state !== "active" ? {
		result: !1,
		reason: "RELEASE_NOT_ACTIVE"
	} : isPublishedPerspective(selectedPerspective) && schemaType.liveEdit !== !0 ? {
		result: !1,
		reason: "PUBLISHED_NOT_WRITEABLE"
	} : isDraftPerspective(selectedPerspective) && !isDraftModelEnabled && schemaType.liveEdit !== !0 ? {
		result: !1,
		reason: "DRAFTS_NOT_WRITEABLE"
	} : { result: !0 };
}
var EMPTY_ARRAY$2 = [];
function trimChildPath$1(path, childPath) {
	return startsWith(path, childPath) ? trimLeft(path, childPath) : EMPTY_ARRAY$2;
}
/** @internal */
function PresenceScope(props) {
	let $ = (0, import_compiler_runtime.c)(7), { readOnly, path, children } = props, contextPresence = (0, import_react.useContext)(FormFieldPresenceContext), t0;
	$[0] !== contextPresence || $[1] !== path || $[2] !== readOnly ? (t0 = readOnly ? EMPTY_ARRAY$2 : (contextPresence || EMPTY_ARRAY$2).filter((presence) => startsWith(path, presence.path)).map((presence_0) => ({
		...presence_0,
		path: trimChildPath$1(path, presence_0.path)
	})), $[0] = contextPresence, $[1] = path, $[2] = readOnly, $[3] = t0) : t0 = $[3];
	let childPresence = t0, t1;
	return $[4] !== childPresence || $[5] !== children ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FormFieldPresenceContext.Provider, {
		value: childPresence,
		children
	}), $[4] = childPresence, $[5] = children, $[6] = t1) : t1 = $[6], t1;
}
var INITIAL_STATE$1 = {
	loading: !0,
	document: null
};
/**
* @internal
* @beta
*
* Observes a document by its ID and returns the document and loading state
* it will listen to the document changes.
*/
function useUnstableObserveDocument(documentId, apiConfig) {
	let $ = (0, import_compiler_runtime.c)(4), documentPreviewStore = useDocumentPreviewStore(), t0;
	return $[0] !== apiConfig || $[1] !== documentId || $[2] !== documentPreviewStore ? (t0 = documentPreviewStore.unstable_observeDocument(documentId, apiConfig).pipe(map(_temp$41)), $[0] = apiConfig, $[1] = documentId, $[2] = documentPreviewStore, $[3] = t0) : t0 = $[3], useSyncObservable(t0, INITIAL_STATE$1);
}
/**
* @deprecated Use `useUnstableObserveDocument` instead
* @internal
* @beta
*/
function _temp$41(document) {
	return {
		loading: !1,
		document
	};
}
var unstable_useObserveDocument = function useObserveDocument(args) {
	return useUnstableObserveDocument(...args);
};
var ChipButtonContainer = gt.span.withConfig({
	displayName: "ChipButtonContainer",
	componentId: "sc-04b5g7-0"
})`display:inline-flex;--border-color:var(--card-border-color);`;
var ChipButton = gt(Button).withConfig({
	displayName: "ChipButton",
	componentId: "sc-04b5g7-1"
})`flex:none;transition:none;cursor:pointer;--card-border-color:var(--border-color);`;
/**
* @internal
*/
function Chip(props) {
	let $ = (0, import_compiler_runtime.c)(6), ref, rest;
	$[0] === props ? (ref = $[1], rest = $[2]) : ({ref, ...rest} = props, $[0] = props, $[1] = ref, $[2] = rest);
	let t0;
	return $[3] !== ref || $[4] !== rest ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChipButtonContainer, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChipButton, {
		ref,
		paddingY: 2,
		paddingLeft: 2,
		paddingRight: 3,
		gap: 2,
		radius: "full",
		...rest
	}) }), $[3] = ref, $[4] = rest, $[5] = t0) : t0 = $[5], t0;
}
var useVersionIsLinked = (documentId, fromRelease) => {
	let $ = (0, import_compiler_runtime.c)(13), t0;
	bb0: {
		if (fromRelease === "published") {
			let t1;
			$[0] === documentId ? t1 = $[1] : (t1 = getPublishedId(documentId), $[0] = documentId, $[1] = t1), t0 = t1;
			break bb0;
		}
		if (fromRelease === "draft") {
			let t1;
			$[2] === documentId ? t1 = $[3] : (t1 = getDraftId(documentId), $[2] = documentId, $[3] = t1), t0 = t1;
			break bb0;
		}
		let t1;
		$[4] !== documentId || $[5] !== fromRelease ? (t1 = getVersionId(documentId, fromRelease), $[4] = documentId, $[5] = fromRelease, $[6] = t1) : t1 = $[6], t0 = t1;
	}
	let versionId = t0, companionDocsStore = useCanvasCompanionDocsStore(), t1;
	$[7] !== companionDocsStore || $[8] !== documentId ? (t1 = companionDocsStore.getCompanionDocs(documentId), $[7] = companionDocsStore, $[8] = documentId, $[9] = t1) : t1 = $[9];
	let companionDocs = useObservable(t1), t2;
	return $[10] !== companionDocs?.data || $[11] !== versionId ? (t2 = companionDocs?.data.some((companion) => companion?.studioDocumentId === versionId), $[10] = companionDocs?.data, $[11] = versionId, $[12] = t2) : t2 = $[12], t2;
};
var VersionChip = (0, import_react.memo)(function VersionChip(props) {
	let $ = (0, import_compiler_runtime.c)(68), { disabled, selected, tooltipContent, onClick, text, contextMenuPortal: t0, tone, locked: t1, onCopyToDraftsComplete, contextValues: t2 } = props, contextMenuPortal = t0 === void 0 || t0, locked = t1 !== void 0 && t1, { documentGroupId, versionId, releases, releasesLoading, documentType, bundleId, disabled: t3, isGoingToUnpublish: t4, release } = t2, contextMenuDisabled = t3 !== void 0 && t3, isGoingToUnpublish = t4 !== void 0 && t4, releasesToolAvailable = useReleasesToolAvailable(), isLinked = useVersionIsLinked(documentGroupId, bundleId), chipRef = (0, import_react.useRef)(null), t5, t6;
	$[0] === selected ? (t5 = $[1], t6 = $[2]) : (t5 = () => {
		selected && chipRef.current?.scrollIntoView({ inline: "center" });
	}, t6 = [selected], $[0] = selected, $[1] = t5, $[2] = t6), (0, import_react.useEffect)(t5, t6);
	let t7;
	$[3] !== contextMenuDisabled || $[4] !== documentGroupId || $[5] !== documentType || $[6] !== onCopyToDraftsComplete || $[7] !== versionId ? (t7 = {
		documentGroupId,
		versionId,
		documentType,
		disabled: contextMenuDisabled,
		onCopyToDraftsComplete
	}, $[3] = contextMenuDisabled, $[4] = documentGroupId, $[5] = documentType, $[6] = onCopyToDraftsComplete, $[7] = versionId, $[8] = t7) : t7 = $[8];
	let { contextMenu, handleContextMenu, popoverRef, referenceElement, setReferenceElement, dialogState, closeDialog, openDiscardDialog, openCreateReleaseDialog, handleCopyToDrafts, handleAddVersion, isScheduledDraft, scheduledDraftMenuActions, sourceReleasePerspective } = useVersionContextMenu(t7), contextMenuHandler = disabled || !releasesToolAvailable ? void 0 : handleContextMenu, isPaused = isPausedCardinalityOneRelease(release), t8;
	bb0: {
		if (isLinked) {
			let t9;
			$[9] === Symbol.for("react.memo_cache_sentinel") ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComposeSparklesIcon, {}), $[9] = t9) : t9 = $[9], t8 = t9;
			break bb0;
		}
		if (isPaused) {
			let t9;
			$[10] === Symbol.for("react.memo_cache_sentinel") ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UnlockIcon, {}), $[10] = t9) : t9 = $[10], t8 = t9;
			break bb0;
		}
		if (locked) {
			let t9;
			$[11] === Symbol.for("react.memo_cache_sentinel") ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LockIcon, {}), $[11] = t9) : t9 = $[11], t8 = t9;
			break bb0;
		}
		t8 = void 0;
	}
	let rightIcon = t8, t9;
	$[12] === Symbol.for("react.memo_cache_sentinel") ? (t9 = [], $[12] = t9) : t9 = $[12];
	let t10;
	$[13] === text ? t10 = $[14] : (t10 = text.replaceAll(" ", "-"), $[13] = text, $[14] = t10);
	let t11 = `document-header-${t10}-chip`, t12 = disabled ? "ghost" : "bleed", t13;
	$[15] === sourceReleasePerspective ? t13 = $[16] : (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleaseAvatarIcon, { release: sourceReleasePerspective }), $[15] = sourceReleasePerspective, $[16] = t13);
	let t14;
	$[17] !== contextMenuHandler || $[18] !== disabled || $[19] !== onClick || $[20] !== rightIcon || $[21] !== selected || $[22] !== setReferenceElement || $[23] !== t11 || $[24] !== t12 || $[25] !== t13 || $[26] !== text || $[27] !== tone ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		ref: chipRef,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Chip, {
			"data-testid": t11,
			ref: setReferenceElement,
			disabled,
			mode: t12,
			onClick,
			selected,
			tone,
			onContextMenu: contextMenuHandler,
			icon: t13,
			iconRight: rightIcon,
			text
		})
	}), $[17] = contextMenuHandler, $[18] = disabled, $[19] = onClick, $[20] = rightIcon, $[21] = selected, $[22] = setReferenceElement, $[23] = t11, $[24] = t12, $[25] = t13, $[26] = text, $[27] = tone, $[28] = t14) : t14 = $[28];
	let t15;
	$[29] !== t14 || $[30] !== tooltipContent ? (t15 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip$1, {
		content: tooltipContent,
		fallbackPlacements: t9,
		portal: !0,
		placement: "bottom",
		children: t14
	}), $[29] = t14, $[30] = tooltipContent, $[31] = t15) : t15 = $[31];
	let t16;
	$[32] !== bundleId || $[33] !== contextMenu || $[34] !== contextMenuDisabled || $[35] !== contextMenuPortal || $[36] !== documentGroupId || $[37] !== documentType || $[38] !== handleAddVersion || $[39] !== handleCopyToDrafts || $[40] !== isGoingToUnpublish || $[41] !== isScheduledDraft || $[42] !== locked || $[43] !== openCreateReleaseDialog || $[44] !== openDiscardDialog || $[45] !== popoverRef || $[46] !== referenceElement || $[47] !== release || $[48] !== releases || $[49] !== releasesLoading || $[50] !== scheduledDraftMenuActions || $[51] !== versionId ? (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VersionContextMenuPopover, {
		contextMenu,
		popoverRef,
		referenceElement,
		documentGroupId,
		versionId,
		documentType,
		bundleId,
		releases,
		releasesLoading,
		onDiscard: openDiscardDialog,
		onCreateRelease: openCreateReleaseDialog,
		onCopyToDrafts: handleCopyToDrafts,
		onCreateVersion: handleAddVersion,
		disabled: contextMenuDisabled,
		locked,
		isGoingToUnpublish,
		release,
		isScheduledDraft,
		scheduledDraftMenuActions,
		portal: contextMenuPortal
	}), $[32] = bundleId, $[33] = contextMenu, $[34] = contextMenuDisabled, $[35] = contextMenuPortal, $[36] = documentGroupId, $[37] = documentType, $[38] = handleAddVersion, $[39] = handleCopyToDrafts, $[40] = isGoingToUnpublish, $[41] = isScheduledDraft, $[42] = locked, $[43] = openCreateReleaseDialog, $[44] = openDiscardDialog, $[45] = popoverRef, $[46] = referenceElement, $[47] = release, $[48] = releases, $[49] = releasesLoading, $[50] = scheduledDraftMenuActions, $[51] = versionId, $[52] = t16) : t16 = $[52];
	let t17 = isScheduledDraft && scheduledDraftMenuActions.dialogs, t18;
	$[53] !== closeDialog || $[54] !== dialogState || $[55] !== documentType || $[56] !== handleAddVersion || $[57] !== handleCopyToDrafts || $[58] !== isGoingToUnpublish || $[59] !== sourceReleasePerspective || $[60] !== t17 || $[61] !== text || $[62] !== versionId ? (t18 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VersionContextMenuDialogs, {
		dialogState,
		onClose: closeDialog,
		versionId,
		documentType,
		title: text,
		sourceReleasePerspective,
		onCreateVersion: handleAddVersion,
		onCopyToDrafts: handleCopyToDrafts,
		isGoingToUnpublish,
		scheduledDraftDialogs: t17
	}), $[53] = closeDialog, $[54] = dialogState, $[55] = documentType, $[56] = handleAddVersion, $[57] = handleCopyToDrafts, $[58] = isGoingToUnpublish, $[59] = sourceReleasePerspective, $[60] = t17, $[61] = text, $[62] = versionId, $[63] = t18) : t18 = $[63];
	let t19;
	return $[64] !== t15 || $[65] !== t16 || $[66] !== t18 ? (t19 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		t15,
		t16,
		t18
	] }), $[64] = t15, $[65] = t16, $[66] = t18, $[67] = t19) : t19 = $[67], t19;
});
var useIsReleaseActive = () => {
	let $ = (0, import_compiler_runtime.c)(2), { selectedPerspective } = usePerspective(), t0;
	return $[0] === selectedPerspective ? t0 = $[1] : (t0 = !isPublishedPerspective(selectedPerspective) && (isDraftPerspective(selectedPerspective) || isReleaseDocument(selectedPerspective) && selectedPerspective.state === "active"), $[0] = selectedPerspective, $[1] = t0), t0;
};
var NO_VERSIONS = {};
/**
* Takes a document id, and returns information about what other versions of the document currently exists.
*
* @param documentId - The document id. Should be the published id
* @deprecated Use {@link useDocumentVersions} instead.
*/
function useDocumentVersionInfo(documentId) {
	let $ = (0, import_compiler_runtime.c)(11), t0;
	$[0] === documentId ? t0 = $[1] : (t0 = getPublishedId(documentId), $[0] = documentId, $[1] = t0);
	let publishedId = t0, t1;
	$[2] === publishedId ? t1 = $[3] : (t1 = { documentId: publishedId }, $[2] = publishedId, $[3] = t1);
	let { versions, loading } = useDocumentVersions(t1), t2;
	$[4] === versions ? t2 = $[5] : (t2 = getDocumentVersionInfoFromVersions(versions), $[4] = versions, $[5] = t2);
	let versionInfo = t2, t3 = loading ? NO_VERSIONS : versionInfo.versions, t4;
	return $[6] !== loading || $[7] !== t3 || $[8] !== versionInfo.draft || $[9] !== versionInfo.published ? (t4 = {
		isLoading: loading,
		draft: versionInfo.draft,
		published: versionInfo.published,
		versions: t3
	}, $[6] = loading, $[7] = t3, $[8] = versionInfo.draft, $[9] = versionInfo.published, $[10] = t4) : t4 = $[10], t4;
}
/**
* Gets all the releases ids
* @internal
*/
function useReleasesIds(releases) {
	let $ = (0, import_compiler_runtime.c)(4), t0;
	$[0] === releases ? t0 = $[1] : (t0 = releases.map(_temp$40), $[0] = releases, $[1] = t0);
	let releasesIds = t0, t1;
	return $[2] === releasesIds ? t1 = $[3] : (t1 = { releasesIds }, $[2] = releasesIds, $[3] = t1), t1;
}
function _temp$40(release) {
	return getReleaseIdFromReleaseDocumentId(release._id);
}
/**
* Check if a perspective is 'raw'
*
* @param perspective - the list with perspective ids or a simple perspective id
* @returns true if the perspective is 'raw'
* @deprecated – use perspective === 'raw' instead
* @internal
*/
function isPerspectiveRaw(perspective) {
	return perspective ? isString$1(perspective) ? perspective === "raw" : perspective.some((p) => p === "raw") : !1;
}
/**
* Hook to check if the current perspective is viewing a paused scheduled draft.
*
* @returns Object containing isPaused boolean and the current release document (if any)
* @internal
*/
function usePausedScheduledDraft() {
	let $ = (0, import_compiler_runtime.c)(9), { selectedReleaseId } = usePerspective(), { data: t0 } = useActiveReleases(), t1;
	$[0] === t0 ? t1 = $[1] : (t1 = t0 === void 0 ? [] : t0, $[0] = t0, $[1] = t1);
	let allReleases = t1, currentRelease, t2;
	$[2] !== allReleases || $[3] !== selectedReleaseId ? (currentRelease = selectedReleaseId ? allReleases.find((release) => getReleaseIdFromReleaseDocumentId(release._id) === selectedReleaseId) : void 0, t2 = isPausedCardinalityOneRelease(currentRelease), $[2] = allReleases, $[3] = selectedReleaseId, $[4] = currentRelease, $[5] = t2) : (currentRelease = $[4], t2 = $[5]);
	let t3;
	return $[6] !== currentRelease || $[7] !== t2 ? (t3 = {
		isPaused: t2,
		currentRelease
	}, $[6] = currentRelease, $[7] = t2, $[8] = t3) : t3 = $[8], t3;
}
var AGENT_TONE = "suggest";
/**
* Filters a list of version document IDs and provides display metadata for
* agent bundles.
*
* - Other users' `agent-*` versions are removed from the returned list.
* - The current user's `agent-*` versions are kept and can be resolved to
*   display overrides via `getVersionDisplay`.
* - Non-agent versions pass through unchanged.
* - While the SSE connection is loading, only the currently active agent
*   bundle (if any) is kept; all others are hidden until ownership is confirmed.
*
* Call this hook once and thread the results down — don't call it per-item.
*
* @internal
*/
function useAgentVersionDisplay(versionIds, activeBundleId) {
	let $ = (0, import_compiler_runtime.c)(32), { bundles, loading } = useAgentBundles(), { t } = useTranslation$1(), t0;
	$[0] === bundles ? t0 = $[1] : (t0 = new Set(bundles.map(_temp$39)), $[0] = bundles, $[1] = t0);
	let myBundleIds = t0, t1;
	if ($[2] !== activeBundleId || $[3] !== loading || $[4] !== myBundleIds || $[5] !== versionIds) {
		let t2;
		$[7] !== activeBundleId || $[8] !== loading || $[9] !== myBundleIds ? (t2 = (id) => {
			let name = getVersionFromId(id);
			return !name || !isAgentBundleName(name) ? !1 : loading ? name === activeBundleId : myBundleIds.has(name);
		}, $[7] = activeBundleId, $[8] = loading, $[9] = myBundleIds, $[10] = t2) : t2 = $[10], t1 = versionIds.find(t2), $[2] = activeBundleId, $[3] = loading, $[4] = myBundleIds, $[5] = versionIds, $[6] = t1;
	} else t1 = $[6];
	let primaryAgentId = t1, t2;
	if ($[11] !== primaryAgentId || $[12] !== versionIds) {
		let t3;
		$[14] === primaryAgentId ? t3 = $[15] : (t3 = (id_0) => {
			let name_0 = getVersionFromId(id_0);
			return !name_0 || !isAgentBundleName(name_0) || id_0 === primaryAgentId;
		}, $[14] = primaryAgentId, $[15] = t3), t2 = versionIds.filter(t3), $[11] = primaryAgentId, $[12] = versionIds, $[13] = t2;
	} else t2 = $[13];
	let filteredVersionIds = t2, t3;
	$[16] === t ? t3 = $[17] : (t3 = t("version.agent-bundle.proposed-changes"), $[16] = t, $[17] = t3);
	let t4;
	$[18] === t3 ? t4 = $[19] : (t4 = {
		displayName: t3,
		tone: AGENT_TONE
	}, $[18] = t3, $[19] = t4);
	let ownDisplay = t4, t5;
	$[20] === t ? t5 = $[21] : (t5 = t("version.agent-bundle.agent-changes"), $[20] = t, $[21] = t5);
	let t6;
	$[22] === t5 ? t6 = $[23] : (t6 = {
		displayName: t5,
		tone: AGENT_TONE
	}, $[22] = t5, $[23] = t6);
	let otherDisplay = t6, t7;
	$[24] !== loading || $[25] !== myBundleIds || $[26] !== otherDisplay || $[27] !== ownDisplay ? (t7 = (versionDocumentId) => {
		let name_1 = getVersionFromId(versionDocumentId);
		return !name_1 || !isAgentBundleName(name_1) ? null : !loading && myBundleIds.has(name_1) ? ownDisplay : otherDisplay;
	}, $[24] = loading, $[25] = myBundleIds, $[26] = otherDisplay, $[27] = ownDisplay, $[28] = t7) : t7 = $[28];
	let getVersionDisplay = t7, t8;
	return $[29] !== filteredVersionIds || $[30] !== getVersionDisplay ? (t8 = {
		filteredVersionIds,
		getVersionDisplay
	}, $[29] = filteredVersionIds, $[30] = getVersionDisplay, $[31] = t8) : t8 = $[31], t8;
}
function _temp$39(b) {
	return b.id;
}
/** @internal */
function getProviderTitle(provider) {
	if (provider === "google") return "Google";
	if (provider === "github") return "GitHub";
	if (provider === "sanity") return "Sanity";
	if (provider?.startsWith("saml-")) return "SAML/SSO";
}
/** @internal */
var remoteSnapshots = memoize$1((client, idPair, typeName, _serverActionsEnabled, pairListenerOptions) => memoizedPair(client, idPair, typeName, pairListenerOptions).pipe(switchMap(({ published, draft, version }) => merge$1(published.remoteSnapshot$, draft.remoteSnapshot$, version?.remoteSnapshot$ ?? EMPTY))), memoizeKeyGen);
var LOADING_STATE = {
	isLoaded: !1,
	documentType: void 0
};
/** @internal */
function useDocumentType(documentId, t0) {
	let $ = (0, import_compiler_runtime.c)(10), specifiedType = t0 === void 0 ? "*" : t0, documentStore = useDocumentStore(), t1;
	$[0] === documentId ? t1 = $[1] : (t1 = getPublishedId(documentId), $[0] = documentId, $[1] = t1);
	let publishedId = t1, isResolved = !!(specifiedType && specifiedType !== "*"), t2;
	$[2] === specifiedType ? t2 = $[3] : (t2 = {
		documentType: specifiedType,
		isLoaded: !0
	}, $[2] = specifiedType, $[3] = t2);
	let SYNC_RESOLVED_STATE = t2, t3;
	$[4] !== SYNC_RESOLVED_STATE || $[5] !== documentStore || $[6] !== isResolved || $[7] !== publishedId || $[8] !== specifiedType ? (t3 = isResolved ? of(SYNC_RESOLVED_STATE) : documentStore.resolveTypeForDocument(publishedId, specifiedType).pipe(map(_temp$38), startWith(LOADING_STATE)), $[4] = SYNC_RESOLVED_STATE, $[5] = documentStore, $[6] = isResolved, $[7] = publishedId, $[8] = specifiedType, $[9] = t3) : t3 = $[9];
	let resolvedState = useSyncObservable(t3, isResolved ? SYNC_RESOLVED_STATE : LOADING_STATE);
	return isResolved ? SYNC_RESOLVED_STATE : resolvedState;
}
function _temp$38(documentType) {
	return {
		documentType,
		isLoaded: !0
	};
}
/**
* @internal
*/
function EventsProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(3), { value, children } = t0, t1;
	return $[0] !== children || $[1] !== value ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EventsContext.Provider, {
		value,
		children
	}), $[0] = children, $[1] = value, $[2] = t1) : t1 = $[2], t1;
}
/**
* @internal
*/
function useEvents() {
	let context = (0, import_react.useContext)(EventsContext);
	if (context === null) throw Error("useEvents must be used within a EventsProvider");
	return context;
}
var addDocumentVariantTypeToEvents = (events, documentVariantType) => events.map((event) => ({
	...event,
	documentVariantType
}));
function createEventsObservable({ releases$, events$, remoteEdits$, expandedEvents$, documentId }) {
	let documentVariantType = getDocumentVariantType(documentId);
	return combineLatest([
		releases$,
		events$,
		remoteEdits$,
		expandedEvents$
	]).pipe(map(([releases, { events, nextCursor, loading, error }, remoteEdits, expandedEvents]) => {
		let eventsWithRemoteEdits = sortEvents({
			remoteEdits,
			events,
			expandedEvents
		});
		return documentVariantType === "published" ? {
			events: updatePublishedEvents(eventsWithRemoteEdits, releases),
			nextCursor,
			loading,
			error
		} : documentVariantType === "draft" ? {
			events: addParentToEvents(eventsWithRemoteEdits),
			nextCursor,
			loading,
			error
		} : {
			events: updateVersionEvents(eventsWithRemoteEdits),
			nextCursor,
			loading,
			error
		};
	}), map((value) => ({
		...value,
		events: addDocumentVariantTypeToEvents(value.events, documentVariantType)
	})), map((value) => ({
		...value,
		events: squashLiveEditEvents(value.events)
	})));
}
function getExpandEvents({ documentId, client }) {
	let expandedEventsMap$ = new BehaviorSubject(/* @__PURE__ */ new Map()), expandedEvents$ = expandedEventsMap$.pipe(map((expandedEventsMap) => Array.from(expandedEventsMap.values()).flatMap((v) => v))), handleExpandEvent = async (event) => {
		if (!expandedEventsMap$.getValue().has(event.id)) if ((isPublishDocumentVersionEvent(event) || isDeleteDocumentVersionEvent(event)) && event.versionRevisionId && event.creationEvent) {
			let editEvents = getEditEvents(await getDocumentTransactions({
				client,
				documentId,
				fromTransaction: event.creationEvent.versionRevisionId,
				toTransaction: event.versionRevisionId
			}), documentId, !1).map((editEvent) => ({
				...editEvent,
				parentId: event.id
			})), value = expandedEventsMap$.getValue();
			value.set(event.id, editEvents), expandedEventsMap$.next(value);
		} else console.error("This event can't be expanded", event);
	};
	return {
		handleExpandEvent,
		expandedEvents$
	};
}
function getRemoteTransactionsSubscription({ client, isLiveEdit, documentId, documentType, onRefetch }) {
	let remoteTransactions$ = new BehaviorSubject([]), remoteEdits$ = remoteTransactions$.pipe(map((transactions) => getEditEvents(transactions, documentId, isLiveEdit))), documentVariantType = getDocumentVariantType(documentId), onMutationReceived = (remoteMutation) => {
		if (!remoteMutation) return;
		let variant = remoteMutation.version;
		if (variant !== documentVariantType) return;
		if (variant === "published" && !isLiveEdit) {
			onRefetch();
			return;
		}
		let effectState = getEffectState({
			apply: remoteMutation.effects.apply,
			revert: remoteMutation.effects.revert
		});
		if (effectState === "created" || effectState === "deleted") {
			onRefetch(), remoteTransactions$.next([]);
			return;
		}
		remoteTransactions$.next([...remoteTransactions$.value, remoteMutationToTransaction(remoteMutation)]);
	}, subscription = remoteSnapshots(client, {
		draftId: getDraftId(documentId),
		publishedId: getPublishedId(documentId),
		...isVersionId(documentId) ? { versionId: documentId } : {}
	}, documentType).pipe(filter((event) => event.type === "remoteMutation"));
	return {
		remoteTransactions$,
		remoteEdits$,
		subscribe: () => subscription.subscribe(onMutationReceived)
	};
}
/**
* Creates an event store for a document.
* If you want to use this in a React component, consider using `useEventsStore` instead.
*
* Consider subscribing the remoteEventsListener to get updates on remote transactions.
*/
function createEventsStore({ client, documentId, documentType, releases$, isLiveEdit }) {
	let initialEvents = getInitialFetchEvents({
		client,
		documentId
	}), { expandedEvents$, handleExpandEvent } = getExpandEvents({
		client,
		documentId
	}), { remoteEdits$, remoteTransactions$, subscribe } = getRemoteTransactionsSubscription({
		client,
		documentId,
		documentType,
		isLiveEdit,
		onRefetch: initialEvents.reloadEvents
	}), eventsObservable$ = createEventsObservable({
		documentId,
		events$: initialEvents.events$,
		remoteEdits$,
		expandedEvents$,
		releases$
	});
	return {
		eventsObservable$,
		getDocumentChanges: (revision$, since$) => getDocumentChanges({
			client,
			eventsObservable$,
			documentId,
			remoteTransactions$,
			to$: revision$,
			since$
		}),
		handleExpandEvent,
		loadMoreEvents: initialEvents.loadMore,
		reloadEvents: initialEvents.reloadEvents,
		remoteTransactionsListener: subscribe
	};
}
var INITIAL_VALUE = {
	events: [],
	nextCursor: "",
	loading: !0,
	error: null
};
/**
* @internal
*/
function useEventsStore(t0) {
	let $ = (0, import_compiler_runtime.c)(72), { documentId, documentType, rev, since } = t0, client = useClient(RELEASES_STUDIO_CLIENT_OPTIONS), { state$: releases$ } = useReleasesStore(), schema = useSchema(), t1;
	$[0] !== documentType || $[1] !== schema ? (t1 = schema.get(documentType), $[0] = documentType, $[1] = schema, $[2] = t1) : t1 = $[2];
	let isLiveEdit = !!t1?.liveEdit, t2;
	$[3] !== client || $[4] !== documentId || $[5] !== documentType || $[6] !== isLiveEdit || $[7] !== releases$ ? (t2 = createEventsStore({
		client,
		documentId,
		documentType,
		releases$,
		isLiveEdit
	}), $[3] = client, $[4] = documentId, $[5] = documentType, $[6] = isLiveEdit, $[7] = releases$, $[8] = t2) : t2 = $[8];
	let eventsStore = t2, { events, loading, error, nextCursor } = useObservable(eventsStore.eventsObservable$, INITIAL_VALUE), t3, t4;
	$[9] === eventsStore ? (t3 = $[10], t4 = $[11]) : (t3 = () => {
		let subscription = eventsStore.remoteTransactionsListener();
		return () => {
			subscription.unsubscribe();
		};
	}, t4 = [eventsStore], $[9] = eventsStore, $[10] = t3, $[11] = t4), (0, import_react.useEffect)(t3, t4);
	let t5;
	bb0: {
		if (rev === "@lastPublished") {
			let t6;
			$[12] === events ? t6 = $[13] : (t6 = events.find(isPublishDocumentVersionEvent), $[12] = events, $[13] = t6), t5 = t6?.id || null;
			break bb0;
		}
		if (rev === "@lastEdited") {
			let t6;
			$[14] === events ? t6 = $[15] : (t6 = events.find(isEditDocumentVersionEvent), $[14] = events, $[15] = t6);
			let editEvent = t6;
			if (editEvent) {
				t5 = editEvent.revisionId;
				break bb0;
			}
		}
		if (rev?.startsWith("@release:")) {
			let t6;
			$[16] === rev ? t6 = $[17] : (t6 = rev.split(":"), $[16] = rev, $[17] = t6);
			let releaseId = t6[1], t7;
			if ($[18] !== events || $[19] !== releaseId) {
				let t8;
				$[21] === releaseId ? t8 = $[22] : (t8 = (event) => isPublishDocumentVersionEvent(event) && event.releaseId === releaseId, $[21] = releaseId, $[22] = t8), t7 = events.find(t8), $[18] = events, $[19] = releaseId, $[20] = t7;
			} else t7 = $[20];
			let releaseEvent = t7;
			if (releaseEvent) {
				t5 = releaseEvent.id;
				break bb0;
			}
			events.length > 0 && !loading && eventsStore.loadMoreEvents();
		}
		if (!rev) {
			let [lastEvent] = events;
			if (lastEvent) {
				if (isPublishDocumentVersionEvent(lastEvent)) {
					t5 = lastEvent.id;
					break bb0;
				}
				if (isDeleteDocumentVersionEvent(lastEvent)) {
					let t6;
					$[23] === events ? t6 = $[24] : (t6 = events.find(isEditDocumentVersionEvent)?.revisionId, $[23] = events, $[24] = t6), t5 = t6;
					break bb0;
				}
			}
		}
		t5 = rev;
	}
	let revisionId = t5, t6;
	$[25] !== client || $[26] !== documentId ? (t6 = (revision) => getDocumentAtRevision({
		client,
		documentId,
		revisionId: revision
	}), $[25] = client, $[26] = documentId, $[27] = t6) : t6 = $[27];
	let getDocumentAtRevision$1 = t6, t7;
	$[28] !== getDocumentAtRevision$1 || $[29] !== revisionId ? (t7 = revisionId ? getDocumentAtRevision$1(revisionId) : of(null), $[28] = getDocumentAtRevision$1, $[29] = revisionId, $[30] = t7) : t7 = $[30];
	let revision$ = t7, revision_0 = useSyncObservable(revision$, null), t8;
	bb1: {
		if (since && since !== "@lastPublished") {
			t8 = since;
			break bb1;
		}
		if (!events) {
			t8 = null;
			break bb1;
		}
		if (since === "@lastPublished" || !since) {
			let t9;
			$[31] === revisionId ? t9 = $[32] : (t9 = (e) => e.id === revisionId, $[31] = revisionId, $[32] = t9);
			let revisionIndex = events.findIndex(t9), t10;
			$[33] !== events || $[34] !== revisionIndex ? (t10 = events.slice(revisionIndex + 1).find(isPublishDocumentVersionEvent)?.id, $[33] = events, $[34] = revisionIndex, $[35] = t10) : t10 = $[35];
			let lastPublishedId = t10;
			if (lastPublishedId) {
				t8 = lastPublishedId;
				break bb1;
			}
			let t11;
			$[36] === events ? t11 = $[37] : (t11 = events.find(isCreateDocumentVersionEvent), $[36] = events, $[37] = t11);
			let creationEvent = t11;
			if (creationEvent) {
				t8 = creationEvent.id;
				break bb1;
			}
		}
		if (!revisionId) {
			t8 = events[1]?.id;
			break bb1;
		}
		let revisionEventIndex = events.findIndex((e_0) => e_0.id === revisionId);
		if (revisionEventIndex === -1) {
			t8 = null;
			break bb1;
		}
		t8 = events[revisionEventIndex + 1]?.id || null;
	}
	let sinceId = t8, t9;
	$[38] !== getDocumentAtRevision$1 || $[39] !== sinceId ? (t9 = sinceId ? getDocumentAtRevision$1(sinceId) : of(null), $[38] = getDocumentAtRevision$1, $[39] = sinceId, $[40] = t9) : t9 = $[40];
	let since$ = t9, t10;
	$[41] !== eventsStore || $[42] !== revision$ || $[43] !== since$ ? (t10 = () => eventsStore.getDocumentChanges(revision$, since$), $[41] = eventsStore, $[42] = revision$, $[43] = since$, $[44] = t10) : t10 = $[44];
	let getChangesList = t10, sinceRevision = useSyncObservable(since$, null), t11;
	$[45] === documentId ? t11 = $[46] : (t11 = getDocumentVariantType(documentId), $[45] = documentId, $[46] = t11);
	let documentVariantType = t11, t12;
	$[47] !== documentVariantType || $[48] !== events || $[49] !== revisionId || $[50] !== since ? (t12 = (nextRev) => {
		if (!events) return [null, null];
		let revisionIndex_0 = events.findIndex((event_0) => event_0.id === nextRev);
		if (revisionIndex_0 === 0) return documentVariantType === "version" && isPublishDocumentVersionEvent(events[0]) ? [since || null, nextRev] : [since || null, null];
		if (!since) {
			let currentRevisionIndex = events.findIndex((event_1) => event_1.id === revisionId);
			return currentRevisionIndex === -1 || revisionIndex_0 === -1 || revisionIndex_0 > currentRevisionIndex ? [null, nextRev] : [revisionId || null, nextRev];
		}
		let sinceIndex = events.findIndex((event_2) => event_2.id === since);
		return sinceIndex === -1 || revisionIndex_0 === -1 || sinceIndex < revisionIndex_0 || sinceIndex === revisionIndex_0 ? [null, nextRev] : [since, nextRev];
	}, $[47] = documentVariantType, $[48] = events, $[49] = revisionId, $[50] = since, $[51] = t12) : t12 = $[51];
	let findRangeForRevision = t12, t13;
	$[52] !== events || $[53] !== rev || $[54] !== revisionId ? (t13 = (nextSince) => {
		if (!events) return [null, null];
		if (!rev || !revisionId) return [nextSince, null];
		let revisionIndex_1 = events.findIndex((event_3) => event_3.id === revisionId), sinceIndex_0 = events.findIndex((event_4) => event_4.id === nextSince);
		return sinceIndex_0 === -1 || revisionIndex_1 === -1 || sinceIndex_0 < revisionIndex_1 || sinceIndex_0 === revisionIndex_1 ? [nextSince, null] : [nextSince, revisionId];
	}, $[52] = events, $[53] = rev, $[54] = revisionId, $[55] = t13) : t13 = $[55];
	let findRangeForSince = t13, t14;
	$[56] === events ? t14 = $[57] : (t14 = events.find(_temp$37)?.id || null, $[56] = events, $[57] = t14);
	let lastNonDeletedRevId = t14, t15;
	return $[58] !== error || $[59] !== events || $[60] !== eventsStore.handleExpandEvent || $[61] !== eventsStore.loadMoreEvents || $[62] !== findRangeForRevision || $[63] !== findRangeForSince || $[64] !== getChangesList || $[65] !== getDocumentAtRevision$1 || $[66] !== lastNonDeletedRevId || $[67] !== loading || $[68] !== nextCursor || $[69] !== revision_0 || $[70] !== sinceRevision ? (t15 = {
		events,
		nextCursor,
		loading,
		error,
		revision: revision_0,
		sinceRevision,
		findRangeForRevision,
		findRangeForSince,
		loadMoreEvents: eventsStore.loadMoreEvents,
		expandEvent: eventsStore.handleExpandEvent,
		getChangesList,
		getDocumentAtRevision: getDocumentAtRevision$1,
		lastNonDeletedRevId
	}, $[58] = error, $[59] = events, $[60] = eventsStore.handleExpandEvent, $[61] = eventsStore.loadMoreEvents, $[62] = findRangeForRevision, $[63] = findRangeForSince, $[64] = getChangesList, $[65] = getDocumentAtRevision$1, $[66] = lastNonDeletedRevId, $[67] = loading, $[68] = nextCursor, $[69] = revision_0, $[70] = sinceRevision, $[71] = t15) : t15 = $[71], t15;
}
function _temp$37(e_1) {
	return !isDeleteDocumentGroupEvent(e_1) && !isDeleteDocumentVersionEvent(e_1);
}
function serialize(item) {
	return typeof item == "object" && item && "serialize" in item ? serialize(item.serialize()) : item;
}
/**
* The observable version of `useTemplatePermissions`
*
* @internal
*/
function getTemplatePermissions({ grantsStore, templateItems, templates, schema, context }) {
	return templateItems?.length ? defer(() => from(templateItems).pipe(mergeMap((item) => {
		let serializedItem = serialize(item), template = templates.find((t) => t.id === serializedItem.templateId);
		if (!template) throw Error(`template not found: "${serializedItem.templateId}"`);
		return from(resolveInitialValue(schema, template, serializedItem.parameters, context, { useCache: !0 })).pipe(timeout({ first: RESOLVE_INITIAL_VALUE_TIMEOUT_MS }), map((resolvedInitialValue) => ({
			item: serializedItem,
			template,
			resolvedInitialValue
		})), catchError((resolveError) => (console.error(`Failed to resolve initial value for template "${serializedItem.templateId}":`, resolveError), of({
			item: serializedItem,
			template,
			resolvedInitialValue: {}
		}))));
	}), map(({ item, template, resolvedInitialValue }) => {
		let schemaType = schema.get(template.schemaType);
		if (!schemaType) throw Error(`schema type not found: "${template.schemaType}"`);
		let liveEdit = schemaType?.liveEdit, { initialDocumentId = "dummy-id" } = item;
		return getDocumentValuePermissions({
			grantsStore,
			permission: "create",
			document: {
				_id: liveEdit ? getPublishedId(initialDocumentId) : getDraftId(initialDocumentId),
				_type: schemaType.name,
				...resolvedInitialValue
			}
		}).pipe(map(({ granted, reason }) => ({
			...item,
			granted,
			reason,
			resolvedInitialValue,
			template,
			i18n: item.i18n || template.i18n,
			title: item.title || template.title,
			subtitle: schemaType.title === (item.title || template.title) ? void 0 : schemaType.title,
			description: item.description || template.description,
			icon: item.icon || template.icon
		})));
	}), toArray(), switchMap((observables) => observables.length ? combineLatest(observables) : of([])))) : of([]);
}
/**
* Takes in an array of initial template values and returns an object of
* `TemplatePermissionsResult` keyed by the IDs of the initial template values
* given.
*
* The `TemplatePermissionsResult` is an object that contains a `granted`
* boolean per key and can be used to determine if a user has the ability to
* create documents using the given initial value template items.
*
* For each initial template value item, the corresponding template is found and
* resolved against the parameters in each the initial template value item. The
* resolved value is then run through the document-value permissions. If there
* are any matching grants for the resolved initial template value, the
* `TemplatePermissionsResult` will include `granted: true`.
*
* @internal
*/
var useTemplatePermissionsFromHookFactory = createHookFromObservableFactory(getTemplatePermissions);
/** @internal */
function useTemplatePermissions(t0) {
	let $ = (0, import_compiler_runtime.c)(9), rest, templateItems;
	$[0] === t0 ? (rest = $[1], templateItems = $[2]) : ({templateItems, ...rest} = t0, $[0] = t0, $[1] = rest, $[2] = templateItems);
	let schema = useSchema(), templates = useTemplates(), grantsStore = useGrantsStore(), initialValueContext = useInitialValueResolverContext(), t1 = rest.grantsStore || grantsStore, t2 = rest.schema || schema, t3 = rest.templates || templates, t4;
	return $[3] !== initialValueContext || $[4] !== t1 || $[5] !== t2 || $[6] !== t3 || $[7] !== templateItems ? (t4 = {
		templateItems,
		grantsStore: t1,
		schema: t2,
		templates: t3,
		context: initialValueContext
	}, $[3] = initialValueContext, $[4] = t1, $[5] = t2, $[6] = t3, $[7] = templateItems, $[8] = t4) : t4 = $[8], useTemplatePermissionsFromHookFactory(t4);
}
/**
* Custom hook which wraps around `useSyncExternalStore`.
* Accepts a selector function which can be used to opt-in to specific timelineStore updates.
*
* @internal
*/
function useTimelineSelector(timelineStore, selector) {
	if (!timelineStore) throw Error("Passed timelineStore is undefined, if your are using the events timeline, call useEvents() instead. If you need to use this hook, opt in by setting the beta.eventsAPI.enabled feature flag to false");
	return (0, import_with_selector.useSyncExternalStoreWithSelector)(timelineStore.subscribe, timelineStore.getSnapshot, null, selector);
}
var INITIAL_TIMELINE_STATE = {
	chunks: [],
	diff: null,
	hasMoreChunks: null,
	isLoading: !1,
	isPristine: null,
	lastNonDeletedRevId: null,
	onOlderRevision: !1,
	realRevChunk: null,
	revTime: null,
	selectionState: "inactive",
	sinceAttributes: null,
	sinceTime: null,
	timelineDisplayed: null,
	timelineReady: !1
};
/**
* Creates a store which handles the creation of a document Timeline,
* TimelineController and also fetches pre-requisite document snapshots.
*
* `TimelineStore` exposes select TimelineController methods used to query
* ranges and fetch more transactions. It can also be used with
* `useSyncExternalStore` to subscribe to selected state changes.
*
* @deprecated Use events store instead, this will be removed in a future release
* @internal
* */
function useTimelineStore(t0) {
	let $ = (0, import_compiler_runtime.c)(32), { documentId, documentType, onError, rev, since } = t0, historyStore = useHistoryStore(), snapshotsSubscriptionRef = (0, import_react.useRef)(null), timelineStateRef = (0, import_react.useRef)(INITIAL_TIMELINE_STATE), client = useClient(DEFAULT_STUDIO_CLIENT_OPTIONS), t1;
	$[0] !== client || $[1] !== documentId || $[2] !== documentType || $[3] !== historyStore ? (t1 = historyStore.getTimelineController({
		client,
		documentId,
		documentType
	}), $[0] = client, $[1] = documentId, $[2] = documentType, $[3] = historyStore, $[4] = t1) : t1 = $[4];
	let controller = t1, t2;
	$[5] === controller ? t2 = $[6] : (t2 = new BehaviorSubject(controller), $[5] = controller, $[6] = t2);
	let timelineController$ = t2, t3, t4;
	$[7] !== controller || $[8] !== rev || $[9] !== since || $[10] !== timelineController$ ? (t3 = () => (controller.setRange(since || null, rev || null), timelineController$.next(controller), controller.setHandler((err, innerController) => {
		err ? timelineController$.error(err) : setTimeout(() => {
			innerController.setRange(since || null, rev || null), timelineController$.next(innerController);
		}, 0);
	}), controller.resume(), () => controller.suspend()), t4 = [
		rev,
		since,
		controller,
		timelineController$
	], $[7] = controller, $[8] = rev, $[9] = since, $[10] = timelineController$, $[11] = t3, $[12] = t4) : (t3 = $[11], t4 = $[12]), (0, import_react.useEffect)(t3, t4);
	let t5, t6;
	$[13] !== client || $[14] !== controller || $[15] !== documentId || $[16] !== documentType ? (t5 = () => (snapshotsSubscriptionRef.current ||= remoteSnapshots(client, {
		draftId: `drafts.${documentId}`,
		publishedId: documentId
	}, documentType).subscribe((ev) => {
		controller.handleRemoteMutation(ev);
	}), () => {
		snapshotsSubscriptionRef.current &&= (snapshotsSubscriptionRef.current.unsubscribe(), null);
	}), t6 = [
		client,
		controller,
		documentId,
		documentType
	], $[13] = client, $[14] = controller, $[15] = documentId, $[16] = documentType, $[17] = t5, $[18] = t6) : (t5 = $[17], t6 = $[18]), (0, import_react.useEffect)(t5, t6);
	let t7, t8, t9;
	$[19] === controller ? (t7 = $[20], t8 = $[21], t9 = $[22]) : (t7 = (chunk) => controller.findRangeForNewRev(chunk), t8 = (chunk_0) => controller.findRangeForNewSince(chunk_0), t9 = () => {
		controller.setLoadMore(!0);
	}, $[19] = controller, $[20] = t7, $[21] = t8, $[22] = t9);
	let t10;
	$[23] === Symbol.for("react.memo_cache_sentinel") ? (t10 = () => timelineStateRef.current, $[23] = t10) : t10 = $[23];
	let t11;
	$[24] !== onError || $[25] !== timelineController$ ? (t11 = (callback) => {
		let subscription = timelineController$.pipe(map(_temp3$9), distinctUntilChanged(import_react_fast_compare.default), catchError((err_0) => (onError?.(err_0), of(INITIAL_TIMELINE_STATE))), tap((timelineState) => {
			timelineStateRef.current = timelineState;
		}), tap(callback)).subscribe();
		return () => subscription.unsubscribe();
	}, $[24] = onError, $[25] = timelineController$, $[26] = t11) : t11 = $[26];
	let t12;
	return $[27] !== t11 || $[28] !== t7 || $[29] !== t8 || $[30] !== t9 ? (t12 = {
		findRangeForRev: t7,
		findRangeForSince: t8,
		loadMore: t9,
		getSnapshot: t10,
		subscribe: t11
	}, $[27] = t11, $[28] = t7, $[29] = t8, $[30] = t9, $[31] = t12) : t12 = $[31], t12;
}
function _temp3$9(innerController_0) {
	let chunks = innerController_0.timeline.mapChunks(_temp$36), lastNonDeletedChunk = chunks.filter(_temp2$16), hasMoreChunks = !innerController_0.timeline.reachedEarliestEntry;
	hasMoreChunks && chunks.length > 16 && innerController_0.setLoadMore(!1);
	let timelineReady = !["invalid", "loading"].includes(innerController_0.selectionState);
	return {
		chunks,
		diff: innerController_0.sinceTime ? innerController_0.currentObjectDiff() : null,
		isLoading: innerController_0.isLoading,
		isPristine: timelineReady ? chunks.length === 0 && !hasMoreChunks : null,
		hasMoreChunks: !innerController_0.timeline.reachedEarliestEntry,
		lastNonDeletedRevId: lastNonDeletedChunk?.[0]?.id,
		onOlderRevision: innerController_0.onOlderRevision(),
		realRevChunk: innerController_0.realRevChunk,
		revTime: innerController_0.revTime,
		selectionState: innerController_0.selectionState,
		sinceAttributes: innerController_0.sinceAttributes(),
		sinceTime: innerController_0.sinceTime,
		timelineDisplayed: innerController_0.displayed(),
		timelineReady
	};
}
function _temp2$16(chunk_1) {
	return !["delete", "initial"].includes(chunk_1.type);
}
function _temp$36(c) {
	return c;
}
var initial = [];
var fallback = of(initial);
/** @internal */
function useGlobalPresence() {
	let $ = (0, import_compiler_runtime.c)(2), [mounted, mount] = (0, import_react.useReducer)(_temp$35, !1), t0, t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = () => (0, import_react.startTransition)(mount), t1 = [], $[0] = t0, $[1] = t1) : (t0 = $[0], t1 = $[1]), (0, import_react.useEffect)(t0, t1);
	let presenceStore = usePresenceStore();
	return useObservable(mounted ? presenceStore.globalPresence$ : fallback, initial);
}
function _temp$35() {
	return !0;
}
/** @internal */
function useProjectDatasets() {
	let $ = (0, import_compiler_runtime.c)(4), projectStore = useProjectStore(), t0;
	$[0] === projectStore ? t0 = $[1] : (t0 = projectStore.getDatasets(), $[0] = projectStore, $[1] = t0);
	let value = useObservable(t0, null), t1;
	return $[2] === value ? t1 = $[3] : (t1 = { value }, $[2] = value, $[3] = t1), t1;
}
/**
* Evaluates a workspace's `hidden` property against the given auth state.
* Falls open (returns `false`) when auth state is unavailable or the callback throws.
*
* @internal
*/
function evaluateWorkspaceHidden(workspace, authState) {
	let { hidden } = workspace;
	if (typeof hidden == "boolean") return hidden;
	if (typeof hidden == "function") {
		if (authState === void 0) return !1;
		try {
			return hidden({ currentUser: authState.currentUser });
		} catch (error) {
			return console.error(`Error evaluating \`hidden\` callback for workspace "${workspace.name}":`, error), !1;
		}
	}
	return !1;
}
/**
* Returns visible workspaces and auth states from the nearest
* `VisibleWorkspacesProvider`.
*
* @internal
*/
function useVisibleWorkspaces() {
	let context = (0, import_react.useContext)(VisibleWorkspacesContext);
	if (context === null) throw Error("useVisibleWorkspaces: missing VisibleWorkspacesProvider in component tree");
	return context;
}
/** @internal */
function useWorkspaces() {
	let workspaces = (0, import_react.useContext)(WorkspacesContext);
	if (!workspaces) throw Error("Could not find `workspaces` context");
	return workspaces;
}
/** @internal */
var ActiveWorkspaceMatcherProvider = (0, import_react.memo)(function ActiveWorkspaceMatcherContextProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(9), { children, history, activeWorkspace, setActiveWorkspace } = t0, t1;
	$[0] !== activeWorkspace || $[1] !== setActiveWorkspace ? (t1 = {
		activeWorkspace,
		setActiveWorkspace
	}, $[0] = activeWorkspace, $[1] = setActiveWorkspace, $[2] = t1) : t1 = $[2];
	let value = t1, t2;
	$[3] !== children || $[4] !== history ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RouterHistoryProvider, {
		history,
		children
	}), $[3] = children, $[4] = history, $[5] = t2) : t2 = $[5];
	let t3;
	return $[6] !== t2 || $[7] !== value ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActiveWorkspaceMatcherContext.Provider, {
		value,
		children: t2
	}), $[6] = t2, $[7] = value, $[8] = t3) : t3 = $[8], t3;
});
/**
* Recursively creates a matching expression.
* A list over normalized workspaces, like
* ```
* [
*  {workspace: {…}, name: 'default', basePath: '/studio/deep/shared/test'},
*  {workspace: {…}, name: 'playground', basePath: '/studio/deep/shared/playground'}
*  {workspace: {…}, name: 'staging', basePath: '/studio/deep/shared/staging'}
* ]
* ```
* Becomes a Regex like `/^(\/studio(\/deep(\/shared(\/|$))?(\/|$))?(\/|$))?$/i`
* @internal
*/
function createCommonBasePathRegex(workspaces) {
	if (workspaces.length === 0) return /^$/;
	let commonBasePath = workspaces.map((workspace) => workspace.basePath.slice(1).split("/")).reduce((commonSegments, segments) => {
		for (let i = 0; i < commonSegments.length; i++) if (commonSegments[i] !== segments[i].toLowerCase()) return commonSegments.slice(0, i);
		return commonSegments;
	});
	function createCommonBasePathRegexRecursively([first, ...rest]) {
		return first ? `(\\/${escapeRegExp(first)}${createCommonBasePathRegexRecursively(rest)}(\\/|$))?` : "";
	}
	return RegExp(`^${createCommonBasePathRegexRecursively(commonBasePath)}$`, "i");
}
/**
* Resolves a pathname against the configured workspaces. Returns a match for a
* known workspace path, a redirect to the first visible workspace when the
* pathname is the common base path, or not-found otherwise.
*
* The default redirect walks workspaces in config order, skipping any with
* `hidden: true` or with a `hidden()` callback that returns `true`. It returns
* `loading` only when a function-hidden workspace is reached whose auth state
* has not yet resolved. If every workspace is hidden, it falls back to the
* first configured workspace.
*
* @internal
*/
function matchWorkspace({ pathname, workspaces, basePathRegex, workspaceAuthStates }) {
	if (workspaces.length === 0) return { type: "not-found" };
	for (let { workspace, basePath, basePathRegex } of workspaces) if (basePathRegex.test(pathname) || basePath === "/") return {
		type: "match",
		workspace
	};
	if (pathname === "/" || basePathRegex.test(pathname)) {
		for (let { workspace, basePath } of workspaces) {
			let { hidden } = workspace;
			if (hidden !== !0) {
				if (typeof hidden == "function") {
					let authState = workspaceAuthStates[workspace.name];
					if (authState === void 0) return { type: "loading" };
					if (evaluateWorkspaceHidden(workspace, authState)) continue;
				}
				return {
					type: "redirect",
					pathname: basePath
				};
			}
		}
		return {
			type: "redirect",
			pathname: workspaces[0].basePath
		};
	}
	return { type: "not-found" };
}
/** @internal */
function useNormalizedWorkspaces(workspaces) {
	let $ = (0, import_compiler_runtime.c)(2), t0;
	return $[0] === workspaces ? t0 = $[1] : (t0 = normalizedWorkspaces(workspaces), $[0] = workspaces, $[1] = t0), t0;
}
/** @internal */
function normalizedWorkspaces(workspaces) {
	return workspaces.map((workspace) => {
		let basePath = workspace.basePath || "/";
		return {
			workspace,
			name: workspace.name,
			basePath,
			basePathRegex: RegExp(`^${escapeRegExp(basePath)}(\\/|$)`, "i")
		};
	});
}
/**
* Reads the `history` pathname and responds to changes, returns matching workspace
*  @internal
*/
function useSyncPathnameWithWorkspace(history, _workspaces, workspaceAuthStates) {
	let $ = (0, import_compiler_runtime.c)(19), workspaces = useNormalizedWorkspaces(_workspaces), t0;
	$[0] === workspaces ? t0 = $[1] : (t0 = createCommonBasePathRegex(workspaces), $[0] = workspaces, $[1] = t0);
	let basePathRegex = t0, t1;
	$[2] === history.location.pathname ? t1 = $[3] : (t1 = () => history.location.pathname, $[2] = history.location.pathname, $[3] = t1);
	let [serverSnapshot] = (0, import_react.useState)(t1), t2;
	$[4] === history ? t2 = $[5] : (t2 = (onStoreChange) => history.listen(onStoreChange), $[4] = history, $[5] = t2);
	let t3;
	$[6] === history.location.pathname ? t3 = $[7] : (t3 = () => history.location.pathname, $[6] = history.location.pathname, $[7] = t3);
	let t4;
	$[8] === serverSnapshot ? t4 = $[9] : (t4 = () => serverSnapshot, $[8] = serverSnapshot, $[9] = t4);
	let t5;
	$[10] !== basePathRegex || $[11] !== workspaceAuthStates || $[12] !== workspaces ? (t5 = (pathname) => matchWorkspace({
		basePathRegex,
		pathname,
		workspaces,
		workspaceAuthStates
	}), $[10] = basePathRegex, $[11] = workspaceAuthStates, $[12] = workspaces, $[13] = t5) : t5 = $[13];
	let t6;
	$[14] !== t2 || $[15] !== t3 || $[16] !== t4 || $[17] !== t5 ? (t6 = {
		subscribe: t2,
		getSnapshot: t3,
		getServerSnapshot: t4,
		selector: t5,
		isEqual: _temp$34
	}, $[14] = t2, $[15] = t3, $[16] = t4, $[17] = t5, $[18] = t6) : t6 = $[18];
	let store = t6;
	return (0, import_with_selector.useSyncExternalStoreWithSelector)(store.subscribe, store.getSnapshot, store.getServerSnapshot, store.selector, store.isEqual);
}
function _temp$34(a, b) {
	if (a.type !== b.type) return !1;
	switch (a.type) {
		case "match": return a.workspace === b.workspace;
		case "redirect": return a.pathname === b.pathname;
		case "not-found":
		case "loading": return !0;
		default: throw Error(`Unknown type: ${a.type}`);
	}
}
var createHistory = () => typeof document > "u" ? createMemoryHistory() : createBrowserHistory();
/** @internal */
function ActiveWorkspaceMatcher(t0) {
	let $ = (0, import_compiler_runtime.c)(29), { children, LoadingComponent, NotFoundComponent, unstable_history: historyProp } = t0, allWorkspaces = useWorkspaces(), { visibleWorkspaces, workspaceAuthStates } = useVisibleWorkspaces(), t1;
	$[0] === historyProp ? t1 = $[1] : (t1 = historyProp || createHistory(), $[0] = historyProp, $[1] = t1);
	let history = t1, t2;
	$[2] !== history || $[3] !== visibleWorkspaces ? (t2 = (workspaceName) => {
		let foundWorkspace = visibleWorkspaces.find((workspace) => workspace.name === workspaceName);
		foundWorkspace && history.push(foundWorkspace.basePath);
	}, $[2] = history, $[3] = visibleWorkspaces, $[4] = t2) : t2 = $[4];
	let setActiveWorkspaceName = t2, t3;
	$[5] === history ? t3 = $[6] : (t3 = () => {
		history.push("/");
	}, $[5] = history, $[6] = t3);
	let handleNavigateToDefaultWorkspace = t3, result = useSyncPathnameWithWorkspace(history, allWorkspaces, workspaceAuthStates), t4;
	$[7] !== history || $[8] !== result.pathname || $[9] !== result.type ? (t4 = () => {
		result.type === "redirect" && history.replace({
			pathname: result.pathname,
			search: history.location.search,
			hash: history.location.hash
		});
	}, $[7] = history, $[8] = result.pathname, $[9] = result.type, $[10] = t4) : t4 = $[10];
	let t5;
	switch ($[11] !== history || $[12] !== result ? (t5 = [history, result], $[11] = history, $[12] = result, $[13] = t5) : t5 = $[13], (0, import_react.useEffect)(t4, t5), result.type) {
		case "match": {
			let matchedWorkspace = result.workspace;
			if (typeof matchedWorkspace.hidden == "function" && workspaceAuthStates[matchedWorkspace.name] === void 0) {
				let t6;
				return $[14] === LoadingComponent ? t6 = $[15] : (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingComponent, {}), $[14] = LoadingComponent, $[15] = t6), t6;
			}
			if (!visibleWorkspaces.some((workspace_0) => workspace_0.name === matchedWorkspace.name)) {
				let t6;
				return $[16] !== NotFoundComponent || $[17] !== handleNavigateToDefaultWorkspace ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NotFoundComponent, { onNavigateToDefaultWorkspace: handleNavigateToDefaultWorkspace }), $[16] = NotFoundComponent, $[17] = handleNavigateToDefaultWorkspace, $[18] = t6) : t6 = $[18], t6;
			}
			let t6;
			return $[19] !== children || $[20] !== history || $[21] !== matchedWorkspace || $[22] !== setActiveWorkspaceName ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActiveWorkspaceMatcherProvider, {
				activeWorkspace: matchedWorkspace,
				history,
				setActiveWorkspace: setActiveWorkspaceName,
				children
			}), $[19] = children, $[20] = history, $[21] = matchedWorkspace, $[22] = setActiveWorkspaceName, $[23] = t6) : t6 = $[23], t6;
		}
		case "redirect":
		case "loading": {
			let t6;
			return $[24] === LoadingComponent ? t6 = $[25] : (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingComponent, {}), $[24] = LoadingComponent, $[25] = t6), t6;
		}
		case "not-found": {
			let t6;
			return $[26] !== NotFoundComponent || $[27] !== handleNavigateToDefaultWorkspace ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NotFoundComponent, { onNavigateToDefaultWorkspace: handleNavigateToDefaultWorkspace }), $[26] = NotFoundComponent, $[27] = handleNavigateToDefaultWorkspace, $[28] = t6) : t6 = $[28], t6;
		}
		default: throw Error(`Unknown type: ${result.type}`);
	}
}
/**
* @internal
*/
function SearchButton(t0) {
	let $ = (0, import_compiler_runtime.c)(12), { ref, onClick } = t0, { t } = useTranslation$1(), t1;
	$[0] === t ? t1 = $[1] : (t1 = t("search.action-open-aria-label"), $[0] = t, $[1] = t1);
	let t2;
	$[2] === t ? t2 = $[3] : (t2 = t("search.button.tooltip"), $[2] = t, $[3] = t2);
	let t3;
	$[4] === Symbol.for("react.memo_cache_sentinel") ? (t3 = [GLOBAL_SEARCH_KEY_MODIFIER, "K"], $[4] = t3) : t3 = $[4];
	let t4;
	$[5] === t2 ? t4 = $[6] : (t4 = {
		content: t2,
		hotkeys: t3,
		portal: !0
	}, $[5] = t2, $[6] = t4);
	let t5;
	return $[7] !== onClick || $[8] !== ref || $[9] !== t1 || $[10] !== t4 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		"aria-label": t1,
		"data-testid": "studio-search",
		icon: SearchIcon,
		tooltipProps: t4,
		onClick,
		mode: "bleed",
		ref
	}), $[7] = onClick, $[8] = ref, $[9] = t1, $[10] = t4, $[11] = t5) : t5 = $[11], t5;
}
var InnerCard = gt(Card).withConfig({
	displayName: "InnerCard",
	componentId: "sc-qijkr4-0"
})`flex-direction:column;overflow:hidden;overflow:clip;pointer-events:all;position:relative;`;
var SearchDialogBox = gt(Box).withConfig({
	displayName: "SearchDialogBox",
	componentId: "sc-qijkr4-1"
})`height:100%;left:0;overflow:hidden;overflow:clip;pointer-events:none;position:fixed;top:0;width:100%;z-index:1;`;
/**
* @internal
*/
function SearchDialog(t0) {
	let $ = (0, import_compiler_runtime.c)(17), { onClose, onOpen, open, previewPerspective, previewVariant } = t0, [inputElement, setInputElement] = (0, import_react.useState)(null), scheme = useColorSchemeValue(), { state: t1 } = useSearchState(), { filtersVisible, terms } = t1, t2;
	$[0] === terms ? t2 = $[1] : (t2 = hasSearchableTerms({ terms }), $[0] = terms, $[1] = t2);
	let hasValidTerms = t2, t3;
	$[2] !== filtersVisible || $[3] !== hasValidTerms || $[4] !== inputElement || $[5] !== onClose || $[6] !== open || $[7] !== previewPerspective || $[8] !== previewVariant || $[9] !== scheme ? (t3 = open && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Portal, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(es2015_default, {
		autoFocus: !supportsTouch,
		returnFocus: !0,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SearchDialogBox, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(InnerCard, {
			display: "flex",
			height: "fill",
			scheme,
			tone: "default",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SearchHeader, {
					onClose,
					ref: setInputElement
				}),
				filtersVisible && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
					borderTop: !0,
					flex: "none",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Filters, {})
				}),
				hasValidTerms ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SearchResults, {
					inputElement,
					previewPerspective,
					previewVariant
				}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RecentSearches, { inputElement })
			]
		}) })
	}) }), $[2] = filtersVisible, $[3] = hasValidTerms, $[4] = inputElement, $[5] = onClose, $[6] = open, $[7] = previewPerspective, $[8] = previewVariant, $[9] = scheme, $[10] = t3) : t3 = $[10];
	let t4;
	return $[11] !== hasValidTerms || $[12] !== onClose || $[13] !== onOpen || $[14] !== open || $[15] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SearchWrapper, {
		hasValidTerms,
		onClose,
		onOpen,
		open,
		children: t3
	}), $[11] = hasValidTerms, $[12] = onClose, $[13] = onOpen, $[14] = open, $[15] = t3, $[16] = t4) : t4 = $[16], t4;
}
/**
* @hidden
* @beta */
function StudioLogo(props) {
	let $ = (0, import_compiler_runtime.c)(2), { title } = props, t0;
	return $[0] === title ? t0 = $[1] : (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		padding: 3,
		"data-testid": "studio-logo",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			weight: "medium",
			children: title
		})
	}), $[0] = title, $[1] = t0), t0;
}
function StudioActiveToolLayout(props) {
	let $ = (0, import_compiler_runtime.c)(3), { activeTool } = props, Component = activeTool.component, t0;
	return $[0] !== Component || $[1] !== activeTool ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Component, { tool: activeTool }), $[0] = Component, $[1] = activeTool, $[2] = t0) : t0 = $[2], t0;
}
function _isReactElement(node) {
	return !!node;
}
var OptionObserveElement = gt(ObserveElement).withConfig({
	displayName: "OptionObserveElement",
	componentId: "sc-lwsqts-0"
})`list-style:none;white-space:nowrap;flex-shrink:0;opacity:0;visibility:hidden;`;
var HiddenRow = gt(Flex).withConfig({
	displayName: "HiddenRow",
	componentId: "sc-lwsqts-1"
})`opacity:0;height:0.1px;overflow:hidden;`;
/**
* Similar to `<CollapseMenu />` but instead of collapsing the inner items by removing the text
* it shows the items that fit, and the rest are rendered in a menu.
* @internal */
function CollapseTabList(props) {
	let $ = (0, import_compiler_runtime.c)(56), childrenProp, collapsed, disableRestoreFocusOnClose, gap, menuButtonProps, onMenuClose, ref, rest;
	$[0] === props ? (childrenProp = $[1], collapsed = $[2], disableRestoreFocusOnClose = $[3], gap = $[4], menuButtonProps = $[5], onMenuClose = $[6], ref = $[7], rest = $[8]) : ({ref, children: childrenProp, gap, menuButtonProps, disableRestoreFocusOnClose, onMenuClose, collapsed, ...rest} = props, $[0] = props, $[1] = childrenProp, $[2] = collapsed, $[3] = disableRestoreFocusOnClose, $[4] = gap, $[5] = menuButtonProps, $[6] = onMenuClose, $[7] = ref, $[8] = rest);
	let [rootEl, setRootEl] = (0, import_react.useState)(null), t0;
	$[9] === Symbol.for("react.memo_cache_sentinel") ? (t0 = [], $[9] = t0) : t0 = $[9];
	let [hiddenElements, setHiddenElements] = (0, import_react.useState)(t0), [showChildren, setShowChildren] = (0, import_react.useState)(!1), t1;
	$[10] === childrenProp ? t1 = $[11] : (t1 = import_react.Children.toArray(childrenProp).filter(_isReactElement), $[10] = childrenProp, $[11] = t1);
	let children = t1, t2;
	bb0: {
		if (collapsed) {
			t2 = null;
			break bb0;
		}
		if (!showChildren) {
			t2 = null;
			break bb0;
		}
		let t3;
		if ($[12] !== children || $[13] !== hiddenElements) {
			let t4;
			$[15] === hiddenElements ? t4 = $[16] : (t4 = (c) => !hiddenElements.some((h) => h.key === c.key), $[15] = hiddenElements, $[16] = t4), t3 = children.filter(t4), $[12] = children, $[13] = hiddenElements, $[14] = t3;
		} else t3 = $[14];
		t2 = t3;
	}
	let displayChildren = t2, t3;
	$[17] === rootEl ? t3 = $[18] : (t3 = {
		root: rootEl,
		threshold: 1,
		rootMargin: "1px"
	}, $[17] = rootEl, $[18] = t3);
	let intersectionOptions = t3, t4;
	$[19] === menuButtonProps?.button ? t4 = $[20] : (t4 = menuButtonProps?.button || /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ContextMenuButton, {}), $[19] = menuButtonProps?.button, $[20] = t4);
	let menuButton = t4, t5;
	$[21] !== children || $[22] !== collapsed || $[23] !== hiddenElements ? (t5 = collapsed ? children : children.filter((t6) => {
		let { key } = t6;
		return hiddenElements.find((o) => o.key === key);
	}), $[21] = children, $[22] = collapsed, $[23] = hiddenElements, $[24] = t5) : t5 = $[24];
	let menuOptionsArray = t5, t6;
	$[25] !== hiddenElements || $[26] !== showChildren ? (t6 = (e, child) => {
		let isHidden = hiddenElements.some((el) => el.key === child.key);
		showChildren || setShowChildren(!0);
		let isIntersecting = e.isIntersecting;
		!isHidden && !isIntersecting && setHiddenElements((prev) => [...prev, child]), isHidden && isIntersecting && setHiddenElements((prev_0) => prev_0.filter((el_0) => el_0.key !== child.key));
	}, $[25] = hiddenElements, $[26] = showChildren, $[27] = t6) : t6 = $[27];
	let handleIntersection = t6, t7;
	$[28] === Symbol.for("react.memo_cache_sentinel") ? (t7 = { position: "relative" }, $[28] = t7) : t7 = $[28];
	let t8;
	$[29] !== collapsed || $[30] !== disableRestoreFocusOnClose || $[31] !== hiddenElements.length || $[32] !== menuButton || $[33] !== menuButtonProps || $[34] !== menuOptionsArray || $[35] !== onMenuClose ? (t8 = (hiddenElements.length > 0 || collapsed) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CollapseOverflowMenu, {
		disableRestoreFocusOnClose,
		menuButton,
		menuButtonProps,
		menuOptions: menuOptionsArray,
		onMenuClose
	}), $[29] = collapsed, $[30] = disableRestoreFocusOnClose, $[31] = hiddenElements.length, $[32] = menuButton, $[33] = menuButtonProps, $[34] = menuOptionsArray, $[35] = onMenuClose, $[36] = t8) : t8 = $[36];
	let t9;
	$[37] !== displayChildren || $[38] !== gap || $[39] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		justify: "center",
		gap,
		flex: 1,
		children: [displayChildren, t8]
	}), $[37] = displayChildren, $[38] = gap, $[39] = t8, $[40] = t9) : t9 = $[40];
	let t10;
	$[41] === menuButton ? t10 = $[42] : (t10 = (0, import_react.cloneElement)(menuButton, {
		disabled: !0,
		"aria-hidden": !0
	}), $[41] = menuButton, $[42] = t10);
	let t11;
	$[43] !== children || $[44] !== handleIntersection || $[45] !== intersectionOptions ? (t11 = children?.map((child_0) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(OptionObserveElement, {
		options: intersectionOptions,
		onIntersectionChange: (e_0) => handleIntersection(e_0[0], child_0),
		children: (0, import_react.cloneElement)(child_0, {
			disabled: !0,
			"aria-hidden": !0,
			tabIndex: -1
		})
	}, `${child_0.key}_observer`)), $[43] = children, $[44] = handleIntersection, $[45] = intersectionOptions, $[46] = t11) : t11 = $[46];
	let t12;
	$[47] !== gap || $[48] !== t10 || $[49] !== t11 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(HiddenRow, {
		justify: "flex-start",
		gap,
		ref: setRootEl,
		"data-hidden": !0,
		"aria-hidden": "true",
		children: [t10, t11]
	}), $[47] = gap, $[48] = t10, $[49] = t11, $[50] = t12) : t12 = $[50];
	let t13;
	return $[51] !== ref || $[52] !== rest || $[53] !== t12 || $[54] !== t9 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		direction: "column",
		ref,
		sizing: "border",
		style: t7,
		...rest,
		children: [t9, t12]
	}), $[51] = ref, $[52] = rest, $[53] = t12, $[54] = t9, $[55] = t13) : t13 = $[55], t13;
}
function ToolCollapseMenu(props) {
	let $ = (0, import_compiler_runtime.c)(13), { activeToolName, tools } = props, scheme = useColorSchemeValue(), [collapseMenuEl, setCollapseMenuEl] = (0, import_react.useState)(null), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = ["arrows"], $[0] = t0) : t0 = $[0];
	let t1;
	$[1] === collapseMenuEl ? t1 = $[2] : (t1 = {
		rootElement: collapseMenuEl,
		navigation: t0
	}, $[1] = collapseMenuEl, $[2] = t1), useRovingFocus(t1);
	let t2;
	$[3] === scheme ? t2 = $[4] : (t2 = { popover: {
		constrainSize: !0,
		portal: !0,
		scheme,
		tone: "default"
	} }, $[3] = scheme, $[4] = t2);
	let menuButtonProps = t2, t3;
	if ($[5] !== activeToolName || $[6] !== tools) {
		let t4;
		$[8] === activeToolName ? t4 = $[9] : (t4 = (tool, index) => {
			let title = tool?.title || startCase(tool.name);
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
				as: function Link(linkProps) {
					let { ref, ...rest } = linkProps;
					return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolLink, {
						...rest,
						ref,
						name: tool.name,
						children: linkProps.children
					});
				},
				"data-as": "a",
				mode: "bleed",
				selected: activeToolName === tool.name,
				text: title
			}, `${tool.name}-${index}`);
		}, $[8] = activeToolName, $[9] = t4), t3 = tools.map(t4), $[5] = activeToolName, $[6] = tools, $[7] = t3;
	} else t3 = $[7];
	let children = t3, t4;
	return $[10] !== children || $[11] !== menuButtonProps ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		justify: "center",
		marginX: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CollapseTabList, {
			"data-testid": "tool-collapse-menu",
			gap: 1,
			menuButtonProps,
			ref: setCollapseMenuEl,
			children
		})
	}), $[10] = children, $[11] = menuButtonProps, $[12] = t4) : t4 = $[12], t4;
}
function ToolVerticalMenu(props) {
	let $ = (0, import_compiler_runtime.c)(9), { activeToolName, isVisible, tools } = props, t0;
	if ($[0] !== activeToolName || $[1] !== isVisible || $[2] !== tools) {
		let t1;
		$[4] !== activeToolName || $[5] !== isVisible ? (t1 = (tool) => {
			let title = tool?.title || startCase(tool.name);
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
				as: "li",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
					as: function Link(linkProps) {
						let { ref, ...rest } = linkProps;
						return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolLink, {
							...rest,
							ref,
							name: tool.name,
							children: linkProps.children
						});
					},
					justify: "flex-start",
					mode: "bleed",
					selected: activeToolName === tool.name,
					size: "large",
					tabIndex: isVisible ? 0 : -1,
					text: title
				})
			}, tool.name);
		}, $[4] = activeToolName, $[5] = isVisible, $[6] = t1) : t1 = $[6], t0 = tools.map(t1), $[0] = activeToolName, $[1] = isVisible, $[2] = tools, $[3] = t0;
	} else t0 = $[3];
	let t1;
	return $[7] === t0 ? t1 = $[8] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		as: "ul",
		gap: 1,
		children: t0
	}), $[7] = t0, $[8] = t1), t1;
}
/**
* @hidden
* @beta */
function StudioToolMenu(props) {
	let $ = (0, import_compiler_runtime.c)(17), context, isSidebarOpen, restProps, tools;
	$[0] === props ? (context = $[1], isSidebarOpen = $[2], restProps = $[3], tools = $[4]) : ({context, isSidebarOpen, tools, ...restProps} = props, $[0] = props, $[1] = context, $[2] = isSidebarOpen, $[3] = restProps, $[4] = tools);
	let { enabled: scheduledPublishingEnabled } = useScheduledPublishingEnabled(), t0;
	if ($[5] !== scheduledPublishingEnabled || $[6] !== tools) {
		let t1;
		$[8] === scheduledPublishingEnabled ? t1 = $[9] : (t1 = (tool) => !(tool.name === "schedules" && !scheduledPublishingEnabled), $[8] = scheduledPublishingEnabled, $[9] = t1), t0 = tools.filter(t1), $[5] = scheduledPublishingEnabled, $[6] = tools, $[7] = t0;
	} else t0 = $[7];
	let visibleTools = t0;
	if (visibleTools.length <= 1) return null;
	if (context === "sidebar") {
		let t1;
		return $[10] !== isSidebarOpen || $[11] !== restProps || $[12] !== visibleTools ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolVerticalMenu, {
			isVisible: isSidebarOpen,
			tools: visibleTools,
			...restProps
		}), $[10] = isSidebarOpen, $[11] = restProps, $[12] = visibleTools, $[13] = t1) : t1 = $[13], t1;
	}
	let t1;
	return $[14] !== restProps || $[15] !== visibleTools ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolCollapseMenu, {
		tools: visibleTools,
		...restProps
	}), $[14] = restProps, $[15] = visibleTools, $[16] = t1) : t1 = $[16], t1;
}
function DocumentLimitsUpsellPanel() {
	let $ = (0, import_compiler_runtime.c)(12), { upsellData, telemetryLogs } = useDocumentLimitsUpsellContext(), t0;
	$[0] === telemetryLogs ? t0 = $[1] : (t0 = () => {
		telemetryLogs.panelPrimaryClicked();
	}, $[0] = telemetryLogs, $[1] = t0);
	let handlePrimaryButtonClick = t0, t1;
	$[2] === telemetryLogs ? t1 = $[3] : (t1 = () => {
		telemetryLogs.panelSecondaryClicked();
	}, $[2] = telemetryLogs, $[3] = t1);
	let handleSecondaryButtonClick = t1;
	if (!upsellData) {
		let t2;
		return $[4] === Symbol.for("react.memo_cache_sentinel") ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingBlock, {
			title: "Loading documents limit",
			showText: !0
		}), $[4] = t2) : t2 = $[4], t2;
	}
	let t2, t3, t4;
	$[5] === Symbol.for("react.memo_cache_sentinel") ? (t2 = {
		opacity: 0,
		scale: .95
	}, t3 = {
		opacity: 1,
		scale: 1
	}, t4 = { duration: .2 }, $[5] = t2, $[6] = t3, $[7] = t4) : (t2 = $[5], t3 = $[6], t4 = $[7]);
	let t5;
	return $[8] !== handlePrimaryButtonClick || $[9] !== handleSecondaryButtonClick || $[10] !== upsellData ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		height: "fill",
		justify: "center",
		align: "center",
		padding: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(motion.div, {
			initial: t2,
			animate: t3,
			transition: t4,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Container, {
				width: 0,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UpsellPanel, {
					data: upsellData,
					onPrimaryClick: handlePrimaryButtonClick,
					onSecondaryClick: handleSecondaryButtonClick
				})
			})
		})
	}), $[8] = handlePrimaryButtonClick, $[9] = handleSecondaryButtonClick, $[10] = upsellData, $[11] = t5) : t5 = $[11], t5;
}
var StudioAuthReadyMeasured = defineEvent({
	name: "Studio Auth Ready Measured",
	version: 2,
	description: "Time from browser navigation start to the moment Studio resolves the auth state (logged-in, logged-out, or unauthorized), with page-visibility context to distinguish foreground loads from backgrounded ones."
});
var StudioReadyMeasured = defineEvent({
	name: "Studio Ready Measured",
	version: 2,
	description: "Time from browser navigation start to the moment the active tool has first rendered and the Studio layout is interactive, with page-visibility context to distinguish foreground loads from backgrounded ones."
});
/**
* Checks whether a request to the API is using a legacy HTTP protocol (HTTP/0.9, HTTP/1.0, or HTTP/1.1).
* Can emit `undefined` if the protocol could not be detected.
*
* @param client - The client to use for the request. If not provided, a fetch request
* will be made to the fallback API URL. Prefer using a client, as it will have the
* correct URL configured.
* @returns An Observable that emits `true` if the API request is using a legacy HTTP
* protocol, `false` if it is not, or `undefined` if protocol could not be detected.
* @internal
*/
function isUsingLegacyHttp(client) {
	return getProtocolForApi(client).pipe(map((protocol) => {
		if (protocol) return protocol.startsWith("http/0") || protocol.startsWith("http/1");
	}));
}
/**
* Detects the protocol used for the API request by using the browser Performance API.
* This is useful for detecting whether the API request is using HTTP/2 or not.
*
* @param client - The client to use for the request. If not provided, a fetch request
* will be made to the fallback API URL. Prefer using a client, as it will have the
* correct URL configured.
* @returns An Observable that emits the protocol used for the API request, or `undefined`
* if it could not be detected.
* @internal
*/
function getProtocolForApi(client) {
	return typeof PerformanceObserver > "u" || typeof PerformanceResourceTiming > "u" ? of(void 0) : detectProtocol(`${client.getUrl("/ping")}?tag=sanity.studio.protocol-check`);
}
/**
* Creates an Observable that sets up a PerformanceObserver, issues the network request,
* and emits the `nextHopProtocol` once the resource timing shows up. If the request
* fails (e.g. the browser is offline or the request is blocked), the Observable emits
* `undefined` to indicate that the protocol could not be detected.
*
* @internal
*/
function detectProtocol(checkUrl) {
	let timingEntry = firstValueFrom(listenBufferedPerformanceEntries().pipe(filter((entry) => entry instanceof PerformanceResourceTiming && entry.name === checkUrl), take(1), map((entry) => localStorage._sanity_debugProtocol ?? entry.nextHopProtocol), timeout({
		first: 2500,
		with: () => of(void 0)
	})));
	return defer(() => fetch(checkUrl).then((res) => (res.body?.cancel(), res))).pipe(mergeMap(() => timingEntry), timeout({
		first: 2500,
		with: () => of(void 0)
	}), catchError((error) => (console.warn("[sanity] Could not detect network protocol:", error), of(void 0))));
}
function listenBufferedPerformanceEntries() {
	return new Observable((subscriber) => {
		let perfObserver = new PerformanceObserver((list) => {
			for (let entry of list.getEntries()) subscriber.next(entry);
		});
		return perfObserver.observe({
			type: "resource",
			buffered: !0
		}), () => perfObserver.disconnect();
	});
}
var HTTP_HELP_URL = generateHelpUrl("http1-performance-issues");
/**
* Checks the network protocol used to communicate with the Sanity API and shows a
* warning toast if it's not using a modern protocol (HTTP/2 or later).
*
* @internal
*/
function useNetworkProtocolCheck() {
	let $ = (0, import_compiler_runtime.c)(40), { t } = useTranslation$1(), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = { apiVersion: "2025-03-01" }, $[0] = t0) : t0 = $[0];
	let client = useClient(t0), t1;
	$[1] === t ? t1 = $[2] : (t1 = t("network-check.slow-protocol-warning.title"), $[1] = t, $[2] = t1);
	let title = t1, [warningSnoozedAtRaw, setWarningSnoozedAt] = useSessionStorageState("sanity-studio.network.check.snooze"), t2;
	$[3] === warningSnoozedAtRaw ? t2 = $[4] : (t2 = warningSnoozedAtRaw ? new Date(warningSnoozedAtRaw) : void 0, $[3] = warningSnoozedAtRaw, $[4] = t2);
	let warningDismissedAt = t2, t3;
	$[5] === warningDismissedAt ? t3 = $[6] : (t3 = warningDismissedAt && (/* @__PURE__ */ new Date()).getTime() - warningDismissedAt.getTime() < 864e5, $[5] = warningDismissedAt, $[6] = t3);
	let isWarningSnoozed = t3, t4;
	$[7] !== client || $[8] !== isWarningSnoozed ? (t4 = isWarningSnoozed ? of(void 0) : isUsingLegacyHttp(client), $[7] = client, $[8] = isWarningSnoozed, $[9] = t4) : t4 = $[9];
	let isOnLegacyHttp = useObservable(t4), t5;
	$[10] === setWarningSnoozedAt ? t5 = $[11] : (t5 = () => setWarningSnoozedAt((/* @__PURE__ */ new Date()).toISOString()), $[10] = setWarningSnoozedAt, $[11] = t5);
	let handleSnooze = t5, t6 = !!(isOnLegacyHttp && !isWarningSnoozed), t7;
	$[12] === t ? t7 = $[13] : (t7 = t("network-check.slow-protocol-warning.description"), $[12] = t, $[13] = t7);
	let t8;
	$[14] === t7 ? t8 = $[15] : (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
		size: 1,
		children: [t7, " "]
	}) }), $[14] = t7, $[15] = t8);
	let t9;
	$[16] === t ? t9 = $[17] : (t9 = t("network-check.slow-protocol-warning.learn-more-button.tooltip"), $[16] = t, $[17] = t9);
	let t10;
	$[18] === t9 ? t10 = $[19] : (t10 = { content: t9 }, $[18] = t9, $[19] = t10);
	let t11;
	$[20] === t ? t11 = $[21] : (t11 = t("network-check.slow-protocol-warning.learn-more-button.text"), $[20] = t, $[21] = t11);
	let t12;
	$[22] !== t10 || $[23] !== t11 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		as: "a",
		href: HTTP_HELP_URL,
		target: "_blank",
		rel: "noreferrer",
		mode: "ghost",
		tone: "primary",
		size: "default",
		icon: LaunchIcon,
		tooltipProps: t10,
		text: t11
	}), $[22] = t10, $[23] = t11, $[24] = t12) : t12 = $[24];
	let t13;
	$[25] === t ? t13 = $[26] : (t13 = t("network-check.slow-protocol-warning.snooze-button.text"), $[25] = t, $[26] = t13);
	let t14;
	$[27] !== handleSnooze || $[28] !== t13 ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		mode: "bleed",
		tone: "neutral",
		size: "default",
		text: t13,
		onClick: handleSnooze
	}), $[27] = handleSnooze, $[28] = t13, $[29] = t14) : t14 = $[29];
	let t15;
	$[30] !== t12 || $[31] !== t14 ? (t15 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		gap: 3,
		children: [t12, t14]
	}), $[30] = t12, $[31] = t14, $[32] = t15) : t15 = $[32];
	let t16;
	$[33] !== t15 || $[34] !== t8 ? (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 4,
		paddingY: 1,
		children: [t8, t15]
	}), $[33] = t15, $[34] = t8, $[35] = t16) : t16 = $[35];
	let t17;
	$[36] !== t16 || $[37] !== t6 || $[38] !== title ? (t17 = {
		id: "network-protocol-check",
		status: "warning",
		enabled: t6,
		title,
		description: t16
	}, $[36] = t16, $[37] = t6, $[38] = title, $[39] = t17) : t17 = $[39], useConditionalToast(t17);
}
function useSessionStorageState(key) {
	let $ = (0, import_compiler_runtime.c)(7), t0;
	$[0] === key ? t0 = $[1] : (t0 = () => {
		let stored = sessionStorage.getItem(key);
		return typeof stored == "string" ? stored : void 0;
	}, $[0] = key, $[1] = t0);
	let [state, setState] = (0, import_react.useState)(t0), t1;
	$[2] === key ? t1 = $[3] : (t1 = (value) => {
		setState(value), value === void 0 ? sessionStorage.removeItem(key) : sessionStorage.setItem(key, value);
	}, $[2] = key, $[3] = t1);
	let setValue = t1, t2 = typeof state == "string" ? state : void 0, t3;
	return $[4] !== setValue || $[5] !== t2 ? (t3 = [t2, setValue], $[4] = setValue, $[5] = t2, $[6] = t3) : t3 = $[6], t3;
}
function NoToolsScreen() {
	let $ = (0, import_compiler_runtime.c)(4), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WarningOutlineIcon, {})
	}) }), $[0] = t0) : t0 = $[0];
	let t1, t2;
	$[1] === Symbol.for("react.memo_cache_sentinel") ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "h1",
		size: 1,
		weight: "medium",
		children: "No configured tools"
	}), t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "p",
		muted: !0,
		size: 1,
		children: "Please configure a tool in your Studio configuration."
	}), $[1] = t1, $[2] = t2) : (t1 = $[1], t2 = $[2]);
	let t3;
	return $[3] === Symbol.for("react.memo_cache_sentinel") ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		height: "fill",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
			align: "center",
			height: "fill",
			justify: "center",
			padding: 4,
			sizing: "border",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Container, {
				width: 0,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
					padding: 4,
					radius: 2,
					shadow: 1,
					tone: "caution",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, { children: [t0, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
						flex: 1,
						marginLeft: 3,
						gap: 3,
						children: [
							t1,
							t2,
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
								as: "p",
								muted: !0,
								size: 1,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
									href: "https://www.sanity.io/docs/studio-tools",
									rel: "noreferrer",
									target: "_blank",
									children: "Learn how to add a tool →"
								})
							})
						]
					})] })
				})
			})
		})
	}), $[3] = t3) : t3 = $[3], t3;
}
function RedirectingScreen(props) {
	let $ = (0, import_compiler_runtime.c)(3), { reason: t0 } = props, reason = t0 === void 0 ? "Redirecting…" : t0, t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DoubleChevronRightIcon, {})
	}) }), $[0] = t1) : t1 = $[0];
	let t2;
	return $[1] === reason ? t2 = $[2] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		height: "fill",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
			align: "center",
			height: "fill",
			justify: "center",
			padding: 4,
			sizing: "border",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Container, {
				width: 0,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
					padding: 4,
					radius: 2,
					shadow: 1,
					tone: "primary",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, { children: [t1, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
						flex: 1,
						marginLeft: 3,
						gap: 3,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
							as: "h1",
							size: 1,
							weight: "bold",
							children: reason
						})
					})] })
				})
			})
		})
	}), $[1] = reason, $[2] = t2), t2;
}
function ToolNotFoundScreen(props) {
	let $ = (0, import_compiler_runtime.c)(3), { toolName } = props, t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WarningOutlineIcon, {})
	}) }), $[0] = t0) : t0 = $[0];
	let t1;
	return $[1] === toolName ? t1 = $[2] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		height: "fill",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
			align: "center",
			height: "fill",
			justify: "center",
			padding: 4,
			sizing: "border",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Container, {
				width: 0,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
					padding: 4,
					radius: 2,
					shadow: 1,
					tone: "caution",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, { children: [t0, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
						flex: 1,
						marginLeft: 3,
						gap: 3,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
							as: "h1",
							size: 1,
							weight: "medium",
							children: ["Tool not found: ", /* @__PURE__ */ (0, import_jsx_runtime.jsx)("code", { children: toolName })]
						})
					})] })
				})
			})
		})
	}), $[1] = toolName, $[2] = t1), t1;
}
/**
* Singleton instance of an error reporter, that will send errors encountered during execution or
* rendering to Sanity (potentially to a third party error tracking service).
*
* @internal
*/
var errorReporter = getSentryErrorReporter();
var MESSAGES = [
	"Failed to fetch dynamically imported module:",
	"Importing a module script failed.",
	"error loading dynamically imported module"
];
/**
* Checks whether an error is caused by a failed dynamic import
* Note: this is best effort, based on matching the error string and should not be trusted for critical stuff
* @param error - {@link Error} the error to test
*/
function isImportError(error) {
	return error.name === "TypeError" && MESSAGES.some((msg) => error.message.toLowerCase().startsWith(msg.toLowerCase()));
}
var View$1 = gt(Box).withConfig({
	displayName: "View",
	componentId: "sc-blgxse-0"
})`align-items:center;`;
function FallbackErrorScreen(props) {
	let $ = (0, import_compiler_runtime.c)(27), { error, eventId, heading: t0, onReset } = props, heading = t0 === void 0 ? "An error occurred" : t0, message = error?.message, stack = typeof error?.stack == "string" && error?.stack, t1;
	$[0] === error ? t1 = $[1] : (t1 = isClientRequestError(error), $[0] = error, $[1] = t1);
	let showRequestErrorTip = t1, t2 = message || heading, t3;
	$[2] === Symbol.for("react.memo_cache_sentinel") ? (t3 = [
		4,
		5,
		6,
		7
	], $[2] = t3) : t3 = $[2];
	let t4;
	$[3] === heading ? t4 = $[4] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Heading, { children: heading }), $[3] = heading, $[4] = t4);
	let t5, t6;
	$[5] === Symbol.for("react.memo_cache_sentinel") ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, { children: "An error occurred that Sanity Studio was unable to recover from." }), t6 = false, $[5] = t5, $[6] = t6) : (t5 = $[5], t6 = $[6]);
	let t7;
	$[7] !== eventId || $[8] !== message || $[9] !== stack ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		border: !0,
		radius: 2,
		overflow: "auto",
		padding: 4,
		tone: "critical",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 4,
			children: [
				message && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Code, {
					weight: "bold",
					size: 1,
					children: message
				}),
				stack && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Code, {
					size: 1,
					children: stack
				}),
				eventId && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Code, {
					size: 1,
					children: ["Event ID: ", eventId]
				})
			]
		})
	}), $[7] = eventId, $[8] = message, $[9] = stack, $[10] = t7) : t7 = $[10];
	let t8;
	$[11] === showRequestErrorTip ? t8 = $[12] : (t8 = showRequestErrorTip && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		border: !0,
		radius: 2,
		padding: 4,
		tone: "caution",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 3,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
					size: 1,
					weight: "medium",
					children: "Developer tip"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
					size: 1,
					muted: !0,
					children: "Request errors that the Studio can recover from (network failures, 5xx, 429, session expiry) shouldn't reach this screen. Handle the error where you issue the request, or delegate it to the Studio's built-in error UI:"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
					border: !0,
					radius: 1,
					overflow: "auto",
					padding: 3,
					tone: "transparent",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Code, {
						size: 1,
						children: "const {attempt, handle} = useStudioErrorHandler()\n\n// retryable read — the dialog offers \"Try again\"\nconst data = await attempt(() => client.fetch(query), {retryable: true})\n\n// or fire-and-surface in a .catch\nclient.create(doc).catch(handle)"
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
					size: 1,
					muted: !0,
					children: "This tip is only shown in development."
				})
			]
		})
	}), $[11] = showRequestErrorTip, $[12] = t8);
	let t9;
	$[13] !== t4 || $[14] !== t7 || $[15] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 4,
		children: [
			t4,
			t5,
			t6,
			t7,
			t8
		]
	}), $[13] = t4, $[14] = t7, $[15] = t8, $[16] = t9) : t9 = $[16];
	let t10;
	$[17] !== error || $[18] !== eventId || $[19] !== onReset ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorActions, {
		error,
		eventId,
		onRetry: onReset,
		size: "large"
	}), $[17] = error, $[18] = eventId, $[19] = onReset, $[20] = t10) : t10 = $[20];
	let t11;
	$[21] !== t10 || $[22] !== t9 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(View$1, {
		display: "flex",
		height: "fill",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Container, {
			width: 3,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
				gap: 6,
				children: [t9, t10]
			})
		})
	}), $[21] = t10, $[22] = t9, $[23] = t11) : t11 = $[23];
	let t12;
	return $[24] !== t11 || $[25] !== t2 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		"data-testid": "studio-error-screen",
		"data-error": t2,
		height: "fill",
		overflow: "auto",
		paddingY: t3,
		paddingX: 4,
		sizing: "border",
		children: t11
	}), $[24] = t11, $[25] = t2, $[26] = t12) : t12 = $[26], t12;
}
var View = gt(Box).withConfig({
	displayName: "View",
	componentId: "sc-h673uk-0"
})`align-items:center;`;
function reloadPage() {
	window.location.reload();
}
function ImportErrorScreen(props) {
	let $ = (0, import_compiler_runtime.c)(25), { error, eventId, autoReload } = props, t0;
	$[0] === autoReload ? t0 = $[1] : (t0 = autoReload ? timer(0, 1e3).pipe(take(6), map(_temp$33)) : of(0), $[0] = autoReload, $[1] = t0);
	let countdownSeconds = useSyncObservable(t0, 5), t1, t2;
	$[2] !== autoReload || $[3] !== countdownSeconds ? (t1 = () => {
		autoReload && countdownSeconds < 1 && reloadPage();
	}, t2 = [autoReload, countdownSeconds], $[2] = autoReload, $[3] = countdownSeconds, $[4] = t1, $[5] = t2) : (t1 = $[4], t2 = $[5]), (0, import_react.useEffect)(t1, t2);
	let t3 = error.message || "Import error", t4;
	$[6] === Symbol.for("react.memo_cache_sentinel") ? (t4 = [
		4,
		5,
		6,
		7
	], $[6] = t4) : t4 = $[6];
	let t5, t6;
	$[7] === Symbol.for("react.memo_cache_sentinel") ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Heading, { children: "Import error" }), t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, { children: "An error occurred during dynamic import." }), $[7] = t5, $[8] = t6) : (t5 = $[7], t6 = $[8]);
	let t7;
	$[9] !== error.message || $[10] !== error.stack || $[11] !== eventId ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		border: !0,
		radius: 2,
		overflow: "auto",
		padding: 4,
		tone: "critical",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 4,
			children: [
				error.message && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Code, {
					weight: "bold",
					size: 1,
					children: error.message
				}),
				error.stack && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Code, {
					size: 1,
					children: error.stack
				}),
				eventId && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Code, {
					size: 1,
					children: ["Event ID: ", eventId]
				})
			]
		})
	}), $[9] = error.message, $[10] = error.stack, $[11] = eventId, $[12] = t7) : t7 = $[12];
	let t8;
	$[13] !== autoReload || $[14] !== countdownSeconds ? (t8 = autoReload ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
		muted: !0,
		children: [
			"Reloading ",
			countdownSeconds <= 0 ? "now" : `in ${countdownSeconds}s`,
			"…"
		]
	}) : null, $[13] = autoReload, $[14] = countdownSeconds, $[15] = t8) : t8 = $[15];
	let t9 = autoReload ? "Reload now" : "Reload", t10;
	$[16] === t9 ? t10 = $[17] : (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Inline, {
		gap: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			onClick: reloadPage,
			text: t9,
			tone: "primary",
			icon: SyncIcon,
			size: "large"
		})
	}), $[16] = t9, $[17] = t10);
	let t11;
	$[18] !== t10 || $[19] !== t7 || $[20] !== t8 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(View, {
		display: "flex",
		height: "fill",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Container, {
			width: 3,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
				gap: 6,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
					gap: 4,
					children: [
						t5,
						t6,
						t7,
						t8,
						t10
					]
				})
			})
		})
	}), $[18] = t10, $[19] = t7, $[20] = t8, $[21] = t11) : t11 = $[21];
	let t12;
	return $[22] !== t11 || $[23] !== t3 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		"data-testid": "studio-error-screen",
		"data-error": t3,
		height: "fill",
		overflow: "auto",
		paddingY: t4,
		paddingX: 4,
		sizing: "border",
		children: t11
	}), $[22] = t11, $[23] = t3, $[24] = t12) : t12 = $[24], t12;
}
function _temp$33(seconds) {
	return 5 - seconds;
}
function useCopyToClipboard() {
	let $ = (0, import_compiler_runtime.c)(3), [copiedText, setCopiedText] = (0, import_react.useState)(null), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = async (text) => {
		if (!navigator?.clipboard) return console.warn("Clipboard not supported"), !1;
		try {
			return await navigator.clipboard.writeText(text), setCopiedText(text), !0;
		} catch (t1) {
			return console.warn("Copy failed", t1), setCopiedText(null), !1;
		}
	}, $[0] = t0) : t0 = $[0];
	let copy = t0, t1;
	return $[1] === copiedText ? t1 = $[2] : (t1 = [copiedText, copy], $[1] = copiedText, $[2] = t1), t1;
}
function getTypeInfo(problem) {
	let first = problem.path[0];
	return first.kind === "type" ? {
		name: first.name || `<anonymous ${first.type}>`,
		type: first.type
	} : null;
}
function formatSchemaErrorsToMarkdown(groups) {
	let text = "# Schema errors\n\n";
	text += "There were errors while attempting to compile the configuration of your Sanity Studio Schema types.\n\n";
	for (let group of groups) {
		let schemaType = getTypeInfo(group);
		schemaType ? text += `## ${capitalize(schemaType.type)} type "${schemaType.name}"\n\n` : text += "## Unknown type\n\n";
		let parts = [];
		for (let segment of group.path) segment.kind === "type" ? parts.push(`${segment.name || `<anonymous ${segment.type}>`}:${segment.type}`) : segment.kind === "property" && parts.push(`${segment.name}`);
		text += `Path: ${parts.join(" → ")}\n`;
		for (let problem of group.problems) text += `${capitalize(problem.severity)}: ${problem.message}\n`, problem.helpId && (text += `[View documentation for this ${problem.severity}](${generateHelpUrl(problem.helpId)})\n`);
		text += "\n";
	}
	return text;
}
function renderPath(path) {
	return path.map((segment) => segment.kind === "type" ? `${segment.name || "<unnamed>"}(${segment.type})` : segment.kind === "property" ? segment.name : null).filter(Boolean).join(" > ");
}
function reportWarnings(schema) {
	let groupsWithWarnings = schema._validation?.filter((group) => group.problems.some((problem) => problem.severity === "warning"));
	groupsWithWarnings?.length !== 0 && (console.groupCollapsed(`⚠️ Schema has ${groupsWithWarnings?.length} warnings`), groupsWithWarnings?.forEach((group) => {
		let path = renderPath(group.path);
		console.group(`%cAt ${path}`, "color: #FF7636"), group.problems.forEach((problem) => {
			console.log(problem.message);
		}), console.groupEnd();
	}), console.groupEnd());
}
var TONES = {
	error: "critical",
	warning: "caution"
};
var SegmentSpan = gt.code.withConfig({
	displayName: "SegmentSpan",
	componentId: "sc-9q1022-0"
})`&&{background:none;color:inherit;}`;
var ErrorMessageText = gt(Text).withConfig({
	displayName: "ErrorMessageText",
	componentId: "sc-9q1022-1"
})`white-space:pre-line;`;
function SchemaProblemGroups(props) {
	let $ = (0, import_compiler_runtime.c)(4), { problemGroups } = props;
	useTranslation$1();
	let ret;
	if ($[0] !== problemGroups) {
		ret = [];
		for (let problemGroup of problemGroups) for (let problem of problemGroup.problems) ret.push({
			group: problemGroup,
			problem
		});
		$[0] = problemGroups, $[1] = ret;
	} else ret = $[1];
	let items = ret, t0;
	return $[2] === items ? t0 = $[3] : (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		as: "ul",
		gap: 4,
		children: items.map(_temp2$15)
	}), $[2] = items, $[3] = t0), t0;
}
function _temp2$15(t0, i) {
	let { group, problem: problem_0 } = t0, isError = problem_0.severity === "error", isWarning = problem_0.severity === "warning", schemaType = getTypeInfo(group);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Card, {
		border: !0,
		padding: 4,
		radius: 2,
		tone: TONES[problem_0.severity],
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
				marginRight: 3,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
					muted: !0,
					size: 1,
					children: [isError && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorOutlineIcon, {}), isWarning && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WarningOutlineIcon, {})]
				})
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
				flex: 1,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
					size: 1,
					weight: "medium",
					children: schemaType ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
						capitalize(schemaType.type),
						" type \"",
						schemaType.name,
						"\""
					] }) : null
				})
			})] }),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
				marginTop: 4,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
					border: !0,
					overflow: "auto",
					padding: 2,
					tone: "inherit",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Breadcrumbs, {
						separator: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
							muted: !0,
							size: 1,
							children: "→"
						}),
						children: group.path.map(_temp$32)
					})
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
				as: "ul",
				marginTop: 4,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
					as: "li",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
						gap: 3,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorMessageText, {
							muted: !0,
							size: 1,
							children: problem_0.message
						}), problem_0.helpId && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
							muted: !0,
							size: 1,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
								href: generateHelpUrl(problem_0.helpId),
								target: "_blank",
								rel: "noopener noreferrer",
								children: "View documentation →"
							})
						})]
					})
				})
			})
		]
	}, i);
}
function _temp$32(segment, j) {
	if (segment.kind === "type") {
		let text = `${_renderSegmentName(segment.name || `<anonymous ${segment.type}>`)}:${segment.type}`;
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			title: text,
			size: 1,
			textOverflow: "ellipsis",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SegmentSpan, { children: text })
		}, j);
	}
	return segment.kind === "property" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		title: segment.name,
		size: 1,
		textOverflow: "ellipsis",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SegmentSpan, { children: segment.name })
	}, j) : null;
}
function _renderSegmentName(str) {
	return str?.startsWith("<unnamed_type_@_index") ? `[${str.slice(1, -1).split("_")[4]}]` : str;
}
function SchemaErrorsScreen(t0) {
	let $ = (0, import_compiler_runtime.c)(29), { schema } = t0, t1;
	$[0] === schema._validation ? t1 = $[1] : (t1 = schema._validation?.filter(_temp2$14) || [], $[0] = schema._validation, $[1] = t1);
	let groupsWithErrors = t1, t2, t3;
	$[2] === schema ? (t2 = $[3], t3 = $[4]) : (t2 = () => reportWarnings(schema), t3 = [schema], $[2] = schema, $[3] = t2, $[4] = t3), (0, import_react.useEffect)(t2, t3);
	let toast = useToast(), { t } = useTranslation$1("studio"), { t: tCopyPaste } = useTranslation$1("copy-paste"), [, copy] = useCopyToClipboard(), t4;
	$[5] !== copy || $[6] !== groupsWithErrors || $[7] !== t || $[8] !== tCopyPaste || $[9] !== toast ? (t4 = async () => {
		let errorsText = formatSchemaErrorsToMarkdown(groupsWithErrors);
		try {
			await copy(errorsText) ? toast.push({
				status: "success",
				title: t("about-dialog.version-info.copy-to-clipboard-button.copied-text", "Copied to clipboard")
			}) : toast.push({
				status: "error",
				title: tCopyPaste("copy-paste.on-copy.validation.clipboard-not-supported.title", "Clipboard not supported")
			});
		} catch {
			toast.push({
				status: "error",
				title: tCopyPaste("copy-paste.on-copy.validation.clipboard-not-supported.title", "Clipboard not supported")
			});
		}
	}, $[5] = copy, $[6] = groupsWithErrors, $[7] = t, $[8] = tCopyPaste, $[9] = toast, $[10] = t4) : t4 = $[10];
	let handleCopyToClipboard = t4, t5;
	$[11] === Symbol.for("react.memo_cache_sentinel") ? (t5 = [
		4,
		5,
		6,
		7
	], $[11] = t5) : t5 = $[11];
	let t6;
	$[12] === t ? t6 = $[13] : (t6 = t("schema-errors.title", "Schema errors"), $[12] = t, $[13] = t6);
	let t7;
	$[14] === t6 ? t7 = $[15] : (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Heading, {
		as: "h1",
		children: t6
	}), $[14] = t6, $[15] = t7);
	let t8;
	$[16] === t ? t8 = $[17] : (t8 = t("about-dialog.version-info.copy-to-clipboard-button.text", "Copy to clipboard"), $[16] = t, $[17] = t8);
	let t9;
	$[18] !== handleCopyToClipboard || $[19] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		text: t8,
		onClick: handleCopyToClipboard
	}), $[18] = handleCopyToClipboard, $[19] = t8, $[20] = t9) : t9 = $[20];
	let t10;
	$[21] !== t7 || $[22] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		justify: "space-between",
		align: "center",
		gap: 2,
		children: [t7, t9]
	}), $[21] = t7, $[22] = t9, $[23] = t10) : t10 = $[23];
	let t11;
	$[24] === groupsWithErrors ? t11 = $[25] : (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SchemaProblemGroups, { problemGroups: groupsWithErrors }), $[24] = groupsWithErrors, $[25] = t11);
	let t12;
	return $[26] !== t10 || $[27] !== t11 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		"data-testid": "studio-error-screen",
		"data-error": "Schema errors",
		height: "fill",
		overflow: "auto",
		paddingY: t5,
		paddingX: 4,
		sizing: "border",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Container, {
			width: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
				gap: 5,
				children: [t10, t11]
			})
		})
	}), $[26] = t10, $[27] = t11, $[28] = t12) : t12 = $[28], t12;
}
function _temp2$14(group) {
	return group.problems.some(_temp$31);
}
function _temp$31(problem) {
	return problem.severity === "error";
}
/**
* The DevServerStoppedErrorScreen will always have been lazy loaded to client
* in instances where it is used, since DevServerStoppedError is only thrown
* when this module is loaded, and this screen is also conditional on this error type
*/
var DevServerStoppedErrorScreen = (0, import_react.lazy)(() => import("./ViteDevServerStopped-BE_YuUiZ-C0r7MogL.js").then((DevServerStopped) => ({ default: DevServerStopped.DevServerStoppedErrorScreen })));
/**
* This is responsible for handling all errors that may happen in a React Subtree of a Studio application.
* Note: there might be several instances of this component mounted in the React tree, avoid writing code that
* assumes there's only one at any given time.
* This will only catch errors that happens in the React effect/render layer, and will not catch errors that happens outside React-land, including the following:
* - Errors happening in a React event handler (i.e. onClick)
* - Async errors/promise rejections (i.e. fetch(), setTimeout()) even if initially triggered by React
*
* Classified request errors (CORS, network, 5xx, 401, 429) are handled by
* `WorkspacesProvider`'s request handler — they don't reach this boundary
* via the normal HTTP path. If one does land here defensively (developer
* code re-throws a classified error during render, etc.), it falls through
* to the standard fallback screen.
*
* @param props - {@link StudioErrorBoundaryProps}
*/
function StudioErrorBoundary(props) {
	let $ = (0, import_compiler_runtime.c)(16), { children, heading: t0, getErrorScreen } = props, heading = t0 === void 0 ? "An error occurred" : t0, [caughtError, setCaughtError] = (0, import_react.useState)(), [errorScreen, setErrorScreen] = (0, import_react.useState)(), t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = () => setCaughtError(void 0), $[0] = t1) : t1 = $[0];
	let handleResetError = t1, t2;
	$[1] === getErrorScreen ? t2 = $[2] : (t2 = (params) => {
		let eventId, run = () => {
			eventId = errorReporter.reportError(params.error, {
				reactErrorInfo: params.info,
				errorBoundary: "StudioErrorBoundary"
			})?.eventId;
		};
		try {
			run();
		} catch (t3) {
			let e = t3;
			e.message = `Encountered an additional error when reporting error: ${e.message}`, console.error(e);
		}
		setErrorScreen(getErrorScreen?.(params.error)), setCaughtError({
			error: params.error,
			componentStack: params.info.componentStack,
			eventId
		});
	}, $[1] = getErrorScreen, $[2] = t2);
	let handleCatchError = t2;
	if (useHotModuleReload(handleResetError), !caughtError?.error) {
		let t3;
		return $[3] !== children || $[4] !== handleCatchError ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ErrorBoundary$1, {
			onCatch: handleCatchError,
			children
		}), $[3] = children, $[4] = handleCatchError, $[5] = t3) : t3 = $[5], t3;
	}
	if (errorScreen) return errorScreen;
	if (caughtError.error instanceof SchemaError) {
		let t3;
		return $[6] === caughtError.error.schema ? t3 = $[7] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SchemaErrorsScreen, { schema: caughtError.error.schema }), $[6] = caughtError.error.schema, $[7] = t3), t3;
	}
	if ("ViteDevServerStoppedError" in caughtError.error && caughtError.error.ViteDevServerStoppedError) {
		let t3;
		return $[8] === Symbol.for("react.memo_cache_sentinel") ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DevServerStoppedErrorScreen, {}), $[8] = t3) : t3 = $[8], t3;
	}
	if (isImportError(caughtError.error)) {
		let t3;
		return $[9] !== caughtError.error || $[10] !== caughtError.eventId ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ImportErrorScreen, {
			error: caughtError.error,
			eventId: caughtError.eventId,
			autoReload: !0
		}), $[9] = caughtError.error, $[10] = caughtError.eventId, $[11] = t3) : t3 = $[11], t3;
	}
	let t3;
	return $[12] !== caughtError.error || $[13] !== caughtError.eventId || $[14] !== heading ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FallbackErrorScreen, {
		heading,
		error: caughtError.error,
		eventId: caughtError.eventId,
		onReset: handleResetError
	}), $[12] = caughtError.error, $[13] = caughtError.eventId, $[14] = heading, $[15] = t3) : t3 = $[15], t3;
}
/**
* Tracks whether the document has been hidden during this page load.
*
* The navigation-anchored timing events (`Studio Auth Ready Measured`,
* `Studio Ready Measured`) record `performance.now()` from navigation start. A
* backgrounded tab is throttled while `performance.now()` keeps advancing, so its
* measured durations are inflated even though no user was waiting. Stamping each
* event with a visibility snapshot lets analysis separate foreground loads from
* backgrounded ones instead of inferring it from a missing web-vitals event.
*
* The latch mirrors web-vitals' `firstHiddenTime` guard, which is not publicly
* exported. The listener registers at module evaluation; the initial value also
* covers a document already hidden at that point (a load opened in the background).
*
* @internal
*/
var isBrowser = typeof document < "u";
/**
* `performance.now()` of the first hide, or `Number.POSITIVE_INFINITY` while the
* document has only ever been visible. Prerendering documents report `'hidden'`,
* so they need no separate handling.
*/
var firstHiddenTime = isBrowser && document.visibilityState === "hidden" ? 0 : Infinity;
function handleVisibilityChange() {
	document.visibilityState === "hidden" && firstHiddenTime === Infinity && (firstHiddenTime = performance.now());
}
isBrowser && document.addEventListener("visibilitychange", handleVisibilityChange, { capture: !0 });
/**
* Visibility snapshot for a measurement taken at `measuredAt`, a `performance.now()`
* reading that defaults to the current time.
*
* @internal
*/
function getPageVisibilitySnapshot(measuredAt = isBrowser ? performance.now() : 0) {
	return {
		wasHidden: firstHiddenTime < measuredAt,
		visibilityState: isBrowser ? document.visibilityState : "visible",
		firstHiddenTime: firstHiddenTime === Infinity ? null : Math.round(firstHiddenTime)
	};
}
var StudioToolMountTimeMeasured = defineEvent({
	name: "Studio Tool Mount Time Measured",
	version: 1,
	description: "Time from a tool being selected to its component rendering its first effect (i.e. first paint after Suspense resolves the lazy chunk)."
});
var mountedTools = /* @__PURE__ */ new Set();
/**
* Invisible timer component rendered inside the active tool's `<Suspense>`
* boundary. Its first `useEffect` runs only after the tool's lazy chunk
* resolves and the tool's first render commits — capturing a fair "tool
* is on screen" moment.
*
* One-shot per mount: the effect runs once. Because the parent
* `<StudioErrorBoundary>` re-keys on `activeTool.name`, every tool
* activation produces a fresh `<ToolMountTimer>` instance and therefore
* exactly one event.
*/
function ToolMountTimer(t0) {
	let $ = (0, import_compiler_runtime.c)(5), { toolName, t0Ref } = t0, telemetry = useTelemetry(), t1, t2;
	return $[0] !== t0Ref || $[1] !== telemetry || $[2] !== toolName ? (t1 = () => {
		let t0$0 = t0Ref.current;
		if (t0$0 === null) return;
		let isFirstMount = !mountedTools.has(toolName);
		mountedTools.add(toolName), telemetry.log(StudioToolMountTimeMeasured, {
			toolName,
			durationMs: performance.now() - t0$0,
			isFirstMount
		});
	}, t2 = [
		telemetry,
		toolName,
		t0Ref
	], $[0] = t0Ref, $[1] = telemetry, $[2] = toolName, $[3] = t1, $[4] = t2) : (t1 = $[3], t2 = $[4]), (0, import_react.useEffect)(t1, t2), null;
}
function claimTokenFromClaimUrl(claimUrl) {
	try {
		let token = new URL(claimUrl).pathname.split("/").findLast(Boolean);
		return token && /^[\w-]+$/.test(token) ? token : void 0;
	} catch {
		return;
	}
}
function getStatusCode(error) {
	if (!(!error || typeof error != "object" || !("statusCode" in error))) return typeof error.statusCode == "number" ? error.statusCode : void 0;
}
/**
* Claim lifecycle of the current project.
* Defaults to `undefined` when the project wasn't created via `sanity new` or public provisioning APIs.
*
* @internal
*/
function useUnclaimedProject(t0) {
	let $ = (0, import_compiler_runtime.c)(16), t1;
	$[0] === t0 ? t1 = $[1] : (t1 = t0 === void 0 ? {} : t0, $[0] = t0, $[1] = t1);
	let { claimAttemptedAt } = t1, { auth, currentUser, projectId } = useWorkspace(), t2;
	$[2] === Symbol.for("react.memo_cache_sentinel") ? (t2 = { apiVersion: "v2026-05-04" }, $[2] = t2) : t2 = $[2];
	let client = useClient(t2), isRobot = currentUser?.provider === "sanity-token", sessionKey = `${projectId}:${isRobot}`, t3;
	$[3] === sessionKey ? t3 = $[4] : (t3 = {
		sessionKey,
		seenUnclaimed: !1
	}, $[3] = sessionKey, $[4] = t3);
	let provenanceRef = (0, import_react.useRef)(t3), [sessionState, setSessionState] = (0, import_react.useState)(), state = sessionState?.sessionKey === sessionKey ? sessionState.value : void 0, t4, t5;
	$[5] === sessionKey ? (t4 = $[6], t5 = $[7]) : (t4 = () => {
		provenanceRef.current.sessionKey !== sessionKey && (provenanceRef.current = {
			sessionKey,
			seenUnclaimed: !1
		}, setSessionState(void 0));
	}, t5 = [sessionKey], $[5] = sessionKey, $[6] = t4, $[7] = t5), (0, import_react.useEffect)(t4, t5);
	let t6, t7;
	return $[8] !== auth || $[9] !== claimAttemptedAt || $[10] !== client || $[11] !== isRobot || $[12] !== projectId || $[13] !== sessionKey ? (t6 = () => {
		if (!isRobot) return;
		let disposed = !1, terminal = !1, lastCheckedAt = 0, lookupSawNotFound = !1, claimLinkSpent = !1, checkInFlight = !1, claimPollingStopped$ = new Subject(), stopClaimPolling = () => {
			claimPollingStopped$.next(), claimPollingStopped$.complete();
		}, getRemainingClaimPollingDuration = () => {
			if (claimAttemptedAt === void 0) return 0;
			let elapsed = Math.max(0, Date.now() - claimAttemptedAt);
			return Math.max(0, 6e5 - elapsed);
		}, isClaimPollingActive = () => getRemainingClaimPollingDuration() > 0, hasSeenUnclaimed = () => provenanceRef.current.sessionKey === sessionKey && provenanceRef.current.seenUnclaimed, update = (next) => {
			disposed || setSessionState({
				sessionKey,
				value: next
			});
		}, finishClaimed = (members) => {
			terminal = !0, stopClaimPolling(), clearUnclaimedProjectRecord(projectId), clearUnclaimedProjectSnooze(projectId), update({ status: "claimed" });
			let humanMembers = members?.filter(_temp$30);
			humanMembers?.length === 1 && client.request({
				uri: `/users/${humanMembers[0].id}`,
				tag: "unclaimed-project.claimant"
			}).then((user) => {
				let email = typeof user.email == "string" ? user.email.trim() : "";
				!disposed && email && update({
					status: "claimed",
					email
				});
			}).catch(_temp2$13);
		}, finishExpired = () => {
			if (terminal = !0, stopClaimPolling(), clearUnclaimedProjectRecord(projectId), clearUnclaimedProjectSnooze(projectId), supportsLocalStorage) try {
				localStorage.removeItem(getAuthTokenStorageKey(projectId));
			} catch {}
			update({ status: "expired" }), auth.logout?.();
		}, dropClaimRecord = () => {
			claimLinkSpent = !0, clearUnclaimedProjectRecord(projectId), disposed || setSessionState((prev) => {
				let value = prev?.sessionKey === sessionKey ? prev.value : void 0;
				return value?.status === "unclaimed" ? {
					sessionKey,
					value: {
						...value,
						claimUrl: void 0,
						claimLinkSpent: !0
					}
				} : prev;
			});
		}, refineFromLookup = async (record, localExpiry) => {
			let claimToken = claimTokenFromClaimUrl(record.claimUrl);
			if (!claimToken) return;
			let lastLookupAt = record.lastLookupAt ? new Date(record.lastLookupAt).getTime() : 0;
			if (Date.now() - lastLookupAt < 18e5) return;
			let throttled = {
				...record,
				lastLookupAt: (/* @__PURE__ */ new Date()).toISOString()
			}, response;
			try {
				let { apiHost } = client.config();
				response = await fetch(`${apiHost}/v2026-06-23/provision/${claimToken}/lookup`);
			} catch {
				!disposed && !terminal && writeUnclaimedProjectRecord(projectId, throttled);
				return;
			}
			if (disposed || terminal) return;
			if (writeUnclaimedProjectRecord(projectId, throttled), response.status === 404) {
				(lookupSawNotFound || localExpiry !== void 0 && Date.now() > localExpiry) && dropClaimRecord(), lookupSawNotFound = !0;
				return;
			}
			if (!response.ok) return;
			let data;
			try {
				data = await response.json();
			} catch {
				return;
			}
			if (!(disposed || terminal)) {
				if (data.state === "claimed") finishClaimed();
				else if (data.state === "expired") dropClaimRecord();
				else if (data.state === "claimable" && data.expiresAt) {
					let expiresAt = new Date(data.expiresAt);
					if (Number.isNaN(expiresAt.getTime())) return;
					claimLinkSpent = !1, writeUnclaimedProjectRecord(projectId, {
						...throttled,
						expiresAt: data.expiresAt
					}), update({
						status: "unclaimed",
						claimUrl: record.claimUrl,
						expiresAt,
						claimLinkSpent: !1
					});
				}
			}
		}, performCheck = async () => {
			let project;
			try {
				project = await client.request({
					uri: `/projects/${projectId}`,
					tag: "unclaimed-project"
				});
			} catch (t8) {
				let statusCode = getStatusCode(t8), hasMintProvenance = hasSeenUnclaimed() || !!readUnclaimedProjectRecord(projectId);
				statusCode === 404 && hasMintProvenance && finishExpired();
				return;
			}
			if (disposed || terminal || !project.organizationId) return;
			let record_0 = readUnclaimedProjectRecord(projectId);
			if (project.organizationId !== "oSystemUnclaimed") {
				(hasSeenUnclaimed() || record_0) && finishClaimed(project.members);
				return;
			}
			provenanceRef.current = {
				sessionKey,
				seenUnclaimed: !0
			};
			let createdAt = project.createdAt ? new Date(project.createdAt) : void 0, derivedExpiresAt = createdAt && !Number.isNaN(createdAt.getTime()) ? new Date(createdAt.getTime() + 2592e5) : void 0, expiresAt_0 = record_0?.expiresAt ? new Date(record_0.expiresAt) : derivedExpiresAt;
			expiresAt_0 && !disposed && setSessionState((prev_0) => {
				let value_0 = prev_0?.sessionKey === sessionKey ? prev_0.value : void 0;
				return {
					sessionKey,
					value: {
						status: "unclaimed",
						claimUrl: record_0?.claimUrl,
						expiresAt: !record_0 && value_0?.status === "unclaimed" ? value_0.expiresAt : expiresAt_0,
						claimLinkSpent
					}
				};
			}), record_0 && await refineFromLookup(record_0, expiresAt_0?.getTime());
		}, check = async () => {
			if (!(terminal || checkInFlight)) {
				checkInFlight = !0, lastCheckedAt = Date.now();
				try {
					await performCheck();
				} catch {}
				checkInFlight = !1;
			}
		}, remainingClaimPollingDuration = getRemainingClaimPollingDuration(), checkSubscription = merge$1(remainingClaimPollingDuration ? timer(0, 1e4).pipe(takeUntil(timer(remainingClaimPollingDuration)), takeUntil(claimPollingStopped$)) : of(0), fromEvent(window, "focus").pipe(exhaustMap(() => isClaimPollingActive() ? of(0) : timer(Math.max(0, 3e5 - (Date.now() - lastCheckedAt))))), fromEvent(document, "visibilitychange").pipe(filter(() => document.visibilityState === "visible" && isClaimPollingActive()))).pipe(tap(() => void check())).subscribe(), onHashChange = () => {
			let record_1 = readUnclaimedProjectRecord(projectId);
			!record_1 || disposed || (claimLinkSpent = !1, setSessionState((prev_1) => {
				let value_1 = prev_1?.sessionKey === sessionKey ? prev_1.value : void 0;
				return value_1?.status === "unclaimed" ? {
					sessionKey,
					value: {
						...value_1,
						claimUrl: record_1.claimUrl,
						expiresAt: record_1.expiresAt ? new Date(record_1.expiresAt) : value_1.expiresAt,
						claimLinkSpent: !1
					}
				} : prev_1;
			}));
		}, hashSubscription = fromEvent(window, "hashchange").pipe(tap(onHashChange)).subscribe();
		return () => {
			disposed = !0, stopClaimPolling(), checkSubscription.unsubscribe(), hashSubscription.unsubscribe();
		};
	}, t7 = [
		auth,
		claimAttemptedAt,
		client,
		isRobot,
		projectId,
		sessionKey
	], $[8] = auth, $[9] = claimAttemptedAt, $[10] = client, $[11] = isRobot, $[12] = projectId, $[13] = sessionKey, $[14] = t6, $[15] = t7) : (t6 = $[14], t7 = $[15]), (0, import_react.useEffect)(t6, t7), state;
}
function _temp2$13() {}
function _temp$30(member) {
	return member.isRobot === !1 && typeof member.id == "string" && !!member.id;
}
var LOCAL_COPY_URL = import.meta.env?.SANITY_STUDIO_UNCLAIMED_PROJECT_COPY_URL;
/** Resolves the managed identity instruction without exposing email through Journey. */
function getClaimedIdentityText(text, email) {
	return interpolateTemplate(text, { identity: email ?? "the account tied to this project" });
}
/** Identifies the managed placeholder so a known claimant can be emphasized safely. */
function getClaimedIdentityTextParts(text, email) {
	if (!email) return;
	let markerIndex = text.indexOf("{{identity}}");
	if (markerIndex !== -1) return {
		before: text.slice(0, markerIndex),
		identity: email,
		after: text.slice(markerIndex + 12)
	};
}
function isRecord$1(value) {
	return typeof value == "object" && !!value;
}
function hasStringProperties(value, keys) {
	return isRecord$1(value) && keys.every((key) => typeof value[key] == "string");
}
function isPositiveInteger(value) {
	return typeof value == "number" && Number.isInteger(value) && value > 0;
}
/** @internal */
function parseUnclaimedProjectCopy(value) {
	if (isRecord$1(value) && !(!isPositiveInteger(value.criticalThresholdHours) || !isPositiveInteger(value.snoozeMinutes) || !hasStringProperties(value.banner, [
		"text",
		"criticalText",
		"claimButtonText"
	]) || !hasStringProperties(value.toast, [
		"title",
		"criticalTitle",
		"description",
		"claimButtonText",
		"snoozeButtonText"
	]) || !hasStringProperties(value.claimed, [
		"text",
		"identityText",
		"signInButtonText"
	]) || !hasStringProperties(value.noClaimUrl, ["text"]))) return {
		criticalThresholdHours: value.criticalThresholdHours,
		snoozeMinutes: value.snoozeMinutes,
		banner: {
			text: value.banner.text,
			criticalText: value.banner.criticalText,
			claimButtonText: value.banner.claimButtonText
		},
		toast: {
			title: value.toast.title,
			criticalTitle: value.toast.criticalTitle,
			description: value.toast.description,
			claimButtonText: value.toast.claimButtonText,
			snoozeButtonText: value.toast.snoozeButtonText
		},
		claimed: {
			text: value.claimed.text,
			identityText: value.claimed.identityText,
			signInButtonText: value.claimed.signInButtonText
		},
		noClaimUrl: { text: value.noClaimUrl.text }
	};
}
/** @internal */
function getLocalUnclaimedProjectCopyUrl({ isDev, value }) {
	if (!(!isDev || !value)) try {
		let url = new URL(value);
		return url.protocol === "http:" && (url.hostname === "localhost" || url.hostname === "127.0.0.1" || url.hostname === "[::1]") ? url.href : void 0;
	} catch {
		return;
	}
}
/** Fetch Journey-owned copy only after the project is known to be part of mint-and-claim. */
function useUnclaimedProjectCopy(enabled) {
	let $ = (0, import_compiler_runtime.c)(5), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
		...DEFAULT_STUDIO_CLIENT_OPTIONS,
		apiVersion: "2026-07-28"
	}, $[0] = t0) : t0 = $[0];
	let client = useClient(t0), t1;
	$[1] === Symbol.for("react.memo_cache_sentinel") ? (t1 = getLocalUnclaimedProjectCopyUrl({
		isDev: import.meta.env?.DEV ?? !1,
		value: LOCAL_COPY_URL
	}), $[1] = t1) : t1 = $[1];
	let localCopyUrl = t1, t2;
	bb0: {
		if (!enabled) {
			let t3;
			$[2] === Symbol.for("react.memo_cache_sentinel") ? (t3 = of(void 0), $[2] = t3) : t3 = $[2], t2 = t3;
			break bb0;
		}
		let t3;
		$[3] === client ? t3 = $[4] : (t3 = (localCopyUrl ? defer(async () => {
			let response = await fetch(localCopyUrl, { credentials: "omit" });
			if (!response.ok) throw Error(`Journey copy request failed: ${response.status}`);
			return response.json();
		}) : client.observable.request({
			uri: "/journey/unclaimed-project",
			tag: "unclaimed-project-copy"
		})).pipe(map(parseUnclaimedProjectCopy), catchError(_temp$29)), $[3] = client, $[4] = t3), t2 = t3;
	}
	return useObservable(t2);
}
function _temp$29() {
	return of(void 0);
}
/**
* Persistent banner + snoozable toast nudging the user to claim a minted-but-unclaimed project
* before it expires, flipping to an identity-aware login banner once it's claimed. Renders
* nothing for anything not part of mint-and-claim — see {@link useUnclaimedProject}.
*
* @internal
*/
function UnclaimedProjectNudge() {
	let $ = (0, import_compiler_runtime.c)(1);
	let t0;
	return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UnclaimedProjectNudgeAuthCheck, {}), $[0] = t0) : t0 = $[0], t0;
}
function UnclaimedProjectNudgeAuthCheck() {
	let $ = (0, import_compiler_runtime.c)(5), { currentUser, projectId } = useWorkspace(), provider = currentUser?.provider, t0, t1;
	if ($[0] !== projectId || $[1] !== provider ? (t0 = () => {
		provider && provider !== "sanity-token" && clearUnclaimedProjectRecord(projectId);
	}, t1 = [projectId, provider], $[0] = projectId, $[1] = provider, $[2] = t0, $[3] = t1) : (t0 = $[2], t1 = $[3]), (0, import_react.useEffect)(t0, t1), provider !== "sanity-token") return null;
	let t2;
	return $[4] === Symbol.for("react.memo_cache_sentinel") ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UnclaimedProjectNudgeStateCheck, {}), $[4] = t2) : t2 = $[4], t2;
}
function UnclaimedProjectNudgeStateCheck() {
	let $ = (0, import_compiler_runtime.c)(7), { projectId } = useWorkspace(), [claimAttempt, setClaimAttempt] = (0, import_react.useState)(), claimAttemptedAt = claimAttempt?.projectId === projectId ? claimAttempt.startedAt : void 0, t0;
	$[0] === claimAttemptedAt ? t0 = $[1] : (t0 = { claimAttemptedAt }, $[0] = claimAttemptedAt, $[1] = t0);
	let state = useUnclaimedProject(t0), t1;
	$[2] === projectId ? t1 = $[3] : (t1 = () => setClaimAttempt({
		projectId,
		startedAt: Date.now()
	}), $[2] = projectId, $[3] = t1);
	let handleClaim = t1;
	if (!state) return null;
	let t2;
	return $[4] !== handleClaim || $[5] !== state ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UnclaimedProjectNudgeInner, {
		onClaim: handleClaim,
		state
	}), $[4] = handleClaim, $[5] = state, $[6] = t2) : t2 = $[6], t2;
}
function UnclaimedProjectNudgeInner(t0) {
	let $ = (0, import_compiler_runtime.c)(89), { onClaim, state } = t0, { auth, projectId } = useWorkspace(), copy = useUnclaimedProjectCopy(!0), unclaimed = state?.status === "unclaimed" ? state : void 0, now = useMinuteTick(!!unclaimed), timeLeft = useRelativeTime(unclaimed?.expiresAt ?? ""), t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = {
		dateStyle: "medium",
		timeStyle: "short"
	}, $[0] = t1) : t1 = $[0];
	let expiresAtFormatter = useDateTimeFormat(t1), templateValues = {
		expiresAt: unclaimed ? expiresAtFormatter.format(unclaimed.expiresAt) : "",
		timeLeft
	}, t2;
	$[1] === projectId ? t2 = $[2] : (t2 = () => ({
		projectId,
		snoozedAt: readUnclaimedProjectSnoozedAt(projectId)
	}), $[1] = projectId, $[2] = t2);
	let [snoozeState, setSnoozeState] = (0, import_react.useState)(t2), handleSnooze, t3, t4;
	if ($[3] !== copy?.snoozeMinutes || $[4] !== now || $[5] !== projectId || $[6] !== snoozeState.projectId || $[7] !== snoozeState.snoozedAt) {
		let snoozedAt = snoozeState.projectId === projectId ? snoozeState.snoozedAt : readUnclaimedProjectSnoozedAt(projectId), t5;
		$[11] === projectId ? t5 = $[12] : (t5 = () => {
			let at = (/* @__PURE__ */ new Date()).toISOString();
			writeUnclaimedProjectSnoozedAt(projectId, at), setSnoozeState({
				projectId,
				snoozedAt: at
			});
		}, $[11] = projectId, $[12] = t5), handleSnooze = t5;
		let snoozeDurationMs = (copy?.snoozeMinutes ?? 0) * 6e4;
		t3 = Boolean, t4 = snoozedAt && snoozeDurationMs && now - new Date(snoozedAt).getTime() < snoozeDurationMs, $[3] = copy?.snoozeMinutes, $[4] = now, $[5] = projectId, $[6] = snoozeState.projectId, $[7] = snoozeState.snoozedAt, $[8] = handleSnooze, $[9] = t3, $[10] = t4;
	} else handleSnooze = $[8], t3 = $[9], t4 = $[10];
	let isSnoozed = t3(t4), critical = !!(copy && unclaimed && unclaimed.expiresAt.getTime() - now <= copy.criticalThresholdHours * 36e5), t5;
	$[13] === auth ? t5 = $[14] : (t5 = () => void auth.logout?.(), $[13] = auth, $[14] = t5);
	let handleSignIn = t5, t6 = critical ? "error" : "warning", t7 = !!(copy && unclaimed) && !isSnoozed, t8 = copy ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: interpolateTemplate(critical ? copy.toast.criticalTitle : copy.toast.title, templateValues) }) : "", t9;
	$[15] !== copy || $[16] !== critical || $[17] !== handleSnooze || $[18] !== onClaim || $[19] !== unclaimed ? (t9 = unclaimed && copy && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 4,
		paddingY: 2,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			weight: "regular",
			children: unclaimed.claimUrl || unclaimed.claimLinkSpent ? copy.toast.description : copy.noClaimUrl.text
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			gap: 3,
			children: [unclaimed.claimUrl && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
				as: "a",
				href: unclaimed.claimUrl,
				target: "_blank",
				rel: "noopener noreferrer",
				mode: "default",
				tone: critical ? "critical" : "primary",
				size: "default",
				iconRight: LaunchIcon,
				text: copy.toast.claimButtonText,
				onClick: onClaim
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
				mode: "bleed",
				tone: "neutral",
				size: "default",
				paddingY: 1,
				text: copy.toast.snoozeButtonText,
				onClick: handleSnooze,
				style: {
					fontSize: "0.6875rem",
					opacity: .6
				}
			})]
		})]
	}), $[15] = copy, $[16] = critical, $[17] = handleSnooze, $[18] = onClaim, $[19] = unclaimed, $[20] = t9) : t9 = $[20];
	let t10;
	if ($[21] !== t6 || $[22] !== t7 || $[23] !== t8 || $[24] !== t9 ? (t10 = {
		id: "unclaimed-project-nudge",
		status: t6,
		enabled: t7,
		title: t8,
		description: t9
	}, $[21] = t6, $[22] = t7, $[23] = t8, $[24] = t9, $[25] = t10) : t10 = $[25], useConditionalToast(t10), !copy) return null;
	if (state?.status === "claimed") {
		let t11;
		$[26] === Symbol.for("react.memo_cache_sentinel") ? (t11 = [
			"block",
			"block",
			"none"
		], $[26] = t11) : t11 = $[26];
		let t12;
		$[27] === Symbol.for("react.memo_cache_sentinel") ? (t12 = {
			flex: 1,
			minWidth: 0
		}, $[27] = t12) : t12 = $[27];
		let t13;
		$[28] === copy.claimed.text ? t13 = $[29] : (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			weight: "medium",
			style: t12,
			children: copy.claimed.text
		}), $[28] = copy.claimed.text, $[29] = t13);
		let t14;
		$[30] === Symbol.for("react.memo_cache_sentinel") ? (t14 = { flexShrink: 0 }, $[30] = t14) : t14 = $[30];
		let t15;
		$[31] !== copy.claimed.signInButtonText || $[32] !== handleSignIn ? (t15 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			mode: "default",
			tone: "positive",
			size: "default",
			text: copy.claimed.signInButtonText,
			onClick: handleSignIn,
			style: t14
		}), $[31] = copy.claimed.signInButtonText, $[32] = handleSignIn, $[33] = t15) : t15 = $[33];
		let t16;
		$[34] !== t13 || $[35] !== t15 ? (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			align: "center",
			gap: 3,
			justify: "space-between",
			children: [t13, t15]
		}), $[34] = t13, $[35] = t15, $[36] = t16) : t16 = $[36];
		let t17;
		$[37] === Symbol.for("react.memo_cache_sentinel") ? (t17 = { overflowWrap: "anywhere" }, $[37] = t17) : t17 = $[37];
		let t18;
		$[38] !== copy.claimed.identityText || $[39] !== state.email ? (t18 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			weight: "medium",
			style: t17,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ClaimedIdentityText, {
				text: copy.claimed.identityText,
				email: state.email
			})
		}), $[38] = copy.claimed.identityText, $[39] = state.email, $[40] = t18) : t18 = $[40];
		let t19;
		$[41] !== t16 || $[42] !== t18 ? (t19 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			display: t11,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
				gap: 3,
				children: [t16, t18]
			})
		}), $[41] = t16, $[42] = t18, $[43] = t19) : t19 = $[43];
		let t20;
		$[44] === Symbol.for("react.memo_cache_sentinel") ? (t20 = [
			"none",
			"none",
			"block"
		], $[44] = t20) : t20 = $[44];
		let t21;
		$[45] === Symbol.for("react.memo_cache_sentinel") ? (t21 = { overflowWrap: "anywhere" }, $[45] = t21) : t21 = $[45];
		let t22;
		$[46] !== copy.claimed.identityText || $[47] !== state.email ? (t22 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ClaimedIdentityText, {
			text: copy.claimed.identityText,
			email: state.email
		}), $[46] = copy.claimed.identityText, $[47] = state.email, $[48] = t22) : t22 = $[48];
		let t23;
		$[49] !== copy.claimed.text || $[50] !== t22 ? (t23 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
			size: 1,
			weight: "medium",
			style: t21,
			children: [
				copy.claimed.text,
				" ",
				t22
			]
		}), $[49] = copy.claimed.text, $[50] = t22, $[51] = t23) : t23 = $[51];
		let t24;
		$[52] !== copy.claimed.signInButtonText || $[53] !== handleSignIn ? (t24 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			mode: "default",
			tone: "positive",
			size: "default",
			text: copy.claimed.signInButtonText,
			onClick: handleSignIn
		}), $[52] = copy.claimed.signInButtonText, $[53] = handleSignIn, $[54] = t24) : t24 = $[54];
		let t25;
		$[55] !== t23 || $[56] !== t24 ? (t25 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			display: t20,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
				align: "center",
				gap: 3,
				justify: "center",
				wrap: "wrap",
				children: [t23, t24]
			})
		}), $[55] = t23, $[56] = t24, $[57] = t25) : t25 = $[57];
		let t26;
		return $[58] !== t19 || $[59] !== t25 ? (t26 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Card, {
			"data-testid": "unclaimed-project-banner",
			tone: "positive",
			padding: 3,
			borderBottom: !0,
			children: [t19, t25]
		}), $[58] = t19, $[59] = t25, $[60] = t26) : t26 = $[60], t26;
	}
	if (!unclaimed) return null;
	let T0 = Card, t12 = critical ? "critical" : "caution", T1 = Flex, T2 = Flex, t21;
	$[61] === Symbol.for("react.memo_cache_sentinel") ? (t21 = { lineHeight: 0 }, $[61] = t21) : t21 = $[61];
	let t22;
	$[62] === Symbol.for("react.memo_cache_sentinel") ? (t22 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		"aria-hidden": "true",
		align: "center",
		justify: "center",
		style: t21,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ClockIcon, { style: { display: "block" } })
	}), $[62] = t22) : t22 = $[62];
	let T3 = Text, t25 = interpolateTemplate(critical ? copy.banner.criticalText : copy.banner.text, templateValues), t26;
	$[63] !== T3 || $[64] !== t25 ? (t26 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(T3, {
		size: 1,
		weight: "medium",
		children: t25
	}), $[63] = T3, $[64] = t25, $[65] = t26) : t26 = $[65];
	let t27;
	$[66] !== T2 || $[67] !== t22 || $[68] !== t26 ? (t27 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(T2, {
		align: "center",
		gap: 2,
		children: [t22, t26]
	}), $[66] = T2, $[67] = t22, $[68] = t26, $[69] = t27) : t27 = $[69];
	let t28;
	$[70] !== critical || $[71] !== unclaimed.expiresAt ? (t28 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UnclaimedProjectCountdown, {
		expiresAt: unclaimed.expiresAt,
		critical
	}), $[70] = critical, $[71] = unclaimed.expiresAt, $[72] = t28) : t28 = $[72];
	let t29;
	$[73] !== copy.banner.claimButtonText || $[74] !== copy.noClaimUrl || $[75] !== critical || $[76] !== onClaim || $[77] !== unclaimed.claimLinkSpent || $[78] !== unclaimed.claimUrl ? (t29 = unclaimed.claimUrl ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		as: "a",
		href: unclaimed.claimUrl,
		target: "_blank",
		rel: "noopener noreferrer",
		mode: "default",
		tone: critical ? "critical" : "primary",
		size: "default",
		iconRight: LaunchIcon,
		text: copy.banner.claimButtonText,
		onClick: onClaim
	}) : unclaimed.claimLinkSpent ? null : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		children: copy.noClaimUrl.text
	}), $[73] = copy.banner.claimButtonText, $[74] = copy.noClaimUrl, $[75] = critical, $[76] = onClaim, $[77] = unclaimed.claimLinkSpent, $[78] = unclaimed.claimUrl, $[79] = t29) : t29 = $[79];
	let t30;
	$[80] !== T1 || $[81] !== t27 || $[82] !== t28 || $[83] !== t29 ? (t30 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(T1, {
		align: "center",
		gap: 3,
		justify: "center",
		wrap: "wrap",
		children: [
			t27,
			t28,
			t29
		]
	}), $[80] = T1, $[81] = t27, $[82] = t28, $[83] = t29, $[84] = t30) : t30 = $[84];
	let t31;
	return $[85] !== T0 || $[86] !== t12 || $[87] !== t30 ? (t31 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(T0, {
		"data-testid": "unclaimed-project-banner",
		tone: t12,
		padding: 3,
		borderBottom: !0,
		children: t30
	}), $[85] = T0, $[86] = t12, $[87] = t30, $[88] = t31) : t31 = $[88], t31;
}
function ClaimedIdentityText(t0) {
	let $ = (0, import_compiler_runtime.c)(7), { text, email } = t0, t1;
	$[0] !== email || $[1] !== text ? (t1 = getClaimedIdentityTextParts(text, email), $[0] = email, $[1] = text, $[2] = t1) : t1 = $[2];
	let parts = t1, t2;
	return $[3] !== email || $[4] !== parts || $[5] !== text ? (t2 = parts ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		parts.before,
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: parts.identity }),
		parts.after
	] }) : getClaimedIdentityText(text, email), $[3] = email, $[4] = parts, $[5] = text, $[6] = t2) : t2 = $[6], t2;
}
/** Isolates the per-second update so the full nudge does not re-render on every tick. */
function UnclaimedProjectCountdown(t0) {
	let $ = (0, import_compiler_runtime.c)(9), { critical, expiresAt } = t0, [now, setNow] = (0, import_react.useState)(_temp$28), t1, t2;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = () => {
		let id = setInterval(() => setNow(Date.now()), 1e3);
		return () => clearInterval(id);
	}, t2 = [], $[0] = t1, $[1] = t2) : (t1 = $[0], t2 = $[1]), (0, import_react.useEffect)(t1, t2);
	let t3 = critical ? "critical" : "caution", t4;
	$[2] === Symbol.for("react.memo_cache_sentinel") ? (t4 = {
		fontVariantNumeric: "tabular-nums",
		letterSpacing: "0.08em",
		minWidth: "7.5ch",
		textAlign: "center"
	}, $[2] = t4) : t4 = $[2];
	let t5;
	$[3] !== expiresAt || $[4] !== now ? (t5 = formatCountdown(expiresAt, now), $[3] = expiresAt, $[4] = now, $[5] = t5) : t5 = $[5];
	let t6;
	return $[6] !== t3 || $[7] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Badge, {
		"aria-hidden": "true",
		"data-testid": "unclaimed-project-countdown",
		fontSize: 1,
		padding: 2,
		radius: 2,
		tone: t3,
		style: t4,
		children: t5
	}), $[6] = t3, $[7] = t5, $[8] = t6) : t6 = $[8], t6;
}
/** Formats the remaining expiry window without locale-specific unit labels. */
function _temp$28() {
	return Date.now();
}
function formatCountdown(expiresAt, now) {
	let totalSeconds = Math.max(0, Math.ceil((expiresAt.getTime() - now) / 1e3));
	return [
		Math.floor(totalSeconds / 3600),
		Math.floor(totalSeconds % 3600 / 60),
		totalSeconds % 60
	].map((value) => String(value).padStart(2, "0")).join(":");
}
/** Re-evaluates the snooze window and the critical flip once a minute while the nudge shows. */
function useMinuteTick(enabled) {
	let $ = (0, import_compiler_runtime.c)(3), [now, setNow] = (0, import_react.useState)(_temp2$12), t0, t1;
	return $[0] === enabled ? (t0 = $[1], t1 = $[2]) : (t0 = () => {
		if (!enabled) return;
		(0, import_react.startTransition)(() => setNow(Date.now()));
		let id = setInterval(() => setNow(Date.now()), 6e4);
		return () => clearInterval(id);
	}, t1 = [enabled], $[0] = enabled, $[1] = t0, $[2] = t1), (0, import_react.useEffect)(t0, t1), now;
}
function _temp2$12() {
	return Date.now();
}
var DetectViteDevServerStopped = (0, import_react.lazy)(() => import("./ViteDevServerStopped-BE_YuUiZ-C0r7MogL.js").then((DevServerStopped) => ({ default: DevServerStopped.DetectViteDevServerStopped })));
var detectViteDevServerStopped = import.meta.hot && true;
var SearchFullscreenPortalCard = gt(Card).withConfig({
	displayName: "SearchFullscreenPortalCard",
	componentId: "sc-ksds8n-0"
})`height:100%;left:0;overflow:hidden;overflow:clip;position:fixed;top:0;width:100%;z-index:200;`;
var studioReadyFired = !1;
/**
* The Studio Layout component is the root component of the Sanity Studio UI.
* It renders the navbar, the active tool, and the search modal as well as the error boundary.
*
* @public
* @returns A Studio Layout element that renders the navbar, the active tool, and the search modal as well as the error boundary
* @remarks This component should be used as a child component to the StudioProvider
* @example Rendering a Studio Layout
* ```ts
* <StudioProvider
*  basePath={basePath}
*  config={config}
*  onSchemeChange={onSchemeChange}
*  scheme={scheme}
*  unstable_history={unstable_history}
*  unstable_noAuthBoundary={unstable_noAuthBoundary}
* >
*   <StudioLayout />
*</StudioProvider>
* ```
*/
function StudioLayout() {
	let $ = (0, import_compiler_runtime.c)(2), Layout = useLayoutComponent(), t0;
	return $[0] === Layout ? t0 = $[1] : (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Layout, {}), $[0] = Layout, $[1] = t0), t0;
}
/**
* @internal
* The default Studio Layout component
* */
function StudioLayoutComponent() {
	let $ = (0, import_compiler_runtime.c)(73), { name, title, tools } = useWorkspace(), telemetry = useTelemetry();
	useNetworkProtocolCheck();
	let t0;
	$[0] === tools ? t0 = $[1] : (t0 = tools.filter(isDefaultRouteTool), $[0] = tools, $[1] = t0);
	let defaultRouteTools = t0, router = useRouter(), activeToolName = useRouterState(_temp$27), t1;
	if ($[2] !== activeToolName || $[3] !== tools) {
		let t2;
		$[5] === activeToolName ? t2 = $[6] : (t2 = (tool) => tool.name === activeToolName, $[5] = activeToolName, $[6] = t2), t1 = tools.find(t2), $[2] = activeToolName, $[3] = tools, $[4] = t1;
	} else t1 = $[4];
	let activeTool = t1, toolMountT0Ref = (0, import_react.useRef)(null), lastToolNameRef = (0, import_react.useRef)(void 0), t2, t3;
	$[7] === activeToolName ? (t2 = $[8], t3 = $[9]) : (t2 = () => {
		activeToolName !== lastToolNameRef.current && (lastToolNameRef.current = activeToolName, toolMountT0Ref.current = activeToolName ? performance.now() : null);
	}, t3 = [activeToolName], $[7] = activeToolName, $[8] = t2, $[9] = t3), (0, import_react.useEffect)(t2, t3);
	let [searchFullscreenOpen, setSearchFullscreenOpen] = (0, import_react.useState)(!1), [searchFullscreenPortalEl, setSearchFullscreenPortalEl] = (0, import_react.useState)(null), [searchOpen, setSearchOpen] = (0, import_react.useState)(!1), t4;
	bb0: {
		let t5;
		$[10] !== name || $[11] !== title ? (t5 = title || startCase(name), $[10] = name, $[11] = title, $[12] = t5) : t5 = $[12];
		let workspaceTitle = t5, toolTitle = activeTool ? activeTool.title || activeTool.name : void 0;
		if (toolTitle) {
			t4 = `${toolTitle} | ${workspaceTitle}`;
			break bb0;
		}
		t4 = workspaceTitle;
	}
	let documentTitle = t4, toolControlsDocumentTitle = !!activeTool?.controlsDocumentTitle, t5, t6;
	$[13] !== documentTitle || $[14] !== toolControlsDocumentTitle ? (t5 = () => {
		toolControlsDocumentTitle || (document.title = documentTitle);
	}, t6 = [documentTitle, toolControlsDocumentTitle], $[13] = documentTitle, $[14] = toolControlsDocumentTitle, $[15] = t5, $[16] = t6) : (t5 = $[15], t6 = $[16]), (0, import_react.useEffect)(t5, t6);
	let t7, t8;
	$[17] !== activeTool || $[18] !== telemetry || $[19] !== tools.length ? (t7 = () => {
		if (studioReadyFired || !activeTool) return;
		studioReadyFired = !0;
		let durationMs = performance.now();
		telemetry.log(StudioReadyMeasured, {
			durationMs,
			toolsCount: tools.length,
			activeToolName: activeTool.name,
			...getPageVisibilitySnapshot(durationMs)
		});
	}, t8 = [
		activeTool,
		telemetry,
		tools.length
	], $[17] = activeTool, $[18] = telemetry, $[19] = tools.length, $[20] = t7, $[21] = t8) : (t7 = $[20], t8 = $[21]), (0, import_react.useEffect)(t7, t8);
	let t9;
	$[22] === Symbol.for("react.memo_cache_sentinel") ? (t9 = (open) => {
		setSearchFullscreenOpen(open);
	}, $[22] = t9) : t9 = $[22];
	let handleSearchFullscreenOpenChange = t9, t10;
	$[23] === Symbol.for("react.memo_cache_sentinel") ? (t10 = (open_0) => {
		setSearchOpen(open_0);
	}, $[23] = t10) : t10 = $[23];
	let handleSearchOpenChange = t10, t11;
	$[24] !== searchFullscreenOpen || $[25] !== searchFullscreenPortalEl || $[26] !== searchOpen ? (t11 = {
		searchFullscreenOpen,
		searchFullscreenPortalEl,
		searchOpen,
		onSearchFullscreenOpenChange: handleSearchFullscreenOpenChange,
		onSearchOpenChange: handleSearchOpenChange
	}, $[24] = searchFullscreenOpen, $[25] = searchFullscreenPortalEl, $[26] = searchOpen, $[27] = t11) : t11 = $[27];
	let navbarContextValue = t11, Navbar = useNavbarComponent(), ActiveToolLayout = useActiveToolLayoutComponent(), t12;
	$[28] !== activeTool || $[29] !== activeToolName || $[30] !== tools ? (t12 = !activeTool && (activeToolName === "desk" || !activeToolName) && typeof window < "u" && /\/desk(\/|$)/.test(window.location.pathname) && tools.some(_temp2$11), $[28] = activeTool, $[29] = activeToolName, $[30] = tools, $[31] = t12) : t12 = $[31];
	let isLegacyDeskRedirect = t12, t13, t14;
	$[32] !== isLegacyDeskRedirect || $[33] !== router ? (t13 = () => {
		isLegacyDeskRedirect && router.navigateUrl({
			path: window.location.pathname.replace(/\/desk/, "/structure"),
			replace: !0
		});
	}, t14 = [isLegacyDeskRedirect, router], $[32] = isLegacyDeskRedirect, $[33] = router, $[34] = t13, $[35] = t14) : (t13 = $[34], t14 = $[35]), (0, import_react.useEffect)(t13, t14);
	let getErrorScreen = _temp3$8, t15;
	$[36] === Navbar ? t15 = $[37] : (t15 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Navbar, {}), $[36] = Navbar, $[37] = t15);
	let t16;
	$[38] !== navbarContextValue || $[39] !== t15 ? (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavbarContext.Provider, {
		value: navbarContextValue,
		children: t15
	}), $[38] = navbarContextValue, $[39] = t15, $[40] = t16) : t16 = $[40];
	let t17;
	$[41] === Symbol.for("react.memo_cache_sentinel") ? (t17 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UnclaimedProjectNudge, {}), $[41] = t17) : t17 = $[41];
	let t18;
	$[42] === isLegacyDeskRedirect ? t18 = $[43] : (t18 = isLegacyDeskRedirect && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RedirectingScreen, {}), $[42] = isLegacyDeskRedirect, $[43] = t18);
	let t19;
	$[44] !== activeTool || $[45] !== defaultRouteTools ? (t19 = !activeTool && defaultRouteTools.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NoToolsScreen, {}), $[44] = activeTool, $[45] = defaultRouteTools, $[46] = t19) : t19 = $[46];
	let t20;
	$[47] !== activeTool || $[48] !== activeToolName || $[49] !== isLegacyDeskRedirect || $[50] !== tools.length ? (t20 = tools.length > 0 && !activeTool && activeToolName && !isLegacyDeskRedirect && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolNotFoundScreen, { toolName: activeToolName }), $[47] = activeTool, $[48] = activeToolName, $[49] = isLegacyDeskRedirect, $[50] = tools.length, $[51] = t20) : t20 = $[51];
	let t21;
	$[52] === searchFullscreenOpen ? t21 = $[53] : (t21 = searchFullscreenOpen && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SearchFullscreenPortalCard, {
		ref: setSearchFullscreenPortalEl,
		overflow: "auto"
	}), $[52] = searchFullscreenOpen, $[53] = t21);
	let t22 = activeTool?.name, t23 = `The ${activeTool?.name} tool crashed`, t24;
	$[54] === Symbol.for("react.memo_cache_sentinel") ? (t24 = detectViteDevServerStopped && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DetectViteDevServerStopped, {}), $[54] = t24) : t24 = $[54];
	let t25;
	$[55] !== ActiveToolLayout || $[56] !== activeTool || $[57] !== activeToolName ? (t25 = activeTool && activeToolName && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RouteScope, {
		scope: activeToolName,
		__unsafe_disableScopedSearchParams: activeTool.router?.__unsafe_disableScopedSearchParams,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Suspense, {
			fallback: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingBlock, { showText: !0 }),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActiveToolLayout, { activeTool }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolMountTimer, {
				toolName: activeTool.name,
				t0Ref: toolMountT0Ref
			})]
		})
	}), $[55] = ActiveToolLayout, $[56] = activeTool, $[57] = activeToolName, $[58] = t25) : t25 = $[58];
	let t26;
	$[59] !== searchFullscreenOpen || $[60] !== t25 ? (t26 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		flex: 1,
		hidden: searchFullscreenOpen,
		children: t25
	}), $[59] = searchFullscreenOpen, $[60] = t25, $[61] = t26) : t26 = $[61];
	let t27;
	$[62] !== t22 || $[63] !== t23 || $[64] !== t26 ? (t27 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(StudioErrorBoundary, {
		heading: t23,
		getErrorScreen,
		children: [t24, t26]
	}, t22), $[62] = t22, $[63] = t23, $[64] = t26, $[65] = t27) : t27 = $[65];
	let t28;
	return $[66] !== t16 || $[67] !== t18 || $[68] !== t19 || $[69] !== t20 || $[70] !== t21 || $[71] !== t27 ? (t28 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		"data-ui": "ToolScreen",
		direction: "column",
		height: "fill",
		"data-testid": "studio-layout",
		children: [
			t16,
			t17,
			t18,
			t19,
			t20,
			t21,
			t27
		]
	}), $[66] = t16, $[67] = t18, $[68] = t19, $[69] = t20, $[70] = t21, $[71] = t27, $[72] = t28) : t28 = $[72], t28;
}
function _temp3$8(error) {
	return isDocumentLimitError(error) ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DocumentLimitsUpsellPanel, {}) : null;
}
function _temp2$11(tool_0) {
	return tool_0.name === "structure";
}
function _temp$27(routerState) {
	return typeof routerState.tool == "string" ? routerState.tool : void 0;
}
function pickToolMenuComponent(plugin) {
	return plugin.studio?.components?.toolMenu;
}
function pickNavbarComponent(plugin) {
	return plugin.studio?.components?.navbar;
}
function pickLayoutComponent(plugin) {
	return plugin.studio?.components?.layout;
}
function pickActiveToolLayoutComponent(plugin) {
	return plugin.studio?.components?.activeToolLayout;
}
/**
* @internal
*/
function useToolMenuComponent() {
	let $ = (0, import_compiler_runtime.c)(1), t0;
	return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
		defaultComponent: StudioToolMenu,
		pick: pickToolMenuComponent
	}, $[0] = t0) : t0 = $[0], useMiddlewareComponents(t0);
}
/**
* @internal
*/
function useNavbarComponent() {
	let $ = (0, import_compiler_runtime.c)(1), t0;
	return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
		defaultComponent: StudioNavbar,
		pick: pickNavbarComponent
	}, $[0] = t0) : t0 = $[0], useMiddlewareComponents(t0);
}
/**
* @internal
*/
function useLayoutComponent() {
	let $ = (0, import_compiler_runtime.c)(1), t0;
	return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
		defaultComponent: StudioLayoutComponent,
		pick: pickLayoutComponent
	}, $[0] = t0) : t0 = $[0], useMiddlewareComponents(t0);
}
/**
* @internal
*/
function useActiveToolLayoutComponent() {
	let $ = (0, import_compiler_runtime.c)(1), t0;
	return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
		defaultComponent: StudioActiveToolLayout,
		pick: pickActiveToolLayoutComponent
	}, $[0] = t0) : t0 = $[0], useMiddlewareComponents(t0);
}
function ConfigIssuesButton() {
	let $ = (0, import_compiler_runtime.c)(19), schema = useSchema(), t0;
	$[0] === schema._validation ? t0 = $[1] : (t0 = schema._validation?.filter(_temp2$10) || [], $[0] = schema._validation, $[1] = t0);
	let groupsWithWarnings = t0, configWarnings = getCollectedConfigWarnings(), totalWarnings = groupsWithWarnings.length + configWarnings.length, scheme = useColorSchemeValue(), { t } = useTranslation$1(), dialogId = (0, import_react.useId)(), [isDialogOpen, setDialogOpen] = (0, import_react.useState)(!1), [buttonElement, setButtonElement] = (0, import_react.useState)(null), t1;
	$[2] === Symbol.for("react.memo_cache_sentinel") ? (t1 = () => setDialogOpen(!0), $[2] = t1) : t1 = $[2];
	let handleOpen = t1, t2;
	$[3] === buttonElement ? t2 = $[4] : (t2 = () => {
		setDialogOpen(!1), buttonElement && buttonElement.focus();
	}, $[3] = buttonElement, $[4] = t2);
	let handleClose = t2;
	if (totalWarnings === 0) return null;
	let t3;
	$[5] === t ? t3 = $[6] : (t3 = t("configuration-issues.button.label"), $[5] = t, $[6] = t3);
	let t4;
	$[7] === t ? t4 = $[8] : (t4 = t("configuration-issues.button.tooltip"), $[7] = t, $[8] = t4);
	let t5;
	$[9] !== scheme || $[10] !== t4 ? (t5 = {
		scheme,
		content: t4
	}, $[9] = scheme, $[10] = t4, $[11] = t5) : t5 = $[11];
	let t6;
	$[12] !== isDialogOpen || $[13] !== t3 || $[14] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusButton, {
		icon: WarningOutlineIcon,
		mode: "bleed",
		onClick: handleOpen,
		ref: setButtonElement,
		selected: isDialogOpen,
		tone: "caution",
		"aria-label": t3,
		tooltipProps: t5
	}), $[12] = isDialogOpen, $[13] = t3, $[14] = t5, $[15] = t6) : t6 = $[15];
	let t7 = isDialogOpen && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog$1, {
		header: "Configuration issues",
		width: 2,
		onClickOutside: handleClose,
		onClose: handleClose,
		scheme,
		id: dialogId,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 4,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
					gap: 3,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
							as: "h2",
							size: 1,
							weight: "medium",
							children: [
								"Found ",
								totalWarnings,
								" configuration warning",
								totalWarnings === 1 ? "" : "s"
							]
						}),
						" ",
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
							muted: !0,
							size: 1,
							children: "Configuration checks are only performed during development and will not be visible in production builds"
						})
					]
				}),
				configWarnings.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
					gap: 3,
					children: configWarnings.map(_temp3$7)
				}),
				groupsWithWarnings.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SchemaProblemGroups, { problemGroups: groupsWithWarnings })
			]
		})
	}), t8;
	return $[16] !== t6 || $[17] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [t6, t7] }), $[16] = t6, $[17] = t7, $[18] = t8) : t8 = $[18], t8;
}
function _temp3$7(warning, index) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		padding: 3,
		radius: 2,
		shadow: 1,
		tone: "caution",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 2,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				weight: "medium",
				children: "Divergent auth config"
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				style: { whiteSpace: "pre-wrap" },
				children: warning.message
			})]
		})
	}, `${warning.type}-${warning.projectId}-${index}`);
}
function _temp2$10(group) {
	return group.problems.some(_temp$26);
}
function _temp$26(problem) {
	return problem.severity === "warning";
}
var TrialDialogViewed = defineEvent({
	name: "Trial Dialog Viewed",
	version: 1,
	description: "User viewed a dialog or popover related to free trial"
});
var TrialDialogDismissed = defineEvent({
	name: "Trial Dialog Dismissed",
	version: 1,
	description: "User dismissed a dialog or popover related to free trial"
});
var TrialDialogCTAClicked = defineEvent({
	name: "Trial Dialog CTA Clicked",
	version: 1,
	description: "User clicked a CTA in a dialog or popover related to free trial"
});
function getTrialStage({ showOnLoad, dialogId }) {
	return showOnLoad && dialogId.toLowerCase() === "free-upgrade-popover" ? "trialStarted" : showOnLoad && dialogId.toLowerCase() === "trial-ending-popover" ? "trialEndingSoon" : showOnLoad && dialogId.toLowerCase() === "project-downgraded-to-free" ? "trialEnded" : !showOnLoad && dialogId.toLowerCase() === "after-trial-upgrade" ? "postTrial" : "trialActive";
}
/**
* Absolute positioned button to close the dialog.
*/
var StyledButton = gt(Button$1$1).withConfig({
	displayName: "StyledButton",
	componentId: "sc-q0hem5-0"
})`position:absolute;top:12px;right:12px;z-index:20;background:transparent;border-radius:9999px;box-shadow:none;color:white;--card-fg-color:white;:hover{--card-fg-color:white;}`;
var Image$2 = gt.img.withConfig({
	displayName: "Image",
	componentId: "sc-q0hem5-1"
})`object-fit:cover;width:100%;height:100%;height:196px;`;
var StyledDialog$1 = gt(Dialog$1).withConfig({
	displayName: "StyledDialog",
	componentId: "sc-q0hem5-2"
})`> [data-ui='DialogCard']{max-width:22.5rem;}`;
function DialogContent(t0) {
	let $ = (0, import_compiler_runtime.c)(39), { onClose, onOpenNext, onOpenUrlCallback, content, open } = t0, t1;
	$[0] === onClose ? t1 = $[1] : (t1 = function handleClose() {
		onClose("xClick");
	}, $[0] = onClose, $[1] = t1);
	let handleClose = t1, t2;
	$[2] === onClose ? t2 = $[3] : (t2 = function handleClickOutside() {
		onClose("outsideClick");
	}, $[2] = onClose, $[3] = t2);
	let handleClickOutside = t2, t3;
	$[4] === onClose ? t3 = $[5] : (t3 = function handleCTAClose() {
		onClose("ctaClicked");
	}, $[4] = onClose, $[5] = t3);
	let handleCTAClose = t3, schemeValue = useColorSchemeValue();
	if (!open) return null;
	let t4;
	$[6] !== content.secondaryButton || $[7] !== handleClose ? (t4 = content.secondaryButton?.text ? {
		text: content.secondaryButton.text,
		mode: "bleed",
		tone: "default",
		onClick: handleClose
	} : void 0, $[6] = content.secondaryButton, $[7] = handleClose, $[8] = t4) : t4 = $[8];
	let t5 = content.ctaButton?.text, t6;
	$[9] !== content.ctaButton || $[10] !== handleCTAClose || $[11] !== onOpenNext || $[12] !== onOpenUrlCallback ? (t6 = content.ctaButton?.action === "openUrl" ? {
		href: content.ctaButton.url,
		target: "_blank",
		rel: "noopener noreferrer",
		as: "a",
		onClick: onOpenUrlCallback
	} : { onClick: content.ctaButton?.action === "openNext" ? onOpenNext : handleCTAClose }, $[9] = content.ctaButton, $[10] = handleCTAClose, $[11] = onOpenNext, $[12] = onOpenUrlCallback, $[13] = t6) : t6 = $[13];
	let t7;
	$[14] !== t5 || $[15] !== t6 ? (t7 = {
		text: t5,
		mode: "default",
		tone: "primary",
		...t6
	}, $[14] = t5, $[15] = t6, $[16] = t7) : t7 = $[16];
	let t8;
	$[17] !== t4 || $[18] !== t7 ? (t8 = {
		cancelButton: t4,
		confirmButton: t7
	}, $[17] = t4, $[18] = t7, $[19] = t8) : t8 = $[19];
	let t9;
	$[20] === handleClose ? t9 = $[21] : (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StyledButton, {
		icon: CloseIcon,
		mode: "bleed",
		tone: "default",
		onClick: handleClose,
		tabIndex: -1,
		tooltipProps: null
	}), $[20] = handleClose, $[21] = t9);
	let t10;
	$[22] === content.image ? t10 = $[23] : (t10 = content.image && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Image$2, {
		src: content.image.asset.url,
		alt: content.image.asset.altText ?? ""
	}), $[22] = content.image, $[23] = t10);
	let t11;
	$[24] === content.headingText ? t11 = $[25] : (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingX: 2,
		marginTop: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Heading, {
			size: 2,
			children: content.headingText
		})
	}), $[24] = content.headingText, $[25] = t11);
	let t12;
	$[26] === content.descriptionText ? t12 = $[27] : (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		marginTop: 4,
		paddingBottom: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UpsellDescriptionSerializer, { blocks: content.descriptionText })
	}), $[26] = content.descriptionText, $[27] = t12);
	let t13;
	$[28] !== t11 || $[29] !== t12 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		padding: 3,
		direction: "column",
		children: [t11, t12]
	}), $[28] = t11, $[29] = t12, $[30] = t13) : t13 = $[30];
	let t14;
	return $[31] !== handleClickOutside || $[32] !== onClose || $[33] !== schemeValue || $[34] !== t10 || $[35] !== t13 || $[36] !== t8 || $[37] !== t9 ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(StyledDialog$1, {
		id: "free-trial-modal",
		onClose,
		onClickOutside: handleClickOutside,
		padding: !1,
		__unstable_hideCloseButton: !0,
		scheme: schemeValue,
		footer: t8,
		children: [
			t9,
			t10,
			t13
		]
	}), $[31] = handleClickOutside, $[32] = onClose, $[33] = schemeValue, $[34] = t10, $[35] = t13, $[36] = t8, $[37] = t9, $[38] = t14) : t14 = $[38], t14;
}
var CenteredStroke = gt.div.withConfig({
	displayName: "CenteredStroke",
	componentId: "sc-ow3y84-0"
})`position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);`;
var SvgFilledOutline = (t0) => {
	let $ = (0, import_compiler_runtime.c)(8), { daysLeft, totalDays } = t0, progress = totalDays - daysLeft, percentage = Math.round(progress / totalDays * 100), strokeDasharray = 2 * Math.PI * 10, strokeDashOffset = strokeDasharray * ((100 - percentage) / 100), t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = { transform: "rotate(-90deg)" }, $[0] = t1) : t1 = $[0];
	let t2 = percentage > 75 ? yellow[600].hex : purple[400].hex, t3;
	$[1] === t2 ? t3 = $[2] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", {
		r: 10,
		cx: 10.6,
		cy: 10.6,
		fill: "transparent",
		strokeWidth: 1.2,
		stroke: t2
	}), $[1] = t2, $[2] = t3);
	let t4;
	$[3] === strokeDashOffset ? t4 = $[4] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("circle", {
		r: 10,
		cx: 10.6,
		cy: 10.6,
		fill: "transparent",
		strokeWidth: 1.2,
		stroke: "var(--card-border-color)",
		strokeDasharray,
		strokeDashoffset: strokeDashOffset
	}), $[3] = strokeDashOffset, $[4] = t4);
	let t5;
	return $[5] !== t3 || $[6] !== t4 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CenteredStroke, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
		xmlns: "http://www.w3.org/2000/svg",
		width: 21.2,
		height: 21.2,
		viewBox: "0 0 21.2 21.2",
		style: t1,
		children: [t3, t4]
	}) }) }), $[5] = t3, $[6] = t4, $[7] = t5) : t5 = $[7], t5;
};
function FreeTrialButtonTopbar(t0) {
	let $ = (0, import_compiler_runtime.c)(14), { ref, toggleShowContent, daysLeft, totalDays } = t0, { t } = useTranslation$1(), t1;
	$[0] !== daysLeft || $[1] !== t ? (t1 = daysLeft ? t("free-trial.tooltip.days-count", { count: daysLeft }) : t("free-trial.tooltip.trial-finished"), $[0] = daysLeft, $[1] = t, $[2] = t1) : t1 = $[2];
	let t2;
	$[3] === t1 ? t2 = $[4] : (t2 = { content: t1 }, $[3] = t1, $[4] = t2);
	let t3;
	$[5] === Symbol.for("react.memo_cache_sentinel") ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 0,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BoltIcon, {})
	}), $[5] = t3) : t3 = $[5];
	let t4;
	$[6] !== daysLeft || $[7] !== totalDays ? (t4 = daysLeft > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SvgFilledOutline, {
		daysLeft,
		totalDays
	}), $[6] = daysLeft, $[7] = totalDays, $[8] = t4) : t4 = $[8];
	let t5;
	return $[9] !== ref || $[10] !== t2 || $[11] !== t4 || $[12] !== toggleShowContent ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Button$1$1, {
		mode: "bleed",
		onClick: toggleShowContent,
		ref,
		tooltipProps: t2,
		children: [t3, t4]
	}), $[9] = ref, $[10] = t2, $[11] = t4, $[12] = toggleShowContent, $[13] = t5) : t5 = $[13], t5;
}
function FreeTrialButtonSidebar(t0) {
	let $ = (0, import_compiler_runtime.c)(7), { ref, toggleShowContent, daysLeft } = t0, { t } = useTranslation$1(), t1;
	$[0] !== daysLeft || $[1] !== t ? (t1 = daysLeft ? t("user-menu.action.free-trial", { count: daysLeft }) : t("user-menu.action.free-trial-finished"), $[0] = daysLeft, $[1] = t, $[2] = t1) : t1 = $[2];
	let t2;
	return $[3] !== ref || $[4] !== t1 || $[5] !== toggleShowContent ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		ref,
		icon: BoltIcon,
		justify: "flex-start",
		mode: "bleed",
		size: "large",
		onClick: toggleShowContent,
		text: t1
	}), $[3] = ref, $[4] = t1, $[5] = toggleShowContent, $[6] = t2) : t2 = $[6], t2;
}
/**
* @internal
*/
var useFreeTrialContext = () => {
	let context = (0, import_react.useContext)(FreeTrialContext);
	if (!context) throw Error("useFreeTrial must be used within a FreeTrialProvider");
	return context;
};
var Image$1 = gt.img.withConfig({
	displayName: "Image",
	componentId: "sc-4oq7qe-0"
})`object-fit:cover;width:100%;height:100%;height:180px;`;
function PopoverContent(t0) {
	let $ = (0, import_compiler_runtime.c)(26), { content, handleClose, handleOpenNext } = t0, t1;
	$[0] === content.image ? t1 = $[1] : (t1 = content.image && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Image$1, {
		src: content.image.asset.url,
		alt: content.image.asset.altText ?? ""
	}), $[0] = content.image, $[1] = t1);
	let t2;
	$[2] === content.headingText ? t2 = $[3] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingX: 2,
		marginTop: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Heading, {
			size: 1,
			children: content.headingText
		})
	}), $[2] = content.headingText, $[3] = t2);
	let t3;
	$[4] === content.descriptionText ? t3 = $[5] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		marginTop: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UpsellDescriptionSerializer, { blocks: content.descriptionText })
	}), $[4] = content.descriptionText, $[5] = t3);
	let t4;
	$[6] !== t2 || $[7] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		padding: 3,
		direction: "column",
		children: [t2, t3]
	}), $[6] = t2, $[7] = t3, $[8] = t4) : t4 = $[8];
	let t5;
	$[9] !== content.secondaryButton || $[10] !== handleClose ? (t5 = content.secondaryButton?.text && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		mode: "bleed",
		text: content.secondaryButton.text,
		tone: "default",
		onClick: () => handleClose("xClick")
	}), $[9] = content.secondaryButton, $[10] = handleClose, $[11] = t5) : t5 = $[11];
	let t6 = content.ctaButton?.text, t7;
	$[12] !== content.ctaButton || $[13] !== handleClose || $[14] !== handleOpenNext ? (t7 = content.ctaButton?.action === "openUrl" ? {
		href: content.ctaButton.url,
		target: "_blank",
		rel: "noopener noreferrer",
		as: "a"
	} : { onClick: content.ctaButton?.action === "openNext" ? handleOpenNext : () => handleClose("ctaClicked") }, $[12] = content.ctaButton, $[13] = handleClose, $[14] = handleOpenNext, $[15] = t7) : t7 = $[15];
	let t8;
	$[16] !== t6 || $[17] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		mode: "default",
		tooltipProps: null,
		text: t6,
		autoFocus: !0,
		tone: "primary",
		...t7
	}), $[16] = t6, $[17] = t7, $[18] = t8) : t8 = $[18];
	let t9;
	$[19] !== t5 || $[20] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		gap: 3,
		justify: "flex-end",
		padding: 3,
		children: [t5, t8]
	}), $[19] = t5, $[20] = t8, $[21] = t9) : t9 = $[21];
	let t10;
	return $[22] !== t1 || $[23] !== t4 || $[24] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		radius: 3,
		overflow: "hidden",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Container, {
			width: 0,
			children: [
				t1,
				t4,
				t9
			]
		})
	}), $[22] = t1, $[23] = t4, $[24] = t9, $[25] = t10) : t10 = $[25], t10;
}
function FreeTrial(t0) {
	let $ = (0, import_compiler_runtime.c)(57), { type } = t0, { data, showDialog, showOnLoad, toggleShowContent } = useFreeTrialContext(), scheme = useColorSchemeValue(), telemetry = useTelemetry(), [ref, setRef] = (0, import_react.useState)(null), [showPopover, setShowPopover] = (0, import_react.useState)(!1), t1, t2;
	$[0] === ref ? (t1 = $[1], t2 = $[2]) : (t1 = () => {
		ref && setShowPopover(!0);
	}, t2 = [ref], $[0] = ref, $[1] = t1, $[2] = t2), (0, import_react.useEffect)(t1, t2);
	let t3;
	$[3] === toggleShowContent ? t3 = $[4] : (t3 = () => toggleShowContent(!0), $[3] = toggleShowContent, $[4] = t3);
	let closeAndReOpen = t3, t4;
	$[5] !== ref || $[6] !== toggleShowContent ? (t4 = () => {
		ref?.focus(), toggleShowContent(!1);
	}, $[5] = ref, $[6] = toggleShowContent, $[7] = t4) : t4 = $[7];
	let toggleDialog = t4, t5;
	$[8] !== data || $[9] !== showOnLoad || $[10] !== telemetry || $[11] !== toggleDialog ? (t5 = (dialogType) => (action) => {
		let dialog = data?.showOnLoad || data?.showOnClick;
		dialog && telemetry.log(TrialDialogDismissed, {
			dialogId: dialog.id,
			dialogRevision: dialog._rev,
			dialogType,
			source: "studio",
			trialDaysLeft: data.daysLeft,
			dialogTrialStage: getTrialStage({
				showOnLoad,
				dialogId: dialog.id
			}),
			dialogDismissAction: action
		}), toggleDialog();
	}, $[8] = data, $[9] = showOnLoad, $[10] = telemetry, $[11] = toggleDialog, $[12] = t5) : t5 = $[12];
	let handleClose = t5, t6;
	$[13] !== closeAndReOpen || $[14] !== data || $[15] !== showOnLoad || $[16] !== telemetry ? (t6 = (action_0) => () => {
		let dialog_0 = data?.showOnLoad || data?.showOnClick;
		dialog_0 && telemetry.log(TrialDialogCTAClicked, {
			dialogId: dialog_0.id,
			dialogRevision: dialog_0._rev,
			dialogType: "modal",
			source: "studio",
			trialDaysLeft: data.daysLeft,
			dialogTrialStage: getTrialStage({
				showOnLoad,
				dialogId: dialog_0.id
			}),
			dialogCtaType: action_0 === "openURL" ? "upgrade" : "learnMore"
		}), closeAndReOpen();
	}, $[13] = closeAndReOpen, $[14] = data, $[15] = showOnLoad, $[16] = telemetry, $[17] = t6) : t6 = $[17];
	let handleDialogCTAClick = t6, t7;
	$[18] !== closeAndReOpen || $[19] !== data || $[20] !== telemetry ? (t7 = () => {
		data?.showOnLoad && telemetry.log(TrialDialogCTAClicked, {
			dialogId: data.showOnLoad.id,
			dialogRevision: data.showOnLoad._rev,
			dialogType: "popover",
			source: "studio",
			trialDaysLeft: data.daysLeft,
			dialogTrialStage: getTrialStage({
				showOnLoad: !0,
				dialogId: data.showOnLoad.id
			}),
			dialogCtaType: "learnMore"
		}), closeAndReOpen();
	}, $[18] = closeAndReOpen, $[19] = data, $[20] = telemetry, $[21] = t7) : t7 = $[21];
	let handlePopoverCTAClick = t7, t8;
	$[22] !== closeAndReOpen || $[23] !== data || $[24] !== telemetry ? (t8 = () => {
		data?.showOnClick && telemetry.log(TrialDialogViewed, {
			dialogId: data.showOnClick.id,
			dialogRevision: data.showOnClick._rev,
			dialogTrigger: "fromClick",
			dialogType: "modal",
			source: "studio",
			trialDaysLeft: data.daysLeft,
			dialogTrialStage: getTrialStage({
				showOnLoad: !0,
				dialogId: data.showOnClick.id
			})
		}), closeAndReOpen();
	}, $[22] = closeAndReOpen, $[23] = data, $[24] = telemetry, $[25] = t8) : t8 = $[25];
	let handleOnTrialButtonClick = t8;
	if (!data?.id) return null;
	let dialogToRender = showOnLoad ? data.showOnLoad : data.showOnClick;
	if (!dialogToRender) return null;
	let t9;
	$[26] !== data.daysLeft || $[27] !== data.totalDays || $[28] !== handleOnTrialButtonClick || $[29] !== type ? (t9 = type === "sidebar" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FreeTrialButtonSidebar, {
		toggleShowContent: handleOnTrialButtonClick,
		daysLeft: data.daysLeft,
		ref: setRef
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FreeTrialButtonTopbar, {
		toggleShowContent: handleOnTrialButtonClick,
		daysLeft: data.daysLeft,
		totalDays: data.totalDays,
		ref: setRef
	}), $[26] = data.daysLeft, $[27] = data.totalDays, $[28] = handleOnTrialButtonClick, $[29] = type, $[30] = t9) : t9 = $[30];
	let button = t9;
	if (dialogToRender?.dialogType === "popover") {
		let t10 = showDialog && showPopover, t11 = type === "sidebar" ? "top" : "bottom-end", t12;
		$[31] === handleClose ? t12 = $[32] : (t12 = handleClose("popover"), $[31] = handleClose, $[32] = t12);
		let t13;
		$[33] !== dialogToRender || $[34] !== handlePopoverCTAClick || $[35] !== t12 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContent, {
			content: dialogToRender,
			handleClose: t12,
			handleOpenNext: handlePopoverCTAClick
		}), $[33] = dialogToRender, $[34] = handlePopoverCTAClick, $[35] = t12, $[36] = t13) : t13 = $[36];
		let t14;
		return $[37] !== button || $[38] !== scheme || $[39] !== t10 || $[40] !== t11 || $[41] !== t13 ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Popover$1, {
			open: t10,
			size: 0,
			scheme,
			radius: 3,
			tone: "default",
			portal: !0,
			placement: t11,
			content: t13,
			children: button
		}), $[37] = button, $[38] = scheme, $[39] = t10, $[40] = t11, $[41] = t13, $[42] = t14) : t14 = $[42], t14;
	}
	let t10;
	$[43] === handleClose ? t10 = $[44] : (t10 = handleClose("modal"), $[43] = handleClose, $[44] = t10);
	let t11, t12;
	$[45] === handleDialogCTAClick ? (t11 = $[46], t12 = $[47]) : (t11 = handleDialogCTAClick("openNext"), t12 = handleDialogCTAClick("openURL"), $[45] = handleDialogCTAClick, $[46] = t11, $[47] = t12);
	let t13;
	$[48] !== dialogToRender || $[49] !== showDialog || $[50] !== t10 || $[51] !== t11 || $[52] !== t12 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogContent, {
		content: dialogToRender,
		onClose: t10,
		onOpenNext: t11,
		onOpenUrlCallback: t12,
		open: showDialog
	}), $[48] = dialogToRender, $[49] = showDialog, $[50] = t10, $[51] = t11, $[52] = t12, $[53] = t13) : t13 = $[53];
	let t14;
	return $[54] !== button || $[55] !== t13 ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [button, t13] }), $[54] = button, $[55] = t13, $[56] = t14) : t14 = $[56], t14;
}
function getFetchKey({ seenBefore, trialState }) {
	return `${trialState ?? ""}:${seenBefore ?? ""}`;
}
/**
* @internal
*/
var FreeTrialProvider = (t0) => {
	let $ = (0, import_compiler_runtime.c)(37), { children } = t0, router = useRouter(), [dismissedFetchKey, setDismissedFetchKey] = (0, import_react.useState)(null), [manualDialogOpen, setManualDialogOpen] = (0, import_react.useState)(!1), t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = { apiVersion: "2023-12-11" }, $[0] = t1) : t1 = $[0];
	let client = useClient(t1), telemetry = useTelemetry(), seenBefore, t2, trialState;
	if ($[1] !== router.state._searchParams) {
		let searchParams = new URLSearchParams(router.state._searchParams);
		trialState = searchParams.get("trialState"), seenBefore = searchParams.get("seenBefore"), t2 = getFetchKey({
			seenBefore,
			trialState
		}), $[1] = router.state._searchParams, $[2] = seenBefore, $[3] = t2, $[4] = trialState;
	} else seenBefore = $[2], t2 = $[3], trialState = $[4];
	let fetchKey = t2, t3;
	$[5] !== seenBefore || $[6] !== trialState ? (t3 = () => new BehaviorSubject({
		seenBefore,
		trialState
	}), $[5] = seenBefore, $[6] = trialState, $[7] = t3) : t3 = $[7];
	let [params$] = (0, import_react.useState)(t3), t4, t5;
	$[8] !== params$ || $[9] !== seenBefore || $[10] !== trialState ? (t4 = () => {
		params$.next({
			seenBefore,
			trialState
		});
	}, t5 = [
		params$,
		seenBefore,
		trialState
	], $[8] = params$, $[9] = seenBefore, $[10] = trialState, $[11] = t4, $[12] = t5) : (t4 = $[11], t5 = $[12]), (0, import_react.useEffect)(t4, t5);
	let t6;
	if ($[13] !== client || $[14] !== params$) {
		let t7;
		$[16] === client ? t7 = $[17] : (t7 = (params) => {
			let queryParams = new URLSearchParams();
			queryParams.append("studioVersion", SANITY_VERSION), params.trialState && queryParams.append("trialState", params.trialState), params.seenBefore && queryParams.append("seenBefore", params.seenBefore);
			let queryURL = queryParams.get("trialState") ? "/journey/trial/override" : "/journey/trial";
			return client.observable.request({ url: `${queryURL}?${queryParams.toString()}` }).pipe(map((response) => ({
				key: getFetchKey(params),
				response
			})), catchError(_temp2$9));
		}, $[16] = client, $[17] = t7), t6 = params$.pipe(distinctUntilChanged(_temp$25), switchMap(t7)), $[13] = client, $[14] = params$, $[15] = t6;
	} else t6 = $[15];
	let trial = useSyncObservable(t6, null), data = trial?.response ?? null, showOnLoad = !!data?.showOnLoad && trial?.key === fetchKey && dismissedFetchKey !== fetchKey, showDialog = showOnLoad || manualDialogOpen, t7, t8;
	$[18] !== data || $[19] !== showDialog || $[20] !== showOnLoad || $[21] !== telemetry ? (t7 = () => {
		let dialog = data?.showOnLoad;
		showDialog && showOnLoad && dialog && telemetry.log(TrialDialogViewed, {
			dialogId: dialog.id,
			dialogRevision: dialog._rev,
			dialogTrialStage: getTrialStage({
				showOnLoad,
				dialogId: dialog.id
			}),
			dialogTrigger: showOnLoad ? "auto" : "fromClick",
			dialogType: dialog.dialogType,
			source: "studio",
			trialDaysLeft: data.daysLeft
		});
	}, t8 = [
		showDialog,
		data,
		showOnLoad,
		telemetry
	], $[18] = data, $[19] = showDialog, $[20] = showOnLoad, $[21] = telemetry, $[22] = t7, $[23] = t8) : (t7 = $[22], t8 = $[23]), (0, import_react.useEffect)(t7, t8);
	let t9;
	$[24] !== client || $[25] !== data?.showOnLoad || $[26] !== fetchKey || $[27] !== showOnLoad ? (t9 = (t10) => {
		let closeAndReOpen = t10 !== void 0 && t10;
		showOnLoad ? (setDismissedFetchKey(fetchKey), setManualDialogOpen(closeAndReOpen), data?.showOnLoad?.id && client.request({
			url: `/journey/trial/${data?.showOnLoad.id}`,
			method: "POST"
		})) : setManualDialogOpen(_temp3$6);
	}, $[24] = client, $[25] = data?.showOnLoad, $[26] = fetchKey, $[27] = showOnLoad, $[28] = t9) : t9 = $[28];
	let toggleShowContent = t9, t10;
	$[29] !== data || $[30] !== showDialog || $[31] !== showOnLoad || $[32] !== toggleShowContent ? (t10 = {
		data,
		showDialog,
		toggleShowContent,
		showOnLoad
	}, $[29] = data, $[30] = showDialog, $[31] = showOnLoad, $[32] = toggleShowContent, $[33] = t10) : t10 = $[33];
	let t11;
	return $[34] !== children || $[35] !== t10 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FreeTrialContext.Provider, {
		value: t10,
		children
	}), $[34] = children, $[35] = t10, $[36] = t11) : t11 = $[36], t11;
};
function _temp$25(prev, next) {
	return prev.trialState === next.trialState && prev.seenBefore === next.seenBefore;
}
function _temp2$9() {
	return EMPTY;
}
function _temp3$6(p) {
	return !p;
}
var STATE_TITLES = {
	loading: "Checking…",
	"logged-in": "",
	"logged-out": "Signed out",
	"no-access": ""
};
var Media = gt.div.withConfig({
	displayName: "Media",
	componentId: "sc-oqasvc-0"
})`width:${(props) => props.$size === "small" ? "25px" : "41px"};height:${(props) => props.$size === "small" ? "25px" : "41px"};border-radius:0.25rem;padding:0;svg{width:100%;height:100%;border-radius:inherit;}`;
var WorkspacePreviewIcon = (t0) => {
	let $ = (0, import_compiler_runtime.c)(5), { icon, size: t1 } = t0, size = t1 === void 0 ? "small" : t1, t2;
	$[0] === icon ? t2 = $[1] : (t2 = createIcon(icon), $[0] = icon, $[1] = t2);
	let iconComponent = t2, t3;
	return $[2] !== iconComponent || $[3] !== size ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Media, {
		$size: size,
		children: iconComponent
	}), $[2] = iconComponent, $[3] = size, $[4] = t3) : t3 = $[4], t3;
};
var createIcon = (Icon) => {
	if ((0, import_react_is.isValidElementType)(Icon)) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {});
	if ((0, import_react.isValidElement)(Icon)) return Icon;
};
function WorkspacePreview(props) {
	let $ = (0, import_compiler_runtime.c)(21), { state, subtitle, selected, title, icon, iconRight } = props, t0;
	$[0] === iconRight ? t0 = $[1] : (t0 = createIcon(iconRight), $[0] = iconRight, $[1] = t0);
	let iconRightComponent = t0, t1;
	$[2] === icon ? t1 = $[3] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspacePreviewIcon, {
		icon,
		size: "small"
	}), $[2] = icon, $[3] = t1);
	let t2;
	$[4] === title ? t2 = $[5] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		textOverflow: "ellipsis",
		weight: "medium",
		children: title
	}), $[4] = title, $[5] = t2);
	let t3;
	$[6] === subtitle ? t3 = $[7] : (t3 = subtitle && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		muted: !0,
		size: 1,
		textOverflow: "ellipsis",
		children: subtitle
	}), $[6] = subtitle, $[7] = t3);
	let t4;
	$[8] !== t2 || $[9] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		flex: 1,
		gap: 2,
		children: [t2, t3]
	}), $[8] = t2, $[9] = t3, $[10] = t4) : t4 = $[10];
	let t5;
	$[11] === state ? t5 = $[12] : (t5 = state && STATE_TITLES[state] && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingLeft: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			muted: !0,
			textOverflow: "ellipsis",
			children: STATE_TITLES[state]
		})
	}), $[11] = state, $[12] = t5);
	let t6;
	$[13] !== iconRightComponent || $[14] !== selected ? (t6 = (selected || iconRightComponent) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		gap: 4,
		paddingLeft: 3,
		paddingRight: 2,
		children: [selected && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CheckmarkIcon, {}) }), iconRightComponent && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			muted: !0,
			children: iconRightComponent
		})]
	}), $[13] = iconRightComponent, $[14] = selected, $[15] = t6) : t6 = $[15];
	let t7;
	return $[16] !== t1 || $[17] !== t4 || $[18] !== t5 || $[19] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		flex: "none",
		gap: 3,
		children: [
			t1,
			t4,
			t5,
			t6
		]
	}), $[16] = t1, $[17] = t4, $[18] = t5, $[19] = t6, $[20] = t7) : t7 = $[20], t7;
}
var LogoMarkContainer = gt(Card).attrs({
	overflow: "hidden",
	radius: 2
}).withConfig({
	displayName: "LogoMarkContainer",
	componentId: "sc-m1eaiy-0"
})`height:${25}px;width:${25}px;`;
var StyledCard = gt(Card).withConfig({
	displayName: "StyledCard",
	componentId: "sc-m1eaiy-1"
})`border-radius:${({ theme }) => rem(theme.sanity.radius[2])};display:flex;outline:none;text-decoration:none;&:focus-visible{box-shadow:${({ theme }) => focusRingStyle$1({
	base: theme.sanity.color.base,
	focusRing: {
		...theme.sanity.focusRing,
		offset: 1
	}
})};}`;
/**
* Home button in the main navbar.
*
* - Displays the workspace icon only.
*/
function HomeButton() {
	let $ = (0, import_compiler_runtime.c)(7), { activeWorkspace } = useActiveWorkspace(), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = { state: {} }, $[0] = t0) : t0 = $[0];
	let { href: rootHref, onClick: handleRootClick } = useStateLink(t0), t1;
	$[1] === activeWorkspace.icon ? t1 = $[2] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		align: "center",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LogoMarkContainer, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
			align: "center",
			height: "fill",
			justify: "center",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspacePreviewIcon, {
				icon: activeWorkspace.icon,
				size: "small"
			})
		}) })
	}), $[1] = activeWorkspace.icon, $[2] = t1);
	let t2;
	return $[3] !== handleRootClick || $[4] !== rootHref || $[5] !== t1 ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StyledCard, {
		as: "a",
		href: rootHref,
		onClick: handleRootClick,
		children: t1
	}), $[3] = handleRootClick, $[4] = rootHref, $[5] = t1, $[6] = t2) : t2 = $[6], t2;
}
function getStoredToken(projectId) {
	if (supportsLocalStorage) try {
		let raw = localStorage.getItem(getAuthTokenStorageKey(projectId));
		return raw ? JSON.parse(raw)?.token : void 0;
	} catch {
		return;
	}
}
function resolveApiHost(apiHost) {
	if (apiHost) return apiHost;
	if (isStaging) return "https://api.sanity.work";
}
async function callAuthId(client) {
	try {
		return typeof (await client.request({
			uri: "/auth/id",
			tag: "auth.probe"
		}))?.id == "string" ? AUTHENTICATED : UNAUTHENTICATED;
	} catch (err) {
		return err?.statusCode === 401 || console.warn("Workspace auth probe failed; treating as unauthenticated:", err), UNAUTHENTICATED;
	}
}
var cache = /* @__PURE__ */ new Map();
function cacheKey(input) {
	let auth = input.token ? `tok:${input.token}` : "cookie";
	return `${input.apiHost ?? "default"}|${input.projectId}|${auth}`;
}
function buildProbe(input, options = {}) {
	let apiHost = resolveApiHost(input.apiHost), token = getStoredToken(input.projectId), factory = options.clientFactory ?? createClient, key = cacheKey({
		apiHost,
		projectId: input.projectId,
		token
	}), existing = cache.get(key);
	if (existing) return existing;
	let client = factory({
		...AUTH_CLIENT_OPTIONS,
		projectId: input.projectId,
		dataset: input.dataset,
		...apiHost ? { apiHost } : {},
		...token ? {
			token,
			ignoreBrowserTokenWarning: !0
		} : { withCredentials: !0 }
	}), tokenKey = getAuthTokenStorageKey(input.projectId), cookieKey = getCookieAuthStateKey(input.projectId), observable$ = using(() => {
		let cookieState = token ? null : createBroadcastState(cookieKey);
		return {
			cookieState,
			unsubscribe: () => cookieState?.dispose()
		};
	}, (resource) => {
		let { cookieState } = resource, cookieTicks$ = cookieState?.value.pipe(skip(1)) ?? EMPTY;
		return merge$1(typeof window > "u" ? EMPTY : fromEvent(window, "storage").pipe(filter((e) => e.key === tokenKey)), cookieTicks$).pipe(startWith(void 0), switchMap(() => defer(() => callAuthId(client))), distinctUntilChanged());
	}).pipe(share({
		connector: () => new ReplaySubject(1),
		resetOnRefCountZero: () => timer(1500)
	}));
	return cache.set(key, observable$), observable$;
}
/**
* Probe whether the current user is authenticated against a workspace's project.
*
* Hits the project-scoped `/auth/id` endpoint, which only returns whether the
* caller has a valid session — no user object, no current-user PII.
*
* Independent of the full `AuthStore`: does not write to localStorage or
* BroadcastChannels, so probing many workspaces never poisons the active
* workspace's auth state. The probe does *listen* to the cookie auth
* broadcast (cookie/dual probes only) and to `storage` events (all probes)
* to re-probe when another tab logs in or out.
*
* Probes are deduped by `(apiHost, projectId, token)`. Workspaces in the same
* project that share a token (or both rely on the cookie) share a single
* underlying request via `share`. The BroadcastChannel and DOM listeners
* are created on first subscribe and disposed when the last subscriber
* unsubscribes (with a short grace window so hover-preload hits the cache).
*
* @internal
*/
function probeWorkspaceAuth(input) {
	return buildProbe(input);
}
/**
* A hook that returns whether the current user can invite members to the project.
*
* @internal
*/
function useCanInviteProjectMembers(opts) {
	let $ = (0, import_compiler_runtime.c)(7), t0;
	$[0] === opts ? t0 = $[1] : (t0 = opts || {}, $[0] = opts, $[1] = t0);
	let { enabled: t1 } = t0, enabled = t1 === void 0 || t1, projectStore = useProjectStore(), t2;
	$[2] === projectStore ? t2 = $[3] : (t2 = projectStore.getGrants().pipe(map(_temp3$5)), $[2] = projectStore, $[3] = t2);
	let result$ = t2, t3;
	return $[4] !== enabled || $[5] !== result$ ? (t3 = enabled ? result$ : of(!1), $[4] = enabled, $[5] = result$, $[6] = t3) : t3 = $[6], useObservable(t3, !1);
}
function _temp3$5(grants) {
	return !!grants["sanity.project.members"]?.some(_temp2$8);
}
function _temp2$8(p) {
	return p.grants.some(_temp$24);
}
function _temp$24(g) {
	return g.name === "invite";
}
function ManageMenu$1(t0) {
	let $ = (0, import_compiler_runtime.c)(30), { multipleWorkspaces } = t0, { projectId } = useWorkspace(), { value: project } = useProject(), { activeWorkspace } = useActiveWorkspace(), envAwareWebsiteUrl = useEnvAwareSanityWebsiteUrl(), canInviteMembers = useCanInviteProjectMembers(), { t } = useTranslation$1(), t1 = multipleWorkspaces ? 3 : 4, t2;
	$[0] === activeWorkspace.icon ? t2 = $[1] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspacePreviewIcon, {
		icon: activeWorkspace.icon,
		size: "large"
	}), $[0] = activeWorkspace.icon, $[1] = t2);
	let t3 = project?.displayName, t4;
	$[2] === t3 ? t4 = $[3] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 0,
		children: t3
	}), $[2] = t3, $[3] = t4);
	let t5;
	$[4] === activeWorkspace.title ? t5 = $[5] : (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 2,
		weight: "medium",
		children: activeWorkspace.title
	}), $[4] = activeWorkspace.title, $[5] = t5);
	let t6;
	$[6] !== t4 || $[7] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		marginLeft: 2,
		gap: 2,
		children: [t4, t5]
	}), $[6] = t4, $[7] = t5, $[8] = t6) : t6 = $[8];
	let t7;
	$[9] !== t2 || $[10] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		children: [t2, t6]
	}), $[9] = t2, $[10] = t6, $[11] = t7) : t7 = $[11];
	let t8 = `${envAwareWebsiteUrl}/manage/project/${projectId}`, t9;
	$[12] === t ? t9 = $[13] : (t9 = t("user-menu.action.manage-project-aria-label"), $[12] = t, $[13] = t9);
	let t10;
	$[14] === Symbol.for("react.memo_cache_sentinel") ? (t10 = { "--card-border-color": "unset" }, $[14] = t10) : t10 = $[14];
	let t11;
	$[15] !== t8 || $[16] !== t9 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		mode: "bleed",
		as: "a",
		href: t8,
		target: "_blank",
		icon: CogIcon,
		text: t9,
		style: t10
	}), $[15] = t8, $[16] = t9, $[17] = t11) : t11 = $[17];
	let t12;
	$[18] !== canInviteMembers || $[19] !== envAwareWebsiteUrl || $[20] !== projectId || $[21] !== t ? (t12 = canInviteMembers && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		mode: "bleed",
		as: "a",
		href: `${envAwareWebsiteUrl}/manage/project/${projectId}/members?invite=true`,
		target: "_blank",
		icon: AddUserIcon,
		text: t("user-menu.action.invite-members"),
		style: { "--card-border-color": "unset" }
	}), $[18] = canInviteMembers, $[19] = envAwareWebsiteUrl, $[20] = projectId, $[21] = t, $[22] = t12) : t12 = $[22];
	let t13;
	$[23] !== t11 || $[24] !== t12 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		justify: "flex-start",
		gap: 3,
		paddingTop: 4,
		children: [t11, t12]
	}), $[23] = t11, $[24] = t12, $[25] = t13) : t13 = $[25];
	let t14;
	return $[26] !== t1 || $[27] !== t13 || $[28] !== t7 ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		paddingX: 4,
		paddingTop: 4,
		paddingBottom: t1,
		children: [t7, t13]
	}), $[26] = t1, $[27] = t13, $[28] = t7, $[29] = t14) : t14 = $[29], t14;
}
/**
* A single row in the workspace switcher menu. Each row probes
* `/auth/id` independently, so the list renders immediately on open and
* resolves badges per-workspace as their probes settle.
*
* @internal
*/
function WorkspaceMenuItem(t0) {
	let $ = (0, import_compiler_runtime.c)(17), { workspace, isSelected, scrollbarWidth } = t0, t1;
	$[0] !== workspace.apiHost || $[1] !== workspace.dataset || $[2] !== workspace.projectId ? (t1 = probeWorkspaceAuth({
		projectId: workspace.projectId,
		dataset: workspace.dataset,
		apiHost: workspace.apiHost
	}), $[0] = workspace.apiHost, $[1] = workspace.dataset, $[2] = workspace.projectId, $[3] = t1) : t1 = $[3];
	let probe = useObservable(t1), t2 = STATE_TITLES[probe ? probe.authenticated ? "logged-in" : workspace.auth.LoginComponent ? "logged-out" : "no-access" : "loading"] || void 0, t3 = isSelected ? CheckmarkIcon : void 0, t4;
	$[4] === workspace.icon ? t4 = $[5] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspacePreviewIcon, {
		icon: workspace.icon,
		size: "small"
	}), $[4] = workspace.icon, $[5] = t4);
	let t5 = workspace?.title || workspace.name, t6 = `calc(1.25rem - ${scrollbarWidth}px)`, t7;
	$[6] === t6 ? t7 = $[7] : (t7 = {
		marginLeft: "1rem",
		marginRight: t6
	}, $[6] = t6, $[7] = t7);
	let t8;
	return $[8] !== isSelected || $[9] !== t2 || $[10] !== t3 || $[11] !== t4 || $[12] !== t5 || $[13] !== t7 || $[14] !== workspace.basePath || $[15] !== workspace.subtitle ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
		as: "a",
		href: workspace.basePath,
		badgeText: t2,
		iconRight: t3,
		pressed: isSelected,
		preview: t4,
		selected: isSelected,
		__unstable_subtitle: workspace.subtitle,
		text: t5,
		style: t7,
		__unstable_space: 0
	}), $[8] = isSelected, $[9] = t2, $[10] = t3, $[11] = t4, $[12] = t5, $[13] = t7, $[14] = workspace.basePath, $[15] = workspace.subtitle, $[16] = t8) : t8 = $[16], t8;
}
var POPOVER_PROPS = {
	constrainSize: !0,
	fallbackPlacements: ["bottom-end", "bottom"],
	placement: "bottom-end",
	tone: "default"
};
function WorkspaceMenuButton() {
	let $ = (0, import_compiler_runtime.c)(30), { visibleWorkspaces } = useVisibleWorkspaces(), { activeWorkspace } = useActiveWorkspace(), { t } = useTranslation$1(), [scrollbarWidth, setScrollbarWidth] = (0, import_react.useState)(0), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = (node) => {
		if (node) {
			let hasScroll = node.scrollHeight > node.clientHeight;
			setScrollbarWidth(hasScroll ? node.offsetWidth - node.clientWidth : 0);
		}
	}, $[0] = t0) : t0 = $[0];
	let stackRef = t0, t1;
	$[1] === visibleWorkspaces ? t1 = $[2] : (t1 = () => {
		visibleWorkspaces.forEach(_temp$23);
	}, $[1] = visibleWorkspaces, $[2] = t1);
	let handlePreload = t1, t2;
	$[3] === t ? t2 = $[4] : (t2 = t("workspaces.select-workspace-tooltip"), $[3] = t, $[4] = t2);
	let t3;
	$[5] === activeWorkspace.title ? t3 = $[6] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		textOverflow: "ellipsis",
		weight: "medium",
		children: activeWorkspace.title
	}) }), $[5] = activeWorkspace.title, $[6] = t3);
	let t4;
	$[7] === Symbol.for("react.memo_cache_sentinel") ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDownIcon, {})
	}), $[7] = t4) : t4 = $[7];
	let t5;
	$[8] === t3 ? t5 = $[9] : (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
		mode: "bleed",
		padding: 2,
		width: "fill",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			align: "center",
			gap: 2,
			children: [t3, t4]
		})
	}), $[8] = t3, $[9] = t5);
	let t6;
	$[10] !== t2 || $[11] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip$1, {
		content: t2,
		portal: !0,
		children: t5
	}), $[10] = t2, $[11] = t5, $[12] = t6) : t6 = $[12];
	let t7;
	$[13] !== handlePreload || $[14] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		onPointerEnter: handlePreload,
		onFocus: handlePreload,
		children: t6
	}), $[13] = handlePreload, $[14] = t6, $[15] = t7) : t7 = $[15];
	let t8;
	$[16] === Symbol.for("react.memo_cache_sentinel") ? (t8 = {
		maxWidth: "350px",
		minWidth: "250px",
		overflowY: "hidden"
	}, $[16] = t8) : t8 = $[16];
	let t9 = visibleWorkspaces.length > 1, t10;
	$[17] === t9 ? t10 = $[18] : (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ManageMenu$1, { multipleWorkspaces: t9 }), $[17] = t9, $[18] = t10);
	let t11;
	$[19] !== activeWorkspace.name || $[20] !== scrollbarWidth || $[21] !== t || $[22] !== visibleWorkspaces ? (t11 = visibleWorkspaces.length > 1 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuDivider, { style: { padding: 0 } }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Box, {
		paddingTop: 2,
		paddingBottom: 1,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			paddingRight: 5,
			paddingLeft: 4,
			paddingBottom: 3,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 0,
				weight: "medium",
				children: t("workspaces.action.switch-workspace")
			})
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
			ref: stackRef,
			gap: 1,
			style: {
				overflowY: "auto",
				maxHeight: "40vh"
			},
			children: visibleWorkspaces.map((workspace_0) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspaceMenuItem, {
				workspace: workspace_0,
				isSelected: workspace_0.name === activeWorkspace.name,
				scrollbarWidth
			}, workspace_0.name))
		})]
	})] }), $[19] = activeWorkspace.name, $[20] = scrollbarWidth, $[21] = t, $[22] = visibleWorkspaces, $[23] = t11) : t11 = $[23];
	let t12;
	$[24] !== t10 || $[25] !== t11 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Menu, {
		padding: 0,
		style: t8,
		children: [t10, t11]
	}), $[24] = t10, $[25] = t11, $[26] = t12) : t12 = $[26];
	let t13;
	return $[27] !== t12 || $[28] !== t7 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuButton$1, {
		button: t7,
		id: "workspace-menu",
		menu: t12,
		popover: POPOVER_PROPS
	}), $[27] = t12, $[28] = t7, $[29] = t13) : t13 = $[29], t13;
}
function _temp$23(workspace) {
	probeWorkspaceAuth({
		projectId: workspace.projectId,
		dataset: workspace.dataset,
		apiHost: workspace.apiHost
	}).pipe(take(1)).subscribe();
}
function AppearanceMenu$1(t0) {
	let $ = (0, import_compiler_runtime.c)(4), { setScheme } = t0, { t } = useTranslation$1(), options = useColorSchemeOptions(setScheme, t), t1;
	$[0] === options ? t1 = $[1] : (t1 = options.map(_temp$22), $[0] = options, $[1] = t1);
	let t2;
	return $[2] === t1 ? t2 = $[3] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		borderTop: !0,
		flex: "none",
		padding: 2,
		overflow: "auto",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
			as: "ul",
			gap: 1,
			children: t1
		})
	}), $[2] = t1, $[3] = t2), t2;
}
function _temp$22(t0) {
	let { icon, label, name, onSelect, selected, title } = t0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		as: "li",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			"aria-label": label,
			icon,
			iconRight: selected && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CheckmarkIcon, {}),
			justify: "flex-start",
			mode: "bleed",
			onClick: onSelect,
			selected,
			size: "large",
			text: title
		})
	}, name);
}
function LocaleMenu$1() {
	(0, import_compiler_runtime.c)(9);
	let { changeLocale, currentLocale, locales } = useLocale();
	return null;
}
function ManageMenu() {
	let $ = (0, import_compiler_runtime.c)(17), { projectId } = useWorkspace(), { t } = useTranslation$1(), envAwareWebsiteUrl = useEnvAwareSanityWebsiteUrl(), canInviteMembers = useCanInviteProjectMembers(), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		as: "li",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FreeTrial, { type: "sidebar" })
	}), $[0] = t0) : t0 = $[0];
	let t1;
	$[1] === t ? t1 = $[2] : (t1 = t("user-menu.action.manage-project-aria-label"), $[1] = t, $[2] = t1);
	let t2 = `${envAwareWebsiteUrl}/manage/project/${projectId}`, t3;
	$[3] === t ? t3 = $[4] : (t3 = t("user-menu.action.manage-project"), $[3] = t, $[4] = t3);
	let t4;
	$[5] !== t1 || $[6] !== t2 || $[7] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		as: "li",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			"aria-label": t1,
			as: "a",
			href: t2,
			icon: CogIcon,
			justify: "flex-start",
			mode: "bleed",
			size: "large",
			target: "_blank",
			text: t3
		})
	}), $[5] = t1, $[6] = t2, $[7] = t3, $[8] = t4) : t4 = $[8];
	let t5;
	$[9] !== canInviteMembers || $[10] !== envAwareWebsiteUrl || $[11] !== projectId || $[12] !== t ? (t5 = canInviteMembers && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		as: "li",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			"aria-label": t("user-menu.action.invite-members-aria-label"),
			as: "a",
			href: `${envAwareWebsiteUrl}/manage/project/${projectId}/members?invite=true`,
			icon: AddUserIcon,
			justify: "flex-start",
			mode: "bleed",
			size: "large",
			target: "_blank",
			text: t("user-menu.action.invite-members")
		})
	}), $[9] = canInviteMembers, $[10] = envAwareWebsiteUrl, $[11] = projectId, $[12] = t, $[13] = t5) : t5 = $[13];
	let t6;
	return $[14] !== t4 || $[15] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		borderTop: !0,
		flex: "none",
		padding: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			as: "ul",
			gap: 1,
			children: [
				t0,
				t4,
				t5
			]
		})
	}), $[14] = t4, $[15] = t5, $[16] = t6) : t6 = $[16], t6;
}
var ANIMATION_TRANSITION = {
	bounce: 0,
	damping: 20,
	mass: .5,
	stiffness: 200,
	type: "spring"
};
var BACKDROP_VARIANTS = {
	open: { opacity: 1 },
	closed: { opacity: 0 }
};
var INNER_CARD_VARIANTS = {
	open: { x: "0%" },
	closed: { x: "-100%" }
};
var Root$3 = gt(Layer).withConfig({
	displayName: "Root",
	componentId: "sc-0h5m5g-0"
})`position:fixed;top:0;left:0;width:100%;height:100%;`;
var BackdropMotion = gt(motion.create(Card)).withConfig({
	displayName: "BackdropMotion",
	componentId: "sc-0h5m5g-1"
})`position:absolute;top:0;left:0;right:0;bottom:0;background:var(--card-shadow-penumbra-color);`;
var InnerCardMotion = gt(motion.create(Card)).withConfig({
	displayName: "InnerCardMotion",
	componentId: "sc-0h5m5g-2"
})`position:relative;pointer-events:all;flex-direction:column;height:100%;min-width:200px;max-width:280px;overflow:auto;`;
var NavDrawer = (0, import_react.memo)(function NavDrawer(props) {
	let $ = (0, import_compiler_runtime.c)(23), { __internal_actions: actions, activeToolName, isOpen, onClose, tools } = props, setScheme = useColorSchemeSetValue(), { auth, currentUser } = useWorkspace(), workspaces = useWorkspaces(), ToolMenu = useToolMenuComponent(), { t } = useTranslation$1(), t0;
	$[0] === onClose ? t0 = $[1] : (t0 = (event) => {
		event.key === "Escape" && onClose();
	}, $[0] = onClose, $[1] = t0);
	let handleKeyDown = t0, t1;
	$[2] === onClose ? t1 = $[3] : (t1 = (action) => {
		action?.(), onClose();
	}, $[2] = onClose, $[3] = t1);
	let handleActionClick = t1, t2;
	$[4] !== actions || $[5] !== handleActionClick ? (t2 = actions?.filter(_temp$21)?.map((action_0) => {
		let { render: ActionComponent } = action_0;
		return ActionComponent ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionComponent, {}, action_0.name) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			icon: action_0?.icon,
			justify: "flex-start",
			mode: "bleed",
			onClick: () => handleActionClick(action_0.onAction),
			selected: action_0.selected,
			size: "large",
			text: action_0.title,
			width: "fill"
		}, action_0.name);
	}), $[4] = actions, $[5] = handleActionClick, $[6] = t2) : t2 = $[6];
	let actionNodes = t2, t3;
	$[7] !== ToolMenu || $[8] !== actionNodes || $[9] !== activeToolName || $[10] !== auth || $[11] !== currentUser?.email || $[12] !== currentUser?.name || $[13] !== handleKeyDown || $[14] !== isOpen || $[15] !== onClose || $[16] !== setScheme || $[17] !== t || $[18] !== tools || $[19] !== workspaces ? (t3 = isOpen && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(es2015_default, {
		returnFocus: !0,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Root$3, {
			onKeyDown: handleKeyDown,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(BackdropMotion, {
				animate: "open",
				"data-open": isOpen,
				exit: "closed",
				initial: "closed",
				onClick: onClose,
				transition: ANIMATION_TRANSITION,
				variants: BACKDROP_VARIANTS
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(InnerCardMotion, {
				animate: "open",
				"data-open": isOpen,
				display: "flex",
				exit: "closed",
				height: "fill",
				initial: "closed",
				shadow: 1,
				transition: ANIMATION_TRANSITION,
				variants: INNER_CARD_VARIANTS,
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
						borderBottom: !0,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
							gap: 3,
							padding: 3,
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
								align: "center",
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
									flex: 1,
									align: "center",
									paddingRight: 2,
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CapabilityGate, {
										capability: "globalUserMenu",
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
											flex: 1,
											align: "center",
											children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserAvatar, {
												size: 1,
												user: "me"
											}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
												flex: 1,
												marginLeft: 3,
												title: currentUser?.name || currentUser?.email,
												children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
													size: 1,
													textOverflow: "ellipsis",
													weight: "medium",
													children: currentUser?.name || currentUser?.email
												})
											})]
										})
									})
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
									icon: CloseIcon,
									mode: "bleed",
									onClick: onClose,
									tooltipProps: { content: t("user-menu.close-menu") }
								})]
							}), workspaces.length > 1 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
								flex: 1,
								gap: 1,
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(HomeButton, {}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspaceMenuButton, {})]
							})]
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
						direction: "column",
						flex: 1,
						justify: "space-between",
						overflow: "auto",
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
							flex: "none",
							padding: 2,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolMenu, {
								activeToolName,
								closeSidebar: onClose,
								context: "sidebar",
								isSidebarOpen: isOpen,
								tools
							})
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
							direction: "column",
							children: [
								actionNodes && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
									flex: "none",
									padding: 2,
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
										gap: 1,
										children: actionNodes
									})
								}),
								setScheme && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AppearanceMenu$1, { setScheme }),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(LocaleMenu$1, {}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CapabilityGate, {
									capability: "globalUserMenu",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ManageMenu, {})
								})
							]
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CapabilityGate, {
						capability: "globalUserMenu",
						children: auth.logout && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
							flex: "none",
							padding: 2,
							borderTop: !0,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
								iconRight: LeaveIcon,
								justify: "flex-start",
								mode: "bleed",
								onClick: auth.logout,
								size: "large",
								text: t("user-menu.action.sign-out")
							}) })
						})
					})
				]
			})]
		})
	}), $[7] = ToolMenu, $[8] = actionNodes, $[9] = activeToolName, $[10] = auth, $[11] = currentUser?.email, $[12] = currentUser?.name, $[13] = handleKeyDown, $[14] = isOpen, $[15] = onClose, $[16] = setScheme, $[17] = t, $[18] = tools, $[19] = workspaces, $[20] = t3) : t3 = $[20];
	let t4;
	return $[21] === t3 ? t4 = $[22] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AnimatePresence, { children: t3 }), $[21] = t3, $[22] = t4), t4;
});
function _temp$21(v) {
	return v.location === "sidebar";
}
function isQueryMatch(query, option) {
	let lowercaseTitle = option.title.toLowerCase();
	return query.trim().toLowerCase().split(/[\s\-_]+/).every((word) => lowercaseTitle.includes(word));
}
function sortResultRelevance(query, a, b) {
	let titleA = a.title.toLowerCase(), titleB = b.title.toLowerCase(), lowercaseQuery = query.trim().toLowerCase();
	return titleA.startsWith(lowercaseQuery) && !titleB.startsWith(lowercaseQuery) ? -1 : titleB.startsWith(lowercaseQuery) && !titleA.startsWith(lowercaseQuery) ? 1 : titleA.includes(lowercaseQuery) && !titleB.includes(lowercaseQuery) ? -1 : titleB.includes(lowercaseQuery) && !titleA.includes(lowercaseQuery) ? 1 : 0;
}
function filterOptions(options, query, getI18nText) {
	return options.map((option) => ({
		...option,
		title: getI18nText({
			...option,
			title: option?.title || option.id
		}).title
	})).sort((a, b) => a.title.localeCompare(b.title)).filter((option) => isQueryMatch(query, option)).sort((a, b) => sortResultRelevance(query, a, b));
}
var StyledPopover = /* @__PURE__ */ gt(Popover$1).withConfig({
	displayName: "StyledPopover",
	componentId: "sc-4a7h9e-0"
})(() => ft`[data-ui='Popover__wrapper']{width:320px;display:flex;flex-direction:column;border-radius:${({ theme }) => theme.sanity.radius[3]}px;position:relative;overflow:hidden;overflow:clip;}`);
var StyledDialog = /* @__PURE__ */ gt(Dialog$1).withConfig({
	displayName: "StyledDialog",
	componentId: "sc-4a7h9e-1"
})(() => ft`[data-ui='DialogCard']{justify-content:flex-start;}`);
var RootFlex = gt(Flex).withConfig({
	displayName: "RootFlex",
	componentId: "sc-4a7h9e-2"
})``;
var PopoverHeaderCard = gt(Card).withConfig({
	displayName: "PopoverHeaderCard",
	componentId: "sc-4a7h9e-3"
})`min-height:max-content;`;
var DialogHeaderCard = gt(Card).withConfig({
	displayName: "DialogHeaderCard",
	componentId: "sc-4a7h9e-4"
})`min-height:max-content;`;
var PopoverListFlex = /* @__PURE__ */ gt(Flex).withConfig({
	displayName: "PopoverListFlex",
	componentId: "sc-4a7h9e-5"
})((props) => {
	let { $maxDisplayedItems, $itemHeight } = props;
	return ft`--item-height:${$itemHeight}px;--max-items:${$maxDisplayedItems};--list-padding:0.5rem;position:relative;max-height:calc(var(--item-height) * var(--max-items) + var(--list-padding));min-height:calc((var(--item-height) * 1));`;
});
function NewDocumentListOption(props) {
	let $ = (0, import_compiler_runtime.c)(33), { option, currentUser, onClick, preview } = props, { selectedReleaseId } = usePerspective(), t0;
	$[0] !== option.schemaType || $[1] !== option.templateId || $[2] !== selectedReleaseId ? (t0 = {
		template: option.templateId,
		type: option.schemaType,
		version: selectedReleaseId
	}, $[0] = option.schemaType, $[1] = option.templateId, $[2] = selectedReleaseId, $[3] = t0) : t0 = $[3];
	let params = t0, t1;
	$[4] === params ? t1 = $[5] : (t1 = {
		intent: "create",
		params
	}, $[4] = params, $[5] = t1);
	let { onClick: onIntentClick, href } = useIntentLink(t1), t2;
	$[6] !== onClick || $[7] !== onIntentClick || $[8] !== option ? (t2 = (event) => {
		onIntentClick(event), onClick(option);
	}, $[6] = onClick, $[7] = onIntentClick, $[8] = option, $[9] = t2) : t2 = $[9];
	let handleDocumentClick = t2, { title } = useI18nText(option), { icon: Icon } = option, t3;
	$[10] !== Icon || $[11] !== preview ? (t3 = Icon && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
		size: preview === "inline" ? 1 : void 0,
		children: [(0, import_react.isValidElement)(Icon) && Icon, (0, import_react_is.isValidElementType)(Icon) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {})]
	}) }), $[10] = Icon, $[11] = preview, $[12] = t3) : t3 = $[12];
	let t4 = preview === "inline" ? 1 : void 0, t5;
	$[13] !== t4 || $[14] !== title ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: t4,
		children: title
	}), $[13] = t4, $[14] = title, $[15] = t5) : t5 = $[15];
	let t6;
	$[16] !== t3 || $[17] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		gap: 3,
		children: [t3, t5]
	}), $[16] = t3, $[17] = t5, $[18] = t6) : t6 = $[18];
	let cardContent = t6, t7;
	$[19] === currentUser ? t7 = $[20] : (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InsufficientPermissionsMessage, {
		currentUser,
		context: "create-document"
	}), $[19] = currentUser, $[20] = t7);
	let t8;
	$[21] !== cardContent || $[22] !== handleDocumentClick || $[23] !== href || $[24] !== option.hasPermission || $[25] !== option.templateId || $[26] !== preview ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: option.hasPermission ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		as: "a",
		"data-testid": `create-new-${option.templateId}`,
		href,
		marginBottom: 1,
		onClick: handleDocumentClick,
		padding: preview === "inline" ? 3 : 4,
		radius: 2,
		children: cardContent
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		as: "button",
		"data-testid": `create-new-${option.templateId}`,
		disabled: !0,
		marginBottom: 1,
		onClick: handleDocumentClick,
		padding: preview === "inline" ? 3 : 4,
		radius: 2,
		children: cardContent
	}) }), $[21] = cardContent, $[22] = handleDocumentClick, $[23] = href, $[24] = option.hasPermission, $[25] = option.templateId, $[26] = preview, $[27] = t8) : t8 = $[27];
	let t9;
	return $[28] !== option.hasPermission || $[29] !== option.id || $[30] !== t7 || $[31] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip$1, {
		disabled: option.hasPermission,
		portal: !0,
		content: t7,
		children: t8
	}, option.id), $[28] = option.hasPermission, $[29] = option.id, $[30] = t7, $[31] = t8, $[32] = t9) : t9 = $[32], t9;
}
var ContentFlex = gt(Flex).withConfig({
	displayName: "ContentFlex",
	componentId: "sc-rn25kl-0"
})`min-height:100px;`;
function NewDocumentList(props) {
	let $ = (0, import_compiler_runtime.c)(27), { currentUser, loading, onDocumentClick, options, preview, searchQuery, textInputElement } = props, { t } = useTranslation$1(), hasOptions = options.length > 0 && !loading, t0;
	$[0] === onDocumentClick ? t0 = $[1] : (t0 = () => {
		onDocumentClick();
	}, $[0] = onDocumentClick, $[1] = t0);
	let handleDocumentClick = t0, t1;
	$[2] === options ? t1 = $[3] : (t1 = (index) => options[index]?.hasPermission ?? !0, $[2] = options, $[3] = t1);
	let getItemDisabled = t1, t2;
	$[4] !== currentUser || $[5] !== handleDocumentClick || $[6] !== preview ? (t2 = (item) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NewDocumentListOption, {
		currentUser,
		onClick: handleDocumentClick,
		option: item,
		preview
	}, item.id), $[4] = currentUser, $[5] = handleDocumentClick, $[6] = preview, $[7] = t2) : t2 = $[7];
	let renderItem = t2;
	if (loading) {
		let t3;
		return $[8] === Symbol.for("react.memo_cache_sentinel") ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingBlock, { showText: !0 }), $[8] = t3) : t3 = $[8], t3;
	}
	if (!hasOptions && searchQuery) {
		let t3;
		$[9] === searchQuery ? t3 = $[10] : (t3 = { searchQuery }, $[9] = searchQuery, $[10] = t3);
		let t4;
		return $[11] !== t || $[12] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ContentFlex, {
			align: "center",
			flex: 1,
			height: "fill",
			justify: "center",
			padding: 4,
			sizing: "border",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				align: "center",
				muted: !0,
				size: 1,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Translate, {
					t,
					i18nKey: "new-document.no-results",
					values: t3
				})
			})
		}), $[11] = t, $[12] = t3, $[13] = t4) : t4 = $[13], t4;
	}
	if (!hasOptions) {
		let t3;
		$[14] === t ? t3 = $[15] : (t3 = t("new-document.no-document-types-found"), $[14] = t, $[15] = t3);
		let t4;
		return $[16] === t3 ? t4 = $[17] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ContentFlex, {
			align: "center",
			flex: 1,
			height: "fill",
			justify: "center",
			padding: 4,
			sizing: "border",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				align: "center",
				muted: !0,
				size: 1,
				children: t3
			})
		}), $[16] = t3, $[17] = t4), t4;
	}
	let t3;
	$[18] === t ? t3 = $[19] : (t3 = t("new-document.new-document"), $[18] = t, $[19] = t3);
	let t4 = preview === "inline" ? 1 : 2, t5;
	return $[20] !== getItemDisabled || $[21] !== options || $[22] !== renderItem || $[23] !== t3 || $[24] !== t4 || $[25] !== textInputElement ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CommandList, {
		activeItemDataAttr: "data-hovered",
		ariaLabel: t3,
		autoFocus: supportsTouch ? void 0 : "input",
		getItemDisabled,
		inputElement: textInputElement,
		itemHeight: 33,
		items: options,
		overscan: 5,
		padding: t4,
		paddingBottom: 0,
		renderItem
	}), $[20] = getItemDisabled, $[21] = options, $[22] = renderItem, $[23] = t3, $[24] = t4, $[25] = textInputElement, $[26] = t5) : t5 = $[26], t5;
}
/**
* @internal
*/
function NewDocumentButton(props) {
	let $ = (0, import_compiler_runtime.c)(90), { canCreateDocument, modal: t0, loading, options } = props, modal = t0 === void 0 ? "popover" : t0, [open, setOpen] = (0, import_react.useState)(!1), [searchQuery, setSearchQuery] = (0, import_react.useState)(""), popoverRef = (0, import_react.useRef)(null), dialogRef = (0, import_react.useRef)(null), [buttonElement, setButtonElement] = (0, import_react.useState)(null), [searchInputElement, setSearchInputElement] = (0, import_react.useState)(null), { t } = useTranslation$1(), getI18nText = useGetI18nText(options), scheme = useColorSchemeValue(), currentUser = useCurrentUser(), schema = useSchema(), hasNewDocumentOptions = options.length > 0, disabled = !canCreateDocument || !hasNewDocumentOptions, t1;
	$[0] === t ? t1 = $[1] : (t1 = t("new-document.filter-placeholder"), $[0] = t, $[1] = t1);
	let placeholder = t1, t2;
	$[2] === t ? t2 = $[3] : (t2 = t("new-document.title"), $[2] = t, $[3] = t2);
	let title = t2, t3;
	$[4] === t ? t3 = $[5] : (t3 = t("new-document.open-dialog-aria-label"), $[4] = t, $[5] = t3);
	let openDialogAriaLabel = t3, t4;
	if ($[6] !== options || $[7] !== schema) {
		let t5;
		$[9] === schema ? t5 = $[10] : (t5 = (option) => {
			let optionSchema = schema.get(option.schemaType);
			return optionSchema && !isDeprecatedSchemaType(optionSchema);
		}, $[9] = schema, $[10] = t5), t4 = options.filter(t5), $[6] = options, $[7] = schema, $[8] = t4;
	} else t4 = $[8];
	let validOptions = t4, t5;
	$[11] !== getI18nText || $[12] !== searchQuery || $[13] !== validOptions ? (t5 = filterOptions(validOptions, searchQuery, getI18nText), $[11] = getI18nText, $[12] = searchQuery, $[13] = validOptions, $[14] = t5) : t5 = $[14];
	let filteredOptions = t5, t6;
	$[15] === Symbol.for("react.memo_cache_sentinel") ? (t6 = (event) => {
		setSearchQuery(event.currentTarget.value);
	}, $[15] = t6) : t6 = $[15];
	let handleSearchChange = t6, t7;
	$[16] === Symbol.for("react.memo_cache_sentinel") ? (t7 = () => setOpen(_temp$20), $[16] = t7) : t7 = $[16];
	let handleToggleOpen = t7, t8;
	$[17] === buttonElement ? t8 = $[18] : (t8 = () => {
		setOpen(!1), setSearchQuery(""), buttonElement?.focus();
	}, $[17] = buttonElement, $[18] = t8);
	let handleClose = t8, t9;
	$[19] === open ? t9 = $[20] : (t9 = (e) => {
		e.key === "ArrowDown" && !open && setOpen(!0);
	}, $[19] = open, $[20] = t9);
	let handleOpenButtonKeyDown = t9, t10;
	$[21] !== handleClose || $[22] !== open ? (t10 = (e_0) => {
		(e_0.key === "Escape" || e_0.key === "Tab") && open && handleClose();
	}, $[21] = handleClose, $[22] = open, $[23] = t10) : t10 = $[23];
	let handlePopoverKeyDown = t10, t11;
	$[24] === buttonElement ? t11 = $[25] : (t11 = () => [
		buttonElement,
		dialogRef.current,
		popoverRef.current
	], $[24] = buttonElement, $[25] = t11), useClickOutsideEvent(open && handleClose, t11);
	let t12 = modal === "popover" ? "inline" : "default", t13;
	$[26] !== currentUser || $[27] !== filteredOptions || $[28] !== loading || $[29] !== searchInputElement || $[30] !== searchQuery || $[31] !== t12 ? (t13 = {
		currentUser,
		loading,
		onDocumentClick: handleToggleOpen,
		options: filteredOptions,
		searchQuery,
		preview: t12,
		textInputElement: searchInputElement
	}, $[26] = currentUser, $[27] = filteredOptions, $[28] = loading, $[29] = searchInputElement, $[30] = searchQuery, $[31] = t12, $[32] = t13) : t13 = $[32];
	let sharedListProps = t13, t14;
	$[33] !== loading || $[34] !== placeholder || $[35] !== searchQuery ? (t14 = {
		__unstable_disableFocusRing: !0,
		border: !1,
		"data-testid": "new-document-button-search-input",
		defaultValue: searchQuery,
		disabled: loading,
		icon: SearchIcon,
		onChange: handleSearchChange,
		placeholder,
		ref: setSearchInputElement
	}, $[33] = loading, $[34] = placeholder, $[35] = searchQuery, $[36] = t14) : t14 = $[36];
	let sharedTextInputProps = t14, t15 = disabled || loading, t16;
	$[37] !== open || $[38] !== openDialogAriaLabel || $[39] !== t15 ? (t16 = {
		"aria-label": openDialogAriaLabel,
		"data-testid": "new-document-button",
		disabled: t15,
		icon: AddIcon,
		text: "",
		mode: "bleed",
		onClick: handleToggleOpen,
		ref: setButtonElement,
		selected: open
	}, $[37] = open, $[38] = openDialogAriaLabel, $[39] = t15, $[40] = t16) : t16 = $[40];
	let sharedOpenButtonProps = t16, t17;
	bb0: {
		if (!hasNewDocumentOptions) {
			let t18;
			$[41] === t ? t18 = $[42] : (t18 = t("new-document.no-document-types-label"), $[41] = t, $[42] = t18);
			let t19;
			$[43] === t18 ? t19 = $[44] : (t19 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				children: t18
			}), $[43] = t18, $[44] = t19), t17 = t19;
			break bb0;
		}
		if (canCreateDocument) {
			let t18;
			$[45] === t ? t18 = $[46] : (t18 = t("new-document.create-new-document-label"), $[45] = t, $[46] = t18);
			let t19;
			$[47] === t18 ? t19 = $[48] : (t19 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				children: t18
			}), $[47] = t18, $[48] = t19), t17 = t19;
			break bb0;
		}
		let t18;
		$[49] === currentUser ? t18 = $[50] : (t18 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InsufficientPermissionsMessage, {
			currentUser,
			context: "create-any-document"
		}), $[49] = currentUser, $[50] = t18), t17 = t18;
	}
	let tooltipContent = t17, t18 = loading || open, t19;
	$[51] !== scheme || $[52] !== t18 || $[53] !== tooltipContent ? (t19 = {
		content: tooltipContent,
		disabled: t18,
		scheme
	}, $[51] = scheme, $[52] = t18, $[53] = tooltipContent, $[54] = t19) : t19 = $[54];
	let sharedTooltipProps = t19;
	if (modal === "dialog") {
		let t20;
		$[55] === sharedOpenButtonProps ? t20 = $[56] : (t20 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, { ...sharedOpenButtonProps }) }), $[55] = sharedOpenButtonProps, $[56] = t20);
		let t21;
		$[57] !== sharedTooltipProps || $[58] !== t20 ? (t21 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip$1, {
			...sharedTooltipProps,
			children: t20
		}), $[57] = sharedTooltipProps, $[58] = t20, $[59] = t21) : t21 = $[59];
		let t22;
		$[60] !== handleClose || $[61] !== open || $[62] !== scheme || $[63] !== sharedListProps || $[64] !== sharedTextInputProps || $[65] !== title ? (t22 = open && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StyledDialog, {
			header: title,
			id: "create-new-document-dialog",
			onClickOutside: handleClose,
			onClose: handleClose,
			ref: dialogRef,
			scheme,
			width: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RootFlex, {
				direction: "column",
				flex: 1,
				height: "fill",
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogHeaderCard, {
					padding: 2,
					borderBottom: !0,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextInput, {
						"data-testid": "new-document-button-search-input",
						...sharedTextInputProps
					})
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
					direction: "column",
					overflow: "hidden",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NewDocumentList, { ...sharedListProps })
				})]
			})
		}), $[60] = handleClose, $[61] = open, $[62] = scheme, $[63] = sharedListProps, $[64] = sharedTextInputProps, $[65] = title, $[66] = t22) : t22 = $[66];
		let t23;
		return $[67] !== t21 || $[68] !== t22 ? (t23 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [t21, t22] }), $[67] = t21, $[68] = t22, $[69] = t23) : t23 = $[69], t23;
	}
	let t20;
	$[70] === sharedTextInputProps ? t20 = $[71] : (t20 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverHeaderCard, {
		sizing: "border",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
			borderBottom: !0,
			padding: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextInput, {
				...sharedTextInputProps,
				fontSize: 1,
				radius: 1
			})
		}) })
	}), $[70] = sharedTextInputProps, $[71] = t20);
	let t21;
	$[72] === sharedListProps ? t21 = $[73] : (t21 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverListFlex, {
		$itemHeight: 33,
		$maxDisplayedItems: 10,
		direction: "column",
		overflow: "hidden",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NewDocumentList, { ...sharedListProps })
	}), $[72] = sharedListProps, $[73] = t21);
	let t22;
	$[74] !== t20 || $[75] !== t21 ? (t22 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RootFlex, {
		direction: "column",
		flex: 1,
		forwardedAs: es2015_default,
		height: "fill",
		returnFocus: !0,
		children: [t20, t21]
	}), $[74] = t20, $[75] = t21, $[76] = t22) : t22 = $[76];
	let t23;
	$[77] !== handleOpenButtonKeyDown || $[78] !== open || $[79] !== sharedOpenButtonProps ? (t23 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		...sharedOpenButtonProps,
		"aria-expanded": open,
		"aria-haspopup": "true",
		onKeyDown: handleOpenButtonKeyDown
	}) }), $[77] = handleOpenButtonKeyDown, $[78] = open, $[79] = sharedOpenButtonProps, $[80] = t23) : t23 = $[80];
	let t24;
	$[81] !== sharedTooltipProps || $[82] !== t23 ? (t24 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip$1, {
		...sharedTooltipProps,
		children: t23
	}) }), $[81] = sharedTooltipProps, $[82] = t23, $[83] = t24) : t24 = $[83];
	let t25;
	return $[84] !== handlePopoverKeyDown || $[85] !== open || $[86] !== scheme || $[87] !== t22 || $[88] !== t24 ? (t25 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StyledPopover, {
		constrainSize: !0,
		onKeyDown: handlePopoverKeyDown,
		open,
		tone: "default",
		portal: !0,
		radius: 3,
		ref: popoverRef,
		scheme,
		content: t22,
		children: t24
	}), $[84] = handlePopoverKeyDown, $[85] = open, $[86] = scheme, $[87] = t22, $[88] = t24, $[89] = t25) : t25 = $[89], t25;
}
function _temp$20(v) {
	return !v;
}
/**
* @internal
*/
function useNewDocumentOptions() {
	let $ = (0, import_compiler_runtime.c)(14), { __internal: t0 } = useSource(), { staticInitialValueTemplateItems } = t0, t1;
	$[0] === staticInitialValueTemplateItems ? t1 = $[1] : (t1 = { templateItems: staticInitialValueTemplateItems }, $[0] = staticInitialValueTemplateItems, $[1] = t1);
	let [permissions, loading] = useTemplatePermissions(t1), t2;
	$[2] !== permissions || $[3] !== staticInitialValueTemplateItems ? (t2 = permissions?.some((p) => staticInitialValueTemplateItems.some((t) => t.id === p.id)) || !1, $[2] = permissions, $[3] = staticInitialValueTemplateItems, $[4] = t2) : t2 = $[4];
	let canCreateDocument = t2, t3;
	if ($[5] !== permissions || $[6] !== staticInitialValueTemplateItems) {
		let t4;
		$[8] === permissions ? t4 = $[9] : (t4 = (item) => ({
			...item,
			title: item?.title || item.id,
			hasPermission: permissions?.find((p_0) => p_0.id === item.id)?.granted || !1
		}), $[8] = permissions, $[9] = t4), t3 = staticInitialValueTemplateItems.map(t4), $[5] = permissions, $[6] = staticInitialValueTemplateItems, $[7] = t3;
	} else t3 = $[7];
	let optionsWithPermissions = t3, t4;
	return $[10] !== canCreateDocument || $[11] !== loading || $[12] !== optionsWithPermissions ? (t4 = {
		canCreateDocument,
		loading,
		options: optionsWithPermissions
	}, $[10] = canCreateDocument, $[11] = loading, $[12] = optionsWithPermissions, $[13] = t4) : t4 = $[13], t4;
}
var PresenceMenuItem = (0, import_react.memo)(function PresenceMenuItem(props) {
	let $ = (0, import_compiler_runtime.c)(31), { locations, user, focused, onFocus } = props, [menuItemElement, setMenuItemElement] = (0, import_react.useState)(null), { t } = useTranslation$1(), t0, t1;
	$[0] !== focused || $[1] !== menuItemElement ? (t0 = () => {
		focused && menuItemElement && (menuItemElement.focus(), menuItemElement.setAttribute("data-selected", "")), focused || menuItemElement?.removeAttribute("data-selected");
	}, t1 = [menuItemElement, focused], $[0] = focused, $[1] = menuItemElement, $[2] = t0, $[3] = t1) : (t0 = $[2], t1 = $[3]), (0, import_react.useEffect)(t0, t1);
	let t2;
	$[4] !== onFocus || $[5] !== user.id ? (t2 = () => {
		onFocus(user.id);
	}, $[4] = onFocus, $[5] = user.id, $[6] = t2) : t2 = $[6];
	let handleFocus = t2, hasLink, t3;
	if ($[7] !== handleFocus || $[8] !== locations || $[9] !== user) {
		t3 = Symbol.for("react.early_return_sentinel");
		bb0: {
			let lastActiveLocation = orderBy(locations || [], ["lastActiveAt"], ["desc"]).find(_temp$19);
			if (hasLink = !!lastActiveLocation?.documentId, lastActiveLocation) {
				let perspective = getVersionFromId(lastActiveLocation.documentId), t4 = {
					id: getPublishedId(lastActiveLocation.documentId),
					path: toString(lastActiveLocation.path)
				}, t5 = perspective ? [["perspective", perspective]] : [], t6;
				$[12] === user ? t6 = $[13] : (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserAvatar, {
					size: 1,
					user
				}), $[12] = user, $[13] = t6);
				let t7;
				$[14] !== handleFocus || $[15] !== t4 || $[16] !== t5 || $[17] !== t6 || $[18] !== user.displayName ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
					as: IntentLink,
					intent: "edit",
					params: t4,
					searchParams: t5,
					"data-as": "a",
					onFocus: handleFocus,
					preview: t6,
					ref: setMenuItemElement,
					text: user.displayName
				}), $[14] = handleFocus, $[15] = t4, $[16] = t5, $[17] = t6, $[18] = user.displayName, $[19] = t7) : t7 = $[19], t3 = t7;
				break bb0;
			}
		}
		$[7] = handleFocus, $[8] = locations, $[9] = user, $[10] = hasLink, $[11] = t3;
	} else hasLink = $[10], t3 = $[11];
	if (t3 !== Symbol.for("react.early_return_sentinel")) return t3;
	let t4 = !hasLink, t5;
	$[20] !== hasLink || $[21] !== t ? (t5 = hasLink ? void 0 : {
		content: t("presence.not-in-a-document"),
		placement: "left"
	}, $[20] = hasLink, $[21] = t, $[22] = t5) : t5 = $[22];
	let t6;
	$[23] === user ? t6 = $[24] : (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserAvatar, {
		size: 1,
		user
	}), $[23] = user, $[24] = t6);
	let t7;
	return $[25] !== handleFocus || $[26] !== t4 || $[27] !== t5 || $[28] !== t6 || $[29] !== user.displayName ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
		as: "div",
		disabled: t4,
		tooltipProps: t5,
		"data-as": "a",
		onFocus: handleFocus,
		preview: t6,
		ref: setMenuItemElement,
		text: user.displayName
	}), $[25] = handleFocus, $[26] = t4, $[27] = t5, $[28] = t6, $[29] = user.displayName, $[30] = t7) : t7 = $[30], t7;
});
function _temp$19(location) {
	return location.documentId;
}
var StyledMenu$2 = gt(Menu).withConfig({
	displayName: "StyledMenu",
	componentId: "sc-72kgw8-0"
})`max-width:260px;`;
var FooterStack = gt(Stack).withConfig({
	displayName: "FooterStack",
	componentId: "sc-72kgw8-1"
})`position:sticky;bottom:0;background-color:var(--card-bg-color);`;
function PresenceMenu() {
	let $ = (0, import_compiler_runtime.c)(42), presence = useGlobalPresence(), { projectId } = useWorkspace(), scheme = useColorSchemeValue(), { t } = useTranslation$1(), hasPresence = presence.length > 0, [open, setOpen] = (0, import_react.useState)(!1), t0;
	$[0] === open ? t0 = $[1] : (t0 = { enabled: open }, $[0] = open, $[1] = t0);
	let canInviteMembers = useCanInviteProjectMembers(t0), [focusedId, setFocusedId] = (0, import_react.useState)(), t1;
	$[2] === Symbol.for("react.memo_cache_sentinel") ? (t1 = (id) => {
		setFocusedId(id);
	}, $[2] = t1) : t1 = $[2];
	let handleItemFocus = t1, t2;
	$[3] === Symbol.for("react.memo_cache_sentinel") ? (t2 = () => {
		setFocusedId("");
	}, $[3] = t2) : t2 = $[3];
	let handleClearFocusedItem = t2, t3;
	$[4] === Symbol.for("react.memo_cache_sentinel") ? (t3 = () => {
		setFocusedId("");
	}, $[4] = t3) : t3 = $[4];
	let handleClose = t3, t4;
	$[5] === t ? t4 = $[6] : (t4 = t("presence.aria-label"), $[5] = t, $[6] = t4);
	let t5 = hasPresence ? "positive" : void 0, t6;
	$[7] === t ? t6 = $[8] : (t6 = t("presence.tooltip-content"), $[7] = t, $[8] = t6);
	let t7;
	$[9] === t6 ? t7 = $[10] : (t7 = { content: t6 }, $[9] = t6, $[10] = t7);
	let t8;
	$[11] !== t4 || $[12] !== t5 || $[13] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusButton, {
		icon: UsersIcon,
		"aria-label": t4,
		mode: "bleed",
		tone: t5,
		tooltipProps: t7
	}), $[11] = t4, $[12] = t5, $[13] = t7, $[14] = t8) : t8 = $[14];
	let button = t8, t9;
	$[15] === Symbol.for("react.memo_cache_sentinel") ? (t9 = ["bottom"], $[15] = t9) : t9 = $[15];
	let t10;
	$[16] === scheme ? t10 = $[17] : (t10 = {
		constrainSize: !0,
		fallbackPlacements: t9,
		placement: "bottom",
		portal: !0,
		scheme,
		tone: "default"
	}, $[16] = scheme, $[17] = t10);
	let popoverProps = t10, envAwareWebsiteUrl = useEnvAwareSanityWebsiteUrl(), t11;
	$[18] === t ? t11 = $[19] : (t11 = t("presence.aria-label"), $[18] = t, $[19] = t11);
	let t12;
	$[20] === Symbol.for("react.memo_cache_sentinel") ? (t12 = () => setOpen(!0), $[20] = t12) : t12 = $[20];
	let t13;
	$[21] !== focusedId || $[22] !== hasPresence || $[23] !== presence ? (t13 = hasPresence && presence.map((item) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PresenceMenuItem, {
		focused: focusedId === item.user.id,
		onFocus: handleItemFocus,
		locations: item.locations,
		user: item.user
	}, item.user.id)), $[21] = focusedId, $[22] = hasPresence, $[23] = presence, $[24] = t13) : t13 = $[24];
	let t14;
	$[25] !== hasPresence || $[26] !== t ? (t14 = !hasPresence && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		padding: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 3,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				weight: "medium",
				size: 1,
				children: t("presence.no-one-else-title")
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				muted: !0,
				children: t("presence.no-one-else-description")
			})]
		})
	}), $[25] = hasPresence, $[26] = t, $[27] = t14) : t14 = $[27];
	let t15;
	$[28] !== canInviteMembers || $[29] !== envAwareWebsiteUrl || $[30] !== projectId || $[31] !== t ? (t15 = canInviteMembers && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(FooterStack, {
		gap: 1,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuDivider, {}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
			as: "a",
			href: `${envAwareWebsiteUrl}/manage/project/${projectId}/members?invite=true`,
			icon: AddUserIcon,
			onFocus: handleClearFocusedItem,
			rel: "noopener noreferrer",
			target: "_blank",
			text: t("presence.action.manage-members")
		})]
	}), $[28] = canInviteMembers, $[29] = envAwareWebsiteUrl, $[30] = projectId, $[31] = t, $[32] = t15) : t15 = $[32];
	let t16;
	$[33] !== t13 || $[34] !== t14 || $[35] !== t15 ? (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(StyledMenu$2, { children: [
		t13,
		t14,
		t15
	] }), $[33] = t13, $[34] = t14, $[35] = t15, $[36] = t16) : t16 = $[36];
	let t17;
	return $[37] !== button || $[38] !== popoverProps || $[39] !== t11 || $[40] !== t16 ? (t17 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuButton$1, {
		button,
		"aria-label": t11,
		id: "global-presence-menu",
		onOpen: t12,
		menu: t16,
		onClose: handleClose,
		popover: popoverProps
	}), $[37] = button, $[38] = popoverProps, $[39] = t11, $[40] = t16, $[41] = t17) : t17 = $[41], t17;
}
/**
* Checks whether the Sentry feedback tunnel is operational by sending
* a minimal valid envelope (header only, no items — Sentry accepts it
* with a 200 and creates nothing).
*
* @internal
*/
function useFeedbackAvailable(options) {
	let $ = (0, import_compiler_runtime.c)(4), { dsn, skip: t0 } = options, skip = t0 !== void 0 && t0, [available, setAvailable] = (0, import_react.useState)(null), t1, t2;
	return $[0] !== dsn || $[1] !== skip ? (t1 = () => {
		if (skip) {
			setAvailable(!1);
			return;
		}
		let body = `${JSON.stringify({
			dsn,
			sent_at: (/* @__PURE__ */ new Date()).toISOString()
		})}\n`;
		fetch(FEEDBACK_TUNNEL_URL, {
			method: "POST",
			body
		}).then(async (response) => {
			setAvailable(response.ok), await response.arrayBuffer().catch(_temp$18);
		}).catch(() => setAvailable(!1));
	}, t2 = [dsn, skip], $[0] = dsn, $[1] = skip, $[2] = t1, $[3] = t2) : (t1 = $[2], t2 = $[3]), (0, import_react.useEffect)(t1, t2), available;
}
function _temp$18() {}
function useLiveUserApplication() {
	return (0, import_react.useContext)(LiveUserApplicationContext);
}
function usePackageVersionStatus() {
	return (0, import_react.useContext)(PackageVersionInfoContext);
}
/**
* Feedback menu item rendered inside the help menu.
*
* @internal
*/
function FeedbackMenuItem(props) {
	let $ = (0, import_compiler_runtime.c)(8), { onClick } = props, { t } = useTranslation$1(feedbackLocaleNamespace), t0;
	$[0] === t ? t0 = $[1] : (t0 = t("feedback.menu-item"), $[0] = t, $[1] = t0);
	let t1;
	$[2] !== onClick || $[3] !== t0 ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
		icon: FeedbackIcon,
		text: t0,
		onClick
	}), $[2] = onClick, $[3] = t0, $[4] = t1) : t1 = $[4];
	let t2;
	$[5] === Symbol.for("react.memo_cache_sentinel") ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuDivider, {}), $[5] = t2) : t2 = $[5];
	let t3;
	return $[6] === t1 ? t3 = $[7] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [t1, t2] }), $[6] = t1, $[7] = t3), t3;
}
/**
* @internal Not a stable API yet
* @hidden
*/
function getHelpResources(client, locale) {
	return client.withConfig({ apiVersion: "1" }).observable.request({
		url: "/help",
		query: {
			m: [`sanity@${SANITY_VERSION}`],
			locale
		},
		tag: "module.version-check",
		json: !0
	});
}
/**
* Fetch help resources (content for the navbar help menu) from `/help`.
*
* @internal
* @hidden
*/
function useGetHelpResources() {
	let $ = (0, import_compiler_runtime.c)(4), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = { apiVersion: "1" }, $[0] = t0) : t0 = $[0];
	let client = useClient(t0), locale = useCurrentLocale().id, t1;
	return $[1] !== client || $[2] !== locale ? (t1 = getHelpResources(client, locale), $[1] = client, $[2] = locale, $[3] = t1) : t1 = $[3], useLoadable(t1);
}
var ProductAnnouncementCardSeen = defineEvent({
	name: "Product Announcement Card Seen",
	version: 1,
	description: "User viewed the product announcement card"
});
var ProductAnnouncementCardClicked = defineEvent({
	name: "Product Announcement Card Clicked",
	version: 1,
	description: "User clicked the product announcement card"
});
var ProductAnnouncementCardDismissed = defineEvent({
	name: "Product Announcement Card Dismissed",
	version: 1,
	description: "User dismissed the product announcement card"
});
var ProductAnnouncementViewed = defineEvent({
	name: "Product Announcement Viewed",
	version: 1,
	description: "User viewed the product announcement"
});
var ProductAnnouncementLinkClicked = defineEvent({
	name: "Product Announcement Link Clicked",
	version: 1,
	description: "User clicked the link in the product announcement "
});
var ProductAnnouncementModalDismissed = defineEvent({
	name: "Product Announcement Dismissed",
	version: 1,
	description: "User dismissed the product announcement modal "
});
var WhatsNewHelpMenuItemClicked = defineEvent({
	name: "Whats New Help Menu Item Clicked",
	version: 1,
	description: "User clicked the \"Whats new\" help menu item"
});
function useStudioAnnouncements() {
	let $ = (0, import_compiler_runtime.c)(1), context = (0, import_react.useContext)(StudioAnnouncementContext);
	if (!context) {
		let t0;
		return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
			studioAnnouncements: [],
			unseenAnnouncements: [],
			onDialogOpen: _temp$17
		}, $[0] = t0) : t0 = $[0], t0;
	}
	return context;
}
function _temp$17() {}
function StudioAnnouncementsMenuItem(t0) {
	let $ = (0, import_compiler_runtime.c)(9), { text } = t0, { onDialogOpen, studioAnnouncements } = useStudioAnnouncements(), telemetry = useTelemetry(), t1;
	$[0] !== onDialogOpen || $[1] !== studioAnnouncements[0]?._id || $[2] !== studioAnnouncements[0]?.name || $[3] !== studioAnnouncements[0]?.title || $[4] !== telemetry ? (t1 = () => {
		onDialogOpen("help_menu"), telemetry.log(WhatsNewHelpMenuItemClicked, {
			source: "studio",
			announcement_id: studioAnnouncements[0]?._id,
			announcement_title: studioAnnouncements[0]?.title,
			announcement_internal_name: studioAnnouncements[0]?.name,
			studio_version: SANITY_VERSION
		});
	}, $[0] = onDialogOpen, $[1] = studioAnnouncements[0]?._id, $[2] = studioAnnouncements[0]?.name, $[3] = studioAnnouncements[0]?.title, $[4] = telemetry, $[5] = t1) : t1 = $[5];
	let handleOpenDialog = t1;
	if (studioAnnouncements.length === 0) return null;
	let t2;
	return $[6] !== handleOpenDialog || $[7] !== text ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
		tone: "default",
		text,
		onClick: handleOpenDialog
	}), $[6] = handleOpenDialog, $[7] = text, $[8] = t2) : t2 = $[8], t2;
}
/**
* A hook that returns whether the current user can deploy the studio.
*
* @internal
*/
function useCanDeployStudio(t0) {
	let $ = (0, import_compiler_runtime.c)(5), enabled = t0 === void 0 || t0, projectStore = useProjectStore(), t1;
	$[0] === projectStore ? t1 = $[1] : (t1 = projectStore.getGrants().pipe(map(hasDeployStudioGrant)), $[0] = projectStore, $[1] = t1);
	let result$ = t1, t2;
	return $[2] !== enabled || $[3] !== result$ ? (t2 = enabled ? result$ : of(!1), $[2] = enabled, $[3] = result$, $[4] = t2) : t2 = $[4], useObservable(t2, !1);
}
function reload$1() {
	document.location.reload();
}
function ResourcesMenuItems(t0) {
	let $ = (0, import_compiler_runtime.c)(35), { error, isLoading, value, latestTaggedVersion, currentVersion, onOpenStudioVersionDialog, newAutoUpdateVersion } = t0, sections = value?.resources?.sectionArray, { t } = useTranslation$1();
	if (isLoading) {
		let t1;
		return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingBlock, { showText: !0 }), $[0] = t1) : t1 = $[0], t1;
	}
	let t1;
	$[1] === t ? t1 = $[2] : (t1 = t("help-resources.action.join-our-community"), $[1] = t, $[2] = t1);
	let t2;
	$[3] === t1 ? t2 = $[4] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
		as: "a",
		text: t1,
		href: "https://www.sanity.io/exchange/community",
		target: "_blank"
	}), $[3] = t1, $[4] = t2);
	let t3;
	$[5] === t ? t3 = $[6] : (t3 = t("help-resources.action.help-and-support"), $[5] = t, $[6] = t3);
	let t4;
	$[7] === t3 ? t4 = $[8] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
		as: "a",
		text: t3,
		href: "https://www.sanity.io/contact/support",
		target: "_blank"
	}), $[7] = t3, $[8] = t4);
	let t5;
	$[9] === t ? t5 = $[10] : (t5 = t("help-resources.action.contact-sales"), $[9] = t, $[10] = t5);
	let t6;
	$[11] === t5 ? t6 = $[12] : (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
		as: "a",
		text: t5,
		href: "https://www.sanity.io/contact/sales?ref=studio",
		target: "_blank"
	}), $[11] = t5, $[12] = t6);
	let t7;
	$[13] !== t2 || $[14] !== t4 || $[15] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		t2,
		t4,
		t6
	] }), $[13] = t2, $[14] = t4, $[15] = t6, $[16] = t7) : t7 = $[16];
	let fallbackLinks = t7, t8;
	$[17] !== currentVersion || $[18] !== latestTaggedVersion || $[19] !== newAutoUpdateVersion || $[20] !== onOpenStudioVersionDialog ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioVersion, {
		currentVersion,
		newAutoUpdateVersion,
		latestTaggedVersion,
		onOpenStudioVersionDialog
	}), $[17] = currentVersion, $[18] = latestTaggedVersion, $[19] = newAutoUpdateVersion, $[20] = onOpenStudioVersionDialog, $[21] = t8) : t8 = $[21];
	let t10, t9;
	$[22] === Symbol.for("react.memo_cache_sentinel") ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioRegistration, {}), t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuDivider, {}), $[22] = t10, $[23] = t9) : (t10 = $[22], t9 = $[23]);
	let t11;
	$[24] !== error || $[25] !== sections ? (t11 = !error && sections?.map((subSection, i) => subSection ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SubSection, { subSection }), i < sections.length - 1 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuDivider, {})] }, subSection._key) : null), $[24] = error, $[25] = sections, $[26] = t11) : t11 = $[26];
	let t12;
	$[27] !== error || $[28] !== fallbackLinks || $[29] !== value ? (t12 = (value === void 0 || error) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: fallbackLinks }), $[27] = error, $[28] = fallbackLinks, $[29] = value, $[30] = t12) : t12 = $[30];
	let t13;
	return $[31] !== t11 || $[32] !== t12 || $[33] !== t8 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		t8,
		t9,
		t10,
		t11,
		t12
	] }), $[31] = t11, $[32] = t12, $[33] = t8, $[34] = t13) : t13 = $[34], t13;
}
function StudioVersion(t0) {
	let $ = (0, import_compiler_runtime.c)(30), { currentVersion, newAutoUpdateVersion, latestTaggedVersion, onOpenStudioVersionDialog } = t0, { t } = useTranslation$1(), isOutdated = latestTaggedVersion ? (currentVersion?.compareMain?.(latestTaggedVersion) ?? 0) < 0 : !1, versionTone = "positive", t1;
	$[0] === t ? t1 = $[1] : (t1 = t("help-resources.up-to-date"), $[0] = t, $[1] = t1);
	let subtitle = t1, action = onOpenStudioVersionDialog, testId = "menu-item-studio-version";
	if (newAutoUpdateVersion) {
		let t2;
		$[2] !== newAutoUpdateVersion.version || $[3] !== t ? (t2 = t("help-resources.studio-auto-update-now", { newVersion: newAutoUpdateVersion.version }), $[2] = newAutoUpdateVersion.version, $[3] = t, $[4] = t2) : t2 = $[4], subtitle = t2, versionTone = "caution", action = reload$1, testId = "menu-item-update-studio-now";
	} else if (isOutdated) {
		let t2 = latestTaggedVersion?.version, t3;
		$[5] !== t || $[6] !== t2 ? (t3 = t("help-resources.latest-sanity-version", { latestVersion: t2 }), $[5] = t, $[6] = t2, $[7] = t3) : t3 = $[7], subtitle = t3, versionTone = "caution";
	}
	let t2 = action, t3 = testId, t4;
	$[8] === t ? t4 = $[9] : (t4 = t("help-resources.studio"), $[8] = t, $[9] = t4);
	let t5;
	$[10] === t4 ? t5 = $[11] : (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		weight: "medium",
		children: t4
	}), $[10] = t4, $[11] = t5);
	let t6;
	$[12] === subtitle ? t6 = $[13] : (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		muted: !0,
		size: 1,
		children: subtitle
	}), $[12] = subtitle, $[13] = t6);
	let t7;
	$[14] !== t5 || $[15] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		direction: "column",
		flex: 1,
		gap: 2,
		padding: 1,
		children: [t5, t6]
	}), $[14] = t5, $[15] = t6, $[16] = t7) : t7 = $[16];
	let t8 = versionTone, t9;
	$[17] !== currentVersion.version || $[18] !== t ? (t9 = t("help-resources.version", { version: currentVersion.version }), $[17] = currentVersion.version, $[18] = t, $[19] = t9) : t9 = $[19];
	let t10;
	$[20] !== t9 || $[21] !== versionTone ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Badge, {
		tone: t8,
		children: t9
	}), $[20] = t9, $[21] = versionTone, $[22] = t10) : t10 = $[22];
	let t11;
	$[23] !== t10 || $[24] !== t7 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "flex-start",
		children: [t7, t10]
	}), $[23] = t10, $[24] = t7, $[25] = t11) : t11 = $[25];
	let t12;
	return $[26] !== action || $[27] !== t11 || $[28] !== testId ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem, {
		padding: 2,
		onClick: t2,
		"data-testid": t3,
		children: t11
	}), $[26] = action, $[27] = t11, $[28] = testId, $[29] = t12) : t12 = $[29], t12;
}
function StudioRegistration() {
	let $ = (0, import_compiler_runtime.c)(11), { t } = useTranslation$1(), { userApplication } = useLiveUserApplication(), sanityWebsiteUrl = useEnvAwareSanityWebsiteUrl(), projectId = useWorkspaces()[0]?.projectId, canDeployStudio = useCanDeployStudio(!userApplication && false), t0;
	$[0] !== canDeployStudio || $[1] !== projectId || $[2] !== sanityWebsiteUrl ? (t0 = () => {
		if (!projectId || !canDeployStudio) return;
		let url = new URL(`${sanityWebsiteUrl}/manage/project/${projectId}/studios`);
		url.searchParams.set("studio", "add"), url.searchParams.set("origin", window.location.origin), window.open(url, "_blank", "noopener,noreferrer");
	}, $[0] = canDeployStudio, $[1] = projectId, $[2] = sanityWebsiteUrl, $[3] = t0) : t0 = $[3];
	return null;
}
function SubSection(t0) {
	let $ = (0, import_compiler_runtime.c)(4), { subSection } = t0, t1;
	$[0] === subSection?.items ? t1 = $[1] : (t1 = subSection?.items?.map(_temp$16), $[0] = subSection?.items, $[1] = t1);
	let t2;
	return $[2] === t1 ? t2 = $[3] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: t1 }), $[2] = t1, $[3] = t2), t2;
}
function _temp$16(item) {
	if (!item || !item.title) return null;
	switch (item._type) {
		case "externalLink": return item.url ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
			as: "a",
			tone: "default",
			text: item.title,
			href: item.url,
			target: "_blank"
		}, item._key) : null;
		case "internalAction": return item.type ? item.type === "studio-announcements-modal" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioAnnouncementsMenuItem, { text: item.title }, item._key) : item.type === "show-welcome-modal" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, { text: item.title }, item._key) : null;
		default: return null;
	}
}
var MonogramContainer = gt(Card).attrs({
	overflow: "hidden",
	radius: 4
}).withConfig({
	displayName: "MonogramContainer",
	componentId: "sc-jems3l-0"
})`height:${75}px;width:${75}px;`;
function reload() {
	window.location.reload();
}
function getVersionType(ver) {
	return ver.prerelease.length === 0 ? "default" : ver.prerelease.length === 1 && ver.prerelease[0] === "dev" ? "development" : "prerelease";
}
var TruncateBadge = gt(Badge).withConfig({
	displayName: "TruncateBadge",
	componentId: "sc-jems3l-1"
})`display:block;span{display:block;white-space:nowrap;text-overflow:ellipsis;overflow:clip;}`;
var HEX_ONLY = /^[0-9a-fA-F]+$/i;
function resolveGithubURLFromVersion(ver) {
	let preids = ver.prerelease;
	if (preids.length === 0) return `https://github.com/sanity-io/sanity/releases/tag/v${ver.version}`;
	if (preids[0] === "pr" && typeof preids[1] == "number") return `https://github.com/sanity-io/sanity/pull/${preids[1]}`;
	if (preids[0] === "next" && ver.build.length === 1 && HEX_ONLY.test(ver.build[0])) return `https://github.com/sanity-io/sanity/tree/${ver.build[0]}`;
	let isGitBranch = ver.prerelease[0] === "git", hasBuildInfo = ver.build.length > 0 && HEX_ONLY.test(ver.build[0]);
	if (isGitBranch && hasBuildInfo) return `https://github.com/sanity-io/sanity/commit/${ver.build[0]}`;
}
function StudioInfoDialog(props) {
	let $ = (0, import_compiler_runtime.c)(74), { t } = useTranslation$1(), { onClose } = props, dialogId = (0, import_react.useId)(), { projectId } = useWorkspace(), { isAutoUpdating, autoUpdatingVersion, currentVersion, importMapInfo, latestTaggedVersion, versionCheckStatus, checkForUpdates } = usePackageVersionStatus(), isUpToDate = latestTaggedVersion && currentVersion && currentVersion.compareMain(latestTaggedVersion) >= 0, t0;
	$[0] === currentVersion ? t0 = $[1] : (t0 = getVersionType(currentVersion), $[0] = currentVersion, $[1] = t0);
	let currentVersionType = t0, t1;
	$[2] !== autoUpdatingVersion || $[3] !== currentVersion ? (t1 = currentVersion && autoUpdatingVersion ? import_semver.default.neq(currentVersion, autoUpdatingVersion) : !1, $[2] = autoUpdatingVersion, $[3] = currentVersion, $[4] = t1) : t1 = $[4];
	let newAutoUpdateVersionAvailable = t1, t2, t3;
	$[5] === checkForUpdates ? (t2 = $[6], t3 = $[7]) : (t2 = () => {
		checkForUpdates();
		let interval = setInterval(checkForUpdates, 1e4);
		return () => clearInterval(interval);
	}, t3 = [checkForUpdates], $[5] = checkForUpdates, $[6] = t2, $[7] = t3), (0, import_react.useEffect)(t2, t3);
	let t4;
	$[8] === currentVersion ? t4 = $[9] : (t4 = resolveGithubURLFromVersion(currentVersion), $[8] = currentVersion, $[9] = t4);
	let githubUrl = t4, sanityWebsiteUrl = useEnvAwareSanityWebsiteUrl(), t5;
	$[10] !== importMapInfo || $[11] !== t ? (t5 = importMapInfo?.valid && !importMapInfo.appId ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Card, {
		padding: 4,
		tone: "caution",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			align: "flex-start",
			gap: 3,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextWithTone, {
				tone: "caution",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WarningOutlineIcon, {})
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
				gap: 4,
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextWithTone, {
						size: 1,
						tone: "caution",
						weight: "medium",
						children: t("about-dialog.configuration-issue.header")
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextWithTone, {
						size: 1,
						tone: "caution",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Translate, {
							t,
							i18nKey: "about-dialog.configuration-issue.missing-appid"
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextWithTone, {
						size: 1,
						tone: "caution",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
							muted: !0,
							size: 1,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("a", {
								href: "https://www.sanity.io/docs/studio/latest-version-of-sanity",
								target: "_blank",
								rel: "noopener noreferrer",
								children: [t("about-dialog.configuration-issue.missing-appid.view-documentation"), " →"]
							})
						})
					})
				]
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, { gap: 2 })]
	}) : null, $[10] = importMapInfo, $[11] = t, $[12] = t5) : t5 = $[12];
	let importMapWarning = t5, versionBadgeTone = currentVersionType === "development" ? "caution" : currentVersionType === "prerelease" ? "suggest" : "neutral", t6;
	$[13] === versionCheckStatus?.checking ? t6 = $[14] : (t6 = versionCheckStatus?.checking ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		margin: 2,
		padding: 2,
		style: {
			position: "absolute",
			right: 0
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Spinner, { size: 0 })
	}) : null, $[13] = versionCheckStatus?.checking, $[14] = t6);
	let t7;
	$[15] === Symbol.for("react.memo_cache_sentinel") ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		align: "center",
		justify: "center",
		paddingY: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MonogramContainer, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SanityMonogram, {
			height: 75,
			width: 75
		}) })
	}), $[15] = t7) : t7 = $[15];
	let t8;
	$[16] === Symbol.for("react.memo_cache_sentinel") ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		justify: "flex-end",
		align: "center",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			as: "h2",
			size: 1,
			weight: "semibold",
			children: "Sanity Studio"
		})
	}), $[16] = t8) : t8 = $[16];
	let t9;
	$[17] === currentVersion.version ? t9 = $[18] : (t9 = ensureVersionPrefix(currentVersion.version), $[17] = currentVersion.version, $[18] = t9);
	let t10;
	$[19] !== t9 || $[20] !== versionBadgeTone ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Badge, {
		tone: versionBadgeTone,
		children: t9
	}), $[19] = t9, $[20] = versionBadgeTone, $[21] = t10) : t10 = $[21];
	let t11;
	$[22] !== currentVersionType || $[23] !== t ? (t11 = t(currentVersionType === "development" ? "about-dialog.version-info.tooltip.development" : currentVersionType === "prerelease" ? "about-dialog.version-info.tooltip.prerelease" : "about-dialog.version-info.tooltip.up-to-date"), $[22] = currentVersionType, $[23] = t, $[24] = t11) : t11 = $[24];
	let t12;
	$[25] === t11 ? t12 = $[26] : (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Badge, { children: t11 }), $[25] = t11, $[26] = t12);
	let t13;
	$[27] !== t10 || $[28] !== t12 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Inline, {
		gap: 1,
		children: [t10, t12]
	}) }), $[27] = t10, $[28] = t12, $[29] = t13) : t13 = $[29];
	let t14;
	$[30] === currentVersion ? t14 = $[31] : (t14 = currentVersion ? ensureVersionPrefix(currentVersion.version) : "unknown", $[30] = currentVersion, $[31] = t14);
	let t15;
	$[32] !== t14 || $[33] !== versionBadgeTone ? (t15 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TruncateBadge, {
		tone: versionBadgeTone,
		children: t14
	}), $[32] = t14, $[33] = versionBadgeTone, $[34] = t15) : t15 = $[34];
	let t16;
	$[35] !== githubUrl || $[36] !== t ? (t16 = githubUrl && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		as: "a",
		target: "_blank",
		rel: "noopener noreferrer",
		icon: GithubIcon,
		mode: "bleed",
		tooltipProps: { content: t("about-dialog.version-info.view-on-github") },
		href: githubUrl
	}), $[35] = githubUrl, $[36] = t, $[37] = t16) : t16 = $[37];
	let t17;
	$[38] !== t15 || $[39] !== t16 ? (t17 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		gap: 2,
		align: "center",
		paddingRight: 2,
		children: [t15, t16]
	}), $[38] = t15, $[39] = t16, $[40] = t17) : t17 = $[40];
	let t18;
	$[41] !== t13 || $[42] !== t17 ? (t18 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip$1, {
		placement: "bottom",
		content: t13,
		children: t17
	}), $[41] = t13, $[42] = t17, $[43] = t18) : t18 = $[43];
	let t19;
	$[44] !== autoUpdatingVersion || $[45] !== currentVersionType || $[46] !== isAutoUpdating || $[47] !== isUpToDate || $[48] !== latestTaggedVersion || $[49] !== newAutoUpdateVersionAvailable || $[50] !== t ? (t19 = isAutoUpdating && newAutoUpdateVersionAvailable ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		justify: "flex-end",
		align: "center",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			weight: "semibold",
			children: t("about-dialog.version-info.new-version.text")
		})
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		justify: "flex-start",
		align: "center",
		gap: 2,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Badge, {
			tone: "primary",
			children: autoUpdatingVersion && ensureVersionPrefix(autoUpdatingVersion?.version)
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			onClick: reload,
			mode: "bleed",
			tone: "primary",
			text: autoUpdatingVersion && getVersionType(autoUpdatingVersion) === "prerelease" ? void 0 : t("about-dialog.version-info.update-button.text"),
			tooltipProps: { content: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				children: t("about-dialog.version-info.reload-to-update")
			}) },
			icon: RefreshIcon
		})]
	})] }) : isUpToDate ? null : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		justify: "flex-end",
		align: "center",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			weight: "semibold",
			children: t("about-dialog.version-info.latest-version.header")
		})
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		justify: "flex-start",
		align: "center",
		gap: 2,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Badge, {
			tone: "primary",
			children: latestTaggedVersion ? ensureVersionPrefix(latestTaggedVersion.version) : "unknown"
		}), currentVersionType === "default" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			as: "a",
			href: "https://www.sanity.io/docs/upgrade",
			target: "_blank",
			rel: "noopener noreferrer",
			mode: "bleed",
			tooltipProps: { content: t("about-dialog.version-info.update-button.tooltip") },
			text: t("about-dialog.version-info.update-button.text"),
			iconRight: LaunchIcon
		}) : null]
	})] }), $[44] = autoUpdatingVersion, $[45] = currentVersionType, $[46] = isAutoUpdating, $[47] = isUpToDate, $[48] = latestTaggedVersion, $[49] = newAutoUpdateVersionAvailable, $[50] = t, $[51] = t19) : t19 = $[51];
	let t20;
	$[52] !== t18 || $[53] !== t19 ? (t20 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Grid, {
		gridTemplateColumns: 2,
		gap: 2,
		children: [
			t8,
			t18,
			t19
		]
	}), $[52] = t18, $[53] = t19, $[54] = t20) : t20 = $[54];
	let t21;
	$[55] !== isAutoUpdating || $[56] !== projectId || $[57] !== sanityWebsiteUrl || $[58] !== t ? (t21 = isAutoUpdating ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		tone: "transparent",
		padding: 2,
		radius: 3,
		marginX: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			align: "center",
			justify: "space-evenly",
			gap: 2,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				muted: !0,
				children: t("about-dialog.version-info.auto-updates.enabled")
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
				as: "a",
				href: `${sanityWebsiteUrl}/manage/project/${projectId}/studios?host=${document.location.hostname}`,
				target: "_blank",
				rel: "noopener noreferrer",
				mode: "ghost",
				text: t("about-dialog.version-info.auto-updates.manage-version"),
				icon: CogIcon
			})]
		})
	}) : null, $[55] = isAutoUpdating, $[56] = projectId, $[57] = sanityWebsiteUrl, $[58] = t, $[59] = t21) : t21 = $[59];
	let t22;
	$[60] !== importMapWarning || $[61] !== t21 ? (t22 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 2,
		paddingY: 3,
		children: [t21, importMapWarning]
	}), $[60] = importMapWarning, $[61] = t21, $[62] = t22) : t22 = $[62];
	let t23;
	$[63] === onClose ? t23 = $[64] : (t23 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		paddingX: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			tone: "primary",
			text: "OK",
			paddingY: 3,
			onClick: onClose
		})
	}), $[63] = onClose, $[64] = t23);
	let t24;
	$[65] !== t20 || $[66] !== t22 || $[67] !== t23 ? (t24 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 3,
		paddingY: 3,
		children: [
			t7,
			t20,
			t22,
			t23
		]
	}), $[65] = t20, $[66] = t22, $[67] = t23, $[68] = t24) : t24 = $[68];
	let t25;
	return $[69] !== dialogId || $[70] !== onClose || $[71] !== t24 || $[72] !== t6 ? (t25 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Dialog$1, {
		width: 0,
		onClickOutside: onClose,
		id: dialogId,
		padding: !1,
		children: [t6, t24]
	}), $[69] = dialogId, $[70] = onClose, $[71] = t24, $[72] = t6, $[73] = t25) : t25 = $[73], t25;
}
function ensureVersionPrefix(str) {
	return str.startsWith("v") ? str : `v${str}`;
}
var StyledMenu$1 = gt(Menu).withConfig({
	displayName: "StyledMenu",
	componentId: "sc-0n5x8n-0"
})`max-width:300px;min-width:200px;`;
function ResourcesButton() {
	let $ = (0, import_compiler_runtime.c)(58), { t } = useTranslation$1(), isInDashboard = useRenderingContext()?.name === "coreUi", t0;
	$[0] === isInDashboard ? t0 = $[1] : (t0 = {
		dsn: STUDIO_DSN,
		skip: isInDashboard
	}, $[0] = isInDashboard, $[1] = t0);
	let feedbackAvailable = useFeedbackAvailable(t0), { userApplication, isLoading: isLoadingUserApplication } = useLiveUserApplication(), { value, error, isLoading } = useGetHelpResources(), { autoUpdatingVersion: autoUpdatingVersionStr, currentVersion: currentVersionStr, latestTaggedVersion: latestTaggedVersionStr } = usePackageVersionStatus(), autoUpdatingVersion, currentVersion, latestTaggedVersion, t1;
	if ($[2] !== autoUpdatingVersionStr || $[3] !== currentVersionStr || $[4] !== latestTaggedVersionStr) {
		currentVersion = import_semver.default.parse(currentVersionStr), autoUpdatingVersion = import_semver.default.parse(autoUpdatingVersionStr) || void 0;
		let t2;
		$[9] === latestTaggedVersionStr ? t2 = $[10] : (t2 = import_semver.default.parse(latestTaggedVersionStr) || void 0, $[9] = latestTaggedVersionStr, $[10] = t2), latestTaggedVersion = t2, t1 = currentVersion && autoUpdatingVersion ? import_semver.default.neq(currentVersion, autoUpdatingVersion) : !1, $[2] = autoUpdatingVersionStr, $[3] = currentVersionStr, $[4] = latestTaggedVersionStr, $[5] = autoUpdatingVersion, $[6] = currentVersion, $[7] = latestTaggedVersion, $[8] = t1;
	} else autoUpdatingVersion = $[5], currentVersion = $[6], latestTaggedVersion = $[7], t1 = $[8];
	let newAutoUpdateVersionAvailable = t1, t2;
	$[11] !== isLoadingUserApplication || $[12] !== newAutoUpdateVersionAvailable || $[13] !== userApplication ? (t2 = () => {
		if (newAutoUpdateVersionAvailable) return "primary";
		if (!isLoadingUserApplication && !userApplication) return "caution";
	}, $[11] = isLoadingUserApplication, $[12] = newAutoUpdateVersionAvailable, $[13] = userApplication, $[14] = t2) : t2 = $[14];
	let getButtonTone = t2, [studioInfoDialogOpen, setStudioInfoDialogOpen] = (0, import_react.useState)(!1), t3;
	$[15] === Symbol.for("react.memo_cache_sentinel") ? (t3 = () => {
		setStudioInfoDialogOpen(!1);
	}, $[15] = t3) : t3 = $[15];
	let handleStudioInfoDialogClose = t3, t4;
	$[16] === Symbol.for("react.memo_cache_sentinel") ? (t4 = () => {
		setStudioInfoDialogOpen(!0);
	}, $[16] = t4) : t4 = $[16];
	let handleOpenVersionDialog = t4, [feedbackDialogOpen, setFeedbackDialogOpen] = (0, import_react.useState)(!1), { feedbackDialogOpened } = useFeedbackTelemetry(), t5;
	$[17] === feedbackDialogOpened ? t5 = $[18] : (t5 = () => {
		feedbackDialogOpened(), setFeedbackDialogOpen(!0);
	}, $[17] = feedbackDialogOpened, $[18] = t5);
	let handleOpenFeedback = t5, t6;
	$[19] === Symbol.for("react.memo_cache_sentinel") ? (t6 = () => setFeedbackDialogOpen(!1), $[19] = t6) : t6 = $[19];
	let handleCloseFeedback = t6, t7;
	$[20] === studioInfoDialogOpen ? t7 = $[21] : (t7 = studioInfoDialogOpen && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioInfoDialog, { onClose: handleStudioInfoDialogClose }), $[20] = studioInfoDialogOpen, $[21] = t7);
	let t8;
	$[22] === feedbackDialogOpen ? t8 = $[23] : (t8 = feedbackDialogOpen && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioFeedbackDialog, {
		dsn: "https://8914c8dde7e1ebce191f15af8bf6b7b9@sentry.sanity.io/4507342122123264",
		feedbackVersion: "1",
		source: "studio-help-menu",
		onClose: handleCloseFeedback
	}), $[22] = feedbackDialogOpen, $[23] = t8);
	let t9;
	$[24] === getButtonTone ? t9 = $[25] : (t9 = getButtonTone(), $[24] = getButtonTone, $[25] = t9);
	let t10;
	$[26] === t ? t10 = $[27] : (t10 = t("help-resources.title"), $[26] = t, $[27] = t10);
	let t11;
	$[28] === t ? t11 = $[29] : (t11 = t("help-resources.title"), $[28] = t, $[29] = t11);
	let t12;
	$[30] === t11 ? t12 = $[31] : (t12 = { content: t11 }, $[30] = t11, $[31] = t12);
	let t13;
	$[32] !== t10 || $[33] !== t12 || $[34] !== t9 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusButton, {
		tone: t9,
		"aria-label": t10,
		icon: HelpCircleIcon,
		"data-testid": "button-resources-menu",
		mode: "bleed",
		tooltipProps: t12
	}), $[32] = t10, $[33] = t12, $[34] = t9, $[35] = t13) : t13 = $[35];
	let t14;
	$[36] !== feedbackAvailable || $[37] !== handleOpenFeedback || $[38] !== isInDashboard ? (t14 = !isInDashboard && feedbackAvailable && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FeedbackMenuItem, { onClick: handleOpenFeedback }), $[36] = feedbackAvailable, $[37] = handleOpenFeedback, $[38] = isInDashboard, $[39] = t14) : t14 = $[39];
	let t15 = newAutoUpdateVersionAvailable ? autoUpdatingVersion : void 0, t16;
	$[40] !== currentVersion || $[41] !== error || $[42] !== isLoading || $[43] !== latestTaggedVersion || $[44] !== t15 || $[45] !== value ? (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourcesMenuItems, {
		currentVersion,
		latestTaggedVersion,
		newAutoUpdateVersion: t15,
		error,
		isLoading,
		value,
		onOpenStudioVersionDialog: handleOpenVersionDialog
	}), $[40] = currentVersion, $[41] = error, $[42] = isLoading, $[43] = latestTaggedVersion, $[44] = t15, $[45] = value, $[46] = t16) : t16 = $[46];
	let t17;
	$[47] !== t14 || $[48] !== t16 ? (t17 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(StyledMenu$1, {
		"data-testid": "menu-button-resources",
		children: [t14, t16]
	}), $[47] = t14, $[48] = t16, $[49] = t17) : t17 = $[49];
	let t18;
	$[50] === Symbol.for("react.memo_cache_sentinel") ? (t18 = {
		constrainSize: !0,
		tone: "default"
	}, $[50] = t18) : t18 = $[50];
	let t19;
	$[51] !== t13 || $[52] !== t17 ? (t19 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuButton$1, {
		button: t13,
		id: "menu-button-resources",
		menu: t17,
		popover: t18
	}), $[51] = t13, $[52] = t17, $[53] = t19) : t19 = $[53];
	let t20;
	return $[54] !== t19 || $[55] !== t7 || $[56] !== t8 ? (t20 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		t7,
		t8,
		t19
	] }), $[54] = t19, $[55] = t7, $[56] = t8, $[57] = t20) : t20 = $[57], t20;
}
function AppearanceMenu(t0) {
	let $ = (0, import_compiler_runtime.c)(5), { setScheme } = t0, { t } = useTranslation$1(), options = useColorSchemeOptions(setScheme, t), t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuDivider, {}), $[0] = t1) : t1 = $[0];
	let t2;
	$[1] === options ? t2 = $[2] : (t2 = options.map(_temp$15), $[1] = options, $[2] = t2);
	let t3;
	return $[3] === t2 ? t3 = $[4] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [t1, t2] }), $[3] = t2, $[4] = t3), t3;
}
function _temp$15(t0) {
	let { icon, label, name, onSelect, selected, title } = t0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
		"aria-label": label,
		icon,
		onClick: onSelect,
		pressed: selected,
		text: title,
		iconRight: selected && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CheckmarkIcon, {}),
		"data-testid": `color-scheme-${name}`
	}, name);
}
function LocaleMenu() {
	(0, import_compiler_runtime.c)(10);
	let { changeLocale, currentLocale, locales } = useLocale();
	return null;
}
var Root$2 = gt.div.withConfig({
	displayName: "Root",
	componentId: "sc-pnmmvq-0"
})`position:absolute;width:18px;height:18px;background:var(--card-bg-color);bottom:-4px;right:-4px;border-radius:50%;display:flex;justify-content:center;align-items:center;border:1px solid var(--card-bg-color);box-sizing:content-box;svg{box-sizing:content-box;border:1px solid var(--card-hairline-soft-color);border-radius:50%;padding:2px;width:12px;height:12px;}&[data-logo='github']{svg{path{fill:var(--card-fg-color);}}}&[data-logo='saml']{svg{path{fill:var(--card-fg-color);}}}`;
var Google = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
	width: "12",
	height: "12",
	viewBox: "0 0 12 12",
	fill: "none",
	xmlns: "http://www.w3.org/2000/svg",
	children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("g", {
		clipPath: "url(#clip0)",
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				d: "M12 6.13764C12 5.72977 11.9663 5.31971 11.8943 4.91846H6.12036V7.22895H9.42684C9.28963 7.97412 8.84876 8.63332 8.20322 9.05221V10.5514H10.1759C11.3342 9.50637 12 7.9631 12 6.13764Z",
				fill: "#4285F4"
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				d: "M6.12019 12.0002C7.77118 12.0002 9.16349 11.4688 10.1779 10.5517L8.20529 9.05253C7.65646 9.41851 6.94793 9.62574 6.12244 9.62574C4.52544 9.62574 3.17136 8.56971 2.68552 7.1499H0.649902V8.69537C1.68908 10.7215 3.80567 12.0002 6.12019 12.0002V12.0002Z",
				fill: "#34A853"
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				d: "M2.68332 7.14986C2.4269 6.40469 2.4269 5.59778 2.68332 4.8526V3.30713H0.649951C-0.218278 5.00252 -0.218278 6.99995 0.649951 8.69533L2.68332 7.14986V7.14986Z",
				fill: "#FBBC04"
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
				d: "M6.12019 2.37483C6.99291 2.3616 7.8364 2.68348 8.46845 3.27433L10.2161 1.56131C9.1095 0.542752 7.64071 -0.017233 6.12019 0.000404333C3.80566 0.000404333 1.68908 1.27911 0.649902 3.3074L2.68326 4.85288C3.16686 3.43087 4.52319 2.37483 6.12019 2.37483V2.37483Z",
				fill: "#EA4335"
			})
		]
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("defs", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("clipPath", {
		id: "clip0",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", {
			width: "12",
			height: "12",
			fill: "white"
		})
	}) })]
});
var GitHub = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
	width: "12",
	height: "12",
	viewBox: "0 0 12 12",
	fill: "none",
	xmlns: "http://www.w3.org/2000/svg",
	children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("g", {
		clipPath: "url(#clip0)",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "M11.195 3.13503C10.6711 2.22838 9.91808 1.47537 9.01144 0.951471C8.09222 0.414976 7.08837 0.146729 5.99987 0.146729C4.91137 0.146729 3.90752 0.414976 2.98831 0.951471C2.08163 1.47534 1.32862 2.22836 0.804744 3.13503C0.268248 4.05497 0 5.05883 0 6.14659C0 7.45399 0.381531 8.62978 1.14459 9.67394C1.90765 10.7181 2.89327 11.4408 4.10143 11.8419C4.24208 11.868 4.34624 11.8497 4.41392 11.7872C4.44666 11.7577 4.47263 11.7215 4.49006 11.681C4.50749 11.6405 4.51595 11.5967 4.51488 11.5527C4.51488 11.537 4.51361 11.3963 4.51105 11.1307C4.5085 10.8651 4.50722 10.6334 4.50722 10.4355L4.32745 10.4666C4.18407 10.4895 4.03885 10.4986 3.89375 10.494C3.71159 10.4908 3.53001 10.4725 3.35087 10.4393C3.16049 10.4045 2.98119 10.3246 2.82796 10.2064C2.66705 10.0846 2.54715 9.91661 2.48428 9.72484L2.40602 9.54506C2.34022 9.40286 2.25754 9.2691 2.15976 9.14666C2.04775 9.00072 1.93447 8.90176 1.81991 8.84977L1.76518 8.81064C1.72769 8.78356 1.6936 8.75205 1.66367 8.71679C1.63479 8.6841 1.61108 8.64719 1.59334 8.60734C1.57766 8.57085 1.5907 8.54085 1.63247 8.51731C1.67461 8.49405 1.75123 8.48256 1.86068 8.48256L2.01692 8.50582C2.12108 8.52662 2.24996 8.5891 2.40356 8.69326C2.5585 8.79876 2.68797 8.93748 2.78254 9.09932C2.90239 9.31294 3.04695 9.47575 3.21624 9.58775C3.38552 9.69976 3.55609 9.75576 3.72793 9.75576C3.87725 9.7578 4.02641 9.74479 4.17312 9.7169C4.29459 9.69195 4.41264 9.65254 4.52473 9.59952C4.57162 9.25037 4.69931 8.98212 4.90782 8.79477C4.63684 8.76849 4.36827 8.72157 4.10444 8.6544C3.84797 8.58382 3.60133 8.48141 3.37029 8.34958C3.12825 8.218 2.91442 8.04015 2.74095 7.82613C2.57421 7.6178 2.4374 7.34417 2.3305 7.00524C2.2236 6.6663 2.1707 6.27584 2.1718 5.83383C2.1718 5.20357 2.37757 4.66708 2.7891 4.22435C2.59629 3.75006 2.61453 3.21885 2.84383 2.63073C2.99487 2.58385 3.21879 2.61897 3.51559 2.73608C3.81239 2.85319 4.02983 2.95352 4.16792 3.03707C4.30601 3.12025 4.41665 3.19058 4.49983 3.24804C5.48153 2.9773 6.51821 2.9773 7.49991 3.24804L7.79679 3.0606C8.0249 2.92356 8.26549 2.80849 8.51535 2.71693C8.79135 2.61276 9.00231 2.58412 9.14825 2.63101C9.38248 3.21949 9.40327 3.7507 9.21064 4.22462C9.62199 4.66735 9.82776 5.20385 9.82795 5.83411C9.82795 6.27684 9.77459 6.66904 9.66787 7.01071C9.56116 7.35238 9.42325 7.62601 9.25415 7.8316C9.07785 8.04326 8.86311 8.21966 8.62124 8.35149C8.39008 8.48329 8.14337 8.5857 7.88682 8.65632C7.62412 8.72277 7.35674 8.76915 7.087 8.79505C7.35698 9.02927 7.49206 9.3994 7.49225 9.90543V11.5527C7.49115 11.5964 7.49929 11.6398 7.51612 11.6802C7.53295 11.7206 7.5581 11.7569 7.58993 11.7869C7.65505 11.8493 7.75794 11.8675 7.89858 11.8416C9.10693 11.4403 10.0926 10.7177 10.8557 9.67367C11.6188 8.62968 12.0003 7.4539 12.0003 6.14632C11.9997 5.05873 11.7313 4.05497 11.195 3.13503V3.13503Z",
			fill: "black"
		})
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("defs", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("clipPath", {
		id: "clip0",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("rect", {
			width: "12",
			height: "12",
			fill: "white"
		})
	}) })]
});
var Saml = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
	width: "17",
	height: "9",
	viewBox: "0 0 17 9",
	fill: "none",
	xmlns: "http://www.w3.org/2000/svg",
	children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M0.0691681 5.98162C0.115833 7.24157 1.07247 8.00221 2.59841 8.00221C4.17102 8.00221 5.12299 7.19957 5.12299 5.86962C5.12299 4.83832 4.55367 4.25501 3.25172 3.96569L2.56108 3.80703C1.8331 3.64837 1.53445 3.38704 1.53445 2.91106C1.53445 2.40708 1.94977 2.09442 2.61708 2.09442C3.27505 2.09442 3.6997 2.43508 3.75103 2.99039H5.00166C4.95966 1.78643 4.01236 0.997795 2.60308 0.997795C1.14713 0.997795 0.218496 1.79577 0.218496 3.05105C0.218496 4.04968 0.815808 4.69366 2.03376 4.96898L2.73374 5.13231C3.51304 5.30497 3.8117 5.54763 3.8117 6.00495C3.8117 6.55093 3.34972 6.90092 2.62174 6.90092C1.85644 6.90092 1.38045 6.56493 1.31979 5.98162H0.0691681Z" }),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M5.97695 5.98162C6.02362 7.24157 6.98025 8.00221 8.5062 8.00221C10.0788 8.00221 11.0308 7.19957 11.0308 5.86962C11.0308 4.83832 10.4615 4.25501 9.15951 3.96569L8.46886 3.80703C7.74089 3.64837 7.44223 3.38704 7.44223 2.91106C7.44223 2.40708 7.85755 2.09442 8.52486 2.09442C9.18284 2.09442 9.60749 2.43508 9.65882 2.99039H10.9094C10.8674 1.78643 9.92015 0.997795 8.51086 0.997795C7.05492 0.997795 6.12628 1.79577 6.12628 3.05105C6.12628 4.04968 6.72359 4.69366 7.94155 4.96898L8.64153 5.13231C9.42083 5.30497 9.71949 5.54763 9.71949 6.00495C9.71949 6.55093 9.2575 6.90092 8.52953 6.90092C7.76422 6.90092 7.28824 6.56493 7.22758 5.98162H5.97695Z" }),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M16.9852 5.01565V3.98435C16.9852 2.11309 16.0099 0.997795 14.3953 0.997795C12.7807 0.997795 11.8054 2.11309 11.8054 3.98435V5.01565C11.8054 6.88692 12.7807 8.00221 14.3953 8.00221C16.0099 8.00221 16.9852 6.88692 16.9852 5.01565ZM14.3953 6.82625C13.5833 6.82625 13.154 6.21494 13.154 5.00165V3.99835C13.154 2.78506 13.5833 2.17375 14.3953 2.17375C15.2073 2.17375 15.6366 2.78506 15.6366 3.99835V5.00165C15.6366 6.21494 15.2073 6.82625 14.3953 6.82625Z" })
	]
});
var LoginProviderLogo = (t0) => {
	let $ = (0, import_compiler_runtime.c)(10), { provider } = t0, t1;
	$[0] === provider ? t1 = $[1] : (t1 = provider?.startsWith("saml-"), $[0] = provider, $[1] = t1);
	let isSaml = t1, logoName = isSaml ? "saml" : provider, t2 = provider === "google" && Google, t3 = provider === "github" && GitHub, t4;
	$[2] === provider ? t4 = $[3] : (t4 = provider === "sanity" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SanityMonogram, {}), $[2] = provider, $[3] = t4);
	let t5 = isSaml && Saml, t6;
	return $[4] !== logoName || $[5] !== t2 || $[6] !== t3 || $[7] !== t4 || $[8] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Root$2, {
		"data-logo": logoName,
		children: [
			t2,
			t3,
			t4,
			t5
		]
	}), $[4] = logoName, $[5] = t2, $[6] = t3, $[7] = t4, $[8] = t5, $[9] = t6) : t6 = $[9], t6;
};
var StyledMenu = gt(Menu).withConfig({
	displayName: "StyledMenu",
	componentId: "sc-hzzvg9-0"
})`min-width:200px;max-width:300px;`;
var AvatarBox = gt(Box).withConfig({
	displayName: "AvatarBox",
	componentId: "sc-hzzvg9-1"
})`position:relative;min-width:${({ theme }) => theme.sanity.avatar.sizes[2].size}px;min-height:${({ theme }) => theme.sanity.avatar.sizes[2].size}px;`;
function UserMenu() {
	let $ = (0, import_compiler_runtime.c)(43), { currentUser, auth } = useWorkspace(), scheme = useColorSchemeValue(), setScheme = useColorSchemeSetValue(), providerTitle = getProviderTitle(currentUser?.provider), { t } = useTranslation$1(), t0;
	$[0] === scheme ? t0 = $[1] : (t0 = {
		placement: "bottom",
		portal: !0,
		preventOverflow: !0,
		scheme,
		constrainSize: !0,
		tone: "default"
	}, $[0] = scheme, $[1] = t0);
	let popoverProps = t0, T0 = MenuButton$1, t1;
	$[2] === Symbol.for("react.memo_cache_sentinel") ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
		mode: "bleed",
		padding: 0,
		radius: "full",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserAvatar, {
			size: 1,
			user: "me"
		})
	}), $[2] = t1) : t1 = $[2];
	let T1 = StyledMenu, T2 = Card, T3 = Flex, T4 = Tooltip$1, t6 = !providerTitle, t8 = t("user-menu.login-provider", { providerTitle }), t9;
	$[3] === Symbol.for("react.memo_cache_sentinel") ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserAvatar, {
		size: 2,
		user: "me"
	}), $[3] = t9) : t9 = $[3];
	let t10;
	$[4] === currentUser ? t10 = $[5] : (t10 = currentUser?.provider && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoginProviderLogo, { provider: currentUser.provider }), $[4] = currentUser, $[5] = t10);
	let t11;
	$[6] === t10 ? t11 = $[7] : (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(AvatarBox, {
		marginRight: 3,
		children: [t9, t10]
	}), $[6] = t10, $[7] = t11);
	let t12;
	$[8] !== T4 || $[9] !== t11 || $[10] !== t6 || $[11] !== t8 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(T4, {
		disabled: t6,
		portal: !0,
		content: t8,
		children: t11
	}), $[8] = T4, $[9] = t11, $[10] = t6, $[11] = t8, $[12] = t12) : t12 = $[12];
	let t13 = currentUser?.name, t14;
	$[13] === t13 ? t14 = $[14] : (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		weight: "medium",
		textOverflow: "ellipsis",
		children: t13
	}), $[13] = t13, $[14] = t14);
	let t15 = currentUser?.email, t16;
	$[15] === t15 ? t16 = $[16] : (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		muted: !0,
		textOverflow: "ellipsis",
		children: t15
	}), $[15] = t15, $[16] = t16);
	let t17;
	$[17] !== t14 || $[18] !== t16 ? (t17 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 2,
		flex: 1,
		children: [t14, t16]
	}), $[17] = t14, $[18] = t16, $[19] = t17) : t17 = $[19];
	let t18;
	$[20] !== T3 || $[21] !== t12 || $[22] !== t17 ? (t18 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(T3, {
		align: "center",
		children: [t12, t17]
	}), $[20] = T3, $[21] = t12, $[22] = t17, $[23] = t18) : t18 = $[23];
	let t19;
	$[24] !== T2 || $[25] !== t18 ? (t19 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(T2, {
		padding: 2,
		children: t18
	}), $[24] = T2, $[25] = t18, $[26] = t19) : t19 = $[26];
	let t20;
	$[27] === setScheme ? t20 = $[28] : (t20 = setScheme && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AppearanceMenu, { setScheme }), $[27] = setScheme, $[28] = t20);
	let t21;
	$[29] === Symbol.for("react.memo_cache_sentinel") ? (t21 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LocaleMenu, {}), $[29] = t21) : t21 = $[29];
	let t22;
	$[30] !== auth.logout || $[31] !== t ? (t22 = auth.logout && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuDivider, {}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem$1, {
		iconRight: LeaveIcon,
		text: t("user-menu.action.sign-out"),
		disabled: !auth.logout,
		...auth.logout && { onClick: auth.logout }
	})] }), $[30] = auth.logout, $[31] = t, $[32] = t22) : t22 = $[32];
	let t23;
	$[33] !== T1 || $[34] !== t19 || $[35] !== t20 || $[36] !== t22 ? (t23 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(T1, {
		"data-testid": "user-menu",
		children: [
			t19,
			t20,
			t21,
			t22
		]
	}), $[33] = T1, $[34] = t19, $[35] = t20, $[36] = t22, $[37] = t23) : t23 = $[37];
	let t24;
	return $[38] !== T0 || $[39] !== popoverProps || $[40] !== t1 || $[41] !== t23 ? (t24 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(T0, {
		button: t1,
		id: "user-menu",
		menu: t23,
		popover: popoverProps
	}), $[38] = T0, $[39] = popoverProps, $[40] = t1, $[41] = t23, $[42] = t24) : t24 = $[42], t24;
}
var EMPTY_ARRAY$1 = [];
var RootLayer = gt(Layer).withConfig({
	displayName: "RootLayer",
	componentId: "sc-lhkvuy-0"
})`min-height:auto;position:relative;&[data-search-open='true']{top:0;position:sticky;}`;
var RootCard = gt(Card).withConfig({
	displayName: "RootCard",
	componentId: "sc-lhkvuy-1"
})`line-height:0;`;
var NavGrid = gt(Grid).withConfig({
	displayName: "NavGrid",
	componentId: "sc-lhkvuy-2"
})`grid-template-columns:auto auto auto;@media screen and (min-width:${({ theme }) => `${theme.sanity.media[4]}px`}){grid-template-columns:1fr auto 1fr;}`;
/**
* @hidden
* @beta */
function StudioNavbar(props) {
	let $ = (0, import_compiler_runtime.c)(116), { __internal_actions: t0 } = props, actions = t0 === void 0 ? EMPTY_ARRAY$1 : t0, { beta, name, tools } = useWorkspace(), routerState = useRouterState(), mediaIndex = useMediaIndex(), activeToolName = typeof routerState.tool == "string" ? routerState.tool : void 0, newDocumentOptions = useNewDocumentOptions(), { t } = useTranslation$1(), { onSearchFullscreenOpenChange, onSearchOpenChange, searchFullscreenOpen, searchFullscreenPortalEl, searchOpen } = (0, import_react.useContext)(NavbarContext), { selectedPerspective, perspectiveStack, selectedVariantName } = usePerspective(), ToolMenu = useToolMenuComponent(), [drawerOpen, setDrawerOpen] = (0, import_react.useState)(!1), routerStateRef = (0, import_react.useRef)(routerState), workspaceNameRef = (0, import_react.useRef)(name), t1, t2;
	$[0] !== name || $[1] !== routerState ? (t1 = () => {
		(routerStateRef.current.tool !== routerState.tool || name !== workspaceNameRef.current) && setDrawerOpen(!1), routerStateRef.current = routerState, workspaceNameRef.current = name;
	}, t2 = [name, routerState], $[0] = name, $[1] = routerState, $[2] = t1, $[3] = t2) : (t1 = $[2], t2 = $[3]), (0, import_react.useEffect)(t1, t2);
	let [drawerButtonEl, setDrawerButtonEl] = (0, import_react.useState)(null), [searchOpenButtonEl, setSearchOpenButtonEl] = (0, import_react.useState)(null), t3 = mediaIndex > 1, t4 = mediaIndex <= 1, t5 = mediaIndex > 1, t6 = mediaIndex <= 1, t7 = mediaIndex > 1 && true, t8 = mediaIndex <= 1, t9 = mediaIndex >= 3, t10;
	$[4] !== t3 || $[5] !== t4 || $[6] !== t5 || $[7] !== t6 || $[8] !== t7 || $[9] !== t8 || $[10] !== t9 ? (t10 = {
		resources: t3,
		collapsedPresenceMenu: t4,
		loginStatus: t5,
		searchFullscreen: t6,
		configIssues: t7,
		newDocumentFullscreen: t8,
		tools: t9
	}, $[4] = t3, $[5] = t4, $[6] = t5, $[7] = t6, $[8] = t7, $[9] = t8, $[10] = t9, $[11] = t10) : t10 = $[11];
	let shouldRender = t10, t11, t12;
	$[12] !== onSearchFullscreenOpenChange || $[13] !== searchFullscreenOpen ? (t11 = () => {
		onSearchFullscreenOpenChange(searchFullscreenOpen);
	}, t12 = [searchFullscreenOpen, onSearchFullscreenOpenChange], $[12] = onSearchFullscreenOpenChange, $[13] = searchFullscreenOpen, $[14] = t11, $[15] = t12) : (t11 = $[14], t12 = $[15]), (0, import_react.useEffect)(t11, t12);
	let t13, t14;
	$[16] !== onSearchFullscreenOpenChange || $[17] !== onSearchOpenChange || $[18] !== shouldRender.searchFullscreen ? (t13 = () => {
		shouldRender.searchFullscreen ? onSearchOpenChange(!1) : onSearchFullscreenOpenChange(!1);
	}, t14 = [
		onSearchFullscreenOpenChange,
		onSearchOpenChange,
		shouldRender.searchFullscreen
	], $[16] = onSearchFullscreenOpenChange, $[17] = onSearchOpenChange, $[18] = shouldRender.searchFullscreen, $[19] = t13, $[20] = t14) : (t13 = $[19], t14 = $[20]), (0, import_react.useEffect)(t13, t14);
	let t15;
	$[21] === onSearchOpenChange ? t15 = $[22] : (t15 = () => {
		onSearchOpenChange(!0);
	}, $[21] = onSearchOpenChange, $[22] = t15);
	let handleOpenSearch = t15, t16;
	$[23] === onSearchFullscreenOpenChange ? t16 = $[24] : (t16 = () => {
		onSearchFullscreenOpenChange(!0);
	}, $[23] = onSearchFullscreenOpenChange, $[24] = t16);
	let handleOpenSearchFullscreen = t16, t17;
	$[25] === onSearchOpenChange ? t17 = $[26] : (t17 = () => {
		onSearchOpenChange(!1);
	}, $[25] = onSearchOpenChange, $[26] = t17);
	let handleCloseSearch = t17, t18;
	$[27] !== onSearchFullscreenOpenChange || $[28] !== searchOpenButtonEl ? (t18 = () => {
		onSearchFullscreenOpenChange(!1), searchOpenButtonEl?.focus();
	}, $[27] = onSearchFullscreenOpenChange, $[28] = searchOpenButtonEl, $[29] = t18) : t18 = $[29];
	let handleCloseSearchFullscreen = t18, t19;
	$[30] === drawerButtonEl ? t19 = $[31] : (t19 = () => {
		setDrawerOpen(!1), drawerButtonEl?.focus();
	}, $[30] = drawerButtonEl, $[31] = t19);
	let handleCloseDrawer = t19, t20;
	$[32] === Symbol.for("react.memo_cache_sentinel") ? (t20 = () => {
		setDrawerOpen(!0);
	}, $[32] = t20) : t20 = $[32];
	let handleOpenDrawer = t20, t21;
	bb0: {
		if (!shouldRender.tools) {
			t21 = null;
			break bb0;
		}
		let t22;
		$[33] === actions ? t22 = $[34] : (t22 = actions?.filter(_temp$14)?.map(_temp2$7), $[33] = actions, $[34] = t22), t21 = t22;
	}
	let actionNodes = t21, t22;
	$[35] === selectedPerspective ? t22 = $[36] : (t22 = getReleaseTone(selectedPerspective), $[35] = selectedPerspective, $[36] = t22);
	let t23;
	$[37] !== shouldRender.tools || $[38] !== t ? (t23 = !shouldRender.tools && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		mode: "bleed",
		icon: MenuIcon,
		onClick: handleOpenDrawer,
		ref: setDrawerButtonEl,
		tooltipProps: {
			content: t("user-menu.open-menu"),
			placement: "bottom"
		}
	}), $[37] = shouldRender.tools, $[38] = t, $[39] = t23) : t23 = $[39];
	let t24;
	$[40] === Symbol.for("react.memo_cache_sentinel") ? (t24 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		gap: 1,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(HomeButton, {}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspaceMenuButton, {})]
	}), $[40] = t24) : t24 = $[40];
	let t25;
	$[41] === t23 ? t25 = $[42] : (t25 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		gap: 2,
		children: [t23, t24]
	}), $[41] = t23, $[42] = t25);
	let t26 = shouldRender.newDocumentFullscreen ? "dialog" : "popover", t27;
	$[43] !== newDocumentOptions || $[44] !== t26 ? (t27 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NewDocumentButton, {
		...newDocumentOptions,
		modal: t26
	}), $[43] = newDocumentOptions, $[44] = t26, $[45] = t27) : t27 = $[45];
	let t28;
	$[46] !== handleOpenSearch || $[47] !== shouldRender.searchFullscreen ? (t28 = !shouldRender.searchFullscreen && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SearchButton, {
		onClick: handleOpenSearch,
		ref: setSearchOpenButtonEl
	}), $[46] = handleOpenSearch, $[47] = shouldRender.searchFullscreen, $[48] = t28) : t28 = $[48];
	let t29;
	$[49] !== t25 || $[50] !== t27 || $[51] !== t28 ? (t29 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TooltipDelayGroupProvider$1, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		gap: 2,
		justify: "flex-start",
		children: [
			t25,
			t27,
			t28
		]
	}) }), $[49] = t25, $[50] = t27, $[51] = t28, $[52] = t29) : t29 = $[52];
	let t30;
	$[53] !== ToolMenu || $[54] !== activeToolName || $[55] !== handleCloseDrawer || $[56] !== shouldRender.tools || $[57] !== tools ? (t30 = shouldRender.tools && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolMenu, {
		activeToolName,
		closeSidebar: handleCloseDrawer,
		context: "topbar",
		isSidebarOpen: !1,
		tools
	}), $[53] = ToolMenu, $[54] = activeToolName, $[55] = handleCloseDrawer, $[56] = shouldRender.tools, $[57] = tools, $[58] = t30) : t30 = $[58];
	let t31;
	$[59] === t30 ? t31 = $[60] : (t31 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		align: "center",
		justify: "center",
		children: t30
	}), $[59] = t30, $[60] = t31);
	let t32;
	$[61] !== handleCloseSearch || $[62] !== handleCloseSearchFullscreen || $[63] !== handleOpenSearch || $[64] !== handleOpenSearchFullscreen || $[65] !== perspectiveStack || $[66] !== searchFullscreenOpen || $[67] !== searchFullscreenPortalEl || $[68] !== searchOpen || $[69] !== selectedVariantName || $[70] !== shouldRender.searchFullscreen ? (t32 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BoundaryElementProvider, {
		element: document.body,
		children: shouldRender.searchFullscreen ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PortalProvider, {
			element: searchFullscreenPortalEl,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SearchDialog, {
				onClose: handleCloseSearchFullscreen,
				onOpen: handleOpenSearchFullscreen,
				open: searchFullscreenOpen,
				previewPerspective: perspectiveStack,
				previewVariant: selectedVariantName
			})
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SearchPopover, {
			onClose: handleCloseSearch,
			onOpen: handleOpenSearch,
			open: searchOpen,
			previewPerspective: perspectiveStack,
			previewVariant: selectedVariantName
		})
	}), $[61] = handleCloseSearch, $[62] = handleCloseSearchFullscreen, $[63] = handleOpenSearch, $[64] = handleOpenSearchFullscreen, $[65] = perspectiveStack, $[66] = searchFullscreenOpen, $[67] = searchFullscreenPortalEl, $[68] = searchOpen, $[69] = selectedVariantName, $[70] = shouldRender.searchFullscreen, $[71] = t32) : t32 = $[71];
	let t33;
	$[72] !== shouldRender.searchFullscreen || $[73] !== t32 ? (t33 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayerProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SearchProvider, {
		fullscreen: shouldRender.searchFullscreen,
		children: t32
	}) }), $[72] = shouldRender.searchFullscreen, $[73] = t32, $[74] = t33) : t33 = $[74];
	let t34;
	$[75] === beta?.variants?.enabled ? t34 = $[76] : (t34 = !beta?.variants?.enabled && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReleasesNav, { withReleasesToolButton: !0 }), $[75] = beta?.variants?.enabled, $[76] = t34);
	let t35;
	$[77] === shouldRender.tools ? t35 = $[78] : (t35 = shouldRender.tools && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FreeTrial, { type: "topbar" }), $[77] = shouldRender.tools, $[78] = t35);
	let t36;
	$[79] === Symbol.for("react.memo_cache_sentinel") ? (t36 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PresenceMenu, {}), $[79] = t36) : t36 = $[79];
	let t37;
	$[80] === shouldRender.configIssues ? t37 = $[81] : (t37 = shouldRender.configIssues && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfigIssuesButton, {}), $[80] = shouldRender.configIssues, $[81] = t37);
	let t38;
	$[82] === shouldRender.resources ? t38 = $[83] : (t38 = shouldRender.resources && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourcesButton, {}), $[82] = shouldRender.resources, $[83] = t38);
	let t39;
	$[84] !== handleOpenSearchFullscreen || $[85] !== shouldRender.searchFullscreen ? (t39 = shouldRender.searchFullscreen && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SearchButton, {
		onClick: handleOpenSearchFullscreen,
		ref: setSearchOpenButtonEl
	}), $[84] = handleOpenSearchFullscreen, $[85] = shouldRender.searchFullscreen, $[86] = t39) : t39 = $[86];
	let t40;
	$[87] === shouldRender.tools ? t40 = $[88] : (t40 = shouldRender.tools && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CapabilityGate, {
		capability: "globalUserMenu",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			flex: "none",
			marginLeft: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserMenu, {})
		})
	}), $[87] = shouldRender.tools, $[88] = t40);
	let t41;
	$[89] !== actionNodes || $[90] !== t33 || $[91] !== t34 || $[92] !== t35 || $[93] !== t37 || $[94] !== t38 || $[95] !== t39 || $[96] !== t40 ? (t41 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TooltipDelayGroupProvider$1, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		gap: 1,
		justify: "flex-end",
		children: [
			t33,
			t34,
			actionNodes,
			t35,
			t36,
			t37,
			t38,
			t39,
			t40
		]
	}) }), $[89] = actionNodes, $[90] = t33, $[91] = t34, $[92] = t35, $[93] = t37, $[94] = t38, $[95] = t39, $[96] = t40, $[97] = t41) : t41 = $[97];
	let t42;
	$[98] !== t29 || $[99] !== t31 || $[100] !== t41 ? (t42 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(NavGrid, {
		gap: 1,
		children: [
			t29,
			t31,
			t41
		]
	}), $[98] = t29, $[99] = t31, $[100] = t41, $[101] = t42) : t42 = $[101];
	let t43;
	$[102] !== t22 || $[103] !== t42 ? (t43 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RootCard, {
		tone: t22,
		borderBottom: !0,
		"data-testid": "studio-navbar",
		"data-ui": "Navbar",
		padding: 3,
		sizing: "border",
		children: t42
	}), $[102] = t22, $[103] = t42, $[104] = t43) : t43 = $[104];
	let t44;
	$[105] !== actions || $[106] !== activeToolName || $[107] !== drawerOpen || $[108] !== handleCloseDrawer || $[109] !== shouldRender.tools || $[110] !== tools ? (t44 = !shouldRender.tools && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavDrawer, {
		__internal_actions: actions,
		activeToolName,
		isOpen: drawerOpen,
		onClose: handleCloseDrawer,
		tools
	}), $[105] = actions, $[106] = activeToolName, $[107] = drawerOpen, $[108] = handleCloseDrawer, $[109] = shouldRender.tools, $[110] = tools, $[111] = t44) : t44 = $[111];
	let t45;
	return $[112] !== searchFullscreenOpen || $[113] !== t43 || $[114] !== t44 ? (t45 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FreeTrialProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RootLayer, {
		zOffset: 100,
		"data-search-open": searchFullscreenOpen,
		children: [t43, t44]
	}) }), $[112] = searchFullscreenOpen, $[113] = t43, $[114] = t44, $[115] = t45) : t45 = $[115], t45;
}
function _temp2$7(action) {
	let { render: ActionComponent } = action;
	return ActionComponent ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionComponent, {}, action.name) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		iconRight: action?.icon,
		mode: "bleed",
		onClick: action?.onAction,
		selected: action.selected,
		text: action.title
	}, action.name);
}
function _temp$14(v) {
	return v.location === "topbar";
}
/**
* Generates a workspace manifest entry from a workspace and its schema descriptor ID.
*/
async function generateWorkspaceManifest(workspace, schemaDescriptorId, resolveIcon) {
	return {
		name: workspace.name,
		projectId: workspace.projectId,
		dataset: workspace.dataset,
		schemaDescriptorId,
		basePath: workspace.basePath || void 0,
		title: workspace.title || void 0,
		subtitle: workspace.subtitle || void 0,
		icon: await resolveIcon(workspace),
		mediaLibraryId: workspace.mediaLibrary?.enabled ? workspace.mediaLibrary.libraryId : void 0
	};
}
/**
* Generates a StudioManifest from workspaces.
* This is a shared utility used by both CLI deployment and live manifest registration.
* @internal
*/
async function generateStudioManifest(options) {
	let { workspaces, resolveSchemaDescriptorId, resolveIcon, bundleVersion, buildId } = options;
	return {
		buildId,
		bundleVersion,
		workspaces: (await Promise.all(workspaces.map(async (workspace) => {
			let schemaDescriptorId = await resolveSchemaDescriptorId(workspace);
			return schemaDescriptorId ? generateWorkspaceManifest(workspace, schemaDescriptorId, resolveIcon) : null;
		}))).filter((manifest) => manifest !== null)
	};
}
/**
* Normalizes icon input to a React element.
*/
function normalizeIcon(Icon, title, subtitle = "") {
	return (0, import_react_is.isValidElementType)(Icon) ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, {}) : (0, import_react.isValidElement)(Icon) ? Icon : createDefaultIcon(title, subtitle);
}
/**
* Renders a workspace icon to an HTML string.
*
* Sanitization is intentionally omitted here — the backend sanitizes
* icon HTML when the manifest is posted to the API.
*/
var resolveIcon = (props) => {
	try {
		let iconElement = normalizeIcon(props.icon, props.title, props.subtitle);
		return (0, import_server_browser.renderToStaticMarkup)(/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ThemeProvider$1, {
			theme: props.theme,
			children: iconElement
		})).trim() || void 0;
	} catch {
		return;
	}
};
var buildId = typeof __SANITY_BUILD_TIMESTAMP__ > "u" ? void 0 : `${__SANITY_BUILD_TIMESTAMP__}`;
/**
* Resolves the Source from a WorkspaceSummary by subscribing to the source observable.
*/
async function resolveSource(workspace) {
	let sourceEntry = workspace.__internal.sources[0];
	if (sourceEntry) return firstValueFrom(sourceEntry.source);
}
/**
* Resolves the schemaDescriptorId for a workspace.
* Returns undefined if the schema hasn't been uploaded or is unavailable.
*/
async function resolveSchemaDescriptorId(workspace) {
	return await (await resolveSource(workspace))?.__internal?.schemaDescriptorId;
}
/**
* Uploads the studio manifest containing all workspace information to the backend.
* Each workspace manifest includes its associated schemaDescriptorId.
*
* @param userApplication - The user application
* @param workspaces - Array of all workspaces in the Studio
* @param theme - The Studio theme to use for rendering icons
* @param signal - Aborts the upload when the registering effect is torn down
* @returns Promise that resolves when upload is complete
* @internal
*/
async function registerStudioManifest(userApplication, workspaces, theme, signal) {
	let { id, projectId } = userApplication, workspace = workspaces.find((ws) => ws.projectId === projectId);
	if (!workspace) return;
	let liveManifest = await generateStudioManifest({
		workspaces,
		resolveSchemaDescriptorId,
		resolveIcon: (ws) => resolveIcon({
			icon: ws.icon,
			title: ws.title || ws.name,
			subtitle: ws.subtitle,
			theme
		}),
		bundleVersion: SANITY_VERSION,
		buildId
	});
	if (liveManifest.workspaces.length === 0) return;
	let { client, authenticated } = await firstValueFrom(workspace.auth.state);
	if (!authenticated) return;
	let studioClient = client.withConfig(DEFAULT_STUDIO_CLIENT_OPTIONS);
	await fetchCanDeployStudio(studioClient) && (signal?.aborted || await studioClient.request({
		method: "POST",
		uri: `/projects/${projectId}/user-applications/${id}/config/live-manifest`,
		body: { value: liveManifest },
		tag: "live-manifest-register",
		signal
	}));
}
var debug$2 = (0, import_browser.default)("sanity:manifest");
/**
* Provider that automatically uploads the studio manifest when the Studio loads.
* This runs once when all workspaces are available and includes all workspace information.
*
* @internal
*/
function LiveManifestRegisterProvider() {
	let $ = (0, import_compiler_runtime.c)(5), workspaces = useWorkspaces(), { userApplication } = useLiveUserApplication(), { theme } = useRootTheme(), t0, t1;
	return $[0] !== theme || $[1] !== userApplication || $[2] !== workspaces ? (t0 = () => {
		if (!userApplication || workspaces.length === 0) return;
		let controller = new AbortController();
		return registerStudioManifest(userApplication, workspaces, theme, controller.signal).catch(_temp$13), () => controller.abort();
	}, t1 = [
		userApplication,
		workspaces,
		theme
	], $[0] = theme, $[1] = userApplication, $[2] = workspaces, $[3] = t0, $[4] = t1) : (t0 = $[3], t1 = $[4]), (0, import_react.useEffect)(t0, t1), null;
}
function _temp$13(error) {
	error?.name !== "AbortError" && debug$2("Failed to upload studio manifest", error);
}
function buildResizeHandleDataUri(hexColor) {
	let encodedStrokeColor = encodeURIComponent(hexColor);
	return `url("data:image/svg+xml,${`%3Csvg width='9' height='9' viewBox='0 0 9 9' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 8L8 1' stroke='${encodedStrokeColor}' stroke-linecap='round'/%3E%3Cpath d='M5 8L8 5' stroke='${encodedStrokeColor}' stroke-linecap='round'/%3E%3C/svg%3E%0A`}")`;
}
var GlobalStyle = () => {
	let $ = (0, import_compiler_runtime.c)(2), { advancedVersionControl: t0 } = useWorkspace(), { enabled: advancedVersionControlEnabled } = t0, t1;
	return $[0] === advancedVersionControlEnabled ? t1 = $[1] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalStyleSheet, { $documentEditorGutterEnabled: advancedVersionControlEnabled }), $[0] = advancedVersionControlEnabled, $[1] = t1), t1;
};
var GlobalStyleSheet = /* @__PURE__ */ St(({ theme, $documentEditorGutterEnabled }) => {
	let { color, font } = getTheme_v2(theme);
	return ft`::-webkit-resizer{background-image:${buildResizeHandleDataUri(color.icon)};background-repeat:no-repeat;background-position:bottom right;}::-webkit-scrollbar{width:${12}px;height:${12}px;}::-webkit-scrollbar-corner{background-color:transparent;}::-webkit-scrollbar-thumb{background-clip:content-box;background-color:var(--card-border-color,${color.border});border:${4}px solid transparent;}::-webkit-scrollbar-thumb:hover{background-color:var(--card-muted-fg-color,${color.muted.fg});}::-webkit-scrollbar-track{background:transparent;}*::selection{background-color:${rgba$1(color.focusRing, .3)};}html{background-color:${color.bg};}body{scrollbar-gutter:stable;}#sanity{font-family:${font.text.family};}b{font-weight:${font.text.weights.medium};}strong{font-weight:${font.text.weights.medium};}`;
});
/**
* This component is not meant to be exported by `sanity`, it's meant only for internal use from the `<StudioProvider>` file.
* It sets the `<PerspectiveProvider>` listening to the changes happening in the router.
*
* If you need to add the PerspectiveProvider you should use that component directly.
* It's up to you to define how the selectedPerspectiveName and excludedPerspectives should worl.
*/
function GlobalPerspectiveProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(7), { children } = t0, router = useRouter(), { document: t1 } = useWorkspace(), { drafts: t2 } = t1, { enabled: isDraftModelEnabled } = t2, selectedPerspectiveName = router.stickyParams.perspective;
	!isDraftModelEnabled && selectedPerspectiveName === void 0 && (selectedPerspectiveName = "published");
	let t3;
	$[0] === router.stickyParams.excludedPerspectives ? t3 = $[1] : (t3 = router.stickyParams.excludedPerspectives?.split(",") || EMPTY_ARRAY$12, $[0] = router.stickyParams.excludedPerspectives, $[1] = t3);
	let excludedPerspectives = t3, selectedVariantName = typeof router.stickyParams.variant == "string" ? router.stickyParams.variant : void 0, t4;
	return $[2] !== children || $[3] !== excludedPerspectives || $[4] !== selectedPerspectiveName || $[5] !== selectedVariantName ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PerspectiveProvider, {
		selectedPerspectiveName,
		selectedVariantName,
		excludedPerspectives,
		children
	}), $[2] = children, $[3] = excludedPerspectives, $[4] = selectedPerspectiveName, $[5] = selectedVariantName, $[6] = t4) : t4 = $[6], t4;
}
var debug$1 = (0, import_browser.default)("sanity:store");
/**
* Creates a cache for user applications.
* Uses a single cache keyed by app ID, with a separate index for project lookups.
* @internal
*/
function createUserApplicationCache() {
	let appCache = {}, projectIndex = {};
	return { get: async (client) => {
		let { projectId } = client.config();
		if (projectId === void 0) return [];
		let existingIndex = projectIndex[projectId];
		if (existingIndex) {
			let appIds = await existingIndex;
			return Promise.all(appIds.map((id) => appCache[id]));
		}
		let promise = client.withConfig(DEFAULT_STUDIO_CLIENT_OPTIONS).request({
			method: "GET",
			url: `/projects/${projectId}/user-applications`,
			tag: "user-application-cache.fetch-user-applications"
		}).then((apps) => (projectIndex[projectId] = apps.map((app) => (appCache[app.id] = app, app.id)), apps)).catch((err) => (debug$1("Fetching user application failed.", { err }), delete projectIndex[projectId], []));
		return projectIndex[projectId] = promise.then((apps) => apps.map((app) => app.id)), promise;
	} };
}
/**
* Provider that creates and provides a cache for user applications.
* The cache is shared across all consumers to avoid duplicate API fetches.
* @internal
*/
function UserApplicationCacheProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(5), { children } = t0, parentCache = (0, import_react.useContext)(UserApplicationCacheContext), t1;
	$[0] === parentCache ? t1 = $[1] : (t1 = parentCache || createUserApplicationCache(), $[0] = parentCache, $[1] = t1);
	let cache = t1, t2;
	return $[2] !== cache || $[3] !== children ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserApplicationCacheContext.Provider, {
		value: cache,
		children
	}), $[2] = cache, $[3] = children, $[4] = t2) : t2 = $[4], t2;
}
/**
* Hook to access the user applications cache.
* @internal
*/
function useUserApplicationCache() {
	let cache = (0, import_react.useContext)(UserApplicationCacheContext);
	if (!cache) throw Error("UserApplicationCache: missing context value. Ensure the component is wrapped in a UserApplicationCacheProvider.");
	return cache;
}
var USER_COLOR_EXCLUDE_HUES = [
	"green",
	"red",
	"gray"
];
var defaultHues = COLOR_HUES.filter((hue) => !USER_COLOR_EXCLUDE_HUES.includes(hue));
var getTints = (scheme) => {
	let isDarkScheme = scheme === "dark";
	return {
		background: isDarkScheme ? "900" : "100",
		border: isDarkScheme ? "700" : "300",
		text: isDarkScheme ? "200" : "700"
	};
};
var getDefaultColors = (scheme) => {
	let { background, border, text } = getTints(scheme);
	return defaultHues.reduce((colors, hue) => (colors[hue] = {
		name: hue,
		background: hues[hue][background].hex,
		border: hues[hue][border].hex,
		text: hues[hue][text].hex,
		tints: hues[hue]
	}, colors), {});
};
var getAnonymousColor = (scheme) => {
	let { background, border, text } = getTints(scheme);
	return {
		name: "gray",
		background: hues.gray[background].hex,
		border: hues.gray[border].hex,
		text: hues.gray[text].hex,
		tints: hues.gray
	};
};
/** @internal */
function createUserColorManager(options) {
	let { anonymousColor: anonymousColorProp, colors, currentUserColor: currentUserColorProp, scheme } = options, userColors = colors || getDefaultColors(scheme), anonymousColor = anonymousColorProp || getAnonymousColor(scheme), currentUserColor = currentUserColorProp || "purple";
	if (!userColors.hasOwnProperty(currentUserColor)) throw Error(`'colors' must contain 'currentUserColor' (${currentUserColor})`);
	let userColorKeys = Object.keys(userColors), subscriptions = /* @__PURE__ */ new Map(), previouslyAssigned = /* @__PURE__ */ new Map(), assignedCounts = userColorKeys.reduce((counts, color) => (counts[color] = 0, counts), {}), assigned = /* @__PURE__ */ new Map(), currentUserId;
	return options?.userStore && options.userStore.me.subscribe((user) => setCurrentUser(user ? user.id : null)), {
		get,
		listen
	};
	function get(userId) {
		return userId ? userColors[getUserHue(userId)] : anonymousColor;
	}
	function getUserHue(userId) {
		if (userId === currentUserId) return currentUserColor;
		let assignedHue = assigned.get(userId);
		if (assignedHue) return assignedHue;
		let prevHue = previouslyAssigned.get(userId);
		if (prevHue && (assignedCounts[prevHue] === 0 || !hasUnusedColor())) return assignHue(userId, prevHue);
		let preferredHue = getPreferredHue(userId);
		return assignedCounts[preferredHue] === 0 ? assignHue(userId, preferredHue) : assignHue(userId, getLeastUsedHue(prevHue));
	}
	function listen(userId) {
		let subscription = subscriptions.get(userId);
		return subscription || (subscription = getObservableColor(userId, getUserHue(userId)), subscriptions.set(userId, subscription), subscription);
	}
	function assignHue(userId, hue) {
		return assigned.set(userId, hue), previouslyAssigned.set(userId, hue), assignedCounts[hue]++, hue;
	}
	function unassignHue(userId, hue) {
		assigned.delete(userId), assignedCounts[hue]--;
	}
	function getUnusedColor() {
		return userColorKeys.find((colorHue) => assignedCounts[colorHue] === 0);
	}
	function hasUnusedColor() {
		return !!getUnusedColor();
	}
	function getLeastUsedHue(tieBreakerPreference) {
		let leastUses = Infinity, leastUsed = [];
		return userColorKeys.forEach((colorHue) => {
			let uses = assignedCounts[colorHue];
			uses === leastUses ? leastUsed.push(colorHue) : uses < leastUses && (leastUses = uses, leastUsed = [colorHue]);
		}), tieBreakerPreference && leastUsed.includes(tieBreakerPreference) ? tieBreakerPreference : leastUsed[0];
	}
	function getObservableColor(userId, hue) {
		return new Observable((subscriber) => {
			let userColor = userColors[hue];
			return subscriber.next(userColor), () => {
				subscriptions.delete(userId), unassignHue(userId, hue);
			};
		}).pipe(shareReplay({ refCount: !0 }));
	}
	function setCurrentUser(userId) {
		currentUserId = userId, assignedCounts[currentUserColor] += userId ? 1 : -1;
	}
	function getPreferredHue(userId) {
		let hash = 0;
		for (let i = 0; i < userId.length; i++) hash = (hash << 5) - hash + userId.charCodeAt(i) | 0;
		return userColorKeys[Math.abs(hash) % userColorKeys.length];
	}
}
/** @internal */
function UserColorManagerProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(6), { children, manager: managerFromProps } = t0, scheme = useColorSchemeValue(), t1;
	$[0] !== managerFromProps || $[1] !== scheme ? (t1 = managerFromProps || createUserColorManager({ scheme }), $[0] = managerFromProps, $[1] = scheme, $[2] = t1) : t1 = $[2];
	let manager = t1, t2;
	return $[3] !== children || $[4] !== manager ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserColorManagerContext.Provider, {
		value: manager,
		children
	}), $[3] = children, $[4] = manager, $[5] = t2) : t2 = $[5], t2;
}
/** Fired when the AuthBoundary resolves from 'loading' to a final auth state. */
var AuthBoundaryResolved = defineEvent({
	name: "Auth Boundary Resolved",
	version: 2,
	description: "Time from AuthBoundary mount until auth state is resolved"
});
var SessionTokenExchangeCompleted = defineEvent({
	name: "Session Token Exchange Completed",
	version: 2,
	description: "Result of the auth exchange flow in handleCallbackUrl"
});
var LINKS = [
	{
		url: "https://snty.link/community",
		i18nKey: "workspaces.community-title",
		title: "Community"
	},
	{
		url: "https://www.sanity.io/docs",
		i18nKey: "workspaces.docs-title",
		title: "Docs"
	},
	{
		url: "https://www.sanity.io/legal/privacy",
		i18nKey: "workspaces.privacy-title",
		title: "Privacy"
	},
	{
		url: "https://www.sanity.io",
		i18nKey: "workspaces.sanity-io-title",
		title: "sanity.io"
	}
];
var StyledText = gt(Text).withConfig({
	displayName: "StyledText",
	componentId: "sc-8o9yl2-0"
})`a{color:inherit;}`;
function Layout(props) {
	let $ = (0, import_compiler_runtime.c)(23), { children, footer, header } = props, prefersDark = usePrefersDark(), t0;
	$[0] === header ? t0 = $[1] : (t0 = typeof header == "object" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, { children: header }), $[0] = header, $[1] = t0);
	let t1;
	$[2] === header ? t1 = $[3] : (t1 = typeof header == "string" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingY: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Heading, {
			align: "center",
			size: 1,
			children: header
		})
	}), $[2] = header, $[3] = t1);
	let t2 = !!header, t3 = !!footer, t4;
	$[4] !== children || $[5] !== t2 || $[6] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingX: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
			borderTop: t2,
			borderBottom: t3,
			children
		})
	}), $[4] = children, $[5] = t2, $[6] = t3, $[7] = t4) : t4 = $[7];
	let t5;
	$[8] === footer ? t5 = $[9] : (t5 = footer && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, { children: footer }), $[8] = footer, $[9] = t5);
	let t6;
	$[10] !== t0 || $[11] !== t1 || $[12] !== t4 || $[13] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		border: !0,
		radius: 3,
		overflow: "auto",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, { children: [
			t0,
			t1,
			t4,
			t5
		] })
	}), $[10] = t0, $[11] = t1, $[12] = t4, $[13] = t5, $[14] = t6) : t6 = $[14];
	let t7;
	$[15] === prefersDark ? t7 = $[16] : (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SanityLogo, { dark: prefersDark })
	}), $[15] = prefersDark, $[16] = t7);
	let t8;
	$[17] === Symbol.for("react.memo_cache_sentinel") ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		align: "center",
		gap: 2,
		children: LINKS.map(_temp$12)
	}), $[17] = t8) : t8 = $[17];
	let t9;
	$[18] === t7 ? t9 = $[19] : (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		direction: "column",
		gap: 4,
		justify: "center",
		align: "center",
		paddingBottom: 4,
		children: [t7, t8]
	}), $[18] = t7, $[19] = t9);
	let t10;
	return $[20] !== t6 || $[21] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 6,
		children: [t6, t9]
	}), $[20] = t6, $[21] = t9, $[22] = t10) : t10 = $[22], t10;
}
function _temp$12(link, index) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(StyledText, {
		muted: !0,
		size: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
			href: link.url,
			target: "_blank",
			rel: "noopener noreferrer",
			children: link.title
		})
	}), index < LINKS.length - 1 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		muted: !0,
		children: "•"
	})] }, link.title);
}
/**
* A single workspace card on the login screen. Probes `/auth/id`
* independently so the list renders immediately and each row resolves
* its state per-workspace.
*
* @internal
*/
function WorkspaceAuthCard(t0) {
	let $ = (0, import_compiler_runtime.c)(15), { workspace, onSelect } = t0, t1;
	$[0] !== workspace.apiHost || $[1] !== workspace.dataset || $[2] !== workspace.projectId ? (t1 = probeWorkspaceAuth({
		projectId: workspace.projectId,
		dataset: workspace.dataset,
		apiHost: workspace.apiHost
	}), $[0] = workspace.apiHost, $[1] = workspace.dataset, $[2] = workspace.projectId, $[3] = t1) : t1 = $[3];
	let probe = useObservable(t1), state = probe ? probe.authenticated ? "logged-in" : workspace.auth.LoginComponent ? "logged-out" : "no-access" : "loading", t2;
	$[4] !== onSelect || $[5] !== state ? (t2 = () => onSelect(state), $[4] = onSelect, $[5] = state, $[6] = t2) : t2 = $[6];
	let t3 = workspace?.icon, t4 = workspace?.subtitle, t5 = workspace?.title || workspace.name, t6;
	$[7] !== state || $[8] !== t3 || $[9] !== t4 || $[10] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspacePreview, {
		icon: t3,
		iconRight: ChevronRightIcon,
		state,
		subtitle: t4,
		title: t5
	}), $[7] = state, $[8] = t3, $[9] = t4, $[10] = t5, $[11] = t6) : t6 = $[11];
	let t7;
	return $[12] !== t2 || $[13] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		as: "button",
		radius: 2,
		padding: 2,
		onClick: t2,
		children: t6
	}), $[12] = t2, $[13] = t6, $[14] = t7) : t7 = $[14], t7;
}
var StyledContainer = /* @__PURE__ */ gt(Container).withConfig({
	displayName: "StyledContainer",
	componentId: "sc-eiwy75-0"
})((props) => {
	let { container } = getTheme_v2(props.theme);
	return {
		width: "auto",
		minWidth: rem(container[0])
	};
});
function WorkspaceAuth() {
	let $ = (0, import_compiler_runtime.c)(42), { visibleWorkspaces } = useVisibleWorkspaces(), { activeWorkspace, setActiveWorkspace } = useActiveWorkspace(), [showChooser, setShowChooser] = (0, import_react.useState)(!1), { t } = useTranslation$1(), canChooseAnotherWorkspace = visibleWorkspaces.length > 1, selectedWorkspace = activeWorkspace, LoginComponent = selectedWorkspace?.auth?.LoginComponent, t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = () => setShowChooser(!0), $[0] = t0) : t0 = $[0];
	let handleBack = t0, t1;
	$[1] !== activeWorkspace.name || $[2] !== setActiveWorkspace ? (t1 = (workspaceName, state) => {
		(state === "logged-in" || state === "loading" || state === "logged-out") && (workspaceName !== activeWorkspace.name && setActiveWorkspace(workspaceName), setShowChooser(!1));
	}, $[1] = activeWorkspace.name, $[2] = setActiveWorkspace, $[3] = t1) : t1 = $[3];
	let handleCardSelect = t1;
	if (LoginComponent && selectedWorkspace && !showChooser) {
		let t2;
		$[4] !== canChooseAnotherWorkspace || $[5] !== t ? (t2 = canChooseAnotherWorkspace && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			icon: ArrowLeftIcon,
			mode: "bleed",
			onClick: handleBack,
			text: t("workspaces.action.choose-another-workspace")
		}) }), $[4] = canChooseAnotherWorkspace, $[5] = t, $[6] = t2) : t2 = $[6];
		let t3 = selectedWorkspace?.subtitle, t4;
		$[7] !== selectedWorkspace.icon || $[8] !== selectedWorkspace.title || $[9] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			padding: 3,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspacePreview, {
				icon: selectedWorkspace.icon,
				title: selectedWorkspace.title,
				subtitle: t3
			})
		}), $[7] = selectedWorkspace.icon, $[8] = selectedWorkspace.title, $[9] = t3, $[10] = t4) : t4 = $[10];
		let t5;
		$[11] === selectedWorkspace.basePath ? t5 = $[12] : (t5 = window.location.pathname.startsWith(selectedWorkspace.basePath) ? `${window.location.pathname}${window.location.search}` : selectedWorkspace.basePath, $[11] = selectedWorkspace.basePath, $[12] = t5);
		let t6 = canChooseAnotherWorkspace ? handleBack : void 0, t7;
		$[13] !== LoginComponent || $[14] !== selectedWorkspace.name || $[15] !== selectedWorkspace.projectId || $[16] !== t5 || $[17] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
			padding: 2,
			paddingBottom: 3,
			paddingTop: 4,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoginComponent, {
				projectId: selectedWorkspace.projectId,
				redirectPath: t5,
				onChooseAnotherWorkspace: t6
			}, selectedWorkspace.name)
		}), $[13] = LoginComponent, $[14] = selectedWorkspace.name, $[15] = selectedWorkspace.projectId, $[16] = t5, $[17] = t6, $[18] = t7) : t7 = $[18];
		let t8;
		$[19] !== t4 || $[20] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Layout, {
			header: t4,
			children: t7
		}), $[19] = t4, $[20] = t7, $[21] = t8) : t8 = $[21];
		let t9;
		return $[22] !== t2 || $[23] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Container, {
			width: 0,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
				gap: 2,
				children: [t2, t8]
			})
		}), $[22] = t2, $[23] = t8, $[24] = t9) : t9 = $[24], t9;
	}
	let t2;
	$[25] === t ? t2 = $[26] : (t2 = t("workspaces.choose-your-workspace-label"), $[25] = t, $[26] = t2);
	let t3;
	$[27] === t ? t3 = $[28] : (t3 = t("workspaces.action.add-workspace"), $[27] = t, $[28] = t3);
	let t4;
	$[29] === t3 ? t4 = $[30] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		padding: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			as: "a",
			href: "https://www.sanity.io/docs/workspaces",
			icon: AddIcon,
			mode: "bleed",
			rel: "noopener noreferrer",
			size: "large",
			target: "__blank",
			text: t3
		})
	}), $[29] = t3, $[30] = t4);
	let t5;
	if ($[31] !== handleCardSelect || $[32] !== visibleWorkspaces) {
		let t6;
		$[34] === handleCardSelect ? t6 = $[35] : (t6 = (workspace) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspaceAuthCard, {
			workspace,
			onSelect: (state_0) => handleCardSelect(workspace.name, state_0)
		}, workspace.name), $[34] = handleCardSelect, $[35] = t6), t5 = visibleWorkspaces.map(t6), $[31] = handleCardSelect, $[32] = visibleWorkspaces, $[33] = t5;
	} else t5 = $[33];
	let t6;
	$[36] === t5 ? t6 = $[37] : (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		gap: 1,
		paddingX: 1,
		paddingY: 2,
		children: t5
	}), $[36] = t5, $[37] = t6);
	let t7;
	return $[38] !== t2 || $[39] !== t4 || $[40] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StyledContainer, {
		width: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Layout, {
			header: t2,
			footer: t4,
			children: t6
		})
	}), $[38] = t2, $[39] = t4, $[40] = t6, $[41] = t7) : t7 = $[41], t7;
}
/**
* Shows a toast explaining why the studio logged the user out, when present.
* Rendered on the auth screen rather than at the moment of the 401: by the
* time the login screen mounts the forced-logout teardown has settled, so the
* toast lands on a stable tree (pushing it mid-teardown gets lost).
*
* Backed by `useConditionalToast`, so it stays up while a logged-out reason is
* present and is dismissed when that clears — including when the user signs
* back in (here or in another tab, which clears the reason cross-tab) and when
* this component unmounts.
*
* @internal
*/
function LoggedOutToast() {
	let $ = (0, import_compiler_runtime.c)(9), reason = (0, import_react.useContext)(LoggedOutReasonContext), { t } = useTranslation$1(), t0 = !!reason, t1;
	$[0] === t ? t1 = $[1] : (t1 = t("login.logged-out.title"), $[0] = t, $[1] = t1);
	let t2;
	$[2] !== reason || $[3] !== t ? (t2 = t(reason === "session-expired" ? "login.logged-out.session-expired" : "login.logged-out.generic"), $[2] = reason, $[3] = t, $[4] = t2) : t2 = $[4];
	let t3;
	return $[5] !== t0 || $[6] !== t1 || $[7] !== t2 ? (t3 = {
		id: "studio-logged-out",
		enabled: t0,
		status: "info",
		closable: !0,
		title: t1,
		description: t2
	}, $[5] = t0, $[6] = t1, $[7] = t2, $[8] = t3) : t3 = $[8], useConditionalToast(t3), null;
}
function AuthenticateScreen() {
	let $ = (0, import_compiler_runtime.c)(2), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoggedOutToast, {}), $[0] = t0) : t0 = $[0];
	let t1;
	return $[1] === Symbol.for("react.memo_cache_sentinel") ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Card, {
		height: "fill",
		overflow: "auto",
		paddingX: 4,
		children: [t0, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
			height: "fill",
			direction: "column",
			align: "center",
			justify: "center",
			paddingTop: 4,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspaceAuth, {})
		})]
	}), $[1] = t1) : t1 = $[1], t1;
}
function NotAuthenticatedScreen() {
	let $ = (0, import_compiler_runtime.c)(16), { activeWorkspace } = useActiveWorkspace(), t0;
	$[0] === activeWorkspace.auth ? t0 = $[1] : (t0 = () => {
		activeWorkspace.auth.logout?.();
	}, $[0] = activeWorkspace.auth, $[1] = t0);
	let handleLogout = t0, currentUser = useObservable(activeWorkspace.auth.state, null)?.currentUser, t1 = currentUser?.provider, t2;
	$[2] === t1 ? t2 = $[3] : (t2 = getProviderTitle(t1), $[2] = t1, $[3] = t2);
	let providerTitle = t2, providerHelp = providerTitle ? ` through ${providerTitle}` : "", t3;
	$[4] === handleLogout ? t3 = $[5] : (t3 = { confirmButton: {
		text: "Sign out",
		onClick: handleLogout,
		tone: "default"
	} }, $[4] = handleLogout, $[5] = t3);
	let t4;
	$[6] === Symbol.for("react.memo_cache_sentinel") ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, { children: "You are not authorized to access this studio. Please contact someone with access to invite you to this project." }), $[6] = t4) : t4 = $[6];
	let t5 = currentUser?.name, t6 = currentUser?.email, t7;
	$[7] !== t5 || $[8] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("strong", { children: [
		t5,
		" (",
		t6,
		")"
	] }), $[7] = t5, $[8] = t6, $[9] = t7) : t7 = $[9];
	let t8;
	$[10] !== providerHelp || $[11] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 4,
		children: [t4, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, { children: [
			"If you think this is an error, verify that you are signed in with the correct account. You are currently signed in as",
			" ",
			t7,
			providerHelp,
			"."
		] })]
	}), $[10] = providerHelp, $[11] = t7, $[12] = t8) : t8 = $[12];
	let t9;
	return $[13] !== t3 || $[14] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		height: "fill",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog$1, {
			id: "not-authorized-dialog",
			header: "Not authorized",
			width: 1,
			footer: t3,
			children: t8
		})
	}), $[13] = t3, $[14] = t8, $[15] = t9) : t9 = $[15], t9;
}
function deriveAccessRequestStatus(requests, projectId) {
	if (!requests || !requests.length) return {
		hasBeenDenied: !1,
		hasPendingRequest: !1,
		hasExpiredPendingRequest: !1
	};
	let projectRequests = requests.filter((request) => request.resourceId === projectId), declinedRequest = projectRequests.find((request) => request.status === "declined");
	return declinedRequest && isAfter(addWeeks(new Date(declinedRequest.createdAt), 2), /* @__PURE__ */ new Date()) ? {
		hasBeenDenied: !0,
		hasPendingRequest: !1,
		hasExpiredPendingRequest: !1
	} : projectRequests.find((request) => request.status === "pending" && isAfter(addWeeks(new Date(request.createdAt), 2), /* @__PURE__ */ new Date())) ? {
		hasBeenDenied: !1,
		hasPendingRequest: !0,
		hasExpiredPendingRequest: !1
	} : projectRequests.find((request) => request.status === "pending" && isBefore(addWeeks(new Date(request.createdAt), 2), /* @__PURE__ */ new Date())) ? {
		hasBeenDenied: !1,
		hasPendingRequest: !1,
		hasExpiredPendingRequest: !0
	} : {
		hasBeenDenied: !1,
		hasPendingRequest: !1,
		hasExpiredPendingRequest: !1
	};
}
function RequestAccessScreen() {
	let $ = (0, import_compiler_runtime.c)(21), { activeWorkspace } = useActiveWorkspace(), t0;
	$[0] === activeWorkspace.auth ? t0 = $[1] : (t0 = () => {
		activeWorkspace.auth.logout?.();
	}, $[0] = activeWorkspace.auth, $[1] = t0);
	let handleLogout = t0, auth = useSyncObservable(activeWorkspace.auth.state, null), currentUser = auth?.currentUser, t1;
	$[2] === auth?.client ? t1 = $[3] : (t1 = auth?.client.config().projectId, $[2] = auth?.client, $[3] = t1);
	let projectId = t1;
	auth?.client;
	let t2;
	$[4] === auth?.client ? t2 = $[5] : (t2 = auth?.client.withConfig({ apiVersion: "2024-07-01" }), $[4] = auth?.client, $[5] = t2);
	let client = t2, t3;
	bb0: {
		if (!client || !projectId) {
			t3 = NEVER;
			break bb0;
		}
		let t4;
		if ($[6] !== client.observable || $[7] !== projectId) {
			let t5;
			$[9] === projectId ? t5 = $[10] : (t5 = (requests) => ({
				error: !1,
				...deriveAccessRequestStatus(requests, projectId)
			}), $[9] = projectId, $[10] = t5), t4 = client.observable.request({
				url: "/access/requests/me",
				tag: "request-access"
			}).pipe(map(t5), catchError(_temp$11)), $[6] = client.observable, $[7] = projectId, $[8] = t4;
		} else t4 = $[8];
		t3 = t4;
	}
	let accessRequests$ = t3, t4 = !client || !projectId, t5;
	$[11] === t4 ? t5 = $[12] : (t5 = { disabled: t4 }, $[11] = t4, $[12] = t5);
	let accessRequestsPromise = useObservablePromise(accessRequests$, t5);
	if (!client || !projectId) {
		let t6;
		return $[13] === Symbol.for("react.memo_cache_sentinel") ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingBlock, {}), $[13] = t6) : t6 = $[13], t6;
	}
	let t6;
	$[14] === Symbol.for("react.memo_cache_sentinel") ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingBlock, {}), $[14] = t6) : t6 = $[14];
	let t7 = currentUser ?? null, t8;
	return $[15] !== accessRequestsPromise || $[16] !== client || $[17] !== handleLogout || $[18] !== projectId || $[19] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Suspense, {
		fallback: t6,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RequestAccessScreenContent, {
			accessRequestsPromise,
			client,
			projectId,
			currentUser: t7,
			onLogout: handleLogout
		})
	}), $[15] = accessRequestsPromise, $[16] = client, $[17] = handleLogout, $[18] = projectId, $[19] = t7, $[20] = t8) : t8 = $[20], t8;
}
function _temp$11(err) {
	return console.error(err), of({
		hasBeenDenied: !1,
		hasPendingRequest: !1,
		hasExpiredPendingRequest: !1,
		error: !0
	});
}
function RequestAccessScreenContent(t0) {
	let $ = (0, import_compiler_runtime.c)(45), { accessRequestsPromise, client, projectId, currentUser, onLogout } = t0, t1 = (0, import_react.use)(accessRequestsPromise), error, fetched, hasExpiredPendingRequest;
	$[0] === t1 ? (error = $[1], fetched = $[2], hasExpiredPendingRequest = $[3]) : ({error, hasExpiredPendingRequest, ...fetched} = t1, $[0] = t1, $[1] = error, $[2] = fetched, $[3] = hasExpiredPendingRequest);
	let toast = useToast(), [msgError, setMsgError] = (0, import_react.useState)(), [isSubmitting, setIsSubmitting] = (0, import_react.useState)(!1), [submittedRequest, setSubmittedRequest] = (0, import_react.useState)(!1), [hasTooManyRequests, setHasTooManyRequests] = (0, import_react.useState)(!1), [submitDenied, setSubmitDenied] = (0, import_react.useState)(!1), [note, setNote] = (0, import_react.useState)(), hasPendingRequest = fetched.hasPendingRequest || submittedRequest, hasBeenDenied = fetched.hasBeenDenied || submitDenied, noteLength = note?.length ?? 0, t2;
	$[4] !== client || $[5] !== note || $[6] !== projectId || $[7] !== toast ? (t2 = () => {
		setIsSubmitting(!0), client.request({
			url: `/access/project/${projectId}/requests`,
			method: "post",
			body: {
				note,
				requestUrl: window?.location.href,
				type: "access"
			}
		}).then((request) => {
			request && setSubmittedRequest(!0);
		}).catch((err) => {
			let statusCode = err?.response?.statusCode, errMessage = err?.response?.body?.message;
			statusCode === 429 ? (setHasTooManyRequests(!0), setMsgError(errMessage)) : statusCode === 409 ? (setSubmitDenied(!0), setMsgError(errMessage)) : toast.push({
				title: "There was a problem submitting your request.",
				status: "error"
			});
		}).finally(() => {
			setIsSubmitting(!1);
		});
	}, $[4] = client, $[5] = note, $[6] = projectId, $[7] = toast, $[8] = t2) : t2 = $[8];
	let handleSubmitRequest = t2, t3 = currentUser?.provider, t4;
	$[9] === t3 ? t4 = $[10] : (t4 = getProviderTitle(t3), $[9] = t3, $[10] = t4);
	let providerTitle = t4, providerHelp = providerTitle ? ` through ${providerTitle}` : "";
	if (error) {
		let t5;
		return $[11] === Symbol.for("react.memo_cache_sentinel") ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NotAuthenticatedScreen, {}), $[11] = t5) : t5 = $[11], t5;
	}
	let t5 = currentUser?.name, t6 = currentUser?.email, t7;
	$[12] !== t5 || $[13] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("strong", { children: [
		t5,
		" (",
		t6,
		")"
	] }), $[12] = t5, $[13] = t6, $[14] = t7) : t7 = $[14];
	let t8;
	$[15] !== providerHelp || $[16] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, { children: [
		"You are not authorized to access this studio (currently signed in as",
		" ",
		t7,
		providerHelp,
		")."
	] }), $[15] = providerHelp, $[16] = t7, $[17] = t8) : t8 = $[17];
	let t9;
	$[18] !== handleSubmitRequest || $[19] !== hasBeenDenied || $[20] !== hasExpiredPendingRequest || $[21] !== hasPendingRequest || $[22] !== hasTooManyRequests || $[23] !== isSubmitting || $[24] !== msgError || $[25] !== note || $[26] !== noteLength ? (t9 = hasTooManyRequests || hasPendingRequest || hasBeenDenied ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		tone: hasPendingRequest ? "transparent" : "caution",
		padding: 3,
		radius: 2,
		shadow: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
			size: 1,
			children: [
				hasTooManyRequests && !hasPendingRequest && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: msgError ?? "You've reached the limit for access requests across all projects. Please wait\n                      before submitting more requests or contact an admin for assistance." }),
				hasPendingRequest && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: "Your request to access this project is pending approval." }),
				hasBeenDenied && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: msgError ?? "Your request to access this project has been declined." })
			]
		})
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, { children: hasExpiredPendingRequest ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: "Your previous request has expired. You may again request access below with an optional note. The administrator(s) will receive an email letting them know that you are requesting access." }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: "You can request access below with an optional note. The administrator(s) will receive an email letting them know that you are requesting access." }) }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 3,
		paddingBottom: 0,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextInput, {
			maxLength: 150,
			disabled: isSubmitting,
			onKeyDown: (e) => {
				e.key === "Enter" && handleSubmitRequest();
			},
			onChange: (e_0) => setNote(e_0.currentTarget.value),
			value: note,
			placeholder: "Add your note…"
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			align: "right",
			muted: !0,
			size: 1,
			children: `${noteLength}/150`
		})]
	})] }), $[18] = handleSubmitRequest, $[19] = hasBeenDenied, $[20] = hasExpiredPendingRequest, $[21] = hasPendingRequest, $[22] = hasTooManyRequests, $[23] = isSubmitting, $[24] = msgError, $[25] = note, $[26] = noteLength, $[27] = t9) : t9 = $[27];
	let t10;
	$[28] !== t8 || $[29] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 4,
		children: [t8, t9]
	}), $[28] = t8, $[29] = t9, $[30] = t10) : t10 = $[30];
	let t11;
	$[31] === onLogout ? t11 = $[32] : (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		mode: "bleed",
		text: "Sign out",
		tone: "default",
		onClick: onLogout,
		size: "large"
	}), $[31] = onLogout, $[32] = t11);
	let t12;
	$[33] !== handleSubmitRequest || $[34] !== hasBeenDenied || $[35] !== hasPendingRequest || $[36] !== hasTooManyRequests || $[37] !== isSubmitting ? (t12 = !hasTooManyRequests && !hasBeenDenied && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		mode: "default",
		text: hasPendingRequest ? "Request sent" : "Request access",
		disabled: hasPendingRequest || isSubmitting,
		loading: isSubmitting,
		tone: "default",
		onClick: handleSubmitRequest,
		size: "large"
	}), $[33] = handleSubmitRequest, $[34] = hasBeenDenied, $[35] = hasPendingRequest, $[36] = hasTooManyRequests, $[37] = isSubmitting, $[38] = t12) : t12 = $[38];
	let t13;
	$[39] !== t11 || $[40] !== t12 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		justify: "space-between",
		paddingTop: 4,
		children: [t11, t12]
	}), $[39] = t11, $[40] = t12, $[41] = t13) : t13 = $[41];
	let t14;
	return $[42] !== t10 || $[43] !== t13 ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		height: "fill",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog$1, {
			id: "not-authorized-dialog",
			header: "Not authorized",
			width: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Box, { children: [t10, t13] })
		})
	}), $[42] = t10, $[43] = t13, $[44] = t14) : t14 = $[44], t14;
}
var authReadyFired = !1;
var INITIAL_AUTH_RESULT = {
	type: "value",
	status: { loggedIn: "loading" }
};
function AuthBoundary(t0) {
	let $ = (0, import_compiler_runtime.c)(26), { children, AuthenticateComponent: t1, LoadingComponent: t2, NotAuthenticatedComponent: t3 } = t0, AuthenticateComponent = t1 === void 0 ? AuthenticateScreen : t1, LoadingComponent = t2 === void 0 ? LoadingBlock : t2, NotAuthenticatedComponent = t3 === void 0 ? NotAuthenticatedScreen : t3, [error, handleError] = (0, import_react.useState)(null);
	if (error) throw error;
	let [callbackSettledFor, setCallbackSettledFor] = (0, import_react.useState)(void 0), { activeWorkspace } = useActiveWorkspace(), callbackSettled = !activeWorkspace.auth.handleCallbackUrl || callbackSettledFor === activeWorkspace.auth, telemetry = useTelemetry(), [mountTime] = (0, import_react.useState)(_temp$10), t4;
	$[0] === activeWorkspace.auth.state ? t4 = $[1] : (t4 = activeWorkspace.auth.state.pipe(map(_temp2$6), catchError(_temp3$4)), $[0] = activeWorkspace.auth.state, $[1] = t4);
	let authResult = useSyncObservable(t4, INITIAL_AUTH_RESULT);
	if (authResult.type === "error") throw authResult.error;
	let { loggedIn, loginProvider } = authResult.status, t5, t6;
	$[2] !== loggedIn || $[3] !== mountTime || $[4] !== telemetry ? (t5 = () => {
		loggedIn !== "loading" && telemetry.log(AuthBoundaryResolved, {
			durationMs: Math.round(performance.now() - mountTime),
			result: loggedIn
		});
	}, t6 = [
		loggedIn,
		telemetry,
		mountTime
	], $[2] = loggedIn, $[3] = mountTime, $[4] = telemetry, $[5] = t5, $[6] = t6) : (t5 = $[5], t6 = $[6]), (0, import_react.useEffect)(t5, t6);
	let t7, t8;
	$[7] !== loggedIn || $[8] !== telemetry ? (t7 = () => {
		if (authReadyFired || loggedIn === "loading") return;
		authReadyFired = !0;
		let durationMs = performance.now();
		telemetry.log(StudioAuthReadyMeasured, {
			durationMs,
			authState: loggedIn,
			...getPageVisibilitySnapshot(durationMs)
		});
	}, t8 = [loggedIn, telemetry], $[7] = loggedIn, $[8] = telemetry, $[9] = t7, $[10] = t8) : (t7 = $[9], t8 = $[10]), (0, import_react.useEffect)(t7, t8);
	let t10, t9;
	if ($[11] !== activeWorkspace.auth || $[12] !== telemetry ? (t9 = () => {
		let auth = activeWorkspace.auth, superseded = !1;
		return auth.handleCallbackUrl?.().then((result) => {
			telemetry.log(SessionTokenExchangeCompleted, result);
		}).catch(handleError).finally(() => {
			superseded || setCallbackSettledFor(auth);
		}), () => {
			superseded = !0;
		};
	}, t10 = [activeWorkspace.auth, telemetry], $[11] = activeWorkspace.auth, $[12] = telemetry, $[13] = t10, $[14] = t9) : (t10 = $[13], t9 = $[14]), (0, import_react.useEffect)(t9, t10), loggedIn === "loading") {
		let t11;
		return $[15] === LoadingComponent ? t11 = $[16] : (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingComponent, {}), $[15] = LoadingComponent, $[16] = t11), t11;
	}
	if (loggedIn === "unauthorized") {
		if (!loginProvider || loginProvider === "sanity") {
			let t11;
			return $[17] === NotAuthenticatedComponent ? t11 = $[18] : (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NotAuthenticatedComponent, {}), $[17] = NotAuthenticatedComponent, $[18] = t11), t11;
		}
		let t11;
		return $[19] === Symbol.for("react.memo_cache_sentinel") ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RequestAccessScreen, {}), $[19] = t11) : t11 = $[19], t11;
	}
	if (loggedIn === "logged-out" && !callbackSettled) {
		let t11;
		return $[20] === LoadingComponent ? t11 = $[21] : (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingComponent, {}), $[20] = LoadingComponent, $[21] = t11), t11;
	}
	if (loggedIn === "logged-out") {
		let t11;
		return $[22] === AuthenticateComponent ? t11 = $[23] : (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AuthenticateComponent, {}), $[22] = AuthenticateComponent, $[23] = t11), t11;
	}
	let t11;
	return $[24] === children ? t11 = $[25] : (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children }), $[24] = children, $[25] = t11), t11;
}
function _temp3$4(err) {
	return of({
		type: "error",
		error: err
	});
}
function _temp2$6(t0) {
	let { authenticated, currentUser } = t0;
	return authenticated && (!Array.isArray(currentUser?.roles) || currentUser.roles.length === 0) ? {
		type: "value",
		status: {
			loggedIn: "unauthorized",
			loginProvider: currentUser?.provider
		}
	} : {
		type: "value",
		status: { loggedIn: authenticated ? "logged-in" : "logged-out" }
	};
}
function _temp$10() {
	return performance.now();
}
/**
* Perform a navigation when a Comlink `dashboard/v1/history/change-path` event is emitted.
*
* Note: this subscription cannot be colocated with the router code because it must retrieve the Comlink store from the Resource Cache, which is rendered beneath the router provider.
*/
function useComlinkRouteHandler() {
	let $ = (0, import_compiler_runtime.c)(4), { node } = useComlinkStore(), { navigateUrl } = useRouter(), t0, t1;
	$[0] !== navigateUrl || $[1] !== node ? (t0 = () => node?.on("dashboard/v1/history/change-path", (t2) => {
		let { path } = t2;
		navigateUrl({
			path: sanitizePath(path),
			replace: !1
		});
	}), t1 = [navigateUrl, node], $[0] = navigateUrl, $[1] = node, $[2] = t0, $[3] = t1) : (t0 = $[2], t1 = $[3]), (0, import_react.useEffect)(t0, t1);
}
/**
* Ensure no more than one `/` character occurs at the beginning of the provided string.
*/
function sanitizePath(path) {
	return path.replace(/^\/+/, "/");
}
var ComlinkRouteHandler = () => (useComlinkRouteHandler(), null);
function getAppUrl(app, internalHost) {
	return app.urlType === "internal" ? `https://${app.appHost}.${internalHost}` : app.appHost;
}
/**
* Check if the current URL matches an app URL.
* Matches if the pathname is exactly equal or is a subpath (on segment boundaries).
*/
function currentUrlMatchesApp({ origin, pathname }, appUrl) {
	let url = new URL(appUrl);
	if (origin !== url.origin) return !1;
	let appPathname = url.pathname;
	return pathname === appPathname || pathname.startsWith(appPathname.endsWith("/") ? appPathname : `${appPathname}/`);
}
async function findUserApplication(cache, workspaces) {
	if (typeof window > "u" || !window.location || workspaces.length === 0) return;
	let workspace = workspaces[0], state = await firstValueFrom(workspace.auth.state);
	if (!state.authenticated) return;
	let internalHost = "sanity.studio";
	return workspace.apiHost !== void 0 && workspace.apiHost.includes("sanity.work") && (internalHost = "studio.sanity.work"), (await cache.get(state.client)).reduce((best, app) => {
		let appUrl = getAppUrl(app, internalHost);
		return appUrl && currentUrlMatchesApp(window.location, appUrl) && appUrl.length > best[1].length ? [app, appUrl] : best;
	}, [void 0, ""])[0];
}
var debug = (0, import_browser.default)("studio:live-user-application");
/**
* Provider that automatically uploads the studio manifest when the Studio loads.
* This runs once when all workspaces are available and includes all workspace information.
*
* @internal
*/
function LiveUserApplicationProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(10), { children } = t0, [userApplication, setUserApplication] = (0, import_react.useState)(void 0), [isLoading, setIsLoading] = (0, import_react.useState)(!0), workspaces = useWorkspaces(), userApplicationCache = useUserApplicationCache(), t1, t2;
	$[0] !== userApplicationCache || $[1] !== workspaces ? (t1 = () => {
		let hasSubscriber = !0;
		return setIsLoading(!0), findUserApplication(userApplicationCache, workspaces).then((found) => {
			hasSubscriber && (setUserApplication(found), setIsLoading(!1));
		}).catch((error) => {
			debug("Error when determining live user application id:", error), hasSubscriber && (setUserApplication(void 0), setIsLoading(!1));
		}), () => {
			hasSubscriber = !1;
		};
	}, t2 = [userApplicationCache, workspaces], $[0] = userApplicationCache, $[1] = workspaces, $[2] = t1, $[3] = t2) : (t1 = $[2], t2 = $[3]), (0, import_react.useEffect)(t1, t2);
	let t3;
	$[4] !== isLoading || $[5] !== userApplication ? (t3 = {
		userApplication,
		isLoading
	}, $[4] = isLoading, $[5] = userApplication, $[6] = t3) : t3 = $[6];
	let contextValue = t3, t4;
	return $[7] !== children || $[8] !== contextValue ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LiveUserApplicationContext.Provider, {
		value: contextValue,
		children
	}), $[7] = children, $[8] = contextValue, $[9] = t4) : t4 = $[9], t4;
}
var MODULES_URL = `${isStaging ? "https://sanity-cdn.work" : "https://sanity-cdn.com"}/v1/modules`;
function currentUnixTimestamp() {
	return Math.floor(Date.now() / 1e3);
}
function getAppIdModuleUrl({ packageName, minVersion, appId }) {
	return `${MODULES_URL}/by-app/${appId}/t${currentUnixTimestamp()}/^${minVersion.version}/${packageName}`;
}
function getModuleUrl({ packageName, minVersion, tag = "latest" }) {
	let timestamp = currentUnixTimestamp();
	return `${MODULES_URL}/${packageName}/${tag}/^${minVersion.version}/t${timestamp}`;
}
/**
* Resolves the version the module CDN will serve for the studio's import map URL on reload
* (the `packageVersion` of the app's module metadata)
*/
var fetchLatestAutoUpdatingVersion = async (options) => {
	let { packageName, minVersion, appId } = options;
	try {
		let res = await fetch(getAppIdModuleUrl({
			appId,
			packageName,
			minVersion
		}), { headers: { accept: "application/json" } });
		if (!res.ok) throw Error(`HTTP ${res.status} ${res.statusText}`);
		return (await res.json()).packageVersion;
	} catch (err) {
		console.error(Error(`Failed to fetch version for package "${packageName}" (using appId=${appId})`, { cause: err }));
		return;
	}
};
var fetchLatestAvailableVersionForPackage = async (options) => {
	let { packageName, minVersion, tag = "latest" } = options;
	try {
		let res = await fetch(getModuleUrl({
			packageName,
			minVersion,
			tag
		}), { headers: { accept: "application/json" } });
		if (!res.ok) throw Error(`HTTP ${res.status} ${res.statusText}`);
		let data = await res.json();
		return {
			latest: data.latest,
			packageVersion: data.packageVersion
		};
	} catch (err) {
		console.error(`Failed to fetch version for package (using tag=${tag})`, packageName, "Error:", err);
		return;
	}
};
var MODULE_PATH_REGEX = /^\/v1\/modules\/sanity\/[^/]+\/[^/]+\/[^/]+\/?$/;
var MODULE_PATH_REGEX_BY_APP = /^\/v1\/modules\/by-app\/[^/]+\/[^/]+\/[^/]+\/[^/]+\/?$/;
/**
* Takes a module CDN URL (eg. read from an importmap and returns the min version range part of it)
* @param moduleCdnUrl - a valid URL on the format https://example.com/v1/modules/sanity/default/%5E4.1.1/t1754072932
*/
function parseImportMapModuleCdnUrl(moduleCdnUrl) {
	let rawParseResult = rawParseModuleCDNUrl(new URL(moduleCdnUrl, "https://example.com"));
	if (!rawParseResult.valid) return rawParseResult;
	let { appId, version: rawVersion } = rawParseResult, minVersion = decodeURIComponent(rawVersion);
	return import_semver.default.validRange(minVersion) ? {
		valid: !0,
		appId,
		minVersion
	} : {
		valid: !1,
		error: /* @__PURE__ */ Error(`Invalid minVersion "${minVersion}" in module cdn url: ${moduleCdnUrl}`)
	};
}
function rawParseModuleCDNUrl(url) {
	if (MODULE_PATH_REGEX_BY_APP.test(url.pathname)) {
		let [, , , , appId, , encodedVersion] = url.pathname.split("/");
		return {
			valid: !0,
			appId,
			version: encodedVersion
		};
	}
	if (MODULE_PATH_REGEX.test(url.pathname)) {
		let [, , , , , encodedVersion] = url.pathname.split("/");
		return {
			valid: !0,
			appId: void 0,
			version: encodedVersion
		};
	}
	return {
		valid: !1,
		error: /* @__PURE__ */ Error(`Unable to parse module CDN URL: ${url.pathname}`)
	};
}
var noop$1 = () => {};
`${Math.floor(Date.now() / 1e3)}`;
var getCurrentVersion = memoize(() => import_semver.default.parse(SANITY_VERSION));
var getSanityImportMapEntryValue = memoize(() => getSanityImportMapUrl());
var REFERENCE_PACKAGE = "sanity";
function PackageVersionStatusProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(17), { children } = t0, t1;
	if ($[0] === Symbol.for("react.memo_cache_sentinel")) {
		bb0: {
			let importMapUrl = getSanityImportMapEntryValue();
			if (!importMapUrl) {
				t1 = void 0;
				break bb0;
			}
			let result = parseImportMapModuleCdnUrl(importMapUrl);
			result.valid || console.warn(Error("Unable to extract version from import map, auto updates may not work as expected", { cause: result.error })), t1 = result.valid ? {
				...result,
				minVersion: import_semver.default.coerce(result.minVersion, { includePrerelease: !0 })
			} : result;
		}
		$[0] = t1;
	} else t1 = $[0];
	let importMapInfo = t1, t2;
	$[1] === Symbol.for("react.memo_cache_sentinel") ? (t2 = getCurrentVersion(), $[1] = t2) : t2 = $[1];
	let currentVersion = t2, isAutoUpdating = !!importMapInfo, lastCheckRef = (0, import_react.useRef)(void 0), [autoUpdatingVersionRaw, setAutoUpdatingVersionRaw] = (0, import_react.useState)(), [latestTaggedVersionRaw, setLatestTaggedVersionRaw] = (0, import_react.useState)(), t3;
	$[2] === autoUpdatingVersionRaw ? t3 = $[3] : (t3 = autoUpdatingVersionRaw ? import_semver.default.parse(autoUpdatingVersionRaw) ?? void 0 : void 0, $[2] = autoUpdatingVersionRaw, $[3] = t3);
	let autoUpdatingVersion = t3, t4;
	$[4] === latestTaggedVersionRaw ? t4 = $[5] : (t4 = latestTaggedVersionRaw ? import_semver.default.parse(latestTaggedVersionRaw) ?? void 0 : void 0, $[4] = latestTaggedVersionRaw, $[5] = t4);
	let latestTaggedVersion = t4, t5;
	$[6] === Symbol.for("react.memo_cache_sentinel") ? (t5 = {
		lastCheckedAt: null,
		checking: !1
	}, $[6] = t5) : t5 = $[6];
	let [versionCheckStatus, setVersionCheckStatus] = (0, import_react.useState)(t5), t6;
	$[7] === Symbol.for("react.memo_cache_sentinel") ? (t6 = async () => {
		if (lastCheckRef.current && lastCheckRef.current + 1e4 > (/* @__PURE__ */ new Date()).getTime()) return;
		lastCheckRef.current = (/* @__PURE__ */ new Date()).getTime(), setVersionCheckStatus(_temp$9);
		let resolveLatestTaggedVersion = fetchLatestAvailableVersionForPackage({
			packageName: REFERENCE_PACKAGE,
			minVersion: importMapInfo?.valid ? importMapInfo.minVersion : currentVersion,
			tag: "latest"
		}), resolveAutoUpdatingVersion = importMapInfo?.valid ? importMapInfo.appId ? fetchLatestAutoUpdatingVersion({
			packageName: REFERENCE_PACKAGE,
			minVersion: importMapInfo?.valid ? importMapInfo.minVersion : currentVersion,
			appId: importMapInfo.appId
		}) : resolveLatestTaggedVersion.then(_temp2$5) : void 0;
		Promise.all([resolveLatestTaggedVersion, resolveAutoUpdatingVersion]).then((t7) => {
			let [nextLatestVersion, nextAutoUpdatingVersion] = t7;
			nextAutoUpdatingVersion !== void 0 && setAutoUpdatingVersionRaw(nextAutoUpdatingVersion), nextLatestVersion?.latest && setLatestTaggedVersionRaw(nextLatestVersion.latest);
		}).catch(_temp3$3).finally(() => setVersionCheckStatus({
			lastCheckedAt: /* @__PURE__ */ new Date(),
			checking: !1
		}));
	}, $[7] = t6) : t6 = $[7];
	let fetchNewVersions = t6, t7, t8;
	$[8] === Symbol.for("react.memo_cache_sentinel") ? (t7 = () => {
		let poll = async function poll() {
			await fetchNewVersions();
		};
		poll();
		let intervalId = setInterval(poll, 9e5);
		return () => clearInterval(intervalId);
	}, t8 = [fetchNewVersions], $[8] = t7, $[9] = t8) : (t7 = $[8], t8 = $[9]), (0, import_react.useEffect)(t7, t8);
	let t9;
	$[10] !== autoUpdatingVersion || $[11] !== latestTaggedVersion || $[12] !== versionCheckStatus ? (t9 = {
		isAutoUpdating,
		autoUpdatingVersion,
		importMapInfo,
		latestTaggedVersion,
		currentVersion,
		checkForUpdates: isAutoUpdating ? fetchNewVersions : noop$1,
		versionCheckStatus
	}, $[10] = autoUpdatingVersion, $[11] = latestTaggedVersion, $[12] = versionCheckStatus, $[13] = t9) : t9 = $[13];
	let contextValue = t9, t10;
	return $[14] !== children || $[15] !== contextValue ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PackageVersionInfoContext.Provider, {
		value: contextValue,
		children
	}), $[14] = children, $[15] = contextValue, $[16] = t10) : t10 = $[16], t10;
}
function _temp3$3(err) {
	console.warn("[sanity] Failed to check for new studio versions:", err);
}
function _temp2$5(result_0) {
	return result_0?.packageVersion;
}
function _temp$9(current) {
	return {
		...current,
		checking: !0
	};
}
function ConfigErrorsScreen() {
	let $ = (0, import_compiler_runtime.c)(1), t0;
	return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: "TODO: implement config errors screen" }), $[0] = t0) : t0 = $[0], t0;
}
function NotFoundScreen(props) {
	let $ = (0, import_compiler_runtime.c)(3), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Heading, {
		as: "h1",
		children: "Workspace not found"
	}), $[0] = t0) : t0 = $[0];
	let t1;
	return $[1] === props.onNavigateToDefaultWorkspace ? t1 = $[2] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		height: "fill",
		sizing: "border",
		tone: "caution",
		display: "flex",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
			direction: "row",
			justify: "center",
			flex: 1,
			align: "center",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
				gap: 4,
				children: [t0, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Inline, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
					text: "Go to default workspace",
					onClick: props.onNavigateToDefaultWorkspace,
					mode: "ghost"
				}) })]
			})
		})
	}), $[1] = props.onNavigateToDefaultWorkspace, $[2] = t1), t1;
}
var keyframe = Et`0%{background-position:100%;}100%{background-position:-100%;}`;
var Root$1 = /* @__PURE__ */ gt.div.withConfig({
	displayName: "Root",
	componentId: "sc-0vcjz1-0"
})((props) => {
	let theme = getTheme_v2(props.theme);
	return ft`position:relative;cursor:pointer;#close-floating-button{opacity:0;transition:opacity 0.2s;}&:hover{> [data-ui='whats-new-card']{--card-bg-color:${theme.color.selectable.default.hovered.bg};box-shadow:inset 0 0 2px 1px var(--card-skeleton-color-to);background-image:linear-gradient( to right,var(--card-bg-color),var(--card-bg-color),${theme.color.selectable.default.enabled.bg},var(--card-bg-color),var(--card-bg-color),var(--card-bg-color) );background-position:100%;background-size:200% 100%;background-attachment:fixed;animation-name:${keyframe};animation-timing-function:ease-in;animation-iteration-count:infinite;animation-duration:2000ms;}#close-floating-button{opacity:1;background:transparent;&:hover{transition:all 0.2s;box-shadow:0 0 0 1px ${theme.color.selectable.default.hovered.border};}}}`;
});
var ButtonRoot = gt.div.withConfig({
	displayName: "ButtonRoot",
	componentId: "sc-0vcjz1-1"
})`z-index:1;position:absolute;top:2px;right:6px;`;
/**
* @internal
* @hidden
*/
function StudioAnnouncementsCard(t0) {
	let $ = (0, import_compiler_runtime.c)(37), { title, id, isOpen, name, preHeader, onCardClick, onCardDismiss } = t0, { t } = useTranslation$1(), telemetry = useTelemetry(), t1, t2;
	$[0] !== id || $[1] !== isOpen || $[2] !== name || $[3] !== telemetry || $[4] !== title ? (t1 = () => {
		isOpen && telemetry.log(ProductAnnouncementCardSeen, {
			announcement_id: id,
			announcement_title: title,
			announcement_internal_name: name,
			source: "studio",
			studio_version: SANITY_VERSION
		});
	}, t2 = [
		telemetry,
		id,
		title,
		isOpen,
		name
	], $[0] = id, $[1] = isOpen, $[2] = name, $[3] = telemetry, $[4] = title, $[5] = t1, $[6] = t2) : (t1 = $[5], t2 = $[6]), (0, import_react.useEffect)(t1, t2);
	let t3;
	$[7] === Symbol.for("react.memo_cache_sentinel") ? (t3 = {
		bottom: 12,
		left: 12,
		top: "none"
	}, $[7] = t3) : t3 = $[7];
	let t4;
	$[8] === t ? t4 = $[9] : (t4 = t("announcement.floating-button.open-label"), $[8] = t, $[9] = t4);
	let t5;
	$[10] === preHeader ? t5 = $[11] : (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		marginRight: 6,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			as: "h3",
			size: 1,
			muted: !0,
			children: preHeader
		})
	}), $[10] = preHeader, $[11] = t5);
	let t6;
	$[12] === title ? t6 = $[13] : (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		weight: "medium",
		children: title
	}), $[12] = title, $[13] = t6);
	let t7;
	$[14] !== t5 || $[15] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 3,
		children: [t5, t6]
	}), $[14] = t5, $[15] = t6, $[16] = t7) : t7 = $[16];
	let t8;
	$[17] !== onCardClick || $[18] !== t4 || $[19] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		"data-ui": "whats-new-card",
		padding: 3,
		radius: 3,
		onClick: onCardClick,
		role: "button",
		"aria-label": t4,
		children: t7
	}), $[17] = onCardClick, $[18] = t4, $[19] = t7, $[20] = t8) : t8 = $[20];
	let t9;
	$[21] === t ? t9 = $[22] : (t9 = t("announcement.floating-button.dismiss-label"), $[21] = t, $[22] = t9);
	let t10;
	$[23] === t ? t10 = $[24] : (t10 = t("announcement.floating-button.dismiss"), $[23] = t, $[24] = t10);
	let t11;
	$[25] === t10 ? t11 = $[26] : (t11 = { content: t10 }, $[25] = t10, $[26] = t11);
	let t12;
	$[27] !== onCardDismiss || $[28] !== t11 || $[29] !== t9 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ButtonRoot, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		id: "close-floating-button",
		mode: "bleed",
		onClick: onCardDismiss,
		icon: CloseIcon,
		tone: "default",
		"aria-label": t9,
		tooltipProps: t11
	}) }), $[27] = onCardDismiss, $[28] = t11, $[29] = t9, $[30] = t12) : t12 = $[30];
	let t13;
	$[31] !== t12 || $[32] !== t8 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Root$1, {
		"data-ui": "whats-new-root",
		children: [t8, t12]
	}), $[31] = t12, $[32] = t8, $[33] = t13) : t13 = $[33];
	let t14;
	return $[34] !== isOpen || $[35] !== t13 ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Popover$1, {
		open: isOpen,
		shadow: 3,
		portal: !0,
		style: t3,
		width: 0,
		placement: "bottom-start",
		content: t13
	}), $[34] = isOpen, $[35] = t13, $[36] = t14) : t14 = $[36], t14;
}
var Hr = gt.hr.withConfig({
	displayName: "Hr",
	componentId: "sc-283zae-0"
})`height:1px;background:var(--card-border-color);width:100%;opacity:${({ $show }) => +!!$show};transition:opacity 0.3s ease;margin:0;border:none;`;
/**
* A divider that fades when reaching the top of the parent.
*/
function Divider(t0) {
	let $ = (0, import_compiler_runtime.c)(5), { parentRef } = t0, itemRef = (0, import_react.useRef)(null), [show, setShow] = (0, import_react.useState)(!0), t1, t2;
	$[0] === parentRef ? (t1 = $[1], t2 = $[2]) : (t1 = () => {
		let item = itemRef.current, parent = parentRef.current;
		if (!item || !parent) return;
		let observer = new IntersectionObserver((t3) => {
			let [entry] = t3;
			setShow(entry.isIntersecting);
		}, {
			root: parent,
			threshold: 0,
			rootMargin: "-60px 0px 0px 0px"
		});
		return observer.observe(item), () => {
			observer.disconnect();
		};
	}, t2 = [parentRef], $[0] = parentRef, $[1] = t1, $[2] = t2), (0, import_react.useEffect)(t1, t2);
	let t3;
	return $[3] === show ? t3 = $[4] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingBottom: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			paddingY: 3,
			paddingX: 3,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Hr, {
				ref: itemRef,
				$show: show
			})
		})
	}), $[3] = show, $[4] = t3), t3;
}
var DATE_FORMAT_OPTIONS = {
	month: "short",
	day: "2-digit"
};
var Root = gt(Box).withConfig({
	displayName: "Root",
	componentId: "sc-27bxsu-0"
})`overflow:auto;max-height:75vh;`;
var DialogHeader = gt(Grid).withConfig({
	displayName: "DialogHeader",
	componentId: "sc-27bxsu-1"
})`position:sticky;display:grid;grid-template-columns:64px 1fr 64px;top:0;z-index:1;background:var(--card-bg-color);`;
var FloatingButtonBox = gt(Box).withConfig({
	displayName: "FloatingButtonBox",
	componentId: "sc-27bxsu-2"
})`position:absolute;top:12px;right:24px;z-index:2;`;
var FloatingButton = gt(Button$1$1).withConfig({
	displayName: "FloatingButton",
	componentId: "sc-27bxsu-3"
})``;
/**
* Renders the unseen document in the dialog.
* Has a sticky header with the date and title, and a body with the content.
*/
function Announcement(t0) {
	let $ = (0, import_compiler_runtime.c)(35), { announcement, mode, isFirst, parentRef } = t0, telemetry = useTelemetry(), dateFormatter = useDateTimeFormat(DATE_FORMAT_OPTIONS), logViewedItemRef = (0, import_react.useRef)(null), t1;
	bb0: {
		if (!announcement.publishedDate) {
			t1 = "";
			break bb0;
		}
		let t2;
		$[0] !== announcement.publishedDate || $[1] !== dateFormatter ? (t2 = dateFormatter.format(new Date(announcement.publishedDate)), $[0] = announcement.publishedDate, $[1] = dateFormatter, $[2] = t2) : t2 = $[2], t1 = t2;
	}
	let formattedDate = t1, t2;
	$[3] !== announcement._id || $[4] !== announcement.name || $[5] !== announcement.title || $[6] !== mode || $[7] !== telemetry ? (t2 = (t3) => {
		let { url, linkTitle } = t3;
		telemetry.log(ProductAnnouncementLinkClicked, {
			announcement_id: announcement._id,
			announcement_title: announcement.title,
			announcement_internal_name: announcement.name,
			source: "studio",
			studio_version: SANITY_VERSION,
			origin: mode,
			link_url: url,
			link_title: linkTitle
		});
	}, $[3] = announcement._id, $[4] = announcement.name, $[5] = announcement.title, $[6] = mode, $[7] = telemetry, $[8] = t2) : t2 = $[8];
	let handleLinkClick = t2, t3;
	$[9] !== announcement._id || $[10] !== announcement.name || $[11] !== announcement.title || $[12] !== mode || $[13] !== telemetry ? (t3 = (scrolledIntoView) => {
		telemetry.log(ProductAnnouncementViewed, {
			announcement_id: announcement._id,
			announcement_title: announcement.title,
			announcement_internal_name: announcement.name,
			source: "studio",
			studio_version: SANITY_VERSION,
			scrolled_into_view: scrolledIntoView,
			origin: mode
		});
	}, $[9] = announcement._id, $[10] = announcement.name, $[11] = announcement.title, $[12] = mode, $[13] = telemetry, $[14] = t3) : t3 = $[14];
	let logViewed = t3, t4, t5;
	$[15] !== isFirst || $[16] !== logViewed || $[17] !== parentRef ? (t4 = () => {
		if (isFirst) {
			logViewed(!1);
			return;
		}
		let item = logViewedItemRef.current, parent = parentRef.current;
		if (!item || !parent) return;
		let observer = new IntersectionObserver((t6) => {
			let [entry] = t6;
			entry.isIntersecting && (logViewed(!0), observer.disconnect());
		}, {
			root: parent,
			threshold: 1,
			rootMargin: "0px 0px -100px 0px"
		});
		return observer.observe(item), () => {
			observer.disconnect();
		};
	}, t5 = [
		logViewed,
		parentRef,
		isFirst
	], $[15] = isFirst, $[16] = logViewed, $[17] = parentRef, $[18] = t4, $[19] = t5) : (t4 = $[18], t5 = $[19]), (0, import_react.useEffect)(t4, t5);
	let t6;
	$[20] === formattedDate ? t6 = $[21] : (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		flex: "none",
		padding: 2,
		paddingRight: 0,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			paddingLeft: 2,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				muted: !0,
				children: formattedDate
			})
		})
	}), $[20] = formattedDate, $[21] = t6);
	let t7;
	$[22] === announcement.title ? t7 = $[23] : (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		flex: 1,
		padding: 2,
		justify: "center",
		ref: logViewedItemRef,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			as: "h2",
			size: 1,
			weight: "semibold",
			children: announcement.title
		})
	}), $[22] = announcement.title, $[23] = t7);
	let t8;
	$[24] !== t6 || $[25] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DialogHeader, {
		padding: 3,
		children: [t6, t7]
	}), $[24] = t6, $[25] = t7, $[26] = t8) : t8 = $[26];
	let t9;
	$[27] === announcement.body ? t9 = $[28] : (t9 = announcement.body || [], $[27] = announcement.body, $[28] = t9);
	let t10;
	$[29] !== handleLinkClick || $[30] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		padding: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UpsellDescriptionSerializer, {
			blocks: t9,
			onLinkClick: handleLinkClick
		})
	}), $[29] = handleLinkClick, $[30] = t9, $[31] = t10) : t10 = $[31];
	let t11;
	return $[32] !== t10 || $[33] !== t8 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Box, { children: [t8, t10] }), $[32] = t10, $[33] = t8, $[34] = t11) : t11 = $[34], t11;
}
/**
* Renders the studio announcement dialog displaying unseen announcements.
* @internal
* @hidden
*/
function StudioAnnouncementsDialog(t0) {
	let $ = (0, import_compiler_runtime.c)(18), { announcements: t1, onClose, mode } = t0, announcements = t1 === void 0 ? [] : t1, dialogRef = (0, import_react.useRef)(null), { t } = useTranslation$1(), T0 = Dialog$1, T1 = Root, t9 = announcements.map((announcement, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Announcement, {
		announcement,
		mode,
		isFirst: index === 0,
		parentRef: dialogRef
	}), index < announcements.length - 1 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Divider, { parentRef: dialogRef })] }, announcement._id)), t10;
	$[0] === t ? t10 = $[1] : (t10 = t("announcement.dialog.close-label"), $[0] = t, $[1] = t10);
	let t11;
	$[2] === t ? t11 = $[3] : (t11 = t("announcement.dialog.close"), $[2] = t, $[3] = t11);
	let t12;
	$[4] === t11 ? t12 = $[5] : (t12 = { content: t11 }, $[4] = t11, $[5] = t12);
	let t13;
	$[6] !== onClose || $[7] !== t10 || $[8] !== t12 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingButtonBox, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingButton, {
		"aria-label": t10,
		icon: CloseIcon,
		mode: "bleed",
		onClick: onClose,
		tooltipProps: t12
	}) }), $[6] = onClose, $[7] = t10, $[8] = t12, $[9] = t13) : t13 = $[9];
	let t14;
	$[10] !== T1 || $[11] !== t13 || $[12] !== t9 ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(T1, {
		ref: dialogRef,
		height: "fill",
		children: [t9, t13]
	}), $[10] = T1, $[11] = t13, $[12] = t9, $[13] = t14) : t14 = $[13];
	let t15;
	return $[14] !== T0 || $[15] !== onClose || $[16] !== t14 ? (t15 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(T0, {
		id: "in-app-communication-dialog",
		onClose,
		onClickOutside: onClose,
		width: 1,
		bodyHeight: "fill",
		padding: !1,
		__unstable_hideCloseButton: !0,
		__unstable_autoFocus: !1,
		children: t14
	}), $[14] = T0, $[15] = onClose, $[16] = t14, $[17] = t15) : t15 = $[17], t15;
}
var KEY = "studio.announcement.seen";
var INITIAL_STATE = {
	value: null,
	error: null,
	loading: !0
};
function useSeenAnnouncements() {
	let $ = (0, import_compiler_runtime.c)(13), router = useRouter(), keyValueStore = useKeyValueStore(), t0;
	$[0] === keyValueStore ? t0 = $[1] : (t0 = keyValueStore.getKey(KEY).pipe(map(_temp$8), startWith(INITIAL_STATE), catchError(_temp2$4)), $[0] = keyValueStore, $[1] = t0);
	let seenAnnouncements$ = t0, t1;
	$[2] === keyValueStore ? t1 = $[3] : (t1 = async (seen) => {
		await keyValueStore.setKey(KEY, seen);
	}, $[2] = keyValueStore, $[3] = t1);
	let setSeenAnnouncements = t1, t2;
	$[4] === router.state._searchParams ? t2 = $[5] : (t2 = new URLSearchParams(router.state._searchParams)?.get("reset-announcements"), $[4] = router.state._searchParams, $[5] = t2);
	let resetAnnouncementsParams = t2, t3, t4;
	$[6] !== resetAnnouncementsParams || $[7] !== setSeenAnnouncements ? (t3 = () => {
		if (resetAnnouncementsParams !== null) {
			let resetValue = resetAnnouncementsParams ? resetAnnouncementsParams.split(",") : [];
			setSeenAnnouncements(resetValue);
		}
	}, t4 = [resetAnnouncementsParams, setSeenAnnouncements], $[6] = resetAnnouncementsParams, $[7] = setSeenAnnouncements, $[8] = t3, $[9] = t4) : (t3 = $[8], t4 = $[9]), (0, import_react.useEffect)(t3, t4);
	let t5;
	return $[10] !== seenAnnouncements$ || $[11] !== setSeenAnnouncements ? (t5 = [seenAnnouncements$, setSeenAnnouncements], $[10] = seenAnnouncements$, $[11] = setSeenAnnouncements, $[12] = t5) : t5 = $[12], t5;
}
function _temp2$4(error) {
	return of({
		value: null,
		error,
		loading: !1
	});
}
function _temp$8(value) {
	return {
		value,
		error: null,
		loading: !1
	};
}
var audienceRoles = [
	"administrator",
	"editor",
	"viewer",
	"contributor",
	"developer",
	"custom"
];
/**
* @internal
* @hidden
*/
function isValidAnnouncementAudience(document, sanityVersion) {
	switch (document.audience) {
		case "everyone": return !0;
		case "specific-version": return (0, import_semver.satisfies)(sanityVersion, `= ${document.studioVersion}`, { includePrerelease: !0 });
		case "greater-than-or-equal-version": return (0, import_semver.satisfies)(sanityVersion, `>= ${document.studioVersion}`, { includePrerelease: !0 });
		case "less-than-or-equal-version": return (0, import_semver.satisfies)(sanityVersion, `<= ${document.studioVersion}`, { includePrerelease: !0 });
		default: return !0;
	}
}
/**
* @internal
* @hidden
*/
function isValidAnnouncementRole(audience, userRoles) {
	return !audience || !audience.length ? !0 : !userRoles || !userRoles.length ? !1 : userRoles.some((role) => audience.includes(role.name)) ? !0 : userRoles.some((role) => !audienceRoles.includes(role.name)) ? audience.includes("custom") : !1;
}
var CLIENT_OPTIONS = { apiVersion: "v2024-09-19" };
function StudioAnnouncementsProviderInner(t0) {
	let $ = (0, import_compiler_runtime.c)(47), { children } = t0, telemetry = useTelemetry(), [dialogMode, setDialogMode] = (0, import_react.useState)(null), [isCardDismissed, setIsCardDismissed] = (0, import_react.useState)(!1), [seenAnnouncements$, setSeenAnnouncements] = useSeenAnnouncements(), { currentUser } = useWorkspace(), client = useClient(CLIENT_OPTIONS);
	currentUser?.roles;
	let t1;
	if ($[0] !== client.observable || $[1] !== currentUser?.roles || $[2] !== seenAnnouncements$) {
		let t2;
		$[4] === currentUser?.roles ? t2 = $[5] : (t2 = (docs) => docs ? docs.filter((doc) => isValidAnnouncementAudience({
			audience: doc.audience,
			studioVersion: doc.studioVersion
		}, SANITY_VERSION) && isValidAnnouncementRole(doc.audienceRole, currentUser?.roles)) : [], $[4] = currentUser?.roles, $[5] = t2), t1 = combineLatest([client.observable.request({ url: "/journey/announcements" }).pipe(map(t2), catchError(_temp$7), startWith([])), seenAnnouncements$]).pipe(map(_temp2$3)), $[0] = client.observable, $[1] = currentUser?.roles, $[2] = seenAnnouncements$, $[3] = t1;
	} else t1 = $[3];
	let getAnnouncements$ = t1, t2;
	$[6] === Symbol.for("react.memo_cache_sentinel") ? (t2 = {
		unseen: [],
		all: []
	}, $[6] = t2) : t2 = $[6];
	let announcements = useObservable(getAnnouncements$, t2), unseenAnnouncements = announcements.unseen, studioAnnouncements = announcements.all, t3;
	$[7] !== setSeenAnnouncements || $[8] !== studioAnnouncements ? (t3 = () => {
		setSeenAnnouncements(studioAnnouncements.map(_temp3$2));
	}, $[7] = setSeenAnnouncements, $[8] = studioAnnouncements, $[9] = t3) : t3 = $[9];
	let saveSeenAnnouncements = t3, t4;
	$[10] === Symbol.for("react.memo_cache_sentinel") ? (t4 = (mode) => {
		setDialogMode(mode), setIsCardDismissed(!0);
	}, $[10] = t4) : t4 = $[10];
	let handleOpenDialog = t4, t5;
	$[11] !== saveSeenAnnouncements || $[12] !== telemetry || $[13] !== unseenAnnouncements[0]?._id || $[14] !== unseenAnnouncements[0]?.name || $[15] !== unseenAnnouncements[0]?.title ? (t5 = () => {
		saveSeenAnnouncements(), setIsCardDismissed(!0), telemetry.log(ProductAnnouncementCardDismissed, {
			announcement_id: unseenAnnouncements[0]?._id,
			announcement_title: unseenAnnouncements[0]?.title,
			announcement_internal_name: unseenAnnouncements[0]?.name,
			source: "studio",
			studio_version: SANITY_VERSION
		});
	}, $[11] = saveSeenAnnouncements, $[12] = telemetry, $[13] = unseenAnnouncements[0]?._id, $[14] = unseenAnnouncements[0]?.name, $[15] = unseenAnnouncements[0]?.title, $[16] = t5) : t5 = $[16];
	let handleCardDismiss = t5, t6;
	$[17] !== telemetry || $[18] !== unseenAnnouncements[0]?._id || $[19] !== unseenAnnouncements[0]?.name || $[20] !== unseenAnnouncements[0]?.title ? (t6 = () => {
		handleOpenDialog("card"), telemetry.log(ProductAnnouncementCardClicked, {
			announcement_id: unseenAnnouncements[0]?._id,
			announcement_title: unseenAnnouncements[0]?.title,
			announcement_internal_name: unseenAnnouncements[0]?.name,
			source: "studio",
			studio_version: SANITY_VERSION
		});
	}, $[17] = telemetry, $[18] = unseenAnnouncements[0]?._id, $[19] = unseenAnnouncements[0]?.name, $[20] = unseenAnnouncements[0]?.title, $[21] = t6) : t6 = $[21];
	let handleCardClick = t6, t7;
	$[22] !== dialogMode || $[23] !== saveSeenAnnouncements || $[24] !== studioAnnouncements[0] || $[25] !== telemetry || $[26] !== unseenAnnouncements[0] ? (t7 = () => {
		let firstAnnouncement = dialogMode === "help_menu" ? studioAnnouncements[0] : unseenAnnouncements[0];
		telemetry.log(ProductAnnouncementModalDismissed, {
			announcement_id: firstAnnouncement?._id,
			announcement_title: firstAnnouncement?.title,
			announcement_internal_name: firstAnnouncement?.name,
			source: "studio",
			studio_version: SANITY_VERSION,
			origin: dialogMode ?? "card"
		}), setDialogMode(null), saveSeenAnnouncements();
	}, $[22] = dialogMode, $[23] = saveSeenAnnouncements, $[24] = studioAnnouncements[0], $[25] = telemetry, $[26] = unseenAnnouncements[0], $[27] = t7) : t7 = $[27];
	let handleDialogClose = t7, t8;
	$[28] !== studioAnnouncements || $[29] !== unseenAnnouncements ? (t8 = {
		studioAnnouncements,
		unseenAnnouncements,
		onDialogOpen: handleOpenDialog
	}, $[28] = studioAnnouncements, $[29] = unseenAnnouncements, $[30] = t8) : t8 = $[30];
	let contextValue = t8, t9;
	$[31] !== handleCardClick || $[32] !== handleCardDismiss || $[33] !== isCardDismissed || $[34] !== unseenAnnouncements[0] || $[35] !== unseenAnnouncements.length ? (t9 = unseenAnnouncements.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioAnnouncementsCard, {
		preHeader: unseenAnnouncements[0].preHeader,
		title: unseenAnnouncements[0].title,
		name: unseenAnnouncements[0].name,
		id: unseenAnnouncements[0]._id,
		onCardClick: handleCardClick,
		isOpen: !isCardDismissed,
		onCardDismiss: handleCardDismiss
	}), $[31] = handleCardClick, $[32] = handleCardDismiss, $[33] = isCardDismissed, $[34] = unseenAnnouncements[0], $[35] = unseenAnnouncements.length, $[36] = t9) : t9 = $[36];
	let t10;
	$[37] !== dialogMode || $[38] !== handleDialogClose || $[39] !== studioAnnouncements || $[40] !== unseenAnnouncements ? (t10 = dialogMode && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioAnnouncementsDialog, {
		mode: dialogMode,
		announcements: dialogMode === "help_menu" ? studioAnnouncements : unseenAnnouncements,
		onClose: handleDialogClose
	}), $[37] = dialogMode, $[38] = handleDialogClose, $[39] = studioAnnouncements, $[40] = unseenAnnouncements, $[41] = t10) : t10 = $[41];
	let t11;
	return $[42] !== children || $[43] !== contextValue || $[44] !== t10 || $[45] !== t9 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(StudioAnnouncementContext.Provider, {
		value: contextValue,
		children: [
			children,
			t9,
			t10
		]
	}), $[42] = children, $[43] = contextValue, $[44] = t10, $[45] = t9, $[46] = t11) : t11 = $[46], t11;
}
/**
* @internal
* @hidden
*/
function _temp3$2(doc_1) {
	return doc_1._id;
}
function _temp2$3(t0) {
	let [all, seen] = t0;
	return seen.loading || seen.error ? {
		unseen: [],
		all
	} : seen.value ? {
		unseen: all.filter((doc_0) => !seen.value?.includes(doc_0._id)),
		all
	} : {
		unseen: all,
		all
	};
}
function _temp$7() {
	return [];
}
function StudioAnnouncementsProvider(props) {
	let $ = (0, import_compiler_runtime.c)(2);
	if (useSource().announcements?.enabled) {
		let t0;
		return $[0] === props ? t0 = $[1] : (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioAnnouncementsProviderInner, { ...props }), $[0] = props, $[1] = t0), t0;
	}
	return props.children;
}
function isKnownError(err) {
	return !!(err instanceof SchemaError || err instanceof ConfigResolutionError || isObject(err) && "ViteDevServerStoppedError" in err && err.ViteDevServerStoppedError);
}
var errorChannel = globalScope.__sanityErrorChannel;
function StudioRootErrorHandler(props) {
	let $ = (0, import_compiler_runtime.c)(12), { children } = props, [errorState, setErrorState] = (0, import_react.useState)(), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = () => setErrorState(void 0), $[0] = t0) : t0 = $[0];
	let handleResetError = t0, toast = useToast();
	useHotModuleReload(handleResetError);
	let t1, t2;
	if ($[1] === toast ? (t1 = $[2], t2 = $[3]) : (t1 = () => {
		if (errorChannel) return errorChannel.subscribe((event) => {
			if (isDocumentLimitError(event.error) || !event.error || !(event.error instanceof Error)) return;
			if (isKnownError(event.error)) {
				setErrorState({ error: event.error });
				return;
			}
			let eventId, run = () => {
				eventId = errorReporter.reportError(event.error)?.eventId;
			};
			try {
				run();
			} catch (t3) {
				let e = t3;
				e.message = `Encountered an additional error when reporting error: ${e.message}`, console.error(e);
			}
			if (isImportError(event.error)) {
				setErrorState({
					error: event.error,
					eventId
				});
				return;
			}
			toast.push({
				id: event.error.message,
				closable: !0,
				description: event.error.message,
				duration: 5e3,
				title: "Uncaught error",
				status: "error"
			});
		});
	}, t2 = [toast], $[1] = toast, $[2] = t1, $[3] = t2), (0, import_react.useEffect)(t1, t2), !errorState?.error) return children;
	if (errorState.error instanceof SchemaError) {
		let t3;
		return $[4] === errorState.error.schema ? t3 = $[5] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SchemaErrorsScreen, { schema: errorState.error.schema }), $[4] = errorState.error.schema, $[5] = t3), t3;
	}
	if (isImportError(errorState.error)) {
		let t3;
		return $[6] !== errorState.error || $[7] !== errorState.eventId ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ImportErrorScreen, {
			error: errorState.error,
			eventId: errorState.eventId
		}), $[6] = errorState.error, $[7] = errorState.eventId, $[8] = t3) : t3 = $[8], t3;
	}
	let t3;
	return $[9] !== errorState.error || $[10] !== errorState.eventId ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FallbackErrorScreen, {
		error: errorState.error,
		eventId: errorState.eventId,
		onReset: handleResetError
	}), $[9] = errorState.error, $[10] = errorState.eventId, $[11] = t3) : t3 = $[11], t3;
}
var _defaultTheme;
/**
* @internal
* @deprecated Will be removed in upcoming major version
* */
function getDefaultTheme() {
	return _defaultTheme ||= buildTheme(), _defaultTheme;
}
/**
* The default theme. This is a proxy to the default theme, in order to lazily initialize it
* on use and not pay the cost of building the theme on module load.
*
* @internal
* @deprecated Will be removed in upcoming major version.
* */
var defaultTheme = /* @__PURE__ */ new Proxy({}, {
	get(_target, prop, receiver) {
		return Reflect.get(getDefaultTheme(), prop, receiver);
	},
	ownKeys() {
		return Reflect.ownKeys(getDefaultTheme());
	},
	getOwnPropertyDescriptor(_target, prop) {
		return Object.getOwnPropertyDescriptor(getDefaultTheme(), prop);
	},
	has(_target, prop) {
		return prop in getDefaultTheme();
	}
});
var isThemerTheme = (theme) => theme.__themer === !0;
function getThemeValues(theme) {
	let defaultTheme = getDefaultTheme();
	return {
		...defaultTheme,
		v2: theme.v2,
		fonts: isThemerTheme(theme) ? defaultTheme.fonts : theme.fonts ?? defaultTheme.fonts,
		color: theme.color ?? defaultTheme.color
	};
}
/** @internal */
function StudioThemeProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(18), { children } = t0, theme = useActiveWorkspace().activeWorkspace.theme;
	if (theme.__legacy) {
		let scheme = theme.__dark ? "dark" : "light", t1;
		$[0] === theme ? t1 = $[1] : (t1 = getThemeValues(theme), $[0] = theme, $[1] = t1);
		let t2;
		$[2] === children ? t2 = $[3] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayerProvider, { children }), $[2] = children, $[3] = t2);
		let t3;
		$[4] !== scheme || $[5] !== t1 || $[6] !== t2 ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ThemeProvider$1, {
			scheme,
			theme: t1,
			children: t2
		}), $[4] = scheme, $[5] = t1, $[6] = t2, $[7] = t3) : t3 = $[7];
		let t4;
		return $[8] !== scheme || $[9] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ColorSchemeSetValueContext.Provider, {
			value: !1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ColorSchemeValueContext.Provider, {
				value: scheme,
				children: t3
			})
		}), $[8] = scheme, $[9] = t3, $[10] = t4) : t4 = $[10], t4;
	}
	let t1;
	$[11] === theme ? t1 = $[12] : (t1 = getThemeValues(theme), $[11] = theme, $[12] = t1);
	let t2;
	$[13] === children ? t2 = $[14] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayerProvider, { children }), $[13] = children, $[14] = t2);
	let t3;
	return $[15] !== t1 || $[16] !== t2 ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ThemeProvider$1, {
		theme: t1,
		children: t2
	}), $[15] = t1, $[16] = t2, $[17] = t3) : t3 = $[17], t3;
}
var WorkspaceFeaturesObserved = defineEvent({
	name: "Workspace Features Observed",
	version: 1,
	description: "Feature flags enabled for the current workspace"
});
/**
* Projects the resolved workspace into the flat, low-cardinality feature
* snapshot emitted by {@link WorkspaceFeaturesObserved}. Values are read
* straight off the resolved workspace, where the config resolver has already
* applied every default and merged plugin contributions, so this never restates
* a default itself: changing a default upstream flows through with no change
* here. The `| undefined` unions track the input `Workspace` type rather than
* runtime behaviour: the resolver writes concrete defaults for almost all of
* these, so `releasesLimit` (unset when no release limit is configured) is the
* only field that is genuinely `undefined` at runtime.
*
* Every entry must stay a boolean, small enum, or short number; never free
* text, identifiers, or customer values, which keeps the payload safe to send
* and cheap to aggregate.
*/
function collectWorkspaceFeatures(workspace) {
	return {
		advancedVersionControlEnabled: workspace.advancedVersionControl.enabled,
		releasesEnabled: workspace.releases?.enabled,
		releasesLimit: workspace.releases?.limit,
		tasksEnabled: workspace.tasks?.enabled,
		scheduledDraftsEnabled: workspace.scheduledDrafts?.enabled,
		scheduledPublishingEnabled: workspace.scheduledPublishing.enabled,
		scheduledPublishingExplicitlyEnabled: workspace.scheduledPublishing.__internal__workspaceEnabled,
		mediaLibraryEnabled: workspace.mediaLibrary?.enabled,
		canvasEnabled: workspace.apps?.canvas?.enabled,
		variantsEnabled: workspace.beta?.variants?.enabled,
		documentGroupInventoryEnabled: workspace.beta?.documentGroupInventory?.enabled,
		eventsApiDocumentsEnabled: workspace.beta?.eventsAPI?.documents,
		eventsApiReleasesEnabled: workspace.beta?.eventsAPI?.releases,
		announcementsEnabled: workspace.announcements?.enabled,
		draftsEnabled: workspace.document.drafts.enabled,
		partialIndexingEnabled: workspace.search.unstable_partialIndexing?.enabled,
		fileDirectUploadsEnabled: workspace.form.file.directUploads,
		imageDirectUploadsEnabled: workspace.form.image.directUploads,
		searchStrategy: workspace.search.strategy
	};
}
var StudioLoaded = defineEvent({
	name: "Studio Loaded",
	version: 1,
	description: "Fired once per studio mount with studio version and environment metadata"
});
var PerformanceINPMeasured = defineEvent({
	name: "Performance INP Measured",
	maxSampleRate: 6e4,
	version: 1,
	description: "Performance INP (Interaction to Next Paint) measurement happened"
});
var PerformanceLCPMeasured = defineEvent({
	name: "Performance LCP Measured",
	version: 2,
	description: "Largest Contentful Paint measurement with attribution"
});
var PerformanceFCPMeasured = defineEvent({
	name: "Performance FCP Measured",
	version: 2,
	description: "First Contentful Paint measurement with attribution"
});
var PerformanceCLSMeasured = defineEvent({
	name: "Performance CLS Measured",
	version: 2,
	description: "Cumulative Layout Shift measurement with attribution"
});
var PerformanceTTFBMeasured = defineEvent({
	name: "Performance TTFB Measured",
	version: 2,
	description: "Time to First Byte measurement with attribution"
});
var PerformanceINPMeasuredV2 = defineEvent({
	name: "Performance INP Measured",
	version: 2,
	description: "Interaction to Next Paint with attribution (web-vitals)",
	maxSampleRate: 3e4
});
function getInterestingAttrs(node) {
	if (!node || !(node instanceof Element)) return;
	let ui = node.getAttribute("data-ui") || void 0, testId = node.getAttribute("data-testid") || void 0;
	return ui || testId ? {
		ui,
		testId
	} : void 0;
}
function getElementIdentifier(node) {
	if (!node) return null;
	if (!(node instanceof Element)) return node.nodeName;
	let { nodeName, classList, id } = node;
	return nodeName.toLowerCase() + (id ? `#${id}` : "") + (classList ? `.${classList.value.replaceAll(" ", ".")}` : "");
}
function isPerformanceEventTiming(entry) {
	return entry.entryType === "event";
}
/**
* @internal
*/
function useMeasurePerformanceTelemetry() {
	let $ = (0, import_compiler_runtime.c)(5), telemetry = useTelemetry(), t0;
	$[0] === telemetry ? t0 = $[1] : (t0 = (list, observer) => {
		let entries = list.getEntries(), maxEntry;
		for (let entry of entries) isPerformanceEventTiming(entry) && entry.duration > (maxEntry?.duration || 0) && (maxEntry = entry);
		maxEntry && telemetry.log(PerformanceINPMeasured, {
			target: getElementIdentifier(maxEntry.target),
			attrs: getInterestingAttrs(maxEntry.target),
			interaction: maxEntry.name,
			duration: maxEntry.duration
		});
	}, $[0] = telemetry, $[1] = t0);
	let onEvent = t0, t1, t2;
	$[2] === onEvent ? (t1 = $[3], t2 = $[4]) : (t1 = () => {
		if (!("PerformanceObserver" in globalThis)) return;
		let observer_0 = new PerformanceObserver(onEvent);
		return observer_0.observe({
			type: "event",
			buffered: !0
		}), () => {
			observer_0.disconnect();
		};
	}, t2 = [onEvent], $[2] = onEvent, $[3] = t1, $[4] = t2), (0, import_react.useEffect)(t1, t2);
}
/**
* Tracks Core Web Vitals using Google's web-vitals library.
*
* This hook should be called once at the root of the application.
* It sets up observers for all Core Web Vitals and logs them to telemetry.
*
* Metrics tracked:
* - **LCP** (Largest Contentful Paint) - loading performance
* - **FCP** (First Contentful Paint) - initial render
* - **CLS** (Cumulative Layout Shift) - visual stability
* - **TTFB** (Time to First Byte) - server response time
* - **INP** (Interaction to Next Paint) - interactivity
*
* Uses the attribution build for diagnostic data to help identify
* the causes of performance issues. Each metric includes:
* - Base data: name, value, rating, delta, id, entries, navigationType
* - Attribution data: metric-specific diagnostic information
*
* @remarks
* - The web-vitals library manages its own PerformanceObservers
* - Each function should only be called once per page load
* - CLS is only reported on page hide (visibilitychange)
* - INP is the worst interaction latency during the page session
*
* @internal
*/
function useWebVitalsTelemetry() {
	let $ = (0, import_compiler_runtime.c)(3), telemetry = useTelemetry(), hasInitialized = (0, import_react.useRef)(!1), t0, t1;
	$[0] === telemetry ? (t0 = $[1], t1 = $[2]) : (t0 = () => {
		hasInitialized.current || (hasInitialized.current = !0, G((metric) => {
			telemetry.log(PerformanceLCPMeasured, metric);
		}), _((metric_0) => {
			telemetry.log(PerformanceFCPMeasured, metric_0);
		}), F((metric_1) => {
			telemetry.log(PerformanceCLSMeasured, metric_1);
		}), Q((metric_2) => {
			telemetry.log(PerformanceTTFBMeasured, metric_2);
		}), U((metric_3) => {
			telemetry.log(PerformanceINPMeasuredV2, metric_3);
		}, { includeProcessedEventEntries: !0 }));
	}, t1 = [telemetry], $[0] = telemetry, $[1] = t0, $[2] = t1), (0, import_react.useEffect)(t0, t1);
}
/**
* Component that tracks performance telemetry for the Studio.
*
* Metrics tracked:
* - INP v1 (legacy, via useMeasurePerformanceTelemetry)
* - Core Web Vitals (LCP, FCP, CLS, TTFB, INP v2 via useWebVitalsTelemetry)
*
* @internal
*/
function PerformanceTelemetryTracker(props) {
	return useMeasurePerformanceTelemetry(), useWebVitalsTelemetry(), props.children;
}
var TYPE_COLORS = {
	log: "#4CAF50",
	"trace.start": "#2196F3",
	"trace.log": "#03A9F4",
	"trace.complete": "#9C27B0",
	"trace.error": "#F44336",
	userProperties: "#FF9800"
};
function logTelemetryEvent(event) {
	let label = event.name || event.type, color = TYPE_COLORS[event.type] || "#999", payload = event.data ?? event.properties, data = payload && typeof payload == "object" ? payload : null;
	if (console.log(`%c${event.type}%c ${label}`, `color: ${color}; font-weight: bold`, "color: inherit"), data && Object.keys(data).length > 0) for (let [key, value] of Object.entries(data)) {
		let formatted = typeof value == "object" && value ? JSON.stringify(value) : value;
		console.log(`  %c${key}:%c ${formatted}`, "color: #999", "color: inherit");
	}
}
function logTelemetryBatch(batch) {
	if (batch.length === 0) return;
	let counts = /* @__PURE__ */ new Map();
	for (let event of batch) {
		let name = event.name || event.type;
		counts.set(name, (counts.get(name) || 0) + 1);
	}
	let names = [...counts.entries()].map(([name, count]) => count > 1 ? `${name} x${count}` : name).join(", ");
	console.groupCollapsed(`%c[telemetry]%c ${batch.length} event${batch.length > 1 ? "s" : ""} %c${names}`, "color: #999; font-weight: normal", "color: inherit; font-weight: bold", "color: #999; font-weight: normal");
	for (let event of batch) logTelemetryEvent(event);
	console.groupEnd();
}
var debugLoggingStore = {
	flushInterval: 1e3,
	resolveConsent: () => Promise.resolve({ status: "granted" }),
	sendEvents: async (batch) => {
		logTelemetryBatch(batch);
	},
	sendBeacon: (batch) => (logTelemetryBatch(batch), !0)
};
var sessionId = createSessionId();
var isClient = typeof window < "u";
function countPlugins(plugins) {
	return plugins ? plugins.reduce((count, plugin) => count + 1 + countPlugins(plugin.plugins), 0) : 0;
}
function getConnection() {
	if (!isClient) return null;
	let nav = navigator, connection = nav.connection || nav.mozConnection || nav.webkitConnection;
	return connection ? {
		effectiveType: connection.effectiveType ?? null,
		downlink: typeof connection.downlink == "number" ? connection.downlink : null,
		rtt: typeof connection.rtt == "number" ? connection.rtt : null,
		saveData: typeof connection.saveData == "boolean" ? connection.saveData : null
	} : null;
}
function StudioTelemetryProvider(props) {
	let client = useClient({ apiVersion: "v2023-12-18" }), projectId = client.config().projectId, workspace = useWorkspace(), workspaceCount = useWorkspaces().length, workspacePlugins = workspace.__internal.options.plugins, workspaceSchema = workspace.schema, { value: orgId } = useProjectOrganizationId(), activeTool = useRouterState((0, import_react.useCallback)((routerState) => typeof routerState.tool == "string" ? routerState.tool : void 0, [])), contextRef = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (!isClient) return;
		let pluginCount = countPlugins(workspacePlugins), schemaTypeCount = workspaceSchema.getTypeNames().length;
		contextRef.current = {
			userAgent: navigator.userAgent,
			screen: {
				density: window.devicePixelRatio,
				height: window.screen.height,
				width: window.screen.width,
				innerHeight: window.innerHeight,
				innerWidth: window.innerWidth
			},
			studioVersion: SANITY_VERSION,
			reactVersion: import_react.version,
			environment: "development",
			connection: getConnection(),
			orgId: orgId || null,
			activeTool,
			workspaceCount,
			activeWorkspace: workspace.name,
			activeProjectId: workspace.projectId,
			activeDataset: workspace.dataset,
			pluginCount,
			schemaTypeCount
		};
	}, [
		orgId,
		activeTool,
		workspaceCount,
		workspace.name,
		workspace.projectId,
		workspace.dataset,
		workspacePlugins,
		workspaceSchema
	]);
	let storeOptions = (0, import_react.useMemo)(() => import.meta && import.meta.env?.SANITY_STUDIO_DEBUG_TELEMETRY === "true" ? debugLoggingStore : {
		flushInterval: 3e4,
		resolveConsent: () => client.request({
			uri: "/intake/telemetry-status",
			tag: "telemetry-consent.studio"
		}),
		sendEvents: (batch) => {
			if (!isClient || !contextRef.current) return Promise.resolve();
			let context = contextRef.current, enrichedBatch = batch.map((event) => ({
				...event,
				context
			}));
			return client.request({
				uri: "/intake/batch",
				method: "POST",
				json: !0,
				body: {
					projectId,
					batch: enrichedBatch
				}
			});
		},
		sendBeacon: (batch_0) => {
			if (!isClient || !contextRef.current) return !1;
			let context_0 = contextRef.current, enrichedBatch_0 = batch_0.map((event_0) => ({
				...event_0,
				context: context_0
			}));
			return navigator.sendBeacon(client.getUrl("/intake/batch"), JSON.stringify({
				projectId,
				batch: enrichedBatch_0
			}));
		}
	}, [client, projectId]), store = (0, import_react.useMemo)(() => createBatchedStore(sessionId, storeOptions), [storeOptions]), studioLoadedFiredRef = (0, import_react.useRef)(!1);
	(0, import_react.useEffect)(() => {
		if (!isClient || !contextRef.current || studioLoadedFiredRef.current) return;
		studioLoadedFiredRef.current = !0;
		let ctx = contextRef.current;
		store.logger.log(StudioLoaded, {
			studioVersion: SANITY_VERSION,
			reactVersion: import_react.version,
			environment: ctx.environment,
			userAgent: ctx.userAgent,
			screenDensity: ctx.screen.density,
			screenHeight: ctx.screen.height,
			screenWidth: ctx.screen.width,
			screenInnerHeight: ctx.screen.innerHeight,
			screenInnerWidth: ctx.screen.innerWidth
		});
	}, [store.logger]);
	let workspaceFeatures = (0, import_react.useMemo)(() => collectWorkspaceFeatures(workspace), [workspace]), observedFeaturesKeyRef = (0, import_react.useRef)(null);
	return (0, import_react.useEffect)(() => {
		if (!isClient) return;
		let workspaceKey = `${workspace.projectId}:${workspace.name}`;
		observedFeaturesKeyRef.current !== workspaceKey && (observedFeaturesKeyRef.current = workspaceKey, store.logger.log(WorkspaceFeaturesObserved, workspaceFeatures));
	}, [
		store.logger,
		workspace.name,
		workspace.projectId,
		workspaceFeatures
	]), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TelemetryProvider, {
		store,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PerformanceTelemetryTracker, { children: props.children })
	});
}
var CenteredContainer$1 = gt(Flex).withConfig({
	displayName: "CenteredContainer",
	componentId: "sc-wfm1ke-0"
})`min-height:100vh;box-sizing:border-box;`;
var ContentWrapper$1 = gt(Box).withConfig({
	displayName: "ContentWrapper",
	componentId: "sc-wfm1ke-1"
})`width:100%;max-width:520px;`;
var HelpLink$1 = gt.a.withConfig({
	displayName: "HelpLink",
	componentId: "sc-wfm1ke-2"
})`color:var(--card-link-fg-color);text-decoration:none;display:inline-flex;align-items:center;gap:0.25em;&:hover{text-decoration:underline;}`;
var InlineCode = gt.code.withConfig({
	displayName: "InlineCode",
	componentId: "sc-wfm1ke-3"
})`font-family:var(--card-code-family,monospace);font-size:0.9em;background:var(--card-code-bg-color);color:var(--card-code-fg-color);padding:0.1em 0.35em;border-radius:3px;`;
/**
* Full-screen takeover for workspace configuration errors — the project
* or dataset the studio points at doesn't exist. Unlike the request-error
* dialog, there's no "Try again": the only path forward is fixing the
* config (or creating the missing resource in Manage), so the screen links
* straight to the relevant Manage page.
*
* @internal
*/
function ConfigErrorScreen(props) {
	let $ = (0, import_compiler_runtime.c)(16), { error, projectId, dataset, isStaging } = props, manageBaseUrl = isStaging ? "https://sanity.work" : "https://sanity.io", { visibleWorkspaces } = useVisibleWorkspaces(), { activeWorkspace, setActiveWorkspace } = useActiveWorkspace(), canChooseAnotherWorkspace = visibleWorkspaces.length > 1, [showChooser, setShowChooser] = (0, import_react.useState)(!1), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = () => setShowChooser(!0), $[0] = t0) : t0 = $[0];
	let handleChoose = t0, t1;
	$[1] !== activeWorkspace.name || $[2] !== setActiveWorkspace ? (t1 = (workspaceName) => {
		workspaceName !== activeWorkspace.name && setActiveWorkspace(workspaceName), setShowChooser(!1);
	}, $[1] = activeWorkspace.name, $[2] = setActiveWorkspace, $[3] = t1) : t1 = $[3];
	let handleSelectWorkspace = t1;
	if (showChooser) {
		let t2;
		$[4] === Symbol.for("react.memo_cache_sentinel") ? (t2 = () => setShowChooser(!1), $[4] = t2) : t2 = $[4];
		let t3;
		return $[5] === handleSelectWorkspace ? t3 = $[6] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspaceChooserScreen, {
			onBack: t2,
			onSelect: handleSelectWorkspace
		}), $[5] = handleSelectWorkspace, $[6] = t3), t3;
	}
	let onChooseAnotherWorkspace = canChooseAnotherWorkspace ? handleChoose : void 0;
	if (error.type === "projectNotFound") {
		let t2;
		return $[7] !== manageBaseUrl || $[8] !== onChooseAnotherWorkspace || $[9] !== projectId ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProjectNotFoundScreen, {
			projectId,
			manageBaseUrl,
			onChooseAnotherWorkspace
		}), $[7] = manageBaseUrl, $[8] = onChooseAnotherWorkspace, $[9] = projectId, $[10] = t2) : t2 = $[10], t2;
	}
	let t2;
	return $[11] !== dataset || $[12] !== manageBaseUrl || $[13] !== onChooseAnotherWorkspace || $[14] !== projectId ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DatasetNotFoundScreen, {
		projectId,
		dataset,
		manageBaseUrl,
		onChooseAnotherWorkspace
	}), $[11] = dataset, $[12] = manageBaseUrl, $[13] = onChooseAnotherWorkspace, $[14] = projectId, $[15] = t2) : t2 = $[15], t2;
}
/**
* Workspace picker, reusing the same card list the auth screen
* (`WorkspaceAuth`) shows: each card probes its own auth state. Selecting
* one switches the active workspace; "Back" returns to the error.
*/
function WorkspaceChooserScreen(props) {
	let $ = (0, import_compiler_runtime.c)(17), { onBack, onSelect } = props, { visibleWorkspaces } = useVisibleWorkspaces(), { t } = useTranslation$1(), t0;
	$[0] === onBack ? t0 = $[1] : (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		icon: ArrowLeftIcon,
		mode: "bleed",
		onClick: onBack,
		text: "Back"
	}) }), $[0] = onBack, $[1] = t0);
	let t1;
	$[2] === t ? t1 = $[3] : (t1 = t("workspaces.choose-your-workspace-label"), $[2] = t, $[3] = t1);
	let t2;
	if ($[4] !== onSelect || $[5] !== visibleWorkspaces) {
		let t3;
		$[7] === onSelect ? t3 = $[8] : (t3 = (workspace) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspaceAuthCard, {
			workspace,
			onSelect: () => onSelect(workspace.name)
		}, workspace.name), $[7] = onSelect, $[8] = t3), t2 = visibleWorkspaces.map(t3), $[4] = onSelect, $[5] = visibleWorkspaces, $[6] = t2;
	} else t2 = $[6];
	let t3;
	$[9] === t2 ? t3 = $[10] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		gap: 1,
		paddingX: 1,
		paddingY: 2,
		children: t2
	}), $[9] = t2, $[10] = t3);
	let t4;
	$[11] !== t1 || $[12] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Layout, {
		header: t1,
		children: t3
	}), $[11] = t1, $[12] = t3, $[13] = t4) : t4 = $[13];
	let t5;
	return $[14] !== t0 || $[15] !== t4 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		"data-testid": "studio-error-screen",
		"data-error": "Config error workspace chooser",
		height: "fill",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CenteredContainer$1, {
			align: "center",
			justify: "center",
			padding: 4,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Container, {
				width: 1,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
					gap: 2,
					children: [t0, t4]
				})
			})
		})
	}), $[14] = t0, $[15] = t4, $[16] = t5) : t5 = $[16], t5;
}
/** "Choose another workspace" button shown above the error heading. */
function ChooseAnotherWorkspaceButton(props) {
	let $ = (0, import_compiler_runtime.c)(5), { t } = useTranslation$1();
	if (!props.onChooseAnotherWorkspace) return null;
	let t0 = props.onChooseAnotherWorkspace, t1;
	$[0] === t ? t1 = $[1] : (t1 = t("workspaces.action.choose-another-workspace"), $[0] = t, $[1] = t1);
	let t2;
	return $[2] !== props.onChooseAnotherWorkspace || $[3] !== t1 ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		icon: ArrowLeftIcon,
		mode: "bleed",
		onClick: t0,
		text: t1
	}) }), $[2] = props.onChooseAnotherWorkspace, $[3] = t1, $[4] = t2) : t2 = $[4], t2;
}
function ConfigErrorLayout(props) {
	let $ = (0, import_compiler_runtime.c)(21), t0;
	$[0] === props.onChooseAnotherWorkspace ? t0 = $[1] : (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChooseAnotherWorkspaceButton, { onChooseAnotherWorkspace: props.onChooseAnotherWorkspace }), $[0] = props.onChooseAnotherWorkspace, $[1] = t0);
	let t1;
	$[2] === props.heading ? t1 = $[3] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Heading, {
		as: "h1",
		size: 2,
		children: props.heading
	}), $[2] = props.heading, $[3] = t1);
	let t2;
	$[4] === props.message ? t2 = $[5] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 2,
		muted: !0,
		children: props.message
	}), $[4] = props.message, $[5] = t2);
	let t3;
	$[6] === props.hint ? t3 = $[7] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		muted: !0,
		children: props.hint
	}), $[6] = props.hint, $[7] = t3);
	let t4;
	$[8] === props.action ? t4 = $[9] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		paddingTop: 2,
		children: props.action
	}), $[8] = props.action, $[9] = t4);
	let t5;
	$[10] === Symbol.for("react.memo_cache_sentinel") ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DocsLink, {}), $[10] = t5) : t5 = $[10];
	let t6;
	$[11] !== props.details || $[12] !== t0 || $[13] !== t1 || $[14] !== t2 || $[15] !== t3 || $[16] !== t4 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CenteredContainer$1, {
		align: "center",
		justify: "center",
		padding: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ContentWrapper$1, {
			paddingBottom: 5,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
				gap: 4,
				children: [
					t0,
					t1,
					t2,
					props.details,
					t3,
					t4,
					t5
				]
			})
		})
	}), $[11] = props.details, $[12] = t0, $[13] = t1, $[14] = t2, $[15] = t3, $[16] = t4, $[17] = t6) : t6 = $[17];
	let t7;
	return $[18] !== props.errorLabel || $[19] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		"data-testid": "studio-error-screen",
		"data-error": props.errorLabel,
		height: "fill",
		children: t6
	}), $[18] = props.errorLabel, $[19] = t6, $[20] = t7) : t7 = $[20], t7;
}
/** Labeled key/value rows for the configured project/dataset values. */
function ConfigDetails(props) {
	let $ = (0, import_compiler_runtime.c)(21), t0 = props.rows, T0, T1, t1, t2, t3, t4, t5, t6, t7;
	if ($[0] !== props.rows) {
		t7 = Symbol.for("react.early_return_sentinel");
		bb0: {
			let rows = t0.filter(_temp$6);
			if (rows.length === 0) {
				t7 = null;
				break bb0;
			}
			T1 = Card, t3 = !0, t4 = 2, t5 = 3, t6 = "transparent", T0 = Stack, t1 = 3, t2 = rows.map(_temp2$2);
		}
		$[0] = props.rows, $[1] = T0, $[2] = T1, $[3] = t1, $[4] = t2, $[5] = t3, $[6] = t4, $[7] = t5, $[8] = t6, $[9] = t7;
	} else T0 = $[1], T1 = $[2], t1 = $[3], t2 = $[4], t3 = $[5], t4 = $[6], t5 = $[7], t6 = $[8], t7 = $[9];
	if (t7 !== Symbol.for("react.early_return_sentinel")) return t7;
	let t8;
	$[10] !== T0 || $[11] !== t1 || $[12] !== t2 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(T0, {
		gap: t1,
		children: t2
	}), $[10] = T0, $[11] = t1, $[12] = t2, $[13] = t8) : t8 = $[13];
	let t9;
	return $[14] !== T1 || $[15] !== t3 || $[16] !== t4 || $[17] !== t5 || $[18] !== t6 || $[19] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(T1, {
		border: t3,
		radius: t4,
		padding: t5,
		tone: t6,
		children: t8
	}), $[14] = T1, $[15] = t3, $[16] = t4, $[17] = t5, $[18] = t6, $[19] = t8, $[20] = t9) : t9 = $[20], t9;
}
function _temp2$2(row_0) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 2,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 0,
			muted: !0,
			weight: "medium",
			children: row_0.label
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InlineCode, { children: row_0.value })
		})]
	}, row_0.label);
}
function _temp$6(row) {
	return !!row.value;
}
function ProjectNotFoundScreen(props) {
	let $ = (0, import_compiler_runtime.c)(9), { projectId, manageBaseUrl, onChooseAnotherWorkspace } = props, t0;
	$[0] === projectId ? t0 = $[1] : (t0 = projectId ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		"No project with the ID ",
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(InlineCode, { children: projectId }),
		" exists, or you don't have access to it."
	] }) : "The Studio is configured for a project that doesn't exist, or that you don't have access to.", $[0] = projectId, $[1] = t0);
	let t1;
	$[2] === Symbol.for("react.memo_cache_sentinel") ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		"Check that the ",
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(InlineCode, { children: "projectId" }),
		" in your Studio config is correct. You can find your project IDs in Manage."
	] }), $[2] = t1) : t1 = $[2];
	let t2 = `${manageBaseUrl}/manage`, t3;
	$[3] === t2 ? t3 = $[4] : (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		as: "a",
		href: t2,
		iconRight: LaunchIcon,
		rel: "noopener noreferrer",
		size: "large",
		target: "_blank",
		text: "Open Manage"
	}), $[3] = t2, $[4] = t3);
	let t4;
	return $[5] !== onChooseAnotherWorkspace || $[6] !== t0 || $[7] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfigErrorLayout, {
		errorLabel: "Project not found",
		heading: "Project not found",
		onChooseAnotherWorkspace,
		message: t0,
		details: null,
		hint: t1,
		action: t3
	}), $[5] = onChooseAnotherWorkspace, $[6] = t0, $[7] = t3, $[8] = t4) : t4 = $[8], t4;
}
function DatasetNotFoundScreen(props) {
	let $ = (0, import_compiler_runtime.c)(14), { projectId, dataset, manageBaseUrl, onChooseAnotherWorkspace } = props, datasetsUrl = projectId ? `${manageBaseUrl}/manage/project/${projectId}/datasets` : `${manageBaseUrl}/manage`, t0;
	$[0] === projectId ? t0 = $[1] : (t0 = {
		label: "Project ID",
		value: projectId
	}, $[0] = projectId, $[1] = t0);
	let t1;
	$[2] === dataset ? t1 = $[3] : (t1 = {
		label: "Dataset",
		value: dataset
	}, $[2] = dataset, $[3] = t1);
	let t2;
	$[4] !== t0 || $[5] !== t1 ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfigDetails, { rows: [t0, t1] }), $[4] = t0, $[5] = t1, $[6] = t2) : t2 = $[6];
	let t3;
	$[7] === Symbol.for("react.memo_cache_sentinel") ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		"Check that the ",
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(InlineCode, { children: "dataset" }),
		" in your Studio config is correct, or create it in Manage."
	] }), $[7] = t3) : t3 = $[7];
	let t4;
	$[8] === datasetsUrl ? t4 = $[9] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
		as: "a",
		href: datasetsUrl,
		iconRight: LaunchIcon,
		rel: "noopener noreferrer",
		size: "large",
		target: "_blank",
		text: "Manage datasets"
	}), $[8] = datasetsUrl, $[9] = t4);
	let t5;
	return $[10] !== onChooseAnotherWorkspace || $[11] !== t2 || $[12] !== t4 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfigErrorLayout, {
		errorLabel: "Dataset not found",
		heading: "Dataset not found",
		onChooseAnotherWorkspace,
		message: "The Studio is configured for a dataset that doesn't exist, or that you don't have access to.",
		details: t2,
		hint: t3,
		action: t4
	}), $[10] = onChooseAnotherWorkspace, $[11] = t2, $[12] = t4, $[13] = t5) : t5 = $[13], t5;
}
function DocsLink() {
	let $ = (0, import_compiler_runtime.c)(1), t0;
	return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		justify: "flex-end",
		paddingTop: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HelpLink$1, {
				href: "https://www.sanity.io/docs/configuration",
				rel: "noopener noreferrer",
				style: { textDecoration: "none" },
				target: "_blank",
				children: "Studio configuration docs →"
			})
		})
	}), $[0] = t0) : t0 = $[0], t0;
}
/**
* Renders the config-error takeover ({@link ConfigErrorScreen}) when
* `WorkspacesProvider` has detected a missing project/dataset, otherwise
* renders its children.
*
* It sits below `ActiveWorkspaceMatcher` (the error is surfaced up through
* `ConfigErrorContext`) precisely so the screen can use the same
* workspace-switcher hooks the auth screens use — `useVisibleWorkspaces`
* and `useActiveWorkspace` are only available here.
*
* The error is shown only while the active workspace is the one that
* failed. `WorkspacesProvider` latches `configError` for the lifetime of
* the mount, so without this check switching to a healthy workspace (via
* the screen's "Choose another workspace" button, which is a router push,
* not a reload) would keep the error up.
*
* @internal
*/
function ConfigErrorGate(t0) {
	let $ = (0, import_compiler_runtime.c)(5), { children } = t0, configError = (0, import_react.useContext)(ConfigErrorContext), { activeWorkspace } = useActiveWorkspace(), isActiveWorkspaceFailing = configError !== null && activeWorkspace.projectId === configError.projectId && activeWorkspace.dataset === configError.dataset;
	if (configError && isActiveWorkspaceFailing) {
		let t1;
		return $[0] !== configError.dataset || $[1] !== configError.error || $[2] !== configError.isStaging || $[3] !== configError.projectId ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfigErrorScreen, {
			error: configError.error,
			projectId: configError.projectId,
			dataset: configError.dataset,
			isStaging: configError.isStaging
		}), $[0] = configError.dataset, $[1] = configError.error, $[2] = configError.isStaging, $[3] = configError.projectId, $[4] = t1) : t1 = $[4], t1;
	}
	return children;
}
var EMPTY_AUTH_STATES = {};
var useWorkspaceAuthStates = createHookFromObservableFactory((workspaces) => workspaces.length === 0 ? of(EMPTY_AUTH_STATES) : merge$1(...workspaces.map((workspace) => workspace.auth.state.pipe(map((state) => [workspace.name, state])))).pipe(scan((acc, [name, state]) => ({
	...acc,
	[name]: state
}), {}), startWith(EMPTY_AUTH_STATES)), EMPTY_AUTH_STATES);
/** @internal */
function VisibleWorkspacesProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(13), { children } = t0, allWorkspaces = useWorkspaces(), t1;
	$[0] === allWorkspaces ? t1 = $[1] : (t1 = allWorkspaces.filter(_temp$5), $[0] = allWorkspaces, $[1] = t1);
	let [workspaceAuthStates] = useWorkspaceAuthStates(t1), t2;
	if ($[2] !== allWorkspaces || $[3] !== workspaceAuthStates) {
		let t3;
		$[5] === workspaceAuthStates ? t3 = $[6] : (t3 = (workspace_0) => !evaluateWorkspaceHidden(workspace_0, workspaceAuthStates[workspace_0.name]), $[5] = workspaceAuthStates, $[6] = t3), t2 = allWorkspaces.filter(t3), $[2] = allWorkspaces, $[3] = workspaceAuthStates, $[4] = t2;
	} else t2 = $[4];
	let t3;
	$[7] !== t2 || $[8] !== workspaceAuthStates ? (t3 = {
		visibleWorkspaces: t2,
		workspaceAuthStates
	}, $[7] = t2, $[8] = workspaceAuthStates, $[9] = t3) : t3 = $[9];
	let value = t3, t4;
	return $[10] !== children || $[11] !== value ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VisibleWorkspacesContext.Provider, {
		value,
		children
	}), $[10] = children, $[11] = value, $[12] = t4) : t4 = $[12], t4;
}
function _temp$5(workspace) {
	return typeof workspace.hidden == "function";
}
/**
* Probe `/check/cors` for the current origin, deduping concurrent probes
* through `cache`. Returns:
*  - a `CorsCheckResult` — the allow/credentials verdict
*  - `'project-not-found'` — a `404 / SIO-404-PNF` body, i.e. the project
*    doesn't exist (the CORS endpoint still answers with CORS headers, so
*    this is the one place a missing project is detectable)
*  - `null` when the probe can't conclude (no projectId/url, other non-ok
*    response, network failure, unparseable body)
*
* @internal
*/
function checkCors(client, cache, options = {}) {
	let { projectId, apiHost, url: baseUrl } = client.config();
	if (!projectId || !baseUrl) return Promise.resolve(null);
	let cacheKey = `${projectId}@${apiHost ?? ""}`;
	if (options.force) cache.delete(cacheKey);
	else {
		let cached = cache.get(cacheKey);
		if (cached) return cached;
	}
	let probeUrl = `${baseUrl.replace(/\/+$/, "")}/check/cors`, check = fetch(probeUrl, {
		method: "GET",
		credentials: "omit"
	}).then(async (response) => {
		let body = await response.json().catch(() => null);
		return response.status === 404 && body?.errorCode === "SIO-404-PNF" ? "project-not-found" : !response.ok || !body ? null : {
			allowed: body.result?.allowed === !0,
			withCredentials: body.result?.withCredentials === !0
		};
	}).catch(() => null).then((result) => {
		let isPositive = result !== null && result !== "project-not-found" && result.allowed && result.withCredentials;
		return (result === null || isPositive) && cache.get(cacheKey) === check && cache.delete(cacheKey), result;
	});
	return cache.set(cacheKey, check), check;
}
/**
* Build a {@link RequestFailureProbe} bound to a client + CORS-probe cache.
*
* Detection order:
*  1. A structured 404 (project/dataset management API) is a config error.
*  2. An opaque network failure (not a timeout) is probed via `/check/cors`,
*     which can still answer where the data API can't: a missing project
*     (`SIO-404-PNF`) or the origin's allow/credentials verdict.
* Everything else is `unknown`.
*
* @internal
*/
function createRequestFailureProbe(client, corsCache) {
	return async (err) => {
		let configClassification = classifyConfigError(err);
		if (configClassification) return configClassification.type === "datasetNotFound" ? { type: "dataset-not-found" } : { type: "project-not-found" };
		if (!isNetworkError(err) || isTimeoutError(err)) return { type: "unknown" };
		let cors = await checkCors(client, corsCache);
		return cors === "project-not-found" ? { type: "project-not-found" } : cors === null || cors.allowed && cors.withCredentials ? { type: "unknown" } : {
			type: "cors",
			allowed: cors.allowed,
			withCredentials: cors.withCredentials
		};
	};
}
/**
* Things a user can check themselves when the studio can't reach the
* Sanity API. Ordered most-likely-first.
*/
var NETWORK_TROUBLESHOOTING = [
	"Check that your device is online.",
	"Disable VPNs, ad blockers, or browser extensions that may block requests.",
	"Check status.sanity.io for ongoing incidents."
];
function NetworkTroubleshooting() {
	let $ = (0, import_compiler_runtime.c)(2), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		weight: "medium",
		children: "Troubleshooting"
	}), $[0] = t0) : t0 = $[0];
	let t1;
	return $[1] === Symbol.for("react.memo_cache_sentinel") ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		border: !0,
		radius: 2,
		padding: 3,
		tone: "transparent",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			gap: 3,
			children: [t0, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
				as: "ul",
				gap: 2,
				style: {
					margin: 0,
					paddingLeft: "1.25em"
				},
				children: NETWORK_TROUBLESHOOTING.map(_temp$4)
			})]
		})
	}), $[1] = t1) : t1 = $[1], t1;
}
/**
* Ticks down from `claim.retryAfterSeconds` to 0, once per second.
* Resets whenever a new claim object arrives — a retried request that
* gets rate-limited again produces a fresh claim, and the countdown must
* restart from the new Retry-After rather than staying at 0 with an
* enabled button.
*
* @internal
*/
function _temp$4(tip) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		as: "li",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			muted: !0,
			children: tip
		})
	}, tip);
}
function useRetryCountdown(claim) {
	let $ = (0, import_compiler_runtime.c)(4), [secondsLeft, setSecondsLeft] = (0, import_react.useState)(claim.retryAfterSeconds ?? 0), t0;
	$[0] === claim.retryAfterSeconds ? t0 = $[1] : (t0 = () => {
		let initial = claim.retryAfterSeconds ?? 0;
		if ((0, import_react.startTransition)(() => setSecondsLeft(initial)), initial <= 0) return;
		let id = setInterval(() => {
			setSecondsLeft((current) => {
				let next = Math.max(0, current - 1);
				return next === 0 && clearInterval(id), next;
			});
		}, 1e3);
		return () => clearInterval(id);
	}, $[0] = claim.retryAfterSeconds, $[1] = t0);
	let t1;
	return $[2] === claim ? t1 = $[3] : (t1 = [claim], $[2] = claim, $[3] = t1), (0, import_react.useEffect)(t0, t1), secondsLeft;
}
/**
* Studio dialog for claimed request errors (network / 5xx / 429).
*
* Button policy is driven by the caller's `retryable` assertion — the
* call site knows whether re-running its request is safe; the studio
* doesn't guess:
*  - `retryable: true` → "Try again" re-runs the parked request(s),
*    plus "Reload Studio" as an escape hatch.
*  - `retryable: false` → only "Reload Studio", with conservative copy
*    warning that the last change may not have been applied.
*  - 429 additionally gates "Try again" behind a live Retry-After
*    countdown.
*
* @internal
*/
function RequestErrorDialog(props) {
	let $ = (0, import_compiler_runtime.c)(20), { claim, onRetry } = props;
	if (claim.type === "rateLimited") {
		let t0;
		return $[0] !== claim || $[1] !== onRetry ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RateLimitedDialog, {
			claim,
			onRetry
		}), $[0] = claim, $[1] = onRetry, $[2] = t0) : t0 = $[2], t0;
	}
	let heading = claim.type === "serverError" ? "Server error" : "Network error", message = claim.type === "serverError" ? claim.retryable ? "The server ran into an issue and couldn't complete the request. You can try again, or reload the Studio." : "The server ran into an issue and couldn't complete the request. Your last change may not have been saved. Reload the Studio to see the current state." : claim.retryable ? "Couldn't reach the Sanity servers. Check your network connection and try again." : "Couldn't reach the Sanity servers. Your last change may not have been sent. Check your network connection and reload the Studio to see the current state.", t0;
	$[3] !== claim.retryable || $[4] !== onRetry ? (t0 = claim.retryable ? {
		cancelButton: {
			text: "Reload Studio",
			onClick: _temp2$1,
			tone: "default"
		},
		confirmButton: {
			text: "Try again",
			onClick: onRetry,
			tone: "default"
		}
	} : { confirmButton: {
		text: "Reload Studio",
		onClick: _temp3$1,
		tone: "default"
	} }, $[3] = claim.retryable, $[4] = onRetry, $[5] = t0) : t0 = $[5];
	let t1;
	$[6] === message ? t1 = $[7] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, { children: message }), $[6] = message, $[7] = t1);
	let t2;
	$[8] === claim.type ? t2 = $[9] : (t2 = claim.type === "networkError" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NetworkTroubleshooting, {}) : null, $[8] = claim.type, $[9] = t2);
	let t3;
	$[10] === claim.type ? t3 = $[11] : (t3 = claim.type === "serverError" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
			href: "https://status.sanity.io",
			rel: "noopener noreferrer",
			style: { color: "var(--card-link-fg-color)" },
			target: "_blank",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
				as: "span",
				align: "center",
				gap: 2,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: "Check Sanity Status" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LaunchIcon, {})]
			})
		})
	}) : null, $[10] = claim.type, $[11] = t3);
	let t4;
	$[12] !== t1 || $[13] !== t2 || $[14] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 4,
		children: [
			t1,
			t2,
			t3
		]
	}), $[12] = t1, $[13] = t2, $[14] = t3, $[15] = t4) : t4 = $[15];
	let t5;
	return $[16] !== heading || $[17] !== t0 || $[18] !== t4 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog$1, {
		id: "request-error-dialog",
		header: heading,
		width: 1,
		footer: t0,
		children: t4
	}), $[16] = heading, $[17] = t0, $[18] = t4, $[19] = t5) : t5 = $[19], t5;
}
function _temp3$1() {
	return window.location.reload();
}
function _temp2$1() {
	return window.location.reload();
}
function RateLimitedDialog(props) {
	let $ = (0, import_compiler_runtime.c)(15), { claim, onRetry } = props, secondsLeft = useRetryCountdown(claim), [retrying, setRetrying] = (0, import_react.useState)(!1), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = () => {
		(0, import_react.startTransition)(() => setRetrying(!1));
	}, $[0] = t0) : t0 = $[0];
	let t1;
	$[1] === claim ? t1 = $[2] : (t1 = [claim], $[1] = claim, $[2] = t1), (0, import_react.useEffect)(t0, t1);
	let t2;
	$[3] === onRetry ? t2 = $[4] : (t2 = () => {
		setRetrying(!0), onRetry();
	}, $[3] = onRetry, $[4] = t2);
	let handleRetry = t2;
	if (!claim.retryable) {
		let t3;
		$[5] === Symbol.for("react.memo_cache_sentinel") ? (t3 = { confirmButton: {
			text: "Reload Studio",
			onClick: _temp4,
			tone: "default"
		} }, $[5] = t3) : t3 = $[5];
		let t4;
		return $[6] === Symbol.for("react.memo_cache_sentinel") ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog$1, {
			id: "request-error-dialog",
			header: "Too many requests",
			width: 1,
			footer: t3,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
				gap: 4,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, { children: "Too many requests at once. Reload the Studio to try again." })
			})
		}), $[6] = t4) : t4 = $[6], t4;
	}
	let t3 = retrying ? "Retrying…" : secondsLeft > 0 ? `Try again (${secondsLeft})` : "Try again", t4 = secondsLeft > 0 || retrying, t5;
	$[7] === Symbol.for("react.memo_cache_sentinel") ? (t5 = { fontVariantNumeric: "tabular-nums" }, $[7] = t5) : t5 = $[7];
	let t6;
	$[8] !== handleRetry || $[9] !== t3 || $[10] !== t4 ? (t6 = { confirmButton: {
		text: t3,
		onClick: handleRetry,
		disabled: t4,
		tone: "default",
		style: t5
	} }, $[8] = handleRetry, $[9] = t3, $[10] = t4, $[11] = t6) : t6 = $[11];
	let t7;
	$[12] === Symbol.for("react.memo_cache_sentinel") ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		gap: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, { children: "Too many requests at once. You can try again shortly." })
	}), $[12] = t7) : t7 = $[12];
	let t8;
	return $[13] === t6 ? t8 = $[14] : (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog$1, {
		id: "request-error-dialog",
		header: "Too many requests",
		width: 1,
		footer: t6,
		children: t7
	}), $[13] = t6, $[14] = t8), t8;
}
function _temp4() {
	return window.location.reload();
}
var CenteredContainer = gt(Flex).withConfig({
	displayName: "CenteredContainer",
	componentId: "sc-rfv2gt-0"
})`min-height:100vh;box-sizing:border-box;`;
var ContentWrapper = gt(Box).withConfig({
	displayName: "ContentWrapper",
	componentId: "sc-rfv2gt-1"
})`width:100%;max-width:640px;`;
var HelpLink = gt.a.withConfig({
	displayName: "HelpLink",
	componentId: "sc-rfv2gt-2"
})`color:var(--card-link-fg-color);text-decoration:none;display:inline-flex;align-items:center;gap:0.25em;&:hover{text-decoration:underline;}`;
var STUDIO_HOST_PATTERN = /^(https?:\/\/)?[\w-]+(\.[\w-]+)+([/?#].*)?$/;
var STUDIO_HOST_DENYLIST = ["127.0.0.1", "0.0.0.0"];
function canRegisterStudioForOrigin(origin) {
	let host = origin.replace(/^https?:\/\//, "").split(/[:/?#]/, 1)[0];
	return !STUDIO_HOST_DENYLIST.includes(host) && STUDIO_HOST_PATTERN.test(origin);
}
/** @internal */
function CorsOriginErrorScreen(props) {
	let $ = (0, import_compiler_runtime.c)(40), { projectId, isStaging, primaryProjectId, allowed, withCredentials, origin } = props, t0;
	$[0] !== origin || $[1] !== primaryProjectId || $[2] !== projectId ? (t0 = !!(primaryProjectId && projectId && primaryProjectId === projectId) && canRegisterStudioForOrigin(origin), $[0] = origin, $[1] = primaryProjectId, $[2] = projectId, $[3] = t0) : t0 = $[3];
	let showRegisterOption = t0, manageBaseUrl = isStaging ? "https://sanity.work" : "https://sanity.io", t1;
	if ($[4] !== manageBaseUrl || $[5] !== origin || $[6] !== projectId) {
		let url = new URL(`/manage/project/${projectId}/api`, manageBaseUrl);
		url.searchParams.set("cors", "add"), url.searchParams.set("origin", origin), url.searchParams.set("credentials", ""), t1 = url.toString(), $[4] = manageBaseUrl, $[5] = origin, $[6] = projectId, $[7] = t1;
	} else t1 = $[7];
	let corsUrl = t1, t2;
	if ($[8] !== manageBaseUrl || $[9] !== origin || $[10] !== projectId) {
		let url_0 = new URL(`/manage/project/${projectId}/studios`, manageBaseUrl);
		url_0.searchParams.set("studio", "add"), url_0.searchParams.set("origin", origin), t2 = url_0.toString(), $[8] = manageBaseUrl, $[9] = origin, $[10] = projectId, $[11] = t2;
	} else t2 = $[11];
	let registerUrl = t2, t3;
	if ($[12] !== manageBaseUrl || $[13] !== origin || $[14] !== projectId) {
		let url_1 = new URL(`/manage/project/${projectId}/api`, manageBaseUrl);
		url_1.searchParams.set("cors", "add"), url_1.searchParams.set("origin", origin), url_1.searchParams.set("credentials", "1"), t3 = url_1.toString(), $[12] = manageBaseUrl, $[13] = origin, $[14] = projectId, $[15] = t3;
	} else t3 = $[15];
	let reAddCorsUrl = t3;
	if (allowed && !withCredentials) {
		let t4, t5;
		$[16] === Symbol.for("react.memo_cache_sentinel") ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Heading, {
			as: "h1",
			size: 2,
			children: "Enable credentials for this Studio"
		}), t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 2,
			muted: !0,
			children: "This origin is registered with your project, but the existing CORS entry doesn't allow credentialed requests. The Studio needs them for login, drafts, and mutations. CORS entries can't be edited, so remove the existing entry for this origin, then re-add it with credentials."
		}), $[16] = t4, $[17] = t5) : (t4 = $[16], t5 = $[17]);
		let t6;
		$[18] === reAddCorsUrl ? t6 = $[19] : (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
			as: "a",
			href: reAddCorsUrl,
			iconRight: LaunchIcon,
			rel: "noopener noreferrer",
			size: "large",
			target: "_blank",
			text: "Re-add CORS origin with credentials"
		}) }), $[18] = reAddCorsUrl, $[19] = t6);
		let t7;
		$[20] === Symbol.for("react.memo_cache_sentinel") ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
			justify: "flex-end",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HelpLink, {
					href: "https://www.sanity.io/docs/cors",
					rel: "noopener noreferrer",
					style: { textDecoration: "none" },
					target: "_blank",
					children: "Need help with CORS? →"
				})
			})
		}), $[20] = t7) : t7 = $[20];
		let t8;
		return $[21] === t6 ? t8 = $[22] : (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
			"data-testid": "studio-error-screen",
			"data-error": "CORS credentials disabled",
			height: "fill",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CenteredContainer, {
				align: "center",
				justify: "center",
				padding: 4,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ContentWrapper, {
					paddingBottom: 5,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
						gap: 5,
						children: [
							t4,
							t5,
							t6,
							t7
						]
					})
				})
			})
		}), $[21] = t6, $[22] = t8), t8;
	}
	let t4, t5;
	$[23] === Symbol.for("react.memo_cache_sentinel") ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Heading, {
		as: "h1",
		size: 2,
		children: "Connect this Studio to your project"
	}), t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 2,
		muted: !0,
		children: "This Studio isn't connected to your project yet. Pick an option below to connect it."
	}), $[23] = t4, $[24] = t5) : (t4 = $[23], t5 = $[24]);
	let t6;
	$[25] === showRegisterOption ? t6 = $[26] : (t6 = showRegisterOption ? [
		1,
		1,
		2
	] : 1, $[25] = showRegisterOption, $[26] = t6);
	let t7;
	$[27] !== registerUrl || $[28] !== showRegisterOption ? (t7 = showRegisterOption && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		border: !0,
		padding: 4,
		radius: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			direction: "column",
			gap: 4,
			height: "fill",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
				gap: 4,
				flex: 1,
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
						size: 2,
						weight: "medium",
						children: "Register Studio"
					}),
					false,
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
						size: 1,
						muted: !0,
						children: "Adds a CORS origin for this URL and registers the Studio so it can sync its schema and manifest with the project. Required for schema-aware search, Content Agent, and other features that read the deployed schema."
					})
				]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
				as: "a",
				href: registerUrl,
				target: "_blank",
				rel: "noopener noreferrer",
				size: "large",
				text: "Register Studio",
				width: "fill"
			})]
		})
	}), $[27] = registerUrl, $[28] = showRegisterOption, $[29] = t7) : t7 = $[29];
	let t8;
	$[30] === Symbol.for("react.memo_cache_sentinel") ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 4,
		flex: 1,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 2,
			weight: "medium",
			children: "Add CORS origin"
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			muted: !0,
			children: "For URLs that don't need schema syncing or other registered-Studio features (localhost, preview deploys, and other one-off origins)."
		})]
	}), $[30] = t8) : t8 = $[30];
	let t9;
	$[31] === corsUrl ? t9 = $[32] : (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		border: !0,
		padding: 4,
		radius: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			direction: "column",
			gap: 4,
			height: "fill",
			children: [t8, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1$1, {
				as: "a",
				href: corsUrl,
				iconRight: LaunchIcon,
				target: "_blank",
				rel: "noopener noreferrer",
				text: "Add CORS origin",
				mode: "ghost",
				size: "large",
				width: "fill"
			})]
		})
	}), $[31] = corsUrl, $[32] = t9);
	let t10;
	$[33] !== t6 || $[34] !== t7 || $[35] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Grid, {
		gridTemplateColumns: t6,
		gapX: 4,
		gapY: 3,
		children: [t7, t9]
	}), $[33] = t6, $[34] = t7, $[35] = t9, $[36] = t10) : t10 = $[36];
	let t11;
	$[37] === Symbol.for("react.memo_cache_sentinel") ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		justify: "flex-end",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HelpLink, {
				href: "https://www.sanity.io/docs/cors",
				target: "_blank",
				rel: "noopener noreferrer",
				style: { textDecoration: "none" },
				children: "Need help with CORS? →"
			})
		})
	}), $[37] = t11) : t11 = $[37];
	let t12;
	return $[38] === t10 ? t12 = $[39] : (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		"data-testid": "studio-error-screen",
		"data-error": "CORS origin error",
		height: "fill",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CenteredContainer, {
			align: "center",
			justify: "center",
			padding: 4,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ContentWrapper, {
				paddingBottom: 5,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
					gap: 5,
					children: [
						t4,
						t5,
						t10,
						t11
					]
				})
			})
		})
	}), $[38] = t10, $[39] = t12), t12;
}
/**
* Wraps `CorsOriginErrorScreen` with a poll loop that periodically re-checks
* whether the CORS misconfig has been fixed (via Manage / register flow).
* On resolution, calls `onResolved` so `WorkspacesProvider` can swap the
* screen for its normal children.
*
* Polling cadence depends on `document.visibilityState`: 5s while focused,
* 30s when hidden. An immediate check fires on `visibilitychange` → visible
* and on `focus`, so returning to the tab after fixing the misconfig in
* Manage resolves quickly.
*
* @internal
*/
function CorsOriginErrorView(props) {
	let $ = (0, import_compiler_runtime.c)(10), { event, primaryProjectId, onResolved } = props, { recheck } = event, t0, t1;
	$[0] !== onResolved || $[1] !== recheck ? (t0 = () => {
		let cancelled = !1, timeoutId, runCheck = async () => {
			if (!cancelled) {
				try {
					let stillMisconfigured = await recheck();
					if (cancelled) return;
					if (!stillMisconfigured) {
						onResolved();
						return;
					}
				} catch {}
				schedule();
			}
		}, schedule = () => {
			if (cancelled) return;
			let interval = typeof document < "u" && document.visibilityState === "hidden" ? 3e4 : 5e3;
			timeoutId = setTimeout(runCheck, interval);
		}, checkNow = () => {
			timeoutId &&= (clearTimeout(timeoutId), void 0), runCheck();
		}, handleVisibility = () => {
			typeof document < "u" && document.visibilityState === "visible" && checkNow();
		};
		return typeof window < "u" && window.addEventListener("focus", checkNow), typeof document < "u" && document.addEventListener("visibilitychange", handleVisibility), schedule(), () => {
			cancelled = !0, timeoutId && clearTimeout(timeoutId), typeof window < "u" && window.removeEventListener("focus", checkNow), typeof document < "u" && document.removeEventListener("visibilitychange", handleVisibility);
		};
	}, t1 = [recheck, onResolved], $[0] = onResolved, $[1] = recheck, $[2] = t0, $[3] = t1) : (t0 = $[2], t1 = $[3]), (0, import_react.useEffect)(t0, t1);
	let t2;
	return $[4] !== event.allowed || $[5] !== event.isStaging || $[6] !== event.projectId || $[7] !== event.withCredentials || $[8] !== primaryProjectId ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CorsOriginErrorScreen, {
		projectId: event.projectId,
		isStaging: event.isStaging,
		primaryProjectId,
		allowed: event.allowed,
		withCredentials: event.withCredentials,
		origin: window.location.origin
	}), $[4] = event.allowed, $[5] = event.isStaging, $[6] = event.projectId, $[7] = event.withCredentials, $[8] = primaryProjectId, $[9] = t2) : t2 = $[9], t2;
}
/** @internal */
function WorkspacesProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(51), { config, children, basePath, LoadingComponent, primaryProjectId } = t0, [corsError, setCorsError] = (0, import_react.useState)(), [configError, setConfigError] = (0, import_react.useState)(), [corsRetry, onCorsRetry] = useObservableEventHandler(), t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = new QuickLRU({
		maxAge: 12e4,
		maxSize: 200
	}), $[0] = t1) : t1 = $[0];
	let corsCache = t1, workspacesRef = (0, import_react.useRef)(null), [requestErrorChannel] = (0, import_react.useState)(_temp$3), t2;
	$[1] === Symbol.for("react.memo_cache_sentinel") ? (t2 = (result, client) => {
		let clientConfig = client.config(), isStaging = !!clientConfig.apiHost?.endsWith(".work");
		if (result.type === "cors") {
			setConfigError(void 0), setCorsError({
				isStaging,
				projectId: clientConfig.projectId,
				allowed: result.allowed,
				withCredentials: result.withCredentials
			});
			return;
		}
		setConfigError({
			error: { type: result.type === "dataset-not-found" ? "datasetNotFound" : "projectNotFound" },
			isStaging,
			projectId: clientConfig.projectId,
			dataset: clientConfig.dataset
		});
	}, $[1] = t2) : t2 = $[1];
	let applyRequestFailure = t2, t3;
	$[2] !== corsRetry || $[3] !== requestErrorChannel ? (t3 = (requestOptions, originalRequest, client_0) => defer(() => originalRequest(requestOptions)).pipe(catchError((requestError, caught) => isInvalidSessionError(requestError) ? (requestErrorChannel.handle(requestError), NEVER) : from(createRequestFailureProbe(client_0, corsCache)(requestError)).pipe(mergeMap((result_0) => {
		if (result_0.type === "unknown") throw requestError;
		return applyRequestFailure(result_0, client_0), result_0.type === "cors" ? corsRetry.pipe(take(1), mergeMap(() => caught.pipe(tap(() => setCorsError(void 0))))) : NEVER;
	})))), $[2] = corsRetry, $[3] = requestErrorChannel, $[4] = t3) : t3 = $[4];
	let requestHandler = t3, t4;
	$[5] === Symbol.for("react.memo_cache_sentinel") ? (t4 = {
		diagnose: (err, client_1) => createRequestFailureProbe(client_1, corsCache)(err),
		onRequestFailure: (result_1, client_2) => applyRequestFailure(result_1, client_2)
	}, $[5] = t4) : t4 = $[5];
	let requestFailureDiagnostics = t4, t5;
	$[6] !== basePath || $[7] !== config || $[8] !== requestErrorChannel || $[9] !== requestHandler ? (t5 = prepareConfig(config, {
		basePath,
		requestHandler,
		requestErrorChannel,
		requestFailureDiagnostics
	}), $[6] = basePath, $[7] = config, $[8] = requestErrorChannel, $[9] = requestHandler, $[10] = t5) : t5 = $[10];
	let workspaces = (0, import_react.useDeferredValue)(t5.workspaces, null), t6, t7;
	$[11] === workspaces ? (t6 = $[12], t7 = $[13]) : (t6 = () => {
		workspacesRef.current = workspaces;
	}, t7 = [workspaces], $[11] = workspaces, $[12] = t6, $[13] = t7), (0, import_react.useEffect)(t6, t7);
	let claim = useSyncObservable(requestErrorChannel.claim$, null) ?? void 0, loggedOutReason = claim?.type === "unauthorized" ? getApiErrorCode(claim.error) === "SIO-401-AEX" ? "session-expired" : "session-not-found" : void 0, t8, t9;
	$[14] === claim ? (t8 = $[15], t9 = $[16]) : (t8 = () => {
		if (claim?.type !== "unauthorized") return;
		let current = workspacesRef.current;
		if (!current) return;
		let target = claim.projectId ? current.find((ws) => ws.projectId === claim.projectId) : current[0];
		target?.auth.logout && target.auth.logout().catch(_temp2);
	}, t9 = [claim], $[14] = claim, $[15] = t8, $[16] = t9), (0, import_react.useEffect)(t8, t9);
	let t10;
	$[17] === onCorsRetry ? t10 = $[18] : (t10 = () => {
		setCorsError(void 0), onCorsRetry();
	}, $[17] = onCorsRetry, $[18] = t10);
	let handleCorsResolved = t10;
	if (workspaces === null) {
		let t11;
		return $[19] === LoadingComponent ? t11 = $[20] : (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingComponent, {}), $[19] = LoadingComponent, $[20] = t11), t11;
	}
	if (corsError && !configError) {
		let t11;
		$[21] !== corsError.projectId || $[22] !== workspaces ? (t11 = async () => {
			let target_0 = corsError.projectId ? workspaces.find((ws_0) => ws_0.projectId === corsError.projectId) : workspaces[0];
			if (!target_0) return !0;
			let { client: client_3 } = await firstValueFrom(target_0.auth.state), result_2 = await checkCors(client_3, corsCache, { force: !0 });
			return result_2 === null || result_2 === "project-not-found" || !(result_2.allowed && result_2.withCredentials);
		}, $[21] = corsError.projectId, $[22] = workspaces, $[23] = t11) : t11 = $[23];
		let t12;
		$[24] !== corsError.allowed || $[25] !== corsError.isStaging || $[26] !== corsError.projectId || $[27] !== corsError.withCredentials || $[28] !== t11 ? (t12 = {
			isStaging: corsError.isStaging,
			projectId: corsError.projectId,
			allowed: corsError.allowed,
			withCredentials: corsError.withCredentials,
			recheck: t11
		}, $[24] = corsError.allowed, $[25] = corsError.isStaging, $[26] = corsError.projectId, $[27] = corsError.withCredentials, $[28] = t11, $[29] = t12) : t12 = $[29];
		let t13;
		return $[30] !== handleCorsResolved || $[31] !== primaryProjectId || $[32] !== t12 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CorsOriginErrorView, {
			event: t12,
			onResolved: handleCorsResolved,
			primaryProjectId
		}), $[30] = handleCorsResolved, $[31] = primaryProjectId, $[32] = t12, $[33] = t13) : t13 = $[33], t13;
	}
	let t11 = configError ?? null, t12;
	$[34] !== claim || $[35] !== configError || $[36] !== requestErrorChannel.retry ? (t12 = !configError && claim && claim.type !== "unauthorized" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RequestErrorDialog, {
		claim,
		onRetry: requestErrorChannel.retry
	}), $[34] = claim, $[35] = configError, $[36] = requestErrorChannel.retry, $[37] = t12) : t12 = $[37];
	let t13;
	$[38] !== children || $[39] !== loggedOutReason || $[40] !== t12 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(LoggedOutReasonContext.Provider, {
		value: loggedOutReason,
		children: [t12, children]
	}), $[38] = children, $[39] = loggedOutReason, $[40] = t12, $[41] = t13) : t13 = $[41];
	let t14;
	$[42] !== requestErrorChannel || $[43] !== t13 ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioErrorHandlerContext.Provider, {
		value: requestErrorChannel,
		children: t13
	}), $[42] = requestErrorChannel, $[43] = t13, $[44] = t14) : t14 = $[44];
	let t15;
	$[45] !== t11 || $[46] !== t14 ? (t15 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfigErrorContext.Provider, {
		value: t11,
		children: t14
	}), $[45] = t11, $[46] = t14, $[47] = t15) : t15 = $[47];
	let t16;
	return $[48] !== t15 || $[49] !== workspaces ? (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspacesContext.Provider, {
		value: workspaces,
		children: t15
	}), $[48] = t15, $[49] = workspaces, $[50] = t16) : t16 = $[50], t16;
}
function _temp2(logoutErr) {
	console.warn("[sanity] Forced logout failed:", logoutErr);
}
function _temp$3() {
	return createRequestErrorChannel();
}
function useObservableEventHandler() {
	let $ = (0, import_compiler_runtime.c)(7), [subject] = (0, import_react.useState)(_temp3), t0;
	$[0] === subject ? t0 = $[1] : (t0 = (value) => {
		subject.next(value);
	}, $[0] = subject, $[1] = t0);
	let callback = t0, t1;
	$[2] === subject ? t1 = $[3] : (t1 = subject.asObservable(), $[2] = subject, $[3] = t1);
	let observable = t1, t2;
	return $[4] !== callback || $[5] !== observable ? (t2 = [observable, callback], $[4] = callback, $[5] = observable, $[6] = t2) : t2 = $[6], t2;
}
function _temp3() {
	return new Subject();
}
/**
* @hidden
* @beta */
function StudioProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(20), { children, config, basePath, onSchemeChange, scheme, unstable_history: history, unstable_noAuthBoundary: noAuthBoundary } = t0, t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = [], $[0] = t1) : t1 = $[0], (0, import_react.useEffect)(_temp$2, t1);
	let primaryProjectId = (Array.isArray(config) ? config[0] : config)?.projectId, t2;
	$[1] === Symbol.for("react.memo_cache_sentinel") ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LiveManifestRegisterProvider, {}), $[1] = t2) : t2 = $[1];
	let t3;
	$[2] === Symbol.for("react.memo_cache_sentinel") ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComlinkRouteHandler, {}), $[2] = t3) : t3 = $[2];
	let t4;
	$[3] === children ? t4 = $[4] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserApplicationCacheProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(LiveUserApplicationProvider, { children: [t2, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspaceLoaderBoundary, {
		LoadingComponent: LoadingBlock,
		ConfigErrorsComponent: ConfigErrorsScreen,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LocaleProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PackageVersionStatusProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResourceCacheProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioTelemetryProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(AppIdCacheProvider, { children: [t3, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioAnnouncementsProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalPerspectiveProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DocumentLimitUpsellProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AssetLimitUpsellProvider, { children }) }) }) })] }) }) }) }) })
	})] }) }), $[3] = children, $[4] = t4);
	let _children = t4, t5;
	$[5] !== _children || $[6] !== noAuthBoundary ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioThemeProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UserColorManagerProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ConfigErrorGate, { children: noAuthBoundary ? _children : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AuthBoundary, {
		LoadingComponent: LoadingBlock,
		AuthenticateComponent: AuthenticateScreen,
		NotAuthenticatedComponent: NotAuthenticatedScreen,
		children: _children
	}) }) }) }), $[5] = _children, $[6] = noAuthBoundary, $[7] = t5) : t5 = $[7];
	let t6;
	$[8] !== history || $[9] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VisibleWorkspacesProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActiveWorkspaceMatcher, {
		unstable_history: history,
		NotFoundComponent: NotFoundScreen,
		LoadingComponent: LoadingBlock,
		children: t5
	}) }), $[8] = history, $[9] = t5, $[10] = t6) : t6 = $[10];
	let t7;
	$[11] !== basePath || $[12] !== config || $[13] !== primaryProjectId || $[14] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToastProvider, {
		paddingY: 7,
		zOffset: Z_OFFSET.toast,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioErrorBoundary, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioRootErrorHandler, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WorkspacesProvider, {
			config,
			basePath,
			LoadingComponent: LoadingBlock,
			primaryProjectId,
			children: t6
		}) }) })
	}), $[11] = basePath, $[12] = config, $[13] = primaryProjectId, $[14] = t6, $[15] = t7) : t7 = $[15];
	let t8;
	return $[16] !== onSchemeChange || $[17] !== scheme || $[18] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DeferredTelemetryProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ColorSchemeProvider, {
		onSchemeChange,
		scheme,
		children: t7
	}) }), $[16] = onSchemeChange, $[17] = scheme, $[18] = t7, $[19] = t8) : t8 = $[19], t8;
}
function _temp$2() {
	errorReporter.initialize(), ensureRefractorLanguages();
}
var _refractorRegistered = !1;
function ensureRefractorLanguages() {
	_refractorRegistered || (_refractorRegistered = !0, import("./dist-Ck6DKIAq.js").then(({ registerLanguage }) => Promise.all([
		import("./bash-BtfEdoXY.js"),
		import("./javascript-BqnxOHtj.js"),
		import("./json-CEZB6de_.js"),
		import("./jsx-Bam61xpu.js"),
		import("./typescript-DatgoJxV.js"),
		import("./groq-Da5uLKQ6.js").then((m) => ({ default: m.refractorGroq }))
	]).then((languages) => languages.forEach((lang) => registerLanguage(lang.default))).catch((error) => console.warn("Failed to load syntax highlighting languages for code blocks", error))));
}
/**
* @hidden
* @beta */
function Studio(props) {
	let $ = (0, import_compiler_runtime.c)(11), { basePath, config, onSchemeChange, scheme, unstable_globalStyles: globalStyles, unstable_history, unstable_noAuthBoundary } = props, t0;
	$[0] === globalStyles ? t0 = $[1] : (t0 = globalStyles && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GlobalStyle, {}), $[0] = globalStyles, $[1] = t0);
	let t1;
	$[2] === Symbol.for("react.memo_cache_sentinel") ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StudioLayout, {}), $[2] = t1) : t1 = $[2];
	let t2;
	return $[3] !== basePath || $[4] !== config || $[5] !== onSchemeChange || $[6] !== scheme || $[7] !== t0 || $[8] !== unstable_history || $[9] !== unstable_noAuthBoundary ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(StudioProvider, {
		basePath,
		config,
		onSchemeChange,
		scheme,
		unstable_history,
		unstable_noAuthBoundary,
		children: [t0, t1]
	}), $[3] = basePath, $[4] = config, $[5] = onSchemeChange, $[6] = scheme, $[7] = t0, $[8] = unstable_history, $[9] = unstable_noAuthBoundary, $[10] = t2) : t2 = $[10], t2;
}
/** @internal */
function renderStudio(rootElement, config, options = {}) {
	if (!rootElement) throw Error("Missing root element to mount application into");
	let { reactStrictMode = !0, basePath } = typeof options == "boolean" ? { reactStrictMode: options } : options, root = (0, import_client.createRoot)(rootElement);
	return root.render(reactStrictMode ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.StrictMode, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Studio, {
		config,
		basePath,
		unstable_globalStyles: !0
	}) }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Studio, {
		config,
		basePath,
		unstable_globalStyles: !0
	})), () => root.unmount();
}
var ListItem = gt(Flex).withConfig({
	displayName: "ListItem",
	componentId: "sc-0xvjkt-0"
})``;
var ErrorMessageRoot = gt(Box).attrs({ padding: 4 }).withConfig({
	displayName: "ErrorMessageRoot",
	componentId: "sc-0xvjkt-1"
})``;
/**
* @internal
*/
function ErrorMessage(t0) {
	let $ = (0, import_compiler_runtime.c)(20), { error, message, path, stack } = t0, t1, t2;
	$[0] === error ? (t1 = $[1], t2 = $[2]) : (t1 = () => {
		console.error(error);
	}, t2 = [error], $[0] = error, $[1] = t1, $[2] = t2), (0, import_react.useEffect)(t1, t2);
	let last = path[path.length - 1], t3;
	$[3] === last.type ? t3 = $[4] : (t3 = startCase(last.type), $[3] = last.type, $[4] = t3);
	let t4;
	$[5] === t3 ? t4 = $[6] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		direction: "column",
		gap: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
			weight: "medium",
			size: 3,
			children: [t3, " Error"]
		})
	}), $[5] = t3, $[6] = t4);
	let t5;
	$[7] === message ? t5 = $[8] : (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		tone: "critical",
		overflow: "auto",
		padding: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Code, { children: message })
	}), $[7] = message, $[8] = t5);
	let t6;
	$[9] === path ? t6 = $[10] : (t6 = path.map(_temp$1), $[9] = path, $[10] = t6);
	let t7;
	$[11] === t6 ? t7 = $[12] : (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		as: "ul",
		direction: "column",
		gap: 2,
		children: t6
	}), $[11] = t6, $[12] = t7);
	let t8;
	$[13] === stack ? t8 = $[14] : (t8 = stack && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("details", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "summary",
		children: "Stack Trace"
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		overflow: "auto",
		marginTop: 4,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Code, { children: stack })
	})] }), $[13] = stack, $[14] = t8);
	let t9;
	return $[15] !== t4 || $[16] !== t5 || $[17] !== t7 || $[18] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ErrorMessageRoot, {
		forwardedAs: Flex,
		direction: "column",
		gap: 4,
		children: [
			t4,
			t5,
			t7,
			t8
		]
	}), $[15] = t4, $[16] = t5, $[17] = t7, $[18] = t8, $[19] = t9) : t9 = $[19], t9;
}
function _temp$1(t0, index) {
	let { name, type } = t0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ListItem, {
		forwardedAs: "li",
		gap: 2,
		align: "center",
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Code, { children: name }) }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			muted: !0,
			size: 1,
			children: type
		}) })]
	}, index);
}
/**
* @internal
* @hidden
*/
function IsLastPaneProvider(t0) {
	let $ = (0, import_compiler_runtime.c)(3), { children, isLastPane } = t0, t1;
	return $[0] !== children || $[1] !== isLastPane ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IsLastPaneContext.Provider, {
		value: isLastPane,
		children
	}), $[0] = children, $[1] = isLastPane, $[2] = t1) : t1 = $[2], t1;
}
function maybeSerialize(template) {
	return isBuilder(template) ? template.serialize() : template;
}
function isNonNullable$1(t) {
	return !!t;
}
/** @internal */
function prepareTemplates(schema, initialValueTemplates) {
	return validateTemplates(schema, initialValueTemplates.map(maybeSerialize));
}
/** @internal */
function defaultTemplateForType(schemaType) {
	return {
		id: schemaType.name,
		schemaType: schemaType.name,
		title: schemaType.title || schemaType.name,
		icon: schemaType.icon,
		value: schemaType.initialValue || { _type: schemaType.name }
	};
}
/** @internal */
function defaultTemplatesForSchema(schema) {
	return prepareTemplates(schema, schema.getTypeNames().filter((typeName) => !typeName.startsWith("sanity.")).map((typeName) => schema.get(typeName)).filter(isNonNullable$1).filter((schemaType) => schemaType.type?.name === "document").map((schemaType) => defaultTemplateForType(schemaType)));
}
/**
* @internal
* @deprecated – Will be removed in upcoming major version
*/
function _buildTints(bg, mid, fg) {
	return {
		50: mix$1(.1, mid, bg),
		100: mix$1(.2, mid, bg),
		200: mix$1(.4, mid, bg),
		300: mix$1(.6, mid, bg),
		400: mix$1(.8, mid, bg),
		500: mid,
		600: mix$1(.8, mid, fg),
		700: mix$1(.6, mid, fg),
		800: mix$1(.4, mid, fg),
		900: mix$1(.2, mid, fg),
		950: mix$1(.1, mid, fg)
	};
}
/**
* @internal
*/
function _toHex(color) {
	let { red, green, blue } = parseToRgb(color);
	return rgb(red, green, blue);
}
/**
* @internal
*/
function _isDark(bg, fg) {
	return getLuminance(bg) < getLuminance(fg);
}
/**
* Blend two colors using the "screen" blend mode
* @internal
*/
function _multiply(bg, fg) {
	return rgbToHex(multiply(parseColor(bg), parseColor(fg)));
}
/**
* Blend two colors using the "screen" blend mode
* @internal
*/
function _screen(bg, fg) {
	return rgbToHex(screen$1(parseColor(bg), parseColor(fg)));
}
var NEUTRAL_TONES = ["default", "transparent"];
/**
* @deprecated – Will be removed in upcoming major version
*/
function buildColor(legacyPalette, legacyTones) {
	return createColorTheme({
		base: ({ dark: navbar, name }) => {
			let stateTones = navbar ? legacyTones.state.navbar : legacyTones.state.default, dark = stateTones.dark, blend = navbar ? _screen : _multiply, tints = stateTones[name] || stateTones.default;
			if (name === "default") {
				let skeletonFrom = stateTones.default[100];
				return {
					fg: stateTones.fg,
					bg: stateTones.bg,
					border: stateTones.default[200],
					focusRing: legacyPalette.focus.base,
					shadow: {
						outline: rgba$1(stateTones.default[500], .4),
						umbra: rgba$1(dark ? legacyPalette.black : stateTones.default[500], .2),
						penumbra: rgba$1(dark ? legacyPalette.black : stateTones.default[500], .14),
						ambient: rgba$1(dark ? legacyPalette.black : stateTones.default[500], .12)
					},
					skeleton: {
						from: skeletonFrom,
						to: rgba$1(skeletonFrom, .5)
					}
				};
			}
			if (name === "transparent") {
				let bg = tints[50], skeletonFrom = blend(bg, tints[100]);
				return {
					fg: tints[900],
					bg,
					border: tints[300],
					focusRing: legacyPalette.focus.base,
					shadow: {
						outline: rgba$1(tints[500], dark ? .2 : .4),
						umbra: rgba$1(dark ? legacyPalette.black : tints[500], .2),
						penumbra: rgba$1(dark ? legacyPalette.black : tints[500], .14),
						ambient: rgba$1(dark ? legacyPalette.black : tints[500], .12)
					},
					skeleton: {
						from: skeletonFrom,
						to: rgba$1(skeletonFrom, .5)
					}
				};
			}
			let bg = tints[50], skeletonFrom = blend(bg, tints[100]);
			return {
				fg: tints[900],
				bg,
				border: tints[200],
				focusRing: tints[500],
				shadow: {
					outline: rgba$1(tints[500], dark ? .2 : .4),
					umbra: rgba$1(dark ? legacyPalette.black : tints[500], .2),
					penumbra: rgba$1(dark ? legacyPalette.black : tints[500], .14),
					ambient: rgba$1(dark ? legacyPalette.black : tints[500], .12)
				},
				skeleton: {
					from: skeletonFrom,
					to: rgba$1(skeletonFrom, .5)
				}
			};
		},
		solid: ({ base, dark: navbar, name, state, tone }) => {
			let buttonTones = navbar ? legacyTones.button.navbar : legacyTones.button.default, dark = buttonTones.dark, blend = dark ? _screen : _multiply, blendInvert = dark ? _multiply : _screen, defaultTints = buttonTones[name] || buttonTones.default, isNeutral = NEUTRAL_TONES.includes(name) && NEUTRAL_TONES.includes(tone), tints = buttonTones[tone === "default" ? name : tone] || defaultTints;
			if (state === "disabled") {
				tints = defaultTints;
				let bg = blend(base.bg, tints[200]), skeletonFrom = blendInvert(bg, tints[800]);
				return {
					bg,
					border: blend(base.bg, tints[200]),
					fg: blend(base.bg, buttonTones.bg),
					icon: blend(base.bg, buttonTones.bg),
					muted: { fg: blend(base.bg, tints[50]) },
					accent: { fg: blend(base.bg, tints[50]) },
					link: { fg: blend(base.bg, tints[50]) },
					code: {
						bg,
						fg: blend(base.bg, tints[50])
					},
					skeleton: {
						from: skeletonFrom,
						to: rgba$1(skeletonFrom, .5)
					}
				};
			}
			if (state === "hovered") {
				let bg = blend(base.bg, tints[600]), skeletonFrom = blendInvert(bg, tints[800]);
				return {
					bg,
					border: blend(base.bg, tints[600]),
					fg: blend(base.bg, buttonTones.bg),
					icon: blend(base.bg, buttonTones.bg),
					muted: { fg: blend(base.bg, tints[200]) },
					accent: { fg: blendInvert(bg, buttonTones.critical[300]) },
					link: { fg: blendInvert(bg, buttonTones.primary[200]) },
					code: {
						bg: blend(bg, tints[50]),
						fg: blend(base.bg, tints[200])
					},
					skeleton: {
						from: skeletonFrom,
						to: rgba$1(skeletonFrom, .5)
					}
				};
			}
			if (state === "pressed") {
				let bg = blend(base.bg, tints[800]), skeletonFrom = blendInvert(bg, tints[800]);
				return {
					bg,
					border: blend(base.bg, tints[800]),
					fg: blend(base.bg, buttonTones.bg),
					icon: blend(base.bg, buttonTones.bg),
					muted: { fg: blend(base.bg, tints[200]) },
					accent: { fg: blendInvert(bg, buttonTones.critical[300]) },
					link: { fg: blendInvert(bg, buttonTones.primary[200]) },
					code: {
						bg: blend(bg, tints[50]),
						fg: blend(base.bg, tints[200])
					},
					skeleton: {
						from: skeletonFrom,
						to: rgba$1(skeletonFrom, .5)
					}
				};
			}
			if (state === "selected") {
				isNeutral && (tints = buttonTones.primary);
				let bg = blend(base.bg, tints[800]), skeletonFrom = blendInvert(bg, tints[800]);
				return {
					bg,
					border: blend(base.bg, tints[800]),
					fg: blend(base.bg, buttonTones.bg),
					icon: blend(base.bg, buttonTones.bg),
					muted: { fg: blend(base.bg, tints[200]) },
					accent: { fg: blendInvert(bg, buttonTones.critical[300]) },
					link: { fg: blendInvert(bg, buttonTones.primary[200]) },
					code: {
						bg: blend(bg, tints[50]),
						fg: blend(base.bg, tints[200])
					},
					skeleton: {
						from: skeletonFrom,
						to: rgba$1(skeletonFrom, .5)
					}
				};
			}
			let bg = blend(base.bg, tints[500]), skeletonFrom = blendInvert(bg, tints[800]);
			return {
				bg,
				border: blend(base.bg, tints[500]),
				fg: blend(base.bg, buttonTones.bg),
				icon: blend(base.bg, buttonTones.bg),
				muted: { fg: blend(base.bg, tints[100]) },
				accent: { fg: blendInvert(bg, buttonTones.critical[200]) },
				link: { fg: blendInvert(bg, buttonTones.primary[100]) },
				code: {
					bg: blend(bg, tints[50]),
					fg: blend(base.bg, tints[100])
				},
				skeleton: {
					from: skeletonFrom,
					to: rgba$1(skeletonFrom, .5)
				}
			};
		},
		muted: ({ base, dark: navbar, name, state, tone }) => {
			let stateTones = navbar ? legacyTones.state.navbar : legacyTones.state.default, blend = stateTones.dark ? _screen : _multiply, defaultTints = stateTones[name] || stateTones.default, isNeutral = NEUTRAL_TONES.includes(name) && NEUTRAL_TONES.includes(tone), tints = stateTones[tone === "default" ? name : tone] || defaultTints;
			if (state === "disabled") {
				tints = defaultTints;
				let bg = base.bg, skeletonFrom = blend(bg, tints[100]);
				return {
					bg,
					border: blend(base.bg, tints[50]),
					fg: blend(base.bg, tints[200]),
					icon: blend(base.bg, tints[200]),
					muted: { fg: blend(bg, tints[100]) },
					accent: { fg: blend(bg, tints[100]) },
					link: { fg: blend(bg, tints[100]) },
					code: {
						bg,
						fg: blend(bg, tints[100])
					},
					skeleton: {
						from: skeletonFrom,
						to: rgba$1(skeletonFrom, .5)
					}
				};
			}
			if (state === "hovered") {
				let bg = blend(base.bg, tints[50]), skeletonFrom = blend(bg, tints[100]);
				return {
					bg,
					border: blend(bg, tints[100]),
					fg: blend(base.bg, tints[900]),
					icon: blend(base.bg, tints[900]),
					muted: { fg: blend(base.bg, tints[600]) },
					accent: { fg: blend(base.bg, stateTones.critical[500]) },
					link: { fg: blend(base.bg, stateTones.primary[600]) },
					code: {
						bg: blend(bg, tints[50]),
						fg: blend(base.bg, tints[600])
					},
					skeleton: {
						from: skeletonFrom,
						to: rgba$1(skeletonFrom, .5)
					}
				};
			}
			if (state === "pressed") {
				isNeutral && (tints = stateTones.primary);
				let bg = blend(base.bg, tints[100]), skeletonFrom = blend(bg, tints[100]);
				return {
					bg,
					border: blend(bg, tints[100]),
					fg: blend(base.bg, tints[800]),
					icon: blend(base.bg, tints[800]),
					muted: { fg: blend(base.bg, tints[600]) },
					accent: { fg: blend(bg, stateTones.critical[500]) },
					link: { fg: blend(bg, stateTones.primary[600]) },
					code: {
						bg: blend(bg, tints[50]),
						fg: blend(bg, tints[600])
					},
					skeleton: {
						from: skeletonFrom,
						to: rgba$1(skeletonFrom, .5)
					}
				};
			}
			if (state === "selected") {
				isNeutral && (tints = stateTones.primary);
				let bg = blend(base.bg, tints[100]), skeletonFrom = blend(bg, tints[100]);
				return {
					bg,
					border: blend(bg, tints[100]),
					fg: blend(bg, tints[800]),
					icon: blend(bg, tints[800]),
					muted: { fg: blend(bg, tints[600]) },
					accent: { fg: blend(bg, stateTones.critical[500]) },
					link: { fg: blend(bg, stateTones.primary[600]) },
					code: {
						bg: blend(bg, tints[50]),
						fg: blend(bg, tints[600])
					},
					skeleton: {
						from: skeletonFrom,
						to: rgba$1(skeletonFrom, .5)
					}
				};
			}
			let bg = base.bg, skeletonFrom = blend(base.bg, tints[100]);
			return {
				bg,
				border: blend(bg, tints[100]),
				fg: blend(bg, tints[700]),
				icon: blend(bg, tints[700]),
				muted: { fg: blend(bg, tints[600]) },
				accent: { fg: blend(bg, stateTones.critical[500]) },
				link: { fg: blend(bg, stateTones.primary[600]) },
				code: {
					bg: blend(bg, tints[50]),
					fg: blend(bg, tints[600])
				},
				skeleton: {
					from: skeletonFrom,
					to: rgba$1(skeletonFrom, .5)
				}
			};
		},
		button: ({ base, mode, muted, solid }) => mode === "bleed" ? {
			enabled: {
				...muted.enabled,
				border: muted.enabled.bg
			},
			hovered: {
				...muted.hovered,
				border: muted.hovered.bg
			},
			pressed: {
				...muted.pressed,
				border: muted.pressed.bg
			},
			selected: {
				...muted.selected,
				border: muted.selected.bg
			},
			disabled: {
				...muted.disabled,
				border: muted.disabled.bg
			}
		} : mode === "ghost" ? {
			...solid,
			enabled: {
				...muted.enabled,
				border: base.border
			},
			disabled: muted.disabled
		} : solid,
		card: ({ base, dark: navbar, muted, name, solid, state }) => {
			if (state === "hovered") return muted[name].hovered;
			if (state === "disabled") return muted[name].disabled;
			let isNeutral = NEUTRAL_TONES.includes(name), stateTones = navbar ? legacyTones.state.navbar : legacyTones.state.default, tints = stateTones[name] || stateTones.default, dark = stateTones.dark, blend = dark ? _screen : _multiply;
			if (state === "pressed") return isNeutral ? muted.primary.pressed : muted[name].pressed;
			if (state === "selected") return isNeutral ? solid.primary.enabled : solid[name].enabled;
			let bg = base.bg, skeletonFrom = blend(base.bg, tints[dark ? 900 : 100]);
			return {
				bg,
				fg: base.fg,
				icon: base.fg,
				border: base.border,
				muted: { fg: blend(base.bg, tints[dark ? 400 : 600]) },
				accent: { fg: blend(base.bg, stateTones.critical[dark ? 400 : 500]) },
				link: { fg: blend(base.bg, stateTones.primary[dark ? 400 : 600]) },
				code: {
					bg: blend(base.bg, tints[dark ? 950 : 50]),
					fg: tints[dark ? 400 : 600]
				},
				skeleton: {
					from: skeletonFrom,
					to: rgba$1(skeletonFrom, .5)
				}
			};
		},
		input: ({ base, dark: navbar, mode, state }) => {
			let stateTones = navbar ? legacyTones.state.navbar : legacyTones.state.default, blend = stateTones.dark ? _screen : _multiply;
			if (mode === "invalid") {
				let tints = stateTones.critical;
				return {
					bg: blend(base.bg, tints[50]),
					bg2: blend(base.bg, tints[50]),
					fg: blend(base.bg, tints[700]),
					border: blend(base.bg, tints[200]),
					placeholder: blend(base.bg, tints[400])
				};
			}
			return state === "hovered" ? {
				bg: base.bg,
				bg2: base.bg,
				fg: base.fg,
				border: blend(base.bg, hues.gray[300].hex),
				placeholder: blend(base.bg, hues.gray[400].hex)
			} : state === "disabled" ? {
				bg: blend(base.bg, hues.gray[50].hex),
				bg2: blend(base.bg, hues.gray[50].hex),
				fg: blend(base.bg, hues.gray[200].hex),
				border: blend(base.bg, hues.gray[100].hex),
				placeholder: blend(base.bg, hues.gray[100].hex)
			} : state === "readOnly" ? {
				bg: blend(base.bg, hues.gray[50].hex),
				bg2: blend(base.bg, hues.gray[50].hex),
				fg: blend(base.bg, hues.gray[800].hex),
				border: blend(base.bg, hues.gray[200].hex),
				placeholder: blend(base.bg, hues.gray[400].hex)
			} : {
				bg: base.bg,
				bg2: base.bg,
				fg: base.fg,
				border: base.border,
				placeholder: blend(base.bg, hues.gray[700].hex)
			};
		},
		selectable: ({ base, muted, tone, solid, state }) => state === "enabled" ? {
			...muted[tone].enabled,
			bg: base.bg
		} : state === "pressed" ? tone === "default" ? muted.primary.pressed : muted[tone].pressed : state === "selected" ? tone === "default" ? solid.primary.enabled : solid[tone].enabled : state === "disabled" ? {
			...muted[tone].disabled,
			bg: base.bg
		} : muted[tone][state],
		spot: ({ base, key }) => {
			let dark = _isDark(base.bg, base.fg);
			return (dark ? _screen : _multiply)(base.bg, hues[key][dark ? 400 : 500].hex);
		},
		syntax: ({ base, dark: navbar }) => {
			let stateTones = navbar ? legacyTones.state.navbar : legacyTones.state.default, blend = stateTones.dark ? _screen : _multiply;
			return {
				atrule: blend(base.bg, hues.purple[600].hex),
				attrName: blend(base.bg, stateTones.positive[600]),
				attrValue: blend(base.bg, stateTones.caution[600]),
				attribute: blend(base.bg, stateTones.caution[600]),
				boolean: blend(base.bg, hues.purple[600].hex),
				builtin: blend(base.bg, hues.purple[600].hex),
				cdata: blend(base.bg, stateTones.caution[600]),
				char: blend(base.bg, stateTones.caution[600]),
				class: blend(base.bg, hues.orange[600].hex),
				className: blend(base.bg, hues.cyan[600].hex),
				comment: blend(base.bg, stateTones.default[400]),
				constant: blend(base.bg, hues.purple[600].hex),
				deleted: blend(base.bg, stateTones.critical[600]),
				doctype: blend(base.bg, stateTones.default[400]),
				entity: blend(base.bg, stateTones.critical[600]),
				function: blend(base.bg, stateTones.positive[600]),
				hexcode: blend(base.bg, stateTones.primary[600]),
				id: blend(base.bg, hues.purple[600].hex),
				important: blend(base.bg, hues.purple[600].hex),
				inserted: blend(base.bg, stateTones.caution[600]),
				keyword: blend(base.bg, hues.magenta[600].hex),
				number: blend(base.bg, hues.purple[600].hex),
				operator: blend(base.bg, hues.magenta[600].hex),
				prolog: blend(base.bg, stateTones.default[400]),
				property: blend(base.bg, stateTones.primary[600]),
				pseudoClass: blend(base.bg, stateTones.caution[600]),
				pseudoElement: blend(base.bg, stateTones.caution[600]),
				punctuation: blend(base.bg, stateTones.default[600]),
				regex: blend(base.bg, stateTones.primary[600]),
				selector: blend(base.bg, stateTones.critical[600]),
				string: blend(base.bg, stateTones.caution[600]),
				symbol: blend(base.bg, hues.purple[600].hex),
				tag: blend(base.bg, stateTones.critical[600]),
				unit: blend(base.bg, hues.orange[600].hex),
				url: blend(base.bg, stateTones.critical[600]),
				variable: blend(base.bg, stateTones.critical[600])
			};
		}
	});
}
/**
* @deprecated – Will be removed in upcoming major version
*/
function buildFonts(cssCustomProperties) {
	return {
		...studioTheme.fonts,
		code: {
			...studioTheme.fonts.code,
			family: cssCustomProperties["--font-family-monospace"] || studioTheme.fonts.code.family
		},
		heading: {
			...studioTheme.fonts.heading,
			family: cssCustomProperties["--font-family-base"] || studioTheme.fonts.code.family
		},
		label: {
			...studioTheme.fonts.label,
			family: cssCustomProperties["--font-family-base"] || studioTheme.fonts.code.family
		},
		text: {
			...studioTheme.fonts.text,
			family: cssCustomProperties["--font-family-base"] || studioTheme.fonts.code.family
		}
	};
}
function buildLegacyPalette(cssCustomProperties) {
	return {
		black: _toHex(cssCustomProperties["--black"]),
		component: {
			bg: _toHex(cssCustomProperties["--component-bg"]),
			fg: _toHex(cssCustomProperties["--component-text-color"])
		},
		defaultButton: {
			default: { base: _toHex(cssCustomProperties["--default-button-color"]) },
			primary: { base: _toHex(cssCustomProperties["--default-button-primary-color"]) },
			success: { base: _toHex(cssCustomProperties["--default-button-success-color"]) },
			warning: { base: _toHex(cssCustomProperties["--default-button-warning-color"]) },
			danger: { base: _toHex(cssCustomProperties["--default-button-danger-color"]) }
		},
		focus: { base: _toHex(cssCustomProperties["--focus-color"]) },
		gray: { base: _toHex(cssCustomProperties["--gray-base"]) },
		mainNavigation: {
			bg: _toHex(cssCustomProperties["--main-navigation-color"]),
			fg: _toHex(cssCustomProperties["--main-navigation-color--inverted"])
		},
		state: {
			info: { fg: _toHex(cssCustomProperties["--state-info-color"]) },
			success: { fg: _toHex(cssCustomProperties["--state-success-color"]) },
			warning: { fg: _toHex(cssCustomProperties["--state-warning-color"]) },
			danger: { fg: _toHex(cssCustomProperties["--state-danger-color"]) }
		}
	};
}
/**
* @deprecated – Will be removed in upcoming major version
*/
function buildLegacyTones(legacyPalette) {
	return {
		state: {
			default: {
				bg: legacyPalette.component.bg,
				fg: legacyPalette.component.fg,
				dark: _isDark(legacyPalette.component.bg, legacyPalette.component.fg),
				default: _buildTints(legacyPalette.component.bg, legacyPalette.gray.base, legacyPalette.component.fg),
				transparent: _buildTints(legacyPalette.component.bg, legacyPalette.gray.base, legacyPalette.component.fg),
				primary: _buildTints(legacyPalette.component.bg, legacyPalette.state.info.fg, legacyPalette.component.fg),
				positive: _buildTints(legacyPalette.component.bg, legacyPalette.state.success.fg, legacyPalette.component.fg),
				caution: _buildTints(legacyPalette.component.bg, legacyPalette.state.warning.fg, legacyPalette.component.fg),
				critical: _buildTints(legacyPalette.component.bg, legacyPalette.state.danger.fg, legacyPalette.component.fg)
			},
			navbar: {
				bg: legacyPalette.mainNavigation.bg,
				fg: legacyPalette.mainNavigation.fg,
				dark: _isDark(legacyPalette.mainNavigation.bg, legacyPalette.mainNavigation.fg),
				default: _buildTints(legacyPalette.mainNavigation.bg, legacyPalette.gray.base, legacyPalette.mainNavigation.fg),
				transparent: _buildTints(legacyPalette.mainNavigation.bg, legacyPalette.gray.base, legacyPalette.mainNavigation.fg),
				primary: _buildTints(legacyPalette.mainNavigation.bg, legacyPalette.state.info.fg, legacyPalette.mainNavigation.fg),
				positive: _buildTints(legacyPalette.mainNavigation.bg, legacyPalette.state.success.fg, legacyPalette.mainNavigation.fg),
				caution: _buildTints(legacyPalette.mainNavigation.bg, legacyPalette.state.warning.fg, legacyPalette.mainNavigation.fg),
				critical: _buildTints(legacyPalette.mainNavigation.bg, legacyPalette.state.danger.fg, legacyPalette.mainNavigation.fg)
			}
		},
		button: {
			default: {
				bg: legacyPalette.component.bg,
				fg: legacyPalette.component.fg,
				dark: _isDark(legacyPalette.component.bg, legacyPalette.component.fg),
				default: _buildTints(legacyPalette.component.bg, legacyPalette.defaultButton.default.base, legacyPalette.component.fg),
				transparent: _buildTints(legacyPalette.component.bg, legacyPalette.defaultButton.default.base, legacyPalette.component.fg),
				primary: _buildTints(legacyPalette.component.bg, legacyPalette.defaultButton.primary.base, legacyPalette.component.fg),
				positive: _buildTints(legacyPalette.component.bg, legacyPalette.defaultButton.success.base, legacyPalette.component.fg),
				caution: _buildTints(legacyPalette.component.bg, legacyPalette.defaultButton.warning.base, legacyPalette.component.fg),
				critical: _buildTints(legacyPalette.component.bg, legacyPalette.defaultButton.danger.base, legacyPalette.component.fg)
			},
			navbar: {
				bg: legacyPalette.mainNavigation.bg,
				fg: legacyPalette.mainNavigation.fg,
				dark: _isDark(legacyPalette.mainNavigation.bg, legacyPalette.mainNavigation.fg),
				default: _buildTints(legacyPalette.mainNavigation.bg, legacyPalette.defaultButton.default.base, legacyPalette.mainNavigation.fg),
				transparent: _buildTints(legacyPalette.mainNavigation.bg, legacyPalette.defaultButton.default.base, legacyPalette.mainNavigation.fg),
				primary: _buildTints(legacyPalette.mainNavigation.bg, legacyPalette.defaultButton.primary.base, legacyPalette.mainNavigation.fg),
				positive: _buildTints(legacyPalette.mainNavigation.bg, legacyPalette.defaultButton.success.base, legacyPalette.mainNavigation.fg),
				caution: _buildTints(legacyPalette.mainNavigation.bg, legacyPalette.defaultButton.warning.base, legacyPalette.mainNavigation.fg),
				critical: _buildTints(legacyPalette.mainNavigation.bg, legacyPalette.defaultButton.danger.base, legacyPalette.mainNavigation.fg)
			}
		}
	};
}
/**
* Build a Sanity UI theme from legacy CSS properties.
*
* @example
* ```tsx
* import {buildLegacyTheme, defineConfig} from 'sanity'
*
* export default defineConfig({
*   // project configuration ...
*
*   // Customize theming
*   theme: buildLegacyTheme({
*     '--black': '#000',
*     '--gray': '#777',
*     '--focus-color': '#00f',
*   })
* })
* ```
*
* @param partialLegacyTheme - Properties to override the theme with. See {@link LegacyThemeProps}
* @public
* @deprecated – Will be removed in upcoming major version
*/
function buildLegacyTheme(partialLegacyTheme) {
	let legacyTheme = resolveLegacyTheme(partialLegacyTheme), legacyPalette = buildLegacyPalette(legacyTheme), color = buildColor(legacyPalette, buildLegacyTones(legacyPalette)), fonts = buildFonts(legacyTheme);
	return {
		__dark: _isDark(color.light.default.base.bg, color.light.default.base.fg),
		__legacy: !0,
		color,
		fonts
	};
}
var defaultCustomProperties = {
	"--font-family-monospace": studioTheme.fonts.code.family,
	"--font-family-base": studioTheme.fonts.text.family,
	"--black": black.hex,
	"--white": white.hex,
	"--brand-primary": blue[500].hex,
	"--component-bg": white.hex,
	"--component-text-color": black.hex,
	"--gray": gray[500].hex,
	"--gray-base": gray[500].hex,
	"--default-button-color": gray[500].hex,
	"--default-button-danger-color": red[500].hex,
	"--default-button-primary-color": blue[500].hex,
	"--default-button-success-color": green[500].hex,
	"--default-button-warning-color": yellow[500].hex,
	"--focus-color": blue[500].hex,
	"--screen-medium-break": "512px",
	"--screen-default-break": "640px",
	"--screen-large-break": "960px",
	"--screen-xlarge-break": "1600px",
	"--state-info-color": blue[500].hex,
	"--state-success-color": green[500].hex,
	"--state-warning-color": yellow[500].hex,
	"--state-danger-color": red[500].hex,
	"--main-navigation-color": black.hex,
	"--main-navigation-color--inverted": white.hex
};
function resolveLegacyTheme(legacyTheme) {
	let props = {
		...defaultCustomProperties,
		...legacyTheme
	};
	return props["--focus-color"] = legacyTheme["--focus-color"] || props["--brand-primary"], props["--default-button-primary-color"] = legacyTheme["--default-button-primary-color"] || props["--brand-primary"], props["--main-navigation-color"] = legacyTheme["--main-navigation-color"] || props["--black"], props["--main-navigation-color--inverted"] = legacyTheme["--main-navigation-color--inverted"] || props["--white"], props["--state-info-color"] = legacyTheme["--brand-primary"] || props["--brand-primary"], props;
}
var INTERNAL_KEYS = [
	"_id",
	"_type",
	"_createdAt",
	"_updatedAt",
	"_rev"
];
/**
* @beta
* Given a document and a reference ID, returns the paths in which the reference is used within the document.
*
* For example:
* ```ts
* const document = {
*   _type: 'test',
*   _id: 'test',
*   _createdAt: '2021-01-01',
*   _updatedAt: '2021-01-01',
*   _rev: 'test',
*   rootReference: {
*     _ref: 'foo-bar',
*     _type: 'reference',
*   },
* }
* const referencePaths = getReferencePaths(document, 'foo-bar')
* expect(referencePaths).toEqual([['rootReference']])
* ```
*/
function getReferencePaths(document, referenceToId) {
	let referencePaths = [];
	function traverse(obj, currentPath) {
		if (obj != null) {
			if (typeof obj == "object" && obj._ref === referenceToId) {
				referencePaths.push([...currentPath]);
				return;
			}
			if (Array.isArray(obj)) {
				obj.forEach((item, index) => {
					item && typeof item == "object" && item._key ? traverse(item, [...currentPath, { _key: item._key }]) : traverse(item, [...currentPath, index]);
				});
				return;
			}
			typeof obj == "object" && Object.keys(obj).forEach((key) => {
				INTERNAL_KEYS.includes(key) || traverse(obj[key], [...currentPath, key]);
			});
		}
	}
	return Object.keys(document).forEach((key) => {
		key.startsWith("_") || traverse(document[key], [key]);
	}), referencePaths;
}
/** @internal */
function isArray(value) {
	return Array.isArray(value);
}
/** @internal */
function isTruthy(value) {
	return !!value;
}
var uncaughtErrorHandler = () => [
	"window.onerror = function(m,u,l,c,e) {",
	"var p=window.location.port;",
	"var h=window.location.protocol+'//'+window.location.hostname+(p?':'+p:'');",
	"var r=document.getElementById('sanity');",
	"while(r.firstChild){r.removeChild(r.firstChild);}",
	"var s=document.createElement('style');",
	"s.appendChild(document.createTextNode('",
	"html,body,#sanityBody,#sanity,#sanityError{height:100%;}",
	"body{-webkit-font-smoothing:antialiased;margin:0;}",
	"#sanityError{position:fixed;top:0;left:0;width:100%;height:100%;overflow:auto;background-color:#fff;color:#121923;font-family:-apple-system, BlinkMacSystemFont, \\'Segoe UI\\', Roboto, \\'Helvetica Neue\\', Helvetica, Arial, system-ui, sans-serif;font-size:16px;line-height:21px;min-height:100%;}",
	"#sanityError>div{background-color:#fff;max-width:960px;margin:0 auto;padding:47px 32px 52px;}",
	"@media(min-width:512px){",
	"#sanityError>div{",
	"padding:47px 84px;",
	"}",
	"}",
	"#sanityError button{-webkit-font-smoothing:inherit;font:inherit;font-weight:500;background-color:#2276FC;color:#fff;padding:7px 12px;border-radius:3px;border:0;}",
	"#sanityError button:hover{background-color:#1E63D0;}",
	"#sanityError button:active{background-color:#1B50A5;}",
	"'));",
	"document.head.appendChild(s);",
	"var f=document.createElement('div');",
	"f.id='sanityError';",
	"f.innerHTML='",
	"<div>",
	"<h1 style=\"font-size:21px;line-height:27px;margin: 0 0 10px;\">Unhandled error</h1>",
	"<p style=\"color:#66758D;margin:10px 0 14px;\">Encountered an unhandled error in this Studio.</p>",
	"<button class=\"sanity-error-handler__reload-btn\" type=\"button\">Reload page</button>",
	"<pre style=\"background-color:#FDEBEA;color:#C3362C;font-size:13px;line-height:17px;padding:8px 12px;border-radius:3px;margin:32px 0 0;overflow:auto;\">",
	"<code style=\"font-family:-apple-system-ui-monospace, \\'SF Mono\\', Menlo, Monaco, Consolas, monospace;\">",
	"'+e.stack.replaceAll(h,'')+'",
	"</code>",
	"</pre>",
	"</div>",
	"';",
	"var b=f.querySelector('.sanity-error-handler__reload-btn');",
	"if(b){",
	"b.onclick=function() {",
	"window.location.reload();",
	"}",
	"};",
	"r.appendChild(f);",
	"};"
].join("");
/**
* Checks whether or not the given user has the role with the given ID
*
* @param user - The user to check (currently only the current user is supported)
*   If `null` is passed, this function always returns `false`.
* @param roleId - The ID of the role to check for
*
* @returns true if the user has the role, false otherwise
*
* @example
* Fetch the current user and check if they have the role "administrator":
* ```ts
* import {userHasRole, useCurrentUser} from 'sanity'
*
* export function MyComponent() {
*   const user = useCurrentUser()
*   const hasAdminRole = userHasRole(user, 'administrator')
*   return <div>Is administrator: {hasAdminRole ? 'Yes' : 'No'}</div>
* }
* ```
* @public
*/
function userHasRole(user, roleId) {
	return user !== null && user.roles.some((role) => role.name === roleId);
}
/**
* Determines whether the open document has a variant-scoped version for the selected variant in
* the current perspective's bundle.
*
*
* @internal
*/
function isDocumentInSelectedVariant({ selectedVariant, bundle, documentVersions }) {
	return !!getTargetDocument({
		bundle,
		variant: selectedVariant._id,
		documentVersions
	});
}
/**
* Builds the initial value for a creatable missing draft variant from its published sibling: the
* sibling's content re-identified as the draft target, with `_system` rewritten for the draft
* (`{variant, bundleId: 'drafts', scopeId, group}`) and the sibling's `_rev` dropped (the
* draft-to-be has no revision).
*
* The value serves double duty through the form's `initialValue`: it is displayed until the
* document exists, and it seeds the create issued by the first keystroke
* (`version.create({...initialDocument})` in the store's patch operation, which strips
* `_id`/`_rev`/`_updatedAt`). Writing `_system` client-side keeps operation routing
* (`getVariantVersionInfo`), the anti-corruption tripwires, and the `version.create` action
* mapping honest during the optimistic window before the server-stamped snapshot arrives.
*
* @internal
* @beta
*/
function buildCreatableVariantInitialValue(options) {
	let { publishedSibling, target, variantId } = options, { _rev, ...content } = publishedSibling;
	return {
		...content,
		_id: target.id,
		_system: {
			group: publishedSibling._system?.group ?? {
				_ref: getPublishedId(target.id),
				_weak: !0
			},
			variant: {
				_ref: variantId,
				_weak: !0
			},
			bundleId: "drafts",
			scopeId: target.scopeId
		}
	};
}
/**
* Resolves the {@link InitialValueState} for the document form: for a creatable missing draft
* variant (see `getCreatableVariantTarget`), the published sibling is observed live and served as
* the initial value via {@link buildCreatableVariantInitialValue}; every other state passes
* `fallback` (the template-resolved initial value) through untouched.
*
* Loading is honest: the state stays `loading` until the sibling document arrives, so the form
* never flashes an empty document where the published variant content belongs. If the sibling is
* concurrently unpublished, the target state flips away from creatable and `fallback` applies
* again.
*
* @internal
* @beta
*/
function useCreatableVariantInitialValue(targetDocumentState, fallback) {
	let $ = (0, import_compiler_runtime.c)(16), documentPreviewStore = useDocumentPreviewStore(), t0;
	$[0] === targetDocumentState ? t0 = $[1] : (t0 = getCreatableVariantTarget(targetDocumentState), $[0] = targetDocumentState, $[1] = t0);
	let creatableTarget = t0, isVariantMissing = targetDocumentState.status === "variant-missing", variantId = isVariantMissing ? targetDocumentState.variant._id : void 0, publishedSiblingId = isVariantMissing ? targetDocumentState.publishedSibling?._id : void 0, targetId = creatableTarget?.id, targetScopeId = creatableTarget?.scopeId, t1;
	bb0: {
		if (!targetId || !publishedSiblingId) {
			let t2;
			$[2] === Symbol.for("react.memo_cache_sentinel") ? (t2 = of(null), $[2] = t2) : t2 = $[2], t1 = t2;
			break bb0;
		}
		let t2;
		$[3] !== documentPreviewStore || $[4] !== publishedSiblingId ? (t2 = documentPreviewStore.unstable_observeDocument(publishedSiblingId).pipe(map(_temp)), $[3] = documentPreviewStore, $[4] = publishedSiblingId, $[5] = t2) : t2 = $[5], t1 = t2;
	}
	let publishedSibling = useSyncObservable(t1, null), t2;
	bb1: {
		if (!targetId || !targetScopeId || !variantId) {
			t2 = fallback;
			break bb1;
		}
		if (!publishedSibling) {
			let t3;
			$[6] !== fallback.value._type || $[7] !== targetId ? (t3 = {
				loading: !0,
				error: null,
				value: {
					_id: targetId,
					_type: fallback.value._type
				}
			}, $[6] = fallback.value._type, $[7] = targetId, $[8] = t3) : t3 = $[8], t2 = t3;
			break bb1;
		}
		let t3;
		$[9] !== publishedSibling || $[10] !== targetId || $[11] !== targetScopeId || $[12] !== variantId ? (t3 = buildCreatableVariantInitialValue({
			publishedSibling,
			target: {
				id: targetId,
				scopeId: targetScopeId
			},
			variantId
		}), $[9] = publishedSibling, $[10] = targetId, $[11] = targetScopeId, $[12] = variantId, $[13] = t3) : t3 = $[13];
		let t4;
		$[14] === t3 ? t4 = $[15] : (t4 = {
			loading: !1,
			error: null,
			value: t3
		}, $[14] = t3, $[15] = t4), t2 = t4;
	}
	return t2;
}
function _temp(doc) {
	return doc ?? null;
}
//#endregion
export { createSourceFromConfig as $, useIsReleaseActive as $t, SearchDialog as A, mergeParseErrors as At, StudioProvider as B, useCopyErrorDetails as Bt, IsLastPaneProvider as C, isPerspectiveRaw as Ct, PresenceScope as D, isValidAnnouncementAudience as Dt, LocaleProviderBase as E, isTruthy as Et, StudioFeedbackProvider as F, serializeError as Ft, VersionChip as G, useDocumentVersionInfo as Gt, TelephoneInput as H, useDocumentLimitsUpsellContext as Ht, StudioLayout as I, uncaughtErrorHandler as It, WorkspacesProvider as J, useFeedback as Jt, VisibleWorkspacesProvider as K, useEvents as Kt, StudioLayoutComponent as L, unstable_useObserveDocument as Lt, StudioAnnouncementsCard as M, remoteSnapshots as Mt, StudioAnnouncementsDialog as N, renderStudio as Nt, RequestErrorDialog as O, isValidAnnouncementRole as Ot, StudioFeedbackDialog as P, resolveConfig as Pt, createMockAuthStore as Q, useInStudioFeedback as Qt, StudioLogo as R, useAgentVersionDisplay as Rt, IntentButton as S, generateHelpUrl as Sn, isDocumentLimitError as St, LocaleProvider as T, isSanityDefinedAction as Tt, TemplatePreview as U, useDocumentOperationEvent as Ut, StudioToolMenu as V, useCreatableVariantInitialValue as Vt, UserColorManagerProvider as W, useDocumentType as Wt, buildLegacyTheme as X, useGlobalCopyPasteElementHandler as Xt, ZIndexProvider as Y, useFormattedDuration as Yt, createConfig as Z, useGlobalPresence as Zt, ErrorMessage as _, useUnstableObserveDocument as _n, getReferencePaths as _t, CapabilityGate as a, useProjectDatasets as an, defaultRenderInlineBlock as at, FormContainer as b, useWorkspaces as bn, isArray as bt, ChangesError as c, useReviewChanges as cn, defaultRenderPreview as ct, ConfigErrorGate as d, useTelemetryConsent as dn, defaultTheme as dt, useManageFavorite as en, createUserColorManager as et, CorsOriginErrorScreen as f, useTemplatePermissions as fn, defineConfig as ft, ErrorActions as g, useTimelineStore as gn, getProviderTitle as gt, DocumentGroupInventoryAction as h, useTimelineSelector as hn, getConfigContextFromSource as ht, BetaBadge as i, useProject as in, defaultRenderField as it, Studio as j, prepareTemplates as jt, SearchButton as k, matchWorkspace as kt, Chip as l, useStudioFeedbackTags as ln, defaultTemplateForType as lt, DocumentGroupInventory as m, useTimeAgo as mn, generateStudioManifest as mt, ArrayOfObjectsInputMember as n, useOnScroll as nn, defaultRenderAnnotation as nt, ChangeConnectorRoot as o, useReleasesIds as on, defaultRenderInput as ot, Delay as p, useTemplatePermissionsFromHookFactory as pn, evaluateWorkspaceHidden as pt, WithReferringDocuments as q, useEventsStore as qt, ArrayOfObjectsInputMembers as r, usePausedScheduledDraft as rn, defaultRenderBlock as rt, ChangeFieldWrapper as s, useRetryCountdown as sn, defaultRenderItem as st, ActiveWorkspaceMatcher as t, useNumberFormat as tn, createWorkspaceFromConfig as tt, CommentsIntentProvider as u, useSyncState as un, defaultTemplatesForSchema as ut, EventsProvider as v, useVariantDocumentOperations as vn, getTemplatePermissions as vt, LiveManifestRegisterProvider as w, isPerspectiveWriteable as wt, ImperativeToast as x, userHasRole as xn, isDocumentInSelectedVariant as xt, FeedbackDialog as y, useVisibleWorkspaces as yn, globalScope as yt, StudioNavbar as z, useConfigContextFromSource as zt };
